# 引継書 — Zenith CAD Kernel

**最終更新**: 2026年8月20日
**ブランチ**: `kernel-accuracy-hardening`（`main` から分岐、未マージ）

この文書は「次に何から手を付けるか」を1枚で分かるようにしたものです。
仕様の詳細は [`KERNEL_SPECS.md`](KERNEL_SPECS.md)、検証の詳細は
[`FREECAD_VALIDATION_REPORT.md`](FREECAD_VALIDATION_REPORT.md) にあります。

---

## 1. まず動かして状態を確認する

```bash
cargo test --release --workspace --exclude zenith_py
```

`zenith_py` は `pyo3` が PATH から Python を探します。見つからない環境では明示します。

```bash
PYO3_PYTHON="C:/Users/hinat/AppData/Local/Programs/Python/Python311/python.exe" cargo test --release --workspace
```

現在の状態（すべて実測）:

| 指標 | 値 |
| :--- | :--- |
| テストバイナリ | 41 すべてグリーン（276テスト） |
| コンパイラ警告 | 0（examples に3件、既存） |
| ビルダー監査 | 21/21 クリーン |
| ブーリアン対応 | 45ケース中28成功、**誤答ゼロ** |
| FreeCAD 相互検証 | 15/15 一致 |
| ショーケース | 16/16 が OpenCASCADE で valid closed solid |
| 他カーネルの解析曲面の読み込み | 7/7 が体積・面積とも OpenCASCADE と一致 |
| 他カーネルのトリム B-spline | 面積 314.1512（真値 314.1593）、体積の相対誤差 2.0e-5 |
| p-curve の忠実度 | 構成に使っていない標本数で測っても 3.5e-11 以下 |

---

## 2. この作業の考え方（重要）

このカーネルで見つかった欠陥は、ほぼすべて**内部からは正常に見える**種類でした。

- 閉多様体だが答えが違うブーリアン
- 面積がちょうど2倍になる断面
- いくら細分しても収束しない積分
- STEP に書いた瞬間に他カーネルで壊れる立体

**主張ではなく測定で判断してください。** そのための常設ツールが揃っています。

```bash
cargo run --release -p zenith_algo --example builder_audit        # 全ビルダーの健全性と解析解との一致
cargo run --release -p zenith_algo --example boolean_envelope     # ブーリアンの実対応範囲（45ケース表）
cargo run --release -p zenith_algo --example step_import_audit    # STEP 往復と他カーネルファイルの読み込み
cargo run --release -p zenith_algo --example mass_convergence     # 質量積分の収束
cargo run --release -p zenith_algo --example slice_probe          # 断面積と解析解の差
cargo run --release -p zenith_algo --example pcurve_fidelity_probe # p-curve が本当に辺の上にあるか
cargo run --release -p zenith_algo --example foreign_reexport      # 他カーネルのファイルを読んで書き戻す一周
```

外部カーネルとの突き合わせ（不一致で非ゼロ終了するのでリリースゲートに使えます）:

```bash
cargo run --release -p zenith_algo --example export_validation_suite
& "C:\Program Files\FreeCAD 1.1\bin\python.exe" tools/freecad_cross_validate.py

cargo run --release -p zenith_algo --example export_showcase
& "C:\Program Files\FreeCAD 1.1\bin\python.exe" tools/verify_showcase.py
```

ブーリアンには検証ゲート（`BooleanResultVerifier`）が入っています。
①全シェルの閉性 ②演算が含意する体積境界 ③384点の内外一貫性 を確認し、
通らなければ**もっともらしいソリッドではなくエラー**を返します。
踏み込んだ改造を安全に試せるのはこのゲートのおかげです。壊さないでください。

---

## 3. 次にやること（優先順）

### 3-1. 済: インポーターの解析曲面（2026年8月20日）

`CONICAL_SURFACE` / `SPHERICAL_SURFACE` / `TOROIDAL_SURFACE` を、円柱と同じく
面の境界から実寸に合わせて張るようにしました。付随して3つの欠陥が出ました。

| 直したもの | 症状 |
| :--- | :--- |
| `FACE_BOUND` の向きフラグを落としていた | 面の境界が逆回りになり、辺が両隣から同じ向きに使われて弾かれる |
| シェル検証が辺を端点だけで対にしていた | トーラスの2本の縫い目は端点が同じなので、4つの使用が互いに対と判定される |
| 縫い目だけのループを p-curve の面積で判定していた | 縫い目上の点は UV の両端に写るので、面がちょうど半分になる |

`VERTEX_LOOP`（球を1面で書いたときの極）にも対応しました。辺の無いループは
書き落としではなく「トリムするものが無い」という意味なので、縫い目だけの
ループと同じ扱いにしています。

OpenCASCADE が書いた参照ファイルに対する実測（すべて OCC の値と一致）:

| ファイル | 体積 | 以前 |
| :--- | ---: | :--- |
| 円錐 r10/r4 h20 | 3267.2564 | 読めず |
| 頂点まで届く円錐 | 2094.3951 | 読めず |
| 球 r10（VERTEX_LOOP） | 4188.7902 | 読めず |
| 半球（縫い目を往復する境界） | 2094.3951 | 読めず |
| トーラス R12 r4（1面表現） | 3789.9281 | 読めず |
| トーラス90度区分 | 947.4820 | 読めず |

参照ファイルは `crates/zenith_algo/tests/fixtures/` に置いてあり、
`foreign_analytic_surface_test` が体積と面積の両方を見ています。
期待値はこのリポジトリが決めた数ではなく OpenCASCADE 自身が報告した数なので、
ここが落ちたら他カーネルとの不一致です。再生成は次のとおり。

```bash
& "C:\Program Files\FreeCAD 1.1in\python.exe" tools/occ_reference_export.py
cargo run --release -p zenith_algo --example step_import_audit
```

### 3-2. 済: p-curve と最近傍点探索（2026年8月20日）

**検査が効いていませんでした。** NURBS 面の p-curve は辺を8等分して作られ、
シェル検証も8等分で測っていました。同じ点なので、p-curve が構成上そこを
通ることしか確かめていませんでした。順番に3つ直しています。

| 直したもの | 症状 |
| :--- | :--- |
| 最近傍点探索が悪化を受け入れていた | 退化行（球の極）でヤコビアンが特異になると打ち切り、極そのものを答えとして返す。半径10の球で **0.446** |
| 最近傍点探索が継ぎ目を越えられない | 端を越える答えを clamp で潰していた。継ぎ目そのものを最近傍として返す。**1.83** |
| 継ぎ目上の p-curve が別名を拾う | 継ぎ目の点は領域の両端どちらでも呼べる。片方だけ遠い名前を拾うと、隣の標本との間で p-curve が球を一周する。**20.0**（直径） |
| アフィンな面を折れ線で近似していた | B-spline 化された平面キャップは1次4隅パッチで写像がアフィン。厳密に作れるのに八角形になっていた。**0.889**、面積 282.47（正しくは 314.16） |
| トリムループの折れが分割数に紐づいていた | 面積は必ず内側に削れ、分割数に比例してしか減らない。512分割でも円形キャップが **1.1e-3** 足りない |

実測（`pcurve_fidelity_probe`。8は構成に使った数、それ以外は使っていない数）:

```
occ_reference_sphere_capped   face 0    20.0   ->  3.5e-11
occ_reference_cylinder_nurbs  face 1   0.889   ->  1.4e-14
```

検証の標本数は 8 から **37**（8と互いに素）に変えました。共有するのは両端だけです。
これを先に入れると、いま正しく読めているファイルが軒並み弾かれます。**順序が要りました。**

上げた結果、**自前の立体にも実在の誤差が見つかりました**。斜めに切ったシリンダの
p-curve が辺から 1.8e-2 外れていました。いまは全て通ります。

派生して直ったもの:

| | 以前 | 現在 | 真値 |
| :--- | ---: | ---: | ---: |
| NURBS円柱のキャップ面積 | 282.47 | 314.1512 | 314.1593 |
| 同 体積 | 12144.19 | 12566.6236 | 12566.3706 |
| 他カーネル7形状の読み→書き→読み | 1件が読めず | すべて 1e-13 | — |

固定してあるもの: `pcurve_fidelity_test`（外部2形状＋自前の曲面プリミティブを
6通りの標本数で）、`foreign_analytic_surface_test` の往復と面積の検査。

**測り方の注意（このセッションで2回踏みました）**
1. 境界が曲面に乗っているかを 80×80 の格子で測って 0.41 を得ましたが、
   それは格子間隔そのものでした（半径10の全周80分割＝0.39）。
2. 最近傍点探索そのものが同じ間違いをしていました。16×16 の粗サンプリングの
   まま返ると、格子間隔（半径10で 1.8）がそのまま答えとして残ります。
   **粗い当たりの周りを詰めてから返すこと。**

### 3-2b. 済/一部残: 読み込んだ形状を書き出し直す

他カーネル → 自前リーダー → 自前ライター → 他カーネル、の一周です。

```bash
cargo run --release -p zenith_algo --example foreign_reexport
& "C:\Program Files\FreeCAD 1.1\bin\python.exe" tools/verify_reexport.py
```

自前で読み直す分は7形状すべて 1e-13 になりました（3-2 で解決）。

**OpenCASCADE 側との比較には注意が要ります。** OCC は有理B-spline を解析曲面と
同じようには測りません。自分の円柱を `toNurbs` してから測ると 12674.63
（解析値 12566.37 に対して +0.86%）です。比較相手は「OCC 自身の NURBS 化」です。
7件中6件はそれと同等かそれ以上で、頂点まで届く円錐と半球は明確に上です。

**残っているのは球1件だけです**（+0.93%、OCC 自身の NURBS 化は -0.04%）。
インポーターが全周を1枚のパッチで作るためで、自前では厳密ですが
OpenCASCADE は巻き付き1枚面を苦手とします。**自前のビルダーは同じ理由で球を
8枚に分割しています**（`KERNEL_SPECS.md` の球の行に経緯）。直すなら、
書き出し時に巻き付き面を分ける（面のトリムループも割る必要あり）か、
読み込み時に分けて作る。

### 3-3. ブーリアン: 解析曲面の交差 ★続き

**済（2026年8月20日）: 平面 × 円錐。** 45ケース中 26 → **28** に増え、誤答はゼロのままです。

円錐の側面は円柱の側面と同じ種類の面（直線を軸まわりに掃いたもの）ですが、
2箇所が「半径は軸に沿って変わらない」と決めつけていました。

| 直したもの | 症状 |
| :--- | :--- |
| パッチ認識が制御網を読み、母線が全て同じベクトルであることを要求 | 円柱では真、円錐では偽。円錐は一つも認識されず、平面と交差できなかった |
| 母線の両端の対応づけを「真上にあるか」で判定 | 円錐の母線は内側に傾くので、両端は軸からの距離が違う。必ず外れる |
| 円柱向けの近道（`cylinder_boolean`）が「最大半径に16点以上乗っていれば円柱」 | 円錐の底円はちょうど最大半径に乗る。円錐が円柱として作り直され、差も積も同じ 3141.593 を返していた |

3つ目は**私の変更より前からある欠陥**でした。ゲートが弾いていたので誤答は
出ていませんが、正しい経路にも進めませんでした。**円錐の幾何だけ直しても
何も変わらなかったのはこれが理由です。**

認識は制御網ではなく**両端の断面**から読むようにしました。それぞれを円として
当てはめ、中心を結ぶ線を軸とし、そのうえで**途中の断面が両端の含意する位置に
乗っているか**を確かめます。最後の確認が、円錐と「たまたま両端が円である
線織面」を分けます。円柱は両半径が等しい場合として同じ経路に乗ります。

promote するのは**軸に垂直な平面だけ**です。断面が円になり、それはパッチの
パラメータ線なので、制御網から厳密に取り出せて端から端まで届きます。
斜めの平面は双曲線・放物線を切り、パラメータ線を横切るので未対応のままです。

実測（いずれも解析解と 1e-6 以内）:

| | 測定値 | 解析解 |
| :--- | ---: | ---: |
| 円錐 ∪ 箱 | 10293.363 | 10293.3627 |
| 円錐 − 箱 | 2293.363 | 2293.3626 |
| 円錐 ∩ 箱 | 973.894 | 973.8937 |

**残り17件の内訳**

| 未対応 | 件数 | 必要なもの |
| :--- | ---: | :--- |
| トーラス × ボックス | 3 | トーラスパッチの認識と、次数2の v 等パラメータ線の取り出し |
| ボックス × 球 | 3 | 球パッチの認識。ただしこのケースは箱の4面が球に**接している**退化配置 |
| 円柱 × 円柱、球 × 球 | 6 | NURBS × NURBS の真の交線（SSI） |
| 円柱が面に接する配置 | 3 | 退化（接線）配置の扱い |
| 交差が空になるケース | 2 | 仕様の問題。空を返すのが正しいかを決める |

**済（同日）: 断面の取り出しと分割を、回転面一般に広げました。**

形を見分けるのをやめ、パッチに2つ問うだけにしました。①平面の法線方向の距離が
片方のパラメータだけで決まるか（決まらなければ、その向きは回転軸ではない）
②その距離が0になるパラメータはあるか（単調なので二分法。単調でなければ交わりが
2本以上あり得るので promote しない）。そのうえで**取り出した線が本当に平面の上に
あるか測って**、外れていれば拒否します。

| 直したもの | 症状 |
| :--- | :--- |
| 等パラメータ線の取り出しが `degree_v == 1` 専用 | トーラスは2次。`NurbsSurface3::iso_curve_u` / `iso_curve_v` を追加（次数一般） |
| 断面方向を v と決め打ち | **軸方向を担うのは、円柱では v、トーラスでは u**。両方向を試すようにした。最初これで何も見つからなかった |
| 分割がピースの側辺を**直線**で作る | 母線なら正しいが、トーラスの側辺は子午線の弧。元の側辺を交点で**切る**ようにした |

実測: トーラス16枚に対し断面 0→**8本**（半径 12±√12 で厳密）、分割 0→**8件**。
分割後の全ピースの境界点がトーラス上 1e-6 以内。円柱・円錐も同じ経路で通ることを
`section_split_test` で固定しています。

**済（同日）: 円環キャップと、分割の巡回の継承。**

| 直したもの | 症状 |
| :--- | :--- |
| キャップがループ1本につき円板1枚 | 平面がトーラスを切ると2本のループが**円環**を囲む。別々の円板で塞ぐと穴まで塞がり、内側ループの辺が同じ向きに2度使われる |
| 分割が辺の**固有方向**でワイヤを組む | 元のワイヤの並びと向きを継いでいない。辺の保存向き次第で隣り合うピースが逆巻きになる |

同一平面のループを入れ子で解き、穴は外周と逆回りに揃えてから面を作ります
（面ごと裏返しても両方が同時に反転するので、後からでは直せません）。
分割は元の巡回から切り出す形に組み直しました。

**スラブがトーラスを横方向まで覆う配置**では、差と積が通ります。

| | 測定値 | 解析解 |
| :--- | ---: | ---: |
| トーラス − スラブ | 740.9351 | 740.9351 |
| トーラス ∩ スラブ | 3048.9929 | 3048.9929 |

縫合は unmatched 8 / same-dir 8 → **0 / 0**。`boolean_torus_test` で固定しています。

**envelope は 28/45 のままです。**

1. **和はまだ面選択を誤ります**（スラブの場合 71259.06、正しくは 72740.94）。
   断面や分割ではなく選択の問題です。
2. **envelope の torus × box は、そもそも断面だけでは足りません。**
   箱は x, y とも ±10 で、トーラスは ρ が最大16なので**横にはみ出します**。
   箱の側面もトーラスを切り、その切り口はパッチのパラメータ線ではありません。

**私が踏んだ間違い（記録）**: 2 に気付かず、箱の場合の 740.9351 / 3048.9929 を
「解析解と一致」と判断し、ゲートの不合格を誤検知だと思いました。**ゲートが正しく、
私の解析解のほうが間違っていました**（箱を無限スラブと取り違えていた）。
内外判定を Halton 点で突き合わせて、外れる35点が全て「箱の外だがトーラスの中」の
領域にあると分かって初めて気付きました。

**次にやること**

- 和の面選択。差と積が正しいのに和だけ外すので、選択規則の問題として切り分けられます。
- 箱の側面のような**パラメータ線でない切り口**。これは本来の SSI で、
  `zenith_geom/src/ssi.rs` の細分割＋Newton marching が出番です。
  ただし分割段は「交線がパッチの端から端まで届く1本」であることを前提にしているので、
  そこも一緒に広げる必要があります。

## 4. 踏んだ落とし穴（繰り返さないために）

**ブーリアンの回転ボックス対応は、部品を1つずつ入れると必ず別のケースを壊しました。**
4回取り下げています。必要だったのは以下の4つ＋同一平面処理で、**同時に入れないと効きません**。

1. 連結分割（切り込みは複数の交線が内部の角で繋がったもの）
2. 境界沿い交線の除外（それは接触の記録であって切り込みではない）
3. 同一境界辺への着地の許可（両端間の経路はその辺の一区間）
4. 頂点の刻み込み（**2度「無意味」と判断して捨てた**。分割が正しくなって初めて効いた）

教訓として、**単独で効果が測れない部品でも、他が揃うまで判断を保留する**ほうが良い場合があります。
ただし取り下げの判断自体は正しく、そのたびに障壁の位置が1段深く特定できました。

**ゲートが落としたとき、疑うのはまず自分の期待値です。** トーラス × ボックスで
差と積が「解析解と一致」したのにゲートが弾き、誤検知だと思いました。実際は箱が
トーラスの横幅を覆っておらず、**私の解析解のほうが間違っていました**。
ゲートは 384 点の内外一貫性で見ているので、体積が合っていても形が違えば落ちます。
体積の一致は形の一致より弱い条件です。

**「いくつか乗っている」は「そういう形である」ではありません。** 円柱の認識は
「最大半径に16点以上乗っていれば円柱」でした。円錐の底円はちょうど最大半径に
乗るので、円錐が円柱として通ります。**全ての標本がそうであること**まで見ないと、
条件を満たす部分集合があるだけで通ってしまいます。

**探索そのものが粗いと、粗さがそのまま答えとして残ります。** 最近傍点探索は
16×16 の粗サンプリングから始めます。ニュートン法が動けなかったとき、そのまま
返すと格子間隔（半径10の球で 1.8）が答えになります。しかもそれは「もっともらしい
小さな値」なので、検査を通ってしまいます。**返す前に当たりの周りを詰めること。**

**「面積・体積がちょうど半分／2倍」は、たいてい継ぎ目の取り違えです。**
トーラスを1面で書いたファイルは、体積がちょうど半分になりました。縫い目上の点は
UV 領域の両端どちらにも写るので、p-curve から囲まれた領域を読むと、投影がどちらを
選ぶかで答えが割れます。**p-curve では原理的に決まらない**ので、位相で見ます
（ループ内のどの辺も2度現れるなら、その面は曲面全体）。断面積がちょうど2倍に
なったときと同じ性質の間違いです。

**プローブの測り方を間違えると誤診します。** 境界までの距離をサンプル点への距離で測っていたため、
「交線が面を横断していない」と誤読し、存在しない問題を追いかけました。線分距離で測り直すと
gap は全て 0 でした。

---

## 5. リポジトリの見取り図

| 場所 | 中身 |
| :--- | :--- |
| `crates/zenith_math` | 点・ベクトル・変換・ロバスト述語 |
| `crates/zenith_geom` | NURBS 曲線・曲面、Coons/Gordon、曲率、SSI |
| `crates/zenith_topo` | Vertex/Edge/Wire/Face/Shell/Solid、シェル検証 |
| `crates/zenith_algo` | ブーリアン・押出/回転/ロフト/掃引・穴/フィレット・断面・質量特性 |
| `crates/zenith_tess` | テッセレーション（積分領域はノット区間に整合） |
| `crates/zenith_io` | STEP 読み書き、STL/OBJ/glTF/IGES |
| `crates/zenith_py` | PyO3 バインディング（45関数） |
| `crates/zenith_algo/examples/` | **測定・診断ツール**（19個） |
| `tools/*.py` | FreeCAD ヘッドレス検証（`occ_*` は診断用） |
| `target/showcase/` | 代表16形状の STEP（`export_showcase` で再生成） |

Blender アドオン本体（`__init__.py`・オペレータ・パネル）は未着手です。
`blender_addon/` にはビルド済みの `zenith_cad.pyd` のみが入っています。

---

## 6. 未コミットのもの

リポジトリ直下に生成物の `.step` が多数、`target_*` ディレクトリが多数あります。
いずれも未追跡のまま残してあります（`.gitignore` の見直し余地あり）。
`reference/` は移植元の Seamless CAD と OCCT で、合計 420MB。gitignore 済みです。
