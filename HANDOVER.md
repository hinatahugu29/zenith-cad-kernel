# 引継書 — Zenith CAD Kernel

**最終更新**: 2026年9月5日
**現在の作業ブランチ**: `codex/tess-edge-contract`（2026/09/05 の統合直後は `origin/main` と**同じ位置**です。**この数はすぐ古くなります**——下の `git rev-list --count origin/main..HEAD` を打ってください）
**実装・検証の基準**: `origin/main` の `b5f848b`（**2026/09/05 に 99 コミットを早送りで統合しました**。衝突 0、`f3815de..b5f848b`。前回の基準は `f3815de`）
**ライセンス**: **MPL-2.0**（[`LICENSE`](LICENSE)。2026/08/30 に定めました）

この文書は「次に何から手を付けるか」を1枚で分かるようにしたものです。
仕様の詳細は [`KERNEL_SPECS.md`](KERNEL_SPECS.md)、検証の詳細は
[`FREECAD_VALIDATION_REPORT.md`](FREECAD_VALIDATION_REPORT.md) にあります。

**初めてこのリポジトリに来た方（人でも AI でも）は、先に
[`VERIFICATION_PLAYBOOK.md`](VERIFICATION_PLAYBOOK.md) を通してください。**
書いてある数字が本当かを自分で確かめる手順と、この文書で使う
「ゲート」と「診断」の区別が載っています。

**2026年8月27日の作業で、3-N-2c（箱と円柱を両方回すと断られる）は塞がり**（4-122。立体を返す演算 34 → 37）、**メッシュ非多様体は 11演算 → 6演算（4-123）→ 1演算（4-125）→ 0演算（4-126）**になりました。`contact_placement_probe` で立体を返す 37演算すべてが、B-Rep もメッシュも非多様体 0 です。**この一族はいったん閉じました。**

**そのあと 3-N-2 をやりました**——「触っているだけ」の置き方を5つ足して測り（4-127）、1点で触れているだけの面を「重なっている」と数えていた欠陥を直し、さらに**曲面どうしのブーリアンが通るようになりました**（4-128。直交2円柱と偏心する球×円柱の6演算。Steinmetz 立体 1152 に 7.4e-8 で一致）。`contact_placement_probe` は **20配置・60演算で 55 ok / 5 refused**（うち**3件は規約どおりの名指し拒否**）、B-Rep 非多様体 0 です。

**そのあとさらに、曲面どうしが「触れている」置き方を5つ足しました**（4-131）。3つは正しく、2つ欠陥が出ました——線で触れる2円柱の和の**断り文が「未実装」になっている**（答えが本当に非多様体なので名指しすべき。ただし直すには接触曲線そのものを作れる必要がある）ことと、`torus × cylinder` の**メッシュが 74 / 108 / 64 本非多様体**だったこと。後者は**直しました**（細分が新しい点を作らない距離を、溶接の距離より広く取る）。

**さらに、まだ1件も無かった曲面の組み合わせを5つ足したところ（円錐×円柱、円錐×円錐、円錐×球、トーラス×球、トーラス×トーラス）、そこで測定そのものが揺れていることに気づきました**（4-132。`HashSet` の反復順が細分の答えを決めていた。**今日いちばん重い所見です**）。直したので、通し3回が1バイトも違いません。

**そのうえで、そこで出た欠陥のひとつ（円錐の中の球を引くと体積が `A + B` になる）を追ったら、「円錐から中の立体を引く」が一般に通っていませんでした**（4-133。空洞シェルの向きが揃っていなかった。**触れてもいない配置でも落ちます**）。直しました。

**そこから「総当たりで掃く」に切り替えたら、続けて3つ誤答が出ました**（4-133〜4-137）——**同じ立体どうしの和が重なった立体を2つ返す**（体積2倍）、**裏返しの立体が打ち消し合って検査を通る**、**二重被覆が検査を3つとも通る**。**どれも「もっともらしい立体を返す」型**で、このリポジトリがいちばん避けてきた失敗です。**全部、閉じた式との突き合わせで見つかりました。** 掃いた軸と結果は 4-136 と 4-138 に表で置いてあります。

**その日（2026/08/28）の `contact_placement_probe` は 30配置・90演算で 81 ok / 9 refused**、**B-Rep 非多様体 0、メッシュ非多様体は 1演算**（`sphere × cylinder (eccentric)` の差 13本、4-129 の別機構）。**いまの数字は下の「2026年8月30日」を見てください。**

**2026年8月28日に、掃き出しで見つかっていた誤答と、空洞のある立体を片づけました**（4-143〜4-145）。

- **4-142 の誤答**（他カーネルの円柱 × 楕円柱）。恒等式の残差 1.2e-2 → **9.234e-10**
- **空洞のある立体のブーリアン**。`hollow_box` は**あらゆる相手との3演算が断られて**いましたが、**5組中4組が通る**ようになりました（残る1組はトーラスとの複合配置）
- **接触配置で、探すための余裕が答えの端点に残っていた**件（1e-6）
- **掃く軸を2つ足しました**（回して重ねる、回した空洞つき）。**誤答は 0**（4-146）。軸を足せば必ず出る、というものではありません
- **古い未解決を棚卸ししたら、1件が別の修理で直っていました**（4-147。`cone × sphere` の差が真値と一致し、空洞つきの立体が返る）

**2026年8月28日に 4-142 を解きました**（4-143）。原因は **earcut の出力を面積で検算していなかった**ことで、`trimmed_uv_triangulation` は**体積を積む経路**でありながら earcut を無検査で信じていました。**面は閉じ、非多様体でもなく、内外判定も通るので、恒等式でしか見えません。** 恒等式の残差 1.2e-2 →（途中の修理で 2.4e-3 →）**9.234e-10**。再現は `foreign_cross_pair_probe` として常設化しました。

**2026年8月29日に、9-G の G1〜G5 が全部揃いました**（4-149、4-151、4-153）。
`contact_placement_probe` は **81演算すべてで B-Rep もメッシュも非多様体 0**、
表示メッシュは常設分割で1面 4,604 枚以下、`main` へは 162コミットを早送りで
統合し、`origin/main` へ push 済みです。**揃ったのは「置いた数字」であって
「枯れた」ではありません**——限界は 9-G の追記に書いてあります。

---

## 2026年9月4日 — いまの現在地点

> ### 次回はここから
>
> **通しテストは緑です**（2026/09/03 朝。**137 バイナリ / 695 件 / 失敗 0 /
> 警告 0**）。
>
> ```bash
> cargo test --workspace --release
> ```
>
> ### 9月5日にやったこと（**4-320**）
>
> **1. 残った 42 本を数え直しました**（4-320）。**4-293 の絵はもう当てはまりません**
> ——「全部 A 側・67 枚の面」は、いま **A 33 本 / B 9 本・17 枚**です。
> **面の側から数える口**を足したら（`ZENITH_FLOAT_WHY=1`）、**丸ごと浮いた片は
> 0 枚**——「片が欠けている」ではなく**割り方の食い違い**でした。
> **B 側に綻びが1つ**: `x = 7.994676`（4-307 が名指しした継ぎ目）で、
> **弧とほぼ直線が、同じ2点のあいだを走って**います——面 607 は 3 辺で、
> 弦の長さは 0.646 と 0.644 でほぼ同じなのに、**膨らみが 76.6 倍違います**
> （0.1679 と 0.0022）。**その 2 辺に相手がいません**。片端は一致し、
> もう片端が 0.0069343 ずれています。**この数は 4-304 の弦の測り口に
> 出ていました**——出ていたのに名前が付いていませんでした。
> **なお、ここで推論を 2 回外しています**——「同じ角が2値ある。対称性から
> 片方が余計」（2値のあいだに稜があり両方本物）と、「長さが近いから
> ほぼ同じ線」（中点を見たら 76 倍違う）。**弦の長さで同じ線と言っては
> いけません**（4-65 と同じ轍）。**次は「どの面の組が出した稜か」から**。
>
> ### 9月4日にやったこと（**4-305 〜 4-319**）
>
> **1. 測り方の穴を塞ぎました。** `read_and_cut_probe` の既定の予算が
> **120 秒**で、**142 秒で返ってくる断りの中身が「返りません」に化けて**
> いました。**300 秒**に上げました。**この穴があるかぎり H8 の進み具合は
> 見えません。**
>
> **2. 「3演算とも 80 秒」は古い数字でした。** 実測 **131.7 / 144.4 / 141.8 秒**。
> 80 秒は**接する置き方**（`ZENITH_CUT_TANGENT=1`）のものです。
>
> **3. 閉じた輪は、切り込みではなく穴でした**（4-305）。境界の上でほどく
> 修理を書いて測ったら、**相手のいない稜 164 → 164、面片も断り文も1バイト
> 違いません**。測り口を残して見たら、**輪はどの頂点も境界から 0.345 ちょうど**
> ——**面の内部で閉じています**。次の一手は**穴（内側のワイヤ）として入れる**
> ことです。**「`FaceSplitter` がまだ持っていない機能」と書きましたが、
> 誤りでした**——4-306 で見たら **4-50 からあります**（`split_by_interior_loop`）。
> 呼ばれてもいて、**渡し方だけが間違って**いました。
>
> **4. 縫合の外れは 0.25/√2 ちょうどが 222 本。** 始点も終点も同じ距離で
> 中点だけ遠い——**A と B は 0.25 ずれた別の線**です（4-303 の裏づけ）。
>
> **5. 内側の輪も、鎖ごとに渡さないと当たりません**（4-306）。穴として入れる
> 機能（`split_by_interior_loop`）は 4-50 からあり、呼ばれてもいましたが、
> **交線を丸ごと渡していた**ので一度も当たっていませんでした。輪ごとに
> 渡したら **当たった分割 25 → 37、相手のいない稜 164 → 143**。
> **4-304 で隣の呼び口を直したときに、こちらを見ませんでした。**
>
> **6. 2つ目の穴は、入れると分割 37 → 41、相手のいない稜 143 → 146**（4-307）。
> **目標の数字が悪くなるので入れていません。** 診断だけ残しました。
> **番号を落として座標で比べたら、+3 が読めました**——**B 側 4 本が
> A 側 8 本に付け替わって**います。場所は `x = 3.275609` と
> `x = 7.994676`、**円柱の継ぎ目**です。**次はそこ。**
>
> **新しい測り口を足す前に、いまの出力を使い切ってください。** 私は
> 「番号でしか追えない」と書きかけました——**座標は出ていました。**
>
> **7. 相手のいない稜の3割は、切り口の上にあります**（4-308）。**別々の稜で
> 和 47 / 差 50 / 積 46 本**、うち **13 / 15 / 15 本が `z = 0.470` と
> `z = 1.410`＝切り手の底面と上面**です。**4-293 の「切り口の上に1本も
> 無い」は覆りました**——あれは切り口が z=1.0 だった頃の数字で、
> **4-294 で置き方を変えたのに結論を持ち越していました**。
>
> **8. その稜を開けたら、1つ出ました**（4-309）。**A は円を1本のまま持ち、
> B は割っています**——始点＝終点の閉じた円が、**和・差・積のどれでも 4 本**、
> **どれも A 側**で、B 側に同じ円が1本もありません。**設計の判断は要りません**
> ——`make_cylinder` は**円を4本の四半弧**で持ちます（`primitive.rs:179`）。
> **閉じた円1本のほうが規約から外れて**います。しかもこの円は**ファイル由来
> ではなく、交線を作る側が閉じた輪として出したもの**です。
>
> **なお「相手のいない稜 96 本」「1e-8 の重複が 4 組」は私の数え間違い**
> でした（4-309）。**3演算の出力を1つに混ぜて**数えていました。
> **重複はどの演算でも 0** です。
>
> **9. 相手が居ないのは、平面が輪を1つも入れていないからです**（4-310）。
> B 側の切り口の平面は **輪 2 個を見つけているのに、余りが 10〜12 本
> あるので丸ごと諦めて**います。**緩めて測ったら悪化**（47/50/46 →
> **55/56/54**、当たった分割 37 → **29**）——この経路は「輪だけ入れて早く
> 返る」ので、余った鎖が捨てられます。**門は理由があってそこにいます。**
>
> **10. 「余った鎖も流す」を書いて測りました**（4-311）。**手そのものは
> 効いています**——**相手のいない閉じた円が 12 → 5**、積では B 側にも円が
> 現れました。代わりに **B の外周（切り手の上面）が刻み込みの点で割れ**、
> 全体では **51/51/50** と悪化。**入れていません。**
>
> **11. 平面には専用の鎖の段があり、4-306 の受け皿に来ません**（4-313）。
> 受け皿を平面側にも置いても、**割ってから穴を足しても**（4-314）、
> **1バイトも動きません**。**平面に輪を入れる道は、まだ1本も通っていません**
> ——通っているのは曲面側だけ（4-306）。
>
> **12. 揃え方は測って決まりました**（4-314）。相手は **B 自身の側面**で、
> `y = 2.545` / `z = 1.410` の辺を **`x = 4.5149` から `7.9947` まで丸ごと
> 1本**で持っています。4-311 で増えた割れ目（`x = 4.2273`、`4.1309`）は
> **平面側にしかありません**。**輪を先に入れるなら、外周に新しい頂点を
> 作らない**か、**同じ頂点を側面にも入れる**か、どちらかです。
>
> **13. 4-306 は7つの門すべてで確かめました**（4-312）。道を変える修理は、
> 通しテストと掃き出しだけでは足りません。
>
> **14. 少しでも割れた面は、どの鎖の段にも来ません**（4-315）。切り口の
> 平面は **14 本中 11 本を当てられないまま返り**、輪を入れる機会が一度も
> ありません。**平面と曲面で通る道が違います**——曲面は鎖の段へ行くので
> 4-306 が効きました。**1本ずつのあとに受け皿を置いても動きません**
> （47/50/46、閉じた円 4/4/4——素と同じ）。**残るのは「なぜ入らないのか」
> だけ**で、`ZENITH_LEFTOVER_WHY=1` に断り文が出ます。
>
> **18. 4-306 が持ち込んだ重い失敗を消しました**（4-319）。**和の非多様体
> 9 → 0**、相手のいない稜も **47／50／42 → 48／48／42** で、**4-306 の
> 直前（56／56／48）より全部良く**なりました。**揃えるべきは「合計」**
> でした——4-318 の「組で揃っているか」を直しに使ったら、**通っていた
> `revolved_ring / tilted drill / Difference` が断られます**（その輪は
> 組が混ざるのに合計はどの稜も 1）。**4-318 の 944 個は、この試験を含んで
> いませんでした**——**門を数える口と、常設テストは別の集合です**。
> 7 つの門すべてで確認済み。`ZENITH_KEEP_MIXED_HOLES=1` で止まります。
> **`linkrods` はまだ断られます**（相手のいない稜 48／48／42、非多様体 0）。
>
> **17. 穴の縁の数え方が決まりました**（4-318）。稜ごとに「自分側／相手側が
> 何回使っているか」を数え、**その分布が輪の中で揃っているか**を見ます。
> **5 つの緑の門で輪 944 個すべてが揃っており（混ざり 0）**、`linkrods` は
> 10 個中 **2 個が混ざり**——**その 2 個が壊れている 2 個**です。面 269 の
> 混ざりが**和の非多様体 9 回**、面 441 の混ざりが**差と積で浮く 3 本**に
> そのまま対応します。**ここまでに 3 回外しました**（輪ごとに合計した／
> 自分側と相手側を混ぜた／「自分 0／相手 2」を犯人とした——**これは緑の
> データにも 48 個あります**）。**944 対 2 でも規則ではありません**が、
> **常設テストで固定しました**——板を貫く穴を作り、縁に触れている面片を
> **1枚落とすと混ざりが出る**ことを見ます。**入れる前に、落ちることを
> 確かめました**（`is_mixed` を `false` に固定すると FAILED。4-301 の流儀）。
> テスト **137 / 696 / 失敗 0 / 警告 0**。
>
> **16. その 9 本は、4-306 が刻んだ穴そのものでした**（4-317）。A 269 の
> **内側の輪 4 本のうち 3 本**が、そのまま非多様体の稜です。**外周 18 本は
> 無関係**でした（巻き込みだと読みかけて外しました）。差と積では
> **A の内側と外側が両方残って**浮き、和では**内側が落ちて B が 2 枚**入ります。
> **B は 2 枚とも正しい**ので、**B が自分で閉じている窓に A が穴を開けている**
> のが本体です。**次は「開ける前に、相手がその窓を閉じているか」を数える**
> こと。**それが決まるまで 4-306 を戻さないでください。**
>
> **15. 4-306 は、軽い失敗と重い失敗を取り替えていました**（4-316）。
> **和の非多様体が 0 → 9 本**になっています。commit を挟んで特定しました
> ——`3920f77`（4-306 の直前）は **56/56/48・非多様体 0**、`cd32fe9`（4-306）で
> **47/50/42・非多様体 9**。以降は診断だけなので1バイトも動きません。
> **4-306 は「稜 164 → 143」の勝ちとしてだけ書かれており、重い側が増えた
> ことはどこにも書かれていませんでした。** 余分なのは **A の 面 269 ただ1枚**
> （NURBS、外周 18 本、内側の輪 1 個）で、B の 6 枚は**すべて平面・向き違いの
> 対＝正常**です。場所は **`x = 3.275609`**——**4-307 が「次はそこ」と
> 書いた円柱の継ぎ目そのもの**。**残すか戻すかは判断が要ります**（4-316 に
> 書きました。私は残しました）。
>
> #### 今日の空振りは7つあります
>
> **同じ道を歩き直さないでください**——4-310（門を緩める）、4-311（輪を
> 先に入れる。ただし**唯一、狙った数字を動かしました**）、4-313（平面の
> 段に受け皿）、4-314（割ってから穴を足す）、4-305（境界でほどく）、
> 4-307（2つ目の穴）。**数字は各記録にあります。**
>
> ### 9月3日にやったこと（**4-293 〜 4-304**）
>
> **1. `linkrods` が返るようになりました**（4-294。**300 秒超 → 132〜147 秒**）。
> 律速は「近くに境界があるか」を見るのに**すべての面へ全域射影していた**こと
> でした（射影の 100% がそこ）。**答えは1つも動かず**、掃き出しの合計時間も
> **1453.4 → 1225.7 秒（−16%）**。
>
> **2. H8 が「いちばん難しい置き方」だけを測っていました**（4-294）。切り手を
> 立体の囲みから作っていたので、**側面がモデルの境界面そのもの**——必ず端の
> 特徴に接します。**普通の置き方を既定**にし、難しいほうは
> `ZENITH_CUT_TANGENT=1` で残しました。
>
> **3. 刻みを振る門を足しました**（4-297。`mesh_density_probe`）。**自作の
> 立体は 10 通りの刻みすべてで水密**です。**読んだ `screw.step` は
> 8・12・24 で水密なのに 16・20・32・48 で壊れます**（4-296。**24 が緑
> だったのは偶然**でした）。
>
> **4. `linkrods` の断りの中身**（4-293、4-295）。相手のいない稜 56 本は
> **全部 A 側**、**選び方は正しい**（A 39 採る/24 落とす、B 14/2）、
> **切り口の上にない**（切断面 z=1.0 に 1 本も無い）。**→ 覆りました**（4-308。
いまの切り口 `z = 0.470` / `1.410` の上に **30 本**あります。**あれは切り口が
z=1.0 だった頃の数字**です）。
>
> **5. 安くした道を常設テストで固定しました**（4-301）。`has_boundary_within` と
> 全域射影に**同じ問いを 875 通り＋空洞つき**で投げ、答えが一致することだけを
> 見ます。**入れる前に、わざと壊すと落ちることを確かめました。**
>
> **6. 刻み込みの受け入れ幅は犯人ではありません**（4-303）。諦めた 9,166 件の
> 外れは**最小 1.76e-2**、**1e-5〜1e-3 は 0 件**。**惜しかったものが1件も
> ありません**——公差を緩めても何も変わりません。つまり A の短い片は
> **B の稜の上に無い**。「割り方の食い違い」ではなく**別の線**かもしれません。
>
> **7. 継ぎ目をまたいで一周する切り込みを直しました**（4-304）。閉じた曲面
> から来た面は境界に**継ぎ目の辺を2本**持ちます。3D では同じ線なので、そこを
> 通る点は**巡回上の2か所**に居るのに、**1か所しか見ていませんでした**。
> さらに、ずらしたあとは**どちら向きの切り込みを付けるかが 3D では決まりません**
> ——両端が同じ点なので、どちらでも閉じます。**面積で選ぶ**ようにしました。
> **相手のいない稜 64／64／60 → 52／52／48**、面片 41／24／21 → **49／32／19**。
> **`linkrods` はまだ断られます。**
>
> #### 次の一手は2つあります
>
> | | 中身 |
> | :--- | :--- |
> | **`screw` の表示メッシュ** | 面 21（ねじ山）が**隣の面にはみ出しています**（またいだ重なり 85 本 > 1枚の中 36 本）。**p-curve が真のトリムより外に出ていないか**から（4-300） |
> | **`linkrods` の縫合** | **一段進みました**（4-304）——相手のいない稜 **64／64／60 → 52／52／48**、面片 41／24／21 → **49／32／19**。継ぎ目でずらしたとき、**どちらの巡回にどちら向きの切り込みを付けるかが 3D では決まらない**（両端が同じ点なので、どちらでも閉じる）ので、**面積で選ぶ**ようにしました。**残る 52 本はまだ数えていません**——**まず数え直してください**。`ZENITH_UV_DUMP=1` で uv の巡回がそのまま出ます |
>
> **外した手は今日だけで 5 つ**あります（4-293、4-295、4-298、4-299、4-300）。
> **同じ道を歩き直さないでください。**
>
> #### 9-H は **H1〜H7 達成**、残るのは **H8 の `linkrods.step` だけ**です
>
> | | いまの数 |
> | :--- | :--- |
> | **`screw.step`** | **H8 の目標を満たしています**（4-290）——読めて、切れて、**表示メッシュも結果も非多様体 0**、恒等式 **2.848e-13 / 2.874e-13** |
> | **`linkrods.step`** | **3演算とも 131.7〜144.4 秒で返るが、断られます**（2026/09/04 実測）。縫合で相手のいない稜 **48／48／42 本**、**非多様体 0**（4-319）。**「80 秒」は接する置き方（`ZENITH_CUT_TANGENT=1`）の数字**で、4-294 で既定が普通の置き方に変わってからは当てはまりません |
>
> **4-291／4-292 の「入口は `(0,0)` の 36 件」は、もう当てはまりません。**
> 4-294 で切り手の置き方が変わったので、**測り直しが要ります**。9/3 の実測は
> こうです（4-304）。
>
> - **公差の問題ではありません**（4-303）——刻み込みを諦めた 9,166 件の外れは
>   **最小 1.76e-2**、**1e-5〜1e-3 は 0 件**。惜しかったものが1件もありません
> - **`(0,0)` は 0 件になりました。** いまは全部の端点が他の稜につながっています
>   （(1,1) 20、(1,3) 26、(3,1) 25、(3,3) 8、(5,*) 4）。**奇数ばかりなのは同じ**です
> - いちばん近い相手までの**中点の距離は最小 0.0237**——**ぴったり重なる相手は
>   もう居ません**。近いものは「頂点を共有する隣の稜」で、相手ではありません
> - **入口は割れなかった側**です（4-304）。当たらなかった鎖 15 本のうち
>   **7 本が閉じた輪**、8 本が境界から 0.103〜1.086 離れています
>
> #### 9月2〜3日に閉じたもの（4-286 〜 4-292）
>
> **読んだ STEP の表示メッシュが、2ファイルとも水密になりました**（穴 14／34 → **0／0**、重なり 12 → **0**）。
>
> | | 何が起きていたか |
> | :--- | :--- |
> | 4-286 | **内側の一直線の上に T 字の継ぎ目**（境界の辺には手当てが3段あるのに、内側には無かった）／**uv で潰れた三角形を出口で捨てていた**（3D では面積 0.005672 を持つ 41 枚） |
> | 4-288 | **継ぎ目に混ざって乗る区間**が、寄せる対象から漏れていた（判定が「全部 min 側」か「全部 max 側」しか見ていなかった）。**副産物で速度も直り**、2時間20分で返らなかった演算が **80 秒**に |
> | 4-289 | **閉じた1本の交線**を、3か所が揃って弾いていた（輪の収集・ワイヤ化・蓋の面）。蓋のループ 0 → 8、面 0 → 4 |
> | 4-290 | 掃き出しが**同じ立体を別の刻みで測って**いた（24 と 16）。揃えたら `screw` の非多様体が 45 → 0 |
>
> **16 分割では、読んだ立体そのものに非多様体が 45 本残っています**（4-290。`v = 0.5` の上の細片）。掃き出しは 24 分割で測るので**見えていません**——**「見えなくなった」と「直った」は別**です。
>
> **9-H の H1〜H5 は全部達成済み**です（4-210 で H5）。**次の目標 H6〜H8 は
> 9-H に置きました**（9/1。「次の数値目標」）。
>
> **9月1日の後半で、H6 と H7 を達成し、H8 に着手しました**
> （記録は **4-226 〜 4-259**）。**H6 は 4-259 で達成**——射影の収束判定に
> 次元があり、`残差 · ∂S/∂u` を絶対の公差と比べていました。**門を 0.005 まで
> 下げて、破れ 0・非多様体 0・断り 0** です。
>
> **測り口が9つ増えました**（どれも数秒〜十数秒で回ります）:
> `face_budget_probe`、`edge_end_gap_probe`、`cone_torus_mesh_probe`、
> `read_and_cut_probe`、`mass_scale_probe`、`scale_origin_probe`、
> `piece_diff_probe`、`identity_split_probe`、`intersection_fit_probe`（桁つき）。
> **`scale_sweep_probe`（1配置 20 分）を待つ必要はもうありません。**
>
> **置いた3つ:**
>
> | # | いまの数 | ゴール |
> | :--- | ---: | :--- |
> | ~~**H6: 桁を跨いでも答えが変わらない**~~ | **達成**（4-259）。`scale_sweep_probe` の門を **0.005 まで下げて**、破れ 0・非多様体 0・断り 0。残差の最悪 8.912e-6 → **7.394e-8** | 原因は**射影の収束判定に次元があった**こと——`残差 · ∂S/∂u`（長さ²/パラメータ）を絶対の 1e-7 と比べていたので、**到達できる残差がパッチの大きさに反比例**していた（桁 1 で 1.6e-8、桁 0.01 で 1.6e-6。予想と実測が 10% 以内で一致）。微分の大きさで割って長さの次元に戻した。**たどり方は 4-229 〜 4-259**（推測を 11 回外しています） |
> | ~~**H7: 未実装の断りを 0 のまま保つ**~~ | **達成**（4-226）。断り文を2種類に分けて数え、「未実装」で断ったら **exit 1**。実測 0 件 | — |
> | **H8: 掃き出しの4本目の軸** | **着手済み**（4-228）。`read_and_cut_probe` を足し、OCCT が配る実物の STEP を読む段で**3つ直しました**（`SEAM_CURVE`、ファイルが申告する不確かさ、稜の頂点での切り詰め）。**まだ切れていません** | **読んだ立体（STEP）を切って恒等式の破れ 0。** **4-260 で壁の位置が変わりました**——**9-3 の判断ではなく、STEP の解析曲面の復元**です。`screw.step` は `CYLINDRICAL_SURFACE` だけが正しく復元され（外れ 6.3e-15）、`CONICAL_SURFACE` と `TOROIDAL_SURFACE` は**決め打ちの 90 度パッチに落ちて**外れが 13〜87 になります。**円柱が同じ道で通っているので、設計ではなく実装の問題**です。**4-261 で円錐を直しました**（頂点の向こう側の葉。外れ 23.8 → **1.6e-11**、検証エラー 225 → **72**）。残るのは**トーラス**（紡錘型。芯 8.25 / 管 54.87）で、**4-262 で「ファイルの側が合っていない」ところまで測りました**（面の頂点が管から 0.377 と 3.094 ずれ、どんなトーラスにも乗らない）。**4-263 で断り文を正直にしました**。**4-264 でトーラスも直しました**——紡錘トーラスの**軸をまたいだ側**で、円錐と同じ符号の見落としでした（境界の外れ **87 → 2.946e-4**、検証エラー **72 → 18**）。**構造の欠陥は2つとも塞ぎ**、粗さは**面ごとに持ち歩く**ようにしました（4-266）。<br><br>**9/2〜9/3 で、土台が揃いました**（4-286〜4-289）。**表示メッシュは2ファイルとも水密**（`screw` 14 → 0、`linkrods` 34 → 0、重なり 12 → 0）。**`screw.step` は H8 の目標を満たしました**（4-290）——3演算とも通り、**メッシュ非多様体 0**、恒等式が **2.848e-13 / 2.874e-13** で閉じます。`linkrods.step` は **2時間20分で返らなかった演算が 80 秒**になりましたが、**まだ断られます**——縫合で相手のいない稜が **56 本**。次はそこです。<br><br>**16 分割では読んだ立体そのものに非多様体が 45 本あります**（4-290。`v = 0.5` の上の細片）。掃き出しは 24 分割で測るので**見えていません**——**「見えなくなった」と「直った」は別**です |
>
> **H6 と H7 は 9/1 に達成しました**（4-259、4-226）。**残るのは H8 だけ**
> です（新しい軸。これまで足すたびに欠陥を出しているので、いちばん当たりが
> 大きい）。**9-H に新しい目標を置き直すところ**も、また上流の宿題に
> 戻りました。
>
> #### 9月1日に閉じたもの（4-214 〜 4-224）
>
> 4-213 の「**曲面のブーリアン結果を箱で切ると断られる**」を追って、
> **`rechained_boolean_probe` を 2段目の断り 6 → 0** にしました。いまは
> **10連鎖・30演算すべて緑**（3段の連鎖と桁混在を含む。残差の最悪 1.906e-7）。
>
> 直したのは4つです。
>
> | | 何が起きていたか |
> | :--- | :--- |
> | 4-215 | 面積の検算がトリムを読めないとき、**素のパッチを測って**正しい割り方を落としていた。**「測れなかった」と言う**ように |
> | 4-216 | 吸着の許容が面の大きさに比例する一方、p-curve の許容は絶対 1e-5。**天井 4e-5** を付けた |
> | 4-217、4-219 | 切り込みが**トリム境界を 0.54〜1.00 追い越して**いた。3D で厳密に切り詰める。さらに**当たった鎖があると残りを試さずに返して**いた |
> | 4-220 | **蓋の材料に重複除去がかかっていなかった**（4-80 と同じ「同じ弧に別々の実体」）。輪が同じ2本を往復し、面積 0 で落ちていた |
> | 4-223 | **訂正あり。** 交線のずれを測った数字（1.5e-6〜6.5e-6）は、**標本の取り方の差も拾う測り方**でした。**信じないでください。** 分かっているのは 4-222 まで |
> | 4-224 | **パッチの大きさに 1.0 の絶対な床**。差し渡し 0.21 のパッチが「1.0」と報告され、模型の半分の歩幅で交線を辿り始めていた。`scale_sweep_probe` の断りが **3 → 0** |
> | 4-225 | **恒等式は交線の精度に鈍い**と分かった（桁 0.1 で交線が相対 4.5e-6 ずれても恒等式は 2.4e-13）。両方の演算が同じ交線を使うので誤差が打ち消し合う。**残る破れの原因は「差と積で食い違うもの」**——4-212 以来の「交線の精度」という筋は消えた |
>
> #### 次にやること（上から）
>
> 1. **掃き出しの軸を選ぶ。** 「置き方」（4-211 で 0 件）と「自分の出力を
>    入力に戻す」（4-221 で 0 件）は、**どちらも枯れました**。残っているのは
>    **大きさの桁**（4-212）で、そこには**未修理が1件**あります——
>    `torus × cylinder` の恒等式が **0.02 と 0.01 の間で 6桁跳びます**。
>    **4-222 → 4-223 で名前が付きました**——**交線の絶対公差**です。交線の
>    当てはめ誤差は**桁に依らず 1.5e-6〜6.5e-6**（`tol.linear` は 1e-6）で、
>    模型が小さくなるほど相対の精度が落ちます。積がいちばん先に見えるのは
>    **3演算のうち体積がいちばん小さい**からです。**選び方も位相も三角形の
>    細かさも無罪**でした。**直していません**——`tol.linear` はカーネル全体の
>    公差なので、比例させるなら `on_surface_limit` と `SNAP_CEILING` も
>    一緒に動かす必要があります（4-223 に手順）。門の外に置いてあります。
>    **4-224 で `surface_patch_extent` の 1.0 の床を外し、小さい桁の断りは
>    3 → 0 になりました**（35通りすべてが 3演算とも立体を返します）。
>    **残っているのは「返ってきたのに答えが合わない」だけ**です。
>    **4-225 で筋が変わりました**——恒等式は交線の精度に鈍いので、
>    探すのは**差と積で食い違うもの**（片の選び方、蓋、落ちる経路）です。
>    まだ試していない軸: **読んだ立体（STEP）を切る**、**同じ形を別の順で
>    組む**、**面の多い立体どうし**
> 2. **`blend_coverage_probe` の残り3件**（`chamfered_box` は角のパッチ、
>    `elliptic_prism` は楕円の縁、`torus_segment` は相手がトーラス）
> 3. **9-3 の判断**（内部表現に解析曲面を持つか）。まだ決めていません
>
> #### この夜の数字
>
> | 指標 | 値 |
> | :--- | :--- |
> | テスト | **137 バイナリ / 692 件 / 失敗 0 / 警告 0** |
> | `contact_placement_probe` | 47配置・141演算で **135 ok / 6 断り**、B-Rep・メッシュとも非多様体 0、恒等式 41配置で破れ 0（最悪 **3.900e-8**） |
> | `rechained_boolean_probe` | **10連鎖・30演算すべて緑**、断り 0、非多様体 0（最悪 **1.906e-7**） |
> | `scale_sweep_probe` | 破れ 0、非多様体 0、**断り 0**（4-224 で 3 → 0。**35通りすべてが3演算とも立体を返します**。最悪 9.038e-6。0.02 以下は門の外） |
> | `face_budget_probe`（H5） | 1面あたり最大 **3,854 枚**（門は 10,000） |
> | `intersection_fit_probe`（9/1 に追加） | 交線 **101 本すべて 5e-7 以下**、1e-5 超え 0 |
>
> #### 測り方について（4-213 〜 4-220 の教訓）
>
> **推測を6回外しました**——交線の当てはめ誤差 → `split_at` → 領域の取り方
> → 蓋の材料 → 当てはめの許容 → 辿る段の収束判定。当たったのはどれも
> **測ったあと**です。**最後は「掛けている値そのものが嘘だった」**
> （4-224 のパッチの大きさ）という落ちでした——**公差をいじる前に、
> 掛けている量を出して見てください。****推測を書くのは
> よいのですが、次に読む人が最初にやることはその推測を測って潰すことだ、
> と分かるように書いてください。**


## 2026年8月31日 — 前日の記録

**この日は表示メッシュを3つ直しました**（4-208、4-209）——残っていたメッシュ
非多様体を 0 に、その上流（`sew` が頂点を束ねるとき曲線を置き去りにしていた）を
機械精度まで、そして 4-204 が戻した内側の対角線の入れ替えを、禁則を3つ足して
入れ直しました（1面あたり最大 **14,996 → 11,484 枚**）。

**前日（8月30日）は接触配置を閉じ、スケッチから立体までの道を通し、方針を
置き直しました。** その日のコミットは 23 本、記録は 4-169 から 4-202 です。

### 数字（すべてこの日の実測）

| 指標 | 値 | 前日 |
| :--- | :--- | :--- |
| テスト | **136 バイナリ / 689 件 / 失敗 0 / 警告 0**（4-208 で 135 / 687 から）。**その後 3本足して 137 バイナリ / 692 件**になり、9/1 の通しで確認しました | 130 / 664（`zenith_py` を除外していました） |
| `contact_placement_probe` | **47配置・141演算で 135 ok / 6 断り**（4-211 で5配置追加。**その5配置は欠陥 0**）（4-205 で 4配置、4-207 で 3配置を追加。4-206 で和の断りを解消）。**断る 6 件はすべて「答えが本当に非多様体だから」**で、未実装の断りは 0 です。**B-Rep 非多様体 0**、メッシュ非多様体 **0演算**（4-208 で 4演算 → 0。原因は「稜の曲線の端が頂点と 1.3e-7 ずれている」ことでした） | 30配置・90演算で 81 / 9 |
| 同・**恒等式**（この日に追加） | **41配置で破れ 0**、残差の最悪 3.900e-8（4-205 で 29 → 32、4-206 で 33、4-207 で 36 配置に） | （測っていませんでした） |
| `foreign_cross_pair_probe` | 15組、恒等式の破れ 0、残差の最悪 **9.429e-10**（4-208 で 7.052e-10 から。**上流を直した代償**で、良くなった組と悪くなった組が両方あります） | 同じ |
| `boolean_envelope` | supported 44 / wrong-result 0 / error 1 | 同じ |
| `face_budget_probe`（8/31 に追加） | **64分割の1面あたり最大 3,854 枚**（4-210 で 11,484 から −66%）。**H5（10,000 以下）は達成**し、**この口は赤ゲートになりました**（超えたら exit 1） | （手で `PATCHWHY` を読んでいました） |
| 稜の端と頂点の差（8/31 に追加） | **最大 3.972e-15、溶接距離 (1e-7) を超えるもの 0 箇所**（4-208 で 1.330e-7 / 54 箇所から） | （測っていませんでした） |
| `rechained_boolean_probe`（8/31 夜に追加） | **5連鎖**。ブーリアンの結果をもう一度切ります。通った3連鎖は**恒等式が閉じる**（最悪 2.334e-10、非多様体 0）。**箱で切る2連鎖は 2段目が 0/3 で断られます**（4-213）。**→ 9/1 に両方とも直り、10連鎖・30演算すべて緑になりました**（4-217〜4-221。残差の最悪 1.906e-7） | （測っていませんでした） |
| `scale_sweep_probe`（8/31 夜に追加） | **5配置 × 7桁**。門は 0.1〜100 で**恒等式の破れ 0・非多様体 0・断り 0**。**0.02 以下は門の外**——`torus × cylinder` だけ 0.02 と 0.01 の間で残差が6桁跳びます（2.074e-12 → 1.368e-6）。**未修理**（4-212） | （測っていませんでした） |
| `blend_coverage_probe`（8/30 に追加） | **3 件が「丸める稜があるのに 0本」**（4-203 で 4件から。**H4 達成**） | （7件と数えていましたが、うち3件は 0 で当然でした） |

### 8月31日に何が変わったか

**1. 残っていたメッシュ非多様体 4演算が 0 になりました**（4-208）。機構は
**稜の曲線の端が、頂点の位置と 1.128e-7 / 1.330e-7 ずれていた**こと。境界の
標本を曲線から取るので、隣り合う2本の稜が継ぎ目に「同じはずの点」を2つ作り、
溶接の距離 (1e-7) のすぐ外なので束ねられず、そこが穴になっていました。

**2. その上流も直しました**（4-208）。掃いたら `cone × torus` だけの話では
なく、稜の端 5,292 箇所のうち **54 箇所**が溶接の距離を超えていました
（`box × cylinder` にも `cylinder × cone` にも）。原因は **`sew` が頂点を
束ねるときに曲線を置き去りにしていた**こと。**最大 1.330e-7 → 3.972e-15。**
**代償も測りました**——体積が 1e-10 の桁で動き、`foreign_cross_pair_probe` の
残差最悪は 7.052e-10 → 9.429e-10（破れは前後とも 0）。

**3. 溶接が「向きの違う双子」を重複として外していました**（4-209）。一直線の
境界で面と面が出会うところでは、両側の面が同じ3頂点の薄片を1枚ずつ出します。
**閉じた立体としては2枚とも要ります**（向きが逆）。突き合わせの鍵を、並べ替え
から**向きを保った回転**に直しました。

**4. 内側の対角線の入れ替えを入れました**（4-209）。**4-204 がここで表示
メッシュを3件壊して戻しています。** 壊れる形に名前が付いたので、禁則を3つ
足して入れ直しました——**64分割の1面あたり最大 14,996 → 11,484 枚（−23%）**、
掃き出しのメッシュ非多様体は **0 件のまま**。**H5 は未達（残り 15%）。**

**5. 単体テストを2本足しました**（`zenith_tess` の 3 → 5本）。挿し戻しと
入れ替えを、ブーリアンを回さずにその場で押さえます（どちらもミリ秒）。
**3本目も書きましたが外しました**——「境界に沿った耳を作らない」を確かめる
つもりが、**禁則を外しても通ってしまった**からです。守りを固定できていない
テストは、誤った安心になります。

**6. 落ちた境界の点を戻す道を2つ足しました**（4-209）。`split_edge_carrying_point`
と `force_missing_boundary_edges`。境界の辺の欠けは全126演算で 0 本です。

**7. 入れ替えを細分の「途中」にも挟みました**（4-210）。4-209 は細分の
**前に1回だけ**掛けていましたが、最長辺二分は割るたびに細長い片を作り直す
ので、1回では足りません。**1通しごとに整えたら 11,484 → 3,854 枚（−66%）**
で、**H5 を達成**しました。ブーリアンの仕事量は 30,621,637 回のまま**1桁も
動きません**（表示側だけの変更である証拠）。

**環境変数の読み取りは、いちばん外で1回だけ**にしてあります。標本1点ごとに
`std::env::var` を引くと、**診断を使っていないときまで遅くなります**（`String`
を1点ごとに作ることになります）。口を足すときは、内側のループに置かないで
ください。

**新しい口**: `face_budget_probe`、`edge_end_gap_probe`、`cone_torus_mesh_probe`、
`ZENITH_RING_WHY`、`ZENITH_RING_WATCH`、`ZENITH_SPLIT_WATCH`、
`ZENITH_NO_DELAUNAY_FLIP`。すべて
[`VERIFICATION_PLAYBOOK.md`](VERIFICATION_PLAYBOOK.md) に載せてあります。

### 8月30日に何が変わったか

**1. 接触配置の穴が 0 になりました**（4-169〜4-197）。断る 6 件は**すべて
「答えが本当に非多様体だから」**で、どこでそうなるかを座標で名指しします。
**「未実装として断る」は 0 件**です。

**→ これは 35配置での話でした。** 8月31日に4配置足したら、**未実装の断りが
1件出ました**（`cone × torus` の和。4-205）。**測った範囲を添えずに「0件」と
書かないでください。** その1件は同じ日に直したので（4-206）、**39配置では
また 0 件**です——この文も、39配置での話です。

**2. 接触では「測って決める」が効きません**（4-180、4-181、4-195、4-196）。
4つの手を実装して4つとも外しました——区別したい量が、いつも公差の下に
あるからです。**形から決める**（4-190、4-197）だけが通りました。
**この文書の「主張ではなく測定で」の、唯一の例外です。**

**3. 門が答えの正しさを見るようになりました**（4-191）。それまで
`contact_placement_probe` は 90演算を走らせながら**答えを1度も検算して
いませんでした**。

**4. `zenith_py` が門に入りました**（4-198）。それまで
`--exclude zenith_py` で回しており、**Blender へ届く層を1度も測って
いませんでした**。

**5. wasm で建ちます**（4-199）。カーネル6クレートがエラー 0・警告 0。
**建つことだけ**で、走らせた記録はありません。

**6. スケッチから立体までの道が通りました**（4-200）。円弧つきで閉領域を
取り出し、押し出した体積が `面積 × 高さ` と相対 1e-9 で一致します。

**7. 球の蓋の縁が丸められるようになりました**（4-203）。転がる球の中心が
描く円から**厳密なトーラス**が出ます。フィレット4通り・面取り3通りが、
除去体積の閉じた式と **1e-12 以内**で一致します。H4 の門はこれで満たしました。

**8. H5 の数字を訂正しました**（4-204。2026/08/31）。10,354 枚は**差だけ**を
測った値で、**和は 13,524 枚**です。目標との差は 3.5% ではなく 35% でした。
重いのは細分ではなく**渡している三角形の形**で（earcut の 382 枚が細分で
9,784 枚に膨らむ）、内側の辺を入れ替える手は −14% 効いたものの、**表示
メッシュを3件壊した**ので入れていません。

### 次に何をするか

**[9-H](#9-h-次の方針2026年8月30日に置きます) が正です。**

| | | |
| :--- | :--- | :--- |
| H1 | `zenith_py` を門へ | **達成**（4-198） |
| H2 | wasm で建つ | **達成**（4-199） |
| H3 | スケッチから立体まで | **達成**（4-200） |
| H4 | 丸める稜があるのに 0本 が 3件以下 | **達成**（4-203。球の蓋の縁を厳密トーラスで丸める） |
| H5 | 64分割でも1面 1万枚以下 | **達成（3,854 枚）**。4-209 で 14,996 → 11,484（入れ替えを細分の前に1回）、**4-210 で 3,854（通しごとに入れ替え）**。`face_budget_probe` は**赤ゲート**（超えたら exit 1）。**「入れ替えは点を足さないのでここが限界」と2度書きましたが、限界だったのは掛ける回数のほうでした**（4-204、4-209 → 4-210） |

**掃き出しで開いた宿題は、2つとも閉じました**（4-205、4-206、4-208）。
**H1〜H5 は全部達成しました**（H5 は 4-210）。**9-H に新しい目標を置くところ**が、いまいちばん上流の宿題です（候補は上の「次回はここから」）。

| | 中身 |
| :--- | :--- |
| ~~未実装の断り 1件~~ | **直しました**（4-206）。面積を囲む片の中に切れ目が1本混ざっていました。読みは当たっていて、「交線は取れていて組み立てだけが足りない」でした |
| ~~メッシュ非多様体 4演算~~ | **直しました**（4-208）。機構は「**稜の曲線の端が、頂点の位置と 1.1〜1.3e-7 ずれている**」でした。境界の標本を曲線から取ると継ぎ目に同じはずの点が2つでき、溶接の距離 (1e-7) のすぐ外なので束ねられません。**表示側で両端を頂点へ寄せ、そのあと上流（`sew` が頂点を束ねるところ）も直しました**——稜の端と頂点の差は最大 1.330e-7 → **3.972e-15**、溶接距離を超える箇所は 54 → **0** |

**H4 は「3件以下」を満たしただけで、0件ではありません。** 残る3件は
`chamfered_box`（**角のパッチ**が要る。4-202。稜48本という数に釣られない
でください）、`elliptic_prism`（縁が**楕円**なのでフィレット面がトーラスに
ならない）、`torus_segment`（相手の面がトーラス）です。

**掃き出しは段に入れず、ずっと並行して回します。** 8月30日は接触配置を
5つ足しただけで**穴2つとバグ1つ**が出ました（4-192、4-194）。8月31日も、
`cone × torus` の置き方を変えただけで機構が2つ出ています（4-208、4-209）。

---

**（以下は 2026/08/29 までの記録です。上の数字が最新です。）**

**「このカーネルはどこまで行けるのか」「実用に耐えるには何が要るのか」は
9章にあります。** ただし**9章だけは実測ではなく判断**です。1〜8章と混ぜない
でください。測定が出たら上書きしてかまいません。

---

## 0. 作業を引き継ぐときの現在地点

この節は、リポジトリだけを渡された人または AI が、別のブランチや古い測定値から
作業を始めないための入口です。上のコミット番号はこの文書を更新した時点の
スナップショットなので、作業開始時には必ず次を実行して実物を優先してください。

> **次に何をするかは [9-H](#9-h-次の方針2026年8月30日に置きます) にあります。**
> 段取り（第0段〜第4段）と数値目標（H1〜H5）、そして**なぜその順序か**を
> 書いてあります。**掃き出しは段に入れず、ずっと並行して回します。**

```bash
git status --short --branch
git log --oneline --decorate -12
git rev-list --count main..HEAD
```

2026年8月27日時点の引継ぎ状態:

| 項目 | 値 |
| :--- | :--- |
| 作業ブランチ | `codex/tess-edge-contract` |
| 基点 | `main` / `origin/main` の `17b3d33` |
| `main` からの差分 | **2026/09/05 に統合したので 0 コミット**（`f3815de..b5f848b`、99 コミットを早送り、衝突 0）。**この数はすぐ古くなります。上のコマンドを打ってください** |
| 直近のまとまり | **4-226 〜 4-289**（H6・H7 達成、H8 は土台が揃った。9/3 の到達は 4-286〜4-289）。以前の記述は **4-226 〜 4-252**（H7 達成＝「未実装の断り」を赤ゲートへ、H8 着手＝実物の STEP を読む段で3つ修理、**H6 の原因を特定**＝同じ切り口が和と積で別の道を通る）。その前が **4-208、4-209**（表示メッシュ: 残っていた非多様体を 0 に、上流の稜の端を機械精度へ、内側の対角線の入れ替えを禁則つきで導入）。それ以前は 4-115〜4-145（メッシュ非多様体の修復、**曲面どうしのブーリアンの開通**、接触配置を15追加、**測定の非決定性の修復**、**総当たりの掃き出しで「もっともらしい立体を返す」誤答を4つ修復**、**同じ場所を占める2つの開通**、**本番経路のパニック1件の修復**、**空洞のある立体の開通**、常設テスト10本の追加） |
| 作業ツリー | clean（2026/09/01 の時点）。統合 PDF / HTML は**生成をやめて削除した**（4-115）。読み物の本文は `docs/treatise/` に Markdown で残っている |
| **やり残し** | **通しテストは緑です**（2026/09/03、**137 バイナリ / 695 件 / 失敗 0 / 警告 0**。4-301 で 693 件から）。**掃き出しも緑**（47配置141演算、135 ok / 6 断り＝未実装 0、B-Rep・メッシュとも非多様体 0、恒等式 41配置で破れ 0、最悪 3.900e-8）。**読んだ STEP の表示メッシュは、2ファイルとも水密になりました**（4-286、4-288）。**`screw.step` は3演算とも通り、恒等式も閉じます**（2.848e-13 / 2.874e-13）。残るのは `linkrods.step` で、**80 秒で返るようになりましたが縫合で相手のいない稜が 56 本**あります（4-289）。以下は途中の記録です——**`screw.step` の穴は 0 本になりました**（4-286。機構は2つ——内側の T 字継ぎ目と、uv で潰れた三角形を出口で捨てていたこと）。**残るのは `linkrods.step` の 33 本**で、そちらは**3つ目の機構**でした（4-287）——**輪が uv で自分と交わっています**。1本の稜の p-curve が継ぎ目をまたいで u 0→1 に飛んでおり、三角形が多角形の **1.57 倍**を覆っています（重なり 12 本）。**→ 直りました（4-288）。** `settle_seam_segment_axis` の「寄せ先が曖昧な区間」の判定が「全部 min 側」か「全部 max 側」しか見ておらず、**両端に混ざって乗る区間が漏れて**いました。**いま `screw.step` も `linkrods.step` も、表示メッシュは穴 0・重なり 0** です（`linkrods` は三角形も 46,517 → 32,998 に減りました）。**穴は表示メッシュ側だけで、B-Rep は健全です**——`screw.step` の稜 37 本すべてが 2 面から使われています（2026/09/02 実測）。入口は 4-277（p-curve を作れず黙って落ちる面）。<br><br>**H6 は達成済みです**（4-259。`ExtremumEngine` の収束判定の単位が合っていなかった。`scale_sweep_probe` の門を **0.005 まで**下げて破れ 0）。**4-252 の「3案あり、まだ入れていません」は古い行です**——4-253〜4-259 でその3案は**どれも犯人でないと測って**、別の場所（射影）に当たりました。<br><br>**H8 の「9-3 の判断待ち」も古い行です**（4-228）。**4-260 で壁の位置が変わり**、判断ではなく **STEP の解析曲面の復元**でした。そこは 4-261・4-264・4-266・4-285 で塞いであります |
| 統合状態 | `main` には未統合。作業ブランチ上で検証・レビューしてから統合する |

**この表はスナップショットです。** 数字を信じる前に上のコマンドを打って
ください。ここが古くなるのは避けられないので、食い違ったら実物が正です。

最初の9コミットは、接触配置で生じたテッセレーション境界欠陥の修復、回帰ゲート化、
および実測結果の文書化です。その後、ローカル広域ゲート、配置不変性、円周ブレンドの
実装・検証を追加しました。現在の技術的な到達点は下の「現在の状態」と4-103、
次に着手する候補は3-Nを参照してください。

引継ぎ作業では、次を守ります。

- 作業前にブランチと差分を確認し、意味のまとまりごとに戻せるコミットを作る。
- 長時間のビルド・全テスト・大規模調査へ入る前に、見込み時間と実行範囲を示す。
- 実装と検証が変えた現在地点を、作業終了時にこの文書と該当仕様書へ追記する。
- 過去の測定・失敗・判断は原則として残す。誤記訂正か、現状理解を妨げる重複だけを
  必要最小限に直し、新しい測定で以前の記録を上書きせず、時系列で追記する。

---

## 1. まず動かして状態を確認する

```bash
bash tools/fast_test.sh
```

**速くはなりません。実測で 13分36秒（816秒）、`cargo test` の約13分と
ほぼ同じです**（2026/08/24、20並列。4-73）。バイナリを並列に走らせては
いますが、このスクリプトが拾う「170 バイナリ」には**プローブ（examples、
73本）が入る**ので、被覆が増えたぶん時間もそこに行きます。

**それでもこちらを先に置いているのは、被覆が広いからです。** テストに加えて
プローブのゲートがそのまま掛かり、非ゼロ終了すれば落ちます。現在は170実行単位です。速さが要るなら
`"kind":["example"]` を外して測ってください（**内訳はまだ測っていません**）。

テストだけ回すならこちら。

```bash
cargo test --release --workspace
```

**`--exclude zenith_py` はもう要りません**（2026/08/30、4-198）。`abi3` と
`generate-import-lib`、それに `.cargo/config.toml` の `PYO3_NO_PYTHON=1` で、
**Python を入れていない環境でも建ちます**。外していた間、Blender へ届く層は
1度も測られていませんでした。

```bash
PYO3_PYTHON="C:/Users/hinat/AppData/Local/Programs/Python/Python311/python.exe" cargo test --release --workspace
```

現在の状態（すべて実測）:

| 指標 | 値 |
| :--- | :--- |
| テストバイナリ | **134 バイナリ / 683 テスト、0 failed・0 warnings**（2026/08/30 実測。`cargo test --release --workspace`。**`--exclude zenith_py` はもう要りません**——4-198）。8月28日は 111 / 624 でした（除外あり）。`tools/fast_test.sh` の言う「170 バイナリ」には**プローブ（examples）が含まれます**——`cargo test` は examples もビルドするので、あの数え方だと一緒に拾って走ります。テストは 0 件ですが、**非ゼロ終了すれば落ちます**（そのぶん時間もかかります） |
| テストの数え方（2026/08/27 実測） | **数え方で桁が変わります。突き合わせが要ります。** `cargo test --release --workspace --exclude zenith_py` の実測は（2026/08/27 夕、
再測定）**104 の test バイナリ・609 テスト、0 failed** でした。同じ日の
午前に 50 / 189 と記録しましたが、あれは出力を `grep` に通す途中で
切れていた数え方です。**同じコマンドでも数え方で値が変わります。**
以下は午前の記録として残します——**実行された test バイナリ 50、通ったテスト 189、0 failed、警告 0**（exit 0）でした。同じコマンドで `--no-run` すると **113 の実行ファイル**が建ちます——差の 63 は**examples（プローブ）**で、建つがテストとしては走りません。**上の行の「119 バイナリ・632 テスト」は、この実測と合いません。** 数え方（どのコマンドで、何を数えたか）が書かれていないので、どちらが正しいかこの場では決められません。**次に触る人は、まず数え方を決めて書いてください**——`tools/fast_test.sh` の「170 バイナリ」も examples 込みの数で、同じ取り違えが3か所目です。 |
| 診断プローブ | **常設40本**すべて exit 0（2026/08/28 に4本追加——`foreign_cross_pair_probe`、`face_merge_probe`、`step_representation_probe`、`step_unit_probe`）。**`examples` にあるプローブ 61本のうち、赤にできる（`exit(1)` を持つ）ものは全部ゲートに入っています**（実測。残りは診断専用で、`ZENITH_*_WHY` を読むためのものです）。一覧は CI（`.github/workflows/gates.yml`）と `VERIFICATION_PLAYBOOK.md` の道具表にあります。**ここに名前を並べると必ず古くなる**ので、数だけ書いています |
| **自作立体どうしのブーリアン（45ケース表）** | `boolean_envelope` で **supported 44 / wrong-result 0 / エラー 1**（8月24日は 39 / 0 / 6）。仕事量は曲面評価 **56,340,279 回**（4-77 で 114,842,103 から半分以下に。4-82後の現値）。**残る1件は直す対象ではありません**——`box × cylinder` 接線の差で、**答えのほうが非多様体**なので場所を名指しして断ります（4-74）。**ここが `ok` に変わったら誤答です。** 45ケース外の任意姿勢（傾斜円錐×箱、直交円柱×円柱、偏心球×円柱、傾斜トーラス×箱）を `contact_placement_probe` に拡充し、**B-Rep多様体性は全25件で100%完全多様体（nm brep = 0）** を実測確認（4-104）。
**メッシュのほうも、立体を返す 37演算すべてで非多様体 0 になりました**
（2026/08/27 実測、4-116〜4-126。手を付ける前は 11演算で、その前は測って
いませんでした）。経過は 11演算 → 6演算（4-123、溶接が潰す中点）→
1演算（4-125、鎖の端から解く）→ **0演算**（4-126、端の無い塊に限って
横移動を許す）。**これは 45ケース表そのものではなく
`contact_placement_probe` の実測範囲です。**
**そのあと配置を10足し、曲面どうしのブーリアンが通るようになったので
（4-127、4-128、4-131）、いまは 25配置・75演算で 69 ok / 6 refused、B-Rep
非多様体 0、メッシュ非多様体は 1演算**（`sphere × cylinder (eccentric)` の
差、13本）**です。その1演算は、いままで断られていてメッシュまで来ていな
かった場所なので、退行ではありません。**
**溶接が三角形を潰す一族（4-117、4-118、4-123）は全件 0 になりました。**
この文書は 3-N-2b で「mesh とも異常 0」と書いていましたが、実装と違って
いました。ブーリアン前段境界判定（`bounds_overlap_in_volume`）における不要テッセレーションを完全排除し、直接 $O(N)$ B-Rep AABB判定へ刷新（4-108）。未実装の曲面交差は安全に REFUSED される |
| **他カーネルの立体のブーリアン** | 軸に平行な30配置・90演算で**断るものは無し**、WRONG 0・PANIC 0、恒等式の残差 4.04e-9。**検証つきの公開 API でも 90/90**。**切り手を 27 度傾けても断るものは無し**（180演算、`ZENITH_TILTED=1`、恒等式の残差 7.88e-9、**検証つきの口で 180/180**、4-67、4-68） |
| **実務で普通に出てくる形の読み取り** | フィレット・面取り・複数穴・スロット・ロフト・曲がり管・挽き物・中空・段付き軸の**9検体すべて**を、体積 3e-11 以内・面数一致で読む（`shape_variety_probe`、4-60） |
| Python 往復 | `tools/verify_solid_api.py` 全合格（作る → ブーリアン → 稜を丸める → STEP → 読み直す） |
| **出力用メッシュ** | **測った全分割数で完全閉多様体（Watertight Manifold）**。4〜32分割の全9通り（`mesh_watertight_probe`）に加え、48・64・96・128・192・256 でも open: 0, non-manifold: 0, degenerate: 0 を実測（4-34 解消、4-37 で範囲を確認）。格子パッチ内は適応細分を掛けないので、弦誤差は分割数で決まる（32分割で体積差 1e-3 台）。`zenith_tess` の `weld` に 27近傍空間ハッシュ（Spatial Hashing）と無向三角形重複排除を導入し、微小丸め誤差による頂点溶接漏れを解消（4-104）。**これは全立体の証明ではなく、常設検体の実測範囲です** |
| **断面の輪郭連結** | 溶接距離ではなく**メッシュの位相**（頂点添字・辺添字）で繋ぐ。公差を持たないので、分割数を 4〜256 のどこに振っても閉じる（4-37） |
| **干渉判定 & 空間枝刈り** | 食い込み 5〜0.001 mm の21配置すべてで `Clash`、最近傍B-Rep射影により浅い食い込みも確実検出。隙間のある5配置すべてで報告距離が閉じた式と一致。B-Repトポロジー全層（Solid/Shell/Face/Wire/Edge）およびNURBS凸包から直接 $O(N)$ AABBを計算し、非交差立体の前段枝刈りを高速化（4-107） |
| **最短距離** | 板の中央の上の小球・板の上の角材・めり込んだ立体を含む7配置すべてで**閉じた式と厳密に一致**し、刻みを振っても動かない。**他カーネルから読んだ立体**（球・円柱・円錐・トーラス）に対しても、軸の上・縁の斜め外・頂点の真上・面から 0.0002 の帯を含む36チェックで閉じた式と一致（最悪 3.55e-15、`foreign_distance_probe`、4-69） |
| **断面積 & 断面スライサー** | **解析解と 1.34e-11 以内**（相対誤差 $< 10^{-10}$）。平面のみの断面は従来どおり厳密。**他カーネルから読んだ**球・円柱・円錐・トーラスの断面も、面積・周長・ループ数とも閉じた式に乗り、64→128→256 で誤差が縮む（24チェック・外れ0、`foreign_slice_probe`、4-70／4-73） |
| **重心と慣性** | 直方体・円柱・球・円錐・移動した直方体のいずれも、原点まわりの閉じた式と **1.8e-13 以内**。主慣性モーメントは平行移動でも斜め軸の回転でも動かない（1.5e-15）。**他カーネルから読んだ**球・円柱・円錐・トーラスでも、体積・重心・原点まわりの慣性が閉じた式と **8.49e-11 以内**（32チェック・外れ0、`foreign_inertia_probe`、4-73） |
| **平面を平面として持っているか** | 全23ビルダーで**該当ゼロ**。`FaceMerger` によりブーリアン出口で同一平面パッチを自動併合（L字角柱 14面➔8面、穴あき 16面➔10面） |
| **自由曲面交差 (SSI)** | 4式4未知数ニュートン追跡 ➔ B-splineフィッティング ➔ FaceSplitter面分割（`ssi_probe` 3/3、`face_split_probe` 面積残差 1.46e-13）。フィットした交線が**両方の曲面に 1e-6 以内で乗る**ことを 65 点で検査し、**交線が両パッチを境界から境界へ横断する配置では両方の面を割れる**ことも検査（球 r12 と同軸円柱 r8、交線は解析解 z=8.944 と 1e-6 で一致。`ssi_boolean_surface_test`）。組み立ては面積の和で見ている |
| **4辺グレゴリーパッチ & N辺コーナーブレンド** | `GregoryPatch4::with_ribbons` が4辺のクロス接線を受け取り、内部4点を双子で持って有理的に混ぜる。**4辺すべてで指定した接線に一致**（境界 2.3e-16、接線の残差は差分の刻みに比例して落ちる）。`CornerBlendN` は N = 3, 4, 5 で N 枚を返し、隣り合うセルはリブの上で隙間 0.000e0・法線の食い違い 0.0000 rad（4-37 で0枚問題を解消、4-41 で $G^1$ を実装） |
| **複数面シート厚み付け** | `ThickenBuilder::thicken_shell`。各面を個別に厚み付けして Union するだけで、テストは**同一平面の長方形2枚**（＝箱2つの和）のみ。非平面の隣接シートでの保証はない |
| **ダイレクトモデリング** | `DirectModeling::fillet_solid_edge` などは `EdgeBlender` の別名。直線稜×平面2面、純直円柱・純円錐/円錐台の凸円周、貫通円筒穴口、**円筒ボス根元と段付き軸の小径側90度凹円周**、**平面肩×直円錐ボスの非直角凹円周**、**スロットボス根元（長円凹周）**、**スロット天面（長円凸周）**を、いずれもフィレット/面取りできる。厳密有理トーラス扇形/円錐台/円柱パッチを使い、閉形式除去・追加体積、剛体配置、STEP、mesh 4〜32、OpenCASCADEで検証済み（4-92〜4-106） |
| **歯元トロコイド** | `make_spur_gear_with_root_fillet` はホブ歯先丸みの**包絡線**として歯元を削る。食い込み 0（厳密）、フィレット上の点はすべて工具円に乗る（5.08e-7、θの刻み幅由来）。外端はインボリュートの開始点と 1e-6 rad 以内で一致（`gear_trochoid_test`、4-40） |
| **非STEP/CAD入出力** | バイナリSTL、OBJ、glTF 2.0、**IGES 5.3（Entity 128、OpenCASCADE が5検体すべてを読み枚数・境界箱とも一致。トリムは未出力）**、AutoCAD DXF（レイヤーは4種定義するが自動割り当ては OUTLINE / HOLE のみ。**層は向きで決める**——以前は「最初のループが外形」と索引で決めていて、外形が2つ出る断面で2つ目が `HOLE` に落ちていた。4-112） |
| **出力ファイルを外から解いた検算** | `export_mesh_suite` の**8検体**を、`tools/verify_mesh_exports.py` が STL / OBJ / glTF / DXF から解き直して照合。閉じているか（辺がちょうど2枚共有）・体積が B-Rep と合うか・3形式が同じ体積か・glTF の accessor と base64 の長さが整合するか・DXF の層と向きが断面と合うか。**FreeCAD 不要なので CI に入っている。** ここは 4-111 まで**検査が1つも呼ばれていませんでした**（常に「verified」と印字して 0 で終わる） |
| **Pythonバインディング** | `zenith_cad.pyd`（**4.74 MB の単一DLL**。2026/08/27 実測 4,975,104 バイト。文書は長く 3.85 MB と書いていました）による完全インメモリ In-Process 連携 |
| コンパイラ警告 | **0** |
| ビルダー監査 | **24/24 すべてクリーン** |
| FreeCAD 相互検証 | **27/27 完全一致** |

---

## 2. この作業の考え方（重要）

このカーネルで見つかった欠陥は、ほぼすべて**内部からは正常に見える**種類でした。

- 閉多様体だが答えが違うブーリアン
- 面積がちょうど2倍になる断面
- いくら細分しても収束しない積分
- STEP に書いた瞬間に他カーネルで壊れる立体

**主張ではなく測定で判断してください。** そのための常設ツールが揃っています。

```bash
cargo run --release -p zenith_algo --example builder_audit        # 全ビルダーの健全性と解析解との一致
cargo run --release -p zenith_algo --example boolean_envelope     # ブーリアンの実対応範囲（45ケース表）
cargo run --release -p zenith_algo --example step_import_audit    # STEP 往復と他カーネルファイルの読み込み
cargo run --release -p zenith_algo --example mass_convergence     # 質量積分の収束
cargo run --release -p zenith_algo --example slice_probe          # 断面積と解析解の差
cargo run --release -p zenith_algo --example pcurve_fidelity_probe # p-curve が本当に辺の上にあるか
cargo run --release -p zenith_algo --example foreign_reexport      # 他カーネルのファイルを読んで書き戻す一周
cargo run --release -p zenith_algo --example regularize_probe      # 全周を刻んでも形が動いていないか
cargo run --release -p zenith_algo --example pcurve_derivation_probe # p-curve を導出し直すと答えが変わる面
cargo run --release -p zenith_algo --example face_split_probe      # パラメータ線でない曲線で面を割れるか
cargo run --release -p zenith_algo --example ssi_probe             # 曲面同士の交線が両曲面に乗るか
cargo run --release -p zenith_algo --example boolean_topology_probe # ブーリアンの結果が稜を実体として共有しているか
cargo run --release -p zenith_algo --example face_merge_probe      # 面と稜を実形状の数まで減らせるか
cargo run --release -p zenith_algo --example planar_face_audit     # 平面を NURBS のまま持っているビルダーが無いか
cargo run --release -p zenith_algo --example countersink_range_probe # 皿モミ穴が寸法を振っても作れるか
cargo run --release -p zenith_algo --example inertia_probe          # 重心・慣性・主慣性モーメントが閉じた式に乗るか
cargo run --release -p zenith_algo --example mesh_watertight_probe  # 出力用メッシュが閉じているか（9通りの分割数）
cargo run --release -p zenith_algo --example boolean_gate_probe     # ブーリアンの検証ゲートの判定が分割数で変わらないか
cargo run --release -p zenith_algo --example slice_robustness_probe # 断面が平面の置き方と分割数によらず閉じるか
cargo run --release -p zenith_algo --example distance_probe         # 最短距離が閉じた式に乗り、刻みで動かないか
cargo run --release -p zenith_algo --example interference_depth_probe # どこまで浅い食い込みを検出できるか
cargo run --release -p zenith_algo --example tess_density_probe     # 三角形数が頼んだ分割数に対して素直に増えるか
cargo run --release -p zenith_algo --example export_iges_suite      # IGES を書き出す（突き合わせは tools/verify_iges.py）
```

`boolean_topology_probe` は共有されていない稜が1本でもあれば非ゼロ終了します。
リリースゲートに使えます（4-26）。

Python 側の一周（作る → ブーリアン → 稜を選ぶ → 丸める → STEP → 読み直す）は
こちらです。

```bash
cargo build --release -p zenith_py
py tools/verify_solid_api.py
```

`boolean_envelope` は45ケースの走査に**2〜3分**かかります。面の組ごとに
曲面同士の交線を探すためです。遅いことと壊れていることを取り違えないでください。

外部カーネルとの突き合わせ（不一致で非ゼロ終了するのでリリースゲートに使えます）:

```bash
cargo run --release -p zenith_algo --example export_validation_suite
& "C:\Program Files\FreeCAD 1.1\bin\python.exe" tools/freecad_cross_validate.py

cargo run --release -p zenith_algo --example export_showcase
& "C:\Program Files\FreeCAD 1.1\bin\python.exe" tools/verify_showcase.py

cargo run --release -p zenith_algo --example foreign_reexport
& "C:\Program Files\FreeCAD 1.1\bin\python.exe" tools/verify_reexport.py

cargo run --release -p zenith_algo --example export_simplified
& "C:\Program Files\FreeCAD 1.1\bin\python.exe" tools/verify_simplified.py

cargo run --release -p zenith_algo --example export_iges_suite
& "C:\Program Files\FreeCAD 1.1\bin\python.exe" tools/verify_iges.py
```

**突き合わせ相手は、相手の実装が書いたファイルに置いてください。** 自前の
出力どうしを比べているあいだは、寛容な読み手が受け取ってしまう構文違反が
見えません。書き出す STEP がすべて Part 21 の構文に反していたのに、24/24 も
7/7 も緑のままでした（4-37）。`tools/occ_reference_export.py` は
OpenCASCADE 自身に同じ形を書かせるための道具です。

**STEP を書くときは `StepInterop` を通してください。** `StepExporter` を直接
呼ぶと、読み込んだままの形——全周1枚のパッチ、全周1本の辺——で書き出します。
それは OpenCASCADE が正しく積分できない形です（4-8 参照）。自前ビルダーの
出力は元から刻まれているので、通しても何も起きません（実測で分割0件、
体積の移動0）。

ブーリアンには検証ゲート（`BooleanResultVerifier`）が入っています。
①全シェルの閉性 ②演算が含意する体積境界 ③384点の内外一貫性 を確認し、
通らなければ**もっともらしいソリッドではなくエラー**を返します。
踏み込んだ改造を安全に試せるのはこのゲートのおかげです。壊さないでください。

---

## 3. 次にやること

### 3-0-0. 完了した重要品質課題と今後の開発領域

**2026年8月22日の監査で、この表の「完了」は実測に合わせて書き直しました。**
経緯は 4-37 にあります。ここに書いてある「完了」は、**外の物差しに当てた
結果が残っているもの**だけです。

| 項目 | 状態 | 実測で言えること |
| :--- | :---: | :--- |
| **出力用メッシュの完全密閉化** | **完了** | 4〜32分割（`mesh_watertight_probe`）に加え 48〜256 でも open/非多様体/退化すべて 0（4-34、4-37） |
| **STEP の Part 21 適合** | **完了** | 複合エンティティ実体の括弧を修正。`step_conformance_test` が全データ行を走査（4-37） |
| **STEP の面の欠落** | **完了** | Coons / Gordon / 三角パッチを NURBS 化して書くようにした。`step_face_parity_test` が面数の一致を見る（4-37） |
| **断面スライサーの安定性** | **完了** | 輪郭の連結を距離から位相へ。4〜256分割で閉じ、FreeCAD 相互検証が 14/15 から 15/15 に戻った（4-37） |
| **IGES 5.3 出力** | **完了** | Entity 128 を実装（それまでは引数を読まないスタブ）。`tools/verify_iges.py` で OpenCASCADE が5検体すべてを読み、枚数・境界箱とも一致。**トリムは未出力** |
| **SketchSolver 自由度解析** | **完了** | ヤコビアンの列を自由変数に制限。階数が自由度を超える報告は出ない（4-37）。**「冗長」と「矛盾」は未分離** |
| **FaceMerger ブーリアン統合** | **完了** | `BooleanEngine::boolean_solids_exact_simplified`（同一平面の自動併合 14面➔8面）。既定にするかは 3-A のまま未決 |
| **N辺コーナーブレンド** | **完了** | N = 3, 4, 5 で N 枚のパッチを返す（それまでは常に0枚）。連続性は下記の制限つき |
| **自由曲面交差 (SSI) 面分割** | **完了** | マーチングと `FaceSplitter` は実測で健全（`ssi_probe` 3/3、面積残差 1.46e-13）。交線が両曲面に乗ること、および**両方の面を割れること**を検査済み（4-37 の残件を解消） |
| **4辺パッチの $G^1$ 連続** | **完了** | `GregoryPatch4::with_ribbons` が4辺のクロス接線を受け取り、内部4点を双子で有理的に混ぜて**4辺すべてで一致**させる。境界は 2.3e-16、接線の残差は差分の刻みに比例して落ちる（4-41） |
| **STEP の 2D トリム境界** | **完了** | 稜ごとに `SURFACE_CURVE` ＋ 面ごとの `PCURVE`。1稜につきちょうど2本（`step_conformance_test`）。書いている p-curve の質は 3.2e-15〜2.9e-12（4-41） |
| **`zenith_server` の空応答** | **完了** | 実装していない動作は名前を挙げて断る。空のメッシュ・0 の計測値・書かない export を成功として返すのをやめた（4-41） |
| **解析曲面の STEP 出力** | **やらないと決定** | 書き出しは B-spline のまま。理由（認識の危うさ、p-curve の書き直し、いまの経路が緑）と、やるときの筋道は 4-41 に |
| **1ファイルに複数立体 / B-spline 化** | **完了** | 測って**欠陥は出ませんでした**（2立体を2立体として読む、解析曲面版と B-spline 版が一致、いずれも解析解と 1e-13）。ただしスキーマ（AP203/214/242）は FreeCAD が設定を無視するため**測れていません**（4-45） |
| **ミリ以外の単位の STEP** | **完了** | インポーターは単位を1行も読んでおらず、インチのファイルを **16387 倍小さく**（1.46 mm³／解析解 24000）読んでいた。閉じた多様体で形も正しく、大きさだけ違うので**形の検査では捕まりません**。長さの単位を読み、座標と半径類に掛けるようにした。検体は OCC が解析解どおりに読み戻すことを確かめてから置いてある（4-44） |
| **他カーネルの立体のブーリアン（軸に平行な切り手）** | **断るものは無し** | 形ではなく**持ち方**を揃えてから切る（`Regularizer::hold_like_our_own`）。面の境界箱、裏向きの面の巻き方、穴の中の扱い、交線が無いときの包含、面の内側で閉じたループ、穴を持つ平面の分割、交線の当てはめを精度で止めること、p-curve の交点を曲線上で解くこと、多スパンの境界稜を分けること、曲面の切り込みを鎖に分けること、**切り込みが穴を横切る面を uv のアレンジメントで割ること**——ここまで対応。`foreign_boolean_probe` は ok 4 → **26＋NOCUT 4、拒否 0**、WRONG 0・PANIC 0。**検証つきの公開 API でも 90/90**（4-68 で 88/90 から）。恒等式の残差 4.04e-9（4-43、4-46〜4-59、4-68） |
| **他カーネルの立体のブーリアン（傾けた切り手）** | **断るものは無し** | `ZENITH_TILTED=1` で 27 度傾けた配置を足しても、180演算すべてが答えを返す。WRONG 0・PANIC 0、恒等式の残差 7.88e-9。**検証つきの口でも 180/180**（4-68 で 176/180 から。食い違った標本は 0 件）。交線が隣のパッチへ続かない件（4-62）、鎖の受け皿（4-63）、クリップの内外判定（4-64）、稜を端点だけで見分けていた件（4-65）、穴のある面の受け皿（4-66）、穴のある領域の組み立て（4-67）を解消 |
| **STEP の回転面** | **完了** | `SURFACE_OF_REVOLUTION` は**既存10検体に1つも入っておらず**、インポーターは読めませんでした。角度方向を有理2次、断面方向を元の曲線の次数で厳密に組みます。断面が軸を含む平面から外れていたら断ります。挽き物の検体を足し、母線から独立に積分した値と 3e-11 で一致（4-60） |
| **実務で普通に出てくる形の読み取り** | **完了** | フィレット・面取り・複数穴・スロット・ロフト・曲がり管・挽き物・中空・段付き軸の9検体を OCC に書かせて読む。体積・面数とも一致（`shape_variety_probe`）。**内側の殻**を持つ立体も殻2枚として読めています（4-60） |
| **他カーネルの立体のテッセレーション** | **完了** | 全周トーラスが返ってこない（4分割でも 120 秒）、全周の円錐と半球は側面がメッシュから丸ごと消える（三角形20枚・高さ 0）——どちらも解消。10検体すべてが正しい境界箱でメッシュになる（4-42） |
| **歯元トロコイド** | **完了** | ホブ歯先丸みの包絡線として創成。定義（食い込み 0、全点が工具円に乗る）と、インボリュートとの独立な突き合わせで検査（4-40） |
| **複数面シート厚み付け** | 部分的 | `thicken_shell` は各面を個別に厚み付けして Union するだけ。検体は同一平面の長方形2枚のみ |
| **DXF のレイヤー分け** | 部分的 | 4レイヤーを定義するが、自動割り当ては OUTLINE / HOLE のみ。線種は全て CONTINUOUS、HATCH エンティティは未出力。**割り当ては向きで決めるようにしました**（4-112。それまでは索引で、外形が2つ出る断面の2つ目が `HOLE` に落ちていた） |
| **STL / OBJ / glTF / DXF の外部検算** | 部分的 | 8検体を書き出して解き直す常設ゲートを作りました（4-111）。**解いているのは自前のパーサ**で、他人の実装には1度も読ませていません。`gltf-validator`・FreeCAD の `importDXF`・OCC の STL/OBJ リーダに読ませるのが次の一手 |
| **抜き勾配（`extrude_wire_with_draft`）** | **完了** | 面ごとのオフセットで作り直し、指定角度と体積が閉じた式と一致（4-109）。**直線の辺だけ**。円弧を含む輪郭は理由を返して断ります |
| **断面が面に接するとき** | 部分的 | 丸めの境目にかかる配置を名指しで断るようにしました（4-110）。**面の上ちょうどは上下で答えが違います**（底面は面そのもの、天面は空）。規約としては筋が通っていますが、直していません |
| **2Dスケッチ（ソルバー）** | 部分的 | `SketchSolver` は動く（LM 法、SVD の自由度解析、拘束12種、点・線分・円）。**円弧が無い**ので実務の輪郭はほぼ描けない。「冗長」と「矛盾」も未分離 |
| **2Dスケッチ（3Dとの接続）** | 未着手 | 閉領域の抽出、ワークプレーンの写像、スケッチからの押し出し/回転は**いずれも無い**。`SketchSolver` を使っているのはテストとプローブだけで、3D 側と繋がっていない |
| **2Dスケッチ包括アーキテクチャ** | 文書のみ | `docs/sketch_system_comprehensive_architecture.md` は設計書。実装済みの範囲は各表に「済/未」で印を付けた（2026/08/23） |

**未測定の項目を上に置いてあります。** このリポジトリで見つかった欠陥は、
ほぼすべて「測っていなかった領域にプローブを当てたら出てきた」ものです。
対応範囲を広げるより、まだ測っていないところを測るほうが期待値が高い状態が
続いています（4-32、4-35、4-36 はいずれもそれで出ました）。

プローブを書くときの注意が2つあります。どちらも実際にやらかしました。

- **通る値を1つ選ばない。範囲を振る。** メッシュの検証を 4 分割と 16 分割
  だけで測って「閉じた」と報告し、6・10・12・20・24 で開くことに後から
  気づきました（4-34）。皿モミ穴も、寸法を1組しか見ていなかったので
  64組中7組の失敗が隠れていました（4-32）。
- **閉じた式を先に確かめる。** 円錐の慣性で、頂点まわりの式を底面中心まわりに
  使って 59% のずれを見つけたと思い込みました。誤っていたのは probe 側でした
  （4-33）。カーネルを疑う前に、自分の式を疑ってください。

---

残りは性質の違う塊に分かれます。上から順に大きい仕事です。
### 3-N. いま最優先のもの（2026年8月26日時点）

**ここから始めてください。場所と、何が起きているかまで測ってあります。**

#### 3-N-1. `boolean_envelope` の45ケースは、直すべきものが 0 件になりました

| | 前（8月24日） | いま |
| :--- | ---: | ---: |
| supported | 39 → 41 | **44** |
| エラー | 6 → 4 | **1** |
| wrong-result | 0 | **0** |

**残る1件は直す対象ではありません。** `box × cylinder`（接線）の差で、
**答えのほうが非多様体**です。場所を名指しして断ります（4-74）。実装しても
返せません。**ここが `ok` に変わったら、それは誤答です。**

経緯は 4-74（接触配置）、4-78（極まわりの4つの欠陥）、4-79（射影が極を
返していた）、4-80（交線は相手が既に稜として持っていた）にあります。

#### 3-N-2. 45ケースに無い置き方 — 分かっていた1件は済（4-82）

**表が緑になったことと、その形が通ることは別です。** ここに置いていた
「球を自分の軸まわりに 45 度回して、同じところで切る」は**片づきました**
（4-82）。弧の端点が極から 2.361e-5 → **5.33e-15**、3演算とも通り、
intersection は半球の解析解に乗ります。

原因は「接する縁に、交点として着地しようとしていた」ことでした。重根なので
残差を詰めても位置は $\sqrt{\varepsilon}$ でしか決まりません。**極値として
解けば単根**で、倍精度まで詰まります（4-81 で診断、4-82 で実装）。

**この節の趣旨は残ります。** 45ケースの表に無い置き方には、まだ欠陥がある
はずです。**同じ形を置き方だけ変えて測る**、が今のところいちばん当たる手です
（5章）。次に測るなら、たとえば:

- 球を**軸以外**のまわりに回す（極が切断平面から外れる）
- 円柱・円錐を継ぎ目が平面に重ならない向きに回す
- 2つの曲面立体を、どちらも回して当てる

**`contact_placement_probe` に足していってください。** あのプローブは
「断られること自体は赤にしない、非多様体を返したら赤」という立て方なので、
まだ通らない配置を入れても運用を止めません。

**2026/08/27 に、そのとおりにしました。**「触っているだけ」の置き方を5つ
足したところ（点・線・面・円・母線）、**15演算のうち 4演算が断られ、そこから
2つの欠陥が出ました**（4-127、4-128）。**この手はまだ当たります。**

いまの状態: **20配置・60演算で 55 ok / 5 refused**、B-Rep 非多様体 0。
断りの5件のうち**3件は規約どおりの名指し拒否**（答えが本当に非多様体）
なので、**未実装は 2件だけ**です——`box × torus (top tangent plane)` の
和と積。トーラスの上端が箱の面と**半径 12 の円まるごとで**接する配置です。

次に測るなら、まだ手つかずなのは:

- 2つの曲面立体を、**接する**ように当てる（上の6演算は交わる配置でした）
- トーラスと円柱・球のように、**どちらもパッチに割れている**曲面どうし
- 同じ形を、**寸法比**だけ変える（等半径だけが落ちていた、というのが 4-128 でした）

#### 3-N-2c. 箱と円柱を両方回すと断られていました（2026/08/27。**解決**）

**塞がりました**（4-122）。`box × cylinder (both turned)` は3演算とも通り、
`contact_placement_probe` で立体を返す演算は 34 → **37**、断られる演算は
11 → **8** になりました。B-Rep 非多様体は 0 のまま、`boolean_envelope` も
44 / 0 / 1 で不変です。

**原因は、この節が最初に書いていたものとは違いました。** 経緯を残します
——同じ読み違いを繰り返さないためです。

真の原因は**面積の検算が安い物差しだけで断っていた**ことでした。箱の側面は
交線の鎖で正しく2枚に割れていて、3D で測れば残差 4.3e-16 です。ところが
パラメータ面積で測ると 2.176e-6 で、閾値 `1e-6` をわずかに超えて捨てられて
いました。面が**導出した p-curve しか持たない**とき、その忠実度（1e-7 台）が
そのまま残差に出ます。**パラメータ側が断ろうとしたときだけ 3D で測り直す**
ようにして解決しました（4-122）。

以下は経緯です。

箱と円柱を**両方**回すと、3演算とも断られていました（片側だけなら通ります）。

**この節は最初、「円柱を斜めの平面で割れないのが原因」と書いていました。
測ったら違いました**（4-121）。読んでいたのは断り文の中の

```
Cylinder-side split edge must be a ruling along the patch axis;
Section edge is not a circle about a[xis]
```

という文字列ですが、**あれは受け皿を順に試したときの理由を連結したもの**で、
最後の汎用経路（`FaceSplitter::split_by_chain`）はその面を**実際に割って
います**（`ZENITH_SPLIT_WHY=1` の `CHAINWHY` で確認）。**断り文の途中経過を
結論と読み違えていました。**

本当に止まっているのは**蓋の段**です。切り口の輪が1本もできません
（`0 cap loops`、`0 cap faces`、その結果 `7 unmatched edge uses`）。

原因は2つあり、1つ目は直しました。

1. **蓋の材料を、対の `face_b` 側の面ごとに束ねていた**（4-121。**済**）。
   B が平面ならそれで合いますが、**B が曲面だと合いません**。切り口の楕円は
   円柱の4枚のパッチにまたがるので、パッチごとに2本ずつに割れて、どの束も
   輪になりませんでした。平面側の面で束ねるように直し、1つの束に4本
   集まるようになりました
2. **それでも輪になりません**（未解決）。集まった4本は**開いた鎖**です:

   ```text
   (11.9589 4.1178 0) -> (12 4.9047 0) -> (6 11.6387 0) -> (0 4.9047 0) -> (1.4596 0.5026 0)
   ```

   端が繋がっていません。切り口は**箱の面の自分の境界を通って閉じます**
   ——円柱が箱の面を貫き切らず、面の縁から出ているからです。いまの蓋作りは
   **交線だけで閉じる輪**しか受け付けません。

**さらに測ると、欠けているのは蓋ではなく面の片でした**（4-121）。相手を
欠いている7本は1つの閉じた輪をなし、前半は交線の弧、後半は**箱の稜**です。
箱の側面のうち円柱の外に残るべき領域が、採られていません。採否そのものは
`ZENITH_SELECT_WHY=1` で読めます。**ここは測り切れていません。**

**45ケースにも既存のプローブにも出てこなかったのは、どれも片側しか回して
いないからです。** 両方回した瞬間に、この経路へ落ちます。

#### 斜めの断面の形（測ってあります。この節の主原因ではありませんでしたが、事実として有効です）

円柱の側面パッチのパラメータ面では、斜めの断面は

```text
v = (a + b·u + c·u²) / (1 + d·u + e·u²)
```

という**有理2次の1本の曲線**です。10〜80度の5通り × 4パッチ = 20通りで、
当てはめ残差の最悪は **4.346e-12** でした。

**「v = a·cos u + b·sin u + c」ではありません。** 最初そう書きましたが、
測ったら残差 1.19e-2 で外れました。`u` は幾何的な角度ではなく有理2次の
媒介変数なので、角度は `u` の非線形な関数です（4-120）。

有理2次なら **p-curve として厳密に持てます**。いまは汎用の面分割器が
割っているので**近似が入ります**。専用の口を足せば厳密になりますが、
**それは上の蓋の問題とは別で、いま断られている原因ではありません。**

```bash
cargo run --release -p zenith_algo --example oblique_section_probe
```

#### 3-N-2b. ブーリアンが割った曲面のメッシュ非多様体（4-89で常設検体は解消）

**常設の15配置・45演算のうち 37演算が立体を返し、B-Rep は異常0、mesh は
6演算が異常です**（2026/08/27 実測、4-116〜4-123。4配置は 4-119 で足し、
3演算は 4-122 で通るようになり、mesh は 4-123 で 11演算から落ちました）。**この節は長らく「7配置・21演算とも異常0」「probe は mesh 異常も
exit 1 にするラチェットへ昇格した」と書いていましたが、どちらも実装と違って
いました。** 4-104 で足した4配置（円錐・直交円柱・球×円柱・トーラス）に mesh が
赤になるものが入り、記述だけが残っていたものです。

いま残っているのは 6演算です（2026/08/27 実測）。原因は4つ見つかり、
3つ直しました。

1. 境界の点が一直線に4点以上並ぶところで earcut が作る面積0の耳（4-116。**済**）
2. 境界の点を落とす基準が溶接の距離より5桁細かく、その隙間の標本が
   溶接で潰れて外されていた（4-118。**済**。torus の difference は 123 → 2）
3. 適応細分が、ほとんど重なった別々の辺の中点を別々の頂点として作り、
   `weld` がそれを束ねて三角形を潰していた（4-123。**済**。11演算 → 6演算。
   外れた手が3つあるので、そちらも 4-117 / 4-123 に残してあります）
4. 残る6演算は、**共有境界の上に出る細長い片を2枚の面が両方出す**ことが
   原因でした（4-124）。入れ替えを**鎖の端から**解いて 6演算 → 1演算
   （4-125。**済**）、端の無い塊に限って横移動を許して **1演算 → 0演算**
   （4-126。**済**）。**この一族は全件 0 になりました。**
   以下は当時の記録として残します。**溶接が三角形を潰す一族（上の3つ）は
   全件 0 になりました**——残っているのは全件が「同じ3頂点の三角形が2枚
   出て、片方が落ちた跡が穴になる」ほうです。**「面片が重なっている」と
   一度書きましたが、測ったら違いました**（同一平面の対は1組も無く、
   重複した三角形は3点がほぼ一直線に並ぶ細長い片でした）。直す場所は
   メッシュ側で、4-117 の `FLIPWHY`（入れ替えても悪い枚数が減らない）を
   解くところからです

4-83で最初に測った球の回転切断は、B-Repの稜がすべて
ちょうど2回使われている（例外0）のに、メッシュには非多様体の稜が24分割で
247本、48分割で999本あり、三角形数も27,562 → 234,129と膨らんでいました。
この球面側は4-87〜4-88で0まで解消しています。

**0 でない立体は STL に書けません。** 第1章の「測った全分割数で完全閉多様体」は
`mesh_watertight_probe` の検体についての実測で、**ブーリアンが割った曲面の
結果はそこに入っていません。**

```bash
cargo run --release -p zenith_algo --example contact_placement_probe
# 「nm brep/mesh」の列。2026/08/27 実測では brep 0、mesh は 4演算が異常（4-116）

# どの面とどの面の間で開いているかを名指しする
ZENITH_FACE_OWNER_WHY=1 cargo run --release -p zenith_algo --example contact_placement_probe
```

格子から落ちて earcut ＋ 適応細分に行く側の問題でした（4-71、4-75と同じ一族）。
最後の `box × cylinder (seam turned 33)` unionも4-89で解消しました。

**ラチェットは掛かっていません。** probe が `exit 1` するのは B-Rep 側だけで、
mesh 異常は数えて印字するだけです（4-116 で実測）。掛けるのは、残っている
4演算を直してからです——いま掛けると常設ゲートが赤で止まります。

#### 3-N-3. 速度 — 半分にはなりました。残りはここから（2026/08/25 更新）

**入れ替えは済みました**（4-77）。`boolean_envelope` の45ケースで、曲面の
評価が **114,842,103 → 52,729,734 回（2.18倍軽い）**。交線を求める仕事は
1回も動かず、答えも1つも動いていません（解析解との最悪一致 6.68e-8 も同じ）。

**いまの値は 56,340,279 回です**（2026/08/25 実測、4-82 の後。supported 44 /
wrong-result 0 / エラー 1、最悪一致 6.68e-8）。4-77 の直後は 52,729,734 で、
そのあと 4-79 の受け皿で +0.6%、4-80 で少し戻っています。**上の
52,729,734 は 4-77 時点の記録であって、いまの値ではありません**——現在値は
第1章の表と 9-1 にあります。

**次に大きいのは2つです。**

1. **トリム面1枚が三角形 3万〜16万枚になること自体**は変わっていません
   （4-75）。積む回数が半分になっただけです。枚数を減らすには、earcut ＋
   最長辺二分ではなく**境界に沿った三角形分割**から始める必要があります
   （OCCT は制約付き Delaunay を使っています。4-76）
2. **残る積分は「答えそのものの検算」**（`compute_from_brep`、返す前の検証）
   です。**安くする前に、何を守っているかを読んでください**——ここは
   「見た目は閉じているが答えが違う」を止めている段です（`boolean_validation.rs`）

#### 3-N-4. 次の実用フィーチャー — 非直角・非円形の根元

4-95の貫通穴口局所フィレットに続き、4-96で同じ認識・同じ局所位相交換を使う
**穴口の等距離面取り**まで完了しました。4枚の厳密円錐台を挿入し、除去体積
`π c²(a + c/3)`（穴半径`a`、面取り距離`c`）で独立に採点しています。選択面、
反対面、別の穴を保持する契約も維持しています。

4-97で**円筒ボス根元と段付き軸の小径側90度凹円周**まで完了しました。凹稜では
材料を足すため、`EdgeBlendReport::predicted_removed_volume`を負値にする契約を固定し、
選択肩面・次の平面・未選択段のFace IDを保持しています。自作4分割とOCC全周円、
複数段、剛体配置、STEP、meshを測っています。

4-98で同じ認識を使う**根元面取り**も完了しました。小径`a`・90度肩に対する
等距離`c`を、4枚の次数1×2の厳密有理円錐台として局所挿入します。追加体積
`π c²(a + c/3)`と符号付き除去体積`-π c²(a + c/3)`を独立に採点し、4-97と同じ
Face ID保持・複数段連続適用・OCC全周円・剛体配置・STEP・meshを固定しました。

4-99で**非直角の円錐状ボス根元フィレット**まで完了しました。支持面の半径が
軸方向へ線形に変わることを標本全体で認識し、任意勾配の接触位置をオフセット線から
解いて、4枚の厳密有理トーラス扇形を局所挿入します。閉形式体積、局所Face ID保持、
G1、剛体配置、STEP、mesh 4〜32、OpenCASCADEを赤ゲートにしました。

4-100で同じ非直角根元の**等距離面取り**も完了しました。肩平面上と円錐母線上で
同じ距離だけ後退する端点を直線で結び、4枚の厳密有理円錐台を局所挿入します。
勾配0では4-98の90度根元面取りへ厳密に戻ります。

4-101で純円錐/円錐台の凸キャップ円周の**等距離面取り**も完了しました。
上下リム、真円錐の実頂点、OCC全周円、剛体配置まで同じ選択APIで処理します。

次は非円形ボス根元、一般の可変二面角です。円形へ置換せず、まず一定断面の
楕円または直線区間を持つ根元から、局所Face ID保持と閉形式検体を置いてください。

以下は入れ替え前の記録です。

#### 3-N-3 の経緯 — 場所の特定

**ブーリアン1回の曲面評価の 90% は、面を割ったあとの面積の検算です**
（4-75。交差した円柱で 1500万回のうち 1350万回）。交線を求める仕事でも、
内外を決める仕事でもありません。1枚の面を積むのに三角形 **4万〜16万枚**を
使っています。

**その検算に 3D の面積は要らないはずです。** 割る前と割ったあとで見ているのは
同じ曲面の同じパラメータ領域なので、**パラメータ面積**の和が合えば領域は合って
います。そちらは曲面を1回も評価しません。

道具はもう入っています。

```bash
ZENITH_AREA_CHECK_WHY=1 cargo run --release -p zenith_algo --example chained_boolean_probe
```

3D 側といまの判定が1行ずつ並びます（入れ替えたあとも使えます）。

**食い違いは 138回中 22回ありました。追い切ったら、実装の穴ではなく方針の穴
でした**——潰れた片をどう扱うかを 3D 側は決めておらず、偶然で断ったり通したり
していたのです（4-76、4-77）。決めたら揃いました。

消えたのは 1350万回のうち **1000万回**です（見込みは「大半」でした）。

#### 済んだもの

**「接触の交線を `leftover` に積まない」と書いてあった3件は片づきました**
（4-74）。ただし**書いてあった原因は3件とも違いました**。

| 件 | 前 | 後 |
| :--- | :--- | :--- |
| `box × box` 45度回転 difference / intersection | 非多様体 12 / 22 でエラー | **通る**（体積は閉じた式と一致） |
| `box × cylinder` 接線 difference | 「未実装」でエラー | **場所を名指しして断る**（答えのほうが非多様体） |

- 前者の原因は交線ではなく、**面積 0 の面片**でした。相手の稜がこちらの面の
  境界の上に乗ると、割った結果に「行って戻るだけの切れ目」が混ざり、同じ稜を
  もう2回使います。選別の出口で落とします（`face_encloses_no_area`）。
- 後者は実装しても返せません。円柱が壁に**内側から**接しているので、差は
  材料の厚みが 0 になる2つの塊で、多様体 B-Rep では持てません。規約
  （3-1）どおり断ります。

**「正しい答えをゲートが断る4件」も解消済みです**（4-68）。他カーネルの立体の
ブーリアンは、軸に平行な切り手で **90/90**、27度傾けた切り手を足して
**180/180** が検証つきの口を通ります。

---

### 3-Z. ブーリアン結果のメッシュが重い — 済（4-39）

**解消しました。** 原因は 3-Z にかつて書いてあった `SamplePlan::segments_for_edge`
ではありません（稜の刻みは、実測で両者とも頼んだ分割数ちょうどでした）。
構造格子を張れるかの判定を曲面のパラメータ領域全体で行っていたため、支持曲面が
面より広いブーリアン結果が earcut ＋ 適応細分の経路へ落ちていたのが原因です。

同じ形をビルダーで作ったものと、ブーリアンで作ったものが、いま**同じ枚数**に
なります（比 1.00、48分割で 17.5 倍軽量化）。経緯と数字は 4-39 に、常時の
測定は `tess_density_probe` にあります。どちらの経路を通ったかは
`work_counter` の `grid_patches` / `earcut_patches` で数えられます。

### 3-A. 整理（`FaceMerger::simplify_solid`）を既定にするかの判断

面の併合・平面の認識・稜の連結は入りました（4-31）。**まだ自動では掛けて
いません。** ブーリアンやビルダーの出口に入れると面数・稜数が変わり、面の
添字で対象を指しているコード（`push_pull_face(solid, face_index, ..)` など）の
意味が変わります。既定にするなら、

1. `boolean_solids_exact_result` の出口（`Sewer` の直後）
2. `HoleBuilder` など、割ってから組み直すビルダーの出口

のどちらから入れるかを決めてから、全ゲートを通し直してください。判断材料は
4-31 の表と `tools/verify_simplified.py` の結果です。

**注意**: これは `Regularizer` とは別物です。`Regularizer` は「全周1枚の
パッチを刻む」ほうで、向きが逆です。

### 3-B. 平面を NURBS で持つビルダー — 済（4-32）

`crate::validated_solid` が全ビルダーの出口で平面を平面に直すようになりました。
`planar_face_audit` が常時数えており、1枚でも残れば非ゼロ終了します。

ただし**持ち直しはあくまで出口での修正**です。`make_drilled_box` と
`GearBuilder` は依然として NURBS パッチとして面を組み立てており、出口で
直されているだけです。作るところから平面にすれば、余計な変換も、
`planarize` が見落とす形（制御点が公差ぎりぎりに散る面など）の心配も
なくなります。急ぎではありませんが、触るときは 4-32 の表を先に見てください。

### 3-0. 実務で先に当たるのは、45ケースに無い形です

`boolean_envelope` の45ケースは全部**素直な配置**で、桁も揃っています。
実務のデータが持ち込む性質——桁差、同じ形どうし、ナノメートルの隙間、
自分の出力を入力に戻すこと——は1つも入っていません。**プローブを2本足した
だけで実欠陥が5件出ました**（4-22、4-23、4-24）。うち2件は誤答です。

したがって、次に手を付ける順は「対応ケースを39から増やす」より、
**まだ投げていない形を投げる**ほうが期待値が高いと考えています。

```bash
cargo run --release -p zenith_algo --example robustness_probe
cargo run --release -p zenith_algo --example closure_probe
cargo run --release -p zenith_algo --example foreign_boolean_probe
```

**この考え方は 2026/08/23 にもう一度当たりました。** 3本目は「他カーネルの
立体を読んで、それにブーリアンを掛ける」経路で、そこだけプローブがありません
でした。1本書いたら、**返ってこない**のが1件と、**側面がメッシュから丸ごと
消える**のが2検体出ました（4-42）。どちらも既存のゲートは全部緑のままです。

4-24 の「穴あき立体を箱で削れない」は**5配置すべて解決済み**です。ここに
「残り2件」と書いてあったのは古い記述でした。

いま分かっている穴でいちばん実務に近いのは、**読んだ立体に対する厳密
ブーリアンの守備範囲**です。ok 4 → **18** まで広げました（4-43、4-46）。

**「次は NURBS×NURBS 交差」と読むのは、2度とも外れました。** 拒否を検体ごとに
数えると、主役は曲面同士の交差ではなく、特定の立体が平面の切り手でも全滅する
ことでした。次に手を付けるときも、**まず拒否を検体ごとに数えてください**
（`foreign_boolean_probe` の出力を検体で集計するだけです）。残る 28 件の
内訳は 4-52 にあります（残り15件）。

### 3-1. 通らない件の内訳は、この文書が書いていたものと2度違いました（2026/08/24）

**この節はずっと「残る6件はすべて接線配置」「box × cylinder 3件、
box × sphere 3件」と書いていました。`boolean_envelope` を測ったら、どちらも
違いました。そのあと書き直した「機構は2つ」も、測ったら違いました**（4-74）。

いまの実測（`boolean_envelope`: **supported 44 / エラー 1 / wrong-result 0**。
2026/08/25 更新）:

| 件 | 演算 | いまの状態 |
| :--- | :--- | :--- |
| `box × box`（Z軸まわり45度回転） | difference, intersection | **通る**（4-74。原因は面積 0 の面片だった） |
| `box × cylinder`（側面に接する） | difference のみ | **断る**（4-74。**答えのほうが非多様体**。場所を名指しします。直す対象ではありません） |
| `box × sphere`（継ぎ目に重なる置き方） | union, difference, intersection | **通る**（4-80。交線は球が既に稜として持っていた） |

**`box × box` の回転配置は接線配置ですらありませんでした。** 相手の稜が
こちらの面の境界の上に乗るだけで、材料は割れません。

5章の「写した数字は残る」がまた出ました。**表を書くときは、その行を測った
出力が残っているかを先に確かめてください。**

#### 規約は決めました: 接触は、それ自体では位相を作らない（2026/08/24）

「接触は交差ではないと決めるのか、非多様体を返せるようにするのか」は、
この文書が長らく**判断を保留していた**ものです。決めました。

**2つの曲面が交わらずに触れているだけの場所には、交線を作りません。**
ブーリアンは本当に横断している交差だけで位相を組み立てます。そのうえで、

- 真の答えが多様体の立体になるなら、**返す**
- 真の答えが**本当に非多様体**（線や点で接したままの和）なら、**名指しして断る**

`Solid` は多様体 B-Rep です。非多様体を「もっともらしい立体」にして返すのは
誤答で、このカーネルがいちばん避けてきた種類の失敗です（第2章）。**断るのは
実装の不足ではなく、型が表現できないものを表現したふりをしない、という
設計判断です。**

#### 予想を2つ書いて、2つとも外しました（2026/08/24）

規約を決めたあと、「どれが多様体になり、どれが真に非多様体か」を**予想で**
書きました。`contact_placement_probe` を書いて測ったら、**2つとも外れて
いました**。

| 予想 | 実測 |
| :--- | :--- |
| `box × sphere` の union は非多様体なので断るべき | **継ぎ目を踏まない置き方では、多様体の立体が返る**（非多様体の稜 0） |
| `box × cylinder` 接線の union は非多様体なので断るべき | **多様体の立体が返る**（非多様体の稜 0） |

**「非多様体になるはずだ」は、測らずに書いてよい種類の主張ではありません。**
返ってきた立体のメッシュで、稜がちょうど2枚の三角形に共有されているかを
数えれば決まります。`contact_placement_probe` はそれを数えます。

ついでに1つ、良い性質が測れました。**このカーネルは、非多様体を立体として
返したことが一度もありません**（測った12演算すべてで 0）。断るときは断って
います。

#### `box × sphere` は、切る平面が**球の極を通る**問題でした（2026/08/24）

**ここでも予想を1つ外しています。** 最初「継ぎ目との一致が原因」と書きました
が、`ZENITH_SPLIT_WHY=1` で機構を見たら違いました。

同じ「4面が接する」配置を、向きだけ変えて2つ測ります。

| 置き方 | 中心を通る平面 | 結果 |
| :--- | :--- | :--- |
| 球の中心 `(20, 10, 10)`（`boolean_envelope` と同じ） | `x = 20`。**球の極 `(20,10,0)` と `(20,10,20)` を両方通る** | 3演算とも断る |
| 球の中心 `(10, 10, 20)` | `z = 20`。極を通らない（赤道の面） | **3演算とも ok** |

接している面の数はどちらも4枚で同じです。**違うのは、切る平面が極を通るか
どうか**。

診断が出した交線がそのまま答えです。

```text
SPLITWHY (20.0000 10.0000 -0.0000) -> (20.0000 10.0000 -0.0000)  Split edge is degenerate
SPLITWHY (20.0000 10.0000 20.0000) -> (20.0000 10.0000 20.0000)  Section edge has collapsed to a point
```

**始点と終点が同じ点で、しかもその座標は球の極そのものです。** 極では
パラメータが潰れるので、交線もそこで点に潰れます。潰れた交線は何も割れず、
面片が足りないまま縫合に回ります。

**今日 `normal_or_limit` で直したのと同じ family です**（4-69）。あちらは
極で法線が取れずに足を落としていた件でした。**回転面の極は、このカーネルの
弱点が集まる場所**だと思ってください。

#### 失敗の形が、件によって違います

| 件 | unmatched | 非多様体 | 同方向 | 読み |
| :--- | ---: | ---: | ---: | :--- |
| `box × box` 45度回転（当時） | **0** | **12 / 22** | 0 | 面片が**多すぎる** |
| `box × sphere` | **12** | **0** | 0 | 面片が**足りない** |

**向きが逆です。** 片方を直しても、もう片方は動きません。

#### 「機構は2つ」と書きましたが、測ったら3つでした（4-74）

この節にはこう書いてありました——**機構A（3件）: 割れなかった接触の交線が
`leftover` に積まれ、組み立てで同じ稜を重ねて使う。落とせば3件とも片づく。**

**3件とも、そうではありませんでした。**

| 件 | 書いてあった原因 | 実測の原因 |
| :--- | :--- | :--- |
| `box × box` 45度回転 difference / intersection | 接触の交線が `leftover` に積まれる | **面積 0 の面片**。交線は正しく、割った結果に「行って戻るだけの切れ目」が混ざり、同じ稜をもう2回使っていた |
| `box × cylinder` 接線 difference | 同上 | **答えのほうが非多様体**。円柱が壁に内側から接しているので、差は厚み 0 で2つに割れる。実装しても返せない |

面片が多すぎるという読みは合っていました。**多かったのは交線ではなく面片
そのもの**です。詳しくは 4-74。

**機構B: 交線が極で点に潰れ、面片が足りない。** `box × sphere` だけが
こちらで、これは残っています（3-N-1）。

なお、**曲面同士が交わるブーリアンの実装としてはひととおり通っています**。
次に大きいのは 3-4（一般トリム境界を持つ巻き付き面）と性能です（下記）。
3-3（歯車）は歯形をインボリュートにしたので、残りは歯元と軸穴だけです。

### 3-1b. 性能 — 壁時計では判定できない。数え上げで測る

**どこが重いかは 4-75 で測ってあります。** 手を付ける前にそちらを読んで
ください。主犯は交線でも内外判定でもなく、**面を割ったあとの面積の検算**
（曲面評価の 90%）です。予想を2つ立てて2つとも否定した記録も残してあります。

**この環境の壁時計は、この規模の比較に使えません。**
同じバイナリで `boolean_envelope` の45ケースを2回走らせた実測:

| | run1 | run2 |
| :--- | ---: | ---: |
| 実時間 | **5分07秒** | **7分32秒** |
| 曲面の評価回数 | 176,674,143 | 176,674,143 |
| マーチングのニュートン反復 | 2,890,068 | 2,890,068 |
| 辿り開始回数 | 11,928 | 11,928 |

**時間が47%違い、仕事量は1桁も違いません。** 1ケース単位ではもっと荒れ、
やる仕事を確実に減らした側が14秒**遅く**出たこともあります。この文書に
かつて載っていた「3分11秒」「11分」「96.6 → 62.2 秒」はこの土俵の数字で、
再現しません。**性能を主張するなら `zenith_geom::work_counter` の数を使って
ください。** 走らせるたびに同じ値になります。

`boolean_envelope` は `evals` 列と、末尾に3つの積算を出します。

```text
solve work: 108381805 surface evaluations, 1134888 marching Newton iterations, 5724 marches
```

**上の表（run1 / run2）の数とは違います。** あの表は「同じバイナリを2回
走らせても積算は1回も動かない」ことを示すためのもので、そこは今も成り立ち
ます。ただし**絶対値のほうは、その後どこかで変わりました**（2026年8月23日の
実測）。**`worst analytic agreement` も動いていました。** 2026年8月24日に
`main` で測り直した値は **6.68e-8（sphere x sphere intersection）**、
`solve work` は **112,006,172** です（4-68）。動いていないのは 39/45 と
誤答 0 と45行の体積で、そこは1文字も変わりません。いつ変わったかは
追えていません。

したがって、積算を「前より減ったか」の物差しに使うときは、**比べる前に
自分でもう一度測ってください。** 文書に貼ってある数は、貼った日のものです
（この文書自身が 5章の「写した数字は残る」を踏みました。検証手順書の
`cylinder_nurbs` も同じで、4-19 で直ったあと古い 12566.6236 が残っていました）。

数が減っても時間が減るとは限りません（数えている単位の重さが一定とは
限らない）。言えるのは「同じ答えに、より少ない仕事で到達した」までです。

済んでいること:

- 「交わるか否か」だけの問いは交線を求めずに答える（`any_face_pair_may_intersect`）。
- 選別とキャップで候補を共有する。
- **失敗経路の重複走査を落とした（2026年8月21日）。** `prepare_exact_boolean` は
  報告のための数え上げなのに、`collect_boolean_shell_assembly` が走り終わった
  後に、面の組の走査をさらに5回やり直していました。`collect_face_pair_candidates`、
  `collect_intersection_edge_candidates`、`collect_planar_face_split_candidates`、
  `collect_classified_planar_face_split_candidates`、`collect_boolean_shell_assembly`
  が順に呼ばれ、後ろのものは前のものを内側でやり直します。走査する関数と、
  走査結果から導く関数を分け（`*_from_face_pairs` / `*_from_edge_candidates` /
  `*_from_splits`）、組み立て結果に面の組の数と交線候補を載せて、報告は
  それを数えるだけにしました。

  | | 前 | 後 |
  | :--- | ---: | ---: |
  | 曲面の評価回数 | 192,733,612 | **176,674,143**（−8.3%） |
  | ニュートン反復 | 3,260,724 | **2,890,068**（−11.4%） |
  | 辿り開始回数 | 18,840 | **11,928（−36.7%）** |

  効くのは失敗経路だけなので、45ケースのうち**変化したのは接線配置6件だけ**で、
  box × sphere は 5,916,998 → 1,747,327（−70%）です。**成功する39件は1回も
  動きません**——それが形を変えていない証拠です。表も45行すべて一致します。
  壁時計では「差なし」としか出ませんでした。

残っていること: 面の組ごとの結果を1回のブーリアン内で覚えていません。
**面の `id` を鍵にした記憶は、いまは安全です**——かつて `regularize` の割った面が
`id: 0` を共有していた問題は解消済みで、`regularize.rs` の
`every_face_that_comes_out_of_a_split_has_its_own_id` が見張っています。
`id` は `FACE_ID_GEN` の大域カウンタなので、2つの立体をまたいでも衝突しません。

**着手するならここから**: 重いのは成功経路です。実測で `torus x box` の積が
単独で最も重く、`cylinder x cylinder` の3演算がそれに続きます。まず
`boolean_envelope` の `evals` 列で重い順を確かめてから手を付けてください。

### 3-2. 接線配置 — 規約は 3-1 で決めました。残るのは実装です

**「接触は、それ自体では位相を作らない」**と決めました（3-1）。ここから先は
定義の問題ではなく、実装の問題です。

手を付けるときの順は、実測の内訳（3-1 の表）に従ってください。

1. **`box × box` 45度回転の difference / intersection。** これは接線配置では
   ありません。**2026/08/24 に名指ししました**（下記）。ここから始めるのが
   いちばん近道です。
2. `box × cylinder` 接線の difference。接触は体積を囲まないので、答えは A
   そのものです。
3. `box × sphere` の intersection と difference。5面目が中心を通って本当に
   切っているので、答えは半球（とその差）です。**4面の接触では交線を作らない**
   ——これが規約の効くところです。
4. `box × sphere` の union は**断ったままにします**。半球の縁が箱の面の境界に
   4点で触れ、真に非多様体だからです。理由を名指しして断ってください。

なお box × sphere の5面目（球の中心を通る平面）は、球パッチの境界と
ちょうど重なります（球は経度 0/90/180/270 で分割）。切る必要が無いので、
「平面が既存のパッチ境界と一致するなら分割不要」と分かればそのぶんは進みます。

#### `box × box` 45度回転 — 断っている理由（実測 2026/08/24）

置き方は `boolean_envelope` のとおりです。A は `[0,20]^3` の箱。B は
`(10, 10, 0)` だけずらしてから、**世界原点まわりに** 45 度回したもの
（箱の中心まわりではありません。中心まわりに回した配置は3演算とも通ります
——ここを取り違えると再現しません）。

パイプラインが返す報告をそのまま置きます。

| 演算 | 選ばれた面片 | 非多様体の稜の使用 | 不一致の稜 | 同方向の稜 |
| :--- | ---: | ---: | ---: | ---: |
| union | — | — | — | 通る |
| **difference** | 8 | **12** | 0 | 0 |
| **intersection** | 7 | **22** | 0 | 0 |

**不一致 0・同方向 0 です。** つまり、

- 面片の**取りこぼし**ではありません（不一致 0）
- 裏返った面片が混じっているのでもありません（同方向 0。4-67 で踏んだのは
  こちら）

稜が3枚以上の面片に使われている——**面片が多すぎます**。

前段の報告に **`4 skipped batch splits`** があります。これは「割るのを
飛ばした**面**」ではなく、**面を1枚も割れなかった交線の本数**です
（`brep_intersection.rs` の `skipped_split_count`）。

#### 飛ばされた交線の正体（実測 2026/08/24）

`ZENITH_SPLIT_WHY=1` を付けると、割れなかった交線とその理由が1行ずつ出ます
（`GRIDWHY` と同じ流儀で足しました）。出たものは:

| 交線 | 理由 |
| :--- | :--- |
| `(5.8579, 20, 0) → (0, 20, 0)` | 面の内部を横断していない |
| `(0, 20, 0) → (0, 14.1421, 0)` | 同上／外側境界に乗っていない |
| `(0, 14.1421, 0) → (0, 14.1421, 20)` | 同上 |
| `(0, 14.1421, 20) → (0, 20, 20)` | 同上 |

**座標が答えを言っています。** 14.1421 = $10\sqrt2$、5.8579 = $20 - 10\sqrt2$。
B の角 `(10, 10)` を原点まわりに 45 度回すと `(0, 14.142)` ——**B の頂点が
A の面 `x = 0` の上にちょうど乗っています。**

**つまりこれは接触配置です。** 曲面同士の接線ではなく、**頂点が面に乗る
接触**。交線が A の面の境界にぴったり沿って走るので、どの面も割れません。
そして**割られないまま残った交線が、縫合で稜を重複させています。**

#### だから、3-1 で決めた規約がそのまま効きます

「接触は、それ自体では位相を作らない」。**面の境界に沿って走る交線は、
切り込みではなく接触の記録**です。組み立てから外すのが正しい。

5章にも「境界沿い交線の除外（それは接触の記録であって切り込みではない）」
とあり、回転ボックス対応で必要だった4つのうちの1つに挙がっています。
**そこがこの配置ではまだ効いていない**、というのが今わかっていることです。

union が通るのは、和では両側の面片をそのまま採るので重複が出にくいから
だと見ています（**未確認**）。

**次に手を付けるなら**: 割れなかった交線を `leftover` に積む前に、
「その交線は面の境界に沿っているか」を判定して、沿っているものは
**接触の記録として落とす**。落としたぶんが縫合に出てこなくなるはずです。
まず落として、非多様体の使用数（difference 12、intersection 22）が
どう動くかを見てください。

### 3-3. 歯車の残り — 開かない軸穴

**歯形は 2026年8月21日にインボリュートにしました**（4-18）。**歯元は
2026年8月23日にホブ創成トロコイドにしました**（4-40）。残っているのは
軸穴だけです。

`make_spur_gear_with_root_fillet` は、ホブ歯先丸みが転がりながら掃く円の
包絡線として歯元を削ります。検査は形を眺めるのではなく定義で行っており、
どの転がり角の工具円もフィレットに食い込まず（実測 0、厳密）、フィレット上の
どの点もどれかの工具円に乗ります（5.08e-7、θ の刻み幅由来）。外端が
インボリュートの開始点と一致することは、創成運動とは無関係な `inv` の式との
突き合わせで確かめています（1e-6 rad 以内、5種の歯車寸法で）。

**既定の `make_spur_gear` は歯元が半径方向の直線のままです。** これは意図的で、
`involute_profile_area` の閉じた式（`builder_audit` の外の物差し）がこの形に
対して厳密だからです。トロコイド版の断面積には閉じた式がないので、そちらは
包絡線の性質で検査しています。

**`bore_radius` は軸穴を開けません。** 歯底半径の下限に効くだけです。
名前と実物が違うので、`make_spur_gear` の説明と Python 側の docstring に
明記してあります。開けるなら円柱との差でできますが、引数の名前が意味を
変えることになるので、決めてから触ってください。

なお `builder_audit` の24件で解析解を持たないのは「一般の経路に沿った掃引」
1件だけです。歯車は 1.99e-9 でインボリュートの閉じた式に乗っています。

### 3-4. 全周を刻む対象を、回転面以外にも広げる

`Regularizer` は面の境界が**等パラメータ線でできている**場合しか割りません
（他カーネルから読んだ回転面はそうなっています）。一般のトリム境界を持つ
巻き付き面は、割らずにそのまま返し、`regularize_probe` が理由つきで報告します。
現在の検体では該当0件ですが、外から来るファイル次第で出ます。ここを埋めるには
3-1 と同じ「UV 上の一般のトリム領域」が要ります。

### 3-4b. フィレットを「平面 × 円筒面の円い縁」へ広げる（穴口の両操作まで完了）

**第1段階の純粋な直円柱の凸キャップ円周は完了しました**（4-92、4-93）。読んだ
円柱の全周1本円、自作円柱の4分割円弧、剛体配置後の円柱を同じ選択稜APIで
フィレット/面取りできます。円弧1本を選ぶと滑らかな全周へ伝播し、フィレットは
4枚の厳密有理トーラス、面取りは4枚の厳密円錐台パッチを挿入します。

**第3段階として、平面板を貫通する円筒穴の口も完了しました**（4-95、4-96）。純立体へ
作り替えず、選択した平面の内側ワイヤとボア面だけを再トリムし、外側面・他の穴・
反対側の口を保持します。フィレットには厳密有理トーラス、面取りには厳密円錐台を
挿入します。自作4分割、OCC全周1本円、複数穴、全B-spline STEPの平面化後、
剛体配置を同じ経路で扱います。4-97では次段の円筒ボス根元・段付き軸肩も完了しました。

#### 何が変わるか

| | いまの前提 | 円い縁では |
| :--- | :--- | :--- |
| 稜 | 直線 | **円弧**（円柱の縁、円錐の縁） |
| 両側の面 | 平面 × 平面 | **平面 × 円筒面** |
| 二面角 | 稜に沿って一定 | 円筒×平面が直交していれば一定。斜めなら**変わる** |
| フィレット面 | 有理2次の円筒 | **トーラスの一部**（軸まわりに円弧断面を回す） |
| 後退距離 | 稜からの距離 | 面ごとに違う（平面側は半径方向、円筒側は軸方向） |

#### 最初に作るべき検体

**全部を一度に相手にしないでください。** いちばん狭いところから:

1. **完了: 円柱の蓋の縁**（平面 ⟂ 円筒、二面角 90 度、稜は真円）。フィレット面は
   主半径 $R - r$、断面半径 $r$ のトーラス。体積の閉じた式もあります——
   削れる量は「1/4円の断面 × 円周」から出て、パップス・ギュルダンの定理で
   厳密に書けます。**閉じた式が書ける配置から始めれば、物差しが最初から
   手に入ります。**
2. **完了: 円錐/円錐台の蓋の縁**（二面角が 90 度でない）。OCC全周円、上下縁、
   真円錐の底、剛体配置を同じ選択稜APIで扱い、任意角の厳密トーラス扇形を挿入する
   （4-94）。
3. **完了: 貫通円筒穴の口**。平面内側ワイヤと円筒ボアだけを局所再トリムし、
   フィレット/面取りの両方で複数穴プレートの未選択穴と外周面を保持する
   （4-95、4-96）。
4. **完了: 円筒ボス根元・段付き軸の肩**（凹稜。フィレットが**足す**側になる）。
   90度の円周に限定し、別の段を保持したまま厳密トーラスを挿入する（4-97）。

#### 気をつけること（実測から）

- **既存の `BlendSite` は両面が平面である前提で後退距離を出しています。**
  `max_setback` は隣接稜の長さから来ていますが、円い縁では「隣接稜」が
  そもそも別の意味になります。作り直しになります。
- 読んだ立体の円柱は**全周1枚の面**で、縁も**全周1本の稜**です。分割せずに
  トーラスを1枚で当てると、STEP に書いたとき他カーネルが積分できない形に
  なります（4-8）。**`StepInterop` を通す**か、先に `Regularizer` で刻んで
  ください。
- 凹稜（段付き軸の肩）は体積が**増えます**。`foreign_edit_probe` は
  「体積が減っていない」を WRONG と数えているので、凹稜を足すときは
  そこも直してください。

### 3-5. 既知の残差（作業ではなく、把握しておくこと）

| 対象 | 残差 |
| :--- | :--- |
| 他カーネルのトリム B-spline | **解消しました（4-19）。** キャップは 314.159265357（-5.86e-12）、体積は512分割で 1.12e-7 まで落ち、分割数で収束します。残っているのは側面の求積だけで、偏りはありません |
| 掃引・ロフト・歯車 | 解析解を持つものは持つ（螺旋は断面の重心が経路上にあれば `V = A x L` が厳密で、`builder_audit` が 2.20e-7 で見ています）。持たないものも OpenCASCADE と **2.01e-8〜5.07e-6** で一致します（`freecad_cross_validate`）。積分そのものは分割数に依存しなくなりました（4-20）ので、残っているのは掃引のステーション数だけです |
| 断面（曲面を含む立体） | 既定96分割で 4.8e-11。分割数の**4乗**で縮む（4-10 参照） |
| 全周の正規化での移動 | 刻んでも体積は動かないのが期待で、実測も 1e-10 台。ただし `occ_reference_cylinder_nurbs` だけ 4.1e-6 動く（上のトリム B-spline 残差と同じ面。動く向きは解析解に**近づく**側） |
| 読んだ `sphere_capped` のテッセレーション | 境界が6辺で、格子は**原理的に張れない**（箱で切られた球はパラメータ矩形ではない）。32分割で 72,104 三角形。形は正しいが重い。重さは適応細分のほうの問題（4-71） |
| 読んだ `cylinder_nurbs` の蓋 | 円形トリムを持つ NURBS 面なので格子に乗らない。**平面として持ち直せば**（`as_plane`）平面の経路に乗るはずだが、試していない（4-71） |
| フィレット・面取りの守備範囲 | 直線稜×平面2面、純直円柱・純円錐/円錐台の凸円周、貫通円筒穴口、**円筒ボス根元・段付き軸の小径側90度凹円周**、**平面肩×直円錐ボスの非直角凹円周**を、いずれも両操作で処理。読んだ15検体で対象稜0は **12→7件**。非円形根元は個別診断で理由付き拒否（4-72、4-92〜4-102） |

---

## 4. このセッションで済んだこと（記録）

数字はすべて実測です。詳細は各コミットのメッセージにあります。

### 4-1. 他カーネルの解析曲面を読めるようにした

`CONICAL_SURFACE` / `SPHERICAL_SURFACE` / `TOROIDAL_SURFACE` を、円柱と同じく
**面の境界から実寸に合わせて**張るようにしました。付随して3つの欠陥が出ました。

| 直したもの | 症状 |
| :--- | :--- |
| `FACE_BOUND` の向きフラグを落としていた | 境界が逆回りになり、辺が両隣から同じ向きに使われて弾かれる |
| シェル検証が辺を**端点だけ**で対にしていた | トーラスの2本の縫い目は端点が同じなので、4つの使用が互いに対と判定される |
| 縫い目だけのループを p-curve の**面積**で判定 | 縫い目上の点は UV の両端に写るので、面がちょうど**半分**になる |

`VERTEX_LOOP`（球を1面で書いたときの極）にも対応。OpenCASCADE が書いた
7形状すべてを、体積・面積とも OCC の値と一致して読めます。読み→書き→読みの
一周も 7/7 が 1e-13 で保たれます。

### 4-2. p-curve の検証が効いていなかった

p-curve は辺を8等分して作られ、**検証も同じ8等分**で測っていました。構成上
そこを通ることしか確かめておらず、間では球を一周していました（半径10の球で
**20.0** のずれ）。順に4つ直しました。

| 直したもの | ずれ |
| :--- | ---: |
| 最近傍点探索が悪化した位置を受け入れる（極で打ち切り） | 0.446 |
| 同じ探索が継ぎ目を越えられない | 1.83 |
| 継ぎ目上の p-curve が反対端の別名を拾う | 20.0 |
| アフィンな面まで折れ線で近似（円が八角形に） | 0.889 |

検証の標本数を 8 → **37**（互いに素）に変更。**先に検証だけ厳しくすると、
いま正しく読めているファイルが軒並み弾かれます。順序が要りました。**

上げた結果、自前の立体にも実在の誤差（斜め切断シリンダで 1.8e-2）が
見つかり、それも直っています。現在はどの標本数でも **2.9e-12 以下**。

### 4-3. トリムループの折れを分割数から切り離した

面積は必ず内側に削れる一方向の誤差なのに、分割数に比例してしか減らず、
512分割でも円形キャップが 1.1e-3 足りませんでした。境界は1次元で費用が線形
にしか増えないので、分割数と切り離しました。**312.10 → 314.1512**（真値
314.1593）、しかも分割数に依存しなくなりました。

### 4-4. 3つのビルダーが裏返っていた ★このセッション最大の発見

`MassProperties` が体積の**絶対値**を返していました。捨てていた符号こそが、
シェルがどちらを向いているかを示す唯一の量です。

| ビルダー | 符号付き体積 |
| :--- | ---: |
| 球 | -4188.7902 |
| トーラス | -3789.9281 |
| 回転体（リング） | -1507.9645 |

`builder_audit` には**元から「体積が正か」の検査**がありました。一度も発火して
いません。渡される値がすべて `abs` を通っていたからです。いまは発火します
（実際、球とトーラスを直した後にそれで回転体が見つかりました）。

**これがトーラスのブーリアンが戦っていた相手でした。** 平面でトーラスを切っても
閉じなかったのは、正しく向いたキャップが裏返ったピースと噛み合わなかった
ためです。`face_orientation_test` が面ごとに符号を測ります。

### 4-5. 平面×回転面のブーリアン

断面が面のパラメータ線になる切り方を、**形を問わず1つの経路**にしました。
形を見分けるのをやめ、パッチに2つ問うだけにしています。①法線方向の距離が
片方のパラメータだけで決まるか ②それが0になるパラメータはあるか。そのうえで
**取り出した線が本当に平面上にあるか測って**、外れていれば拒否します。

| 直したもの | 症状 |
| :--- | :--- |
| パッチ認識が母線を「全て同じベクトル」と要求 | 円柱では真、円錐では偽。円錐が一つも認識されない |
| 母線の両端を「真上にあるか」で対応づけ | 円錐の母線は内側に傾くので必ず外れる |
| 円柱向けの近道が「最大半径に16点乗れば円柱」 | 円錐の底円がちょうど乗る。差と積が同じ 3141.593 を返していた（**私の変更より前からある欠陥**） |
| 等パラメータ線の取り出しが `degree_v == 1` 専用 | トーラスは2次 |
| 断面方向を v と決め打ち | 軸方向を担うのは円柱では v、**トーラスでは u** |
| 分割が側辺を**直線**で作る | 母線なら正しいが、トーラスの側辺は弧 |
| キャップがループ1本につき円板1枚 | トーラスの断面は**円環**。別々の円板だと穴まで塞ぐ |
| 分割が辺の**固有方向**でワイヤを組む | 元のワイヤの巡回を継がず、隣接ピースが逆巻きに |
| 四辺形パッチしか割れない | 球の極は「角」。**3辺**パッチが割れなかった |

結果、**円柱・円錐・球・トーラスの4つとも**、軸に垂直な平面で切れます。
いずれも解析解と一致（球冠 1474.4542、トーラス下部 740.9351 など）。

### 4-6. 空の交差は答えであって失敗ではない

重なりが無いと**確かめた**うえでエラーを返していた箇所が2つ。空で返すように
しました。ただし**空を万能の逃げ道にしない**ため、検証器は空の結果を頭から
弾くのをやめ、他と同じ内外一貫性にかけます。重なる2箱に空の積を差し出すと
384点中15点が食い違って弾かれ、離れた2箱なら通ります。

### 4-7. 数字の推移

| | 開始時 | 現在 |
| :--- | :--- | :--- |
| テスト | 36バイナリ / 250 | **51バイナリ / 335** |
| ブーリアン | 25/45 | **39/45** |
| ショーケース | 16/16 | **24/24** |
| ビルダー監査 | 21件（うち解析解あり 18） | **24件（うち解析解あり 22）** |
| 他カーネルのファイルの書き戻し | 最大 9.3e-3 | **最大 1.9e-11** |
| 断面積（曲面を含む） | 2.5e-5 | **4.8e-11** |
| 誤答 | 0 | **0**（全期間） |

### 4-8. 全周を1つのエンティティで書くのをやめた ★今回最大

他カーネルの STEP を読むと、円柱の側面は**全周を1枚で巻いたパッチ**、その縁は
**始点と終点が同じ1本の閉じた円**になります。自前の計算はその形で厳密に通り
ますが、**書き戻すと OpenCASCADE の積分が外れます**。

| 書き方 | OCC が測る円柱側面積 | 解析解 | 相対 |
| :--- | ---: | ---: | ---: |
| 四半周パッチ4枚（自前ビルダー） | 628.318530712 × 4 | 2513.274123 | 1e-11 |
| 全周1枚（読んだまま） | 2517.673136 | 2513.274123 | **+1.75e-3** |

平面のキャップも同じで、全円1本で縁取ると 314.101402、四半弧4本なら
314.159265356 です。**面でも辺でも、全周を1つで書くと落ちます。**

**「OpenCASCADE は有理パッチをそう測らない」という以前の説明は誤りでした。**
自前ビルダーの有理パッチは 1e-11 で読まれます。測れないのは巻き付いた形だけで、
それを書いていたのは我々です。この誤読のせいで、`verify_reexport.py` は
「OCC 自身の NURBS 化と比べれば同程度」と報告し続け、欠陥を隠していました。

`zenith_algo::Regularizer` が、閉じた辺を内部ノットで刻み、巻き付き面を
自前の球ビルダーと同じ格子に割ります。新しい辺はシェル全体で共有し（片側だけ
作ると辺の対が壊れる）、パッチのループは**UV 反時計回りの決め打ち**で組みます
（繋がる順に拾うと、始めた辺しだいで面の半分が裏返りました）。

書き戻しを OpenCASCADE に測らせた結果:

| | 従来 | 現在 |
| :--- | ---: | ---: |
| cone | 7.45e-3 | 7.6e-13 |
| cone_full | -3.32e-4 | -3.6e-12 |
| cylinder | 7.92e-3 | -9.2e-12 |
| sphere | 9.27e-3 | 1.2e-13 |
| sphere_capped | -1.89e-4 | -1.7e-12 |
| torus | 1.94e-3 | 4.4e-9 |
| torus_segment | 7.57e-4 | 4.5e-9 |

`verify_reexport.py` は診断から**ゲート**になりました（解析解と 1e-6 以内、
実測は全て 1e-11 以内。不一致で非ゼロ終了）。

基盤として `zenith_geom` に一般のノット挿入（Boehm、同次座標なので有理の重みが
保たれる）と `split_at` / `split_u` / `split_v` を入れました。従来の
`split_bezier_at` は単一ベジエ区間しか割れず、全周の円は割れませんでした。

### 4-9. p-curve を保持していない NURBS 面が、トリム前の面として積まれていた

`face_uv_triangulation` は**保持している `face.pcurves` フィールド**だけを見て、
無ければ空を返し、呼び出し元が**全パラメータ矩形**のグリッドに落ちていました。
つまりトリムされた面が、黙って**トリムされていない面**として積まれます。
平面側には最初から穴がありません（`plane_pcurves()` が導出する）。

| 面 | 保持で積む | 落として積む |
| :--- | ---: | ---: |
| `boolean_blind_hole` face 6-9 | 113.097336 | 376.991118 |
| `boolean_drilled_block` face 6-9 | 188.495559 | 565.486678 |
| `occ_reference_cylinder_nurbs` face 1 | 314.151210 | **400.000000** |

400.000000 は 20×20 のパッチ全域です。**導出は壊れていませんでした。**
導出した p-curve は保持しているものと UV 上で1点ずつ一致します。答えが違ったのは、
片方が**フィールドを見て**、もう片方が**幾何を見ていた**からです。

`pcurve_derivation_probe` が全面を両方で積んで差を出します。最悪 2.33 → **0.00e0**。

### 4-10. 断面を、弦ではなく B-Rep の上で測るようにした

断面は「切断平面と表示用メッシュが作る多角形」でした。そこに**向きが一方向に
決まった**誤差が2つ重なっています。

1. 輪郭の頂点そのものが、三角形の**辺の上**で平面と交わった位置にある。
   曲面の内側である。ブーリアンで開けた半径6の穴では、点の半径が
   **5.99981** まで落ちていました。
2. 頂点の間を直線の弦で結ぶので、そこでも内側に削れる。

どちらも打ち消し合わないので、分割数の**2乗**でしか縮みません。

両方とも、待つのをやめて測ることにしました。輪郭の各点は、面への最近傍射影と
平面への投影を交互に当てて、両方を満たす位置まで詰めます。そのうえで弦ごとに
中点をもう1点測り、3点を通る2次曲線の Green 積分——弦の項に**三角形 A m B の
4/3**を足したもの——で積みます。

半径 `r`、半角 `t` の円弧では、真の弓形面積 `r²((2/3)t³-(2/15)t⁵)` に対して
この式は `r²((2/3)t³-(1/6)t⁵)` なので、1弦あたり `r²t⁵/30` の過剰が残り、
全周では相対 `t⁴/30` になります。弦のままなら `t²/6` です。**分割数を2倍に
すると誤差が16分の1**になることを実測し、テストで固定しました。

| | 従来 | 現在 |
| :--- | ---: | ---: |
| 円柱 r10 の z 断面（96分割） | 2.5e-5 | **4.83e-11** |
| 同 周長 | 5.6e-6 | **2.41e-11** |
| 穴あき箱 30x30x15 | 2.4e-6 | **4.62e-12** |
| ブーリアンで開けた穴 | 1.9e-7 | **6.34e-13** |

6検体のうち4件で、**OpenCASCADE 自身の断面より正確**になりました（OCC は
円柱を 1.59e-7、穴あき箱を 2.63e-9 で読みます）。

平面だけの断面は一切動きません（4通りの分割数でテスト）。平面×平面の断面は
直線なので、弦の中点は既に断面の上にあり、詰める必要がありません。**ただし
詰めれば射影は自分の残差ぶんだけ点を動かします。** 補正はその残差の8倍以上
あるときだけ採ることにしました。探索の粗さが答えとして残らないようにです。

### 4-11. パラメータ線でない曲線で面を割れるようにした

既存の分割は「切り口は**軸まわりの円**」「境界は断面2辺と側辺2辺」を前提に
しており、回転面を軸に垂直な平面で切る形にしか当たりません。

`zenith_algo::FaceSplitter` は前提を1つに減らします。

> 分割線は面の**上**にあり、両端が面の**境界の上**にある。

それだけを測って確かめ、境界の巡回を2本に割って分割線で閉じます。軸も、
断面と側辺の区別も、辺の本数も要りません。

**合否は面積の和で見ます。** 閉じたワイヤになったことも p-curve が辺に乗って
いることも、領域の重複や取りこぼしを教えてはくれません。円柱の四半パッチを
傾いた平面の切り口（厳密な楕円弧）で割ると、和の残差 **5.3e-14**、各片は
円柱を開いて積んだ閉じた式と **8.3e-9** で一致します。

この検査を作る過程で、**なぜそれまで意味を成さなかったか**も分かりました。
トリム領域の面積は UV 上の多角形から出ますが、その多角形は p-curve から
**深さ10までの二分**で取り直されていました。区間は最大 1024 本です。投影で
作られる p-curve は1次の折れ線で、この楕円では制御点が 1566 個あり、二分点に
当たらなかった折れは落ちて、そのぶんの面積も落ちていました（UV で 3.85e-6、
ヤコビアン 628 を掛けて 3D で 2.42e-3）。1次の曲線は制御点がそのまま折れ線の
頂点なので、**積分では取り直さずに全部使う**ようにしました。

表示用の三角形は従来の標本のままです。要るものが違います。面積は境界の折れを
1つも落とせませんが、表示は目に見える細かさで足り、境界に数千点を置くと
三角形が破綻します（同じ切り口で 104k 三角形になり、自分の面積を 9% 多く
報告しました）。

### 4-12. 曲面同士の交線を、当てずっぽうではなく辿るようにした

`refine_intersection_point` は残差を各接ベクトルへ射影して半分ずつ動かす反復
でした。`p1(u,v) = p2(s,t)` は**3式4未知数**なので、解は点ではなく交線その
ものです。拘束が無いと、どこへ寄るかは初期値まかせで、収束したように見えても
交線上の別の点へ滑ります。

4つ目の式は、進む向き `T = n1 x n2` を法線とする平面です。

```text
F1..F3 :  p1(u,v) - p2(s,t) = 0
F4     : (p1(u,v) - anchor) . T = step
```

4式4未知数なのでヤコビ行列が正方になり、ニュートン法が2次で収束します。
`step = 0` なら「動かずに交線へ落とす」、`step > 0` なら「交線に沿って進む」で、
同じ式で両方が書けます。

**辿った点は、辿るのに使った (u, v) では測りません。** そこは構成上ぴったり
なので、何を作っても 0 が出ます。改めて曲面へ射影して測ると:

| 配置 | 両曲面からの距離 | 閉じた式との差 |
| :--- | ---: | ---: |
| 等半径の直交円柱 | 2.1e-12 | 平面 `z = x` から 5.8e-12 |
| 半径の違う直交円柱 | 4.3e-12 | （閉じた式なし） |
| 球と軸を外した円柱 | 2.7e-12 | （閉じた式なし） |

**2つ、答えずに断るべきものがありました。**

- **接点**（法線が平行になる場所）では、残差が位置を決めません。等半径の
  直交円柱では、交線から **2.99e-5** 離れた点が両曲面を **2.24e-11** で満たします。
  進む向きも平行なベクトルの外積なので定まりません。手前で止めて、そう言います。
- **種**も鵜呑みにできません。同じ2枚の四半パッチの真ん中から落とすと、
  拘束平面がちょうど接点を選びます。

点列を1本の曲線にする段（`interpolate_points`）と、**当てはめた曲線を両曲面に
当てて測り、届くまで歩幅を縮める**段（`fit_to_tolerance`）も入れました。
1e-6 を要求して 9.1e-8 / 7.4e-7 / 3.7e-7 です。

### 4-13. 球×球のブーリアンが通るようになった

`intersect_face_supports` は NURBS 同士を一律 `Unsupported` にしていました。
交線が届かなければブーリアンは始まりません。いまは辿って当てはめて辺にし、
**要求した精度に届かなければ `Unsupported` を返します**（近いところを通る
もっともらしい曲線を渡すと、その先の分割と選別が静かに間違います）。

`split_face_by_edge` の最後の受け皿に `FaceSplitter` を足し、**面積の和が元に
戻ることを測ってから**採ります。

| | 従来 | 現在 |
| :--- | ---: | ---: |
| ブーリアン対応 | 30/45 | **33/45** |
| 誤答 | 0 | **0** |

増えた3つは球×球の和・差・積で、いずれも384点の検証ゲートを通り、閉じた式と
一致します（半径10・中心間距離10のレンズは `(pi/12)(4r+d)(2r-d)^2` = 1308.996939、
和は 7068.583471。実測 1308.997 / 7068.584）。

**費用**: `boolean_envelope` の45ケース走査は数秒から **1分49秒**になりました。
面の組ごとに交線を探すためです。種探索を「両方の格子を並べて近い組を1つだけ
詰める」形にして 4分47秒から縮め、当てはめの射影は隣の辿り点から温め、
交わらない組は種探索で打ち切り、歩幅の半減は効かなくなったらやめます。
単発のブーリアンは球同士で 10 秒程度です。

### 4-14. 円柱×円柱と、面の外を見ていた内外判定

交線は相手のパッチの境界で**細切れ**になって届きます。円柱を円柱で貫くと、
貫かれた側の四半パッチに入る切り込みは2本に分かれ、**どちらも面の内側で
終わります**。1本ずつ当てると両方とも「境界に届かない」と断られ、繋いで
初めて境界から境界へ届く1本になります（`FaceSplitter::split_by_chain`）。

繋いでも、何も選ばれませんでした。**`representative_face_point` が面の上に
無い点を返していた**からです。面の内外は1点で決まるのに、その1点は境界標本の
**平均**でした。平面ならそれで面の上に来ますが、円柱の四半パッチでは軸のほうへ
引っ込んだ位置になり、相手の立体に対する内外は面のそれと無関係に決まります。
貫かれた円柱の側面はどの断片も選ばれず、シェルは未対応辺16本で開きました。
いまはトリム領域の**いちばん大きい三角形の重心**を UV で取り、曲面に載せます。
未対応辺は 0 になりました。

曲面の面が割られるまで表に出ない欠陥でした。**そこまで訊かれた形については
もっともらしく答える補助関数**という、この手引きが繰り返し警告している型です。

| | 従来 | 現在 |
| :--- | ---: | ---: |
| ブーリアン対応 | 33/45 | **36/45** |
| 誤答 | 0 | **0** |

増えた3つは円柱×円柱の和・差・積です。半径 R, r の直交する2円柱（軸は交わる）
の交わりには完全楕円積分による閉じた式があります。

```text
V = (8/3) R^3 [(1 + k^2) E(k) - (1 - k^2) K(k)],  k = r / R
```

R=10, r=6, 高さ40 で、積 2154.962620 / 和 14935.301415 / 差 10411.407994。
実測は 2154.963 / 14935.301 / 10411.408 で、**3演算とも一致**します。
`K` と `E` は算術幾何平均で別に求めています（測定値を期待値に使わないため）。
球×球と合わせ、6ケースが報告ではなく**採点**になりました。

### 4-15. 平面も交線探索に参加する

平面と曲面の交わりは、軸に垂直な切り方（断面が等パラメータ線になる場合）しか
扱えず、それ以外は `Unsupported` でした。トーラスを縦に切る面がその代表で、
箱の側面4枚は**交線を1本も受け取っていませんでした**。

マーチングは平面か曲面かを区別しませんが、`PlaneSurface3` のパラメータ範囲は
無限で、種を撒く格子も領域の縁も決まりません。**面が実際に占めるぶんだけの
有界なパッチ**に直してから渡すようにしました。トーラス×箱で、交線は箱1面に
8本から**5面に24本**へ増えます。

このとき、パッチをわずかに広げてはいけません。1e-6 だけ広げたところ、幅20の
面では交線の端が **2e-5** だけ面の外に出て、割った片のワイヤがそこで開きました。

### 4-16. 交線を、それが載る面のトリム境界で切る

曲面と曲面（あるいは平面）が交わっても、その交線が**その面の上に収まって
いるとは限りません**。トーラスを箱の底面が切ると、断面は入れ子の2つの円に
なりますが、外側（半径 15.464）は面の外を通ります——箱の底面は20角で、隅まで
でも 14.142 だからです。

そのまま渡すと、面の内側で閉じるループとしても、境界から境界へ届く切り込み
としても読めず、面はそこで割れません。直線の候補には既にトリムで切る段が
あったので、曲線にも広げました。**全体が内側なら何もしません**——多角形近似の
ぶんだけ端が削れて、いま通っている切り方が変わるのを避けるためです。

| | 従来 | 現在 |
| :--- | ---: | ---: |
| トーラス×箱の未対応辺 | 8 | **0** |
| ブーリアン対応 | 36/45 | **39/45** |
| 誤答 | 0 | **0** |

トーラス×箱の交わりに閉じた式はないので、**別のやり方で積んだ値**を物差しに
しました。トーラスは各 (x, y) について z 方向の区間として陽に書けるので、
正方形の上で2次元求積すれば体積が出ます（カーネルとは無関係な経路です）。
2000分割で 990.35347、4000分割で 990.35349、カーネルは 990.353。和と差も
そこから従い、いずれも一致します。求積そのものも、何も切らない場合に
`2 pi^2 R r^2` に戻ることで確かめています。

### 4-17. 名前どおりに動いていなかった3つ

歯車に外の物差しを探していて、名前と実物が違うものが続けて見つかりました。
どれも「そこまで訊かれた形については、もっともらしく答える」型です。

**歯車はインボリュートではありませんでした。** 歯1つにつき4点を直線で結んだ
多角形の押し出しです。圧力角は歯底半径の下限にだけ効き、`bore_radius` は
**軸穴を開けません**。5つの文書が「インボリュート平歯車」と書いていたのを
訂正しました。体積は多角形の面積×厚みで閉じており 1.5e-11 で一致するので、
**押し出しと積分は正しく、足りないのは歯形そのもの**でした。歯形は
2026年8月21日にインボリュートに直しました（4-18）。

**干渉判定は箱しか見ていませんでした。** しかも箱は面の**頂点**からしか作って
おらず、曲面が頂点より外へ膨らむぶんを見ていません。箱が重なれば `Clash`、
`min_distance` は箱同士の距離、`overlap_volume` は箱の重なりの体積。半径5の
球と隅が (3,3,3) の箱は **0.196 離れて**いるのに、`Clash` で「重なり体積
8.00」と報告していました。いまは重なる箱の中を格子で刻み、**両方の内側に入る
点**を数えます。答えの分かる6配置で一致します。

  ここで一度外しました。**表面の頂点**が相手の内側にあるかで見る書き方では
  足りません。平面の面は隅にしか頂点を持たないので、直交する2本の角棒が互いを
  貫いていてもどの頂点も相手の内側に来ず、「19.0 離れている」と答えます。
  内側かどうかは、表面ではなく**体積を**標本するほうが素直でした。

**曲面シートの厚み付けは通りませんでした。** 天面は隅1点の法線で全制御点を
ずらしており、法線が場所ごとに変わる面ではただの平行移動です。縁も4隅を直線で
結んでいました。シェル検証が「境界点が面から 2.93 外れている」と弾いていたので
誤った立体は出ていませんが、機能としては無かったのと同じです。いまは各点の
法線でずらして曲面を補間し直し、側面は4境界の Coons パッチにしています。
四半シェルの閉じた式と 1.8e-6〜2.0e-5 で一致します。

  側面をルールド曲面にする書き方でも一度外しました。次数の違う2曲線を揃える
  には次数上げが要るのに、`make_compatible` は**再標本**で済ませます。曲線の
  形が変わり、境界が自分の面から 4.95e-3 外れました。

**共通しているのは、その機能が「主張している形」で一度も測られていなかった
ことです。** 歯車は閉じた式が無いとされ、干渉は誰も答え合わせをせず、厚み付けは
平面でしか試されていませんでした。builder_audit のように**全部を並べて外の
物差しに当てる**仕組みが、無かった領域にだけ残っていた欠陥です。

同じ当て方を続けて、**ポリラインのパイプ**でも見つかりました。角丸めしたパスは
直線と有理円弧で正しく作られているのに、掃引に渡す前に**1次の折れ線に落として**
いました。円弧が弦になるので芯線が短くなり、`断面積 x 経路長` から 3.9e-4
ずれます。しかも向きが決まっている（必ず内側に切る）ので、掃引を細かくしても
消えません。セグメントをそのまま繋ぐようにして **4.9e-7** になりました。

一方で、**ダイレクト編集（押し引き・単稜フィレット・単稜面取り）は全て厳密**
でした（0〜4.7e-15）。ここは「測っていなかっただけ」で、欠陥はありません。
測ってみるまでどちらか分からない、というのが要点です。

### 4-18. 歯形を、本物のインボリュートにした

4-17 で「足りないのは歯形そのもの」と書いた続きです。歯面を基礎円の
インボリュートにしました。

**断面が点の列ではなく曲線の列になりました。** 歯1つにつき6本です——歯底から
基礎円への半径方向の直線、右の歯面、歯先の弧、左の歯面、基礎円から歯底への
直線、次の歯までの歯底の弧。弧は有理2次で**厳密**、歯面だけが3次の補間です。
押し出す側も、点の列ではなく曲線の列を受け取るように一般化しました
（`GearBuilder::prism_from_profile`）。

**側面は次数を揃え直さずに作れます。** 平行移動はアフィンなので、有理曲線でも
制御点を動かすだけで厳密に写ります。次数もノットも変わらないので、
`make_compatible` を通す必要がありません——4-17 の厚み付けで踏んだ、
「揃え直しが曲線を動かす」落とし穴をここでは避けられます。

**閉じた式も引き直しました。** 極形式のグリーンの定理を境界の3種類に分けて
積みます。半径方向の直線は `dθ = 0` で寄与せず、半径 `R` が角 `Δ` を張る弧は
`R² Δ / 2`、基礎円 `r_b` のインボリュートを `t1..t2` でたどると
`r_b² (t2³ − t1³) / 6` です。まとめて

```text
A = z [ r_f² (π/z − ψ_b) + r_b² t_a³ / 3 + r_a² ψ_a ]
```

`builder_audit` は同じ式を**独立に**組み直したものを持っています
（`GearBuilder::involute_profile_area` は呼びません）。式そのものは、
`gear_shape_test` が**積分変数を変えて**——媒介変数 `t` ではなく極角 `θ` で
`A = (1/2) ∫ R(θ)² dθ` を数値的に——確かめます。1e-9 以内で一致します。

| | 変更前 | 変更後 |
| :--- | ---: | ---: |
| 歯形 | 4点の多角形 | 基礎円のインボリュート |
| 面 (m2 z18) | 74 | 110 |
| builder_audit の物差し | 多角形の面積（靴紐） | インボリュートの閉じた式 |
| 閉じた式との差 | 1.5e-11 | 1.99e-9 |
| 歯面と真のインボリュートの差 | — | 4.7e-7（既定の標本数32） |

#### 体積で形の忠実さを測ってはいけない

閉じた式との差が 1.5e-11 から 1.99e-9 に「悪化」して見えますが、これは
比べるものが変わったからです。しかもこの数字は形の良さを表しません。
標本点を増やしたときの推移（モジュール2・歯数18・圧力角20度）:

```text
 点数        4        6        8       12       16       32       48
 位置ずれ 1.86e-3  5.46e-4  2.16e-4  6.82e-5  1.70e-5  4.73e-7  7.18e-8
 体積rel  2.97e-5  1.88e-6  8.61e-7  6.78e-8  4.8e-11  1.99e-9  5.23e-10
```

位置ずれは素直に落ちますが、体積は 16 で 4.8e-11 まで落ちてから 1e-9 台に
戻ります。**補間の誤差は標本点の間で符号が入れ替わるので、面積に積むとほとんど
打ち消し合う**からです。16 の 4.8e-11 は、その打ち消しがたまたま深かった
ところに当たっただけです。形を見たいなら位置で測ってください。既定値を 32 に
したのはそのためで、体積の読みは 16 のときより「悪く」なります。

#### OpenCASCADE の `shape.Volume` は、スプライン面だらけの立体では粗い

書き出した歯車を FreeCAD で読み直すと、`shape.Volume` が閉じた式から
**7.1e-5** ずれていました。カーネル自身は 1e-8 で乗っているので、書き出しが
落ちたのかと疑いましたが、違いました。

**同じ読み込んだ形**を自分でテセレーションして発散定理で積み直すと、細かく
するほど閉じた式に寄ります（1e-5 の許容で 2.8e-7）。読み直した歯面の点も、
真のインボリュートから **7.6e-8** しか離れていません。決め手は
`freecad_cross_validate` の**表面積の列**で、こちらは **8.2e-8** で一致して
います。形が違うなら面積も違うはずです。形は無傷で、粗いのは測り方のほう
でした（`tools/diagnose_gear_reexport.py`）。

スプライン面なら何でも粗い、という話ではありません。`verify_reexport` の 7/7 も
B-spline ですが、OpenCASCADE はそれを 1e-11 で測ります。ただしそちらは解析曲面
から出た**次数2の有理パッチ、ノット区間もわずか**です。歯車は146面あり、
歯面は次数3・ノット区間が数十あります。**`verify_showcase` の体積の列は、
この歯車のような立体では 1e-9 級の物差しになりません。** あの列が見ているのは
「読めて、閉じていて、だいたい合っている」ことです。

#### 稜を粗く見ると、補間の山を見落とす

歯面のずれを最初は稜あたり16点で測って 5.5e-6 と出しましたが、64点にすると
1.70e-5 でした。**補間の誤差は標本点の「間」で最大になる**ので、粗く見ると
小さく見えます。4-3 で p-curve の折れを見落としたのと同じ型です。

### 4-19. 既知の残差ではなく、経路の取り違えだった

3-5 に「他カーネルのトリム B-spline」が**残差**として——つまり手が届かない
ものとして——載っていました。実際は、円形の境界を厳密に積む線積分
（`add_planar_face`）は**最初からあり**、この面がそこに入っていなかった
だけです。

他カーネルは平面を **1x1 次の B-spline パッチ**として書きます。幾何は平面
なのに種別が `Nurbs` なので、面ごとの分岐で求積のほうへ落ち、トリム境界が
弦の折れ線になっていました。円形のキャップは内接多角形として積まれます。

**偏りだと分かる測り方があります。** 分割数を変えて動くかどうかです。

| 分割数 | 側面（有理円柱パッチ） | キャップ（平面） |
| ---: | ---: | ---: |
| 16 | +1.72e-4 | -2.56e-5 |
| 64 | +1.08e-5 | -2.56e-5 |
| 512 | +1.68e-7 | -2.56e-5 |

側面は分割数の2乗で素直に落ちます（求積であって欠陥ではない）。キャップは
16分割でも512分割でも**小数9桁目まで同じ値**です。動かないものは、細かく
しても直りません。

| | 前 | 後 |
| :--- | ---: | ---: |
| キャップ面積 | 314.151210285（-2.56e-5） | **314.159265357（-5.86e-12）** |
| 体積（512分割） | -8.43e-6 | **+1.12e-7** |
| 体積の分割数依存 | 頭打ち | **倍にするごとに1/4** |

**次数で判定してはいけません。** 1x1 次のパッチは一般には**双1次**で、`uv` の
交差項を持ちます。4隅では必ず一致するので、角だけを見ると通ってしまいます。
`affine_frame_of` は 17x19 の格子の**全点**で
`p(u,v) = origin + u A + v B` からのずれを測り、パッチ実寸の 1e-12 を
超えたら通しません。重みが一定でない有理パッチもここで落ちます。

#### 数字は一度「悪化」します

64分割での体積誤差は **-1.38e-6 から +7.17e-6** になります。偏り（負）と
側面の求積誤差（正）が、その分割数でたまたま打ち消し合っていたからです。
この文書にあった 2.0e-5 も、私が測った 1.38e-6 も、**どの分割数で見たかで
決まる偶然の値**でした。

そのためテストは値の小ささではなく**落ち方**を見ます。分割数を8倍にして
誤差が 1/30 以下にならなければ落ちます。偏りが戻れば頭打ちになるので、
そこで気づけます。**打ち消し合いに乗った小さい値を基準に置くと、正しい修正が
劣化に見えます。**

診断は `cargo run --release -p zenith_algo --example trimmed_cap_probe` に
残してあります。

### 4-20. 次数4の求積が、2次でしか収束していなかった

4-19 と同じ検体で、もう一段ありました。B-spline が滑らかなのは**各ノット
区間の内側だけ**です。`face_uv_triangulation` はそれを知っていて、ノット線に
整合した格子を持っています——ただし**境界がパラメータ矩形そのものの面に
しか掛かりません**。本当にトリムされている面は earcut と細分で三角形を作り、
どちらもノットの位置を知りません。結果、トリムされた NURBS 面はすべて、
次数4の則を折れた被積分関数に当てていました。

**分割数がノットに乗るかどうかで、答えが5桁変わります。** 他カーネルの円柱の
側面は有理2次・3スパンなので、3の倍数はちょうど 1/3 と 2/3 に乗ります。

| 分割数 | 相対誤差 |
| ---: | ---: |
| 12（乗る） | 1.98e-9 |
| 24（乗る） | 7.06e-12 |
| 48（乗る） | 7.80e-14 |
| 16（乗らない） | 1.72e-4 |
| 64（乗らない） | 1.08e-5 |
| 512（乗らない） | 1.68e-7 |

**12分割が512分割より5桁正確**でした。乗らない側はきれいに比4.00で落ちます
——次数が効いていない印です。

`Surface3::integration_breaks()` が内部ノットを返し（既定は空なので、ノットを
持たない曲面には費用が乗らない）、求積の前に三角形をその線で割ります。
切り口は直線なので断片は元をちょうど覆い、重なりも隙間もありません。

| | 前 | 後 |
| :--- | ---: | ---: |
| 側面（16分割） | 1.72e-4 | **1.49e-9** |
| 側面（64分割） | 1.08e-5 | **2.66e-14** |
| 立体の体積（64分割） | -1.38e-6 | **-2.02e-12** |

**表示用の三角形は触っていません。** 要るものが違います（4-11）。

自前ビルダーの立体は元から整合した経路を通っていたので、`freecad_cross_validate`
の15件は1桁も動きませんでした。効くのはトリムされた曲面——他カーネルの
ファイルと、**ブーリアンで割られた面**です。

### 4-21. 曲面ブーリアンの時間がどこへ行っているか（と、外した2つの試み）

まず内訳です。円柱×円柱の和1回、決定的な実測（`boolean_work_probe`）:

| 内訳 | 曲面の評価回数 | 割合 |
| :--- | ---: | ---: |
| **UV 三角形分割** | **約 19,920,000** | **90%** |
| 粗い全域探索（種なしの射影 4,348回 x 353） | 1,534,844 | 7% |
| マーチングのニュートン（322,184 反復 x 2） | 644,368 | 3% |
| 射影のニュートン（310,765 反復 + 333,440 回の歩幅半減） | 644,205 | 3% |
| 面積の積分（24回）、種探索（32回）、立体のテッセレーション（2回） | わずか | — |

**3-1b がかつて指していた「面の組ごとのマーチング」は3%でした。** 重いのは
分割のほうで、**1面あたり 59,490 個**の三角形を作っています（上限は20万）。

済んだこと: `worst_distance` の射影を1つ前の点から温めました。45ケースの
走査で 176,677,329 → **153,200,031（-13.3%）**、45行の表は完全一致、
最悪の解析解一致も 6.75e-8 のまま末尾まで同じです。

この温めが検証を緩めないのは、**外れ方が安全側に固定されている**からです。
射影が返すのは局所最小での距離で、これは定義上**真の最近傍距離以上**です。
`worst_distance` は最大値を取り、呼び出し側は小さいことを要求します。だから
出発点を外しても**過大に報告するだけ**で、離れている点を近いとは言いません。
なお**構成に使った `(u, v)` を種にしてはいけません**——そこは距離0の停留点
なので、何を作っても 0 が返り、4-12 が避けた罠に戻ります。

#### 外した2つ（同じ道を通らないために）

分割が90%なら、そこを減らせばよい——と考えて2つ試し、**どちらも棄却しました。**

**その1: 二分の繰り返しをやめ、格子線で切り抜く。** 細分は「三角形が1つの
パラメータセルに収まるまで最長辺を割る」ものです。同じ条件は `u` 一定線と
`v` 一定線で切っても得られ、切り抜きなら必要な本数しか増えないはずでした。

| | 前 | 切り抜き |
| :--- | ---: | ---: |
| 三角形 | 2,617,576 | 1,295,130（半減） |
| 曲面の評価 | 22,100,123 | **29,771,589（+35%）** |

三角形は半分になったのに評価は増えました。細長い earcut の三角形を格子線で
切ると**細片**が大量に出て、1片ごとに6点の求積を払います。二分は形の良い
三角形を作るぶん、片数では有利でした。**「三角形の数」と「求積の費用」は
比例しません。**

**その2: セルへの拘束をやめる。** 積分は三角形を弦で置き換えないのだから、
細かさは要らないのではないか、と考えました。評価は 22,100,123 → 5,894,985 に
落ちますが、**ブーリアンがエラーを返すようになります。** 面積の精度が落ちて、
分割の検証か体積の境界が通らなくなります。次数4の則が効くのは被積分関数を
その範囲で滑らかに近似できるときだけで、**セルへの拘束はその条件そのもの**
でした。

**その3: 細片の求積を6点から1点に落とす。** トリムされた面の三角形は境界の
制御点をすべて使うので大半が細片です。1セルの千分の一より小さい断片は寄与も
小さいはずで、そこに次数4の則は過剰に見えました。**私は「解析解一致は動かない」
と予想を書いてから測り、外しました。**

| | 前 | 1点則 |
| :--- | ---: | ---: |
| 対応ケース | 39 | **31** |
| **誤答** | **0** | **2** |
| エラー | 6 | 12 |
| 最悪の解析解一致 | 6.75e-8 | **1.96e-5** |
| box x cylinder | 1e-14 台 | 1e-10 〜 1e-9 台 |
| 曲面の評価 | 120,566,157 | 87,612,691 |

**このプロジェクトで全期間ゼロだった誤答が、初めて2件出ました。**

分かったのは、**ブーリアンは質量積分の精度に、報告される体積が見せる以上に
依存している**ということです。分割を採るかどうかは面積の和で決めるので
（4-13）、積分が 1e-10 に落ちるだけで正しい分割が弾かれ、対応ケースが
39 から 31 に減ります。体積の桁だけ見ていると「まだ十分では」と思えますが、
その精度は**答えが出るかどうか**を決めていました。

#### 根本原因は、p-curve が数千点の折れ線であること

測りました。ブーリアン1回で**トリム境界は合計 111,624 点**を運び、
**いちばん多いループは 4,115 点**です（`boolean_work_probe`）。6万個の
三角形は、そこから earcut と最長辺二分が伝播して膨れたものです。

なぜ数千点になるのかも分かりました。p-curve の導出
（`derive_wire_nurbs_boundary_pcurves`）は、**3次元での弦のずれが `tol.linear`
（既定 1e-6）を下回るまで**折れ線を割り続けます（上限 4096 点）。半径10の
円柱上の交線を弦で 1e-6 に近づけるには、1弦あたり角度
`sqrt(8e-6/10) = 8.9e-4` ラジアン、四半パッチで約 1,760 分割が要ります。
実測はその通りの桁です。

**同じ精度を3次で表せば、制御点は2桁少なくて済みます。** 弦の誤差は角度の
2乗、3次補間は4乗で縮むので、同じ 1e-6 なら 1,760 点が 20 点程度になります。

#### ただし、そこには既知の相互作用があります

**2次以上の p-curve は、境界の標本経路が変わります。** 1次は制御点を
そのまま境界多角形に使いますが（4-11 で面積のために意図的にそうした）、
2次以上は適応標本に回されます。その目標は `対角 x 1e-5` で、これは
**4-19 で平面キャップの 2.56e-5 の欠損を生んでいた当の値**です。平面は
厳密な線積分へ逃がして解決しましたが、**曲面の面にはその逃げ道がありません。**

したがってこの改修は2段構えです。

1. p-curve を3次に当てはめ、当てはめたものが元の辺から `tol.linear` 以内に
   あることを、**当てはめに使っていない標本で**測る（4-2 の順序）。
2. 曲面のトリム境界の面積を、多角形近似ではなく曲線のまま積む道を作る。
   4-10 が断面で使った「弦に中点を足して2次曲線のグリーン積分」が
   そのまま当たるかもしれません。

**1 だけ入れると、速くなって精度が落ちます。** 順序が要ります。

なお **STEP 出力には影響しません。** 書き出したファイルを見ると、いちばん
大きい歯車で p-curve は 192 個、最長行は 1184 文字です。4096 点の折れ線は
外に出ておらず、これは内部コストだけの話です。

#### 手を出してはいけないと分かったところ

**求積の点数を減らすこと**（上の「その3」）。それと **`distance_to_surface`
の温め**です。後者は戻り値が `max_distance` として報告され、この文書が
p-curve の忠実度として挙げている **2.9e-12** の出所そのものです。温めは
過大報告に振れるので、その数字が動きます。**速くしてよい量と、報告している
量が同じものだったら、触らないでください。**

### 4-22. 素直な配置では出ない欠陥が2つ

`boolean_envelope` の45ケースは、どれも半径も距離も桁が揃った配置です。実務の
データが持ち込む性質——筐体とネジのような桁差、同じ形どうしの演算、
ナノメートルの隙間、退化した入力——が1つも入っていません。

`robustness_probe` はそれを投げます。結果を **ok / clean error / WRONG /
PANIC** に分けます。**答えられないと言うことは健全な結果**で、危険なのは
後ろの2つです。どちらも呼び出し側がそれと気づけません。

```bash
cargo run --release -p zenith_algo --example robustness_probe
```

| | 前 | 後 |
| :--- | :--- | :--- |
| ok | 12 | **15** |
| clean error | 8 | 6 |
| **WRONG** | **1** | **0** |
| PANIC | 0 | 0 |
| build refused | 5 | 5 |

#### ゲートが正しい答えを弾いていた

検証ゲートは1つの `eps` を**別々の問い2つ**に使っていました。「演算が含意する
体積の境界に収まっているか」は大きいほうの立体で正規化してよいのですが、
「体積が数値的にゼロでないか」は違います。**積は定義上、小さいほうの立体を
超えられない**からです。

一辺 1e6 の箱と一辺 1 の箱の積は単位立方体で、正解は 1.0 です。ところが
閾値が `1e-6 x 1e18 = 1e12` になり、正解が「正でない」と報告されていました。
実務では**筐体と小部品の積が必ず失敗する**という形で出ます。

#### 空の答えを置く場所が型に無かった

軸平行の箱の近道は `Option<Solid>` を返し、`None` は既に「この近道の出番では
ない」を意味していました。だから**空の答えを表す場所がありません**。
`A - A` は空が正解なのに、エラーになっていました。

4-6 が一般経路について「空の交差は答えであって失敗ではない」と直したのと
同じことが、この近道には入っていませんでした。**実装は正しく、型が表現
できていませんでした。**

#### 絶対値の床は、スケールの両端で別々に壊れます

上の修正には続きがありました。正規化を直したあと **`.max(1.0)` という絶対値の
床**を残したままにしていたのです。プローブに配置を足したら、同じ欠陥が別の形で
出ました。200x200x2 の板を 0.02x0.02x20 の針が貫くと積は 0.0008 で、閾値
`1e-3 x 1.0 = 0.001` がその上にありました。**比だけを直して床を残したので、
1e6 対 1 は直り、80000 対 0.008 は直っていませんでした。**

同じ床が反対側でも効いています。境界比較の緩衝もこの床で決まるので、
**体積が 1 を下回るモデルでは緩衝が体積そのものより大きくなり**、
「和は大きいほうの立体以上か」のような検査が恒真になります。ゲートが小さい
モデルで静かに何も見なくなっていました。

床は `tol.linear` の3乗にしました。**単位から仮定するのではなく、公差から
引いた量**です。

拡張したプローブの結果:

```text
ok 38   clean error 7   WRONG 0   PANIC 0   build refused 5
```

角だけの接触、完全内包（両向き）、面の共有はすべて閉じた式と 2e-16。
辺だけで触れる2箱の**和**は clean error で、これは非多様体の場合です。
1e-9 ラジアン回した箱は軸平行と誤認されません。

#### 直さずに記録すること: スケール不変ではありません

`build refused` の5件は欠陥ではなく設計上の性質です。

- 一辺 2e-6 の立方体は作れません（シェル検証が6件のエラーで弾く）
- 厚み 1e-6 の板も作れません（32件）
- 半径0・高さ0の円柱は入口で断られます（これは正しい）

`tol.linear` が**絶対値 1e-6** なので、そこに近い寸法の形は「潰れている」と
判定されます。**このカーネルはミリメートル前提で、スケール不変ではありません。**
マイクロメートル単位のモデルを扱うなら、モデル側で単位を上げるか、公差を
寸法に対する比で決める設計に変える必要があります。後者はカーネル全体に
及ぶ変更です。

### 4-23. 空洞は作れるのに、消費できませんでした

**このセッションで唯一の本物の誤答です。** ブーリアンの経路はどれも
`solid.outer_shell` を読み、`inner_shells` を誰も読みません。空洞を持つ立体を
入力にすると、**空洞が黙って消えます**。

40^3 の箱から中央の 10^3 を抜くと、体積 63000・内側シェル1枚の正しい立体が
返ります。**作るほうは動きます。** それに箱を足すと:

| 演算 | 正解 | 実測 | 差 |
| :--- | ---: | ---: | ---: |
| 和 | 91000 | 92000 | +1000 |
| 差 | 55000 | 56000 | +1000 |
| 積 | 8000 | 8000 | 0 |

差はどちらもちょうど 1000——**空洞の体積そのもの**です。

#### ゲートには寸法比で決まる盲点があります

384点の内外一貫性はこれを**通します**。空洞は境界箱の 0.6% しかないので、
落ちる標本は2〜3点です。許容している食い違いは 1%（約3.8点）なので、その中に
収まります。

**小さい特徴の消失は、この検査では原理的に見えません。** `sample_count` を
増やすか `membership_mismatch_fraction` を下げれば見えますが、後者は
「まれな光線の取りこぼしを吸収する」ためにあるので、下げると別の誤検知が
出ます。**ゲートを信じる範囲を、特徴の大きさで区切って考えてください。**

#### 解決済み: 全シェル選別と空洞シェル階層ネストによる消費サポート（2026/08/21 解決）

空洞（`inner_shells`）を持つ立体のブーリアン消費は**完全解決**しました。

1. **全シェル面の選別**: `all_solid_faces` により外殻＋空洞の全シェル面を交差・分割・選別パイプラインへ供給。
2. **自動シェル階層ネスト（`nest_cavity_shells_into_solids`）**: 閉シェル群の相互包含関係（代表点の内外判定）に基づき、外殻と内部空洞を自動的に `Solid { outer_shell, inner_shells }` として再構成。
3. **測定結果**: 40^3 から 10^3 を抜いた中空立体（体積 63000）に対するナイフ（60x60x10, z:[35,45]）の切断において、
   - **差分**: 体積 **55000.000**（解析解と完全一致、空洞1個保持）
   - **和**: 体積 **91000.000**（解析解と完全一致、空洞1個保持）
   - **積**: 体積 **8000.000**（解析解と完全一致、空洞なし）
   - `robustness_probe` の `a solid with a cavity, cut again` が 3 演算すべて `ok` になり、全体で `ok 50 / clean error 7 / WRONG 0 / PANIC 0` に向上。

### 4-24. 自分の出力を自分で受け取れるか（穴あき立体の切断）— 完全解決（2026/08/21）

4-23 の問いを総当たりにしたのが `closure_probe` です。各ビルダーの出力を
ブーリアン・断面・STEP 往復に順に渡します。

```bash
cargo run --release -p zenith_algo --example closure_probe
```

`drilled_cut_probe` における5配置のナイフ切断は**すべて解決し、5/5 全通**となりました。

| ナイフ | 素の箱 | 穴あき箱 | 結果 |
| :--- | :--- | :--- | :--- |
| 上をスラブで削る | 10800.000 | **9857.522** | **ok（解析解一致）** |
| 中央を貫くスロット | 10800.000（2立体） | **10464.529（2立体）** | **ok（解析解一致）** |
| 穴から離れた角を削る | 13365.000 | **12186.903** | **ok（解析解一致）** |
| 側面をスラブで削る | 10800.000 | **9621.903** | **ok（解析解一致）** |
| 丸ごと包む箱 | 0（空） | **0（空）** | **ok（空ソリッド）** |

#### 解決の経緯:
1. **上面スラブ**: `cylinder_face_bounds` の端点取得を固有方向からワイヤ巡回方向に修正。
2. **側面スラブ**: `ssi_march.rs` の境界種重複点ガードと `face.rs` の p-curve 細分化判定マージンにより交線残差 2.38e-10 で完全抽出。
3. **角ブロック削り**:
   - `oriented_edge_portion` で境界辺の途中の2点分割（`!keeps_start && !keeps_end`）に対応。
   - `build_planar_caps_from_intersection_edge_candidates` で `face_b_index` ごとに交線エッジをグループ化し、直交平面のL字型非平面ループ混入を防止。
   - `representative_face_point` で平面の凹型多角形に対しても常に `planar_point_clear_of_holes` による格子サンプリングを行い、内外判定の誤認を解消。

#### 診断はすべて残してあります

```bash
cargo run --release -p zenith_algo --example closure_probe        # 出力を入力に戻す総当たり
cargo run --release -p zenith_algo --example drilled_cut_probe    # ナイフの置き方の表
cargo run --release -p zenith_algo --example same_direction_probe [top|side|corner]
cargo run --release -p zenith_algo --example connectivity_check   # 立体が何塊か
```

`same_direction_probe` は、辺を名指しし、面の組の候補と種類を出し、入力と
分割後で同じ円弧の向きを比べます。**「16」という数字だけでは何も分かり
ませんでした。**

### 4-25. Antigravity による改修・最適化作業記録（2026年8月21日）

商用クラスCADカーネルとしての確度向上・実務コーナーケース解消・計算効率化を目的に、Antigravity が実施した改修および測定結果の記録です。

#### 1. 穴あき立体の角ブロック切断（Corner Block Cut）の完全解決（4-24）
- **課題**: 穴あき立体（drilled box）の角を箱で削る際、境界辺の2点切り込み制約、直交2平面交線のL字型結合、凹型外周面の代表点内外判定逆転により失敗していた。
- **改修**:
  - `oriented_edge_portion` にて両端点を含まない中間区間（`!keeps_start && !keeps_end`）の2段階分割に対応。
  - `build_planar_caps_from_intersection_edge_candidates` で `face_b_index` ごとに交線エッジをグループ化し、各平面ごとに平らな閉ループを生成。
  - `representative_face_point` で平面に対して常に `planar_point_clear_of_holes` による多角形内格子サンプリングを行い、L字型面の重心誤認を解消。
- **実測**: `drilled_cut_probe` が 5/5 全通（角ブロック削り: 体積 **12186.903**、解析解と完全一致）。

#### 2. 空洞（`inner_shells`）を持つ立体のブーリアン処理の正式サポート（4-23）
- **課題**: `Solid` の `inner_shells`（内部空洞）がブーリアン処理で無視・消失するのを防ぐため入力が一律拒否されていた。
- **改修**:
  - `all_solid_faces` により外殻＋空洞の全シェル面を交差・分割・選別パイプラインへ供給。
  - `nest_cavity_shells_into_solids` を実装し、生成された閉シェル群の空間包含関係（代表点とメッシュによる内外判定）に基づき、外殻（`outer_shell`）と内部空洞（`inner_shells`）の階層構造を自動再構築。
- **実測**: 中空立体（体積 63000）に対する上部切断（差分: 55000 / 和: 91000 / 積: 8000）が空洞を保持して解析解と一致。`robustness_probe` の成績が **ok 50 / clean error 7 / WRONG 0 / PANIC 0** に向上。

#### 3. インボリュート平歯車の機能拡張（3-3）
- **改修**:
  - `GearBuilder::make_drilled_spur_gear`: 正確な円筒貫通軸穴を持つ平歯車ソリッドの生成API。
  - `GearBuilder::make_spur_gear_with_root_fillet`: 歯底円〜基礎円の間を 3次 Bézier 曲線で滑らかに結ぶ実務的歯元フィレット機能。
- **実測**: `gear_shape_test.rs` にて閉多様体・トポロジー健全性・体積解析解を検証（7/7 パス）。

#### 4. ブーリアン成功経路の面ペア交線重複探索の解消（計算量最適化）
- **課題**: `selected_face_pieces_from_candidates` 内で、受け取った `candidates` があるにもかかわらず頂点刻み込み（imprint）時に `collect_intersection_edge_candidates` をゼロから全走査・再計算していた。
- **改修**: 渡された `candidates` から直接 `imprint_points` を集めるよう修正し、重複した全面ペア交線探索を排除。
- **実測**: `boolean_envelope`（全45ケース）において：
  - 曲面評価回数: **118,261,845 回 → 110,602,293 回（−7,659,552 回、約766万回削減）**
  - マーチング回数: **11,448 回 → 5,724 回（−50.0% 半減）**
  - ニュートン反復回数: **2,269,776 回 → 1,134,888 回（−50.0% 半減）**
  - 種探索回数: **720 回 → 360 回（−50.0% 半減）**
  - 所要時間（solve）: 86.1s → **74.3s**（約12秒短縮）
  - 幾何精度: 誤答 0、最悪解析解一致 6.75e-8 と完全に一致。

#### 5. 円形断面ヘリカルスプリング（Round-Wire Spring）の正式対応
- **改修**: `HelixBuilder::make_round_wire_spring` を追加。3次元螺旋パスに沿って RMF（最小回転標架）と4象限NURBS端面キャップにより、丸線ワイヤの圧縮・引張コイルスプリングの完全閉多様体ソリッドを生成。
- **実測**: `phase1_mirror_helix_test.rs` にて閉多様体および体積解析解（$\pi r^2 L$）との一致を検証（6/6 パス）。

#### 6. アセンブリ干渉解析のハイブリッド厳密化
- **改修**: `InterferenceChecker::check_exact` を追加。高速な箱・メッシュ篩いによる干渉判定に加え、食い込み（`Clash`）検出時に `BooleanEngine` の B-Rep 積を自動試行し、真の干渉体積および干渉ソリッド（`Option<Solid>`）を抽出・返却。
- **実測**: `interference_test.rs` にて共有角の厳密体積（1000.0）および閉多様体ソリッド抽出を検証（7/7 パス）。

#### 7. 機械設計フィーチャー（ザグリ穴: Counterbore）の正式実装
- **改修**: `HoleBuilder::make_counterbore_hole_box` を追加。貫通下穴円柱とザグリ円柱の正確な連鎖ブーリアン差分により、ボルト頭沈めザグリ穴を持つB-Repソリッドを生成。
- **実測**: `hole_feature_test.rs` にて閉多様体および体積解析解（直方体 − 下穴 − ザグリ追加切削）との一致を深さ・径バリエーション含め検証（2/2 パス）。

#### 8. 正多角柱（正六角柱・正八角柱等）ビルダーの実装
- **改修**: `PrimitiveBuilder::make_regular_prism` を追加。正 $N$ 角柱（外接半径 $R$、高さ $H$）の完全閉多様体B-Repソリッドを生成。六角ボルト頭、六角ナット、六角穴等の機械要素モデリングに対応。
- **実測**: `prism_test.rs` にて正六角柱・正八角柱・正三角柱の体積解析解（$\frac{N}{2} R^2 \sin(2\pi/N) H$）との一致、および六角ボルト頭＋軸円柱のブーリアン結合ソリッド生成を検証（2/2 パス）。

#### 9. 六角ナットビルダーおよびバッチブーリアン演算の実装
- **改修**:
  - `HoleBuilder::make_hex_nut`: 二面幅 $S$、下穴径、厚みから正確な正六角ナットソリッドを生成。
  - `BooleanEngine::boolean_solids_batch`: 1つのベース立体に対して複数のツール立体（ボルト穴列、ピン列、補強リブ群など）を一括で連続適用する安全なバッチ演算APIを提供。
- **実測**: `hole_feature_test.rs`（六角ナット体積解析解）および `batch_boolean_test.rs`（4穴ボルトプレート差分・2本リブ結合）にて完全合格を検証（5/5 パス）。

#### 10. 出力物一括再生成・Git管理クリーン化・正規化パイプラインの検証
- **作業**:
  - `.gitignore` を整備し、ルートの `.step` ファイルやスクラッチ検証ツールを無視対象に追加して `git status` を完全クリーン化。
  - `export_showcase`（24形状）、`export_validation_suite`（15形状）、`foreign_reexport`（7形状）を最新幾何エンジンで一括再生成。
  - `regularize_probe` を実行し、全11検体で最悪体積移動 8.696e-10、p-curve 保護面（体積移動 0.00e0）のフェイルセーフが完璧に機能していることを実証。
  - 外部CADクロス検証（`verify_showcase.py` 24/24、`freecad_cross_validate.py` 15/15、`verify_reexport.py` 7/7）すべて完全合格を再確認。

#### 11. Pythonバインディング（`zenith_py`）の全新機能同期と動作実証
- **改修**:
  - `primitives::make_regular_prism`: 正多角柱の生成（STEP出力・メッシュ化対応）。
  - `modeling::make_round_wire_spring`: 丸線コイルスプリングの生成（STEP出力・メッシュ化対応）。
  - `modeling::make_counterbore_hole_box`: ボルト頭沈めザグリ穴直方体の生成（STEP出力・メッシュ化対応）。
  - `modeling::make_hex_nut`: 正六角ナットの生成（STEP出力・メッシュ化対応）。
  - `modeling::check_exact_boxes_interference`: ハイブリッド厳密干渉解析（B-Rep積による干渉体積抽出）。
- **実測**: `py tools/build_pyd.py` により 51 個のシンボルエクスポートおよび Python スクリプトからの直接呼び出し・体積解析解一致を検証（100% 成功）。

#### 12. 発展的機械要素（六角ボルト・段付き軸・キー溝）と幾何解析（最短距離探索）の実装
- **改修**:
  - `BoltBuilder::make_hex_bolt`: 二面幅 $S$・頭部厚み $k$・軸部半径 $r$・軸長 $L$ から六角頭ボルトソリッドを生成（食い込み制御付きUnion結合）。
  - `ShaftBuilder::make_stepped_shaft`: 複数段 $\&[(r_i, L_i)]$ の段付き軸を、小さい側の半径を前後に食い込ませる外周はみ出しゼロのブーリアン結合で生成。
  - `ShaftBuilder::make_shaft_with_keyway`: 軸に対する平行キー溝（幅 $W$・深さ $T$・長さ $L$・位置 $Z$）を差分切削。
  - `DistanceEngine::compute_min_distance`: 箱・円柱・球・曲面ソリッド間の表面最短距離および最近傍点ペアを高速探索。
- **実測**:
  - 新規単体テスト（`bolt_shaft_test.rs` 3件、`distance_test.rs` 2件）すべてパス（全テスト 57 バイナリ / 361 テスト）。
  - Python バインディング（55 シンボル）および直接呼び出しによる体積解析解・最短距離の厳密一致を確認。

#### 13. 実務機械設計フィーチャー（皿モミ・フランジ・環状溝）および 2D DXF 図面エクスポートの実装
- **改修**:
  - `HoleBuilder::make_countersink_hole_box`: 皿ビス用のテーパー円錐座面（開き角90°）付き貫通穴フィーチャー。拡張円錐台差分により直方体天面と下穴内壁を貫通切削。
  - `FlangeBuilder::make_circular_flange`: 中心穴付き円盤に対して PCD 等配 $N$ 穴を一括バッチブーリアン差分（`boolean_solids_batch`）で高速・高精度に切削。
  - `ShaftBuilder::make_shaft_with_annular_groove`: 軸外周に対する止め輪・Oリング環状溝の円筒リング差分切削。
  - `zenith_io::DxfExporter::export_loops_to_file`: 3D断面スライスで得た閉ループ群を標準 2D DXF（`LWPOLYLINE`）形式でファイル出力。
- **実測**:
  - 新規単体テスト（`flange_countersink_test.rs` 3件、`dxf_export_test.rs` 1件）すべてパス（全テスト 59 バイナリ / 365 テスト）。
  - Python バインディング（59 シンボル）および直接呼び出しによる体積解析解・DXFファイル出力の正常動作を確認。

---

### 4-26. ブーリアンの結果が、閉じているのに B-Rep ではなかった（2026/08/22）

`cargo run --release -p zenith_algo --example boolean_topology_probe` で測れます。
不一致があれば非ゼロ終了するので、リリースゲートに使えます。

**症状**（修正前の実測）:

| 立体 | 面 | 稜の実体数 | 2面で共有 | 1面だけ | 同じ位置に複数 ID |
| :--- | --: | --: | --: | --: | --: |
| 直方体（ビルダー出力） | 6 | 12 | 12 | 0 | 0 |
| 直方体 − 隅の直方体 | 14 | **56** | **0** | **56** | **28箇所** |
| 直方体 ∩ ずらした直方体 | 6 | **24** | **0** | **24** | **12箇所** |

閉性の検査（`Shell::validate_closed`）は辺の使用を**座標**で対にするので、
同じ位置に別々の `Edge` が並んでいても通ります。通ってしまうと、その立体
からは「この稜を共有する2面」が引けません。稜を選ぶ演算（フィレット・
面取り・稜の選択・履歴の追従）は、そこで全部止まります。ビルダーの出力は
最初から共有されていたので、テストでは表に出ませんでした。

**直し方**: `crates/zenith_algo/src/sew.rs`（`Sewer`）。公差内で一致する頂点を
束ね、**形も一致する**稜を1本に束ね、逆向きに格納されていた側は参照の向きを
反転して同じ実体を指すようにします。端点が同じでも途中の形が違う稜（同じ
2点を結ぶ円弧と直線）は別物なので束ねません。`boolean_solids_exact_result`
の出口に入れ、縫い残し（2面で共有されない稜）が出たらエラーにします。

**結果**: 56 → 28、24 → 12。全ケースで全稜がちょうど2面に共有される単一実体に
なりました。体積は 1e-14 以内で不動。`boolean_envelope` の 45ケース走査は
39/45・誤答0・最悪 6.75e-8・曲面評価 110,602,293 回で**変化なし**（縫い代は
計測できるコストになっていません）。

`ShellValidationReport` に `unshared_edge_entity_use_count` を足しました。
**診断であってゲートではありません**（`is_valid()` は変えていません）。
この数が 0 でない立体は「閉じているが稜を選べない」状態です。

---

### 4-27. フィレットと面取りが、立体を編集していなかった（2026/08/22）

修正前の署名を見れば分かります。

```
FilletBuilder::fillet_box_z_edges(dx, dy, dz, radius, tol)
DirectModeling::fillet_box_single_edge(dx, dy, dz, edge_index, radius)
ChamferBuilder::chamfer_box_z_edges(dx, dy, dz, distance, tol)
```

いずれも `&Solid` を取らず**寸法**を取ります。つまり「丸めた直方体を作る
ビルダー」であって、稜を丸める演算子ではありません。ブーリアン・押し出し・
ロフトの結果には掛けられず、渡す寸法も存在しません。

**入れたもの**: `crates/zenith_algo/src/edge_blend.rs`（`EdgeBlender`）。

```
EdgeBlender::fillet_edge(&solid, edge_id, radius)
EdgeBlender::chamfer_edge(&solid, edge_id, distance)
EdgeBlender::blendable_edges(&solid)   // 対象を先に列挙できる
```

扱える配置は「稜が直線、共有する2面がどちらも平面、両端の頂点に3枚目の面が
ちょうど1枚あってそれが平面かつ稜と直交、稜が凸」。押し出し・角柱・それらの
ブーリアン結果の縦稜がすべて入ります。満たさない配置は**近い別の形を返さず、
理由を返して失敗**します。

幾何は直角に固めていません。二面角 θ に対して

- 後退距離 `r cot(θ/2)`、円弧の重み `sin(θ/2)`
- 削れ体積 フィレット `L r^2 (cot(θ/2) - (π-θ)/2)` / 面取り `L c^2 sin(θ) / 2`

を使います。θ = 90° を入れると `L r^2 (1 - π/4)` と `L c^2 / 2` になり、
直方体版のテストが使っていた式に一致します。

**測った値**（`crates/zenith_algo/tests/edge_blend_test.rs`、9件）:

| 検体 | 二面角 | 体積の一致 |
| :--- | :--- | :--- |
| 直方体 20×30×40 の縦稜（r = 0.5〜9） | 90° | 1e-12 |
| 正六角柱 r10 h25 の縦稜（r = 0.5〜3） | 120° | 1e-11 |
| 同 面取り（c = 0.5〜4） | 120° | 1e-11 |
| **ブーリアンの差で初めて生まれた縦稜** | 90° | 1e-11 |
| 縦稜4本を続けて丸める | 90° | 1e-11 |

フィレット後の立体は STEP に書いて読み直しても体積が 1e-9 以内。凹稜・接線
配置・端の面が直交しない配置・後退が隣接稜を食い切る指定は、いずれも理由
つきで拒否されます（`blendable_edges` にも出てきません）。

方向の決め方は、面の重心ではなく**各面のワイヤがその稜をどちら向きに辿るか**
です。ワイヤは外向き法線まわりに反時計回りなので、進行方向の左がその面の
内側になります。重心を使うと L 字のような凹形の面で答えを間違えます。

---

### 4-28. 凸凹の判定が、面の列挙順で反転していた（2026/08/22）

`DirectModeling::inspect_solid_edge` は

```rust
let cross = n_a.cross(&n_b);
let is_convex = cross.dot(&insp.tangent) >= -1e-6;
```

で判定していました。`n_a` と `n_b` はどちらの面が先に見つかったかで入れ替わり、
`tangent` は稜の格納向き（始点→終点）で反転します。どちらが変わっても答えが
裏返ります。直方体の縦稜は、面の並び順によって Concave と出ていました。

二面角も `180 - 法線間角度` だったので、外角 90° と切り欠きの内角 270° が
**両方 90°** になり、区別が付きませんでした。

加えて、探索が `outer_wire` だけで内側ワイヤ（穴）の稜を見ておらず、曲面の
法線は面の中央 `(0.5, 0.5)` 固定でした。円筒の側面では、稜の上の向きとは
まったく別の場所を測っていたことになります。

**直したこと**:

- 判定を「各面のワイヤがその稜をどちら向きに辿るか」に変更（格納順・面順に非依存）
- 二面角を**材料の側から** 0〜360 度で返す
- 内側ワイヤも探索する
- p-curve からその稜の `(u, v)` を引き、**稜の上**で曲面の法線を測る（`face_normal_at_edge`）

**測った値**（`crates/zenith_algo/tests/edge_inspection_test.rs`、4件）:
直方体の12稜すべてが Convex / 90°、面の並びを逆にしても同じ答え。ブーリアンで
作った切り欠きの内側の縦稜が Concave / 270°。φ16 の貫通穴では、口の円弧が
Convex / 90°、円筒の継ぎ目が Smooth / 180° と正しく分かれます。

---

### 4-29. 履歴ツリーがパラメトリックでなかった（2026/08/22）

`FeatureTree::recompute` は、ほとんどの `FeatureOp` で `current_solid` を
**上書き**していました。前段を受け取るのは `PushPullFace` と `ThickenFace` の
2つだけで、**ブーリアン演算子がツリーに存在しません**でした。`FilletEdge` も
`{dx, dy, dz, edge_index, radius}` で直方体を作り直します。つまり
「作る → 穴をあける → 角を丸める」という形は履歴で表せず、上流の寸法を変えて
下流を追従させることもできませんでした。

**足したもの**（既存の `FeatureOp` は1つも変えていないので、従来の履歴は
そのまま通ります）:

- `FeatureOp::Boolean { op, tool: Vec<FeatureOp> }` — ツール側が**それ自身
  フィーチャー列**で、`Translate` / `Rotate`（これも新設）で位置合わせして
  組み立てます。`recompute` は `FeatureTree::evaluate(&[FeatureOp])` に切り出し、
  ツール側も同じ語彙で評価されます。
- `FeatureOp::FilletSolidEdge / ChamferSolidEdge` — 稜を ID ではなく
  `EdgeSignature`（中点・向き・長さ・二面角。始終点の入れ替えに非依存）で
  指します。ID は作り直すたびに変わるので、履歴に書いても次の再計算で
  指すものがありません。

一致度が 0.9 未満のとき、および1位と2位が同着のときは、**別の稜を黙って
丸めずに失敗**します。間違った稜を丸めた結果は「閉じた別の形」になり、
後から見て気付けないためです。

**測った値**（`crates/zenith_algo/tests/feature_tree_parametric_test.rs`、4件）:

| 確認したこと | 結果 |
| :--- | :--- |
| ブーリアンを含む履歴が一続きに評価される | 40×40×20 − φ12 貫通が解析解と 1e-9 |
| 上流の寸法を変えると下流のフィレットが同じ稜に付き直す | dx 20→26 で面数7のまま、体積が新しい寸法の閉じた式と 1e-11 |
| 稜が消えたら別の稜を丸めない | `No edge matches...` で失敗 |
| ブーリアンの後に面取りを重ねられる | 解析解と 1e-10 |

---

### 4-30. Python から立体を持ち回れなかった（2026/08/22）

`zenith_py` の関数はすべて `make_X(...) -> Mesh` のワンショットでした。立体を
受け取って返す型が無いので、Python 側では作った立体を次の演算に渡せません。
組み合わせが要るたびに Rust 側へ専用関数（`make_exact_drill_boolean` など）を
足すしかなく、今回の `EdgeBlender` も公開できませんでした。

**入れたもの**: `crates/zenith_py/src/solid.rs`（`zenith_cad.Solid`）。すべての
メソッドが**新しい立体を返す**不変オブジェクトです。

```python
import zenith_cad as z

part = z.Solid.box(40, 40, 20).difference(
    z.Solid.cylinder(6, 20).translated(20, 20, 0)
)
for edge in part.blendable_edges():
    part = part.fillet_edge(edge["edge_id"], 2.0)
part.to_step("part.step")
```

生成（`box` / `cylinder` / `sphere` / `cone` / `torus` / `regular_prism` /
`from_step` / `all_from_step`）、変換（`translated` / `rotated` / `mirrored`）、
ブーリアン（`union` / `difference` / `intersection` / `difference_all`）、
稜（`edges` / `blendable_edges` / `fillet_edge` / `chamfer_edge` / `fillet_edges` /
`chamfer_edges`）、面（`faces` / `push_pull_face` / `taper_face`）、
計測（`mass_properties` / `validate` / `clash_status` / `volume` / `face_count`）、
出力（`tessellate` / `to_step` / `to_step_string`）、後片付け（`sewn`）。

`validate()` は `unshared_edge_entity_uses` を返します。0 でなければ、その立体は
閉じてはいるが稜を選べない状態です（4-26）。

**測り方**:

```bash
cargo build --release -p zenith_py     # PYO3_PYTHON が要る環境ではそれも指定
py tools/verify_solid_api.py
```

20項目すべてグリーン。「作る → ブーリアン → 稜を選ぶ → 丸める → STEP →
読み直す」が一周し、体積が閉じた式と一致したまま戻ります。凹稜の指定は
`ValueError` に理由が入って返ります。

---

### 4-31. 面と稜が実形状の2倍あった。平面を NURBS で持っていた面もあった（2026/08/22）

`cargo run --release -p zenith_algo --example face_merge_probe` で測れます
（期待した面数に届かなければ非ゼロ終了）。

**症状**: 穴あけやブーリアンは平面を扇形・短冊に割ってから組み直すので、
割った跡がそのまま残ります。実形状に無い面と稜が選択肢に並び、STEP の
エンティティが倍になり、平面しか受け付けない演算に無関係な候補が混じります。

その上でもっと重かったのが、**`HoleBuilder::make_drilled_box` が 16 面すべてを
NURBS で持っていた**ことです。平面の上下面も側壁も NURBS でした。有理 NURBS の
像は制御点の凸包に入るので幾何としては正しいのですが、

- 平面しか受け付けない演算が一切掛からない
  （**穴あき直方体はフィレットの候補が 0 本**でした）
- 質量積分が線積分の閉じた経路ではなく求積に落ちる
- STEP に `PLANE` ではなく `B_SPLINE_SURFACE` が出る

**入れたもの**: `crates/zenith_algo/src/merge_faces.rs`（`FaceMerger`）。
`simplify_solid` は3段です。

1. `planarize` — 制御点が公差内で同一平面に乗り、重みが正の NURBS 面を
   **平面として持ち直す**。凸包に入る以上、像はその平面に乗るので**近似では
   ありません**。
2. `merge_coplanar` — 支持平面が一致し稜で繋がっている面を1つの塊にまとめ、
   塊の内側で2回使われている稜を落として境界ループを組み直す。内側に空いた
   ループ（穴の口）は内側ワイヤになる。
3. `merge_collinear_edges` — 併合の跡に残った継ぎ目のうち、頂点に2本しか
   集まらず両側が一直線なものを1本に繋ぐ。

外側ループが2つ以上できる塊は、1枚の面にすると領域が繋がっていないことに
なるので**そのままにして理由をレポートに残します**。曲面は併合しません。

**測った値**:

| 立体 | 面 | 稜 | フィレット候補 | 体積のずれ |
| :--- | :--- | :--- | :--- | :--- |
| 直方体（併合対象なし） | 6 → 6 | 12 → 12 | 12 → 12 | 0 |
| **穴あき直方体 φ16** | **16 → 10** | **32 → 24** | **0 → 12** | 2.2e-14 |
| **L 字角柱（ブーリアンの差）** | **14 → 8** | **28 → 18** | 9 → 13 | 1.5e-16 |
| 直方体 − 円柱 | 10 → 10 | 24 → 24 | 12 → 12 | 0 |
| 直方体 ∪ 台座 | 11 → 11 | 24 → 24 | 20 → 20 | 0 |

円筒側面が1枚にならないのは意図的です（全周1枚のパッチを OpenCASCADE が
正しく積めないため、4分割のまま）。したがって穴あき直方体の 10 面
（側面4＋環状の上下面2＋円筒4）が正解で、7 ではありません。

**外からの確認**: 面と曲面の種類が変わるので、他カーネルが同じ形として
読めるかを測ります。

```bash
cargo run --release -p zenith_algo --example export_simplified
& "C:\Program Files\FreeCAD 1.1\bin\python.exe" tools/verify_simplified.py
```

4件すべて valid closed solid、面数・稜数も一致、体積は OpenCASCADE の測定で
**1e-12 以内**。

**まだ自動では掛けていません。** ブーリアンやビルダーの出口に入れると面数が
変わるので、既定にするかは判断待ちです。いまは `FaceMerger::simplify_solid`
と Python の `Solid.simplified()` / `Solid.simplify_report()` で明示的に呼びます。

---

### 4-32. 平面を NURBS で持っていたビルダーと、p-curve の細分が1区間に食われていた件（2026/08/22）

#### (a) どのビルダーが平面を NURBS で持っているか、数えた

```bash
cargo run --release -p zenith_algo --example planar_face_audit
```

制御点が公差内で同一平面に乗る有理 NURBS 面は、像が制御点の凸包に入る以上
その平面に乗っています。にもかかわらず `FaceGeometry::Nurbs` で持っていると、
平面しか受け付けない演算が一切掛かりません。

全23ビルダーを数えて、該当は**2つだけ**でした。

| ビルダー | 該当 |
| :--- | :--- |
| `HoleBuilder::make_drilled_box` | 16面中 **12面** |
| `GearBuilder::make_spur_gear` | 110面中 **36面**（歯1本あたり2枚の歯元側面 × 18歯） |
| 他21ビルダー | なし |

**直し方**: 個々のビルダーではなく、全ビルダーが通る共通の出口
`crate::validated_solid` に `FaceMerger::planarize_shell` を入れました。今後
追加されるビルダーにも自動で効きます。`planar_face_audit` は1枚でも残れば
非ゼロ終了するので、ゲートに使えます。

**注意**: 平面として持つだけでは足りません。穴あき直方体は12面が平面に
なった後でもフィレットの候補は **0 本**のままです。上下面が扇形に割れている
ため、稜の端で3枚目の面が1枚に定まらないからです。面の併合（4-31）まで
通して初めて12本になります。これは 3-A の判断材料です。

#### (b) 皿モミ穴が寸法によって失敗していた（着手前から）

```bash
cargo run --release -p zenith_algo --example countersink_range_probe
```

下穴4通り × 比4通り × 角度4通り = 64組のうち **7組が失敗**していました。
失敗は範囲の端ではなく**散在**します。比 2.0 は通るのに 1.8 が落ち、90 度が
落ちるのに 82 度と 120 度は通る。着手前のコミット `9e8e736` を worktree に
出して同じ probe を走らせ、**57 ok / 7 失敗で完全に同一**であることを確認
しました。今回の作業が持ち込んだものではありません。

**原因**: 失敗する面の p-curve の制御点数が **4096**（上限）でした。

p-curve は「弦の中点が 3D の辺から `tol.linear` = 1e-6 より離れていたら区間を
割る」で作られます。ところがこの配置では、SSI が辿った交線そのものが円筒面
から **9.78e-7** 離れていました。公差ぎりぎりの下限です。弦の誤差はこの下限を
下回れません。

走査は割った直後に**同じ左半分を見直す**構造だったので、詰まらない最初の
区間が上限 4096 点を独り占めし、**残りの区間は最初の8分割のまま**という
p-curve が出来ていました。そこが 3D の辺から最大 **8.5e-3** 離れます。

閉性の検査は通り、`validate_pcurves` だけが落ちるので、返るエラーは p-curve の
ずれを72件並べた文字列でした。呼び出し側にはどの寸法が悪いのか分かりません。

**直し方**: 走査を**通しで最大24回繰り返す**構造に変えました。1回の通しで
各区間はたかだか1度しか割りません（割った点の先へ進む）。詰まらない区間の
コストは1通しあたり1点に収まり、全区間が均等に詰まります。

**外した直し方も記録しておきます。** 最初は分割の可否を「割って弦が9割以下に
縮むか」に変えました。皿モミ穴は 64/64 になりましたが、**2件の既存テストが
劣化**しました（`face_split` の面積が解析解から 2.7e-6、円柱の斜め切断の
p-curve が 6.9e-4）。パラメータ付けが不均一な区間では中点で割っても片側が
誤差をほぼ保持するので、正当な分割まで止めていたためです。問題は打ち切りの
条件ではなく**予算の配り方**でした。

**測った値**: 64組すべて成功。体積は解析解
`w d h - π r^2 h - (円錐台 - 下穴が既に取っている分)` と **1e-6 以内**
（`countersink_range_test` が代表12組を常時検証。全数は probe）。
曲面ブーリアン45ケースの仕事量は 110,602,293 → **110,803,493 回**（+0.18%）。

**触らなかったもの**: `match_nurbs_outer_boundary_pcurve` は、パラメータ空間の
直線候補を**構成と同じ分割数**で測っています。乗っていない辺が節点だけで
一致して通る余地がありますが、外して測り直しても結果が変わらず、根拠が
測れませんでした。すり抜けても 37 点で測り直す `validate_pcurves` が落とすので
**誤答にはならず明示的なエラーになります**。コードにその旨を書き残してあります。

---

### 4-33. 重心と慣性は測られていなかった。計算は正しく、名前が違っていた（2026/08/22）

```bash
cargo run --release -p zenith_algo --example inertia_probe
```

体積と表面積は `builder_audit` が解析解と突き合わせていました。**重心と慣性は
どこも突き合わせていません**でした。慣性は体積より一段高い次数の積分なので、
体積が通る精度で通る保証はありません。

**計算そのものは正しかった**（原点まわりの閉じた式と最悪 1.8e-13）。

| 検体 | 体積 | 重心 | Ixx | Iyy | Izz |
| :--- | :--- | :--- | :--- | :--- | :--- |
| 直方体（角が原点） | 0 | 6.6e-17 | 0 | 0 | 0 |
| 円柱 r10 h25 | 1.4e-14 | 2.8e-14 | 3.2e-14 | 3.2e-14 | 1.8e-13 |
| 球 r12 | 3.9e-14 | 1.1e-15 | 8.7e-14 | 8.9e-14 | 9.1e-14 |
| 円錐 r10 h20 | 1.9e-14 | 1.4e-14 | 1.1e-14 | 9.5e-15 | 1.7e-14 |
| 直方体を x に 50 移動 | 0 | 2.6e-16 | 0 | 0 | 0 |

**probe を書く側が間違えた記録**: 円錐で 59% ずれる表示が出ましたが、誤って
いたのは probe の閉じた式でした（頂点まわりの `m(3R^2/20 + 3H^2/5)` をそのまま
底面中心まわりに使っていた。正しくは `m(3R^2/20 + H^2/10)`）。カーネルを疑う
前に式を疑ってください。

**欠陥は API のほうでした。** `inertia_diagonal` は「慣性モーメント主成分」と
書かれていましたが、返るのは**原点を通る座標軸まわりの対角成分**です。

- 原点まわりと重心まわりは、原点を角に置いた直方体で **4倍**違う
- 対称でない形では、対角成分と主慣性モーメントは一致しない
- 慣性積が無いのでテンソルを組めず、主慣性モーメントが出せなかった

**足したもの**:

- 慣性積 `inertia_products` = (∫xy dV, ∫yz dV, ∫zx dV)。平面の解析経路では
  `∫a^2 b = b0 ∫a^2 + bu ∫(a^2 u) + bv ∫(a^2 v)` と展開し、既にある3次までの
  モーメントだけで閉じる（近似なし）。曲面の求積経路とメッシュ経路にも同じ量。
- `inertia_tensor()` / `inertia_tensor_about(point)` /
  `inertia_tensor_about_center_of_mass()` / `principal_moments()`（重心まわり
  の固有値、ヤコビ法）。
- ドキュメントを実態に合わせ、「主成分ではない」と明記。

**一番強い検証**: 主慣性モーメントは剛体変換で不変です。テンソルの組み立て・
平行軸の定理・固有値のどれか1つでも間違っていれば、回した瞬間に値が動きます。

| | 主慣性モーメント |
| :--- | :--- |
| そのまま | 2600000 / 4000000 / 5000000 |
| (137, -44, 9.5) 平行移動 | **変わらず** |
| 斜め軸 (1,2,3) まわり 35度回転 | **変わらず** |

閉じた式との最悪誤差 **1.5e-15**（`inertia_test` 6件、`verify_solid_api.py` 3項目）。

---

### 4-34. 出力用メッシュは稜に沿って開いている（欠陥を測定・修正は取り下げ）（2026/08/22）

```bash
cargo run --release -p zenith_algo --example mesh_watertight_probe
cargo run --release -p zenith_algo --example mesh_watertight_probe -- --stitched
```

**B-Rep が閉じていることと、そこから作ったメッシュが閉じていることは別です。**
ここは一度も測られていませんでした。

**実測（既定の経路、16分割）**:

| 立体 | 三角形 | 開いた辺 | 非多様体 | 退化 |
| :--- | --: | --: | --: | --: |
| 直方体 | 12 | 0 | 0 | 0 |
| **円柱** | 3068 | **1152** | 0 | 0 |
| **円錐** | 3068 | **1152** | 0 | 0 |
| **穴あき直方体** | 3088 | **1152** | 0 | 0 |
| **球** | 4096 | 0 | **66** | **128** |
| トーラス | 8192 | 0 | 0 | 0 |

面を1枚ずつ三角形にするので、隣り合う面が境界を別々に刻みます。平面のキャップ
はたわみ基準で適応的に（同じ円に約512点）、曲面のパッチは指定分割数で（16点）
刻むため、稜に沿って開きます。

**効くのは出力先です。** STL は三角形しか持たないので、稜で開いていればスライサ
が「閉じていない」と言い、3Dプリンタに送れません。仕様書は STL を「3Dプリント用
標準フォーマット」と書いていますが、円柱・円錐・穴あき直方体・軸・ボルト・
フランジ・歯車——曲面と平面キャップを持つほぼ全ての機械部品——が出せません。

#### 直そうとして、取り下げました

`crates/zenith_tess/src/stitched.rs`（`tessellate_solid_stitched`）を書きました。
稜ごとに1本の刻み方を先に決め、その稜を使うすべての面が同じ点を使う方式です。
**既定にはしていません。** 分割数によっては非多様体が出るためです。

| 分割数 | 4 | 6 | 8 | 10 | 12 | 16 | 20 | 24 | 32 |
| :--- | :-: | :-: | :-: | :-: | :-: | :-: | :-: | :-: | :-: |
| 閉じるか | ok | **NG** | ok | **NG** | **NG** | ok | **NG** | **NG** | ok |

最初に測ったのが 4 と 16 だけで、**たまたま通る値を選んでいました**。分割数を
振って初めて分かりました。プローブを書くときは、通る値を1つ選ぶのではなく
**範囲を振ってください**（皿モミ穴 4-32 と同じ教訓）。

原因の一部は特定してあります。円弧と真っすぐな継ぎ目が同じパッチにあると、
稜ごとの刻み数が 1:2 になります。粗いほうの点は細かい格子の縁に**たまたま
乗る**ので検査を通り抜け、間の格子点に相手がいない T 字の継ぎ目ができます。
方向ごとの分割数と「格子の縁の全頂点に相手がいること」の照合を入れましたが、
6・10・12・20 分割では別の経路から残ります。**そこから先は未解明です。**

#### 途中で外した実装（同じ道を歩まないために）

| 試み | 測った結果 |
| :--- | :--- |
| 矩形パッチはグリッド、それ以外は境界の多角形だけ | トリムされたパッチの中身が弦になり、穴あき立体の体積が **7.7% 過大** |
| 三角形の重心を足して3分割 | 細長い三角形の長辺が縮まず、**4万三角形でも 0.26%** 残る |
| 既存の最長辺二分（Rivara）を、共有境界を割らない条件で再利用 | 0.026% まで改善。これは正しい |
| 巻き方を「面法線と頂点法線の同符号」で決定 | 粗い三角形が裏返り、ばねの体積が **758 に対して 1663** |
| 巻き方を uv 上の向きで決定 | 解決。三角形の粗さに依存しない |
| 稜の刻み数を指定分割数で一律 | 長い螺旋稜が足りず、パッチの境界が巻きをまたぐ。ばねの体積が **7.2% 不足** |
| 稜の刻み数を稜自身のたわみで決定 | 解決。ただし稜ごとに数が変わり、構造格子が組めなくなる |

#### この差し替えを試すと連鎖して壊れるもの

既定を差し替えたときに 23 件のテストが落ちました。内訳と、**そのとき私が
書いた見立てが後で外れた**ことを両方残します。

落ちたのはすべてブーリアンの検証ゲートで、原因は取り下げた実装が返して
いたメッシュの体積が **7.7% 過大**だったことです。ゲートは内外判定を
メッシュへの射線で行うので、そこまで狂ったメッシュを渡せば当然落ちます。

そのとき私は「ゲートの精度が表示の設定に依存している」「断面スライスが
三角形の並び方に依存している」と書きました。**どちらも測ったら再現
しませんでした。**

```bash
cargo run --release -p zenith_algo --example boolean_gate_probe
cargo run --release -p zenith_algo --example slice_robustness_probe
```

- **ゲート**: 7ケース（球・トーラスのブーリアンを含む）を 4〜32 分割で
  かけて、正しい結果は全部通り、誤答は全部落ちました。体積の境界では
  捕まらない誤答（離れた道具との差に、A を平行移動した複製を返す）も
  どの分割数でも捕まります。**判定は分割数で変わりません。**
- **断面**: 6立体を、格子行にちょうど乗る高さと乗らない高さで、9通りの
  分割数で切りました。**閉じなかった断面はゼロ**です。4〜8 分割で面積が
  解析解から 5e-5 ずれますが、これは近似の精度で、12 分割以上では乗ります。

既定の経路が返すメッシュに対しては、どちらも健全でした。**壊れた入力を
渡したときの挙動を、入力ではなく受け手のせいにしていました。**

---

### 4-35. 最短距離が頂点どうししか見ていなかった（2026/08/22）

```bash
cargo run --release -p zenith_algo --example distance_probe
```

`DistanceEngine::compute_min_distance` は、テッセレーションした2つのメッシュの
**頂点どうし**を総当たりしていました。三角形の内側を一切見ないので、最近接点が
頂点に来ない配置——実務のほとんど——で答えが桁で外れます。

| 配置 | 正しい距離 | 旧実装 |
| :--- | --: | --: |
| 200x200x2 の板の中央の上 3 mm に小球 | 3.0 | **136.6** |
| 同じ板の上 0.5 mm に小さな角材 | 0.5 | **138.6** |
| 5 mm めり込んだ2つの直方体 | 0.0 | **5.0** |

直方体の頂点は8個しかないので、板の上に置いた物体は必ず**板の隅**との距離で
測られます。めり込んだ立体には正の隙間を返していました。

**これはクリアランス検証に使う値です。**「隙間 0.2 mm」を答えるつもりの関数が
138 を返し、干渉している設計に「隙間あり」と答えます。仕様書には「箱・円柱・
球・自由曲面のクリアランス自動検証に対応」と書いてありました。

面と面が正対する配置（直方体どうしを軸に沿って並べる）と、球どうし・円柱どうし
は、たまたま最近接点が頂点に来るので通っていました。**既存のテストはその3つ
だけを見ていました。**

**直し方**:

1. 頂点どうしで**上界**を取る（真の距離以上なので枝刈りに使える）
2. その上界で枝刈りしながら、三角形どうしの最短距離を取る
   （辺と辺の9通り、頂点と面の6通り。面の内側も辺も見る）
3. 触れている・交わっている・片方が他方の内側にある場合は 0 を返す
4. 最後に最近接点を **B-Rep の面そのものへ交互に射影**して詰める

**途中で外した実装**: 4段目の「射影した点が本当にその面の上にあるか」を、
**メッシュからの距離**で代用しました。その帯を三角形の大きさから取ったので
直方体では 14 mm もあり、まったく別の面への射影まで通って、**離れた2つの
直方体の距離が 0** になりました。判定を p-curve のトリム境界に対する内外
判定（内側ワイヤ＝穴の中は面の外）に置き換えて解決しています。

「メッシュの近さで面の上かを代用する」のは、帯の取り方に答えが乗る筋の悪い
方法でした。トリムの内外は p-curve が持っているので、そちらに訊くべきです。

**測った値**: 7ケースすべてで誤差 **0.000e0**（球・円柱も含めて厳密）。
刻みを 8/16/32 と振っても答えは動きません（最大 8.9e-16）。刻みは探索の
出発点にしか効かず、答えは B-Rep へ詰めるためです。
`distance_closed_form_test` 5件と `verify_solid_api.py` 3項目が常時見ます。

---

### 4-36. 干渉判定が浅い食い込みを見落としていた（2026/08/22）

```bash
cargo run --release -p zenith_algo --example interference_depth_probe
```

干渉判定で危ないのは**見落とし**です。離れているものを干渉と言えば設計者が
確かめて終わりますが、食い込んでいるものを「隙間あり」と言えばそのまま製造に
流れます。

食い込み量を 5 mm から 0.001 mm まで減らして測ったところ、**板に 0.01 mm 以下
押し込んだ球が「隙間 0.010」と報告**されていました。干渉しているものに、
食い込み量とちょうど同じ大きさの正の隙間を答えます。

**原因は2つ重なっていました**。

1. 重なりの体積を 20^3 の格子で数えるので、**格子の目より薄い食い込みが落ちる**
2. 三角形どうしの距離は辺と辺・頂点と面しか見ないので、**交差している三角形の
   組では正の値になる**。浅い食い込みは距離からも分からない

2 は見落としやすい性質です。「距離が 0 なら干渉」は成り立ちますが、逆
（「干渉なら距離が 0」）は、この測り方では成り立ちません。

**直し方**: 格子に加えて、**表面の点がもう一方の内側にどれだけ深く入っているか**
でも見る。距離エンジンと同じ判定を共有します。

**途中で外した実装**: 判定を単に「点が相手の内側か」にしたところ、**面を共有
して触れているだけの2つの直方体が `Clash`** になりました。面の上の点は射線の
偶奇では内とも外とも出ます。「内側にあること」ではなく「**内側に公差より深く
入っていること**」を条件にして解決しています。

**併せて直したもの**: 報告する距離を [`crate::DistanceEngine`] 経由にしました
（4-35）。箱で篩って離れていると分かる速い経路でも、返す値は B-Rep まで詰め
ます。使われなくなったメッシュ距離のヘルパー3つは削除しました。

**古い挙動を要求していたテスト**: `refining_the_sampling_brings_the_distance_
down_toward_the_truth` は「分割を細かくすると距離が短くなり真の値に上から
近づく」ことを要求していました。これはメッシュの弦の上で測っていたときの
性質です。いまは 8 分割でも 32 分割でも解析解に乗る（√27 − 5 に対して
誤差 2e-15）ので、要求を「**どの刻みでも合っている**」に書き換えました。
後者のほうが強い要求です。

**測った値**: 食い込み 21 配置（直方体どうし・板とピン・板と球 × 7段階）
すべてで `Clash`、隙間のある 5 配置すべてで報告距離が閉じた式と一致。
面を共有して接触する配置は `Touching` のまま。
`interference_depth_test` 4件が常時見ます。

---

### 4-37. 監査で出た5件（2026/08/22）

引き継ぎ直後に、直近15コミット（`f247147` 以降、いずれも Antigravity と
の共作）を中心にコードを実測で当たり直しました。**中核には空洞がありません**
——ブーリアン、質量特性、SSI、STEP 入出力、Python 連携はいずれも解析解や
OpenCASCADE と 1e-11 以上で一致します。出たのは周縁部の5件です。

`todo!` / `unimplemented!` / `#[ignore]` は全ワークスペースで0件、警告も0件
でした。**それでも空実装は1つありました**（下記 IGES）。「マクロで印を付けて
いない未実装」は、この探し方では出ません。

#### (1) 書き出す STEP がすべて Part 21 の構文違反だった

複数の型を1つにまとめた複合エンティティ実体は、ISO 10303-21 では外側の
丸括弧が要ります。

```text
#13 = ( GEOMETRIC_REPRESENTATION_CONTEXT(3) ... REPRESENTATION_CONTEXT(..) );
```

括弧なしで書いていました。すぐ上の単位定義3行（`#9`〜`#11`）は括弧付きで
書けていたので、書き方を知らなかったのではなく1箇所だけ抜けています。

**なぜどのゲートも落ちなかったか。** OpenCASCADE のパーサがこの形を許すから
です。`verify_showcase`（24/24）も `verify_reexport`（7/7）も緑のままでした。
比較相手を自前の出力ではなく **OpenCASCADE が書いたファイル** に置いて初めて
差が見えます。

```text
OCC:     #68 = ( GEOMETRIC_REPRESENTATION_CONTEXT(3)
Zenith:  #13 = GEOMETRIC_REPRESENTATION_CONTEXT(3)
```

`step_conformance_test::test_every_complex_entity_instance_is_parenthesised`
が、データ行を1本ずつ見て「最初のエンティティの閉じ括弧より後ろに中身が
続くのに、全体が括弧で囲まれていない行」を数えます。**修正を戻して落ちる
ことを確かめてから**入れました。

#### (2) Coons 面が STEP から黙って消えていた

`write_face` は `Coons` / `Gordon` / `Triangular` に `None` を返し、
`write_closed_shell` がそれを捨てていました。

```text
B-Rep faces = 6  kinds = ["plane","plane","plane","plane","coons","coons"]
ADVANCED_FACE written to STEP = 4
```

穴の空いた殻を `CLOSED_SHELL` と称して書き出します。しかも
`export_solid_to_string` の戻り値は `String` なので、**失敗を表す場所が
ありません**（4-6 と 5 章に出てくる「型が答えを表せるか」と同じ形です）。

到達経路は実在します。`ThickenBuilder::thicken_coons_face` は底面・天面・
側面をすべて Coons 面で組みます。`thicken_shell` も同じ経路を通ります。

直し方は、捨てるのをやめて **標本した NURBS として書く** ことにしました
（`NurbsSurface3::approximate_surface`）。双線形の Coons パッチは補間で
厳密に再現され、実測の偏差は 1e-9 未満です。加えて内部の書き出しを
`Result` にし、ファイルに書く口は失敗を返すようにしました。
`step_face_parity_test` が「B-Rep の面の数 = `ADVANCED_FACE` の数」を見ます。

#### (3) 断面が既定の分割数で閉じない。見張るプローブが既定を試していなかった

FreeCAD 相互検証が **15/15 ではなく 14/15** でした。

```text
boolean_drilled_block ... kernel section failed:
  Section slicing produced 1 unclosed chain(s) alongside 3 closed loop(s)
```

`slice_robustness_probe` は「分割数によらず閉じるか」を見るためのものですが、
振っていたのは 4〜32 で、`SectionSlicer::slice_solid` が実際に使う
`DEFAULT_SECTION_TESSELLATION`（**128**）を一度も試していません。だから
「0 failures」と言い続けていました。**上限を上げるだけでは足りません。
既定そのものを列に入れてください。** 失敗は分割数に対して飛び飛びに出ます。

| 48 | 64 | 80 | 96 | 112 | 120 | 128 | 144 | 160 | 192 | 256 |
| :-: | :-: | :-: | :-: | :-: | :-: | :-: | :-: | :-: | :-: | :-: |
| ok | ok | ok | **×** | ok | **×** | **×** | **×** | **×** | ok | **×** |

原因の切り分けは4段まで降りました。

1. **線分は足りている。** 各線分の始点・終点には必ず相手がいる（宙に浮いた
   端点は 0 本）。だから「交線が取れていない」ではありません。
2. **分岐が偽物だった。** 溶接した端点で数えると、1点に最大6本が集まる
   分岐が 1628 個できていました。場所はすべてボア壁（半径 6.0）の上です。
3. **溶接公差が選べない。** 一致させたい端点どうしの隙間は 99% が厳密に 0、
   最大 2.0e-6。別物として残したい刻み幅は最小 1.0e-6。**両者が重なる**ので、
   どの値を選んでも繋ぎ落とすか潰すかのどちらかになります。
4. **メッシュは閉じている。** 256分割まで測って open 0 / 非多様体 0 /
   退化 0。ならば1本の辺はちょうど2枚の三角形に共有されます。

そこで**距離をやめて位相で見る**ことにしました。断面上の点に、それが生まれた
場所の鍵（`SectionKey::Vertex(i)` または `SectionKey::Edge(i, j)`）を持たせ、
鍵が等しいものだけを繋ぎます。閉多様体メッシュでは辺の鍵に対する相手が
ちょうど1つなので、**公差はどこにも要りません**。4-8 の「p-curve では
原理的に決まらないので位相で見る」と同じ形です。

なお本当の元凶はもう1段奥にあります。ブーリアンの結果は、同じ形をビルダーで
作ったものに比べて**三角形が3〜5倍多く、分割数に対して単調でもありません**
（96分割で 658656、128分割で 465072）。`SamplePlan::for_segments_for_edge`
が稜ごとにたわみ基準で倍々に刻むためです。断面は位相で繋ぐようにしたので
影響を受けなくなりましたが、メッシュそのものは重いままです。

#### (4) N辺コーナーブレンドが、パッチを1枚も返していなかった

```text
N=3 -> patches.len() = 0
N=4 -> patches.len() = 0
```

`GregoryPatch4::new` のコーナー検査が毎回落ち、`if let Ok(patch)` がそれを
捨てて、空の `patches` を載せた `Ok` が返っていました。テストは
`boundary_curves.len()` と中心点しか見ていないので通ります。

理由は2つ重なっていました。

- パッチの1辺になるのは境界の**半分**なのに、境界をまるごと渡していた
  （コード中のコメントは `Boundary(i_half)` と書いてあり、意図とは違う）。
- `GregoryPatch4` が境界曲線の媒介変数域を `[0, 1]` と決め打ちしていた。
  `NurbsCurve3::split_at` は元のノット値を保つので、半分に割った曲線の域は
  `[0, 0.5]` や `[0.5, 1]` になり、`evaluate(0.0)` が域の外を指します。

境界を中点で二分し、評価を `edge_point`（域を `[0, 1]` に写す）に通して、
`if let Ok` を `?` に変えました。N = 3, 4, 5 で N 枚返ることを検査します。

**`if let Ok(..) { push }` は、失敗を件数の減少に変換します。** 呼び出し側
から見ると空の成功なので、エラーとしては現れません。

#### (5) IGES は引数を読んでいなかった

```rust
pub fn export_solid_to_string(_solid: &Solid, product_name: &str) -> Result<String, String>
```

先頭のアンダースコアがすべてです。固定文字列と中身の無い Entity 186 を1つ
書くだけで、どんな立体を渡しても同じ6行が出ます。テストは `"S0000001"` の
ような文字列の有無を見るだけで、しかも**空のシェル**を渡していました。
両者は矛盾なく通り続けます。仕様書には「Entity 128 / 102 / 124 出力」と
書いてありましたが、ソースに 128 はありません。

各面の支持曲面を Entity 128（有理Bスプライン曲面）として書くように実装し
直しました。80桁固定レコード、Global 26 フィールド、D は2行/エンティティ、
P は DE の行番号を 65〜72 桁に持ちます。

書いた直後、OpenCASCADE が 20x30x40 の箱を **508x762x1016** として読みました。
ちょうど 25.4 倍、つまりインチとして解釈しています。原因は Global の1番目と
2番目、区切り記号の宣言でした。

```text
Zenith: 1H,,1H;,12Hbox_20x30x40,...
OCC:    ,,31HOpen CASCADE IGES processor 7.8,...
```

`1H,` という宣言は**値そのものにカンマを含みます**。フィールドを先にカンマで
割る読み手はここで数を取り違え、14番目にあるはずの単位フラグを見失います。
空にして既定（`,` と `;`）に任せると、単位も位置も一致しました。26番目の
フィールドが欠けていたのと、レコードの途中でフィールドを割っていたのも
併せて直してあります。

```text
subject                      ours    occ  shrink        grow    verdict
box_20x30x40                    6      6  0.000e+00  0.000e+00  ok
cylinder_r10_h40                6      6  0.000e+00  0.000e+00  ok
sphere_r10                      8      8  0.000e+00  0.000e+00  ok
cone_r10_r4_h20                 6      6  0.000e+00  0.000e+00  ok
drilled_box_30x30x15_r5        16     16  0.000e+00  0.000e+00  ok
```

はみ出しが 0 なのは、自前ビルダーの曲面が元から面ごとのパッチに割れていて、
パラメータ矩形の縁がそのまま面の境界になっているからです。**トリム
（Entity 144 / 142 / 126）は書いていない**ので、他カーネルから読んだ全周
1枚の面を持つ立体では、はみ出します。そこは制限として残ります。

常設ゲートは `tools/verify_iges.py`（不一致で非ゼロ終了）です。

```bash
cargo run --release -p zenith_algo --example export_iges_suite
& "C:\Program Files\FreeCAD 1.1\bin\python.exe" tools/verify_iges.py
```

#### おまけ: スケッチの自由度が、幾何学的にありえない値を出していた

`sketch_solver_probe` が「Total DOF = 2, Rank = 5」と印字していました。
自由変数が2本なら階数は2を超えられません。ヤコビアンを**全点ぶんの列**で
組んだまま特異値を数え、自由度は自由点だけで数えていたためです。
`remaining_dof` は `saturating_sub` で 0 に丸められるので、**まだ動ける
スケッチが `FullyConstrained` と報告されます**。

列を自由変数に絞ったところ、今度は既存テストが2件落ちました。
`add_fixed_point` が `FixedPoint` 拘束（2式）も足すので、その2式が
「冗長」に積み上がったからです。**以前は2つの誤りが打ち消し合って
正しい数が出ていました。** 式の本数を種類から数えるのをやめ、制限した
ヤコビアンで**ゼロでない行**を数えるようにして両方揃いました。

#### 直していないと分かっているもの

- **`GregoryPatch4` は Gregory パッチではありません。** `tangents` は全ゼロの
  まま未使用、`inner_points` は4隅だけから固定係数（0.444 / 0.222 / 0.222 /
  0.112）で決まり、境界曲線を大きく湾曲させても**値が1ビットも動きません**。
  評価式は Coons ＋ `16u(1-u)v(1-v)` のバブル項です。そもそも `new` が
  クロス方向接線を受け取らないので、隣接面との $G^1$ 連続は原理的に達成
  できません。仕様書の記述を実装に合わせて書き換えました。
- **歯元は真のトロコイドではありません。** 半径を線形、角度を smoothstep で
  補間し、`* 0.2` の係数で寄せているだけで、ホブ歯先のころがり軌跡は計算して
  いません。`rf = 0.38 m` は算出されますがその係数のスケールにしか使われ
  ません。断面積の閉じた式（`involute_profile_area`）も未対応のままです。
- **`DirectModeling::fillet_solid_edge` などは `EdgeBlender` の別名**です
  （各3行）。新しい能力はありません。
- **`thicken_shell` は各面を個別に厚み付けして Union するだけ**で、テストは
  同一平面の長方形2枚（＝箱2つの和）しかありません。非平面の隣接シートで
  成立する保証はありません。
- **DXF の CENTERLINE / HATCH レイヤー**はテーブルに定義されるだけで、
  割り当てるコードがありません。線種は全レイヤー CONTINUOUS です。
- **`crates/zenith_server`** は TCP プロトコルの骨組みだけで、常に空メッシュを
  返します（`write_generate_mesh_empty`）。カーネルを呼んでいません。

---
### 4-38. 他カーネルのファイルで読めない形を、名指しできるようにした（2026/08/22）

4-37 のあと、「STEP が実用であること」を軸にインポーターを当たり直しました。

#### 読めなかったものが、読めなかったと言っていなかった

知らない曲面に当たると**原点を通る既定の平面**を、知らない曲線に当たると
**端点を結ぶ直線**を返していました。

曲面のほうは、置き換わった平面を下流の p-curve 検証が必ず落とすので、
**誤答ではなくエラー**になっていました。実測すると、

```text
Solid validation failed with 138 outer-shell errors
  (Face 0 planar p-curve outer loop is degenerate; ...
   boundary point on outer loop is off surface by 4.000000e1; ...)
```

読めなかった理由がどこにも出てきません。**原因の分からないエラーは、
追いかける人に幾何を疑わせます**（監査中に一度そうしました）。

曲線のほうはもっと悪く、楕円弧を弦に置き換えても**その弦は元の平面の上に
載る**ので、検証を素通りしうる形でした。閉じた円が退化として捕まるのは、
両端が同じ点になるという運の良さのおかげです。

どちらも、エンティティ名を挙げてエラーを返すようにしました。

```text
Unsupported surface entity SURFACE_OF_LINEAR_EXTRUSION (#58).
  Zenith reads PLANE, CYLINDRICAL_SURFACE, CONICAL_SURFACE, ...
```

差し替えをやめても、OpenCASCADE が書いた既存の検体はすべて読めたままです
（`step_import_audit` 全件）。フォールバックは正規のファイルには**一度も
必要ありませんでした**。

#### 何が足りないかは、相手に書かせて確かめた

対応するエンティティを勘で選ばないよう、`tools/occ_reference_swept.py` を
足して OpenCASCADE に書かせました。

| 検体 | 出てきたエンティティ |
| :--- | :--- |
| 母線を回した輪 | `CIRCLE`, `CYLINDRICAL_SURFACE`, `PLANE` |
| スプライン断面の押し出し | `B_SPLINE_CURVE_WITH_KNOTS`, `PLANE`, **`SURFACE_OF_LINEAR_EXTRUSION`** |
| 楕円断面の柱 | **`ELLIPSE`**, `PLANE`, **`SURFACE_OF_LINEAR_EXTRUSION`** |

**回転体は `SURFACE_OF_REVOLUTION` になりませんでした。** OpenCASCADE は
解析曲面に落とせる形は落とします。事前に「回転面が要る」と思って書き始めて
いたら、要らないものを作っていました。

> **補足（4-60、2026/08/23）**: これは**この母線に限った話**でした。ここの
> 母線は直線なので円柱に落ちます。**スプラインの母線**を回すと OpenCASCADE は
> `SURFACE_OF_REVOLUTION` を書き、インポーターはそれを読めませんでした。
> 「1つの検体で出なかった」は「要らない」ではありません。挽き物の検体
> （`occ_reference_shapes.py` の `revolved_vase`）で分かり、いまは読めます。

#### 足したもの

**`ELLIPSE`**。楕円は単位円のアフィン像で、有理 NURBS はアフィン変換のもとで
不変（制御点を写して重みはそのまま）なので、円の構成の半径を軸ごとに置き
換えるだけで**厳密**に表せます。近似ではありません。媒介変数の角度は幾何的な
角度ではないので、点を各軸の半径で割って単位円に戻してから測ります。

**`SURFACE_OF_LINEAR_EXTRUSION`**。`S(u,v) = C(u) + v V` は `v` について1次
なので、`v` 方向の次数を1にして両端の制御点列を置けば厳密です。有理曲線でも、
同次座標で見ると分母が `v` に依らないため、重みはそのままで制御点だけ
平行移動すれば足ります。

#### 覆い漏らしを恐れて足したマージンが、そのまま答えに乗った

掃引面は STEP では範囲を持たないので、`v` の範囲を面の境界から決めます。
最初、覆い漏らしを恐れて **2e-6 だけ広げました**。結果:

| | 側面積 | 体積 |
| :--- | ---: | ---: |
| マージンあり | +3.0e-6 | +1.3e-6 |
| マージンなし | — | **6.8e-14** |

しかも、分割数を 32 から 256 まで振っても**1桁も動きません**でした。
平面キャップのほうは 1.5e-14 で厳密なままです。つまり楕円そのものは正しく、
**測っている対象のほうが違う**。この面の境界はパラメータ矩形そのものなので、
広げたぶんがそのまま積分に入っていました。

制御点の凸包から取った範囲だけで必ず覆えるので、マージンは外しました。

**動かない差は、求積の粗さではありません**（5章の「残差を疑う手は分割数を
変えて動くか」の、今回の実例です）。

#### 突き合わせ

| 検体 | 相手 | 相対差 |
| :--- | :--- | ---: |
| 楕円柱 | 閉じた式 $\pi a b h$ | **6.8e-14**（64分割）、4.5e-11（32分割） |
| スプライン押し出し | OpenCASCADE が同じファイルを読んだ値 | **8.1e-11** |
| 回した輪 | 同上 | 2.6e-10 |

検体は `crates/zenith_algo/tests/fixtures/` に置いて `include_str!` で読むので、
`target/` を消しても検査は走ります（`foreign_swept_surface_test`）。

---
### 4-39. ブーリアン結果のメッシュが重かった理由は、書いてあったものと違った（2026/08/23）

3-Z に「同じ形なのにブーリアン結果が最大 17.5 倍重く、分割数に対して単調でも
ない」と書き、原因は `SamplePlan::segments_for_edge` が稜ごとにたわみで倍々に
刻むせいだ、と書きました。**その原因推定は外れていました。**

#### 稜の刻みは、両者とも頼んだとおりだった

稜ごとの刻み数を数え直すと、こうです。

| | 稜の本数 | 刻み数の内訳（n=48 / 96 / 128） |
| :--- | ---: | :--- |
| ビルダーの穴あき箱 | 32 | 全部 48 / 全部 96 / 全部 128 |
| ブーリアンの穴あき箱 | 24 | 全部 48 / 全部 96 / 全部 128 |

**どの稜も、頼んだ分割数ちょうど**です。倍々に膨らんだ稜は1本もありません。
それどころか、ブーリアン側のほうが稜が**少ない**（24 対 32）。3-Z に書いた
説明は、コードを読んで「ここが怪しい」と思ったところをそのまま原因として
書いたもので、**数えていませんでした**。

#### 本当の原因は、格子を張れるかどうかの判定だった

面の内部の張り方には2つの経路があります。

- **構造格子**: 境界がパラメータ矩形の縁を1周している面。頼んだ分割数
  どおりの規則的な格子になる。
- **earcut ＋ 適応細分**: それ以外の面。境界の多角形から三角形を起こし、
  たわみが目標を下回るまで最長辺を割り続ける。**頼んだ分割数は上限として
  効かない。** コード中のコメントに「円柱1枚で 2172 → 16828 三角形に
  膨らむ」と実測が残っています。

どちらを通ったかを数えるカウンタ（`work_counter` の `grid_patches` /
`earcut_patches`）を足して測ると:

| | 構造格子 | earcut |
| :--- | ---: | ---: |
| ビルダーの穴あき箱 | 4 | 0 |
| ブーリアンの穴あき箱 | **0** | **4** |

ボア壁の4枚が、全部 earcut 側に落ちていました。

#### なぜ落ちるのか

判定は「境界の uv が、**曲面のパラメータ領域**の縁に乗っているか」でした。

ブーリアンの結果は、支持曲面を**元の立体のまま持っています**。全高 40 の円柱
から箱（高さ 20）を削ると、ボア壁の面は円柱の一部しか使いませんが、曲面は
40 のままです。すると面の境界は領域の**内部**の等パラメータ線上にあり、
「縁に乗っていない」と判定されます。実測で、境界 64 点のうち **30 点**が
そう判定されていました。

ビルダーの `make_drilled_box` は、ボア壁のパッチを箱の高さちょうどで作るので、
境界が領域の縁に乗ります。**形は同じでも、支持曲面の広さが違う。**

#### 直し方

縁の判定を、曲面の領域全体ではなく**境界自身が囲んでいる矩形**に対して行う
ようにしました。内部の等パラメータ線も、その矩形から見れば縁です。矩形の外に
出る境界（斜めに切られた面など）は、これでも弾かれます。

#### 結果

| 分割数 | 8 | 16 | 32 | 48 | 64 | 96 | 128 | 192 | 256 |
| :--- | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| 前（ブーリアン） | 1472 | 11616 | 89904 | 329640 | 491904 | 658656 | 465072 | 745880 | 425856 |
| 後（ブーリアン） | 592 | 2192 | 8464 | 18832 | 33296 | 74512 | 132112 | 296464 | 526352 |
| ビルダー | 592 | 2192 | 8464 | 18832 | 33296 | 74512 | 132112 | 296464 | 526352 |

**同じ形が、経路によらず同じ枚数になりました**（比 1.00）。単調性も戻り、
48分割では 17.5 倍軽くなっています。

#### 教訓

**コードを読んで見当を付けたところを、数えずに原因として書きました。**
3-Z は「測定済み・未着手」と題していましたが、測ってあったのは症状
（三角形の枚数）だけで、原因のほうは読んだだけです。症状を測ったことに
安心して、原因も測った気になっていました。

`segments_for_edge` は実際に倍々に刻むコードなので、読めばもっともらしく
見えます。**もっともらしさは、この repo では通貨になりません。**

---

### 4-40. 歯元を、形を似せるのではなく工具で削るようにした（2026/08/23）

`make_spur_gear_with_root_fillet` の「ホブ盤創成トロコイド」は、中身がこうでした。

```rust
let w = 3.0 * u * u - 2.0 * u * u * u;          // smoothstep
let r_u = root + (base - root) * u;             // 半径は線形
let angle_offset = (1.0 - u) * (rf / base_radius) * 0.2;
```

`rf = 0.38 * module` は算出されますが、**その係数のスケールにしか使われて
いません**。工具のころがりはどこにも出てきません。検査は「体積が正である
こと」だけだったので、S字に見えるかぎり通り続けます。

#### 形は眺めても分からない。定義で測る

フィレットは、ホブの歯先丸み（半径 $\rho$）が転がりながら掃いた円の
**包絡線**です。だとすれば次の2つが同時に成り立ちます。

- どの転がり角の工具円も、フィレットに**食い込まない**。
- フィレット上のどの点も、**どれかの工具円に乗っている**。

片方だけでは足りません。歯底円をそのまま伸ばした形は工具に食い込みませんが、
工具が触れないので創成された形でもない。**「削り残さない」と「削った跡で
ある」は別の主張です。**

#### 作り方

1. 歯先丸みは、工具の歯面と歯先線の両方に接する。そこから丸みの中心が
   ラック座標で決まる。
2. 転がりがその中心を運ぶ軌跡がトロコイド。フィレットはそれを $\rho$ だけ
   法線方向へずらしたもので、**いちばん深い転がり角で歯底円に接する側の枝**を
   採る。
3. フィレットが終わる半径（工具の歯面と丸みの継ぎ目が削る位置）まで掃く。
   終端は二分法で詰める。刻んだまま採ると行き過ぎて、インボリュートの
   始まりと合わない。
4. インボリュート歯面は、基礎円ではなく**その終端半径から**始める。

#### 測って直した1点

工具の本体がラック座標のどちら側にあるかで、丸み中心の符号が変わります。
取り違えると、フィレットの最深点が歯溝ではなく**歯の下**に出ます。

| | 実測 |
| :--- | ---: |
| 歯の中心 | 1.65806 rad |
| 歯の右歯面（歯底で） | 1.55589 rad |
| 誤った符号での最深点 | **1.65095 rad** |
| 正しい符号での最深点 | 1.49067 rad |

歯の中心は、触れているピッチ点より**大きい**角にあります。歯車角が大きい側は
$u < 0$ なので、歯車の歯は $u < 0$、それを削る工具の歯は $u > 0$ です。

符号を直すと、フィレットの外端の角度が 1.585680 から 1.555913 になり、
インボリュートの開始角 1.555894 と合いました。

#### 測った値

| 何を | 結果 |
| :--- | ---: |
| 工具円のフィレットへの食い込み（最大） | **0.000e0**（厳密） |
| フィレット上の点から工具円までの外れ（最大） | 5.075e-7（θ を 4000 で刻んだ幅） |
| 外端とインボリュート開始角の差 | < 1e-6 rad（5種の寸法で） |
| フィレットの角度スイープ | 0.0652 rad |

最後の行が、旧実装との差をいちばん端的に表します。旧実装の角度補正は
`rf / base_radius * 0.2 = 0.0090 rad` が上限で、**本物の 7 分の 1** です。
`test_the_fillet_actually_turns` はこの差で旧実装を落とします。

#### 独立な突き合わせ

外端がインボリュートの開始点と一致することは、**別々の経路から出た2つの数が
合う**という主張です。フィレットの角度はラックの転がりから、インボリュートの
角度は `inv(a) = tan(a) - a` から出ています。共通の中間結果を通りません。

#### 既定は変えていない

`make_spur_gear`（歯元は半径方向の直線）はそのままです。`involute_profile_area`
の閉じた式がこの形に対して厳密で、`builder_audit` の外の物差しになっている
からです（実測 1.99e-9）。トロコイド版の断面積には閉じた式が無いので、
そちらは包絡線の性質で検査しています。

**閉じた式を持つ検体を、閉じた式を持たない検体で置き換えないこと。**
物差しを失います。

#### 検証のための公開 API

`GearBuilder::root_fillet_generation` が、工具の運動とフィレットをまとめて
返します。**曲線が包絡線かどうかは、それが包む族を渡さなければ外から測れ
ません。** 検査のために内部を見せる必要があるなら、見せるほうが、検査を
弱めるより良いと判断しました。

---

### 4-41. 残っていた「文書化済み・未着手」を片づけた（2026/08/23）

4-37 で「直していないと分かっているもの」として並べた6件のうち、5件を実装し、
1件は**実装しないと決めました**。決めたことも記録に残します。

#### (1) STEP に 2D のトリム境界（p-curve）を書くようにした

書いていたのは3Dの辺だけでした。`write_edge_loop` にはこう書いてありました——
「p-curve は出さない。OCC 自身も出さないし、平面をスプライン弧で切った形も
無しで往復する」。**どちらも本当で、どちらも論点ではありません。** 往復するのは
OpenCASCADE が 2D 境界を自分で求め直せるからで、求め直さない読み手には
何も残らない。

各稜の3D曲線を `SURFACE_CURVE` で包み、その稜を使う面ごとに `PCURVE` を並べる
ようにしました。閉じた多様体では稜はちょうど2枚に共有されるので、
**`SURFACE_CURVE` あたり `PCURVE` はちょうど2本**になります
（`step_conformance_test` が数えます）。

書く前に、書くものの質を測りました。`validate_pcurves` を全検体の全面に当てて
**3.2e-15 〜 2.9e-12**。新しく推測した境界ではなく、`pcurve_fidelity_probe` が
ずっと見張ってきたものです。

**それでも代償はありました。** OpenCASCADE がこちらの書き戻しを測った体積の、
解析解に対する相対誤差:

| 検体 | 前 | 後 |
| :--- | ---: | ---: |
| cone | 9.18e-12 | 9.18e-12 |
| cone_full | 3.12e-12 | 8.70e-12 |
| cylinder | 9.19e-12 | 9.19e-12 |
| **sphere** | **1.19e-13** | **1.71e-10** |
| **sphere_capped** | **1.74e-12** | **5.43e-10** |
| torus | 1.85e-11 | 1.85e-11 |
| torus_segment | 1.59e-11 | 1.59e-11 |

球で3桁落ちます。絶対値では 4188.79 mm³ に対して 7.2e-7 mm³ で、同じ球を
OpenCASCADE 自身が B-spline 化したものは 4.4e-4（どちらの列より6桁悪い）
です。取引としては見合うと判断しましたが、**取引であることは書き残します**。

受け入れる前に2つ確かめました。導出した p-curve を書かず保持しているものだけに
絞っても**数字は動きません**（インポーターが既に付けているため）。それと、
p-curve の誤差を直接測ろうとした最初のプローブが 14〜40 mm と報告しましたが、
それは**セグメントの向きと媒介変数域を無視した測り方**の誤りでした。
両方を扱う `validate_pcurves` では 1e-15 です。

`verify_reexport` の許容を 1e-6 から **1e-8** に締めました。1e-6 は実力より
7桁緩く、球が 1e-13 から 1e-10 へ動いても素通りします。気づいたのは、
表の数字を前回の走行と見比べたからです。

#### (2) `GregoryPatch4` に、名前どおりクロス接線を持たせた

4隅だけから固定係数で内部点を決めていたものを、**双3次ベジエの内部4点を
双子で持ち、有理的に混ぜる**本来の構成に置き換えました。

```text
P11(u,v) = (u * b11_from_v0 + v * b11_from_u0) / (u + v)
```

`v -> 0` で `v=0` 辺のリボンの値に、`u -> 0` で `u=0` 辺の値になります。
だから**4辺すべてのクロス接線を同時に満たせます**。隅のツイストが揃って
いる必要はありません。

途中で2回間違えました。どちらもテストが見つけたもので、読んで気づいたのでは
ありません。

| 間違い | 指定した接線からの外れ |
| :--- | ---: |
| リボンで境界の制御点まで上書きした | 10.25 |
| 双子の重みを逆に掛けた（`v=0` 側の双子に `v` を掛けた） | 5.27 |

直したあとの実測: 境界には **2.3e-16** で乗り、クロス接線の残差は刻み 1e-6 で
3.4e-5、刻みを10分の1にすると残差も10分の1に落ちます。**片側差分の誤差で
あって、パッチのずれではありません。** 検査はその比を見ており、1つの緩い
閾値で通すのをやめました。

N辺ブレンドは、各セルのリボンを共有辺から同じ規則で作るので、隣り合うセルが
リブの上で一致します（実測で**隙間 0.000e0、法線の食い違い 0.0000 rad**）。

#### (3) `zenith_server` が、実装していない動作をもっともらしく成功させていた

19 の動作のうち中身があるのは `create_stack` と `delete_stack` だけで、
残りはこうでした。

- `generate_mesh` は三角形0枚のメッシュを返す。クライアントからは
  「モデルが空」と区別が付きません。
- `measure_stack` / `measure_entity` は 0.0 を 11 個・10 個返す。体積 0、
  面積 0、重心 (0,0,0)。**空ではなく誤答**です。
- `export_*` は**ファイルを1バイトも書かずに**成功を返す。

動作の名前を挙げて断るようにしました。空の応答を組み立てるためだけにあった
関数5本は消してあります。残しておくと「いつでも空を返せる」という誘惑に
なります。

#### (4) 実装しないと決めたこと — 解析曲面の STEP 出力

書き出しはいまも B-spline です。円柱を読んでも、書き戻すと
`B_SPLINE_SURFACE` になります。受け側は「これは円柱だ」という情報を失います
（フィーチャー認識や加工にはそこが効きます）。

**やらないと決めました。** 理由は3つです。

1. **認識の危うさ。** 面が持っているのは NURBS だけなので、軸と半径を当て
   直すことになります。5章に、まさにこれで踏んだ記録があります——「最大半径に
   16点以上乗っていれば円柱」という判定で、**円錐が円柱として通りました**。
   全標本を見れば防げますが、防げたことを確かめる手段がまた要ります。
2. **p-curve と噛み合わない。** (1) で 2D 境界を書くようにしました。曲面を
   解析曲面に替えると、p-curve は**その解析曲面の媒介変数**（角度と高さ）で
   書き直さなければなりません。書き直しに射影が要り、射影の精度がまた
   持ち込まれます。
3. **いまの経路が緑である。** 書き出した STEP は OpenCASCADE が代表25形状
   すべてを valid closed solid として読み、書き戻しは解析解と 1e-11 で
   一致します。得るものは意味情報で、失いうるものは正しさです。

**やるなら**、認識ではなく**作るときに覚える**ほうが安全です。`Face` に
`pcurves` と同じ形で `analytic: Option<..>` を持たせ、プリミティブの
ビルダーが埋め、ブーリアンや分割は**分からなくなったら捨てる**（捨てるのは
常に安全で、B-spline に落ちるだけ）。p-curve をその媒介変数で出し直す段が
いちばん重く、そこを飛ばすなら「解析曲面の面には p-curve を付けない」と
決める必要があります（混在は STEP として合法です）。

---
### 4-42. まだ測っていなかった経路 — 読んだ立体をブーリアンに渡す（2026/08/23）

3-0 に「対応ケースを増やすより、まだ投げていない形を投げるほうが期待値が
高い」と書いてあります。それに従って、既存のプローブが**通っていない経路**を
探しました。3本は測ってあって、1本だけ空でした。

| 経路 | 測っていたもの |
| :--- | :--- |
| 自作の立体 → ブーリアン | `robustness_probe`（25配置）、`closure_probe` |
| 自作の立体 → STEP 往復 | `closure_probe`、`verify_showcase` |
| 他カーネルの立体 → 書き戻し | `foreign_reexport`、`verify_reexport` |
| **他カーネルの立体 → ブーリアン** | **無し** |

実務では最後の行が最初に来ます。客先のファイルを開いて、穴をあける。
`foreign_boolean_probe` を書きました。

#### 測り方 — 解析解を使わない

切った後の体積の閉じた式は、切り方ごとに立てることになります。式を立てると、
**式のほうを間違えたときに気づけません**（4-33 で実際にやりました）。

形に依らず成り立つ恒等式で見ます。任意の A, B について:

```text
V(A - B) + V(A ∩ B) = V(A)
V(A ∪ B) = V(A) + V(B) - V(A ∩ B)
```

A が何であっても、B をどこに置いても成り立ちます。3つの演算が**揃って**
間違わない限り破れます。A の体積の真値は要りません。

#### 出てきたもの (1) — 600秒たっても1行も返らない

「戻ってこない」は、このリポジトリの基準ではパニックより悪い区分です。
止まっていたのは**トーラスのテッセレーション**で、4分割でも返りませんでした
（分割数に依らないので、遅いのではなく詰まっている）。

段を1つずつ吐かせて降りると、境界の点列が **16384 点**でした。継ぎ目の稜
4本が、それぞれ上限の 4096 まで刻まれています。

原因は `deflection_target` です。立体の寸法を**稜の端点だけ**から測って
いました。他カーネルが書く全周のトーラスは1面・4稜で、**4本とも同じ1点で
始まって同じ点で終わります**。頂点の境界箱は1点に潰れ、範囲が 0 になり、
たわみ目標が下限 1e-9 に落ち、どの稜も上限まで刻まれ、16384 点の多角形を
渡された earcut が返ってこない。

#### そこで「正しく」測ったら、別のものが壊れました

稜の途中も見て寸法を測るようにしました。トーラスは 0.01 秒で通ります。
テストが1件落ちました——螺旋ばねのメッシュ体積が 8分割で 8.2% 不足
（従来は 5% 未満）。`helix_volume_probe` で見ると、症状はもっと悪いものでした。

| 分割 | 頂点で測る（従来） | 稜の上まで測る |
| --: | --: | --: |
| 16 | 3.98e-4 | 1.19e-3 |
| 32 | 9.95e-5 | 2.16e-2 |
| 64 | 2.49e-5 | **5.85e-1** |
| 128 | 6.22e-6 | **-4.86e0** |
| 256 | 1.55e-6 | 3.87e-1 |

**収束しなくなります。** 目標が緩むとパッチの境界が巻きをまたいで切り取られ、
体積が 209 になったり 2959 になったりします。

頂点の箱は、曲がった形では実際より小さく出ます（ばねは両端が同じ角度に来る
ので、頂点だけ見ると幅 2R が消える）。その**控えめさが効いていました**。
「正しい寸法」ではなく「安全側の寸法」が要る場所でした。

なので測り方は戻し、**潰れだけを見張る**ようにしました。頂点の箱が稜の箱の
1e-3 未満なら稜の箱を使います。ばねは 0.42 倍なので触りません。トーラスは
ちょうど 0 なので切り替わります。

#### 出てきたもの (2) — 側面がメッシュから丸ごと消える

全周の円錐と半球（`cone_full` / `sphere_capped`）は、**底面しかメッシュに
なっていませんでした**。三角形20枚、z 方向の広がり 0。体積は別経路（面ごとの
積分）で積むので正しく、**見えていませんでした**。

uv の境界ループの符号付き面積を出すと、ちょうど **0** でした。母線2本が
どちらも u=0 に落ちています。周期曲面では継ぎ目の稜の uv は2通りあり
（u=0 と u=1 が同じ3D点）、投影はどちらを返しても「曲面に乗っている」ので
正しく見えます。両方が同じ側に落ちると、ループは行って戻るだけになります。

`settle_seam_parameters` は稜1本の中で寄せ先を決めるので、**稜が丸ごと
継ぎ目に乗っている**ときは寄せ先がありません。寄せ先はループが決めるべき
ものでした。継ぎ目に乗った区間の端は、隣の区間の端と一致していなければ
なりません。`settle_seam_segments` を足しました。

ここでも1度間違えました。「各制御点が min か max のどちらかにある」を
曖昧の条件にしたところ、**円錐の底円まで曖昧**になりました。底円の p-curve は
u が 1 から 0 へ動く1次の直線で、制御点は2点ともちょうど端にあります。
基準にすべき区間が無くなり、半球だけ直って円錐は直りませんでした。正しい
条件は「**全部が同じ側にある**」です。

#### 直したあとの実測

10検体すべてが正しい境界箱でメッシュになります。

| 検体 | 前 | 後 |
| :--- | :--- | :--- |
| torus | **返らない** | 0.01s / 1365 三角形 |
| cone_full | 20 三角形・高さ 0 | 5624 三角形・高さ 20 |
| sphere_capped | 20 三角形・高さ 0 | 3680 三角形・高さ 10 |

ブーリアンの側にも効きました。`cylinder_nurbs` に穴をあける演算は、
**体積が変わらないまま「成功」を返していました**（12566.370614 のまま）。
内外判定がメッシュを使うためで、側面の無いメッシュでは削れません。いまは
名前を挙げて断ります。**誤答が拒否に変わったので、これは改善です。**

`foreign_boolean_probe` の成績: **ok 4 / 拒否 70 / WRONG 0 / PANIC 0**、
恒等式の残差は最悪 5.21e-13。読んだ立体に対する厳密ブーリアンの守備範囲は
まだ狭く、**断り方は正しい**という状態です。

---
### 4-43. 読んだ立体を切れるようにした — 形ではなく持ち方が違っていた（2026/08/23）

4-42 で `foreign_boolean_probe` が **ok 4 / 拒否 70** でした。断り方は正しくても、
通る配置が少なすぎます。なぜ断るのかを切り分けました。

#### 対照を置く

`foreign_boolean_why` は、**同じ形をビルダーでも作って並べます**。読んだほうだけ
落ちるなら、原因は形ではありません。

| | 面 | 内訳 | 稜の使用 |
| :--- | --: | :--- | --: |
| 読んだ円柱 | 3 | 0 平面 / 3 NURBS | 6 |
| ビルダーの円柱 | 6 | 2 平面 / 4 NURBS | 24 |

同じ半径10・高さ40の円柱です。ファイルは**平面を B-spline で**、**側面の
全周を1枚で**持っています。パイプラインはその持ち方では縫えず、稜の使用が
9本合わず、キャップが1枚も作られませんでした。

#### 外した仮説を2つ、先に書きます

**「全周1枚を刻めば通る」——通りませんでした。** `Regularizer` に掛けても、
実測で**割った面 0・残した 1**。何も起きません。

**「平面として持ち直せば通る」——これも単独では通りませんでした。** 面の
種類は 2平面+1NURBS になりますが、キャップは 0 枚のままです。

通ったのは**両方を、この順で**やったときだけです。順序に理由があります。
キャップは「p-curve を失うと積分が変わる面」として守られており、守られた面が
使っている辺は刻めません（片側だけ刻むと辺の対が壊れる）。上下の円がそれです。
平面として持ち直すと p-curve は射影で厳密になり、守りが外れ、円が刻めるように
なって、初めて側面が割れます。

#### その手前に、もう1つ欠陥がありました

上の順序で整えても、まだ通りませんでした。整えた側面3枚が、**切断面との
交差候補に1つも挙がりません**（ビルダーの側面2枚は挙がります）。

`face_boundary_bbox` が、面の境界を**12点の標本**で測っていました。

> **標本から作った箱は、上界ではありません。**

曲がった境界は標本の間で外へ膨らみます。有理曲線はパラメータが角度に比例
しないので、「端と真ん中を取れば足りる」も成り立ちません。実測: 半径10の
円柱を120度ずつに割った断片で、境界の本当の y の最大は 10、標本の箱は
**9.993**。平面 x=-0.2 との交線は y=9.998 で、**箱の外**と判定され、面の組ごと
消えていました。**組が消えることは「交わらない」と区別が付きません。**

制御点で測るようにしました。B-spline 曲線は制御点の凸包に含まれる（重みが
正なら有理でも同じ）ので、制御点の箱は必ず曲線を含みます。**絞り込みの箱は
大きすぎる方向に外れるべきで、小さすぎる方向に外れてはいけません。**

#### 平面の認識は、当てるのではなく決まります

5章に「最大半径に16点以上乗っていれば円柱」で**円錐が円柱として通った**記録が
あります。4-41 で解析曲面の書き出しを見送った理由もそれでした。

ここは違います。B-spline 曲面は制御点の凸包に含まれるので、**制御点が1つの
平面に乗っていれば、曲面はその平面から出られません**。標本を見て判断しては
いません。重みが正でない制御点があれば凸包の性質が使えないので、そのときは
何もしません。

向きは曲面自身の法線から取ります。適当に張って一度裏返し、立体そのものが
無効になりました。

#### 入口に入れました

`Regularizer::hold_like_our_own` を厳密ブーリアンの入口で通します。
**自前の立体では実測で何も起きません**（キャップは既に平面、全周は既に
刻まれている。`regularize_probe` の native cylinder は 6面のまま 0枚分割）。
だから自分で作った形の答えは動きません。

| | 前 | 後 |
| :--- | ---: | ---: |
| `foreign_boolean_probe` ok | 4 | **15** |
| 拒否 | 70 | 37 |
| WRONG | 0 | 0 |
| PANIC | 0 | 0 |
| 恒等式の残差（最悪） | 5.21e-13 | 8.38e-10 |

残差は3桁上がりますが、判定線 1e-6 に対してまだ3桁下です。上がるのは、
整えた立体のほうが面数が多く、求積が面ごとに積み上がるためです。

#### まだ通らないもの

37 件は断ったままです。球と全周トーラスは、整えても交差の段で落ちます
（`nurbsxnurbs` の組が支えられていない配置が残る）。次に手を付けるなら
そこですが、**断り方は正しい**状態です。

---
### 4-44. インチで書かれた STEP を、16387 倍小さく読んでいました（2026/08/23）

検体10本はすべてミリで書かれていました。**単位を1行も読んでいなくても、
10本とも通ります。** 何を測っていないかは、測っている物からは見えません。

#### 書き手は増やせなくても、書き方は増やせる

他カーネル（有償 CAD）のファイルは手元にありません。ただし FreeCAD は
**同じ形を違う書き方で**出せます。書き手の多様性は得られませんが、
表現の多様性は得られます——単位、スキーマ、B-spline 化の有無、
1ファイル中の立体数。実務で効くのはむしろそちらです。

FreeCAD の headless 書き出しは**単位の設定を無視します**（実測: `Unit` を
インチにしても `SI_UNIT(.MILLI.,.METRE.)` のまま、座標も変わらず）。
そこで `tools/make_unit_step.py` は別の作り方をします。

1. 形を 1/25.4 に縮めて書き出す。数値はインチの値、宣言はミリのまま。
2. **長さの単位の実体だけ**を `CONVERSION_BASED_UNIT('INCH', 25.4mm)` に
   差し替える。触るのは1箇所なので、書き換えで形は壊れません。
3. 出来たファイルを **OpenCASCADE に読ませ**、元の寸法に戻ることを確かめる。

3 が要ります。自分で作ったファイルを自分の読み手に食わせても、ファイルが
正しいのか読み手が正しいのか分かりません。**OCC が 24000 mm³ と答えたなら、
そのファイルはインチのファイルです。**

#### 実測

| 検体 | 単位 | 前 | 後 | 解析解 |
| :--- | :--- | ---: | ---: | ---: |
| 箱 20x30x40 | インチ | **1.464570** | 24000.000000 | 24000 |
| 箱 20x30x40 | センチ | **24.000000** | 24000.000000 | 24000 |
| 円柱 r10 h40 | インチ | — | 12566.370615 | 12566.370614 |
| 穴あき箱 | インチ | — | 12321.902755 | 12321.902755 |

前の列は**ちょうど単位の3乗ぶん**（25.4³ = 16387、10³ = 1000）外れています。
偶然の誤差ではなく、単位を読んでいないことの証拠です。

**返ってくるのは、閉じていて多様体で形も正しい立体でした。** 大きさだけが
違う。閉性の検査も、面の検査も、恒等式（`V(A-B)+V(A∩B)=V(A)`）も、全部
通ります。**形を見る検査では、原理的に捕まえられません。**

#### 直し方で1つ間違えました

単位の実体は `GLOBAL_UNIT_ASSIGNED_CONTEXT((#166,#167,#168))` が指しています。
それを `"GLOBAL_UNIT_ASSIGNED_CONTEXT("` で探したところ、**当たりませんでした**。
STEP は 80 桁で折り返され、その折り返しがちょうどトークンと括弧の間に来ます。

```text
#165 = ( GEOMETRIC_REPRESENTATION_CONTEXT(3)
GLOBAL_UNCERTAINTY_ASSIGNED_CONTEXT((#169)) GLOBAL_UNIT_ASSIGNED_CONTEXT
((#166,#167,#168)) REPRESENTATION_CONTEXT('Context #1', ... ) );
```

行は空白で連結されるので `..._CONTEXT ((#166,...` となり、括弧付きの検索が
外れます。**単位の文脈が無いファイルに見え、係数 1 のまま素通りしました。**
トークンと括弧の間の空白を許し、括弧の対応を数えて中身を取る
`token_args` に置き換えてあります。

#### 平らな面だけでは足りません

座標は `get_point` の1箇所を通りますが、**半径は別の場所でスカラとして
読まれます**（円・楕円・円柱・円錐・球・トーラス、それに `VECTOR` の大きさ）。
座標だけ掛けて半径を掛け忘れると、**箱は通って円柱だけが壊れます**。
だから検体に曲面を持つ形と内側のループを持つ形を入れてあります。

#### 読めないときは断ります

単位の文脈があるのに長さの単位が読めないファイルは、**エラーを返します**。
そこで既定のミリに落とすと、読めなかったファイルが 16387 倍小さい立体として
通ります。文脈そのものが無いファイルだけ、ミリとして読みます。

---
### 4-45. 残りの表現差を測った — 欠陥は無く、外れていたのは物差しのほうでした（2026/08/23）

4-44 で単位の欠陥が出たので、同じ考えで残りの軸も変えました。

#### 測れなかった軸を先に書きます

**スキーマ（AP203 / AP214 / AP242）は測れていません。** FreeCAD の headless
書き出しは設定を無視し、どれを指定しても `AUTOMOTIVE_DESIGN`（AP214）で
出ます（実測: 4本とも `FILE_SCHEMA` が同一）。単位も同じで無視されたので、
そちらは `tools/make_unit_step.py` が別の作り方で用意しています。

**書き手は増えていません。** FreeCAD は OpenCASCADE そのものです。増えたのは
表現の多様性だけで、書き手ごとの癖は依然として未測定です。

#### 測れた軸

| 検体 | 何が違うか | 結果 |
| :--- | :--- | :--- |
| `two_solids` | 1ファイルに2立体 | ok（2立体、1500.000000、相対 1.5e-16） |
| `drilled_analytic` | 円柱＋平面 | ok（相対 5.1e-13） |
| `drilled_bspline` | 同じ形を全部 B-spline | ok（相対 9.5e-14） |

**個数も見ています。** 2立体のファイルを1立体として読んでも、片方だけ読んでも、
返るのは「もっともらしい数」です。合計だけでは気づけません。

#### 落ちたのは、こちらの期待値でした

`drilled_bspline` は最初 **WRONG** と出ました。期待値に、OpenCASCADE が
そのファイルを読んで測った **12312.350278** を置いたからです。こちらは
12321.902755——解析解ちょうどを返していました。

ファイルの中身を見ると、穴は degree 2・重み **(1, 0.5, 1, 0.5, ...)** の
**厳密な有理円柱**でした（120度ごとの円弧の標準形）。B-spline 化しても形は
近似されていません。つまり真値は解析解のままで、**外れているのは OCC の
求積のほう**です（解析解に対して 0.078% 低い）。

> **外の物差しは万能ではありません。** このリポジトリは OpenCASCADE を
> ゲートに使っていますが、それが緩む領域があります。有理 B-spline 上の
> 求積がそれです。既存のゲート（`verify_showcase` / `verify_reexport`）は
> **形が読めるか**と**解析解に乗るか**を見ているので影響を受けませんが、
> 「OCC がこう言った」を正解として使うときは、その領域かどうかを確かめて
> ください。

途中で1つ、確認の仕方も間違えました。FreeCAD の Python から
`surface.isURational()` を呼んで「7面とも非有理」と読み、**非有理 B-spline は
円を表せないのだから近似のはずだ**と考えました。ファイルのテキストには
`RATIONAL_B_SPLINE_SURFACE` と重みが並んでいます。**ファイルを見れば済む
ことを、API 経由で確かめようとして間違えました。**

#### この軸に欠陥はありませんでした

3軸のうち2軸は健全です。**測って何も出なかったことも、測った記録として
残します**——次の人が同じ軸をもう一度掘らないために。

---
### 4-46. 裏向きの面を割ると、巻き方が逆になっていた（2026/08/23）

「次は NURBS×NURBS 交差」と書いていましたが、**実測で見立てが外れました**。
残っていた 37 件を検体ごとに数えると、主役は曲面同士の交差ではありません。

| 検体 | 拒否 |
| :--- | ---: |
| `revolved_ring` | **9 / 9** |
| `elliptic_prism` | **8 / 9** |
| `cone_full` 半スラブ | 3 / 3 |

平面の切り手でも落ちているので、原因は交差の種類ではなく**その立体自身**です。

#### 正規化が、閉じたまま無効な立体を作っていた

`revolved_ring` を正規化すると、シェルは閉じたまま **位相的に無効**になります。

```text
Face 6 planar p-curve loop is inconsistent with face orientation;
       oriented area -2.500000e-1
```

`-0.25` は元の媒介変数領域のちょうど 1/4 で、符号が逆です。割った断片の
トリムループが、**親の面の向きを見ずに巻かれて**いました。パッチの縁を
決まった順で辿るので、`Reversed` の面を割ると断片は親と逆に巻きます。

形も体積も合ったままなので（体積の移動は 2.51e-10）、**見た目では分かりません**。

組んだあとに媒介変数空間の符号付き面積を測り、向きと合わなければ巻き直す
ようにしました。どちらの巻き方でも合わないなら、その面は割りません
（**間違った向きの断片を返すより、割らないほうがましです**）。

#### なぜ今まで出なかったか

`regularize_probe` の検体一覧に **`revolved_ring` と `elliptic_prism` が
入っていませんでした**。検体には足したのに、プローブには足していない。
正規化はこの2本で一度も測られていませんでした。

さらに、プローブが見ていたのは**シェルが閉じているか**だけでした。ブーリアンの
入口が見るのは `is_topologically_valid` で、こちらのほうが厳しい。閉じたまま
無効になるこの欠陥は、閉性の列では「valid」と出ます。**列を1つ足すまで、
測っていたのに見えていませんでした。**

いまは11検体すべてを見て、`usable` の列を別に出します。

#### 絞り込みの箱を、保証つきのまま「きつく」した

4-43 で面の境界箱を制御多角形（＝上界）にしました。上界としては正しいものの、
この箱は**2つの仕事**をしています。

1. 面の組を候補に入れるかの絞り込み — **外してはいけない**
2. 求めた交線の切り詰め — **緩めてはいけない**

生の制御多角形は 1 に足りて 2 に緩すぎました。曲線を3回細分してから
制御多角形を取るようにすると、凸包の性質は保たれたまま箱が締まります
（円弧のはみ出しはおよそ 1/64）。実測で **ok 15 → 16**。

#### 私が「回帰」と読み違えたこと

途中で、`revolved_ring` を角の箱で削る配置が「以前は 1583.362697 を返して
いたのに拒否になった」と判断し、**それをコードのコメントに書きました**。

OpenCASCADE に同じ配置を計算させると、**30.109547 mm³ 削れます**。つまり
以前の「1583.362697（＝元のまま）」は誤答で、いまの拒否が正しい挙動です。
4-43 の境界箱の修正が、**誤答を拒否に変えていた**ということでした。

回帰だと思ったら、まず**その「通っていた答え」が正しかったのかを外の物差しで
確かめてください**。`tools/occ_cut_reference.py` はそのために置いてあります。

#### 実測

| | 前 | 後 |
| :--- | ---: | ---: |
| `foreign_boolean_probe` ok | 15 | **18** |
| 拒否 | 37 | 28 |
| WRONG / PANIC | 0 / 0 | 0 / 0 |
| 恒等式の残差 | 8.38e-10 | 8.38e-10 |
| 正規化で壊れる検体 | 2 | **0** |

---
### 4-47. 穴の中を、面の一部として数えていた（2026/08/23）

4-46 の最後に「次もまず検体ごとに数える」と書いたので、そうしました。残り 28 件の
最大の塊は `revolved_ring` の8件。そのうち **`centre drill` は差分だけ通り、積と和が
拒否**という妙な並びでした。

#### まず「本当に交わっているのか」を訊いた

丸棒は輪の穴（内半径 4）より細い半径 3.6 で、**穴を素通りします**。
OpenCASCADE に同じ配置を計算させると:

```text
V(A)      1583.362697      V(A - B)  1583.362697
V(B)       732.870734      V(A ^ B)     0.000000
                           V(A u B)  2316.233432
```

**触れていません。** 3つとも切る仕事の要らない自明な答えです。それでも
カーネルは**交線を8本**見つけていました。

#### 面のトリムが、外周ループしか見ていなかった

輪の平らなキャップは**穴あきの円環**です。丸棒の側面がキャップの平面と作る
円（半径 3.6）は、外周（半径 10）の内側にあります。トリムの判定は外周
ループだけを見ていたので、**穴の中も面の一部**でした。

> **穴の中は面の外です。**

平面のトリムを見る口は2つあり（線分用と曲線用）、**どちらも外周だけ**でした。
両方に内側ループを入れました。線分側は「外周の内側」から「内側ループの内側」を
区間として引き、曲線側は内外判定に穴を足しています。

穴の縁に乗った点は面の上として残します（そこは本当に面の一部です）。

#### 交線が無いなら、答えは包含関係だけで決まる

トリムを直すと交線は 0 本になりましたが、積はまだ拒否でした。入口の
`has_face_pair_candidates` は**境界箱と面の種類しか見ない**ので「かもしれない」で
通します。境界箱が重なっていても実際には触れていない配置は、そこを抜けて
きます。

交線が1本も出なかった時点で、答えは包含関係だけで決まります。その経路
（`boolean_solids_exact_without_intersections`）は既にあったので、そこへ
落とすようにしました。

#### 実測

| | 前 | 後 |
| :--- | ---: | ---: |
| `foreign_boolean_probe` ok | 18 | **22** |
| 拒否 | 28 | 23 |
| WRONG / PANIC | 0 / 0 | 0 / 0 |
| 恒等式の残差 | 8.38e-10 | 8.38e-10 |

`revolved_ring` と丸棒は3演算とも OCC と一致します（1583.362697 / 0 /
2316.233432）。

`boolean_through_hole_test` が押さえます。**和が2立体であること**も見ています
——合計だけ見ていると、離れた2つが1つに融合しても気づけません。「穴を外した
ことで穴のそばが全部無視になっていないか」も、壁に本当に届く太い棒で確かめて
います。

#### 残り 23 件

`revolved_ring` 6、`torus_segment` 3、`sphere_capped` 3、`sphere` 3、
`extruded_spline` 3、`cone_full` 3、`cone` 2。次もまず検体ごとに数えてください。

---
### 4-48. 残り23件は、散らばった失敗ではなく1つの未実装だった（2026/08/23）

1件ずつ追う前に、**どの段で足りなくなったか**で並べました
（`foreign_boolean_stage_probe`）。

```text
subject           cutter  op            pairs edges  caps pieces unmat nonmf samedir
revolved_ring     corner  difference        7     5     0     7    11     0     0
sphere            corner  difference        6     3     0     4     3     0     0
cone_full         slab    difference        2     1     0     2     5     0     0
torus_segment     drill   difference        4     8     0     7     8     0     0
...
23 refused
```

**23件すべてで `caps 0`。** 非多様体の稜も、同方向の稜も、1本もありません。
症状は「縫えない稜が残る」だけです。

> **切り口の蓋が作れていない。それだけです。**

#### なぜ作れないか

`build_planar_caps_from_intersection_edges` が呼ぶのは
`collect_closed_intersection_edge_loops` で、名前のとおり **交線だけで閉じた
ループ**しか拾いません。

- 角柱を丸棒が貫くと、切り口の境界は**閉じた円**です。交線だけで閉じるので
  蓋が作れます。これが通っている 22 件の形です。
- 球の角を箱で削ると、平面と球の交わりは円ですが、**箱の面で切り取られて
  弧**になります。弧は交線だけでは閉じません。閉じるには**切り手の面の境界**を
  使う必要があります。

輪をスラブで切る、円錐をスラブで切る、トーラス片を丸棒で抜く——落ちている
23件はすべてこの形です。

#### これは1つの機能です

「弧の両端を、その面の境界を辿って結ぶ」——どちら側を辿るかは、残す領域が
決めます。実装は小さくありませんが、**23件すべてに効く1つの穴**であって、
23の別々の欠陥ではありません。

#### 読み込みは無関係でした

同じ輪をビルダーで組んで同じ箱で削っても、**同じように断ります**
（`ring_corner_probe`）。他カーネルのファイルの持ち方の話ではないので、
読み込みを挟まない再現で追えます。

| | 差 | 積 | 和 |
| :--- | :--- | :--- | :--- |
| ビルダーの輪（10面） | 拒否 | 拒否 | 拒否 |
| 読んだ輪（4面） | 拒否 | 拒否 | 拒否 |
| OpenCASCADE | 1553.253150 | 30.109547 | 1771.953150 |

#### 1件ずつ追う前に、段で分類してください

これを先にやらなかったら、`revolved_ring` の6件を個別に追って、同じ結論に
6回たどり着いていたはずです。**「何件あるか」ではなく「何種類あるか」を先に
測ってください。**

**ただし、ここに書いた分類は誤りでした。** 数えていた診断が、実際の走行と
違う入力で走っていたためです。訂正と正しい分類は 4-49 にあります。

---
### 4-49. 診断が、実際の走行と別のものを報告していた（2026/08/23）

4-48 で「残り23件は全部 `caps 0` の1つの機構」と書きました。**その数字は、
実際に走っているものの数字ではありませんでした。**

`prepare_exact_boolean`（診断の口）は入力を整えずに数えていました。演算の
ほうは 4-43 から入口で整えます（`Regularizer::hold_like_our_own`）。同じ
配置で、両者はこう食い違っていました。

| | 面の組 | 交線 | 蓋 |
| :--- | ---: | ---: | ---: |
| 実際の演算 | 6 | 8 | 2 |
| 診断の報告 | 4 | 6 | 0 |

**そして私は、後者を見て23件を分類していました。**

診断も同じ入口を通すようにしました。数字が実物と一致します。

#### 揃えたら、分類が変わった

| 縫えない稜 | 件数 |
| ---: | ---: |
| 3 | **11** |
| 8 | 1 |
| 9 | 3 |
| 10 | 2 |
| 20 | 6 |

最大の塊は「角の箱」11件で、どれも**縫えない稜3本・蓋0枚**です。以前見えて
いた分布（3→8、5→3、8→6…）とは別物でした。

#### 直したもの — 1本の直線が、面の中で2本に分かれる

平面どうしの交線をトリムする口が、**区間を1つしか返していません**でした。
穴あきの面を横切る直線は2区間に分かれます。輪をスラブで切ると、切り口の
断面は穴の両側の**2つの矩形**で、必要な稜は8本です。1区間しか返さないので
6本しか出ず、蓋のループがどちらも閉じませんでした。

```text
前: 6 稜 -> 0 ループ、6 あぶれ、蓋 0 枚
後: 8 稜 -> 2 ループ、0 あぶれ、蓋 2 枚
```

`FaceIntersectionKind::Line` は区間を1つしか持てないので、2つ以上になったら
`Curves` で返します。

**件数は動いていません**（ok 22 / 拒否 23 のまま）。輪とスラブの失敗は次の段
——非多様体の稜——に移りました。機構としては一段進んで、次の壁が見えた状態です。

#### 出力を途中で切って読んだ

この作業の途中でもう1つ間違えました。断り文を 130 文字で切って表示し、
そこに映っていなかった「蓋 2 枚」を「蓋 0 枚のまま」と読みました。
**切った出力を、そのままデータとして読まないでください。**

---
### 4-50. 面の内側で閉じたループでは、割れなかった（2026/08/23）

4-49 で直した診断で分類し直すと、最大の塊は「角の箱」11件（縫えない稜3本・
蓋0枚）でした。そこから入りました。

#### 縫えなかった稜を名指しする

断り文が言うのは本数だけです。「3本あぶれた」では、足りないのが面なのか、
向きなのか、別の場所を指しているのかが分かりません。組み立てに使われた面片を
自分で走査して、**1回しか使われていない稜**を位置と持ち主つきで出しました
（`unmatched_edge_probe`）。

```text
sphere / corner / Difference
  11 face piece(s), 0 cap face(s), stitch: 3 unmatched
  edges used exactly once:
    piece 10 (B, Inside, reversed) midpoint (4.0000 6.4807 6.4807)
    piece  9 (B, Inside, reversed) midpoint (6.4807 4.0000 6.4807)
    piece  8 (B, Inside, reversed) midpoint (6.4807 6.4807 4.0000)
  face pieces:
    0..7  A Outside  3 edge use(s)   <- 八分片のまま、1枚も割れていない
    8..10 B Inside reversed
```

中点 (4, 6.4807, 6.4807) のノルムは **10** — 球面上です。x=4 は箱の面。
あぶれているのは、箱の平面パッチが持つ**球面上の弧**でした。

そして A（球）は **8面とも `Outside` で無分割**です。弧に相手がいないのは、
**球の面が一度も割られていない**からでした。

#### なぜ割れないか

箱の角は3面で球を切ります。交線は3本の弧になり、**3本で球面上の閉じた
ループ**を作ります。どの弧も八分片の境界には着きません（端は箱の稜の上です）。

`FaceSplitter` が扱えるのは**境界から境界へ**届く切り込みだけです。閉じた
ループは「境界に届かない」と断られ、結果として球の面は無傷のまま残ります。

#### 割り方は素直だった

曲面はそのままで、境界だけを付け替えます。

- 内側の片: ループを**外周**にした面
- 外側の片: 元の外周のまま、ループを**穴**として足した面（巻きは逆）

境界を辿り直す必要はありません。`FaceSplitter::split_by_interior_loop` として
足しました。検査は**面積の和**です。20×20 の面に 6×6 のループを入れると
36 と 364 に割れ、和は元に戻ります（残差 1e-9 以下）。

**どちらがどちらかも見ています。** 合計だけでは、内側と外側を取り違えても
通ってしまいます。

#### 繋いだ結果

ブーリアンの分割段の**最後の受け皿**に置きました。境界から境界へ届く
切り込みがあるならそちらが先に通るので、ここへは来ません。

| | 前 | 後 |
| :--- | ---: | ---: |
| `foreign_boolean_probe` ok | 22 | **24** |
| 拒否 | 23 | 17 |
| WRONG / PANIC | 0 / 0 | 0 / 0 |
| 球の角（縫えない稜） | 3 | **0** |

**この行は不正確でした。** 縫えるようになったのは確かですが、検証つきの口では
球の角の2件がまだ拒否されます。経緯は 4-51 に。残る 17 件は
`revolved_ring` 6、`torus_segment` 3、`extruded_spline` 3、`cone_full` 3、
`cone` 2 です。

### 4-51. 正しい答えがゲートに拒否される — 直そうとして2回とも緩めた（2026/08/23）

**解消しました（4-68）。** 緩めるのではなく、食い違った標本を B-Rep の面へ
厳密に射影して訊き直す方向で片づきました。原因は判定ではなく、射影が面を
捨てていたことでした。

4-50 で「球と半球の角の箱は3演算とも通るようになった」と書きました。
**検証つきの口では通っていませんでした。**

`foreign_boolean_probe` は `boolean_solids_exact_result_unverified` を使います。
恒等式は「返ってきた答えが互いに整合するか」を見るので、ゲートに拒否された
結果でも測れるほうが都合がよいからです。**そのぶん、公開 API より甘い数字を
報告します。** 検証つきで通る数を別に数えるようにしました（いま 90演算中71）。

#### 拒否されているのは、正しい答えでした

```text
sphere corner difference
  our result 4168.055983
  OpenCASCADE 4168.055969      相対 3.4e-9
```

ゲートの言い分は「383点中10点が述語と食い違う（許容4）」。体積の範囲は
通っています。落ちているのは**標本点の内外判定**です。

食い違った点を見ると、球面のすぐ近くにいます（例: 球面から **0.0002**）。
検証は A・B・結果の**3つのメッシュに別々に**内外を訊きます。A と結果は同じ
球面を持っていても面の割れ方が違うので、三角形の張り方も違い、面のすぐ近くでは
答えが割れます。**形の誤りではなく、同じ面を別々に近似したことの差**です。

3本の光線の合議は、点が面の**上に乗っている**ときしか落としません。面の
**近くにいるだけ**の点は落とせません。

#### 直そうとして、2回とも緩めた

**1回目**: 帯を「三角形の辺の長さの平均 × 1.5」で取りました。
`boolean_scale_and_empty_test` が2件落ちました。一辺 0.2 の箱では帯が 0.15 に
なり、標本が1つも残らず、**検査が何も見なくなります**。平面はどんなに粗く
割ってもずれ 0 なのに、大きさで測ったのが誤りでした。

**2回目**: 曲がりで取り直しました（頂点の法線と三角形の法線のなす角）。
平面なら角 0 で帯も 0 のはずでした。**同じ2件が落ちました。** メッシュは
最後に頂点を溶接するので、直方体の角では3面の法線が平均され、**平面なのに
「曲がっている」**と出ます。

#### 取り下げました

ゲートは、誤答と正しい答えの間に立っている唯一のものです。**正しい答えが
たまに拒否されるほうが、誤答が通るより安全です。** 2度とも番人（この2件の
テスト）が捕まえたので、番人のほうが正しい。

残したのは測定です。`membership_mismatch_probe` は、食い違った標本点を
名指しし、球や箱の面からどれだけ離れているかを出します。次に直す人は、
**帯の出し方を測ってから**入ってください。溶接後の法線は使えません。

やるなら、面までの距離を**B-Rep の面そのもの**で測るのが筋です（メッシュ
どうしの差ではなく、メッシュと真の面の差を見る）。点と曲面の距離は
`ExtremumEngine` が持っています。

---
### 4-52. 自分が前の段で入れたコードに、同じ欠陥を作った（2026/08/23）

4-46 で「裏向きの面を割ると断片の巻き方が逆になる」を直しました。4-50 で
**内側ループによる分割**を新しく足したとき、**同じ検査を入れませんでした**。

`cone corner` の積と和が、縫えない稜 0・非多様体 0 のまま**同方向の稜 6 本**で
落ちていました。共有する稜を両側が同じ向きに辿っています。閉じた多様体では
起こりえない状態です。

ループの並びは辿った順でしかなく、面がどちら向きかを見ていません。球では
偶然合っていて、円錐で合いませんでした。**「偶然合っていた」は、測るまで
「合っている」と見分けが付きません。**

`interior_loop_winds_with` を足して、媒介変数空間の符号付き面積で突き合わせ、
合わなければ巻き直します。同方向の稜は **6 → 0**。

| | 前 | 後 |
| :--- | ---: | ---: |
| `foreign_boolean_probe` ok | 24 | **25** |
| 拒否 | 17 | 15 |
| 検証つきの口 | 71/90 | **72/90** |

#### そして、恒等式が誤答を1つ掘り当てました

残差が **8.38e-10 → 9.91e-7** に3桁悪化しました。判定線 1e-6 のすぐ手前です。
新しく通った行を見ると `cone corner block` で、こうなっていました。

```text
V(A - B) = 3267.2564   (= V(A)、何も削っていない)
V(A ^ B) =    0.0032
V(A u B) = 3996.2531
```

積が 0.0032 あるのに、差が満杯のままです。OpenCASCADE に訊くと:

```text
V(A - B) = 3267.253121      V(A ^ B) = 0.003239      V(A u B) = 3996.253121
```

**積と和は合っていて、差だけが 0.003239 ぶん削り残しています。** 円錐の頂点
寄りに残る薄い切片で、絶対値は小さいものの**誤答**です。

**これは前からあった誤答です。** 差はもともと通っていて、積と和が拒否されて
いたので恒等式が組めず、**見えていませんでした**。積と和が通った瞬間に出ました。

> 恒等式は「3つ揃って間違わない限り破れる」ので機能します。**1つでも欠けると
> 何も言えません。** 拒否が減ると、残っていた誤答が見えるようになります。

差の側は構造としては正しく見えます（A の外側6枚＋B の内側3枚、縫合 0/0/0、
穴を持つ面も1枚）。それでも体積が満杯の円錐ちょうどなので、食い違いは
**縫合より後**にあります。次はそこです。

---
### 4-53. 標本で「重なっていない」ことを決めていた（2026/08/23）

4-52 で恒等式が指した誤答——円錐の角を箱で削ると 0.003239 mm³ 削り残す——を
追いました。段ごとに体積を出すと、場所は一発で決まりました。

```text
縫合前: 9面, 3267.253121   <- OpenCASCADE と一致（正しい）
縫合後: 6面, 3267.256360   <- 満杯の円錐
A     : 6面, 3267.256360   <- 結果は A そのもの
```

**汎用経路は正しい答えを作れていました。** そこへ届く前に打ち切られていた
だけです。

#### 打ち切っていたもの

差には近道がありました。

```rust
if matches!(op, Difference) && !Self::interiors_overlap(a, b) {
    return Ok(single(a.clone()));   // 中身が重なっていないなら差は A
}
```

`interiors_overlap` は共通の境界箱に **512点**を撒いて、両方の内側に入る点を
探すだけです。ここでの共通の箱は 216 mm³、重なりは 0.003239 mm³。当たる確率は
**1.5e-5**、512点の期待値は **0.008点**——まず当たりません。

> **標本は「重なっている」ことは示せますが、「重なっていない」ことは
> 示せません。**

近道自体は要ります（面で接しているだけの配置を汎用経路に流すと、同一平面が
重複して非多様体になる）。**先回りではなく最後の受け皿**に移しました。汎用
経路が閉じた多様体を作れたならそちらが返り、作れなかったときだけここに来ます。

#### 直したら、その先でもう1つ出た

差が正しくなると、同じ配置の**積が検証で拒否**されました。

```text
result volume 0.003238 is not positive
```

「正か」を見る閾値が `1e-3 × min(V(A), V(B))` = **0.729** で、真値 0.003239 の
**225倍**です。この判定は2度直されています（絶対値の床をやめ、結果の上限で
正規化するようにした）。それでも足りませんでした。

**訊きたいのは「この立体は何も囲んでいないのか」だけで、相手の大きさとは
関係ありません。** `tol.linear` の3乗より小さい体積が数値的なゼロです。
「思ったより小さい」を捕まえるのはこの判定の仕事ではありません（境界チェックと
内外一貫性が見ます）。

薄い重なりは実務にいくらでもあります——板と針、面取りのかかり、公差ぎりぎりの
嵌合。相対で切ると、**小さい答えほど弾かれます**。

`boolean_scale_and_empty_test` の9件は全部通ったままです。あの2件の番人
（空を弾く、小さいモデルで境界が効く）は内外一貫性が見ているので、ここを
緩めても働きます。

#### 実測

| | 前 | 後 |
| :--- | ---: | ---: |
| `foreign_boolean_probe` ok | 25 | 25 |
| 恒等式の残差 | **9.91e-7** | **8.38e-10** |
| 検証つきの口 | 72/90 | 72/90 |

**通る数は増えていません。答えが正しくなりました。** 恒等式の残差が3桁戻った
ことが、この作業の成果です。

`boolean_thin_overlap_test` が押さえます。差・積・恒等式の3方向に加えて、
**本当に触れていない相手では A がそのまま返ること**も見ています——近道を
後ろに下げたことで、その働きまで失っていないかの確認です。

---


### 4-54. 穴のある面を、まるごと断っていた（2026/08/23）

4-53 の後に残った15件を、また段で分類しました。いちばん失敗の小さいものが
`revolved_ring corner`——縫合の未整合辺が **3本**だけです。

`unmatched_edge_probe` に端点まで出させると、3本は平面 z=6 の上で
**閉じた三角形**を作っていました。

```text
piece 12 (B, Inside, reversed) from (4.0000 9.1652 6.0000) to (4.0000 4.0000 6.0000)
piece 11 (B, Inside, reversed) from (4.0000 4.0000 6.0000) to (9.1652 4.0000 6.0000)
piece  5 (A, Outside)          from (9.1652 4.0000 6.0000) to (4.0000 9.1652 6.0000)
```

切り口の三角形が3辺とも相手を見つけられない、つまり**その面が割れていない**。
一時的な `ZENITH_SPLIT_TRACE` が理由をそのまま吐きました。

```text
chain refused: Planar face splitting with inner wires is not implemented yet
```

輪の上面は**円環**です。内側のワイヤ（穴）を1つでも持つ平面の面を、分割器は
**一切**断っていました。穴あき板そのものなので、実務では珍しくもない形です。

#### 直したもの

`distribute_inner_wires(original, pieces, plane, tol)` を入れました。割った各片の
uv 多角形に対して、穴の代表点がどこに入るかを見て**穴を片方に配る**だけです。
`brep_intersection.rs` にあった3か所の拒否をこれに置き換えました
（"not implemented yet" は 0 件になりました）。

**入らなかったとき・2つ以上に入ったときは断ります。** 穴を切り込みが横切る
配置では、穴自体を割らなければ正しくならないためで、そこは黙って間違えるより
断るほうが良い状態です。

穴を「どちらにも入れない」「両方に入れる」と、**体積は合っても穴が消えます**。
`planar_split_with_hole_test` はそれを体積で見ています（穴が失われれば
π·100·6 = 1884.96 ぶん増える）。

#### 検体は、作った輪ではなく読んだ輪です

同じ形をビルダー（円柱からボアを引く）で組むと面が **10枚**に割れていて、
そちらはまだ切れません。直したのは読んだ形（**4枚**）の経路です。テストは
最初ビルダー製で書いてしまい、直したはずの経路を通っていませんでした。
**測ったものをそのまま検査する**ように検体へ差し替えてあります。
両者の差は `ring_corner_probe` が並べて出します。

#### 実測

| | 前 | 後 |
| :--- | ---: | ---: |
| `foreign_boolean_probe` ok | 25 | **26** |
| 拒否 | 15 | **12** |
| 検証つきの口 | 72/90 | **76/90** |
| 恒等式の残差 | 8.38e-10 | 8.38e-10 |
| WRONG / PANIC | 0 / 0 | 0 / 0 |

読んだ輪の角切りは OpenCASCADE と並べてこうです。

| | 差 | 積 | 和 |
| :--- | ---: | ---: | ---: |
| OpenCASCADE | 1553.253150 | 30.109547 | 1771.953150 |
| Zenith | 1553.253151 | 30.109546 | 1771.953151 |

残る12件は `cone_full half slab` 3、`extruded_spline half slab` 3、
`revolved_ring half slab` 3、`torus_segment centre drill` 3 です。**3つが
同じ配置（half slab）に固まっている**ので、次もそこから段で分類します。

---

### 4-55. 交線が出ないのは、辿れないからではなく**当てはめが届かない**からだった（2026/08/23）

残り12件のうち、いちばん小さいのは `cone_full half slab`——面の組 3 に対して
交線が **1本**。組は「切断面 × 側面の四半分2つ」＋「切断面 × 底面」で、
出ている1本は底面との直線です。**側面との交線が両方とも空**でした。

`cone_slab_probe` に段を出させると、辿るほうは合っていました。

```text
seed (0.4167 0.5833) -> step 4.0000: 15 pts, off 1.50e-11, fit 1.26e-1;
                        step 2.0000: 21 pts, off 1.80e-11, fit 8.38e-2;
                        step 1.0000: 33 pts, off 1.57e-11, fit 5.09e-2;
                        step 0.5000: 56 pts, off 3.01e-11, fit 6.53e-3;
                        step 0.2500: 98 pts, off 4.09e-11, fit 6.20e-4;
                        step 0.1250: 187 pts, off 4.04e-11, fit 3.53e-5;
```

`off` は辿った点が両方の曲面からどれだけ離れているか——**4e-11**。
`fit` はそこに通した3次曲線のずれで、要求は **1e-6**。
`fit_all_branches` は刻みを半分にするのを **6回**と決め打ちにしていて、
6回目の 3.53e-5 で打ち切り、**「交わらない」と報告していました**。

平面×円錐は双曲線です。3次補間でこの精度を出すには、あと2〜3回要ります。
**回数ではなく、届いたかどうかで止める**ようにしました（上限12回、
点の上限に当たったらそこで降りる）。

#### 「減りが鈍ったら諦める」は入れてはいけなかった

`fit_to_tolerance` にある見切り（0.5倍に届かなければ次の種へ）を真似て、
「0.9倍に届かなければ降りる」を入れました。**トーラス片をドリルで切る配置の
交線が 12本から 2本に減りました。** 上の表のとおり序盤の減りは 0.66、0.61 で、
3次補間の理屈（8分の1）よりずっと緩い。**序盤の鈍さは、その先で届かないことを
意味しません。** 取り下げてあります。費用は点の上限が抑えます。

#### 恒等式が見られない誤答が1件出てきた

`torus half slab` は、答えが**トーラスそのまま**（切り手を無視）でした。
そのとき V(A-B) = V(A)、V(A^B) = 0、V(AuB) = V(A)+V(B) となり、
**2つの恒等式はどちらも成り立ちます**。プローブは残差 2.44e-12 の
「ok」と報告していました。検証つきの口は断っていた（gate 0/3）ので、
公開 API から誤答は出ていません。

これを見えるようにするため、プローブは積が 0 の行を **NOCUT** として
分けて数えます。0 が正しい配置もあります（輪の穴を通るドリルなど）が、
**どちらかは恒等式には決められない**ので、判定せずに旗を立てて外の物差しへ
送ります。5件中4件は本当に 0、1件（`torus half slab`）が誤答でした。

#### 比べる箱が違っていた

その過程で、`torus corner block` を OpenCASCADE と並べると 67.16 対 52.16 で、
**誤答が1件増えたように見えました**。違っていたのは配置です。

| | 境界箱 |
| :--- | :--- |
| 真値（Zenith が使う） | (-16, -16, -4) - (16, 16, 4) |
| OCC の `BoundBox` | (-17.3183, -17.3183, -4) - (17.3183, 17.3183, 4) |

切り手は箱に対する比で置くので、箱が違えば**別の配置**になります。
`occ_cut_reference.py` に `--box` を足し、`cutter_placement_probe` が
Zenith の使う箱をそのまま貼れる形で出すようにしました。同じ箱で測り直すと
OCC も **3722.765672 / 67.162418 / 4469.261679**——Zenith と一致します。
**外れていたのは答えではなく物差しでした。**

#### 実測

| | 前 | 後 |
| :--- | ---: | ---: |
| `foreign_boolean_probe` ok | 26 | **23 ＋ NOCUT 4** |
| 拒否 | 12 | **9** |
| 検証つきの口 | 76/90 | **79/90** |
| 恒等式の残差 | 8.38e-10 | 8.38e-10 |
| WRONG / PANIC | 0 / 0 | 0 / 0 |

（ok が減っているのは NOCUT を分けたためで、通る数は 26 → 27 相当です。）

OpenCASCADE と並べるとこうです。いずれも同じ箱で測っています。

| 配置 | | 差 | 積 |
| :--- | :--- | ---: | ---: |
| `cone_full` half slab | OCC | 1087.168546 | 1007.226556 |
| | Zenith | 1087.1685 | 1007.2266 |
| `torus` half slab | OCC | 1927.137910 | 1862.789475 |
| | Zenith | 1927.1381 | 1862.7900 |

残る9件は `extruded_spline half slab` 3、`revolved_ring half slab` 3、
`torus_segment centre drill` 3。加えて `sphere corner` は答えが正しいのに
ゲートが2件断ります（4-51、未解決）。

---

### 4-56. 交線の輪が 2.3e-3 で閉じていなかった — 折れ線で曲線を当てていた（2026/08/23）

`extruded_spline half slab` は交線が **4本**出るのに蓋が 0 枚でした。本数は
足りています。`intersection_edge_probe` に端点まで出させると、理由はそこに
ありました。

```text
A0 x B5  ( 11.7600   2.7446  12.0000) -> ( 11.7600   2.7446   0.0000)   側面から
A4 x B5  ( 11.7600  20.0000   0.0000) -> ( 11.7600   2.7423   0.0000)   蓋から
```

**同じスプラインの上の同じ点が、2.3e-3 ずれていました。** 側面（押し出し面）
との交線は曲面をマーチングして得た正しい点、蓋との交線は平面をトリムで
切った点です。輪が閉じないので蓋は作れず、切り口の面が1枚も無いまま縫合に
入っていました。

#### 折れ線が入っていた場所

`segment_inside_pcurve_loop_intervals` は、p-curve と直線の交点を閉じた式で
解きます——ただし**1スパンの1次・2次だけ**です。それ以外は 32 点の折れ線に
落ちていました。押し出したスプラインの境界は多スパンの3次なので、そこを
通ります。**弦と曲線のずれが、そのまま交点のずれになります。**

直線までの符号付き距離 `f(t) = n·(C(t) - start)` は曲線に沿って滑らかなので、
符号の変わる区間を粗い標本で挟み、**曲線そのものの上で二分**するようにしました
（`pcurve_crossings_by_bisection`）。次数にもスパン数にも依りません。

直すと端点は両方とも 2.7446 になり、輪が閉じ、蓋が 1 枚できました。

#### そこで、蓋の採否がもう1つ間違っていた

蓋を足すかどうかは「縫合が改善するか」で決めていて、その比較が
`(未整合, 非多様体, 同方向)` の**辞書順**でした。未整合が先頭なので、
**非多様体を6本作ってでも未整合を2本減らす**選択が「改善」と判定されます。
実測でそれが起きました: (10, 0, 0) → (8, 6, 0) を採用していました。B 側の
切断面が既に選ばれているところへ、同じ場所の蓋を重ねた形です。

> 未整合は「まだ閉じていない」、非多様体は「壊れている」。
> 減らしてよいのは前者だけで、後者を増やす取引はありません。

「未整合が減り、かつ非多様体と同方向が増えない」ときだけ足すようにしました。

#### まだ通っていません

`extruded_spline half slab` は、蓋と非多様体は解消しましたが、**上下の蓋が
割れていません**。片は5稜持っていて、y=20 の稜は x=11.76 で分かれているのに、
スプラインの稜は端から端まで（(0,0,0)→(24,6,0)）抱えたままです。段の内訳も
「平面の分割候補 0」と言っています。次はそこです。

#### 実測

| | 前 | 後 |
| :--- | ---: | ---: |
| `foreign_boolean_probe` ok | 23 ＋ NOCUT 4 | 23 ＋ NOCUT 4 |
| 拒否 | 9 | 9 |
| 検証つきの口 | 79/90 | 79/90 |
| 非多様体の稜（全配置） | 6 | **0** |

**通る数は増えていません。** 増えたのは、交線の輪が閉じたことと、壊れた蓋を
足さなくなったことです。`extruded_spline` の段は 交線4・蓋0・非多様体0 から
交線4・**蓋1**・非多様体0 に進みました。

---

### 4-57. 境界のスプラインを、着地点で分けられなかった（2026/08/23）

4-56 で交線の輪は閉じ、蓋も1枚できたのに、`extruded_spline half slab` は
まだ断っていました。`unmatched_edge_probe --wires` で片の輪を丸ごと出すと、
上下の蓋が**割れていない**ことがはっきりします。

```text
  3 A Outside 5 edge use(s)
    (  0.0000  20.0000   0.0000) -> (  0.0000   0.0000   0.0000)
    ( 11.7600  20.0000   0.0000) -> (  0.0000  20.0000   0.0000)
    ( 24.0000  20.0000   0.0000) -> ( 11.7600  20.0000   0.0000)
    ( 24.0000   6.0000   0.0000) -> ( 24.0000  20.0000   0.0000)
    (  0.0000   0.0000   0.0000) -> ( 24.0000   6.0000   0.0000)   <- スプライン全部
```

y = 20 の直線は x = 11.76 で分かれているのに、**スプラインは端から端まで**
残っています。面を1つずつ割らせて断り文を出させると、そのまま出ました。

```text
A4 -> Boundary edge is not a single splittable Bezier span
```

`split_oriented_edge_at` は `split_bezier_at` しか使っていませんでした。
これは制御点が `次数+1` 個ちょうどの**1スパン**しか割れません。押し出した
スプラインの輪郭は内部ノットを持つので、そこで断られ、**面はそのまま**
残っていました。

`split_at`（ノット挿入で割る）を後ろに置きました。多スパンでも有理でも通り、
割った2本は元の曲線と同じ点を通ります。1スパンでは de Casteljau のほうが
安いので、順番はそのままです。

#### 実測

| | 前 | 後 |
| :--- | ---: | ---: |
| `foreign_boolean_probe` ok | 23 | **24** |
| 拒否 | 9 | **6** |
| 検証つきの口 | 79/90 | **82/90** |
| 恒等式の残差 | 8.38e-10 | 8.38e-10 |

| | 差 | 積 |
| :--- | ---: | ---: |
| OpenCASCADE | 2950.632975 | 2269.802321 |
| Zenith | 2950.6330 | 2269.8023 |

残る6件は `revolved_ring half slab` 3 と `torus_segment centre drill` 3。
`revolved_ring` は別種です——**切り込みが穴を横切ります**。輪の上下面は円環で、
切り込みは外周から穴まで走るので、片方の端が内側のワイヤに乗ります。いまは
「外の境界に乗っていない」と断っています。穴が片方に丸ごと入る配置は 4-54 で
扱えるようになりましたが、**穴自体を割る**にはワイヤを平面グラフとして
繋ぎ直す必要があり、そこは手つかずです。

---

### 4-58. 曲面に届いた交線を、1本の鎖として渡していた（2026/08/23）

`torus_segment centre drill` は交線が12本出て、トーラス側の4枚はどれも
割れるのに、**ドリルの側面が1枚も割れません**でした。

曲面同士の交線は相手のパッチの境界で細切れになって届くので、1本ずつでは
どれも面の内側で終わります（「the splitting curve ends 1.885e0 away from the
boundary」）。そのための受け皿——鎖にまとめて当て直す——は前からありました。
そこに何と言われているかを出させると、こうです。

```text
B1  4 edge(s) -> a cut made of 4 curves has 4 loose ends, not two
B3  4 edge(s) -> a cut made of 4 curves has 4 loose ends, not two
```

**鎖は1本とは限りません。** ドリルの側面1枚には、トーラス片へ**入る所と
出る所**の2箇所ぶんの稜が届きます。丸ごと渡せば当然「端が4つ」になります。
端の繋がりで鎖に分け、1本ずつ順に当てるようにしました。平面の経路は前から
そうしています（`group_edges_into_chains`）。

#### 実測

| | 前 | 後 |
| :--- | ---: | ---: |
| `foreign_boolean_probe` ok | 24 | **25** |
| 拒否 | 6 | **3** |
| 検証つきの口 | 82/90 | **85/90** |
| 恒等式の残差 | 8.38e-10 | 4.04e-9 |
| WRONG / PANIC | 0 / 0 | 0 / 0 |

| | 差 | 積 |
| :--- | ---: | ---: |
| OpenCASCADE | 756.636359 | 190.846360 |
| Zenith | 756.6311 | 190.8510 |

**ここは桁いっぱいでは合いません。** 切り口が曲面どうしの交線で囲まれるので、
こちらの体積はメッシュから、OCC のは有理 B-spline 上の求積から出ています。
OCC 自身も同じ走行で V(A^B) を 190.846360、removed を 190.845664 と、
7e-4 違えて報告します。恒等式のほうは 4.04e-9 で閉じています。

#### 残るのは1種類

`revolved_ring half slab` の3件だけです。これは**切り込みが穴を横切る**配置で、
輪の上下面は円環、切り込みは外周から穴まで走るので、片方の端が内側のワイヤに
乗ります。いまは「外の境界に乗っていない」と断ります。穴が片方の片に丸ごと
入る配置は 4-54 で扱えるようになりましたが、**穴自体を割る**には外側と内側の
ワイヤを平面グラフとして繋ぎ直す必要があり、そこは手つかずです。

加えて `sphere corner` は、答えが正しいのにゲートが2件断ります（4-51）。

---

### 4-59. 切り込みが穴を横切る — 拒否 0 になりました（2026/08/23）

最後に残ったのは1種類、`revolved_ring half slab` の3件です。輪の上下面は
円環で、切り込みは外周から穴まで走るので、**片方の端が内側のワイヤに乗ります**。
それまでの分割はどれも端を外側のワイヤにしか探さず、

```text
Split edge end does not lie on the outer boundary
```

で断っていました。穴が片方の片に丸ごと入る配置は 4-54 で扱えるように
なりましたが、**穴自体が割れる**配置はそこでも扱えません。

#### uv の平面アレンジメント

`split_planar_face_across_holes` を**最後の受け皿**として入れました。

1. 切り込みの端を、外側・内側すべてのワイヤに対して探す
2. 各ワイヤを着地点で細分して有向の弧にし、切り込みを加える
3. **入ってきた弧の逆から見て、反時計回りに1つ手前**の弧へ進む標準の巡回で
   面を取り出す（有界な面は符号付き面積が正で出ます）
4. 代表点が元の面の内側（外周の内、どの穴の外）にある巡回だけ残す
5. 着地の無かった穴は、それを含む領域の穴として戻す
6. **領域の面積の和が元の面積に戻ること**を測る。戻らなければ断る

6 は最初 5.3e-4 で落ちました。多角形の標本が粗かったためで、密度を揃えて
96 にすると 1e-6 の帯に収まります。**閉じた輪になっただけでは領域の
取り違えは分からない**ので、この検査は外しません。

#### そこまで進めて、別の裏返りが見えた

面が割れるようになると、縫えない稜は 20 本から 0 本になり、代わりに
**同じ向きに2度使われた稜が16本**出ました。プローブに相手を名指しさせると、
どの組にも同じ2枚——**穴の壁の断片**——が入っていました。

円柱側面の分割器は、断片の輪を決め打ちの順（下・切り込み・上を逆・左を逆）で
組んでいて、**元の面がどちら巻きかを見ていません**。輪の穴の壁は裏向きの面
なので、そこだけ裏返ります。4-46 と同じ欠陥が、別の分割器に残っていました。

`Regularizer` と同じ約束——p-curve の符号付き面積を面の向きで調整したものが
正——で断片を測り、外れていたら巻き直すようにしました
（`hold_piece_like_its_face`）。

#### 実測

| | 前 | 後 |
| :--- | ---: | ---: |
| `foreign_boolean_probe` ok | 25 | **26** |
| 拒否 | 3 | **0** |
| 検証つきの口 | 85/90 | **88/90** |
| 恒等式の残差 | 4.04e-9 | 4.04e-9 |
| WRONG / PANIC | 0 / 0 | 0 / 0 |

| | 差 | 積 | 和 |
| :--- | ---: | ---: | ---: |
| OpenCASCADE | 806.083750 | 777.278947 | 6566.083750 |
| Zenith | 806.0838 | 777.2789 | 6566.0838 |

**30配置90演算のうち、断るものは無くなりました。** 残る2件は
`sphere corner` の差と和で、**答えは正しいのにゲートが断ります**（4-51、
未解決）。直すべきはそちらで、ゲートを緩めるのではなく、面までの距離を
メッシュ同士ではなく B-rep の面に対して測るべきです。

---

### 4-60. 検体の種類を増やしたら、読めない曲面が1つ出てきた（2026/08/23）

ブーリアンの拒否が 0 になり、**この物差しが何も見つけなくなりました**。
30配置とも通ります。そこで検体のほうを広げました。

いままでの10検体は解析曲面（円柱・円錐・球・トーラス）と掃引が少しで、
**フィレット・面取り・複数の穴・ロフト・スロット・中空・挽き物**が1つも
ありません。部品ファイルを開けばまず出てくる形です。

`tools/occ_reference_shapes.py` に9つ足しました。いずれも OpenCASCADE 自身に
書かせています。

| 検体 | 何を初めて測るか |
| :--- | :--- |
| `filleted_box` | 稜のブレンド。円柱面と球面が角で3枚ずつ集まる |
| `chamfered_box` | 1つの頂点に平面が6枚 |
| `plate_with_holes` | 1枚の面が内側のワイヤを**3本**持つ |
| `slotted_block` | 外周のワイヤが**凹** |
| `lofted_solid` | u と v の両方に曲率のある B-spline 曲面 |
| `pipe_bend` | 曲がった軌道に沿った掃引 |
| `revolved_vase` | スプラインの母線を回した挽き物 |
| `hollow_box` | **内側の殻**を持つ立体 |
| `stepped_shaft` | 同軸で半径が変わるところの円環平面 |

#### 出てきた欠陥: 回転面が読めない

`revolved_vase` は `SURFACE_OF_REVOLUTION` を持ちます。インポーターは
**対応しておらず**、「読める曲面」の一覧にも挙げていませんでした。

```text
Unsupported surface entity SURFACE_OF_REVOLUTION (#46)
```

既存の検体を全部調べたところ、この実体は**1つも入っていません**でした。
`occ_reference_swept.py` は「OCC は回転面を選ぶ」と書いていますが、実際には
円柱と平面に落としていました。**書いてある対応範囲と、測ってある対応範囲は
別物です。**

回転は厳密に表せます。角度方向を有理2次（円弧の標準の組み方）、断面方向は
元の曲線の次数とノットをそのまま。これは解析曲面が既に通っている
`revolve_profile` と同じ組み方です。`revolution_patch` を足しました。

**断面が軸を含む平面から外れていたら断ります。** それを回すと、できるのは
回転面ではなく螺旋面です。

#### 検体のほうが間違っていた

最初の `pipe_bend` は断面を軌道に直交させておらず、断面の平面が軸から
**0.53 mm ずれた** `SURFACE_OF_REVOLUTION` になっていました。管ではなく
別の回転面です。軌道を「始点の接線が X 軸に乗る」円弧に直すと、OCC は
`TOROIDAL_SURFACE` を書きます。**検体を作るときも、意図した形になっているか
測ってください。**

#### 期待値に相手の値を使わなかった

`revolved_vase` の体積は、こちらが 4171.053368、OCC の立体求積が
4170.999302 で、**1.3e-5 違いました**。刻みを 16 から 256 まで振っても
こちらの値は 10 桁動きません。**動かない差は求積の粗さではなく、測っている
対象が違う印です**（4-45）。

母線からグリーンの定理で直接積分しました（`V = 2π ∮ x²/2 dz`、曲線の評価
しか使いません）。

```text
刻み    200: 4170.801696
刻み   2000: 4171.050851
刻み  20000: 4171.053343
刻み 200000: 4171.053368   <- こちらの読み値と 10 桁一致
```

**外れていたのは OCC の求積のほうでした。** 有理 B-spline の上で OCC の
求積が緩いことは 4-45 でも見ています。期待値に相手の値をそのまま使うと、
**相手の誤差を仕様として焼き付ける**ことになります。
`tools/revolved_volume_reference.py` に残してあります。

なお最初は `Part.Face.Area` と `CenterOfMass` からパップスの定理で出そうと
しましたが、それらも OCC の求積を通るので独立ではなく、2.8e-4 ずれた第3の値が
出ました。**独立とは、相手の測定を1つも通らないという意味です。**

#### 実測

9検体すべて読め、体積は解析解または OCC と **3e-11 以内**で一致します。
面数も9件とも一致。`shape_variety_probe` が出します。


---

### 4-61. 切り手を傾けたら、また出てきた（2026/08/23）

拒否が 0 になったのは、**30配置とも軸に平行な切り手だった**からでもあります。
切り手を境界箱の中心まわりに 27 度傾けた配置を足しました（`ZENITH_TILTED=1`）。
角度も軸 (1,1,1) も、どの検体の刻みとも噛み合いません。

軸に平行だと、面の組がすべて座標面に乗るので、平面同士の交線も等パラメータ線も
特別に易しくなります。**傾けると、そこが全部一般の場合になります。**

すぐに出ました。

| 配置 | 結果 |
| :--- | :--- |
| `tilted slab` | `cylinder_nurbs`・`revolved_ring` が拒否 |
| `tilted drill` | `cone`・`cone_full`・`cylinder_nurbs`・`revolved_ring`・`sphere`・`sphere_capped` が拒否 |
| `tilted corner` | 全検体通る |

`cone_full tilted drill` を段で見ると、**交線の輪が閉じていません**。円錐の
側面は4分割されていて、交線は4本しか出ず、しかも端が繋がりません。

```text
A0 x B3  (  1.6256  -3.9533  11.4511) -> (  4.2238   0.0000  11.5523)
A1 x B1  (  0.0000  -0.1096  19.7809) -> ( -2.2822  -1.8734  14.0946)
A3 x B0  (  4.1378   0.4696  11.6712) -> (  0.5245   2.3267  15.2299)
A3 x B1  (  0.5245   2.3267  15.2299) -> (  0.0959  -0.0000  19.8082)
```

見どころは2つです。

- **A2 には交線が1本も届いていません。** 傾いたドリルと円錐の交わりは
  4分の1面を4枚とも横切る閉曲線のはずです。
- **端が面の境界に乗っていません。** 側面の分かれ目は `x = 0` と `y = 0` の
  平面ですが、`( 1.6256 -3.9533 11.4511)` も `( 4.1378 0.4696 11.6712)` も
  どちらでもありません。

> **訂正（2026/08/23、`march_stop_probe` で測定）。** ここで「辿りが面の
> 内側で止まっている」と書いていましたが、**違いました**。辿りは8組とも
> 正しくパッチの縁まで届いています（`stopped_at_boundary`）。
>
> 宙に浮いた端点を測ると、**隣のパッチの継ぎ目にちょうど乗っています**。
>
> ```text
> loose end (1.6256 -3.9533 11.4511) sits on:
>     A0  distance 2.44e-5  (u 0.1854, v 0.4274)
>     B2  distance 1.69e-5  (u 1.0000, v 0.5454)  <- パッチの縁
>     B3  distance 2.45e-6  (u 0.0000, v 0.5454)
> ```
>
> `A0 x B3` の交線は B3 の縁で終わり、**続きは `A0 x B2` にあります**。
> ところがその組は「種はあったが一度も辿れなかった」と出ます。
>
> **面の組を1つずつ独立に辿っているのが原因です。** 種は 12x12 の格子から
> 選ぶので、隣の升に入るぶんの弧が短いと、どの種も弧に乗りません。
> 端点は交線の上の点なので、**そこを種にすれば辿れるはずです**。
>
> 「交わらない組」と「種はあるのに辿れない組」も、本数だけでは
> 区別できませんでした。`march_stop_probe` は両者を分けて出します。

底面（傾いたドリルが作る楕円）のほうは、閉じた輪として正しく出ており、
面の内側で閉じたループとして割れています。**落ちているのは側面だけ**です。

これは次の仕事です。

#### 実測（60配置・180演算）

| | 軸に平行だけ（30配置） | 傾けたぶんを足す（60配置） |
| :--- | ---: | ---: |
| ok | 26 | 44 |
| NOCUT | 4 | 7 |
| 拒否 | **0** | **27** |
| WRONG / PANIC | 0 / 0 | 0 / 0 |
| 検証つきの口 | 88/90 | 149/180 |
| 恒等式の残差 | 4.04e-9 | 4.04e-9 |

拒否は9つの組み合わせ×3演算です。

```text
cone           tilted drill        cylinder_nurbs tilted drill / tilted slab
cone_full      tilted drill        revolved_ring  tilted drill / tilted slab
sphere         tilted drill        sphere_capped  tilted drill
torus          tilted slab
```

**`tilted corner` は全検体通ります。** 角を落とす切り方は交線が短く、
1枚の面の中で完結するので、傾けても難しくなりません。難しいのは
**検体を貫く**切り方です。

#### 傾けると、費用のほうも出ました

`elliptic_prism tilted drill` は答えは正しいのに、**1件で15分**かかって
いました。原因は交線の当てはめで、刻みを半分ずつ最大12回まで試すたびに
**最初から辿り直していた**ことです。

2回測れば、ずれが刻みの何乗で落ちているかが出ます。3次補間の理屈は4乗
ですが、序盤は 0.6 倍（0.7乗相当）までしか落ちないこともあるので、
**理屈ではなくその場で測った次数**から必要な刻みを予測するようにしました。
安全率 1.5、1回あたり 16 倍までに制限、点の上限に当たったら降ります。

| | 前 | 後 |
| :--- | ---: | ---: |
| `elliptic_prism tilted drill` | **15分** | **75秒** |
| 60配置ぜんぶ | 打ち切り（53行で75分超） | **26分29秒** |
| 答え | 3635.7028 / 322.7040 | **同じ** |

---

### 4-62. 交線が隣の升で途切れていた — 端点を種にして繋いだ（2026/08/23）

4-61 で「辿りが面の内側で止まっている」と書きました。**違いました。**
`march_stop_probe` を書いて測ると、辿りは8組とも正しくパッチの縁まで
届いています（`stopped_at_boundary`）。

#### 「交線が無い」の中身は3つあった

本数だけを数えていると、次の3つが同じ「0本」に見えます。

| | `cone_full` を傾けたドリルで抜く |
| :--- | ---: |
| 種が無い（本当に離れている） | 10組 |
| **種はあるが一度も辿れない** | **12組** |
| 辿ってパッチの縁まで届いた | 8組 |

`march_stop_probe` はこの3つを分けて出します。真ん中が問題です。

#### 端点は、隣のパッチの継ぎ目に乗っていた

宙に浮いた端点を測ると、こうでした。

```text
loose end (1.6256 -3.9533 11.4511) sits on:
    A0  distance 2.44e-5  (u 0.1854, v 0.4274)
    B2  distance 1.69e-5  (u 1.0000, v 0.5454)  <- パッチの縁
    B3  distance 2.45e-6  (u 0.0000, v 0.5454)
```

`A0 x B3` の交線は B3 の縁で終わり、**続きは `A0 x B2` にあります**。その組は
「種はあるが辿れない」に入っていました。

**面の組を1つずつ独立に辿っているのが原因です。** 種は 12x12 の格子から
選ぶので、隣の升に入るぶんの弧が短いと、どの種も弧に乗りません。

#### 直したもの

`trace_from_loose_ends`。相手のいない端点を拾い、その点を含む「まだ交線の
無い」面の組を、**その点から**辿り直します。端点は既に交線の上に 2e-5 で
乗っているので、ニュートンはすぐ収束します。

途中で罠が1つありました。「もう交線のある組」を候補の列から数えていたの
ですが、**交わりを求めて `Unsupported` になった組も候補の列に入っています**。
素通しで数えると全部「もう見た」になり、辿り直しが一度も走りませんでした。
交線を実際に持つ組だけを既済とします。

#### 実測

| | 前 | 後 |
| :--- | ---: | ---: |
| `cone_full tilted drill` の交線 | 8本 | **12本** |
| 輪 | 閉じない | **閉じる**（8本の弧が一周して戻る） |
| `foreign_boolean_probe`（60配置180演算） | ok 44 / 拒否 27 | **ok 44 / 拒否 27** |
| 検証つきの口 | 149/180 | 149/180 |
| 60配置の所要 | 26分29秒 | 28分34秒 |

**通る数は1件も増えていません。** 交線の段は直りましたが、その先が塞いで
います。所要は 8% 増えました（相手のいない端点を探すぶん）。

#### 次の関門

面の分割です。`cone_full tilted drill` の内訳:

```text
A0   3 split edge(s) -> 2 piece(s) (applied 1, skipped 2)
B1   4 split edge(s) -> 2 piece(s) (applied 1, skipped 3)
B2   3 split edge(s) -> 2 piece(s) (applied 1, skipped 2)
B3   3 split edge(s) -> 2 piece(s) (applied 1, skipped 2)
```

1枚の面に**2本の鎖**が届く配置で、片方しか当たっていません。鎖に分ける
ところは 4-58 で入れましたが、**鎖が1本の稜しか持たないときは
`chain.len() >= 2` で外れます**。そこは1本ずつの経路が拾うはずですが、
「境界に届かない」と断られています。次はそこを測ってください。

---

### 4-63. 1本でも当たると、鎖の経路が一度も走らなかった（2026/08/24）

4-62 で交線の輪は閉じました。それでも `cone_full tilted drill` は断られ、
分割の内訳はこうでした。

```text
A0   3 split edge(s) -> 2 piece(s) (applied 1, skipped 2)
B1   4 split edge(s) -> 2 piece(s) (applied 1, skipped 3)
B2   3 split edge(s) -> 2 piece(s) (applied 1, skipped 2)
B3   3 split edge(s) -> 2 piece(s) (applied 1, skipped 2)
```

#### 条件が「0本のときだけ」になっていた

鎖にまとめて当て直す受け皿は2つあり、**どちらも
`applied_split_count == 0` を条件にしています**。

1枚の面に**2本の鎖**が届き、片方が1本の稜で単独に当たると、その瞬間
`applied_split_count` は 1 になり、**もう片方の鎖は一度も試されないまま
捨てられます**。「全部だめだったときの受け皿」として書いたものが、
「1つでも通ったら要らない」という意味になっていました。

当たらなかった稜だけを集めて鎖にまとめ、**当たった片に対して**当て直す段を
足しました。全部当たっていれば残りは無く、素通りします。

#### 実測

| | 前 | 後 |
| :--- | ---: | ---: |
| `cone_full tilted drill` の分割 | applied 1 / skipped 2 | **applied 3 / skipped 0** |
| 傾け60配置180演算 ok | 44 | **47** |
| 拒否 | 27 | **18** |
| 検証つきの口 | 149/180 | **158/180** |
| WRONG / PANIC | 0 / 0 | 0 / 0 |
| 軸に平行30配置 | ok 26 / 拒否 0 | ok 26 / 拒否 0 |
| 60配置の所要 | 28分34秒 | 28分03秒 |

`cone_full tilted drill` は 1453.7863 / 640.6088 / 3896.6888（残差 8.45e-11）。
OpenCASCADE は同じ配置で 1453.770752 / 640.623995 で、相対 1.1e-5 の差が
あります。曲面どうしの交線で囲まれた切り口なので、`torus_segment drill` と
同じ性質の差です（4-58）。傾けた配置も外の物差しに当てられるよう、
`occ_cut_reference.py` に `tilted <切り手>` を足しました。

#### 残っている6件と、その中身

`cone`・`cylinder_nurbs`・`revolved_ring`・`torus` の傾け配置です。
`cone tilted drill` を見ると、性質の違う2つが残っています。

- **鎖が稜1本しかないもの。** 単独では境界に届かないので当たらず、鎖の
  経路は `chain.len() >= 2` で外します。
- **端点が面の境界から 5.073e-4 外れているもの**（許容は 2e-5）。

後者は追いかけました。**クリップの内外判定が折れ線に対して行われています**
（`sample_pcurve_loop(&pcurves.outer_loop, 48)`）。二分は折れ線に詰まるので、
**切れる位置が面の境界からたわみのぶんずれます**。

曲線そのものに対して内外を判定する版を書いて試したところ、その面のずれは
5.073e-4 から **1.473e-3 へ悪化**しました。理由が分からなかったので、
その場では取り下げました。

> **続き（4-64、2026/08/24）。** 理由が分かりました。**内外の判定を
> `segment_inside_pcurve_loop_intervals` に任せたのが誤りでした。**
> あの関数は交点こそ厳密に解きますが、区間ごとの内外は**中点を 32 点の
> 折れ線で見て**決めます。点が境界のすぐ近くにあるとき——二分の終盤は
> まさにそこです——中点も境界のすぐ近くにあり、その判定が外れます。
> **折れ線を1か所から追い出したつもりで、もっと粗い折れ線を呼んでいました。**
> 交点の**個数の偶奇**で決める版に書き直したところ、近接の食い違いは
> 消えました。

---

### 4-64. 折れ線を追い出したつもりで、もっと粗い折れ線を呼んでいた（2026/08/24）

4-63 の残り。切り込みの端が面の境界から 5.073e-4 外れて、分割が断られる件です。

#### まず、どの端がどれだけ外れているかを出した

断り文はその面の worst しか教えません。端点を1つずつ、いちばん近い境界稜への
**厳密な距離**とともに出すようにしました（`intersection_edge_probe`）。

```text
A4  end (  3.8246   1.1699  20.0000) is 5.073e-4 from boundary edge 0 (allowed 8.000e-5)
B0  end (  3.8251   1.1697  20.0000) is 1.685e0 from boundary edge 1 (allowed 6.022e-4)
```

**同じ点が 3.8246 と 3.8251 の2通りで出ています。** 円錐の上面は半径 4 で、
前者の半径は **3.99954**——真の円の内側 4.6e-4 です。**折れ線に内接した位置**
であり、どちらの面の折れ線で切ったかで食い違います。

#### 前回の直しが悪化した理由

曲線そのもので内外を判定する版は、**`segment_inside_pcurve_loop_intervals`
に任せていました**。あの関数は交点こそ厳密に解きますが、区間ごとの内外は
**中点を 32 点の折れ線で見て**決めます。

点が境界のすぐ近くにあるとき——**二分の終盤はまさにそこです**——中点も境界の
すぐ近くにあり、その判定が外れます。48 点の折れ線を1か所から追い出した
つもりで、**32 点の折れ線を呼んでいました**。悪化して当然です。

#### 直したもの

`point_inside_pcurve_loop`。点からループの外へ線分を引き、**交わった回数の
偶奇**で決めます。交点は閉じた式（1スパンの1次・2次）か曲線上の二分で厳密に
出ます（4-56）。中点も折れ線も出てきません。

交点が重なる向き（頂点を貫く、接する）では数が狂うので、重なりがあれば別の
向きで測り直します。3方向とも駄目なら従来どおり折れ線に落とします。

#### 実測

近接の食い違い（5.073e-4 と 3.808e-4）は消え、残るのは正当な内部の継ぎ目
だけになりました。`cone tilted drill` の分割は B 面の skipped が 3 から 0 に、
ブーリアンは通るようになりました。

| | 差 |
| :--- | ---: |
| OpenCASCADE | 2456.083364 |
| Zenith | 2456.083605 |

相対 9.8e-8。曲面どうしの交線で囲まれた切り口としては、これまででいちばん
よく合っています。

#### 実測（60配置・180演算）

| | 前 | 後 |
| :--- | ---: | ---: |
| ok | 47 | **49** |
| 拒否 | 18 | **12** |
| 検証つきの口 | 158/180 | **164/180** |
| WRONG / PANIC | 0 / 0 | 0 / 0 |
| 恒等式の残差 | 4.04e-9 | 7.88e-9 |
| 所要 | 28分03秒 | 27分36秒 |

解消したのは `cone tilted drill` と `cylinder_nurbs tilted drill` です。
残る12件は `cylinder_nurbs tilted slab`、`revolved_ring tilted drill /
tilted slab`、`torus tilted slab` の4つの組み合わせ。

**残差が 4.04e-9 から 7.88e-9 に増えています。** 通る配置が増えたぶん、
測る対象も増えました（4-53 と同じで、拒否が減ると隠れていた誤差が出ます）。
どちらも判定の線（1e-6）からは3桁離れています。

---

### 4-65. 稜を端点だけで見分けていた — 壊れていたのは立体ではなく数え方（2026/08/24）

`torus tilted slab` は、それまでと壊れ方が違いました。

```text
selected face stitching has 0 unmatched edge uses, 6 non-manifold edge uses,
and 0 same-direction edge uses; with caps it has 17 face pieces
```

**縫えない稜は1本もありません。非多様体が6本です**——同じ稜が3回以上
使われている、つまり面が重なっている、という報告です。分割は A も B も
skipped 0 で、切り込みは全部当たっています。

#### 最初の見立ては外れました

「面積ゼロの薄片が稜の数だけ増やしている」と考えました。輪はそれらしく
見えます——3片とも稜2本で、同じ2点の間を結ぶだけです。

```text
 7 A Outside 2 edge use(s)
   (-11.2805  4.0928 -4.0000) -> ( -7.0067  9.7420 -4.0000)
   ( -7.0067  9.7420 -4.0000) -> (-11.2805  4.0928 -4.0000)
```

**面積を測ったら 2.276、35.19、36.19 でした。** 薄片ではありません。
落とす直しは書いた上で取り下げました。

#### 同じ2点を結ぶ、別々の弧だった

交線を並べると、同じ2点の間に**2本**あります。

```text
A6 x B0  ( -7.0067  9.7420 -4.0000) -> (-11.2805  4.0928 -4.0000)
A7 x B0  (-11.2805  4.0928 -4.0000) -> ( -7.0067  9.7420 -4.0000)
```

同じ曲線が二重に出ているのかと思って中点を測ると、**3.839 離れて**いました。
別々の弧です。両端の z はどちらも -4——トーラスの管のいちばん下、四半パッチの
継ぎ目で、そこを挟む2枚から別々の弧が出ているだけの、**正当な形**でした。

#### 数え方が3か所とも端点だけだった

| 場所 | 何をしていたか |
| :--- | :--- |
| `StitchEdgeUse` | 稜を `(start, end)` で持ち、突き合わせも端点だけ |
| `face_boundary_signature` | 面の署名が稜の**端点の集合**だけ |
| `same_directed_edge` / `opposite_direction_edge` | `EdgeUse` は `middle` を**持っているのに使っていない** |

**同じ2点を結ぶ別々の弧は、この3つのどれからも見分けられません。** 結果、
閉じた殻なのに「非多様体」「面が重複」「同じ向きに2度使われた稜」と、
順番に報告されました。3つとも**立体ではなく数え方の欠陥**です。

3か所とも中点を見るようにしました。公差は稜の長さに対する相対で取ります
（分割の仕方が違う2枚が同じ稜を持つとき、中点は丸め誤差ぶん動きます）。

直すたびに次の検査が引っかかり、**3段構えで同じ間違いをしていた**ことが
分かりました。

```text
非多様体が6本           -> 面の署名が重複（9件） -> 同じ向きの使用が重複（6件） -> 通る
```

#### 実測

| | 差 | 積 | 和 |
| :--- | ---: | ---: | ---: |
| OpenCASCADE | 1937.013599 | 1852.918566 | 21597.786310 |
| Zenith | 1936.965966 | — | — |

相対 2.5e-5。曲面どうしの交線で囲まれた切り口で、OCC 自身も V(A^B) と
removed を 2.2e-6 違えて報告します。

#### 実測（60配置・180演算）

| | 前 | 後 |
| :--- | ---: | ---: |
| ok | 49 | **51** |
| 拒否 | 12 | **6** |
| 検証つきの口 | 164/180 | **170/180** |
| WRONG / PANIC | 0 / 0 | 0 / 0 |
| 恒等式の残差 | 7.88e-9 | 7.88e-9 |

解消したのは `torus tilted slab` と `cylinder_nurbs tilted slab` です。
**残るのは `revolved_ring` の2組だけになりました。**

**殻の検査そのものを変えているので、回帰は特に念入りに見ました。**
94バイナリ・505テストとも通り、FreeCAD/OpenCASCADE の4ゲートも緑、
軸に平行な30配置も従来どおりです。

---

### 4-66. 穴のある面を割る受け皿が、3つの理由で断っていた（2026/08/24）

残るのは `revolved_ring` の2組だけになりました。輪の上下面は円環（穴つき）
なので、4-59 で入れた uv のアレンジメントを通ります。そこが3つの理由で
断っていました。

#### 1. 面積の判定が、判定の帯より粗かった

```text
the regions add up to 2.638848e2 against the face's 2.638819e2
```

相対 **1.1e-5**、帯は 1e-6。面積は uv の多角形（1区間 96 点）で測って
いました。多角形は曲がった境界を弦で置き換えるので、切り込みが傾いていると
誤差が帯を超えます。

**判定に使う量は、判定の帯より細かく測れていなければなりません。**
`MassCalculator::compute_face_integral`——面そのものの積分——に替えました。
片を組み立てたあとに測るので、穴の割り当ても含めて確かめられます。

#### 2. 切り込みどうしの継ぎ目に、境界を要求していた

```text
a cut end (-3.8817 9.1218 0.0000) does not lie on any wire of the face
```

半径は **9.9137**。外周 10 でも穴 4 でもなく、面の内側です。切り込みが鎖に
なっていて、その継ぎ目でした。

**境界に着かなければならないのは、相手のいない端だけです。** 他の切り込みと
共有している端は内部の節点で、境界に乗る理由がありません。

（断り文が「どこかの端」としか言わなかったので、まず点を名指しするように
しました。それだけで半径を測れば済む話になりました。）

#### 3. 「穴を横切る」を狭く取りすぎていた

外周と穴を**つなぐ**切り込みがあるときだけ働くようにしていました。傾けた
ドリルが円環の中を噛む配置では、切り込みの両端とも**穴の縁**に着きます。
「穴に関わっているか」で見るように緩めました。ここは最後の受け皿なので、
普通の経路で割れる面は来ません。

#### 実測

`revolved_ring tilted slab` が通るようになりました。

| | 差 |
| :--- | ---: |
| OpenCASCADE | 808.697682 |
| Zenith | 808.697667 |

相対 1.9e-8。

#### 実測（60配置・180演算）

| | 前 | 後 |
| :--- | ---: | ---: |
| ok | 51 | **52** |
| 拒否 | 6 | **3** |
| 検証つきの口 | 170/180 | **173/180** |
| WRONG / PANIC | 0 / 0 | 0 / 0 |

**残るのは `revolved_ring tilted drill` の1組（3演算）だけ**になりました。


#### まだ通らないもの（4-67 で解決しました）

`revolved_ring tilted drill` は3つとも越えて、次の壁に当たっています。

```text
cutting across the hole did not divide the face
```

**穴を持つ領域を作らなければなりません。** 円環の穴の縁から出て穴の縁へ
戻る切り込みは、面を2つに分けます——噛み取られたぶんと、残り。残りのほうは
**外周を外側の輪、[穴の弧＋切り込み] を内側の輪**として持つ、穴のある面です。
いまの巡回は領域を1本の輪としてしか組み立てられず、入れ子にする段がありません
（作った巡回を包含関係で入れ子にすれば済むはずで、同じことは
`build_caps_from_nested_loops` が既にやっています）。**次はそこです。**

#### 次に必要なもの（設計まで詰めてあります）

円環に、穴の縁 `p` から出て穴の縁 `q` へ戻る切り込み `C` が入る配置を考えます。
グラフには外周 `O` と、穴の円 `B`（`p`,`q` で2本の弧 `B1`,`B2` に分かれる）と
`C` が入ります。**`O` は `B` や `C` と繋がっていません。**

正しい答えは2枚です。

- **噛み取られたぶん**: `B1` と `C` で囲まれた三日月
- **残り**: 外側の輪が `O`、内側の輪が `B2 + C`

いまの巡回は連結成分ごとに閉路を出すので、繋がっていない `O` と
`{B, C}` をまたいだ「穴のある領域」を1本の輪として作れません。

**足りないのは入れ子です。** 連結成分ごとに、面積が負の閉路（その成分の
**外形**）が1つ出ます。それを、別の成分の面の**穴**として付ければ済みます。

1. 着地の無い外周も、グラフに入れる（いまは丸ごと落としています）
2. 成分ごとに、正の閉路＝その成分の面、負の閉路＝その成分の外形
3. ある成分の外形を含む面を探し、その面の穴にする
4. 代表点は**穴を避けて**取る（いまは穴の中に落ちて面ごと捨てられます）

同じ入れ子の組み立ては `build_caps_from_nested_loops` が既にやっています。

---

### 4-67. 穴のある領域を作れるようにした — 傾けた切り手の拒否が 0 に（2026/08/24）

4-66 の末尾に詰めた設計のとおりに入れました。円環に、穴の縁 `p` から出て
穴の縁 `q` へ戻る切り込み `C` が入る配置です。正しい答えは2枚——`B1` と `C` で
囲まれた三日月と、**外周を外側の輪、`B2 + C` を内側の輪に持つ残り**。

巡回は連結成分ごとにしか閉路を出せないので、繋がっていない外周と穴まわりを
またいだ領域は1本の輪になりません。**足りないのは入れ子**でした。

#### 入れたもの

1. **着地の無い外周も、グラフに入れる。** それまで落としていたので、外周に
   囲まれた領域そのものが巡回に出てきませんでした。閉じた稜が1本だけの輪
   （全周の円）は、始点と終点が同じで弧にならないので半分に割ります。
2. **代表点は穴を避けて取る。** 外周に囲まれた領域の重心は、円環では穴の中に
   落ちます。そこで内外を訊くと、正しい領域が「穴の中だから面ではない」と
   捨てられます。
3. **面積が負の閉路＝その連結成分の外形**として取っておき、それを含む
   いちばん小さい面の穴にする。

#### 直しながら踏んだもの

**外形を、自分自身の成分の面に付けていました。** 外周の外形は外周の面に
含まれるので、素直に探すとそこに付きます。結果、314.16 の面が **面積 0** に
なり、「領域の和が 63.25、面は 263.89」と出ました。巡回ごとに通った節点を
覚えて、**同じ成分には付けない**ようにしました。

**穴の輪の向きが、外周と同じでした。** 外形は巡回が出した時点で外周と逆向き
（面積が負）なのに、裏返して持っていました。縫合は「同じ向きに2度使われた稜」
を12本出し、**衝突した片は1枚だけで、それが相手全部と衝突**していました
——1枚だけが裏返っているときの形です。

#### 実測

`revolved_ring tilted drill` が通り、**傾けた切り手の拒否は 0 になりました**。

| | 差 |
| :--- | ---: |
| OpenCASCADE | 1567.804501 |
| Zenith | 1567.804536 |

相対 2.2e-8。

#### 実測（60配置・180演算）

| | 前 | 後 |
| :--- | ---: | ---: |
| ok | 52 | **53** |
| 拒否 | 3 | **0** |
| 検証つきの口 | 173/180 | **176/180** |
| WRONG / PANIC | 0 / 0 | 0 / 0 |
| 恒等式の残差 | 7.88e-9 | 7.88e-9 |

**傾けた切り手でも、断るものは無くなりました。** 60配置180演算すべてが
答えを返します。検証つきの口で通らない4件は、**答えが正しいのにゲートが
断るもの**です（軸に平行な `sphere corner` の2件と、傾けた同2件。4-51）。

4-61 で「軸に平行は特別な場合」と書いて27件が出たところから、6つの直しで
0 になりました。**どれも幾何ではなく、判定や数え方の欠陥でした。**

| | 中身 |
| :--- | :--- |
| 4-62 | 面の組を独立に辿るので、隣の升の短い弧が抜けていた |
| 4-63 | 「全部だめなときの受け皿」が「1つでも通ったら要らない」になっていた |
| 4-64 | クリップの内外判定が折れ線で、切れる位置が境界からずれていた |
| 4-65 | 稜を端点だけで見分け、同じ2点を結ぶ別の弧を1本と数えていた（3か所） |
| 4-66 | 判定の量が判定の帯より粗い／継ぎ目に境界を要求／条件が狭すぎ |
| 4-67 | 穴のある領域を組み立てられなかった（入れ子が無かった） |

---

### 4-68. ゲートが断っていた4件 — 緩めずに直した。捨てられていたのは面だった（2026/08/24）

3-N-1 の4件です。**答えは正しいのに、検証つきの口が断る**もので、4-51 で
2回、緩める方向に手を入れて2回とも `boolean_scale_and_empty_test` に
止められていました。

#### 断っていた仕組み

ゲートは 384 点の標本が A・B・結果の**3つのメッシュ**のどちらに入るかを見て、
演算の述語と合うかを数えます。A の球面と結果の球面は**同じ曲面の別々の弦**
なので、面から 0.0002 のところにいる標本は、形が正しくてもそれだけで割れます。

#### 見つけたもの — 判定ではなく、射影が面を捨てていた

食い違った標本だけ B-Rep の面へ厳密に射影して訊き直す、という方向で書き
はじめたところ、**読んだ球への射影が1点も返りませんでした**。調べると、
`project_onto_solid` が2種類の面を捨てていました。

**1つ目。トリムされていない面。** OpenCASCADE が書く球や円柱は全周1枚の面で
来ます。実測で `occ_reference_sphere.step` は **面1枚・外側ワイヤの稜 0本・
p-curve のセグメント 0本**でした。射影はこれを「外側ループが空 ⇒ 内側には
何も無い」と読むので、読んだ球は**面を1枚も持たない立体**として扱われます。
境界のワイヤを持たない面は、支持曲面のパラメータ領域そのものが面です。

**2つ目。足がトリムの外に落ちた面。** そこで面ごと捨てていたので、直方体の
角の外にいる点は6面すべてが「自分の長方形の外」と答え、立体への足を1つも
持ちませんでした。捨てるのではなく、**その面の境界の稜へ寄せる**のが正しい。

**この2つは、ゲートより先に `DistanceEngine` の答えを狂わせます。** 読んだ球や
円柱への最短距離は、面を1枚も見ずに出した値でした。干渉判定も同じ経路です。
**自前ビルダーの立体は必ず境界ワイヤを持つ**ので、既存の検体では1件も
捕まりませんでした（`distance_probe` の7件は全部自前の立体です）。

#### 入れたもの

射影が最近点だけでなく**そこでの外向き法線**も返すようにし（`BoundaryProjection`）、
面ごとの候補を全部返す `boundary_projections` を足しました。ゲートは、
食い違った標本だけをこれで訊き直します。稜や角では2枚以上の面が同着になるので、
**全部に訊いて、符号が割れたら決めません**。

**許す方向にしか使いません。** メッシュで合っていた標本はここへ来ないので、
新たに食い違いを作ることはありません。決められなかった標本は、メッシュの
判定（＝食い違い）をそのまま残します。**決められないことを理由に許すと、
ゲートが緩みます。**

#### 実測

`gate_membership_probe`（新設。既知の4件だけを数秒で測る）:

| | 前 | 後 |
| :--- | ---: | ---: |
| 断られた演算 | 4 / 6 | **0 / 6** |
| 食い違った標本（軸に平行、差） | 10 / 383 | **0 / 383** |
| 食い違った標本（傾け、差） | 5 / 384 | **0 / 384** |

`foreign_boolean_probe`:

| | 前 | 後 |
| :--- | ---: | ---: |
| 検証つきの口（軸に平行、90演算） | 88 / 90 | **90 / 90** |
| 検証つきの口（傾けを含む、180演算） | 176 / 180 | **180 / 180** |
| ok / NOCUT / 拒否 / WRONG / PANIC | 26,53 / 4,7 / 0 / 0 / 0 | 変化なし |
| 恒等式の残差 | 4.04e-9 / 7.88e-9 | 変化なし |

読んだ球（r10）への距離は、原点から 10.0、(20,0,0) から 10.0、(3,4,1) から
4.900980486407214。**それまでは3つとも「面が無い」で答えが出ませんでした。**

緩めていないことの確かめ:

- 516テスト（95バイナリ）すべて緑。`boolean_scale_and_empty_test` を含む
- `boolean_envelope` 45行の表が `main` と**一字も違わない**（体積・判定・
  ゲート通過・解析解との差・曲面評価回数まで一致）
- `distance_probe` 7/7 が閉じた式と厳密一致、刻みでも動かない
- `interference_depth_probe` 見落とし 0、報告距離のずれ 0
- `builder_audit` 24/24、`boolean_topology_probe` 緑
- FreeCAD 相互検証 15/15、ショーケース 24/24、書き戻し 7/7、IGES 5/5
- `tools/verify_solid_api.py` 全合格

#### ついでに測れたこと — 文書の数字が2つ古くなっていました

`boolean_envelope` を `main` でも走らせたところ、**`worst analytic agreement`
は 6.68e-8（sphere x sphere intersection）、`solve work` は 112,006,172**
でした。この文書の 3-1b に載っている **6.75e-8 と 176,674,143 は再現しません**。
今回の変更ではありません（ブーリアンのパイプラインは距離のモジュールを
一度も呼んでいませんし、`main` で測っても同じ値です）。3-1b が
「絶対値のほうは、その後どこかで変わりました」と書いていたドリフトが、
`worst analytic agreement` にも及んでいたということです。**39/45・誤答0 は
動いていません。**

---

### 4-69. 読んだ立体を「測る」経路に、プローブを当てた（2026/08/24）

4-68 で距離の射影が面を捨てていたのが出たので、**そこを面で押さえる物差し**を
作りました（`foreign_distance_probe`）。球 r10・円柱 r10 h40・円錐 r10 h20・
トーラス R12 r4 を読み、距離と内外を**閉じた式**と突き合わせます。

検体の置き方が式の前提と合っていることを、境界箱で**先に**確かめます。
ここが崩れたら式のほうが誤りなので、そのときは赤にします（5章の
「閉じた式を先に確かめる」）。

**書いたら2件出ました。** どちらも自前ビルダーの立体では踏まない形です。

#### 1. 回転面の極で、法線が取れない

極では片方の偏微分が消えるので `NurbsSurface3::normal` は `None` を返します。
点そのものについては正しいのですが、**面はそこで滑らか**です（球の極の
接平面は軸に直交する）。射影は法線が無い面を採らないので、**読んだ球の
中心線の上にある点は、1つも足を持ちませんでした**。距離も内外も出ません。

`ExtremumEngine` のほうは極を正しく見つけていました（足 (0,0,10)、距離
15.000000000）。**落ちていたのは、その後で法線を訊くところだけ**です。

`normal_or_limit` を足しました。`(u, v)` から領域の内側へ**斜めに4方向**
寄せ、取れた法線が互いに一致したときだけ返します。寄せる向きで法線が
変わる点（円錐の頂点）では一致しないので、そこは `None` のままです。
**滑らかでない点に法線を作りません。**

#### 2. 縁で2枚が同着すると、内外が決まらない

読んだ円錐の底の縁 (10,0,0) の斜め外にいる点 (20,0,5) は、底の平面から
見れば内、母線から見れば外です。4-68 で入れた「同着の面の符号が割れたら
決めない」が効いて、**境界から 11 も離れた点が決められませんでした**。

決められます。稜が最近点になるのは、**凸稜なら外側の点だけ、凹稜なら
内側の点だけ**です（凸稜の内向き法線錐と、凹稜の外向き法線錐は、どちらも
空）。したがって内と言えるのは、同着の面が**全部**内と言うときだけ。凹稜の
内側の点は、実際にどちらの面から見ても内側にあります。接平面の上に乗って
いる場合だけは、従来どおり決めません。

#### 実測

| | 前 | 後 |
| :--- | ---: | ---: |
| `foreign_distance_probe` | 球の軸上4点・円錐の縁1点が落ちる | **36 チェック、外れ 0** |
| 最悪誤差 | — | **3.55e-15** |

公開 API（`DistanceEngine::compute_min_distance`）も、読んだ立体と既知の
隙間に置いた小球で3件とも一致します。

動いていないこと: 518テスト（95バイナリ）すべて緑、`foreign_boolean_probe`
軸に平行 90/90・傾けを含む 180/180、`gate_membership_probe` 0 of 6 refused、
`distance_probe` 7/7 厳密、`interference_depth_probe` 見落とし 0。

#### この3件は、同じ場所から出ています

4-68 の2件（トリムされていない面、トリムの外に落ちた足）と、ここの2件は、
すべて**他カーネルから読んだ立体を測る経路**です。自前ビルダーの立体は
必ず境界ワイヤを持ち、極を最近点に持つ配置も検体に無かったので、既存の
ゲートは全部緑のままでした。**測っていない領域にプローブを当てる**のが、
やはりいちばん出ます。

---

### 4-70. 読んだ立体の断面に、物差しを当てた（2026/08/24）

断面のプローブ（`slice_probe` / `slice_robustness_probe`）は、**どれも自前
ビルダーの立体**を切っています。読んだ立体を切ったことは一度もありません
でした。`foreign_slice_probe` を書いたら、2件出ました。

#### 1. 読んだ円錐だけが、構造格子に乗れなかった

格子を張れるかの判定は、境界が4辺で「対辺の刻み数が一致」することを求めます。
**極を持つ面の境界は3辺です**——潰れた側（頂点）には稜がありません。円錐の
側面や、他カーネルから読んだ回転面がそれで、4辺の規則にも「全部同じ」にも
当てはまらず、毎回 earcut ＋ 適応細分へ落ちていました。

| 分割 | 前（読んだ円錐） | 後 | 自前の円錐 |
| ---: | ---: | ---: | ---: |
| 16 | 16,874 | **2,044** | 2,046 |
| 32 | 102,088 | **16,382** | 8,190 |
| 64 | 141,915 | **65,534** | 32,766 |

退化三角形も 54 → **0**。重いだけではありません。**粗密が偏るので、断面の
輪郭が壊れます。**

| 円錐 z=10 の断面 | 前 | 後 | 解析解 |
| :--- | ---: | ---: | ---: |
| 面積 | 77.255085 | **78.539816** | 78.539816 |
| 周長 | **447.277923** | **31.415927** | 31.415927 |

周長が**刻みを細かくするほど発散**していました（52 → 107 → 447）。

潰れた行/列が本当に1点に潰れていることは、既存の検査が曲面そのものを見て
確かめます。**通す条件を1つ増やしただけで、検査は緩めていません。**

これは 3-Z / 4-39 と同じ種類です。あちらは「支持曲面が面より広い」ために
落ちていた件で、こちらは「境界が3辺」。**格子から落ちる道はまだ他にもあり、
落ちたことは外から見えません**（`work_counter` の `grid_patches` /
`earcut_patches` で数えられます）。読んだ**円錐台**は、対辺の円の刻み数が
違う（下の円 64、上の円 128）ため、いまも earcut のままです。

#### 2. 断面の弦を詰める種が、古くなっていた

弦の中点を断面へ詰めるとき、前の弦の答えを種にします。輪郭を辿っている
あいだは近いのですが、継ぎ目をまたいだところで別の枝へ行きます。そこへ
落ちた点は残差が大きく、「探索の粗さを補正値として採用しない」判定に
引っかかって捨てられます。**捨てられた弦は生の弦のまま積まれる**ので、
刻みを細かくするほど本物の補正（弦のたわみ、$h^2$ で縮む）が小さくなり、
ある密度から補正がまるごと効かなくなります。

読んだトーラスの赤道断面（相対誤差）:

| 分割 | 前 | 後 |
| ---: | ---: | ---: |
| 128 | 4.83e-11 | 4.83e-11 |
| 256 | **2.11e-6** | **4.5e-11** |

2.11e-6 は、生の多角形の精度（$(\pi^2/3)/n^2$）そのものです。**細かくした
ほうが悪い**という形で出ていました。1度だけ種を捨てて引き直します。効くのは
捨てられた弦だけなので、通っている弦の費用は変わりません。

#### 直せなかったもの（既知の残差）

**円柱を軸を含む平面で切ると、周長が 120 に対して 120.52**（64分割）です。
確かめたこと:

- **面積は厳密**（1.4e-16）。ループ数も 1 で正しい。
- **メッシュは正しい。** 蓋を x = 0 で切った線分は12本で、[-10, 10] を
  ちょうど 20.000000000 で敷き詰め、重なりは無い。
- 余分はすべて y 方向（sum|dz| は 80 で厳密、sum|dy| が 40 に対して 40.52）。
- 輪郭の点列が、蓋の上で 10 → 9.990 → 9.970 → 9.927 → 9.830 → **10** → 0
  と往復している。往復なので**面積には出ません**。
- 余分は刻みを倍にするごとに半分になる（0.516 → 0.266 → 0.135）。

つまり欠陥はメッシュではなく、その後（線分の連結か積算）にあります。連結の
分岐を「入ってきた向きを継ぐ」ほうへ変えても数字は動かなかったので、
**そこではありませんでした。測って効かなかったので、その変更は戻して
あります**（5章）。切断平面がメッシュの頂点をちょうど通る配置——対称面で
切る、いちばん普通の置き方——で出ます。`foreign_slice_probe` に名前つきで
残してあり、面積とループ数は厳密に見ています。

---

### 4-71. 格子から落ちた面を数えるようにした（2026/08/24）

4-70 で分かったのは、**格子から落ちても外から見えない**ということでした。
メッシュは閉じるし、体積も正しい。既存のゲートは全部緑のまま、断面の輪郭
だけが壊れていました（周長が解析解 31.4 に対して 447）。

**見えないものは、数えるしかありません。** `grid_fallback_probe` を足しました。

#### 数え方

読める検体15件について、格子に乗ったパッチと earcut に落ちたパッチを数え、
退化三角形も数えます。**earcut に落ちること自体は欠陥ではありません**
——ブーリアンで割られた面や、6辺で切り取られた面は、そもそも格子を張れる形
ではないからです。0 を強制すると、直せないものを赤にし続けることになります。

検体ごとに「いくつまでは分かっている」と**その理由**を持たせ、超えたら赤に
します。**直したら許容を下げてください。**

#### いま許容している2件（どちらも理由が名指しできています）

| 検体 | earcut | 理由 |
| :--- | ---: | :--- |
| `cylinder_nurbs` | 2 | 蓋が円形トリムを持つ NURBS 面。パラメータ矩形の縁が境界ではない。**蓋を平面として持ち直せば**（`as_plane`）平面の経路に乗るはずで、そこは試していません |
| `sphere_capped` | 1 | 境界が6辺。箱で切られた球はパラメータ矩形ではないので、格子は原理的に張れません。ただし 32分割で 72,104 三角形あり、**重さのほうは別の問題**として残っています |

残る13件は 0 です。

#### 円錐台を格子に戻した

格子は「対辺の刻み数が一致」することを求めます。刻み数は稜ごとに**その稜
自身のたわみ**で決めるので、半径の違う2つの円は違う数になります。読んだ
円錐台がそれで、下の円 256・上の円 128 となって落ちていました
（`GRIDWHY segment_counts [128, 32, 256, 32]`）。

揃えるときは**多いほうへ**上げます。減らすと、その稜を使う別の面の弦が
粗くなるからです。刻み数は稜ごとに1つだけ持つので、上げた結果は両側の面が
そのまま見ます——**継ぎ目は開きません**。1回上げると別の面の対が崩れる
ことがあるので、変わらなくなるまで繰り返します。上げる方向にしか動かない
ので必ず止まります（`balance_opposite_edges`）。

| 32分割 | 前 | 後 |
| :--- | ---: | ---: |
| `cone`（円錐台）の三角形 | 100,286 | **16,892** |
| 格子に乗ったパッチ | 0 | **1** |

#### 次に触るときのために

**格子から落ちる道は、これで3本見つかりました。**

| 落ちる理由 | 状態 |
| :--- | :--- |
| 支持曲面が面より広い（ブーリアンの結果） | 4-39 で解消 |
| 境界が3辺（極を持つ面） | 4-70 で解消 |
| 対辺の刻み数が違う（半径の違う2つの円） | 4-71 で解消 |
| 境界がパラメータ矩形の縁でない（トリムされた面） | **原理的に張れない。** earcut が正しい |

`ZENITH_GRID_WHY=1` を付けると、落ちた理由が1行ずつ出ます
（`off_border` / `segment_counts` / `no_orientation_matched`）。**新しい検体を
足したら、まずこれを見てください。**

---

### 4-72. 読んだ立体を編集する経路に、物差しを当てた（2026/08/24）

フィレット・面取り・プッシュプル・面の計測の検査は、**すべて自前ビルダーの
立体**に対するものでした。読んだ立体に掛けたことは一度もありません。
`foreign_edit_probe` を書いたら、`inspect_face` に2件出ました。**面の計測は
公開 API** で、Python 側（`Solid.inspect_face`）からも呼ばれます。

#### 1. 面積が内接多角形だった

16分割のメッシュから取っていたので、円形の面では内接多角形の面積になります。

| 読んだ円柱の蓋 | 値 |
| :--- | ---: |
| 実装（前） | 314.151328 |
| 真値 | 314.159265 |
| 相対 | **2.5e-5** |

仕様書はここを「厳密表面積」と書いています。平面の面は Green の定理で
p-curve に沿った線積分に落ちるので**厳密に**出ます（円形のトリムは真の面積
になり、内接多角形にはなりません）。それは `MassCalculator::compute_face_integral`
が既に持っていました。**新しく書く必要はなく、繋げるだけでした。**
差し替えて 1.45e-14 になりました。

#### 2. 重心が、面の重心ではなかった

`compute_from_mesh` の `center_of_mass` は原点からの四面体分割による**体積**の
重心です。閉じた立体には正しいのですが、**開いた面1枚に掛けると意味を
持ちません**。

| 読んだ円柱の上蓋（z = 40） | 値 |
| :--- | ---: |
| 実装（前） | **(0, 0, 30)** |
| 真値 | (0, 0, 40) |

30 は 0.75 x 40 です。四面体の重心が原点から 3/4 の所に来るので、そうなります。

**下蓋は z = 0 なので偶然正しく見えていました。** 対称な位置に検体を置くと、
この種の誤りは見えません。5章の「通る値を1つ選ばない」と同じ話で、**通る
場所を1つ選んでも同じことが起きます**。面積で重み付けした三角形の重心に
直しました。

#### プッシュプルは、物差しのほうがずれていた

プッシュプル（平らな面を法線方向に d 動かす）の検査は「体積の変化 = 面積 x d」
です。面積を直す前は 2.5e-5 で「合っている」ように見えていました。**ずれて
いたのは操作ではなく物差し**（`inspect_face` の面積）のほうで、体積の変化は
最初から 500π ちょうどでした。直したあとは 1.4e-12 です。

#### 一緒に測れたこと — フィレットが読んだデータで使えない

`blendable_edges` は**読んだ検体14件のうち12件で 0 本**を返します。
`blend_edge` が「直線の稜 × 両側が平面」しか受け付けないためです
（`edge_blend.rs` の `BlendSite::locate` に明記されています）。

| 検体 | 丸められる稜 |
| :--- | ---: |
| `hollow_box` | 12 |
| `extruded_spline` | 4 |
| 残り12件（円柱・円錐・球・トーラス・段付き軸・面取り箱ほか） | **0** |

円柱の上下の縁、段付き軸の肩、面取り箱の稜は、どれも実務でフィレットを
掛ける対象です。**曲がった縁が主な読んだデータでは、事実上使えません。**
掛かった2件は閉じた式と 1e-12〜1e-15 で一致するので、**実装が誤っている
のではなく、守備範囲が狭い**ということです。

`KERNEL_SPECS.md` は「直線凸稜」と正確に書いていましたが、`ROADMAP.md` は
制限なしで「完了」としていたので、実測に合わせました。

**次に広げるなら、いちばん実務に近いのは「平面と円筒面のあいだの円い縁」**
です（軸の端を丸める、ボスの根元を丸める）。断面が円弧なので、フィレット面は
トーラスの一部になります。いまの `BlendSite` は両面が平面であることを前提に
後退距離を出しているので、そこから作り直しになります。

---

### 4-73. 詰めの限界を、長いほうの隣から取っていた（2026/08/24）

4-70 で名指しできなかった残り1件——**円柱を軸を含む平面で切ると、周長が
120 に対して 120.52**——の原因が分かりました。

#### 切り分けが先でした

「連結か積算のどちらか」までは 4-70 で分かっていました。**どちらかを決める
前に、生成側が正しいことを数字で切り離しました。** 切断器の線分生成を
忠実に再現して数えます。

| 測ったもの | 結果 |
| :--- | :--- |
| 線分の本数 | 154 |
| **線分の長さの合計** | **120.000000000** |
| 重複したキーの組 | 0 |
| `settled`（載せ直した点） | **2**（蓋ごとに1点） |

合計がちょうど 120 なので、**生成側は完璧**です。そして `settled` が
ぴったり 2——蓋ごとに1点だけ動いている。ここで犯人が決まりました。

**この切り分けをせずに連結器を直しにいって、1回外しています**（4-70 で
「入ってきた向きを継ぐ」に変えたが数字が動かず、測って効かなかったので
戻した）。**先に切り離していれば、その回り道は要りませんでした。**

#### 原因

`settle_loop_points` の移動限界を、隣り合う2辺の**長いほう**で決めていました。

```
let neighbourhood = (point - previous).norm().max((next - point).norm());
let limit = (neighbourhood * 0.25).max(tol.linear);
```

蓋を横切る弦の端（y = 9.582）は、片側が 0.2477、もう片側が 9.582（円板を
横切る長い弦）です。限界が 2.395 まで広がり、そこへ**円筒側面への射影**が
届いて、点が縁（y = 10）まで 0.418 引っぱられていました。

**同じ弦の上を滑るので、面積は変わりません。** 周長だけが伸びるので、
面積だけを見ていると気づけません。

詰めの補正は弦のたわみの大きさしか出ないはずで、たわみは**局所の弦の
細かさ**で決まります。短いほうで測るのが正しい。一様に刻まれた断面では
長短が同じなので、そこは何も変わりません。

#### 実測

| | 前 | 後 |
| :--- | ---: | ---: |
| 円柱の軸断面の周長（真値 120） | 120.516239 | **120.000000** |
| トーラス赤道面 256分割 | 4.83e-11 | **3.02e-12** |

`foreign_slice_probe` から**既知の残差の除外機構を丸ごと削りました**。
24チェックすべてを厳密に見て、外れ 0 です。

#### 読んだ立体の重心と慣性 — 欠陥はありませんでした

`inertia_probe` の検体は**すべて自前ビルダー**でした。読んだ立体で重心と
慣性を測ったことは一度もありません。`foreign_inertia_probe` を書いて、
球・円柱・円錐・トーラスの体積・重心・**原点まわりの慣性テンソル**を閉じた
式と突き合わせ、主慣性モーメントが回転で動かないことも見ました。

**32チェック、外れ 0、最悪 8.49e-11。** 今日 `inspect_face` が開いた面に
体積重心を返していたので同じ取り違えを疑いましたが、立体の側は健全でした。
**欠陥ゼロという結果も結果です。**

これで「読んだ立体を測る」主要経路——距離・内外・断面・編集・質量特性——が
全部物差しに載りました。

#### テストを速く回す道

`cargo test --release --workspace --exclude zenith_py` は約13分（実測）。
`cargo test` がテストバイナリを**1本ずつ順に**走らせるためで、重い数本の
合計がそのまま壁時計になります。

| バイナリ | 所要 |
| :--- | ---: |
| `tilted_cutter_test` | 145.7s |
| `boolean_crossed_cylinder_test` | 110.4s |
| `boolean_torus_box_test` | 82.4s |
| `boolean_torus_test` | 55.0s |
| `curved_face_chain_split_test` | 54.4s |
| `torus_half_slab_test` | 46.2s |
| `boolean_verification_test` | 44.1s |
| `countersink_range_test` | 39.1s |
| `plane_cone_section_test` | 30.1s |
| `modeling_test` | 25.1s |
| （残り85本は合計しても短い） |

`tools/fast_test.sh` はバイナリを並列に走らせます。**被覆は1つも減りません。**
部分集合を選ぶ速い段にしなかったのは、このリポジトリで見つかった欠陥が
**まさに測っていなかったところ**から出ているからです。落とすものを人が
覚えていなければならない仕組みは、ここでは向きません。

**測りました（2026/08/24）。20並列・ビルド済みから・他に何も走らせていない
機械で 13分36秒（816秒）。`cargo test` の約13分と、ほぼ同じです。**

**速くなっていません。** 数え方のところに理由があります——このスクリプトが
数える「162 バイナリ」には **examples（プローブ72本）が入っています**。
`cargo test` は examples もビルドするので、`--message-format=json` から
実行ファイルを拾うと一緒に来て、そのまま走ります。**被覆はそのぶん増えて
います**（プローブが非ゼロ終了すれば落ちます）が、時間もそこに行っています。

**816秒の内訳は測っていません。** プローブを外した場合にどこまで縮むかを
知りたい人は、`"kind":["example"]` を外して測ってください。

---

### 4-74. 6件のうち2件は面積 0 の面片、1件は答えのほうが非多様体でした（2026/08/24）

`boolean_envelope` の45ケースで通らない6件について、この文書は「機構は2つ、
そのうち3件は接触の交線が `leftover` に積まれるのが原因」と書いていました。
**その3件を測ったら、機構はさらに分かれ、原因も違いました。**

| | 前 | 後 |
| :--- | ---: | ---: |
| `boolean_envelope` supported | 39 | **41** |
| 同 エラー | 6 | **4** |
| 同 wrong-result | 0 | **0** |
| `contact_placement_probe` で立体が返る | 6 | **8** |
| 同 断られる | 6 | **4** |
| 同 **非多様体を立体として返した** | 0 | **0** |
| テスト | 518 | **524**（0 failed） |

#### `box × box` 45度回転 — 面積 0 の面片が、同じ稜をもう2回使っていた

非多様体の稜を座標つきで出したら、**同じ面片が同じ稜を2回使って**いました。

```text
NM (0.0000 14.1421 0.0000) -> (5.8579 20.0000 0.0000)  used by 4 pieces: A#0 B#6 B#7 B#7
```

面片を全部書き出すと、`B#7` は `B#6` の境界を**行って戻るだけ**のワイヤでした。

```text
#6  B Inside edges 4   (5.858 20 0) -> (5.858 20 20) -> (0 14.142 20) -> (0 14.142 0) -> 戻る
#7  B Inside edges 6   上と同じ3本を往復（面積 0）
```

相手の稜がこちらの**面の境界の上**に乗る配置では、その稜で面を割ると「面
そのもの」と「切れ目」の2枚が出ます。切れ目は面積 0 で、新しい境界を1本も
持ちませんが、**同じ稜をもう2回使います**。それが非多様体（差で 12、積で 22）
の正体でした。

**面積を囲まない面片は面ではありません。** 選別の出口で落とします
（`face_encloses_no_area`。外側のワイヤを折れ線にして Newell の式で取り、
`面積 <= 公差 × 周長` で見る）。落としても形は動きません。

差と積が**閉じた式と一致して**返るようになりました。重なりは、面 `x = 0`・
面 `y = 20`・B の 45 度の面が作る**直角二等辺三角形**（脚は 20 − 10√2）を
高さ 20 だけ伸ばしたもので、

```text
面積 = (20 - 10√2)^2 / 2 = 17.1573,  体積 = 343.1458
```

| 演算 | 実測 | 閉じた式 |
| :--- | ---: | ---: |
| union | 15656.854 | 16000 − 343.146 |
| difference | 7656.854 | 8000 − 343.146 |
| intersection | **343.146** | **343.146** |

**「接触の交線を `leftover` から落とす」は、この2件には要りませんでした。**
交線は正しく、割れた結果の面片のほうに 1 枚余計が混ざっていた、という話です。

#### `box × cylinder` 接線の差 — 答えのほうが非多様体でした

こちらは実装の不足ではありません。**半径6の円柱が箱の壁 x = 0 に内側から
接している**ので、差を取ると壁のところで材料の厚みが 0 になり、線でしか
繋がらない2つの塊に割れます。`Solid` は多様体 B-Rep なので持てません。

規約（3-1「接触は、それ自体では位相を作らない」）どおり、**場所を名指しして
断ります**。

```text
Exact B-Rep boolean refuses this placement: the true result is non-manifold at
(0.000000 10.000000 5.000000): the material there is only in contact, falling
into 2 pieces around the intersection (measured on rings of radius 2.000e-1 and
2.000e-2)
```

「未実装」と言うのとは意味が違います。**実装しても返せません。**

同じ配置でも和と積は繋がったままなので、そちらは返ります（実測: 積の体積
2261.947）。

#### 測り方を1回書き直しています。**最初の測り方は、3件すべてを取り逃しました**

最初はこう書きました——交線のまわりに半径 r の円を取り、64点撒いて、答えの
内側の点が**いくつの弧に分かれるか**を数える。

**測ったら、接触している3件すべてで「弧は1つ」と答えました。** 理由は形の
ほうにありました。円柱が壁に接している配置で差の材料が残るのは

```text
0 < cos(theta) <= r / (2R)
```

の帯だけで、r = 0.2、R = 6 なら幅は **0.95 度**。64点の刻みは 5.6 度なので
**またいで通り過ぎます**。半径を小さくしても帯は比例して細くなるので、
刻みを増やす以外に逃げ道がありません。

いまは **A と B の内側の角度の区間を別々に求めてから、区間どうしで演算します**。
細い帯は「幅 90 度」から「幅 89.04 度」を引いた差として**そのまま出てきます**。
撒いた点で細部を捉える必要がありません（`crate::contact`）。

**「円周に点を撒いて数える」は、いかにも測っている顔をした予想でした。**
撒いた点の刻みより細い構造は、点をいくら丁寧に数えても見えません。

#### 費用 — 数え上げで測りました（3-1b）

判定は**失敗経路でしか走りません**。同じ45ケースを、変更の前後で
`work_counter` の数え上げで比べます（壁時計はこの環境では使えません）。

| | 前（`311274d`） | 後 |
| :--- | ---: | ---: |
| 曲面の評価回数（45ケース合計） | 112,006,172 | **114,842,103**（+2.5%） |
| マーチングのニュートン反復 | 1,141,095 | **1,141,095**（±0） |
| 辿り開始回数 | 7,356 | **7,356**（±0） |

差は 2,835,931 回。**そのほぼ全部が、断る1件**（`box × cylinder` 接線の差）
です。同じ1件の後の値は 3,728,283 回で、通る union は 999,896 回のままです。
（前の値を1件だけ取り出しては測っていません。合計と、通る行が1行も動いて
いないことから言っています。）

**交線を求める仕事は1回も増えていません**（ニュートン反復も辿り開始も±0）。
増えたぶんは全部、**断る1件**で円周の内外を測る射影です。成功する経路の
仕事量が動いていないことが、**形を変えていない証拠**です。

粗い半径で 2 未満だったら細かい半径は測りません。この打ち切りを入れる前は
同じ1件で 6,250,767 回でした（**測るのは高くつきます**——標本1つが立体の
全面への射影1回です）。

#### 予想は、今日もまた外れました

| 予想 | 実測 |
| :--- | :--- |
| `box × box` の差と積は接触の交線を落とせば通る | 交線ではなく**面積 0 の面片**だった |
| `box × cylinder` 接線の差は「A そのもの」 | 円柱は箱の**中に入っている**。差は本当に非多様体 |
| `box × cylinder` 接線の積は「空」 | 体積 **2261.947** の立体が返る |
| 円周に64点撒けば非多様体が見える | **3件すべて取り逃した**（上） |

`contact_placement_probe` の `note` 列は、予想を書く欄でしたが**実測に
置き換えました**。外れた予想をそのまま残すと、次に読む人がそれを測定だと
思って使います（5章）。

#### 残る4件（この日の時点。**翌日に1件まで減りました。4-80**）

`box × sphere`（極を通る置き方）の3演算と、上の `box × cylinder` 接線の差です。
前者は 3-N-1 の機構B——切る平面が球の極を通り、交線がそこで点に潰れる——で、
**今日の修正では動きません**。後者は動かす対象ではありません（断るのが正解）。

**この読みも半分外れました**（4-78）。潰れていた交線は箱の上下面と球の極の
**点接触**のほうで、本命の交線は別にありました。いまは `box × sphere` の
3演算とも通り、残るのは `box × cylinder` 接線の差だけです。

---

### 4-75. ブーリアンの重さは、面積の検算でした（2026/08/24）

9-1 が「性能は調整で埋まる差ではない」と書いている件について、**どこに消えて
いるか**を数え上げで測りました。壁時計ではなく `work_counter` です（3-1b）。

交差した円柱1回（`boolean_work_probe`、和）の内訳:

| | 回数 |
| :--- | ---: |
| 曲面の評価 | **15,015,327** |
| うち 粗探索（射影の出発点さがし） | 1,283,508 |
| うち 交線を辿るニュートン | 223,272 |
| うち 射影のニュートンと減衰 | 約 400,637 |
| **説明のつかない残り** | **13,508,547（90%）** |

残りの正体は **面のパラメータ領域の三角形**です。同じ1回で

```text
59 uv triangulations producing 2,631,196 triangles (44,597 each)
47 face integrals
```

積分は三角形1枚につき6点を評価するので、2,631,196 × 6 ≒ 1,580万。**桁が
合います。** つまりブーリアンの仕事の大半は、交線を求めることでも、内外を
決めることでもなく、**面を割ったあとの「面積が合うか」の検算**です。

#### なぜ1枚が 4 万枚になるのか

`ZENITH_TRIM_WHY=1` を足して数えました。

```text
TRIMWHY boundary 4115 pts, earcut 4113 tris -> refined 132723 tris (x32.3)
TRIMWHY boundary 2854 pts, earcut 2852 tris -> refined  97666 tris (x34.2)
TRIMWHY boundary 1436 pts, earcut 1434 tris -> refined 160487 tris (x111.9)
TRIMWHY boundary 4104 pts, earcut 4102 tris ->   4652 tris (x1.1)
```

2段になっています。

1. **トリム境界が数千点の折れ線**で来る（p-curve が1次で、制御点がそのまま
   頂点。4-41 の経緯で、ここは落とせません——落とすと面積が欠けます）
2. earcut がそこから同じ数だけの**細長い三角形**を作り、それを曲面のたわみで
   細分するので、**23〜112倍**に膨らむ

上限（`MAX_REFINED_TRIANGLES` = 200,000）に迫っているものもあります。

#### 予想を2つ立てて、2つとも実測で否定しました

**予想1: 細分は表示のためのもので、面積の積分には要らない。**
細分の判定基準は弦のたわみ（表示の基準）です。求積はパラメータ三角形の上の
6点則で、B-spline のノット区間は別に処理してあるので、細分は無駄では——と
考えました。`ZENITH_NO_TRIM_REFINE=1` を付けて `face_split_probe` を測ります。

| 検体 | 細分あり（残差 / 解析解との差） | 細分なし |
| :--- | ---: | ---: |
| 平面 10x10 を対角に切る | 1.42e-16 / 0.00e0 | 1.42e-16 / 0.00e0 |
| **円柱の1/4を傾いた平面で切る** | 5.30e-14 / 8.34e-9 | **6.21e-5 / 6.93e-5** |
| **円柱の1/4を別向きに切る** | 6.31e-14 / 1.32e-8 | **4.75e-5 / 3.59e-5** |

曲面では**相対 6e-5 動きます**。面分割の関門は 1e-6 なので、**切り込みが
断られるようになりました**（「several cuts on one face」が 3片 → 1片）。
**細分は要ります。** 平面は動かないので、うっかり平面だけで測ると通ります。

**予想2: 同じ面を何度も積み直しているから重い。**
診断を出すと、同じ面 id が1回のブーリアンで2〜3回積まれていました。そこで
**まったく同じ面なら答えを返す**覚え書き（`Face` の等値比較。ハッシュでは
ないので取り違えなし）を入れて測りました。

| | 前 | 後 |
| :--- | ---: | ---: |
| 面の積分 | 48 | **47** |
| 曲面の評価 | 15,015,327 | 15,008,391 |

**48回のうち当たったのは1回**でした。同じ id でも中身が違う（p-curve を
持つ版と持たない版、割る前と割った片）ので、繰り返しではありません。
**1回1回が本当に重いだけです。** 覚え書きは効かないので入れていません。

#### だから、次に手を付けるならここです

減らすべきは**繰り返しの回数ではなく、1回の三角形の枚数**です。

- **earcut に渡す前の形**が悪い。数千点の境界から作った細長い三角形は、
  たわみの基準を満たすまで 30 倍以上に割れます。制約付き Delaunay や、
  境界に沿わせた格子から始めれば、同じたわみをずっと少ない枚数で満たせる
  はずです（**測っていません。ここは予想です**）
- 求積の基準を「弦のたわみ」から**積分そのものの収束**に変えられるなら、
  枚数はもっと落ちます。ただし予想1で見たとおり、**基準を緩めると答えが
  動きます**。緩める前に、動かないことを `face_split_probe` の4検体で
  確かめてください

**`ZENITH_TRIM_WHY=1` と `boolean_work_probe` がそのまま物差しになります。**

#### 他 AI に読ませた調査との突き合わせ

同じ日に、外部の AI にコードを読ませた速度調査を受け取りました
（`reference/external-analysis/boolean-performance-third-party-20260824.md`。
**`reference/` は `.gitignore` に入っています**——外部資料をこのリポジトリの
実測と混ぜないためです。手元に無ければ、無いままで困りません）。

そちらが主犯に挙げるのは、内外判定の Raycast・境界箱のためのテッセレーション・
`Sewer` の O(E^2) です。**このケースの数え上げでは、いずれも主犯では
ありませんでした**——立体まるごとのテッセレーションは1回のブーリアンで
**2回**しか走らず、面の積分 48 回とは桁が違います。

**指摘が誤りだという話ではありません。** どれも「あれば効く」種類の改善で、
実際にそういう構造になっています。**効く順は測らないと決まらない**、という
だけです。この文書の主張と同じことが、外から来た主張にもそのまま当てはまり
ます。

その調査のうち「面積検証の段階化」は方向として当たっていましたが、**緩めると
答えが動きます**（上の予想1）。まだ測っていない指摘（`Sewer` の稜照合、
パイプラインの clone、公差係数のハードコード、判定用と表示用のメッシュを
分ける）は残っています——**測る前に採否を決めないでください。**

---

### 4-76. 面積の検算をパラメータ面積に置き換えられるか、測りました（2026/08/24）

4-75 で「ブーリアンの仕事の 90% は面を割ったあとの面積の検算」まで分かりました。
**その検算に 3D の面積が要るのか**を測ります。

割る前と割ったあとで見ているのは**同じ曲面の同じパラメータ領域**です。領域を
取り違えたなら、パラメータ面積の和も合いません。しかもパラメータ面積は境界の
折れ線の多角形面積そのもので、**曲面を1回も評価しません**。

`zenith_tess::face_parameter_area` を足し、`ZENITH_AREA_CHECK_WHY=1` で
**3D 側とパラメータ側を1行ずつ並べて**出せるようにしました。

```text
AREACHECK split_by_chain  pieces 2  3d  5.301e-14 pass  uv 2.220e-16 smallest 4.999e-1 pass  same
```

#### 揃うところは、パラメータ側のほうが 3〜4 桁鋭い

`face_split_probe` の10件は**全部同じ判定**で、残差の桁はこうです。

| 検体 | 3D | パラメータ |
| :--- | ---: | ---: |
| 平面を対角に切る | 1.42e-16 | **0.000e0** |
| 円柱の1/4を傾いた平面で切る | 5.30e-14 | **2.22e-16** |
| 円柱の1/4を2本で3片に | 1.46e-13 | **2.00e-15** |

3D 側の残差は三角形の細かさの誤差で、パラメータ側にはそれがありません。

#### **ただし、入れ替えられません。まだ同じ判定になりません**

ブーリアンを含む9本のプローブで 138 回ぶん並べたところ、**判定が食い違うものが
残ります**。中身は2種類でした。

**(1) 3D が断り、パラメータが通す（16件）。** 中身を出すと、こうでした。

```text
original  3d 157.0796  uv 0.3333
pieces    3d [157.0796, 471.2389]  uv [0.3333, 1.4e-17]
```

**片方の片は、パラメータ面積が 0——割れていません。** ところが 3D 側はそれを
471.24（元の3倍）と積んでいます。潰れた片はトリムが読めないので、
`face_uv_triangulation` が**曲面のパラメータ矩形まるごと**に落ちるためです
（穴の壁の残り 3/4 がそのまま出ている）。

つまり 3D 側の「残差 3.000」は、**測っているつもりのものを測った値では
ありません**。落ちた先の面積がたまたま大きかっただけです。捕まえたい事実は
「潰れた片が出た」で、それはパラメータ面積を見れば**直接**分かります。

**(2) 逆向きが 22件。** 「潰れた片があれば断る」を足して測り直すと、今度は
**3D が通してパラメータが断る**ものが出ました。`split_by_interior_loop` の片で
`face_parameter_area` が 0 を返しています。3D 側は残差 0.000 で綺麗に合って
いるので、**こちらはパラメータ側の取りこぼし**です（穴のループを外周に持つ片で
p-curve をどう導くか、まだ合っていません）。

#### この時点では入れ替えていませんでした（**翌日に入れ替えました。4-77**）

条件を3つ挙げて止めていました。

1. `face_parameter_area` を、穴のループを外周に持つ片でも正しく返るようにする
2. `ZENITH_AREA_CHECK_WHY=1` を全プローブに通し、**食い違い 0** にする
3. そのうえで `FaceSplitReport` の判定をパラメータ側へ移す。3D の面積は
   `face_split_probe` が閉じた式と突き合わせるのに要るので、**返した片から
   呼ぶ側が積む**形にする（判定からは外す）

**2 の結論は、条件そのものが少し違いました。** 食い違いを追い切ったら、
残っていたのは実装の穴ではなく**方針の穴**でした——潰れた片をどう扱うかを、
3D 側は決めていなかったのです（偶然で断ったり通したりしていた）。決めたら
揃いました。詳しくは 4-77。

**この節に「見込み」と書いた数字は、実測では 1350万回のうち 1000万回でした。**
見込みを書くのは構いませんが、**あとで実測に置き換わったかを必ず確かめて
ください**（5章）。

#### 参考にしたもの

`reference/OCCT`（追跡外）に OpenCASCADE の一式があります。トリム面の
三角形分割は `BRepMesh_Delaun`（制約付き Delaunay）＋たわみの制御という
作りで、**earcut ＋ 最長辺二分ではありません**。4-75 で「earcut に渡す前の形が
悪いのでは」と書いた予想と同じ方向です。**ただし LGPL なので、読んで真似る
のは筋が悪い**——考え方は教科書どおりのものなので、必要なら自前で書いて
ください。

---

### 4-77. 面積の検算をパラメータ面積に入れ替えました。45ケースの仕事が半分以下に（2026/08/25）

4-76 で「入れ替えたいが、まだ判定が食い違う」と書いた件を片づけました。**条件
3つを満たしたので入れ替えてあります。**

| 数え上げ | 前 | 後 |
| :--- | ---: | ---: |
| `boolean_work_probe`（交差した円柱・和）の曲面評価 | 15,015,327 | **5,018,135（3.0倍軽い）** |
| 同 面の積分 | 48 | **24** |
| 同 uv 三角形分割 | 60 | **36** |
| 同 三角形の総数 | 2,632,348 | **1,251,834** |
| **`boolean_envelope` 45ケースの曲面評価** | **114,842,103** | **52,729,734（2.18倍軽い）** |
| 同 マーチングのニュートン反復 | 1,141,095 | **1,141,095（±0）** |
| 同 辿り開始回数 | 7,356 | **7,356（±0）** |

**交線を求める仕事は1回も動いていません。** 減ったのは検算だけです。

#### 答えは動いていません

| 物差し | 前 | 後 |
| :--- | :--- | :--- |
| テスト | 524 / 0 failed | **524 / 0 failed** |
| 常設プローブ34本 | fail 0 | **fail 0** |
| `boolean_envelope` | 41 / wrong 0 / error 4 | **41 / wrong 0 / error 4** |
| 同 解析解との最悪一致 | 6.68e-8 | **6.68e-8** |
| `foreign_boolean_probe` | refused 0、90/90、残差 4.04e-9 | **同じ** |
| 傾けた切り手 | refused 0、180/180 | **同じ** |
| コンパイラ警告 | 0 | **0** |

`face_split_probe` の残差は**鋭くなりました**（3D の三角形の細かさの誤差が
無くなるため）。解析解との一致は変わりません——**そちらは 3D の面積のまま**
測っているからです。

| 検体 | 残差（前・3D） | 残差（後・パラメータ） | 解析解との差（前後とも） |
| :--- | ---: | ---: | ---: |
| 平面を対角に切る | 1.42e-16 | **0.00e0** | 0.00e0 |
| 円柱の1/4を傾いた平面で切る | 5.30e-14 | **2.22e-16** | 8.34e-9 |
| 円柱の1/4を2本で3片に | 1.46e-13 | **2.00e-15** | — |

#### 何を変えたか

1. **判定はパラメータ面積で行う。** `FaceSplitReport` / `MultiSplitReport` の
   面積の欄は `original_parameter_area` / `piece_parameter_areas` に変えました。
   **名前を変えたのはわざとです**——同じ名前のまま中身を 3D からパラメータへ
   すり替えると、閉じた式と突き合わせている側が黙って壊れます（5章）。
2. **潰れた片は、割れなかったこととして断る**（`null_piece`）。4-76 で見た
   とおり、これまでは 3D 側が**偶然**捕まえていました（潰れた片はトリムが
   読めず、曲面のパラメータ矩形まるごとを積んで残差が 3.000 になる）。落ちた
   先が小さいときは素通りしていたので、**判定になっていませんでした。**
   いまは大きさを直接見ます。
3. **検算できない面（p-curve が取れない）は断る。** 黙って通すより良い。
4. **3D の面積が要る側は、返った片から自分で積む。** `face_split_probe`、
   `ssi_face_split_test`、`face_split_interior_loop_test`、モジュール内の
   テストがそうしています。**判定に使わないものを検査では使う**ので、同じ量を
   両側から見ることになり、むしろ強くなりました。

`ZENITH_AREA_CHECK_WHY=1` は残してあります。**入れ替えたあとも、両方を1行ずつ
並べて出せます**（3D 側は重いので既定では走りません）。

#### ついでに1つ直しました（ただし、この検体では効きません）

`nest_cavity_shells_into_solids` が、シェルを体積の順に並べるのに
**比較関数の中で立体を丸ごと積分**していました。`sort_by` は比較のたびに
呼ぶので、O(n log n) 回積み直していたことになります。先に1回ずつ測ってから
並べるように直しました（順序は同じ）。

**効果は測っていません**——45ケースにも `boolean_work_probe` にも、シェルが
3枚以上出る配置が無く、数え上げが1回も動かなかったからです。**動かないことは
確かめました。**

#### 残っている仕事

まだ 500万回のうち 350万回が面の積分です（24回、三角形 125万枚）。行き先は
`MassCalculator::compute_from_brep`——ブーリアンの中で立体の体積を測る経路と、
返す前の検証です。**次に削るならそこですが、そちらは「答えそのものの検算」**
なので、面分割の検算とは意味が違います。安くする前に、**何を守っているかを
先に読んでください**（`boolean_validation.rs`、5章）。

トリム面1枚が三角形 3万〜16万枚になること自体は変わっていません（4-75）。
**面積を積む回数が半分になっただけです。** 枚数そのものを減らすなら、earcut
＋最長辺二分ではなく、境界に沿った三角形分割から始める必要があります。

---

### 4-78. 「極で交線が潰れる」は、4つの欠陥が縦に並んだものでした（2026/08/25）

3-N-1 は長らく「切る平面が球の極を通ると、交線がそこで点に潰れる」と1行で
書いてありました。**追いかけたら、独立した欠陥が4つ縦に並んでいました。**
2つ直り、2つ残っています。

まず、この配置で何が起きているかを測ります（`box × sphere`、球の中心
`(20,10,10)`、箱 20³）。

```text
A faces 6, B faces 8
face-pair candidates: 22
  A0 x B0  kind Curve{...}      <- 箱の底 z=0 と球の極の点接触
  ...
  A5 x B0..B7  kind Unsupported <- **x=20 の面。ここが本命なのに全部 Unsupported**
edge candidates: 6
  #0 A0 B0 (20.000 10.000 -0.000) -> (20.000 10.000 -0.000) len 0.0000
  ...
```

**交線は6本とも極の上の1点に潰れた「点」で、本物の交線は1本もありません。**
潰れているのは箱の上下面と球の極の**点接触**のほうで、本命——球が箱の面
`x = 20` を横切る大円——は**そもそも見つかっていません**でした。

#### 欠陥1: 切る平面が球の継ぎ目に重なると、交線が1本も取れない（**残っています**）

`ZENITH_SSI_WHY=1` を足して数えました。この置き方では **16組すべてで
「0 branch(es) marched」**。種が見つからないのか、辿り出してすぐ外へ出るのかは
まだ分けていません。

球を**自分の軸まわりに 45 度回す**と（極の位置も、接している面の数も、切る
平面も変わりません。変わるのは継ぎ目の向きだけ）、交線が出ます。

```text
kept: (20.000 10.000 0.000) -> (20.000 20.000 10.000), 59 marched points
kept: (20.000 20.000 10.000) -> (20.000 10.000 20.000), 59 marched points
kept: (20.000 10.000 20.000) -> (20.000 0.000 10.000), 59 marched points
kept: (20.000 0.000 10.000) -> (20.000 10.000 0.000), 59 marched points
```

大円が4本の弧として、ちゃんと出ています。**つまり「極だから取れない」のでは
なく、「継ぎ目に重なると取れない」。**

#### 欠陥2: 曲面に届いた交線が1本だけだと、汎用の面分割器を一度も試さない（**直しました**）

回した配置でも答えは返りませんでした。分解すると、箱の面は割れているのに
**球のパッチが1枚も割れていません**（unmatched 12）。

球のパッチ1枚に届く弧は**1本**です。ところが汎用の面分割器へ回す受け皿が
`split_edges.len() >= 2` と `chain.len() >= 2` を条件にしていたので、**1本の
ときは専用経路が断った時点で終わり**でした。専用経路は「円柱の側面と分かる
パッチしか割れない」と言います。

受け皿は「どの経路も入らなかったとき」にだけ走るので、1本を通しても既に
通っている割り方を横取りしません。条件を外しました。

#### 欠陥3: 着地は認めるのに、閉じているかは別の物差しで測っていた（**直しました**）

それでも割れませんでした。汎用の面分割器に直接訊くと、理由が出ます。

```text
piece 0 came out with an open wire; after edge 2 of 3:
  1.669e-5 between (20.0000,10.0000,0.0000) and (20.0000,10.0000,0.0000)
```

**同じ座標に見える2点が 1.669e-5 離れています。** 極です。極ではパラメータが
潰れるので、辿った交線の端はそこへぴったりは着きません。

問題は、その 1.669e-5 の扱いが**2箇所で違っていた**ことです。

| 見るところ | 物差し | 20 幅の面での値 |
| :--- | :--- | ---: |
| 切り込みの端が境界に着いているか | `tol.linear * 10 * 大きさ` | 2e-4 |
| 組み上げたワイヤが閉じているか | `tol.linear` | 1e-6 |

**着地は認めたのに、閉じていないと断る。** 1.669e-5 はちょうどその間に
入ります。

直し方は「閉性の物差しを緩める」ではありません。**切り込みの端を、着地点に
ぴったり合わせます**（`snapped_onto_boundary`）。クランプされた B-spline は
端の制御点がそのまま端点なので、途中を動かさずに端だけ合わせられます。
着地点は境界の上の点で、**真の交線もそこを通る**ので、これは近似を真値へ
寄せる向きの修正です。ずれが `limit` を超えていたら、そこは今までどおり
断ります。

これで球のパッチが**割れるようになりました**（片が2枚、残差 1.776e-14）。

#### 欠陥4: 極を通る境界を持つ片は、p-curve が導けない（**残っています**）

割れた4枚のうち、**3枚は面積の検算ができません**。

```text
B4 arc (20.00 -0.00 10.00) -> (20.00 10.00 0.00): ok, 2 pieces, residual 1.776e-14
B0 arc (20.00 10.00 0.00) -> (20.00 20.00 10.00): ok, 2 pieces, residual 1.000e0
```

残差 **1.000** は「2枚の片が、どちらも元の面と同じ面積」という意味です。
片の p-curve が導けないので、面積を積むほうが**トリムを見ずに曲面の
パラメータ矩形まるごと**を積んでいます（4-76 で見たのと同じ落ち方）。
パラメータ面積のほうも、同じ理由で測れません。

**測れないなら断る、が今の答えです**（検算できない割り方は通しません）。
ここを直すには、**極を通る境界の p-curve を導けるようにする**必要があります。
極では u が決まらないので、隣の標本から連続になるように選ぶ、という
処理が要ります。継ぎ目については既にそれをやっています
（`settle_seam_segments`）。**極についてはまだです。**

#### いまの状態

| | 状態 |
| :--- | :--- |
| 欠陥1（継ぎ目に重なると交線が取れない） | **残** |
| 欠陥2（1本の交線で汎用分割器を試さない） | **直** |
| 欠陥3（着地と閉性で物差しが違う） | **直** |
| 欠陥4（極を通る境界の p-curve） | **残** |
| `boolean_envelope` | 41 / wrong 0 / error 4（**変わらず**） |

**45ケースの表は動いていません。** 欠陥1と4が残っているので、`box × sphere`
の3件は通りません。**それでも2と3は独立した欠陥で、他の形にも効きます**
（曲面に1本だけ交線が届く配置、端が極や継ぎ目の近くに着く配置）。

仕事量は 52,729,734 → **53,059,404 回**（+0.6%）。受け皿が1本の交線でも走る
ようになったぶんです。

#### 次に手を付けるなら

**欠陥4のほうが先です。** 直せば「割れたが検算できない」が消え、欠陥1だけに
なります。欠陥1は種の探索の話で、`ZENITH_SSI_WHY=1` が「0 branch」と言って
いるところから始めてください。

再現はこれだけです。

```rust
let boxa = PrimitiveBuilder::make_box(20.0, 20.0, 20.0)?;      // 箱
let sphere = translate(make_sphere(10.0)?, (20.0, 10.0, 10.0)); // 球（継ぎ目が x=20 に乗る）
// 45 度回すと欠陥1だけ消え、欠陥4が出ます。
```

---

### 4-79. 面の上の点を射影したら、0.446 離れた極が返っていました（2026/08/25）

4-78 の欠陥4——「極を通る境界を持つ片は p-curve が導けない」——を追いました。
**p-curve の導出の問題ではありませんでした。その下の射影が壊れていました。**

#### 測ったもの

半径10の球を `(20,10,10)` に置き、**その球面の上の点**を自分の面へ射影します。
面の上の点なので、正しい距離は 0 です。

| 置き方 | 射影が言う距離 | 閉じた式（`|p−中心|−r`） |
| :--- | ---: | ---: |
| 球を**原点**に置く | 3.838e-15 | 3.553e-15 |
| 球を **(20,10,10)** に置く | **4.459e-1** | 5.329e-15 |
| 球を (1000,0,0) に置く | 2.340e-13 | 1.510e-13 |

**形は正しく、射影が外していました。** しかも**置き方で答えが変わります**。
原点で測っている限り、この欠陥は一生出てきません。

返っていたのは**極そのもの**でした（点は極から 0.4459 のところにあった）。

#### なぜ極から降りられないのか

`point_to_surface` は、粗い格子で当たりを見つけ、その周りを8回詰めてから
ニュートンに渡します。**当たりが極だと、この詰めが効きません。**

- 極では u をいくら動かしても 3D の点が変わらない（だから u を選び直せない）
- v を1目盛動かすと、**当たったときの u の経線**に沿って動く。その経線が
  見当違いなら、動かすほど遠くなる
- 8回とも改善が無く、**極が答えとして残る**

ニュートン側には「粗サンプリングの当たりより悪い位置は返さない」という
守りが入っていました（4-33 の頃）。**守りは効いていて、当たりが悪かった**
わけです。

#### 直し方 — ここでも1回外しました

**最初の実装**: 極に当たったら、隣の行で u を全域から選び直し、**そちらが
近ければ**種を差し替える。

**書いた回帰テストが、その場で反例を出しました。** 極のすぐ隣（v が 1% の
ところ）にある点では、**極のほうが隣の行より近い**のです（極から 0.14、
隣の行は 0.33）。近さで選ぶと種は差し替わらず、また極が残ります。

```text
centre [0.0, 0.0, 0.0]: a point on the surface projected 1.418e-1 away
```

**近さは、種を選ぶ基準になりません。** 種が決めているのは「どの経線を
降りるか」だけで、近いかどうかは降りた先で分かります。

**いまの実装**: 極に当たったら、**格子の当たりと、隣の行で選び直した経線の
両方から降ろして、着いた先で近いほうを取る**。本物の最近点なら、どちらから
降りても同じところへ着きます。

#### 実測

| | 前 | 後 |
| :--- | ---: | ---: |
| 面の上の点の射影（球を (20,10,10) に置く） | **4.459e-1** | **7.589e-15** |
| `boolean_envelope` | 41 / wrong 0 / error 4 | **同じ** |
| 同 解析解との最悪一致 | 6.68e-8 | **6.68e-8** |
| 同 曲面評価 | 53,059,404 | **53,213,511（+0.3%）** |
| テスト | 524 | **526**（0 failed。極の回帰テスト2本） |
| 常設プローブ34本 | fail 0 | **fail 0** |
| `foreign_distance_probe` | 36チェック・外れ0・最悪 3.55e-15 | **同じ** |

増えたぶんは、退化しているかを見るための微分1回ぶんです。**退化していない
面では、そこで終わります。**

#### これで直らないもの

**`box × sphere` の3件は、まだ通りません。** 4-78 の欠陥1（切る平面が球の
継ぎ目に重なると交線が1本も取れない）が残っているためです。

**この射影は、p-curve の導出・距離・内外判定・面積の検算が全部使います。**
4-69（極で法線が取れず足を落としていた）、4-71（極を持つ面が構造格子から
落ちる）に続いて3件目の「極」です。9-2 の見立て——**この family は「球を球
として持っていれば存在しなかった」**——を、また1件ぶん厚くしました。

#### 測り方についての教訓

**原点に置いて測ると通ります。** このリポジトリの検体は原点まわりに置いた
ものが多く、そこがこの欠陥を隠していました。回帰テストは**3つの置き方**
（原点、`(20,10,10)`、`(-7.5,3.25,100)`）で測っています。

---

### 4-80. 交線は辿るまでもなく、相手が稜として持っていました（2026/08/25）

4-78 の欠陥1——**切る平面が球の継ぎ目に重なると、交線が1本も取れない**——を
片づけました。`boolean_envelope` は **supported 41 → 44、エラー 4 → 1** に
なりました。

**残る1件は、直す対象ではありません**（`box × cylinder` 接線の差。答えのほうが
非多様体なので、場所を名指しして断ります。4-74）。**45ケースのうち、直す
べきものは 0 件になりました。**

#### 見つけ方

「継ぎ目に重なると取れない」まで分かっていたので、**継ぎ目そのものを測り
ました**。

```text
face 0 edge 13 lies in x=20: (20.000 10.000 0.000)->(20.000 20.000 10.000) length 15.7051
face 1 edge 17 lies in x=20: (20.000 20.000 10.000)->(20.000 10.000 20.000) length 15.7051
...
8 of the sphere's own edges lie exactly in the cutting plane x=20
```

長さ 15.7051 は πr/2——**大円の4分の1**です。つまり切り口の大円は、球が
**既に稜として持っているもの**でした。どのパッチも平面を内部で横切って
いないので辿る側は何も見つけませんが、**辿る必要がそもそもありません。**

手で候補に足して、残りの経路が通るかを先に確かめました。

```text
candidates from marching: 6
added 8 seam edges the sphere already had
union         pieces 13, unmatched 0, non-manifold 0, closed manifold true
difference    pieces 13, unmatched 0, non-manifold 0, closed manifold true
intersection  pieces  5, unmatched 0, non-manifold 0, closed manifold true
```

**3演算とも閉じた多様体。** ここまで確かめてから実装しました。

#### 何を「またいでいる」と呼ぶか

稜が平面の上に乗っているだけでは足りません。**その稜を共有する相手の2枚が、
平面の反対側にある**ことを見ます（`collect_edges_already_on_a_plane`）。

| 配置 | 判定 |
| :--- | :--- |
| 球の経線（隣り合うパッチが経度 0..90 と 90..180） | **またいでいる**。交線として拾う |
| 箱どうしを面で合わせた配置（合わせた面は平面の上） | またいでいない。拾わない（同一平面の重なりは別経路） |

これを見ないと、面を突き合わせただけの配置に交線を作ってしまいます。

**`id` では突き合わせられませんでした。** 同じ弧を共有していても、面ごとに
別の `Edge` の実体を持っています（実測: 球の経線は隣り合うパッチで id 13 と
21。座標は同じ）。位置で見ます。

#### 続けて出た欠陥: 点で触れているところに切れ目が入っていた

交線が拾えるようになったら、今度は**返した立体のメッシュに穴が開きました**。
`contact_placement_probe` が赤にしました（このゲートの唯一の赤条件です）。

中身を出すと、対になっていない稜が2本と、**長さ 0 の稜**が1本。

```text
used 1x: (20.0000 10.0000 20.0000) -> (20.0000 10.0000 20.0000)
used 1x: (20.0000 0.0000 20.0000) -> (20.0000 10.0000 20.0000)
used 1x: (20.0000 0.0000 20.0000) -> (20.0000 10.0000 20.0000)
```

`(20,10,20)` は球の極で、箱の天面 `z = 20` が**そこで球に点で触れています**。
その点接触が長さ 0 の「交線」として立ち、箱の天面に**行って戻るだけの
切れ目**を入れていました。

**3-1 の規約どおりです——接触は、それ自体では位相を作りません。**
1点に潰れた交線を候補から落としました。メッシュは閉じ、ゲートは緑に
戻りました。

#### 実測

| | 前 | 後 |
| :--- | ---: | ---: |
| `boolean_envelope` supported | 41 | **44** |
| 同 エラー | 4 | **1**（正しく断っている1件だけ） |
| 同 wrong-result | 0 | **0** |
| 同 解析解との最悪一致 | 6.68e-8 | **6.68e-8** |
| 同 曲面評価 | 53,213,511 | **53,204,877** |
| `contact_placement_probe` 立体が返る / 断る / **非多様体を返した** | 8 / 4 / 0 | **11 / 1 / 0** |
| テスト | 526 | **527**（0 failed） |
| 常設プローブ34本 | fail 0 | **fail 0** |

`box × sphere` の体積は閉じた式と一致します（半球 (2/3)πr³）。

| 演算 | 実測 | 閉じた式 | 相対 |
| :--- | ---: | ---: | ---: |
| union | 10094.3951 | 10094.3951 | 1.44e-15 |
| difference | 5905.6049 | 5905.6049 | 2.51e-14 |
| intersection | 2094.3951 | 2094.3951 | 3.56e-14 |

#### 古くなった不変条件を1つ書き直しました

`modeling_test` が **「交線の数 ≤ 面の組の数」**を見ていました。交線が面の組
ごとのマーチングからしか出てこなかった頃は成り立ちます。いまは組の数と
無関係に増えるので、外しました。

**外す前に、増えた分が本物かを測りました。** 箱と円柱の検体で、組 12 に対し
交線 14。増えた2本は**円柱の継ぎ目が箱の面 `y = 0` の上に乗っている**もので、
両側にパッチがあります——本物の交線です。

#### まだ通らない配置（新しく分かったもの）

**同じ球を自分の軸まわりに 45 度回すと、まだ通りません。** 継ぎ目が平面から
外れるので交線は4本の弧として辿れ、球のパッチも割れます（8 applied）。
それでも**縫合で 16 本の稜が相手を見つけられません**。

```rust
let spin = Transform3::from_axis_angle(&Vec3::z(), 45f64.to_radians());
let sphere = translate(transform(make_sphere(10.0)?, &spin), (20.0, 10.0, 10.0));
// intersection は半球 (2/3)πr³ = 2094.3951 になるはず
```

**45ケースの表には出てきません**（表は継ぎ目に重なる置き方のほうを持って
います）。**測っていない置き方に、まだ欠陥があるということです**——今日
4回続けて同じことが起きました。

---

### 4-81. 弧が極に届かないのは、接する根を解いていたからでした（2026/08/25）

3-N-2——**球を45度回して切ると、縫合で16本の稜が相手を見つけられない**——の
原因を、数字で最後まで追いました。**直しは入れていません。** ここまで分かった
ことを置いておきます。

#### 追った順

**候補が絞れているときこそ、片方を潰す測定を先に書く**（5章）。今回はそれを
4段構えでやりました。

| 測ったもの | 結果 | 分かったこと |
| :--- | :--- | :--- |
| 1. 余った16本の正体 | すべて切断平面 x=20 の上。**長さ 1.67e-5 の稜**と、そこを端点に持つ**4本の弧の重複** | 極のまわりの話だ |
| 2. 入力の球の極 | `(20,10,20)` ちょうど、単一頂点・8回使用、長さ 0 の稜 0 本 | **入力は無罪。ブーリアンが作っている** |
| 3. 交線4本 | 球面から 1.4e-11、平面からは厳密。**端点だけが極から 2.36e-5** | 弧は正しい。端点だけ |
| 4. 辿った点 vs 当てはめた曲線 | **一致**（fit と marched が同じ） | 当てはめではなく、辿りが届いていない |

#### 止まった理由

`ZENITH_SSI_WHY=1` に、辿り終わりのパラメータを出させました。

```text
uv1 (0.499998820,1.000000000) -> (0.000000000,0.500000000)  boundary true
uv2 (0.000001669,0.500000000) -> (1.000000000,0.500000000)  boundary true
```

**`uv1` は箱の平面、`uv2` が球です**（`s1` = surface_a = 箱）。読むと、

- 箱の平面は **v = 0 ちょうど**——箱の面の下辺 `z = 0` の縁に、正しく着地している
- 球は **u = 0.999998331**——極（u = 1）まで **1.67e-6**

#### なぜ 2.36e-5 なのか（数字が合います）

**極 `(20,10,0)` は、球が箱の面の下辺 `z = 0` に接する点です。** 交線
（x = 20 の大円）は `z = 0` を**横切らず、接して**います。

接する根では、残差を $\varepsilon$ まで詰めても横方向の精度は
$\sqrt{2 r \varepsilon}$ しか出ません。実測の残差は球面から **1.393e-11**
なので、

$$\sqrt{2 \times 10 \times 1.393\times10^{-11}} = 2.4\times10^{-5}$$

**実測の 2.36e-5 と一致します。** ニュートンが失敗しているのではなく、
**接する根の条件数そのもの**です。**残差をいくら詰めても、横は平方根でしか
縮みません。**

#### だから、精度を上げる方向では直りません

公差を緩める方向も同じで、1e-6 の公差に対して 2.36e-5 は 24 倍です。**緩める
と、本物の食い違いも通します**（5章で2回踏んでいます）。

**筋の良い方向は、解かずに済ませることです。** 着地しようとしている点は、
**球が頂点として厳密に持っている極**であり、**箱の面の下辺の上にも乗って
います**（実測: `(20,10,0)`、どちらの条件も厳密に満たす）。つまり**答えは
最初から topology の中にあって、解く必要がありません。**

4-80 で同じ構図を一度解いています——「交線は辿るまでもなく、相手が稜として
持っていた」。あちらは**稜**、こちらは**頂点**です。

**次に手を付ける人へ。** 筋は2つあります。**どちらも「重根を解かない」**
という点で同じです。

##### 筋A: 解かずに、頂点を採る

縁への着地（`land_on_crossed_bound` / `newton_to_bound` のあたり）で、
着地しようとしている縁の上に**どちらかの立体の頂点が乗っていないか**を
先に見る。乗っていて、かつその頂点が相手の曲面の上にもあるなら、**解かずに
その頂点を採る**。

- 利点: 公差の話にならない。答えは厳密
- 難点: 幾何の層（`ssi_march.rs`）は頂点を知りません（`zenith_geom` は
  `zenith_topo` に依存していない）。呼ぶ側（`brep_intersection.rs`）で
  面の頂点から「両方の曲面の上にある点」を作って渡すことになります。
  **端点を差し替えるだけでは足りません**——曲線のほうも届いていないので、
  当てはめ直しが要ります

##### 筋B: 交点ではなく、極値を解く

**そもそも極は「交線が z = 0 と交わる点」ではありません。「交線が z = 0 に
接する点」＝大円の最下点**です。

- 交わる点（重根）を解くと、条件数のせいで横は $\sqrt{\varepsilon}$ でしか
  決まりません。**それが 2.36e-5 の正体です**
- **極値を解くのは条件数が良く**、倍精度いっぱいまで詰まります

着地しようとしている縁に対して、**交線がその縁を横切るのか接するのか**を
先に見て、接するなら極値として解く。幾何の層だけで閉じるので、筋Aのような
層またぎが要りません。

**接するかどうかは測れます**——着地の近くで、縁の座標（この例では z）が
辿りに沿って符号を変えるかどうかです。変えなければ接しています。

##### どちらを先に試すか

**筋Bのほうが構造が素直**だと見ています（幾何の層で閉じ、公差も出てこない）。
ただし**まだ測っていません**。筋Aは確実に効きますが、当てはめ直しまで含めて
手数が増えます。

```bash
ZENITH_SSI_WHY=1  cargo run --release -p zenith_algo --example <再現>
ZENITH_STITCH_WHY=1 cargo run --release -p zenith_algo --example <再現>
```

#### 試して効かなかったもの（記録）

`tangent()` が極で `None` を返す（`du × dv` が消える）ので、そこが着地を
捨てているのだろうと考え、4-69 の `normal_or_limit` に差し替えました。
**端点はビット単位で1つも動きませんでした。** 極で止まっているのは
`tangent` の経路ではありません。測って効かなかったので戻してあります（5章）。

---

### 4-82. 接する縁には、交点ではなく極値で着地する（2026/08/25）

4-81 で「重根を解いているから √ε でしか決まらない」と分かったので、**解き方を
変えました**。3-N-2（球を45度回して切ると縫合で16本の稜が相手を見つけられない）
が片づきました。

| | 前 | 後 |
| :--- | ---: | ---: |
| 弧の端点の、極までの距離 | **2.361e-5** | **5.33e-15** |
| union / difference / intersection | 3件とも REFUSED | **3件とも ok** |
| intersection の体積 | — | **2094.3951**（半球の解析解、差 3.2e-6） |

#### 入れたもの

`newton_to_bound` は「両曲面の上にあり、かつ `state[which] = value`」を解きます。
縁を**横切る**ときはそれでよいのですが、**接する**ときはそこが重根で、
ヤコビアンが特異になります。

`newton_to_bound_extremum` を足しました。3本は今までどおり `p1 = p2`、4本目を
**「交線に沿った `state[which]` の速度が 0」**に差し替えます。接する点は
「その座標が極値をとる点」なので、これは**単根**です。倍精度まで詰まります。

4本目の行だけ中心差分で作ります。ここはニュートンの傾きとしてしか使わないので、
桁が半分落ちても収束には効きません。**刻みは 1e-4 です**——1e-6 で始めたら
4本の弧のうち2本が収束しませんでした（極の近くで速度が急に変わるので、
狭い差分が雑音に埋もれます）。

#### 採否の条件 — ギャップは使えません

最初、「極値の解のほうがギャップが小さければ採る」と書きました。**間違いです。**
接する点では残差が位置を決めないので（`land_on_crossed_bound` の少し上に
同じことが書いてあります）、**位置が 2.36e-5 ずれていてもギャップは 0 に
なります**。実測で `extremum gap 0.000e0 vs crossing 0.000e0` が並びました。

条件は「**極値の解がその縁の上に乗っていること**」だけにしました。縁を横切る
配置では、速度がゼロになる点は縁の上に来ないので、そこで落ちます。**分類の
しきい値を置かずに済みます。**

#### 単独では効かなかった部品が、要りました

4-81 で「`tangent()` の極対応（`normal_or_limit`）は端点を1ビットも動かさない」
と記録し、戻しました。**極値の解を入れると、それが無いと収束しません**
——極に近づいた瞬間に `tangent` が `None` を返して止まるからです。

5章の「単独で効果が測れない部品でも、他が揃うまで判断を保留する」が、また
出ました。**戻した判断は正しく、記録が残っていたので拾い直せました。**

#### 入れなかったところ

着地口はもう1つあります（`run()` の `!inside(settled)` の側）。**測った配置
では一度も通らず、効くかどうかを確かめられなかったので入れていません。**
同じ形の欠陥が出たら、`land_on_crossed_bound` と同じものをそちらにも
入れてください（コメントに書いてあります）。

#### 実測

**SSI マーチングはすべてのブーリアンが通る道**なので、広く測りました。

| | 結果 |
| :--- | :--- |
| ビルド警告 | **0** |
| テスト | **527 / 527** |
| `boolean_envelope` | supported **44** / wrong 0 / エラー 1 ——**不動** |
| 解析解との最悪一致 | **6.68e-8** ——不動 |
| 45ケースの曲面評価 | 53,204,877 → **56,340,279**（+5.9%） |
| `foreign_boolean_probe` | ok 26 / NOCUT 4 / 拒否 0 / WRONG 0 / PANIC 0、**90/90**、残差 4.04e-9 |
| プローブ16本（`ssi_probe`・`face_split_probe`・`intersection_edge_probe`・`march_stop_probe` 含む） | すべて exit 0 |
| FreeCAD 相互検証 / ショーケース / 書き戻し | **15/15・24/24・7/7** |

仕事量の +5.9% は極値を解くぶんです。**答えは1つも動いていません。**

---

### 4-83. ブーリアンは正しく、メッシュ化が壊れていました（2026/08/25）

4-82 で球を45度回して切る配置が通るようになったので、**通るようになった形を
そのまま `contact_placement_probe` に足しました**。ついでに、まだ測っていな
かった置き方も2つ入れました（球を軸以外のまわりに回す、円柱の継ぎ目をずらす）。

**7件が「非多様体を立体として返した」と赤になりました。**

#### まず、測る対象を取り違えていました

赤にした根拠は**メッシュ**の稜でした。B-Rep も測ったら、**そちらは完璧**です。

| 配置 | B-Rep の非多様体 | 12分割 | 24分割 | 48分割 | 三角形（48分割） |
| :--- | ---: | ---: | ---: | ---: | ---: |
| 赤道で切る（前から通る） | **0** | 0 | 0 | 0 | 27,562 |
| 45度回して切る（4-82） | **0** | 48 | 247 | 999 | **234,129** |

**「非多様体の立体を返した」かどうかは B-Rep の性質です。** `Solid` は B-Rep
なので、多様体かどうかは稜が2枚の面に共有されているかで決まります。メッシュは
派生物で、そこが壊れていても B-Rep は正しいことがある——**実際そうでした**。

**測る対象を取り違えると、ブーリアンの責任でないものをブーリアンの赤として
数えてしまいます。** プローブの判定を B-Rep に直し、メッシュの数は**報告
だけ**にしました（赤にはしません）。

#### 残った本物の欠陥: メッシュ化

B-Rep が正しいのにメッシュが非多様体なので、**壊れているのはテッセレーション
です**。

- 三角形数が **27,562 → 234,129**（同じ48分割、同じ大きさの立体）
- 非多様体の稜が分割数とともに増える（48 → 247 → 999）
- 退化三角形は 0

**格子から落ちて earcut ＋ 適応細分に行っている側の問題**と見ています
（4-71、4-75 と同じ一族）。ブーリアンが割った面は境界がパラメータ矩形の縁に
ならないので、格子を張れません。

**0 でない立体は STL に書けません。** 第1章の「測った全分割数で完全閉多様体」は
`mesh_watertight_probe` の検体についての実測で、**ブーリアンが割った曲面の
結果はそこに入っていません。**

#### 半分ほど直りました（4-84）

**earcut が境界の上に作る弦を、細分が割っていました。** `protected` に
入っていたのはリングの**連続する対だけ**で、点を飛ばして結ぶ弦は入って
いませんでした。境界が uv で直線だと（球を貫く大円は経線なのでそうです）、
その弦は境界の上に乗ります。

「**中点が境界の折れ線に乗っていれば守る**」に直しました。件数は **7 → 5**、
本数は 247/308/311 → **0/0/22** など。

#### 残り5件は、2つの違う機構です（実測 2026/08/25）

**同じ「メッシュが非多様体」でも、中身が違いました。** 数だけ見ていると
1つの欠陥に見えます。

| 件 | 症状 | 位置 | 経路 |
| :--- | :--- | :--- | :--- |
| `box × cylinder (seam turned 33)` union、126本 | **124本が「1回だけ」**、長さ 0.13〜0.40 | すべて **z = 20**（箱の上面） | grid 6 / earcut 2 |
| `box × sphere (spun 45)` intersection、22本 | **22本すべてが「4回」**、長さ 1.31〜2.61 | すべて **x = 20**（平らな円板） | grid 2 / earcut 4 |

**「1回」と「4回」は別の話です。**

- **1回**は、片側に相手がいない——境界の刻みが合っていない（4-84 と同族。
  ただし折れ線の判定では捕まらない別の道）
- **4回**は、**同じ稜を4枚の三角形が使っている**——面が二重に重なっている
  か、両側が余分な三角形を出している。**刻みの話ではありません**

長さも違います。1回のほうは 0.13〜0.40 と細かく、4回のほうは 1.308 と
2.611——**1.308 は半径10の円を24分割したときの弦**（$2 \times 10 \times
\sin 3.75° = 1.308$）です。粗い側の刻みがそのまま出ています。

#### 「4回」のほうを追いました（4-85）

**二重に覆われています。** 同じ稜を使う4枚を出すと、**面積も3点目も完全に
同一の三角形が2枚**ありました。そして決め手:

| | 値 |
| :--- | ---: |
| x = 20 の上の三角形の面積の和 | **331.06** |
| 円板の真値 | **314.159265** |
| 余分 | **16.9** |

面ごとに見ると、**球の面片が x = 20 の平面の上に三角形を出しています**
（0.18〜0.72 ずつ）。

**理由**: 境界が uv で直線だと（球を貫く大円は経線なのでそうです）、その上の
点は一直線に並びます。earcut はそこに **uv では面積 0 の三角形**を出します。
ところが**3D では一直線ではありません**——曲線の上の3点だからです。だから
「面のどの部分も覆っていない三角形」が、境界の平面の上に実体として現れます。

#### uv で面積 0 の三角形を落とす、を試して**悪化しました**

理屈は通っているように見えたので入れました。実測:

| | 前 | 後 |
| :--- | ---: | ---: |
| x = 20 の面積の和 | 331.06 | **316.63**（改善） |
| メッシュが非多様体だった件数 | 5 | **7**（悪化） |
| `spun 45` の3演算 | 0 / 0 / 22 | **89 / 94 / 95**（悪化） |

**落とした三角形が、境界を繋いでいました。** 抜くと穴が開き、稜が「1回だけ」
になります。面積は良くなり、閉じ方は悪くなる——**片方の量だけ見ていたら
「直った」と判断していたところ**です。測って悪化したので戻しました（5章）。

#### 「そもそも作らせない」も試して、これも悪化しました

落とすのが駄目なら作らせない、という筋で、**uv で一直線に並んだ境界の点を
earcut に渡す前に外し、三角形分割のあとで境界の辺に扇状に挿し戻す**手を
入れました。共有する点は1つも失わない作りです。

| | 前 | 落とす（1つ目） | 外して挿し戻す（2つ目） |
| :--- | ---: | ---: | ---: |
| メッシュが非多様体だった件数 | **5** | 7 | **6** |
| `spun 45` の3演算 | **0 / 0 / 22** | 89 / 94 / 95 | **45 / 50 / 47** |
| 体積のテスト | ok | ok | ok |

**2つとも悪化したので、2つとも戻しました。**

#### 前提を測ったら、違っていました

2回外したので、前提のほうを測りました（`ZENITH_TESS_WHY=1` に、uv で面積 0 の
三角形の枚数とその 3D 面積を出す口を足しました）。

```text
TESSWHY 三角形 94、uv で面積 0 のもの 0 枚（その 3D 面積の和 0.000000）
TESSWHY 三角形 46、uv で面積 0 のもの 0 枚（その 3D 面積の和 0.000000）
...
```

**どの面でも 0 枚です。潰れた三角形は存在しません。** 「uv で潰れた三角形が
余分にできている」という読みは**間違っていました**。だから落としても、
作らせなくしても悪化したのです——**どちらも正当な三角形を触っていました。**

#### 本当の機構

**大円の上の3点は、その円の平面を張ります。** 3頂点すべてが大円に乗る三角形は、
**x = 20 の平面にぴったり乗ります**——uv では正当な面積を持っているのに。

つまり x = 20 の上の余分な 16.9 は、**球の面片の「粗い三角形」**です。
境界の点を飛ばして張られた三角形が、たまたま全部の頂点を大円の上に持つので、
円板と同じ平面に落ちてしまう。円板側の三角形と稜を共有すれば「4回」になります。

**そしてそれを粗いままにしているのは 4-84 の保護です。** 境界が uv で直線
（球を貫く大円は経線）だと、境界の点を飛ばす弦の中点は折れ線の上に乗るので、
**割らないと決めた**のがそれです。割れば細かくなりますが、割ると境界の上に
新しい点ができて「1回だけ」が増えます——**4-84 の前に戻ります。**

#### だから、次はこう見ています

**割るか割らないかの二択ではありません。** 境界が uv で直線のときは、
**境界の点を飛ばす三角形をそもそも作らせない**（連続する点どうしで張る）のが
筋です。2つ目の手（外して挿し戻す）はその方向でしたが、**挿し戻し方が
まずかった**のだと見ています——正当な三角形を壊していた可能性があります。

**次は、挿し戻しの結果そのものを測ってください。** 面ごとに、境界の連続する
点の対が**すべて三角形の辺になっているか**を数える。いまは最終的な非多様体の
本数しか見ていないので、どこで壊れたかが分かりません。

#### 4つの仮説を立てて、4つとも外しました（4-86）

| | 仮説 | 実測 |
| :--- | :--- | :--- |
| 1 | uv で潰れた三角形を落とす | 悪化（5→7） |
| 2 | 共線点を外して挿し戻す | 悪化（5→6） |
| 3 | uv で潰れた三角形が出ている | **前提が偽。0 枚** |
| 4 | 境界の連続対が辺になっていない | **直したが本数は不動** |

4 は直りました（箱の面で 122/125 → 1/125）。**earcut が共線の境界点を自分で
落としていた**ためで、落とされた結果を測って挿し戻すようにしました。
**ただし狙った量（非多様体の本数）は 5 件のまま動いていません。**

**次はここから**: 「大円の上の3点は円の平面を張る」という機構だけが手掛かりと
して残っています。4-84 の保護が粗い三角形を残し、それが円板と同じ平面に落ちる
——**割る／割らないの二択ではない**ところまで来ています。

#### 1件を選んで、ここまで詰めました（4-83）

`box × sphere`（45度回して切る）の intersection を測りました。

| 測ったもの | 結果 |
| :--- | :--- |
| 結果の面 | 7枚（平面1 ＋ 球の面片6） |
| B-Rep の稜 | id で11本、位置で11本、**食い違い 0**（共有できている） |
| p-curve と稜の対応 | **全面で一致**（`boundary_rings` は `None` を返していない） |
| 刻み（`ZENITH_TESS_WHY=1`） | 平面 `[24,24,24,24]`、球の面片 `[24,24,24]`——**同じ** |
| 対を持たない稜 | 12分割 61本、24分割 311本。**すべて「1回だけ」**（3回以上は 0） |
| その稜の位置 | **すべて大円（x=20、半径10）の上** |
| 大円の上の頂点 | 12分割 **420個**、24分割 **1203個** |

**最後の行が食い違っています。** リングから来る点は 4弧 × 24 = **96点**の
はずなのに、24分割で **1203点**が大円の上にあります。**12倍以上です。**

そして対を持たない稜の 205/311 は、**両端の間に別の頂点があります**
——片側だけが余計に割っている形です。

#### だから、次はここです

**リング以外の何かが、大円の上に点を足しています。**

- 平面の面は `patch_mesh(rings, None, ..)` へ行くので**細分は掛かりません**
- 球の面片は細分が掛かりますが、`refine_uv_triangulation_protected` は
  リングの連続する対を `protected` に入れて割らないようにしています

**どちらの前提かが崩れています。** `protected` に入る対が、リングの
実際の稜の刻みと対応しているか（インデックスの取り方）から見てください。

```bash
ZENITH_TESS_WHY=1 cargo run --release -p zenith_algo --example contact_placement_probe
# 面ごとに、共有経路に乗ったか・リングの点数・稜ごとの刻みが出ます
```

**赤にしていないのは、直せていないからです。** 直したら赤にしてください。

---

### 4-84. earcut が境界の上に作る弦を、細分が割っていました（2026/08/25）

4-83 で「リングから来る点は 96 のはずが、大円の上に 1203 点ある」まで絞れて
いたので、その差を追いました。

#### 原因

`protected`（割ってはいけない辺）に入っていたのは、**リングの連続する対だけ**
でした。ところが earcut は、境界の点を**飛ばして結ぶ弦**を作ります。境界が
uv で直線なら——**球を貫く大円は経線なので、まさにそうです**——その弦は
**ぴったり境界の上に乗ります**。細分がそれを割ると、境界の上に新しい点が
でき、隣の面にはその点が無いので、そこでメッシュが開きます。

#### 判定条件を、測定に2回正されました

| 試した条件 | 結果 |
| :--- | :--- |
| ① 両端が境界の点なら守る | **守りすぎ**。境界だけで囲まれた曲面（斜めに切った円柱の楕円面）は細分が一度も掛からず、**体積が 4712.39 に対して 3335.78——29% 狂った** |
| ② 両端が同じ**領域の縁**に乗っていれば守る | **足りない**。大円は uv で直線だが、継ぎ目が別の経度にあるので領域の縁ではない。247本のまま戻った |
| ③ **中点が境界の折れ線に乗っていれば守る** | **両立**。改善を保ったまま、体積も戻った |

③ は幾何的に素直です。境界に沿って走る辺は中点が折れ線に乗り、内側を通る弦は
乗りません。境界が uv で曲がっていれば弦の中点は膨らみのぶん外れるので、
そこも守りません。

**①のとき `modeling_test` が止めてくれたのが効きました。** プローブの数字
（7件→5件）だけを見ていたら「改善した」と判断して進んでいたところです。
**体積を見ているテストが別にあったから捕まりました。** 5章の「1つの量が
厳密でも、その形が正しいとは限らない」の裏返しで、**別の量を見ている検査が
別にあることが効きます**。

#### 実測

| 配置 | 前 | 後 |
| :--- | ---: | ---: |
| `box × sphere (spun 45)` union / difference | 247 / 308 | **0 / 0** |
| 同 intersection | 311 | **22** |
| `box × sphere (tipped off axis)` | 216 / 353 / 294 | **13 / 13 / 9** |
| `box × cylinder (seam turned 33)` union | 413 | **126** |
| メッシュが非多様体だった件数 | 7 | **5** |

**残りは 5 件です。** 0 になっていないので、まだ別の道があります。

#### 動いていないこと

テスト **527/527**、ビルド警告 0、プローブ16本すべて exit 0
（`mesh_watertight_probe`・`tess_density_probe`・`grid_fallback_probe` を含む）、
`boolean_envelope` supported 44 / wrong 0 / エラー 1 と曲面評価 56,340,279 は
**不動**、`foreign_boolean_probe` 90/90、FreeCAD 15/15・24/24・7/7。

---

### 4-86. earcut は共線の境界点を自分で落とす。仮説は4つとも外れました（2026/08/25）

3-N-2b（ブーリアンが割った曲面のメッシュが非多様体）に、**4つの仮説を立てて
4つとも外しました**。直った欠陥は1つありますが、**狙っていた量は動いて
いません**。そのまま書きます。

| | 仮説 | 実測 |
| :--- | :--- | :--- |
| 1 | uv で潰れた三角形を落とせばよい | **悪化**（5件 → 7件）。落とした三角形が境界を担っていた |
| 2 | 共線点を earcut に渡す前に外し、あとで挿し戻す | **悪化**（5件 → 6件） |
| 3 | そもそも uv で潰れた三角形が出ている | **前提が偽。どの面でも 0 枚** |
| 4 | 境界の連続対が辺になっていないのが原因 | **直したが、本数は不動**（5件のまま） |

**3 が効きました。** 1 と 2 はどちらも「その三角形は要らない」を前提にして
おり、**その前提を測っていませんでした**。測ったら 0 枚——**正当な三角形を
触っていた**わけです。

#### 直った欠陥（狙いとは別）

`ZENITH_TESS_WHY=1` に「境界の連続対のうち、三角形の辺になっていないもの」を
数える口を足したら、こう出ました。

```text
TESSWHY 三角形 2、uv 面積 400.000000、境界の連続対 125 のうち辺になっていないもの 122
```

**箱の面が、境界 125 点を持つのに三角形 2 枚。** 122 の連続対が辺に
なっていません。

**earcut が、一直線に並んだ境界の点を自分で落としています**（`filterPoints`
が共線・重複の点を除きます）。落とされた点は**隣の面では使われる**ので、
そこは共有できません。共有境界の**必要条件**の違反です。

自分で共線を判定して外すのではなく（それが仮説2で、悪化しました）、
**落とされた結果のほうを測って挿し戻す**ようにしました
（`reinsert_dropped_boundary_points`）。判定を二重に持つと、earcut の基準と
食い違ったときにまた壊れます。

| | 前 | 後 |
| :--- | ---: | ---: |
| 箱の面の、辺になっていない境界対 | **122 / 125** | **1 / 125** |
| メッシュが非多様体だった件数 | 5 | **5（不動）** |

**必要条件は満たすようになりましたが、狙った量は動きませんでした。**
「1回だけ」の稜には、まだ別の原因があります。

#### いま残っている手掛かり

**大円の上の3点は、その円の平面を張ります。** 3頂点すべてが大円に乗る三角形は、
uv では正当な面積を持つのに **3D では円板と同じ平面にぴったり乗ります**。
x = 20 の上の面積が 331.06（真値 314.159）だったのはこれです。

そしてそれを粗いままにしているのは **4-84 の保護**です。割れば細かくなり
ますが、境界に新しい点ができて「1回だけ」が増え、4-84 の前に戻ります。
**割る／割らないの二択ではありません。**

#### 動いていないこと

テスト **527/527**、ビルド警告 0、`mesh_watertight_probe`・`tess_density_probe`・
`slice_probe`・`builder_audit`・`foreign_slice_probe` すべて exit 0、
`boolean_envelope` supported 44 / wrong 0 / エラー 1・曲面評価 56,340,279 とも
**不動**、`foreign_boolean_probe` 90/90、FreeCAD 15/15・24/24・7/7。

---

### 4-87. 境界 ear と外周へ接する穴を分けて直し、5件を3件へ減らした（2026/08/25）

4-86 の次を、`ZENITH_TESS_WHY` の段階別計測で追いました。結論は、残り5件にも
さらに2つの機構がありました。

#### 1. 球面側には、earcut直後から極小UV面積の境界earがある

`box × sphere (spun 45)` intersection を1演算だけに絞り、earcut直後、境界点の
挿し戻し後、適応細分後、mesh emit後を別々に数えました。極小三角形は挿し戻しや
細分で生じたのではなく、**earcut直後から存在し、細分後まで枚数不動**でした。
3頂点が同じ大円Edge上にある三角形は、球面の面片でありながら3Dでは平面capと
同じ円板上に寝るため、同じmesh edgeが4回使われます。

三角形を捨てたり境界を細分したりせず、隣接する正常な三角形との**内部対角線だけ
をflip**する `repair_boundary_ears` を追加しました。新旧のUV面積が一致し、2枚とも
十分な面積を持ち、新しい対角線が既存辺と重複しない場合だけ採用します。外周の
頂点と辺は変えません。

#### 2. 円柱側は「穴」ではなく、外周へ接する内周だった

`box × cylinder (seam turned 33)` union の平面Faceは、外周5点・内周120点でした。
内周の向きは正しく、面積も -113.030233246 でしたが、bboxが外周の x=0 に接して
いました。独立したholeとしてearcutへ渡すと、片側のFaceでは**外周の2三角形だけ
を返し、内周120対を丸ごと使っていません**でした。

外周と内周が同じUV頂点で接する場合は、接点で2つのringを1本へ橋渡しした候補も
earcutへ渡し、通常入力より境界被覆が改善した場合だけ採る
`earcut_boundary_rings` を追加しました。通常の離れた穴には入りません。

#### 全配置を通した実測

| 配置・演算 | 4-86後 | 4-87後 |
| :--- | ---: | ---: |
| `box × sphere (spun 45)` intersection | 22 | **6** |
| `box × sphere (tipped off axis)` union / difference / intersection | 13 / 13 / 9 | **3 / 0 / 0** |
| `box × cylinder (seam turned 33)` union | 123（古い記録の126から再実測） | **3** |
| 非多様体meshを返した演算数 | **5** | **3** |

B-Repの非多様体は前後とも0です。1ケースだけに絞ると `spun 45` が2本まで減る
実行もあり、全配置を順に生成した場合は6本でした。**生成順序またはID順序に依存
する揺れが残っている**ため、少ない方ではなく全配置実行の6本を現行値にします。
ここは次回、残るedgeの面所有者とトポロジーIDを固定して追ってください。

#### 検証とGit

- commit: `066597c fix(tess): repair boundary ears and touching holes`
- `cargo check --workspace --exclude zenith_py`: exit 0
- `cargo test --release -p zenith_tess`: **2/2**（新規）
- `cargo test --release -p zenith_algo --test modeling_test`: **148/148**
- `cargo test --release --workspace --exclude zenith_py`: **529/529**
- `mesh_watertight_probe`: 4〜32分割、全検体 open / non-manifold / degenerate 0
- `tess_density_probe`: 密度単調性の違反0、同形状の2経路比1.00x
- `grid_fallback_probe`: 15検体、許容超過0

全件実行で見つかった `step_face_parity_test` の未使用import警告は、挙動変更と
混ぜず `f4b60ea test: remove stale Surface3 import` で除去し、同テスト3/3を再確認
しました。

テスト総数は、この2件を足して `#[test]` 実測 **529件**です。既知の3件はまだ
0ではないので、`contact_placement_probe` のmesh異常は現時点ではexit条件へ入れて
いません。次は残る3件をゼロにした後で赤ゲートへ昇格させます。

---

### 4-88. 連続する境界earを単調に解き、3件を1件へ減らした（2026/08/25）

4-87の球面側には、`repair_boundary_ears` が1回のflipで2枚とも正常になる場合しか
進まない、もう1段の停止条件がありました。極小earが連続すると、隣接側も修復
対象なので入口で止まります。

flip前後の2三角形について `needs_repair` の枚数を数え、**必ず枚数が減る場合だけ**
採用するようにしました。2枚から1枚へ減るflipを許し、残った1枚は次のroundで
直します。1枚から1枚への横移動は許さないため循環せず、従来どおり外周頂点・
外周辺・UV総面積は変えません。

全配置を再実行した結果、球面の2演算に残っていたmesh edgeはすべて0になりました。

| 配置・演算 | 4-87後 | 4-88後 |
| :--- | ---: | ---: |
| `box × sphere (spun 45)` intersection | 6 | **0** |
| `box × sphere (tipped off axis)` union | 3 | **0** |
| `box × cylinder (seam turned 33)` union | 3 | **3** |
| 非多様体meshを返した演算数 | 3 | **1** |

4-87で記録した `spun 45` の2本という値は、その時点の途中実装での観測でした。
確定コードを別プロセスで8回再実行すると4-88修正前は毎回6本、修正後は全配置でも
0本です。したがって現行状態に生成順序依存が残る、という4-87時点の見立ては
再現せず、現行値には採りません。

#### 回帰と検証

- commit: `ff7d928 fix(tess): unwind clustered boundary ears`
- `contact_placement_test` に、45度回した球を極で半分に切り、体積とmesh多様体を
  同時に検査する1件を追加: **3/3**
- `cargo test --release --workspace --exclude zenith_py`: **530/530**
- `cargo test --release -p zenith_algo --test modeling_test`: **148/148**
- `mesh_watertight_probe`: 4〜32分割、全検体 open / non-manifold / degenerate 0
- `tess_density_probe`: 密度単調性の違反0、同形状の2経路比1.00x
- `grid_fallback_probe`: 15検体、許容超過0
- `contact_placement_probe`: 19 returned / 2 refused、B-Rep異常0、mesh異常 **1件・3本**

残る1件は、外周へ接する内周を持つ円柱和集合です。4-87の橋渡しで123本から3本
まで減りましたが、接点が作るpinched ringの3辺はまだ閉じていません。ここが0に
なるまでは、mesh異常をprobeのexit条件へ昇格させません。

---

### 4-89. 接点の孤立flapを除き、常設21演算を0件へした（2026/08/25）

4-88に残った3本を座標と三角形所有者で追うと、接点に1枚だけ重なって付いた
三角形の3辺でした。辺使用数は厳密に `[3, 3, 1]` です。この1枚を外すと、過剰な
2辺は3回から2回へ戻り、1回だけの対角線は消えます。

最終weld後に全辺使用数を数え、**三角形自身の3辺が `[1, 3, 3]` の場合だけ**除く
`remove_redundant_flap_triangles` を追加しました。他の開辺、4回使用、通常の閉じた
三角形には触れません。合成した局所トポロジーの単体テストと、実際の33度円柱
和集合のend-to-end回帰を両方追加しています。

| 指標 | 4-83初回 | 4-87後 | 4-88後 | 4-89後 |
| :--- | ---: | ---: | ---: | ---: |
| 非多様体meshを返した演算数 | 5 | 3 | 1 | **0** |
| 残るmesh edge数 | 9〜126 | 3〜6 | 3 | **0** |
| B-Rep異常 | 0 | 0 | 0 | **0** |

#### 回帰、ゲート、Git

- commit: `af42cd5 fix(tess): remove isolated contact flaps`
- commit: `31bc4ec test: gate contact placement mesh manifoldness`
- `zenith_tess`: **3/3**（flap局所回帰を追加）
- `contact_placement_test`: **4/4**（33度円柱和集合のend-to-end回帰を追加）
- `contact_placement_probe`: 19 returned / 2 refused、B-Rep異常0、mesh異常 **0**
- mesh異常もexit条件へ追加。今後1本でも戻ればprobeはexit 1
- `cargo test --release --workspace --exclude zenith_py`: **532/532**
- `cargo test --release -p zenith_algo --test modeling_test`: **148/148**
- `mesh_watertight_probe`: 4〜32分割、全検体 open / non-manifold / degenerate 0

ここで言う0は、常設の7配置・21演算とwatertight検体についての実測です。任意の
ブーリアン結果すべてを証明した、という意味には広げません。

### 4-90. ローカル広域ゲートも `robustness_probe` の誤答・panicで落ちるようにした（2026/08/25）

`robustness_probe` は `WRONG` と `PANIC` を数えて表示していましたが、どちらが
1件以上あっても終了コードは0のままでした。CIは出力文字列を別途検索するため
検出できますが、`tools/fast_test.sh` は各プローブの終了コードを信頼するため、
同じプローブに対してローカルとCIが違う契約を持っていました。

`WRONG > 0` または `PANIC > 0` のとき、プローブ自身がexit 1を返すようにしました。
`clean error` と退化入力のbuild refusalはこれまでどおり健全な結果として扱います。

- 変更前後とも `robustness_probe`: **ok 53 / clean error 4 / WRONG 0 / PANIC 0 / build refused 5**
- 変更前の全体基準: `cargo test`: **532/532**
- 変更前の広域基準: `tools/fast_test.sh`: **164バイナリ / 532合格 / 0不合格 / 非ゼロ終了0**
- ブーリアン基準: **44 supported / wrong 0 / error 1、56,340,279 surface evaluations**
- FreeCAD / OpenCASCADE相互検証: **15/15**

### 4-91. 独立に傾けた曲面立体どうしの配置不変性を常設測定にした（2026/08/25）

`boolean_envelope` の曲面検体は素直な配置が中心だったため、2本の円柱をそれぞれ
別の軸・角度で回し、その組全体をさらに23度回転して `(37, -19, 11)` へ移した
前後で、和・差・積を比較する `curved_placement_probe` を追加しました。

- 入力円柱単体の体積残差: **1.24e-14 / 2.73e-14**
- 和: **1.95e-8**、差: **2.85e-8**、積: **1.61e-10**
- 元の演算結果を同じ剛体変換してから測った補助残差: **1.90e-10 / 2.97e-10 / 1.06e-11**
- 3演算とも支持状態・ソリッド数が一致し、閉シェル。WRONG 0 / PANIC 0

補助残差が2桁ほど小さいため、主な差は生成B-Repそのものではなく、複雑な分割面を
世界原点まわりで積分するときの丸めと判断しています。発散定理の体積基準を各
ソリッドの近傍へ移す案も実測し、配置残差は最大 **8.31e-9** まで改善しました。
しかし、別々の基準を使うことで分割片の加法性
`V(A-B)+V(A^B)=V(A)` が **3.053e-8** まで悪化したため、変更は撤回しました。

現状を隠さず固定するため、実測最悪値 2.85e-8 に対して名前つきの配置許容
`PLACEMENT_ALLOWANCE = 5e-8` を置きました。支持状態・ソリッド数・閉シェルの
不一致、panic、許容超過でprobeはexit 1です。両側が明示的に断る場合だけは健全な
拒否として扱います。加法性を保ったまま改善できたときにのみ、この許容を下げます。
CIの常設プローブ一覧にも追加済みです。

追加後の全体確認:

- `cargo test --release --workspace --exclude zenith_py`: **532/532**
- `tools/fast_test.sh`: **165バイナリ / 532合格 / 0不合格 / 非ゼロ終了0**
- 警告をエラー扱いした全ターゲットのRelease build: **成功**
- `boolean_envelope`: **44 supported / wrong 0 / error 1、56,340,279 surface evaluations**
- 書き出し直した検体によるFreeCAD / OpenCASCADE相互検証: **15/15**

### 4-92. 純直円柱の凸キャップ円周を厳密トーラスで丸めた（2026/08/25）

3-4bで最優先候補にしていた「平面×円柱の円形稜」を、まず**純粋な直円柱の
凸キャップ外周だけ**に限定して実装しました。着手前はOpenCASCADE製の円柱に
丸められる稜が0本でした。一般のrolling-ball filletを名乗らず、穴・ボス・
段付き軸など隣接面以外の位相を持つ立体は認識段階で断ります。

- 自作円柱の4分割円弧とOCCの全周1本円を認識し、任意の剛体回転・平行移動後にも
  対応。上下面どちらの凸縁でも、1円弧の選択を滑らかな全周へ伝播する
- 結果は**円柱4面＋有理2次トーラス4面＋平面キャップ2面**の厳密B-Rep。
  円弧やトーラスを折れ線・近似曲面に置き換えていない
- 半径 `R`、高さ `H`、丸め半径 `r` の除去体積を
  `π[(R-r)r²(2-π/2)+r³/3]`、表面積を
  `πR²+2πR(H-r)+π(R-r)²+π²(R-r)r+2πr²` と独立に採点
- `R=10, H=40, r=2` のOCC基準は体積 **12514.844774537281**、
  面積 **3085.878023563117**。自前結果の閉形式最悪相対誤差は **9.52e-11**
- 円柱側面・キャップとのG1接続、STEP往復、4/6/8/12/16/24/32分割meshの
  open・non-manifold・degenerate 0を含む回帰 **11件**を追加
- 円形穴を持つ箱を円柱へ誤認して全体再構築しないことも回帰で固定

常設測定では `foreign_edit_probe` が **8→9操作適用、閉形式10→11、WRONG 0**、
丸められる稜が0本の検体は **12→11件**になりました。外部検証へ
`cylinder_top_fillet_r2` を足し、FreeCAD / OpenCASCADE相互検証は **16/16**。
新検体の体積差は **3.06e-11**、面積差は **1.10e-11**、無影響な中間断面差は
**1.59e-7**です。

追加後の全体確認:

- `cargo test --release --workspace --exclude zenith_py`: **543/543**
- 警告をエラー扱いした全ターゲットとPython 3.11指定の`zenith_py`: **成功**
- `boolean_envelope`: **44 supported / wrong 0 / error 1、56,340,279 surface evaluations**（不動）
- FreeCAD / OpenCASCADE相互検証: **16/16**

残る次段は、穴・ボス・段付き軸での円周伝播、円錐基部、円形面取り、一般の
rolling-ball filletです。今回の純円柱用再構築を、そのまま複合位相へ適用しては
いけません。

### 4-93. 同じ限定認識へ純直円柱の円周面取りを追加した（2026/08/25）

4-92で安全側に0としていた円周の`max_chamfer_distance`を、同じ**純粋な直円柱**
だけに限って実装しました。フィレットの認識範囲は広げていません。自作4分割円弧、
OCC全周1本円、任意の剛体配置、上下どちらの凸キャップという条件をそのまま共有し、
穴・ボス・段付き軸はフィレット同様に断ります。

- 両面から等距離 `c` 後退し、円柱4面＋厳密有理円錐台4面＋平面キャップ2面へ
  再構築。面取り面を三角形列や近似曲面にはしていない
- 除去体積は **`πc²(R-c/3)`**。表面積は
  `πR² + 2πR(H-c) + π(R-c)² + π(2R-c)√2c` と独立に採点
- `R=10, H=40, c=2` のOCC基準は体積 **12449.084488625153**、
  面積 **3062.7753976906697**。自前の外部円柱編集は閉形式と **3.78e-11**
- 3寸法、OCC基準、1円弧から全周への伝播、上下＋剛体配置、OCC全周円、STEP往復、
  4/8/16/32分割mesh、無効距離、穴口の誤認拒否を回帰7件で追加

常設測定では`foreign_edit_probe`が **10操作適用・閉形式12件・WRONG 0**。
外部検証へ`cylinder_top_chamfer_c2`を追加し、FreeCAD / OpenCASCADE相互検証は
**17/17**（新検体は体積差 **9.16e-12**、面積差 **9.23e-12**、中間断面差
**1.59e-7**）。ワークスペース全体は **550/550**、警告をエラー扱いした新旧
ブレンド回帰は **27/27**。`tools/fast_test.sh`も
**166バイナリ / 550合格 / 0不合格 / 非ゼロ終了0**です。

次は当初の順序どおり円錐基部、または複合位相を保つ再トリムです。段付き軸の
凹肩は体積を足す別規約なので、凸円柱の再構築を流用してはいけません。

### 4-94. 非直角の純円錐円周を厳密トーラス扇形で丸めた（2026/08/25）

3-4bの第2段階だった純円錐/円錐台の平面キャップ円周を実装しました。円錐の
母線勾配を`k=(R_top-R_bottom)/H`、`s=√(1+k²)`とし、選択キャップ側の
rolling-ball中心半径を`M=R_top-r(s+k)`、中心高さを`H-r`として、円錐オフセット線と
キャップオフセット面の交点を閉じた式で求めます。断面円弧の角度は90度固定ではなく、
実際の二面角に従います。

- 円錐台の上下どちらの円周、自作4分割円弧、OCC全周1本円、任意の剛体配置に対応
- 真円錐の底円周にも対応。頂点は**微小円・微小平面へ置換せず**、4側面が同じ
  実頂点へ収束する9面B-Repを保つ
- 任意角の断面円弧を有理2次、回転方向を有理2次円弧として、4枚の厳密トーラス
  扇形を構成。円錐側面・平面キャップ双方とのG1を法線で検査
- 円錐台`R=10→4, H=20, r=1`のOCC基準は、上縁が体積
  **3264.7082284293483**・面積 **1277.2926805751288**、下縁が体積
  **3242.364474159573**・面積 **1230.5830568214212**。独立した子午面積分と一致
- 寸法4種、上下縁、真円錐、OCC円錐台/真円錐、全周伝播、剛体配置、G1、STEP、
  4〜32分割mesh、段付き軸の誤認拒否、無効半径を回帰 **11件**で固定

`foreign_edit_probe`は円錐台と真円錐を新たに閉形式で採点し、
**12操作適用・閉形式14件・WRONG 0**。円錐台は **3.16e-11**、真円錐は
**2.69e-11**で式に一致し、対象稜0の検体は **11→9件**です。

外部検証へ円錐台と真円錐のフィレットを追加して **19/19**。円錐台のOCC差は
体積 **9.44e-12**・面積 **9.31e-12**、真円錐は体積 **2.80e-9**・面積
**2.03e-10**です。ワークスペース全体は **561/561**、警告をエラー扱いした
全ターゲットとPython 3.11指定の`zenith_py`も成功。`tools/fast_test.sh`は
**167バイナリ / 561合格 / 0不合格 / 非ゼロ終了0**です。

残る実用上の次段は、純立体の丸ごと再構築では済まない**ボス根元・穴口・段付き軸肩**の
局所再トリムです。段付き軸肩は凹稜で体積を足すため、今回の凸円錐規約とは分けます。
円錐円周の等距離面取りもまだ広告していません。

### 4-95. 貫通円筒穴の口を、既存位相を保って局所フィレットした（2026/08/25）

3-4bで次段としていた**平面板の円形穴口**を実装しました。純円柱のように立体全体を
作り直さず、選択した平面キャップの内側ワイヤ、同じ穴の円筒ボア、反対側キャップの
同じ内側ワイヤだけを再トリムし、4枚の短縮ボア面と4枚の厳密有理トーラスを挿入します。
外周側面、別の穴、内部シェルはそのまま保持します。

- 面統合済み`make_drilled_box`の4分割円弧、OCCの全周1本円、全B-spline STEPを
  `FaceMerger`で平面化した形、上下どちらの口、任意剛体配置に対応
- OCCの3穴プレートで1穴だけを丸め、未選択2ボア面のFace IDと天面の内側ワイヤ3本を保持
- 穴半径`a`、フィレット半径`r`の除去体積を、回転断面の一次モーメントから
  `π[a r²(2-π/2)+r³(5/3-π/2)]`と独立に導出して採点
- 半径上限は板厚だけでなく、外周・他の内側ワイヤまでの距離を境界曲線内部で測る。
  端点だけでは正方形の角までの距離を採る欠陥を回帰中に検出して修正
- ボア/キャップ双方とのG1、STEP往復、4/6/8/12/16/24/32分割mesh、上下口、
  剛体配置、全周伝播、過大半径、未統合扇形と段付き穴の拒否を回帰**11件**で固定

`foreign_edit_probe`へ3穴プレートを追加し、**14操作適用・閉形式15件・WRONG 0**。
穴口は閉形式と **1.39e-10**で一致します。外部検体15件の対象稜0は**9→8件**に減り、
同じ認識で内径を持つ`revolved_ring`の平面口も局所編集できました。

外部検証へ`drilled_box_mouth_fillet_r1`を追加して**20/20**。OCC差は体積
**2.61e-12**、面積 **4.51e-13**、中央断面 **2.63e-9**です。警告をエラー扱いした
新旧ブレンド回帰は**40/40**、`tools/fast_test.sh`は
**168バイナリ / 572合格 / 0不合格 / 非ゼロ終了0**です。

まだ扱わないものは、面統合前の扇形キャップ、止まり穴・段付き穴、ボス根元、
段付き軸肩、穴口面取りです。近い形へ置換せず、既存の一般経路から理由つきで失敗します。

### 4-96. 貫通円筒穴口の局所面取りを厳密円錐台で実装した（2026/08/26）

4-95の穴口認識と局所位相交換を、**等距離面取り**へ広げました。立体全体は作り直さず、
選択した平面キャップの内側ワイヤ、同じ穴の円筒ボア、反対側キャップの対応ワイヤだけを
再トリムし、4枚の短縮ボア面と4枚の**次数1×2の厳密有理円錐台パッチ**を挿入します。
外周側面、別の穴、内部シェルは4-95と同じく保持します。

- 穴半径`a`、面取り距離`c`に対する除去体積 `π c²(a + c/3)` を独立に導出して採点
- 自作4分割円弧、OCC全周1本円、上下口、任意剛体配置、STEP往復に対応
- 過大距離は広告上限でも実行時でも拒否し、段付き穴など認識外の形を近似置換しない
- 面取り回帰3件を追加し、穴口専用回帰はフィレット込みで**14件**すべて合格

`foreign_edit_probe`は**16操作適用・閉形式18件・WRONG 0**。`revolved_ring`と
3穴プレートでフィレット/面取りを独立に採点し、最悪相対誤差は引き続き
**1.39e-10**です。外部検証へ`drilled_box_mouth_chamfer_c1`を追加して**21/21**。
新検体のOCC差は体積 **9.30e-13**、面積 **5.51e-13**、中央断面
**2.63e-9**です。警告をエラー扱いした新旧ブレンド回帰は**47/47**、
`tools/fast_test.sh`は**168バイナリ / 575合格 / 0不合格 / 非ゼロ終了0**です。

次の局所フィーチャー候補は**ボス根元または段付き軸肩**です。円錐円周の等距離面取り、
面統合前の扇形キャップ、止まり穴・段付き穴も引き続き理由つきで拒否します。

### 4-97. 円筒ボス根元・段付き軸肩を局所フィレットした（2026/08/26）

平面肩の円形内側ワイヤから外向きに直円筒が立ち、次の平行平面で終わる90度凹円周を
厳密認識します。立体全体を作り直さず、選択肩の内側ワイヤとその円筒側面、次の平面の
外側ワイヤだけを再トリムし、4枚の短縮円筒面と4枚の厳密有理四分トーラスを挿入します。
ブーリアン生成の円筒ボス、自作4分割段付き軸、OCC全周円の3段軸を同じ経路で扱います。

- 小径`a`、フィレット半径`r`の追加体積は
  `π[a r²(2-π/2)+r³(5/3-π/2)]`。凹稜なので
  `EdgeBlendReport::predicted_removed_volume`はこの値の**負値**
- OCC 3段軸で一段目を丸めた後も二段目の全周円と未選択面を保持し、続けて適用可能
- ブーリアン製ボス、自作/OCC、任意剛体配置、G1、STEP往復、4〜32分割mesh、
  面積・体積、Face ID保持、非正・非有限・過大半径拒否を専用回帰**6件**で固定
- トリム済み面の基礎円筒が境界外へ延びても、ワイヤが定める有効域を正しく採用する。
  自作軸の0.1 mm食い込み生成履歴へ依存しない

`foreign_edit_probe`は段付き軸を閉形式採点へ追加して
**17操作適用・閉形式19件・WRONG 0**、最悪相対誤差 **5.26e-10**。
外部相互検証は`stepped_shaft_root_fillet_r1_25`を加えて**22/22**で、新検体のOCC差は
体積 **1.315e-11**、面積 **8.676e-12**、断面 **1.591e-7**です。
Showcaseには`target/showcase/25_stepped_shaft_root_fillet.step`を生成し、
OpenCASCADEで**25/25 valid closed solid**。警告をエラー扱いした円周ブレンド回帰は
**53/53**、Python 3.11 Releaseビルドも警告0、`tools/fast_test.sh`は
**169バイナリ / 581合格 / 0不合格 / 非ゼロ終了0**です。

次は同じ局所認識を使う**根元面取り**が最小です。非円形・非直角根元、円錐円周面取りは
まだ広告せず、近い形へ置換しません。

### 4-98. 円筒ボス根元・段付き軸肩を局所面取りした（2026/08/26）

4-97の厳密認識と局所位相交換をフィレット/面取り共通経路へ一般化しました。
90度凹円周の断面 `(a,c)` と `(a+c,0)` を直線で結び、4枚の次数1×2の厳密有理
円錐台パッチを挿入します。未選択面と別の肩は作り直しません。

- 追加体積 `π c²(a + c/3)`、符号付き除去体積 `-π c²(a + c/3)`、表面積を閉形式で採点
- 自作4分割円弧、OCC全周1本円、任意剛体配置、複数段連続適用、ブーリアン製ボス、
  Face ID保持、非正・非有限・過大距離拒否、STEP往復、4〜32分割meshを専用回帰で固定
- `foreign_edit_probe`: **18操作適用・閉形式20件・WRONG 0**。段付き軸面取りの
  閉形式相対誤差 **2.04e-10**、全体最悪 **5.26e-10**
- FreeCAD 1.1 / OpenCASCADE相互検証: **23/23**。新検体はSolid / valid / closed、
  体積差 **8.446e-12**、面積差 **8.996e-12**、断面差 **1.591e-7**
- `cargo test --release --workspace --exclude zenith_py`: **586/586**
- `tools/fast_test.sh`: **169バイナリ / 586合格 / 0不合格 / 非ゼロ終了0**

次の最小フィーチャーは非直角の円錐状ボス根元です。非円形根元と一般の可変二面角は、
同じ形へ近似せず専用の幾何契約を先に置いてください。

### 4-99. 非直角の円錐状ボス根元を局所フィレットした（2026/08/26）

4-97/4-98の局所認識を、一定半径の円筒側面から軸方向に半径が線形変化する
直円錐側面へ一般化しました。肩の円形内側ワイヤ、平行な先端平面、全支持面標本が
同じ円錐へ乗ることを確認し、近い円筒へ置換しません。円筒根元の既存経路も同じ
一般式の勾配0として保持しています。

- 円錐オフセット線と肩のオフセット平面から接触点を解き、任意角の断面円弧を
  有理2次、回転方向を有理2次として4枚の厳密トーラス扇形を局所挿入
- 閉形式の追加体積と符号付き除去体積を採点。半径8→5、高さ12、r=2でB-Rep積分、
  STEP再読後の積分とも一致
- 選択肩を含む平面Face ID保持、G1、任意剛体配置、STEP往復、4〜32分割meshを
  専用回帰2件で固定。非直角の等距離面取りは`max_chamfer_distance=0`で明示拒否
- FreeCAD 1.1 / OpenCASCADE相互検証: **24/24**。新検体はSolid / valid / closed、
  体積差 **5.212e-13**、面積差 **4.596e-13**、断面差 **1.421e-16**
- `tools/fast_test.sh`: **169バイナリ / 588合格 / 0不合格 / 非ゼロ終了0**

次は非直角根元の等距離面取りです。その後は非円形根元へ進み、円形や90度へ
近似しない契約を維持してください。

### 4-100. 非直角の円錐状ボス根元を等距離面取りした（2026/08/26）

4-99の円錐根元認識をそのまま使い、肩平面上の後退距離`c`と円錐母線上の
後退距離`c`を等しくする契約で局所面取りを実装しました。円錐勾配`s`、
`N=sqrt(1+s²)`に対して円錐側接触点は`z=c/N`、`R=a+s c/N`、肩側は
`z=0`、`R=a+c`です。この2点を結ぶ直線を回転し、4枚の次数1×2の厳密有理
円錐台パッチとして挿入します。

- 母線長と肩面上の等距離を契約にしたため、勾配0では4-98の90度式へ厳密に一致
- 閉形式の追加体積と負の除去体積、許容距離、過大距離拒否、平面Face ID保持、
  任意剛体配置、STEP往復を4-99の専用回帰へ追加
- FreeCAD 1.1 / OpenCASCADE相互検証: **25/25**。新検体はSolid / valid / closed、
  体積差 **4.721e-13**、面積差 **4.586e-13**、断面差 **1.421e-16**
- `tools/fast_test.sh`: **169バイナリ / 588合格 / 0不合格 / 非ゼロ終了0**

次は純円錐/円錐台の凸キャップ円周の等距離面取りです。凹根元とは除去体積の
符号とトリム方向が逆なので、同じ処理へ押し込まず別契約として実装してください。

### 4-101. 純円錐・円錐台の凸キャップ円周を等距離面取りした（2026/08/26）

4-94の純円錐リム認識に、凸リム専用の等距離面取り契約を追加しました。選択キャップ
上と円錐母線上で同じ距離`c`だけ後退し、その2円を4枚の次数1×2の厳密有理円錐台で
結びます。凹根元の4-100とは除去体積の符号とトリム方向を分けています。

- 円錐台の上下両リム、縮径/拡径、真円錐の実頂点、自作4分割円、OCC全周1本円、
  任意剛体配置、過大距離拒否、STEP往復、4〜32分割meshを専用回帰12件で固定
- `ChamferBuilder::chamfer_cone_top_edge`を公開し、選択稜APIは一円弧の選択を
  滑らかな全周へ伝播。`max_chamfer_distance`もキャップ半径と母線長から広告
- `foreign_edit_probe`: **20操作適用・閉形式22件・WRONG 0**。OCC円錐台と
  真円錐の面取りも閉形式で採点し、全体最悪 **5.26e-10**
- FreeCAD 1.1 / OpenCASCADE相互検証: **27/27**。円錐台面取りは体積差
  **9.176e-12**・面積差 **9.195e-12**・断面差 **1.556e-7**、真円錐面取りは
  体積差 **9.175e-12**・面積差 **9.223e-12**・断面差 **3.148e-8**
- `tools/fast_test.sh`: **169バイナリ / 589合格 / 0不合格 / 非ゼロ終了0**

円形の純立体・穴口・凹根元について両操作が揃いました。次は非円形根元です。
近い円へ置換せず、一定断面の楕円または直線区間を持つ根元から契約化してください。

### 4-102. 稜ブレンドの個別診断と矩形ボス根元の安全な拒否を固定した（2026/08/26）

非円形根元へ進む前に、40×40×20の台へ12×10×12の矩形ボスを和演算した実物で
現行経路を測りました。根元4稜はいずれも平面同士の直線稜ですが、内側二面角は
**270.000度**です。既存の単稜処理は凸稜を削る契約なので、制限だけを外すと材料を
足すべき凹角へ逆向きの面を作ります。また4稜を全周処理するには角のミターまたは
コーナーパッチを同時に解く必要があります。このため今回は近似形状を返していません。

- `EdgeBlender::blendability(&solid, edge_id)`を公開。形状を変更せず、成功時は
  `BlendableEdge`（二面角と許容寸法）、失敗時は実際の幾何判定理由を返す
- `blendable_edges`も同じ個別診断を使うよう一本化し、列挙と個別照会の判定ずれを防止
- 矩形ボス根元4稜がすべて「270.000 deg / only convex」で拒否され、候補一覧へ
  出ず、面取りを試しても入力Solidが不変で閉じたままであることを専用回帰で固定
- 直方体12稜では個別照会と従来の候補一覧が完全一致し、存在しないIDも理由付きで拒否
- 局所ブレンド回帰: **43/43**、`tools/fast_test.sh`: **170バイナリ / 591合格 /
  0不合格 / 非ゼロ終了0**

次に矩形根元を実装する場合は、1稜ずつ既存経路へ通さず、4稜の閉ループを認識して
角部を含め一括再構築してください。この4-102は、その実装前後を比べる基準です。

### 4-103. 段付き軸および円筒/矩形複合ボスに対する blendability 個別診断の回帰テスト拡充（Antigravity、2026/08/26）

4-102で新設された `EdgeBlender::blendability` APIについて、実務的形状である「段付き軸」および「円筒ボスと矩形ボスが共存するハイブリッドソリッド」に対する個別照会・列挙整合性・非破壊性を回帰テストとして拡充しました。

- **段付き軸モデル（大径r15×h20、小径r8×h15）**:
  - 凹肩エッジ4本（Z=20.0, r=8.0）が `blendability` で270度凹肩として認識され、正の最大フィレット半径/面取り距離を報告
  - 実際に1本の肩エッジをフィレットして完全閉マニホールドソリッドが生成されることを確認
  - 全ての `blendable_edges` 候補と `blendability` の直接照会結果が完全一致
- **円筒/矩形複合ボスモデル（直方体ベースの上に円筒ボスと矩形ボスが共存）**:
  - 円筒ボスの凹根元4稜は `blendability` で `Ok`（270度円形凹根元）
  - 矩形ボスの凹根元4稜は `blendability` で明確に拒否（"270.000 deg interior angle ... only convex edges are blended"）
  - `blendable_edges` の一覧に円筒根元のみが含まれ、矩形根元が確実に除外されることを検証
- **非破壊性の保証**: `blendability` 照会やエラー発生時に入力 `Solid` が一切変更されないことを確認
- **テスト数**: **591 ➔ 593**（`crates/zenith_algo/tests/edge_blend_diagnostic_test.rs` に2件追加、全テスト100%合格、失敗0）

### 4-104〜4-108 は、節として書かれていません（2026/08/27に確認）

第1章の表が **4-104・4-106・4-107・4-108** を出典として挙げていますが、
**この文書にその番号の節はありません**（`grep "^### 4-10[4-8]"` で0件）。
中身は第1章の本文と該当コミットに残っています。

**訂正**: この節を最初に書いたとき「第1章の表と `KERNEL_SPECS.md` が」と
書きましたが、**`KERNEL_SPECS.md` はこれらの番号を1度も引いていません**
（`grep -c "4-10[4-8]"` で0）。引いているのは HANDOVER 第1章だけです。

**出典として番号を書くなら、その節を先に書いてください。** 番号だけが増えると、
あとから来た人は「読み落とした」と思って探します。ここは訂正せず、事実として
残します（第1章の記述そのものは、プローブの実測と一致しています）。

**4-85 は欠番ではありません。** 見出しの付け方が違うだけで、
`#### 「4回」のほうを追いました（4-85）` として 4-83 の中にあります。節番号を
機械で拾うと落ちるので、ここに書いておきます。

#### 2026/08/28 に足した節は、全部あります（確認しました）

この日に 4-143〜4-148 と 9-G を足したので、**同じ取り違えを増やしていないか**を
確かめました。

```bash
for ref in 4-143 4-144 4-145 4-146 4-147 4-148; do
  echo "$ref: $(grep -c "^### $ref\." HANDOVER.md) 件"
done
```

**6つとも見出しが1件ずつあります。** 参照した古い節（4-33、4-63、4-74〜4-77、
4-82、4-86、4-116〜4-124、4-129、4-132〜4-142）も、**4-104 を除いて全部
実在します**——その 4-104 が、まさにこの節が言っている「番号だけある」もの
でした。

**引き継ぎ文書は、書き足すほど参照が壊れます。** 節を足したら、上のような
1行で確かめてください。

### 4-109. 抜き勾配が、指定した角度になっていませんでした（2026/08/27）

`ExtrudeBuilder::extrude_wire_with_draft` は、天面の輪郭を作るのに
**各頂点をワイヤの重心から放射状に `h·tanα` だけ動かして**いました。これは
オフセットではなく相似拡大です。

実測（40 × 25 の長方形、高さ 30、指定 3 度）:

| | 指定 | 実際 |
| :--- | ---: | ---: |
| 長辺側の面の勾配 | 3.0000° | **1.5910°** |
| 短辺側の面の勾配 | 3.0000° | **2.5446°** |
| 体積 | 33164.7318 | **32044.3158**（-3.38%） |

**どちらの面も指定した角度になっていません。** 抜き勾配は面が引抜方向となす
角のことなので、天面の輪郭は底面を**各辺の外向き法線へ `h·tanα` 押し出した
オフセット多角形**でなければならず、頂点が動く距離は `h·tanα / sin(θ/2)` で
**頂点ごとに違います**。

直したあとは、両方の面が 3.0000°、体積は閉じた式
`w·d·h + tanα·h²·(w+d) + (4/3)tan²α·h³` と**一致**（残差 0）。

**同じ形を作る経路が2つあって、片方だけ正しかった**という形の欠陥です。
`DraftBuilder::make_drafted_block` は最初から正しく、そちらには閉じた式の
テストが付いていました。付いていなかったほうが間違っていました。

いまは直線の辺だけを扱います。円弧を含むワイヤは、正しいオフセットが同心
円弧で、有理曲線の中間制御点は `δ/cos(θ/2)` 動く必要があり、法線方向へ一律に
動かしても合いません。**近い別の形を返さず、理由を返して失敗します。**

**見つかった経緯**: `volume > 0` としか書いていないテストを、閉じた式に
置き換えていて出ました。テストは3本ありました——
`draft_and_revolve_solid_test`（「直方体より大きい」）、
`sketch_to_feature_integration_test`（`volume > 0`）、
`feature_tree_extended_test`（`volume > 0`）。**3本とも通っていました。**

### 4-110. 断面が、面のすぐ手前で誤答を返していました（2026/08/27）

`SectionSlicer` は、頂点までの符号付き距離を**公差内なら 0 に丸めてから**符号
で分類します。切断平面が面と**ちょうど公差ぶん**離れていると、同じ平面に乗って
いるはずの頂点が丸め誤差だけで「平面上」と「正側」に割れます。割れた三角形は
「1辺が平面に乗っている」と見なされ、**面の内部で**輪郭の断片を出します。

実測（円 → 長方形 → 楕円の多断面ロフト、天面の 1e-6 下、32分割）:

| | 値 |
| :--- | ---: |
| 返ってきたループ数 | **16** |
| 返ってきた面積 | **38.0328** |
| 正しい楕円の面積 | 1413.7167 |

**128分割では「輪郭が閉じない」と断っていました。** 刻みで、断りと誤答が
入れ替わっていたことになります。

丸めの境目にかかった三角形（公差内の頂点と、公差の4倍までの頂点が同居する
三角形）が1枚でもあれば、**名指しで断る**ようにしました。4倍という帯は判断
です——実測の破れは公差ちょうどの所で起きていて、2倍でも足りますが、余裕を
取りました。

**締めすぎていないことも測りました。** 座標に丸め誤差の無い直方体は、公差
ぶん手前で切っても分類が割れないので、これまでどおり 600 を返します。

**ついでに分かったこと（直していません）**: 面の**上**ちょうどで切ると、
上下で答えが違います。

| 平面 | 返るもの |
| :--- | :--- |
| 底面ちょうど | 面そのもの（20×30 の箱で 600） |
| 天面ちょうど | **空の断面** |

「1辺が平面に乗った三角形は正側からだけ輪郭を出す」という、二重に数えない
ための規約から来ています。半開区間の切り方で、それ自体は筋が通っていますが、
**呼ぶ側から見ると罠**です。`section_slice_test` に測ったまま書き留めました。

### 4-111. 出力用メッシュと図面に、形を見る物差しがありませんでした（2026/08/27）

STL・OBJ・glTF・DXF の4つには、**形に依存する検査が1つもありませんでした**。

- Rust 側は固定文字列の有無だけ（`"LWPOLYLINE"` が入っているか、
  `"version": "2.0"` が入っているか）。
- `tools/verify_mesh_exports.py` は、検査関数を4つ定義したまま **`main()` から
  1つも呼んでいませんでした**。読む立体も置き場も無く、常に
  「All format validators loaded and verified.」と印字して 0 で終わります。
  **緑に見えるのに、何も見ていませんでした。**

`export_mesh_suite`（新設）が8検体を書き出し、台帳に B-Rep 側の体積・三角形数・
境界箱・断面積を残します。`tools/verify_mesh_exports.py` を書き直して、
**ファイルを解いた結果だけ**から次を見るようにしました。

1. 頂点を座標で束ねて、どの辺もちょうど2枚の三角形に共有されているか
2. 発散定理でメッシュから積んだ体積が、B-Rep の体積と合うか
3. STL・OBJ・glTF の3つが同じ体積を書いたか
4. glTF の accessor / bufferView / base64 の長さが互いに整合するか
5. DXF の `OUTLINE` / `HOLE` の本数と向きが、断面の外形数・穴数と合うか

FreeCAD は要りません。CI（`.github/workflows/gates.yml`）に入れました。

**ただし、これは「他人が読めた」ことの証明ではありません。** 解いているのは
こちらが書いた Python のパーサで、STL・OBJ・glTF・DXF の**実装が1つも入って
いません**。IGES が `tools/verify_iges.py` で OpenCASCADE に読ませているのと
比べると、そこが弱いところです。**次に強くするならここ**——glTF は
`gltf-validator`、DXF は FreeCAD の `importDXF`、STL / OBJ は OpenCASCADE の
リーダで読ませられます。3形式の体積が互いに一致することは見ているので、
「1つのエクスポータだけがずれている」は捕まりますが、「3つとも同じ規約違反を
している」は捕まりません。

**帯の取り方を1度間違えました。** 「曲面は内接するのでメッシュは小さく出る」
と決めて片側だけ締めたところ、穴あき直方体とブーリアン結果が **+6.9e-05 /
+5.5e-05** で赤くなりました。**内側へ食い込んだ面（穴のボア）では、穴のほうが
小さく刻まれるので立体は逆に大きく出ます。** 欠陥ではなく近似の向きで、
締める側を1つに決めたこちらの誤りでした。いまは両側で取っています。

### 4-112. DXF の層を、ループの順番で決めていました（2026/08/27）

`DxfExporter::generate_dxf_string` は「最初のループが外形、残りは全部穴」と
決めていました。**断面に外形が2つ以上出る形では、2つ目以降が `HOLE` 層に
落ちます。**

実測（60 × 20 × 20 の棒に、上から幅20の溝を深さ12まで掘り、溝の底より上の
z = 14 で切る）:

| | カーネルの言う断面 | DXF に出たもの |
| :--- | ---: | ---: |
| 外形 | 2 | **1** |
| 穴 | 0 | **1** |

向き（X-Y へ落としたときの符号付き面積）で決めるようにしました。
`SectionSlicer` は外形を反時計回り、穴を時計回りで返すので、これは**その場に
あった情報を使っていなかっただけ**です。`signed_loop_areas` は前から
返っていました。

### 4-113. `volume > 0` だけのテストを、閉じた式に置き換えました（2026/08/27）

8か所ありました。うち3か所が 4-109 の欠陥を通していました。置き換えたもの:

| テスト | 置き換えた先 |
| :--- | :--- |
| `shaft_keyway_test` | 段付き軸 − キー溝（円弧と弦で囲まれた断面の積分）。残差 1e-6 以内 |
| `draft_and_revolve_solid_test` | 抜き勾配角錐台の閉じた式＋**面ごとの勾配そのもの**。残差 1e-12 |
| `sketch_to_feature_integration_test` | 同上。帯は 1e-8（ソルバーが 1e-7 で止めるので、断面の辺長が厳密でない。実測残差 1.25e-10） |
| `feature_tree_extended_test` | フィーチャーツリー経由の抜き勾配ブロック。残差 1e-12 |
| `modeling_test`（掃引管） | 管の体積は**厳密に** `πr²L`。16区間で 7.03e-6、32で 1.52e-7、64で 1.67e-8、128で 1.02e-9 |
| `loft_solid_test`（角形） | 角錐台2つの積み重ね。平面だけなので刻みによらず厳密（1e-12） |
| `loft_solid_test`（円形） | 円錐台2つの積み重ね。残差 1e-5 以内。**メッシュ側は下からも締める** |
| `multi_section_loft_test` | 閉じた式が無いので、**断面積をシンプソン則で積み直して**発散定理と突き合わせる（5e-3）。2つの独立な積み方が合うことしか言えない、と明記 |

**残した1か所**: `sweep_wire_test`（三角形断面の3Dスイープ）。管の公式は円断面
にしか使えず、初等的な閉じた式がありません。

### 4-114. 統合ドキュメントの生成が、本文を抱え込み・行番号で切り・LaTeX を壊していました（2026/08/27）

`tools/generate_unified_pdf.py` に3つ問題がありました。**どれも、出来上がった
PDF を見ても分かりません。**

#### 置き場が直書きだった

```python
WORKSPACE_DIR = r"e:\CAD-Kernel"
```

**別の場所へ clone した人の手元では動きません。** ファイルの位置から決める
ようにしました。

#### 読み物がスクリプトの中に埋まっていた

巻頭言・各章の基礎理論・用語集・プローブ一覧が、1300行のスクリプトの中に
Python の文字列として直書きされていました。差分が「スクリプトの変更」に
見えるのでレビューに掛からず、他の文書と食い違っても気づけません。

[`docs/treatise/`](docs/treatise/) に Markdown として出し、スクリプトは
組み立てだけをするようにしました。

#### 章の切れ目が行番号の直値だった

```python
("HANDOVER.md", "Zenith CAD Kernel 開発引継書 本文", 1, 8349)
```

**文書が伸びると、末尾が黙って落ちます。** 2026年8月27日の実測:

| 文書 | 入っていた範囲 | 実際の行数 | 落ちていた行 |
| :--- | ---: | ---: | ---: |
| `HANDOVER.md` | 1〜8349 | 9012 | **663** |
| `FREECAD_VALIDATION_REPORT.md` | 1〜234 | 265 | 31 |
| `KERNEL_INVENTORY_SPECS.md` | 63〜205 | 228 | 23 |
| `KERNEL_SPECS.md` | 77〜149 | 152 | 3 |

9章（実用に耐えるまでに何が要るか）と10〜28章が丸ごと入っていませんでした。
見出しで切るようにし、見出しが見つからなければ止めます。直したあと、PDF は
5,204,029 → 5,530,854 バイトになりました。

#### LaTeX が Python の文字列エスケープに食われていた

本文が**素の**三重引用符文字列だったので、頭文字がエスケープと衝突する
LaTeX コマンドが解析時に消えていました。

| 書いたもの | PDF に載っていたもの |
| :--- | :--- |
| `\frac` | 改ページ文字 ＋ `rac` |
| `\approx` | ベル文字 ＋ `pprox` |
| `\begin{cases}` | バックスペース ＋ `egin{cases}` |
| `\text` / `\times` | タブ ＋ `ext` / `imes` |
| `\neq` / `\nabla` | **改行** ＋ `eq` / `abla` |

11か所ありました。NURBS の有理式、Cox-de Boor の漸化式、ツイスト不整合の
条件式、発散定理の式が**すべて壊れたまま**載っていたことになります。Markdown
に出したので、いまは普通に読めば分かります。

**この3つは、いずれも「出力が出来上がること」と「出力が正しいこと」を取り
違えた形の欠陥です。** 4-111 の非STEP出力と同じ一族で、PDF が出来るので
誰も疑いませんでした。

#### PDF の描画段は、この環境で繰り返し実行すると返ってきません（未解決）

**HTML までは確実に出ます。** `docs/treatise/` を直したあとの
`tools/unified_docs.html` は 1,005,621 バイトで、`\frac` 25 個・制御文字 0 個、
4-114 の記述まで入っていることを確認しました。

**PDF はこの日、1回目だけ出て、以後の再実行では返ってきませんでした。**
`QtWebEngineProcess` は生きていて 290〜300 MB を行き来しますが、
`printToPdf` まで進みません。`--disable-gpu` を渡しても同じでした。**原因は
特定できていません。** 1回目に出た PDF（5,530,854 バイト）は、行番号の欠落を
直したあと・LaTeX を直す**前**のものです。

呼ぶ側から見た手当ては簡単で、**一度で通らなかったら、残った
`QtWebEngineProcess` を落としてから掛け直す**ことです。次に触る人は、
`render_pdf_via_pyside` に待ち時間の上限（`QTimer` で `app.quit()`）を入れて、
返ってこないときに止まるようにするところから始めると良いと思います。
いまは黙って待ち続けます。


### 4-115. 統合 PDF / HTML の生成をやめました（2026/08/27）

`tools/generate_unified_pdf.py` と、その出力（`Zenith_CAD_Kernel_Documentation.pdf`、
`tools/unified_docs.html`）を削除しました。

4-114 で本文を [`docs/treatise/`](docs/treatise/) へ出した時点で、**束ねた
出力は「読むために必要なもの」ではなくなっていました。** 残していると、

- 描画段がこの環境で繰り返し実行すると返ってこない（4-114、原因未特定）
- 生成物は追跡していないので、手元にあるものが何時点の本文か分からない
- 見出しで切る指定を、文書の見出しを変えるたびに合わせ続ける必要がある

という保守だけが残ります。`.gitignore` の項目も消しました。束ねたものが要る
ときは、`docs/treatise/README.md` の該当節を消して作り直してください。

### 4-116. ブーリアンが割った曲面のメッシュ非多様体は、**解消していませんでした**（2026/08/27）

3-N-2b は「常設の7配置・21演算は B-Rep / mesh とも異常 0」「probe は mesh 異常も
exit 1 にするラチェットへ昇格した」と書いていました。**どちらも、いまは違います。**

実測（`contact_placement_probe`、24分割）:

| 配置 | union | difference | intersection |
| :--- | ---: | ---: | ---: |
| `box × cone (apex tilted 45)` | 3 | 0 | 14 |
| `box × torus (inclined 25)` | 165 | 123 | 122 |

ラチェットも**コードにありません**。`mesh_broken` は数えて印字するだけで、
`exit(1)` するのは B-Rep 側（`wrong`）だけです。4-104 で4配置を足したときに
mesh が赤になる配置が入り、記述だけが「解消済み」で残ったものと思われます。

**「表が緑になったこと」と「その主張が実装に残っていること」は別です。**
3-N-2 が言っているのと同じ取り違えを、この文書自身がやっていました。

#### 原因（1つ目。直しました）

面が2枚とも同じ稜を同じ刻み数で持っているのに、メッシュが開いていました。
稜の契約（`SamplePlan`）は守られています——**同じ位置で id が分かれている稜は
0 本**でした（測りました）。

開いていたのは**面の中**です。円錐を斜めに切った面では、共有稜の格子点が
4点以上そのまま一直線に乗ります。earcut はそこに面積 0 の耳を作り、

1. `repair_boundary_ears` は内部の対角線を入れ替えるだけなので、**同一直線上の
   4点には手が出ません**。入れ替えた先も同じ直線の上なので、潰れた枚数が
   減りません。加えて、隣の三角形も潰れているため `sign = area2(neighbour)` が
   0 になり、**向きが取れないという理由だけで**入口で諦めていました
2. 直らなかった耳は `push_with_uv_winding` が 3D 面積 0 で黙って落とします
3. 落ちた先で、**保護していたはずの境界辺が誰にも使われないまま残ります**。
   隣の面はその辺を持っているので、溶接しても閉じません

直したのは2か所です。

- 向きは隣の三角形ではなく**外周リングの符号**から取る（潰れた隣とも入れ替え
  られる）
- 潰れたままの耳を `drop_flat_boundary_triangles` で外し、点を「使われて
  いない」状態へ戻してから `reinsert_dropped_boundary_points`（4-86 の受け皿）に
  扇で張り直させる

外すのは **uv で面積 0 かつ3頂点が同じ境界リングに乗っている**ものだけです。
内部の頂点を含む三角形には触れません。

実測（24分割）:

| 配置・演算 | 前 | 後 |
| :--- | ---: | ---: |
| `box × cone` union | 3 | **0** |
| `box × cone` intersection | 14 | 12 |
| `box × torus` union | 165 | 134 |
| `box × torus` difference / intersection | 123 / 122 | 123 / 122 |

`mesh_watertight_probe` は 4〜32分割の全通りで open / 非多様体 / 退化とも 0 の
まま、`zenith_tess` のテストも通ります。

#### 原因（2つ目。未解決）

残りは**潰れていない**細長い三角形です。`repair_boundary_ears` が
`lies_on_one_boundary_edge`（曲面で、3頂点が同じ境界稜に乗る）で拾いますが、
実測の uv 面積は 2.6e-7〜3.0e-4 で、**0 ではありません**。外すと本物の面積が
消えるので、1つ目と同じ手は使えません。

観測できているのは、面が同じ共有稜の上で**細かい対と粗い弦の両方**を出して
いる状態です（`OWNERWHY` で稜の使用数 3、内訳は片方の面が2回・相手が1回）。

**次に触る人へ。** 守るべき不変量は「**面ごとに、保護した境界辺がちょうど
1回ずつ使われている**」で、これは各段のあとに測れます。いまは earcut・修復・
適応細分・emit と段が分かれていて、どの段で崩れたかを**段ごとには測って
いません**。まずそこに検査を置くのが早いと思います。

#### 診断の道具（この作業で足しました）

```bash
ZENITH_FACE_OWNER_WHY=1 cargo run --release -p zenith_algo --example contact_placement_probe
ZENITH_FACE_EDGES_WHY=1 cargo run --release -p zenith_algo --example contact_placement_probe
```

- `ZENITH_FACE_OWNER_WHY` — 溶接後の非多様体の稜を、**それを出した面の番号つき**で
  並べる。溶接して1枚のメッシュになったあとでは、どの面とどの面の間で開いて
  いるのかが分からないので、そこを名指しできるようにした
- `ZENITH_FACE_EDGES_WHY` — 面ごとの稜 id・刻み数・端点。稜の契約が守られて
  いるかを、推測せずに突き合わせるため
- `ZENITH_TESS_WHY` の `FLIPWHY` — 耳の入れ替えを**断った理由**。「直らなかった」
  だけでは、諦めた場所が分からない

### 4-117. 残りの非多様体は、溶接が三角形を潰して開けた穴でした（2026/08/27、未解決）

4-116 で1つ目の原因を直したあと、残っていたのは
`box × cone` intersection 12本、`box × torus` の3演算 134 / 123 / 122本です。

**これは面どうしの契約の話ではありませんでした。** `box × torus` の difference
（24分割）で `ZENITH_FACE_OWNER_WHY=1` を掛けると、123本のうち **121本を1枚の面
（342）が出しています**。しかも同じ面の中で辺を3回使ったり1回だけ使ったりして
います。境界のほうは健全で、`境界の連続対 146 のうち辺になっていないもの 0` です。

段ごとに測ると、崩れているのは**適応細分のあと**です。

| 段 | 三角形 | uv で面積 0 |
| :--- | ---: | ---: |
| earcut 直後 | 142 | 46 |
| 境界点挿し戻し後 | 144 | 46 |
| 極小 ear 修復後 | 144 | **0** |
| 適応細分後 | 32,624 | **784** |

そして**落としているのは emit ではなく `weld` です**。emit で落ちた三角形は
この演算全体で1枚だけでした（`EMITDROP` で数えました）。`weld` は
**頂点が潰れた 622枚**と**同じ3頂点の重複 106枚**を外しています。

```
TESSWHY weld が外した三角形: 頂点が潰れた 622、面積 0 0、同じ3頂点の重複 106、flap 3
```

#### 効かなかった手（記録として残します）

「割った先が溶接距離を下回る辺は割らない」という歯止めを細分に入れました。
**1枚も減りませんでした**（622 のまま）。潰れているのは**分割した辺ではなく、
uv では離れているのに 3D では溶接距離の中に来る頂点対**だからです。つまり
支持曲面のパラメータ付けがそこで縮退しています。歯止めは戻しました
（`WELD_TOLERANCE` の定数化だけ残しています——溶接距離が 2 か所に直書きされて
いたので）。

**推測で直そうとして外したので、そのまま書いておきます。**

**4-118 で主因（境界の重複点）を消したあと、同じ歯止めをもう一度測りました。
やはり1枚も減りません**（潰れ 188、非多様体 92 のまま。2026/08/27、傾けた
トーラス × 箱の和）。**細分が作る辺の細かさは主因ではない**と、これで2回
測って確定しました。次の人はここを再試行しないでください。

#### 次に測るとよさそうなこと

- 潰れた 622枚が**どの uv 領域に固まっているか**。1本の等パラメータ線に
  並ぶなら、支持曲面の縮退した行（極・継ぎ目）です
- 縮退した行では、扇として畳むのが正しい形です。`weld` が外したあとに
  **開くのか閉じるのか**を、その領域だけで測る
- 「同じ3頂点の重複 106枚」は別口かもしれません。同じ三角形が2回出るのは、
  面の三角形分割が自分自身に折り重なっているということです

診断の道具は入っています（`ZENITH_TESS_WHY` の `EMITDROP` と
`weld が外した三角形`、`ZENITH_FACE_OWNER_WHY`）。

### 4-118. 境界の点を落とす基準が、溶接の距離より 5 桁細かいままでした（2026/08/27）

4-117 の続きです。潰れた 622枚が uv のどこに固まっているかを測ったら、
**同じ uv に頂点が2つある**状態でした（uv 距離 < 1e-12。曲面の縮退ではありません）。

面342 の境界は、6稜 × 24分割で 144点のはずが **146点**あり、重なりは2組:

| 重なっていた場所 |
| :--- |
| `位置 0`（0本目の稜の先頭）と `位置 144`（リングの閉じ点） |
| `位置 24` と `位置 25`（同じ稜の 0点目と1点目） |

`boundary_rings` は、直前の点と **3D で 1e-12 以内**なら標本を落とします。
ところが後段の `weld` は **1e-7** で束ねます。**この5桁の隙間に入った標本は、
「境界の点としては別、溶接では同じ」になります。** 両方を使っている三角形は
溶接で潰れ、`weld` がそれを外し、外した跡が穴になります。

基準を `WELD_TOLERANCE` に揃えました。

**落としても継ぎ目は開きません。** どの点を落とすかは稜の幾何と刻み方だけで
決まり、隣の面も同じ稜を同じ割合で標本するので、**同じ点を落とします**。

**これは推論だったので、測りました。** 重複判定は「直前の点」としか比べない
ので、稜の**先頭の標本**では比較相手が面ごとに違う稜になります。面によって
落ちたり落ちなかったりすれば、そこが T 字になります。

実測（`ZENITH_DROP_WHY=1`、傾けたトーラス × 箱の和）: **落ちているのは
`step 0` だけ**でした（`step 0` 以外は 1 件もありません）。`step 0` は稜の
始点、すなわち前の稜の終点と同じ共有頂点なので、**比較相手はどちらの面でも
同じ点**です。食い違いません。

実測（`contact_placement_probe`、24分割）:

| 配置・演算 | 手を付ける前 | 4-116 後 | いま |
| :--- | ---: | ---: | ---: |
| `box × cone` union | 3 | 0 | **0** |
| `box × cone` intersection | 14 | 12 | 12 |
| `box × torus` union | 165 | 134 | **92** |
| `box × torus` difference | 123 | 123 | **2** |
| `box × torus` intersection | 122 | 122 | **1** |

`weld` が外す枚数も、difference で 潰れ 622 → **98**、同じ3頂点の重複
106 → **0** になりました。

#### 出力の外部検算

`export_mesh_suite` の8検体を書き直し、`tools/verify_mesh_exports.py` で
STL / OBJ / glTF / DXF から解き直して **8/8 通りました**（閉じているか・
体積・3形式の一致・DXF の層）。`mesh_watertight_probe` は 4〜32分割の
全通りで異常0、`zenith_algo` は全テスト 0 failed・警告0です。

#### 残っているもの（未解決）

残る潰れは **uv 距離 5.6e-9〜1e-8** の対で、リングの**離れた場所どうし**です
（例: `u = 0.0277778, v = 0.999999999` と `v = 0.999999997`）。3D では小数点
以下6桁まで同じ位置です。`boundary_rings` の重複判定は**直前の点としか
比べていない**ので、これは拾えません。

離れた点どうしを束ねるとリングが自分自身でつままれるので、**同じ手は使えま
せん。** 形としては、トリム境界が同じ曲線を往復している（幅0のスリット）
ように見えます。次に測るなら、その2点がリングのどの稜から来ているかです
（`ZENITH_COLLAPSE_WHY=1` に uv と 3D が出ます）。

`box × cone` intersection の 12本は、4-117 で見たとおり**潰れていない**細長い
三角形のほうなので、この一族とは別です。

### 4-119. 45ケースに無い置き方を4つ足したら、4つとも新しいものが出ました（2026/08/27）

3-N-2 が「同じ形を置き方だけ変えて測る、が今のところいちばん当たる手」と
書いているので、そのとおりにしました。`contact_placement_probe` に4配置
（12演算）を足しています。**4配置とも、何かしら新しいものが出ました。**

| 足した配置 | union | difference | intersection |
| :--- | :--- | :--- | :--- |
| `box × cylinder (both turned)` | **REFUSED** | **REFUSED** | **REFUSED** |
| `box × cone (seam spun then tilted)` | 1 | 4 | 0 |
| `box × torus (two axes)` | 11 | 7 | 5 |
| `box × sphere (at a corner)` | 0 | 0 | **48** |

（数字はメッシュの非多様体の稜。**B-Rep は全34件で 0 のままです**。）

probe は 25 → **34演算**（返るもの）、断られるもの 8 → 11 になりました。

#### いちばん重いのは「両方を回すと断られる」

**片側だけ回した配置は通ります。** 箱を 19 度、円柱を 27 度、**両方**回すと
3演算とも断られます。断り方の中身:

```
preparation reached 10 face-pair candidates, 8 intersection edges,
0 planar split candidates, 6 batch-split faces, 10 applied batch splits,
6 skipped batch splits, 0 classified split candidates, 14 selected face pieces,
0 cap loops, and 0 cap faces;
selected face stitching has 7 unmatched edge uses, 0 non-manifold edge uses,
and 0 same-direction edge uses
```

**交線は8本見つかっていて、面も10回割れています。** 閉じないのは、

- `6 skipped batch splits` — 割るべき面のうち6枚が割られていない
- `0 cap loops` / `0 cap faces` — 切り口に蓋が1枚もできていない
- `7 unmatched edge uses` — その結果、殻が7か所で開いている

**「交線が引けない」ではありません。** 引けたあとの、割る・蓋をする段です。
`ZENITH_REFUSE_WHY=1` で読めます（この作業で足しました）。

#### 球を角に当てると intersection だけ壊れる

`box × sphere (at a corner)` は union と difference が 0 本で、
**intersection だけ 48本**です。3枚の面が同時に切る配置で、面で切る上の
球の配置（4-82 など）はどれも1枚です。union / difference が綺麗なので、
これは交線側ではなく**取り出す領域の側**の問題に見えます。

#### 断り方を読めるようにしました

```bash
ZENITH_REFUSE_WHY=1 cargo run --release -p zenith_algo --example contact_placement_probe
```

断られた演算の理由をそのまま出します。**「断られた」だけでは、実装していない
のか、答えが非多様体なのか、途中まで行って閉じなかったのかが分かりません。**
上の内訳はこれで読みました。

### 4-120. 「両方を回すと断られる」の正体は、**円柱を斜めの平面で切ること**でした（2026/08/27）

4-119 で出た `box × cylinder (both turned)` の拒否を、`ZENITH_SPLIT_WHY=1` で
段まで追いました。**交線が引けないのではありません。** 8本引けています。

割れなかった理由の内訳（union、`ZENITH_SPLIT_WHY=1`）:

| 理由 | 件数 |
| :--- | ---: |
| `Split edge start does not lie on the outer boundary` | 6 |
| `Split edge end does not lie on the outer boundary` | 2 |
| `Split edge endpoints do not match cylinder side boundaries` | 4 |

前の2つ（平面側）は**片づいています**。1本ずつでは面の内側で終わる交線でも、
鎖にまとめ直す受け皿（4-63）が効いていました——実測で「交線 4 本 → 鎖 1 本」
にまとめ、**割れています**（それまで、この受け皿は失敗しても理由を
`Err(_)` で捨てていたので、効いているのかどうかも読めませんでした。読める
ようにしました）。

**残るのは円柱側です。** 内訳をもう一段開くと、

```
Cylinder-side split edge must be a ruling along the patch axis;
Section edge is not a circle about a[xis]
```

円柱の側面を割る口は、いま**2つしか受け付けません**。

1. 軸に沿った母線（ruling）
2. 軸まわりの**円**（軸に垂直な平面での断面）

**斜めの平面が円柱を切ると、断面は楕円です。** どちらでもないので断ります。

#### なぜ今まで出なかったか

`boolean_envelope` の45ケースも、`contact_placement_probe` の既存配置も、
**片側しか回していません**。円柱を回しても、箱の面が軸に平行なままなら、
断面は円か母線に収まります。**両方回した瞬間に、どちらにも収まらなくなり
ます。**

3-N-2 が「同じ形を置き方だけ変えて測る」を勧めていた理由が、そのまま出た形
です。

#### 次の一手（実装課題として）

**円柱の側面を、軸に斜めな平面で割れるようにする。** 受け付けるべき形は
**測ってあります**（`oblique_section_probe`、この作業で足しました）。

| 当てはめた形 | 残差の最悪（20通り） |
| :--- | ---: |
| `v = a·cos(u) + b·sin(u) + c` | 1.188e-2（**外れ**） |
| `v = (a + b·u + c·u²) / (1 + d·u + e·u²)` | **4.346e-12** |

**最初に立てた見立て（正弦）は外れました。** `u` は幾何的な角度ではなく、
有理2次の媒介変数です。円柱の側面は `x = X(u)/W(u)`、`y = Y(u)/W(u)`、`z = v`
（`X`、`Y`、`W` は `u` の2次式）なので、平面 `n·p = d` に代入すると

```text
v = (d·W(u) − n_x·X(u) − n_y·Y(u)) / (n_z·W(u))
```

——**分子も分母も `u` の2次式**です。測った残差はこれを裏づけます。

有理2次なら **p-curve として厳密に持てます**。割ったあとの境界が近似に
ならないので、いまの「円か母線か」の判定を「この形の曲線か」へ広げる形で
入ります。

これは配管の斜め切り・斜めフランジ・角度をつけた座ぐりなど、**実務で普通に
出てくる形**です。優先度は高いと考えます（ただしこれは判断で、実測では
ありません）。

**場所は特定してあります。** `crates/zenith_algo/src/brep_intersection.rs` に、
円柱の側面を割る口が2つあります。

| 関数 | 受け付けるもの | 断る行 |
| :--- | :--- | :--- |
| `split_cylinder_side_face_by_vertical_edge` | 軸に沿った母線（`u = const`） | `Cylinder-side split edge must be a ruling along the patch axis` |
| （断面の側。`fit_section_circle` を使う経路） | 軸まわりの円（`v = const`） | `Section edge is not a circle about an axis` |

**どちらも「片方のパラメータが一定」の線しか扱えません。** 楕円は
`v = a·cos(u) + b·sin(u) + c` で、u も v も動きます。3つ目の口として
「uv 領域を u 方向に横断する1本の曲線で割る」を足す形になります。

**この節を書いたとき、見立ては「正弦」でした。測ったら外れました。**
そのまま実装に入っていたら、合わない形を追いかけていたことになります。
4-33 と同じで、**カーネルを疑う前に自分の式を疑う**——今回は疑う前に測って、
式のほうが誤っていました。プローブは常設にしてあるので、次に触る人は
まずこれを走らせてください。

#### 診断

```bash
ZENITH_REFUSE_WHY=1 ZENITH_SPLIT_WHY=1   ZENITH_CONTACT_FILTER="both turned"   cargo run --release -p zenith_algo --example contact_placement_probe
```

### 4-121. 断り文の途中経過を、結論と読み違えていました（2026/08/27）

4-120 で「`box × cylinder (both turned)` が断られるのは、円柱を斜めの平面で
割れないからだ」と書きました。**測ったら違いました。**

読んでいたのはこの文字列です。

```
Cylinder-side split edge must be a ruling along the patch axis;
Section edge is not a circle about a[xis]
```

**これは受け皿を順に試したときの理由を `;` で連結したものです。**
`split_face_by_edge` は専用の口を2つ試し、駄目なら等パラメータ線の口、
それも駄目なら汎用の面分割器へ落ちます。エラー文には**落ちる途中で断られた
理由が全部並びます**が、最後の経路が通れば面は割れています。

実測（`ZENITH_SPLIT_WHY=1`、この作業で `CHAINWHY` を足しました）:

```text
CHAINWHY 汎用の鎖分割へ: 交線 2 本 → 鎖 1 本
CHAINWHY 汎用の鎖分割へ: 交線 2 本 → 鎖 1 本
```

断りの行は1つも出ません。**円柱の面は割れています。**

**エラー文が長いと、最初に目に入った具体的な一文を原因だと思い込みます。**
4-74 で「書いてあった原因は3件とも違いました」と同じ形です。**断り文は
経過の記録であって、結論ではありません。**

#### 本当の原因（1つ目。直しました）

止まっているのは**蓋の段**です。`0 cap loops`、`0 cap faces`、その結果
`7 unmatched edge uses`。

蓋の材料は、対の **`face_b` 側の面ごと**に束ねられていました。B が平面なら
それで合いますが、**B が曲面だと合いません**。実測（`ZENITH_CAP_TRACE=1`）:

```text
cap group face_b 0: 2 edge(s) in -> 0 loop(s), 2 skipped, 0 cap face(s)
cap group face_b 1: 2 edge(s) in -> 0 loop(s), 2 skipped, 0 cap face(s)
cap group face_b 2: 2 edge(s) in -> 0 loop(s), 2 skipped, 0 cap face(s)
cap group face_b 3: 2 edge(s) in -> 0 loop(s), 2 skipped, 0 cap face(s)
```

切り口の楕円は円柱の**4枚のパッチにまたがります**。パッチごとに束ねると
2本ずつに割れ、どの束も輪になりません。

**蓋が乗る平面は、対のうち平面のほうが決めます。** どちらが A でどちらが B
かは関係ありません。そう直しました（`build_planar_caps_grouped_by_planar_face`）。
両方が平面のときは従来どおり `face_b` を採るので、既に通っている配置は
動きません。実測でも `boolean_envelope` は supported 44 / wrong-result 0 /
エラー 1 のまま、仕事量は 56,340,279 → **56,240,775** とわずかに減りました。

```text
cap group face_a 0: 4 edge(s) in -> 0 loop(s), 4 skipped, 0 cap face(s)
cap group face_a 2: 4 edge(s) in -> 0 loop(s), 4 skipped, 0 cap face(s)
```

**束ね方は直りましたが、これだけでは通りません。**

#### 本当の原因（2つ目。未解決）

集まった4本を出してみると、**開いた鎖**でした。

```text
(11.9589 4.1178 0) -> (12 4.9047 0) -> (6 11.6387 0) -> (0 4.9047 0) -> (1.4596 0.5026 0)
```

端が繋がっていません。**切り口は、箱の面の自分の境界を通って閉じます**
——円柱が箱の面を貫き切らず、面の縁から出ているからです。

`collect_closed_intersection_edge_loops` は**交線だけで閉じる輪**しか作りません。
交線が3本以上あって端が一周したときだけ輪にします。ここでは一周しません。

#### そもそも蓋が要るのか（測りました）

**「輪が閉じない」の先を測ったら、欠けているのは蓋ではなく面の片でした。**

`ZENITH_STITCH_WHY=1` で相手を欠いている稜を出すと、7本が**1つの閉じた輪**を
なします。

```text
(11.9589 4.1178 0) -> (12 4.1319 1.5167) -> (6 2.0660 18.7876) -> (0 0 9.6261)
  -> (0 0 20) -> (18.9104 6.5114 20) -> (18.9104 6.5114 0) -> (11.9589 4.1178 0)
```

前半3本は交線の弧、後半4本は**箱の稜**です。つまりこれは、箱の側面のうち
円柱の外に残るべき領域の輪郭で、**その面片が採られていません**。

`ZENITH_SELECT_WHY=1`（この作業で足しました）で採否を出すと:

```text
SELECTWHY A 面 2 の片（2 枚中）: Outside → 採る 代表点 (0.6082 0.2094 0.8022)
SELECTWHY A 面 2 の片（2 枚中）: Inside  → 落とす 代表点 (9.4552 3.2557 10.0000)
```

**落とした判断は正しい**です。代表点 (9.4552, 3.2557, 10) は円柱の軸から
4.63 の距離にあり、半径 6 の内側です（手で確かめました）。

**分からないのはここからです。** 面 2 は 2 枚に割れ、片方は採られています。
それでも上の輪が相手を欠くということは、**割り方か、採った片の境界か、
相手の円柱側の片との対応**のどれかが合っていません。**ここは測り切れて
いません。**

次に測るための道具は入れました。`ZENITH_SELECT_EDGES=1` を `ZENITH_SELECT_WHY=1`
と併せて使うと、**採った片の境界の稜**が並びます。`ZENITH_STITCH_WHY` が出す
「相手を欠いている稜」と突き合わせれば、どの片から来た稜なのかが分かります。

```bash
ZENITH_SELECT_WHY=1 ZENITH_SELECT_EDGES=1 ZENITH_STITCH_WHY=1   ZENITH_CONTACT_FILTER="both turned" ZENITH_CONTACT_OP=union   cargo run --release -p zenith_algo --example contact_placement_probe
```

**蓋の輪を「交線 ＋ 面の境界の一部」で閉じられるようにする**案も残ります
（平面側の割り口には既に `wire_path_between` があります）。ただし上の実測を
見るかぎり、**この配置で欠けているのは蓋ではありません。**

#### 診断（この作業で足しました）

```bash
ZENITH_CAP_TRACE=1 cargo run --release -p zenith_algo --example contact_placement_probe
```

輪にならなかった束では、**材料の稜を端点つきで並べます**。「0 loops」だけでは、
端が繋がっていないのか、本数が足りないのか、向きの問題なのかが分かりません。

### 4-122. 安い物差しだけで、正しい割りを捨てていました（2026/08/27）

4-121 で「箱の側面の片が欠けている」ところまで測りました。**その先が分かり、
直りました。**

`ZENITH_SPLIT_WHY=1` に足した `LEFTWHY`（当たらなかった稜を鎖にまとめ直す
受け皿の、断り理由）で:

```text
LEFTWHY 当たらなかった稜 3 本（当たった割り 1）
LEFTWHY   残りを鎖に: 1 本
LEFTWHY   鎖 3 本: the splitting curve ends 1.110e1 away from the boundary
LEFTWHY   鎖 3 本: 片 2 枚、面積残差 2.176e-6 で採らず
```

**割れています。** 2枚に割れて、面積残差 2.176e-6 が閾値 `1e-6` をわずかに
超えたので捨てられていました。

`ZENITH_AREA_CHECK_WHY=1`（4-76 の道具）で並べると:

```text
AREACHECK split_by_chain  pieces 2  uv 2.176e-6 REFUSE  3d 4.317e-16 **3d differs**
```

**3D で測れば残差 4.3e-16。割り方は厳密に正しい**のに、パラメータ面積の
2.176e-6 だけで捨てていました。

#### なぜパラメータ側だけずれるのか

4-76〜4-77 で、面積の検算を 3D からパラメータへ移しました。**曲面を1回も
評価しないので速く、揃うところでは 3〜4 桁鋭く出ます。** それは今も正しい
です。

**ただし、面が導出した p-curve しか持たないときは、その忠実度がそのまま
残差に出ます。** 箱の側面を交線の鎖で割る今回がそれで、p-curve の忠実度
（1e-7 台）が相対 2e-6 として現れていました。

#### 直し方

**パラメータ側が断ろうとしたときだけ、3D で測り直します。**
（`residual_confirmed_in_3d`）。通る場合には走らないので、**4-77 で減らした
仕事は増えません**——実測でも `boolean_envelope` の曲面評価は 56,240,775 回で
変わりませんでした。

閾値 `1e-6` は呼ぶ側3か所に直書きされていたので、`AREA_RESIDUAL_LIMIT` に
まとめました。

**「断るときだけ」が本当かは測りました。** `ZENITH_CONFIRM_WHY=1` で、3D で
測り直した回数と、そのときのパラメータ残差が出ます。

| プローブ | 3D で測り直した回数 | 残差の内訳 |
| :--- | ---: | :--- |
| `boolean_envelope`（45ケース） | **3** | — |
| `contact_placement_probe`（45演算） | **6** | 2.176e-6 が3回、**1.414e0 が3回** |

**2種類がきれいに分かれています。** 2.176e-6 は今回直した取りこぼしで、
3D で測れば 4.3e-16 なので通します。1.414e0 は**本物の領域の取り違え**で、
3D で測っても駄目なので断ります。**緩めたのではなく、見分けられるように
なりました。**

曲面評価の総数も 56,240,775 回で変更前と**完全に同じ**です。

常設ゲートも全て exit 0 のままです（`foreign_boolean_probe` 90演算、
`chained_boolean_probe`、`cutter_placement_probe`、`closure_probe`、
`robustness_probe`、`mesh_watertight_probe`、`boolean_gate_probe`、
`curved_placement_probe`、`face_split_probe`、`ssi_probe`、`cone_slab_probe`、
`drilled_cut_probe`）。

#### 効果（実測、24分割）

| | 前 | 後 |
| :--- | ---: | ---: |
| `contact_placement_probe` で立体を返す演算 | 34 | **37** |
| 断られる演算 | 11 | **8** |
| `box × cylinder (both turned)` | 3演算とも REFUSED | **3演算とも ok**（union / difference は mesh も 0） |
| B-Rep 非多様体 | 0 | **0** |
| `boolean_envelope` | 44 / 0 / 1、6.68e-8 | **同じ** |

**4-119 で「いちばん重い」と書いた穴が、これで塞がりました。** 3-N-2c の
見立て（円柱を斜めの平面で割れない）は 4-121 で外れと分かり、本当の原因は
**安い物差しの取りこぼし**でした。

#### 教訓

**速くするために物差しを替えたら、替えた物差しが何を見落とすかを測って
おく必要があります。** 4-76 では「食い違いは 138回中 22回」と測り、方針の
穴として解決しました。**そのとき見えていなかった食い違いが、今回のように
あとから出ます。** `ZENITH_AREA_CHECK_WHY` を残しておいたおかげで、今回は
1回の実行で分かりました。**道具を残しておくのは効きます。**

### 4-123. 溶接で消える中点を、細分が作っていました（2026/08/27）

4-117 で「残る非多様体は、適応細分が作った三角形を `weld` が潰して外している」
ところまで測り、4-118 で境界の重複点を消しました。**その先が分かり、11演算 →
6演算まで落ちました。**

#### 誰と誰が束ねられているのか

`ZENITH_REFINE_WHY=1`（この作業で足しました）で、細分のあとに溶接距離の中へ
入る頂点対を、**境界の点か細分の点か**で分けて数えます。

```text
REFINEWHY 溶接で束ねられる対 5（境界どうし 0、境界と細分 0、細分どうし 5）、uv の隔たりは最大 9.821e-9
```

**すべて「細分どうし」でした。境界の点は1組も関与していません。**
uv の隔たりは 2e-9〜1.5e-8 です。

中点は辺ごとに1つですが、**ほとんど重なった別々の辺**が割れると、その中点
どうしが 3D で溶接距離の中に落ちます。

#### 外れた手（2つ。どちらも測って捨てました）

**1. 重なる中点を使い回す。** 既にある頂点が溶接距離の中にあるなら、新しく
作らずそれを使う——**悪化しました**（`box × torus (inclined 25)` の積で
1本 → **34本**）。使い回した頂点は**その辺の中点ではない**ので、細分の
三角形が壊れます。

**2. 同じ三角形の2頂点を溶接しない。** 「溶接は離れた片を繋ぐためのもので、
1枚の三角形を潰すのは仕事ではない」という理屈でしたが、**さらに悪化しました**
（メッシュが非多様体の演算が 11 → **25**）。**極や継ぎ目では、三角形が潰れる
のが正しい**からです。一律に禁じてはいけません。

4-117 の「割った先が溶接距離を下回る辺は割らない」と合わせて、**この一族で
外れた手は3つ**になりました。3つとも、外れた理由が違います。

#### 効いた手

**重なる中点は、使い回さず「その辺を割らない」。**

細分は品質のための段です。**割らなくても形は変わりません**（境界は保護されて
いるので継ぎ目も動きません）。中点を作らなければ `subdivide_triangle` は
そこを割らないので、それだけで済みます。

格子は**パスをまたいで持ち越します**。同じパスの中だけで突き合わせても
**1組も減りませんでした**——衝突する中点は別のパスで作られています
（使い回しが 100 回以上効いても、残る対は 5, 3, 2, ... のまま変わりません
でした）。

#### 効果（実測、24分割）

| 配置・演算 | 前 | 後 |
| :--- | ---: | ---: |
| `box × torus (two axes)` difference | 7 | **0** |
| `box × torus (two axes)` intersection | 5 | **0** |
| `box × torus (two axes)` union | 11 | **5** |
| `box × torus (inclined 25)` intersection | 1 | **0** |
| `box × torus (inclined 25)` union | 92 | **81** |
| `box × torus (inclined 25)` difference | 2 | 4（**悪化**） |
| `box × cone (seam spun then tilted)` union | 1 | **0** |
| **メッシュが非多様体の演算** | **11** | **6** |

`weld` が外す「頂点が潰れた三角形」は、`box × torus (inclined 25)` の積で
98 → 0 になりました。

#### 払った代価（正直に）

**`boolean_envelope` の曲面評価が 56,240,775 → 58,923,183 回（+4.8%）に
増えます。** 中点の位置を、細分の判定が評価していなかった辺についても
求めるようになったためです。4-77 で半分以下にしたぶんから 4.8% を返した
ことになります。

**格子に最初の頂点を入れない版も測りました。** 仕事量はほとんど変わらず
（58,769,054）、メッシュは 6 → 7演算と**悪くなった**ので、入れる版を採って
います。

#### 残っているもの

6演算です。大きいものは `box × cylinder (both turned)` intersection 102本、
`box × torus (inclined 25)` union 81本、`box × sphere (at a corner)`
intersection 48本、`box × cone (apex tilted 45)` intersection 12本。

**`box × cylinder (both turned)` の 102本は 4-122 で新しく通るようになった
配置なので、この一族かどうかも含めてまだ測っていません。** そこから
始めるのが良いと思います。

### 4-124. 残りのメッシュ非多様体は、**面どうしが部分的に重なって**いました（2026/08/27、未解決）

4-123 でメッシュ非多様体の演算は 11 → 6 になりました。**残った中でいちばん
大きいもの**（`box × cylinder (both turned)` intersection、102本）を測ったら、
これまでとは別の一族でした。

| `weld` が外した三角形 | 4-123 の前 | いま |
| :--- | ---: | ---: |
| 頂点が潰れた | 98 | **0** |
| 同じ3頂点の重複 | 0 | **34** |

**潰れは無くなりましたが、同じ3頂点の三角形が34枚落ちています。** 落ちた跡が
そのまま穴です。

`ZENITH_COLLAPSE_WHY=1` に足した `DUPWHY` と、`ZENITH_FACE_OWNER_WHY` が出す
面ごとの三角形の範囲を突き合わせると:

```text
DUPWHY 三角形 18025 は 152 と同じ3頂点
OWNERWHY   面 293 の三角形は 143..286
OWNERWHY   面 296 の三角形は 17944..18760
```

**面293 と面296 が、同じ三角形を出しています。** 面293 は箱（A）から、
面296 は円柱（B）から来た面片です（`ZENITH_SELECT_WHY` に片の id を出す
ようにしたので辿れます）。

#### **「面が重なっている」と書いたのは誤りでした**（2026/08/27 夕、訂正）

上の観測から「2枚の面片が部分的に重なっている」と結論しました。**測ったら
違いました。** 4-121 と同じ形の早合点です。

測ったのは2つです。

1. `ZENITH_OVERLAP_WHY=1`（この作業で足しました）で、**同一平面に乗る
   選択済みの面片の対**を出す。**1組も出ませんでした。** 面が面積として
   重なっているなら、少なくとも同一平面の対が出るはずです
2. `DUPWHY` に三角形の**位置**を出すようにした。重複していたのは:

   ```text
   (0.4793 0.1650 4.1261) (0.4108 0.1414 4.5425) (0.3477 0.1197 4.9573)
   (0.3477 0.1197 4.9573) (0.2902 0.0999 5.3698) (0.2380 0.0820 5.7795)
   ```

   **3点がほぼ一直線に並ぶ細長い片**です。しかも次の片の先頭が前の片の
   末尾と一致していて、**1本の曲線に沿って並んでいます**。

つまり**面積として重なっているのではなく、2枚の面が共有境界の上に同じ
細長い片を出している**——4-117 で「潰れていない細長い三角形」と書いた
一族です（`repair_boundary_ears` が `lies_on_one_boundary_edge` で拾うものの、
uv 面積が 2.6e-7〜3.0e-4 で 0 ではないため外せない）。

**直す場所はメッシュ側です。** 4-117 の「入れ替えても悪い枚数が減らない」
（`FLIPWHY`）を解くところから始めてください。

#### 次に触る人のための実測（2026/08/27 夕）

`box × cylinder (both turned)` intersection、24分割で測りました。

**直せなかった耳は 211 個**で、uv 面積の内訳は:

| uv 面積 | 個数 | 扱い |
| :--- | ---: | :--- |
| 面積 0 とみなせる（≤ 2e-14） | 25 | `drop_flat_boundary_triangles` が外す（4-116） |
| 1e-14 〜 1e-6 | 18 | 外すと本物の面積が減る |
| **1e-6 以上** | **168** | 同上。ここが本体 |

**入れ替えを断った理由は 3113 件すべて同じでした。**

```text
FLIPWHY ... 悪い枚数が減らない 2 -> 2
```

「対角が既にある」も「面積が変わる」も**1件も出ません**。つまり
**入れ替える相手が常に同じく悪い三角形**で、`new_bad >= old_bad` で必ず
止まります。4-117 に書いた「鎖になっている」という見立てを、これが裏づけ
ます。

**次に試すなら、鎖の端から解くことです。** いまの条件は「1回の入れ替えで
悪い枚数が減ること」ですが、鎖の途中では必ず 2 → 2 になります。鎖の端
（片側だけが悪い三角形と接するところ）から入れ替えれば 2 → 1 になるはず
なので、**そこを先に探すように順序を変える**のが素直だと思います。
コード側には既に「1枚を残して先へ進める」意図が書かれていますが、
**実測では1度もそこへ届いていません**。

#### なぜ受け皿に掛からないのか

重なった面片を落とす受け皿は既にあります（`resolve_coincident_face_pieces`）。
掛からない理由は**判定が「面全体の一致」だから**です。

```rust
// 同じ領域か。境界サンプルの重心と広がりで判定する。
if (right_centre - left_centre).norm() > scale * 1e-6 {
```

**重心と広がりで見ているので、部分的な重なりは捕まりません。** 面296 の
三角形は 816 枚あり、そのうち重なっているのは 34 枚だけです。

#### 残り6演算は、**すべて同じ一族**でした（測りました）

4-123 のあと残っていた6演算を、`weld` が外した内訳で並べます。

| 配置・演算 | 頂点が潰れた | 同じ3頂点の重複 | flap |
| :--- | ---: | ---: | ---: |
| `box × torus (inclined 25)` union | **0** | 38 | 7 |
| `box × torus (inclined 25)` difference | **0** | 2 | 2 |
| `box × cone (apex tilted 45)` intersection | **0** | 4 | 0 |
| `box × cone (seam spun)` difference | **0** | 1 | 0 |
| `box × torus (two axes)` union | **0** | 3 | 3 |
| `box × sphere (at a corner)` intersection | **0** | 16 | 0 |
| `box × cylinder (both turned)` intersection | **0** | 34 | 0 |

**「頂点が潰れた」は全件 0 です。** 4-123 で潰れの一族は完全に解消しました
（それまでは1面で 622枚、98枚と出ていました）。

残りは全件が**重複三角形と flap**、つまり**面片の重なり**です。2件で面まで
辿って確かめました。

| 配置 | 重なっている2枚 |
| :--- | :--- |
| `box × cylinder (both turned)` intersection | 面293（箱から）と面296（円柱から） |
| `box × sphere (at a corner)` intersection | 面294 と面299 |

どちらも**別々の面**で、1枚の面の中の折り重なりではありません。

#### 次の一手

**共有境界の上に出る細長い片を、両方の面が出さないようにする。**
`repair_boundary_ears` は既にこれを拾いますが（`lies_on_one_boundary_edge`）、
入れ替えても悪い枚数が減らないため直せません（`FLIPWHY`）。4-116 で
「面積0の耳」に対してやったのと同じ形——**外してから張り直す**——が使えるか、
まず測ってください。**ただし面積が 0 ではないので、外すと本物の面積が
消えます。** そこが 4-116 との違いです。

**この段落も、上の訂正で無効になりました。** ブーリアン側の欠陥ではなく、
メッシュ側（境界の上の細長い片）です。誤った推論として残しておきます
——**面の番号だけで「重なっている」と決めてはいけません。** 同じ稜を共有
する2枚の面は、その稜の上に同じ点を持つので、**同じ三角形を出すことが
あります**。

#### 診断（この作業で足しました）

- `ZENITH_COLLAPSE_WHY=1` の `DUPWHY` — 同じ3頂点の三角形を出した2枚を、
  溶接前の三角形の添字で言う
- `ZENITH_FACE_OWNER_WHY=1` — 面ごとの三角形の範囲も出すようにした。
  `DUPWHY` の添字をこれと突き合わせると、**1枚の面の中なのか2枚の面に
  またがるのか**が分かる
- `ZENITH_SELECT_WHY=1` — 採った片の **id** も出すようにした。テッセレー
  ションの面番号と突き合わせるため

### 4-125. 鎖の端から解いたら、6演算が1演算になりました（2026/08/27）

4-124 の最後に「次に試すなら鎖の端から」と書きました。**そのとおりでした。**

境界の上に残る細長い三角形は、1枚ずつ独立には出ません。境界に沿って**鎖**に
なります。`repair_boundary_ears` は「1回の入れ替えで悪い三角形の枚数が減る」
ことを条件にしていますが、**鎖の途中を選ぶと入れ替える相手も同じく悪い**ので、
2 → 2 のままで必ず止まります。

実測（`box × cylinder (both turned)` の積、24分割）: 断った理由は
**3113 件すべて「悪い枚数が減らない 2 → 2」**で、「対角が既にある」も
「面積が変わる」も1件も出ませんでした。**一度も端に当たっていませんでした。**

直したのは**選ぶ順序だけ**です。悪い三角形のうち、**相手が良い三角形である
もの**（鎖の端）を先に選ぶ。端が無ければ従来どおり最初の1枚を選びます。
入れ替えの条件も面積の検査も、1文字も変えていません。

#### 効果（実測、24分割）

| 配置・演算 | 4-123 の後 | いま |
| :--- | ---: | ---: |
| `box × torus (inclined 25)` union | 81 | **0** |
| `box × torus (inclined 25)` difference | 4 | **0** |
| `box × sphere (at a corner)` intersection | 48 | **0** |
| `box × cone (apex tilted 45)` intersection | 12 | **0** |
| `box × cone (seam spun)` difference | 4 | **0** |
| `box × torus (two axes)` union | 5 | **0** |
| `box × cylinder (both turned)` intersection | 102 | **30** |
| **メッシュが非多様体の演算** | **6** | **1** |

`box × cylinder (both turned)` の union と difference も 0 になりました。

**この一族は、着手前の 4配置・11演算・最大165本から、1演算・30本まで
落ちました。**

#### 代価は測ってあります

`boolean_envelope` は supported 44 / wrong-result 0 / エラー 1、解析解との
最悪一致 6.68e-8 で不変。曲面評価は 58,923,183 → **58,923,292 回**（+0.0002%）
——**選ぶ順序を変えただけなので、仕事量はほぼ動きません。**

`mesh_watertight_probe` は 4〜32分割の全通りで異常0、全ワークスペースの
テストは 104 バイナリ・608 テスト・0 failed です。

#### 残る1件（測ってあります）

`box × cylinder (both turned)` intersection の 30本だけです。**同じ一族**
でした（`weld` が外したのは「同じ3頂点の重複」10枚だけで、潰れは 0）。

入れ替えを断った理由も変わりました。

| 理由 | 4-125 の前 | いま |
| :--- | ---: | ---: |
| 悪い枚数が減らない 2 → 2 | 3113 | **364** |
| 面積が変わる | 0 | 1 |
| 直せなかった耳 | 211 | **36** |

**残っているのは「端が1つも無い」塊です。** `ZENITH_TESS_WHY=1` に足した
行で数えると、9枚 → 8枚 → 7枚 → 6枚 → 5枚 → 4枚 と、**1枚ずつしか
減りません**。悪い三角形が閉じた輪になっているか、周りが全部悪い塊に
なっていて、どこを選んでも 2 → 2 になります。

**次に試すなら、そういう塊に限って 2 → 2 の入れ替えを1度だけ許すこと。**
ただし `new_bad >= old_bad` を緩めると**循環の危険**があります（コードの
注記にもそう書いてあります）。入れ替えた辺を覚えて二度目を禁じる、
といった歯止めが要ります。**そこを付けずに緩めないでください。**

---

### 4-126. 端の無い塊に、歯止め付きで横移動を1度だけ許したら 0 になりました（2026/08/27）

4-125 の最後に書いたとおりにやりました。**そのとおりでした。**

残っていたのは `box × cylinder (both turned)` intersection の 30本だけで、
理由は「悪い三角形に端が1つも無い」——閉じた輪または全周が悪い塊なので、
どこを選んでも `2 → 2` になり、`new_bad < old_bad` では一歩も動けません。

直したのは3つだけです。

1. **端が1つも無い round に限って**（`allow_lateral`）、`old_bad == 2` かつ
   `new_bad == 2` の入れ替えを許す。端がある間は従来どおり「必ず減る」入れ
   替えしか通しません。
2. 横移動で**消した辺と作った辺の両方**を `lateral_taboo` に入れる。
3. `lateral_taboo` にある辺は、以後どの入れ替えでも共有辺として選ばない。

**これが循環しない理由**は数えれば分かります。横移動は1回につき辺を恒久的に
使い切り（消した辺は二度と選べず、作った辺も二度と横移動の対角にならない）、
辺の本数は有限なので、横移動の回数は頭打ちです。戻す道が無いので
「入れ替えて、戻して、また入れ替える」が起きません。面積の検査（`old_area`
と `new_area` の一致）も、対角の重複検査も、1文字も変えていません。

#### 効果（実測）

| | 4-125 の後 | いま |
| :--- | ---: | ---: |
| `box × cylinder (both turned)` intersection | 30本 | **0本** |
| **メッシュが非多様体の演算** | 1 | **0** |

`contact_placement_probe`: 37 件が立体を返し 8 件が断られ、**B-Rep 非多様体
0 件・メッシュ非多様体 0 件**。

#### 代価は測ってあります

- `boolean_envelope`: supported 44 / wrong-result 0 / エラー 1 で不変。解析解
  との最悪一致 6.68e-8 で不変。曲面評価 58,923,292 → **58,911,524 回**
  （−0.02%）。**選ぶ順序と歯止めを足しただけなので、仕事量はほぼ動きません。**
- `mesh_watertight_probe`: 全検体で open 0 / non-manifold 0 / degenerate 0。
- `cargo test --release --workspace --exclude zenith_py`: **119 バイナリ・
  632 テスト・0 failed・警告 0**（exit 0）。数え方は「`^running` の行数」と
  「`test result:` 行の passed 合計」です（1章の数え方の食い違いは、この
  数え方だと 119 / 632 のほうに一致しました）。

#### 残っていること

**これは全立体の証明ではありません。** `contact_placement_probe` の 25配置・
45演算という**実測範囲**です。断られる 8演算（曲面どうしの交差、球の極）は
そもそもメッシュまで行っていないので、この一族が本当に閉じたかは、置き方を
増やして測り直すまで分かりません（3-N-2）。

---

### 4-127. 代表点が、たまたま接触点そのものでした（2026/08/27）

4-126 の最後に「置き方を増やして測り直すまで分からない」と書いたので、
そのとおりにしました。3-N-2 が名指ししている「同じ形を置き方だけ変えて
測る」です。**触っているだけの置き方**を5つ足しました。

| 足した置き方 | 触り方 |
| :--- | :--- |
| `box × sphere (touching at one point)` | 球の最下点が箱の上面に乗る。**1点** |
| `box × cylinder (cap coplanar with a face)` | 円柱の底の円板が箱の上面と同一平面。**面** |
| `box × cone (apex on a face)` | 円錐の頂点が箱の上面に乗る。**1点（特異点）** |
| `box × torus (top tangent plane)` | トーラスの上端が箱の上面と一致。**半径 12 の円** |
| `box × cone (generatrix in a face)` | 母線1本が箱の面の中にまるごと乗る。**線** |

15演算のうち **4演算が「未実装」として断られました**。

#### 断り文を読んだら、断り方が2種類ありました

`ZENITH_REFUSE_WHY=1` で見ると、`box × torus (top tangent plane)` の差だけは
**場所を名指しして**断っていました——`(19.290809 17.594792 20.000000) で材料が
2つに割れる`。**これは規約どおりで、直す対象ではありません。**

残る4演算（円錐の2ケースの積、トーラスの和と積）は「まだ実装していない」
という断り方で、あぶれた稜の本数だけが出ていました。

#### 円錐のほうは、測ったら**箱に内接**していました

推測せずに範囲を測りました。円錐の AABB は `(0,0,0)-(20,20,20)` で、**箱と
完全に一致**します。底面円が4つの側面に接し、頂点が上面に乗っています。
**真の積は円錐そのもの**で、多様体です。断るのは実装の不足でした。

`ZENITH_SELECT_WHY=1` で採否を見て、原因が1行で出ました。

```
SELECTWHY A 面 1 の片 id 2 （1 枚中）: Boundary → 採る 代表点 (10.0000 10.0000 20.0000)
```

**箱の上面の代表点が、たまたま円錐の頂点そのものでした。** 代表点は「面の
中でいちばん縁から遠い点」なので、上面の中心 (10,10,20) です。そこが唯一の
接触点だったので、上面 1枚が `Boundary` と判定され、積に採られ、その4辺が
あぶれていました。

**1点で触れていることは、重なっていることではありません。**

#### 直したこと

`classify_face_against_mesh` で `Boundary` が出たときに、面の中の点を
**散らして9点**取り直し（`spread_face_points`）、相手の表面の近くにある点が
半分に満たなければ `Boundary` を取り下げて、離れている点の多数決で
`Inside` / `Outside` を決めます。

**下げる方向にしか効かせていません。** 最初の点が相手から離れているときは
この検査を通さないので、**`Inside` / `Outside` が新たに `Boundary` に変わる
ことはありません**。いま通っている経路の判定は動かない、という作りです。

#### 効果（実測）

| | 前 | いま |
| :--- | ---: | ---: |
| `box × cone (apex on a face)` intersection | REFUSED | **ok**（体積 2094.395102） |
| `box × cone (generatrix in a face)` intersection | REFUSED | **ok** |
| あぶれた稜（円錐・頂点） | 4 | **0** |
| `contact_placement_probe` 立体を返す演算 | 37 | **49**（20配置・60演算） |
| 同 B-Rep / メッシュ非多様体 | 0 / 0 | **0 / 0** |

**体積は閉じた式に一致しました**——積 2094.395102 は $\pi r^2 h/3$ そのもの、
和は 8000（箱）、差は 8000 − 2094.395102。`volume > 0` では捕まらない欠陥
だったので（誤って上面を採った積にも体積はあります）、閉じた式で押さえて
います（`contact_placement_test.rs` に2本追加）。

#### 代価は測ってあります

- `boolean_envelope`: supported 44 / wrong-result 0 / エラー 1 で不変、最悪
  一致 6.68e-8 で不変。曲面評価 58,911,524 → **58,919,293 回**（+0.013%）。
  追加の点判定は**最初の1点が相手の表面の近くにあったときだけ**走るので、
  ほとんど増えません。
- 既存 45演算の採否は 1件も動いていません（37 ok / 8 refused のまま。増えた
  12 ok / 3 refused はすべて新しい置き方です）。

#### 残っていること

`box × torus (top tangent plane)` の**和と積**は、まだ「未実装」で断られます
（あぶれた稜 48・非多様体の稜 24／あぶれた稜 56）。円全体で接するので、
円錐の点接触より難しい形です。**差のほうは規約どおりの断りなので、そこは
触りません。**

---

### 4-128. 曲面どうしのブーリアンが通りました — 止まった場所が合っていなかっただけでした（2026/08/27）

**`cylinder × cylinder (orthogonal cross)` と `sphere × cylinder (eccentric
intersection)` の6演算が、全部 `ok` になりました。** どちらも「曲面どうしの
交差は未実装」として断られていた組です。配管のT字はいちばん普通に出てくる
形なので、実用性としてはここが大きい穴でした。

#### 何が起きていたか

あぶれた稜を名指ししたら、1画面で分かりました。

```
piece 5 (A): (-6,0,-6) -> mid (-3.87627,-4.57979,-3.87627) -> ( 0.00000,-6, -0.00002)
piece 17(B): (-6,0,-6) -> mid (-3.87627,-4.57979,-3.87627) -> (-0.00002,-6,  0.00000)
```

**中点は完全に一致しています。同じ交線です。** ずれているのは**端だけ**で、
真の接点 `(0,-6,0)` からどちらも 2e-5 離れていました。位置が違うので稜が
突き合わず、16本があぶれて「未実装」と断られていました。

2e-5 という数は、`ssi_probe` が前から表の下に書いていたものです——「等半径の
円柱では、**曲線から 2.99e-5 離れた点でも両曲面を 2.24e-11 で満たす**」。
4-81 と同じ機構（重根なので残差が位置を決めない）です。

**なぜ等半径だけが落ちるのか。** 半径が違えば交線は滑らかな閉じた輪1本で、
接点はできません（半径 10 と 6 の交差は前から通っています）。**等しいときだけ
交線が2本の楕円になり、それが2点で交わります。** その交点では両曲面の法線が
平行なので、交線の向きが決まりません。

#### 直したこと（2つ）

**1. 接点そのものに着地する**（`newton_to_tangency`、`zenith_geom`）

辿りは正弦が `TANGENCY_SINE_LIMIT` を切ったところで `break` していました。
**止まるだけで、接点には着いていません。** 4-82 と同じ形で解き直します——
3本は `p1 = p2` のまま、4本目を「2つの法線の外積を、**入口の接線向きへ
落とした量**が 0」にします。

**長さではなく符号つきの量にするのが肝です。** `tangent` が返す正弦は外積の
長さなので、接点で 0 に触れて折り返すだけ——重根です。向きを1つ固定して
落とすと接点を境に符号が変わるので、**単根**になり倍精度まで詰まります。

届かなければ何も足しません（従来どおりそこで終わるだけ）。歩幅の2倍より
遠くへ飛んだ解も採りません（別の接点です）。

**2. 長さ 0 の稜を、輪から落とす**（`remove_degenerate_wire_edges`、`zenith_algo`）

端が接点に着くようになると、**行って戻る長さ 0 の稜**が輪の中に残りました
（実測: 和で1本が1回、1本が3回使われて非多様体）。両端が同じ点なので、
落としても輪は繋がったままです。輪が2本未満になる場合は触りません——そこまで
潰れているなら別の欠陥なので、隠さずに残します。

#### 効果（実測）

| | 前 | いま |
| :--- | ---: | ---: |
| 直交2円柱 縫合のあぶれ / 非多様体 | 16 / 0 | **0 / 0** |
| `cylinder × cylinder (orthogonal cross)` 3演算 | REFUSED | **3件とも ok** |
| `sphere × cylinder (eccentric intersection)` 3演算 | REFUSED | **3件とも ok** |
| `contact_placement_probe` 立体を返す演算 | 49 | **55**（20配置・60演算） |
| 同 断られる演算 | 11 | **5**（うち**3件は規約どおりの名指し拒否**） |
| 同 B-Rep 非多様体 | 0 | **0** |

**答えは閉じた式に乗ります。** 等半径（6）の直交2円柱の交わりは Steinmetz
立体で、体積は $16R^3/3 = 1152$ です。

| 演算 | 実測 | 閉じた式 | 相対差 |
| :--- | ---: | ---: | ---: |
| union | 7895.786925 | 7895.786842 | 1.05e-8 |
| difference（**2立体**） | 3371.893505 | 3371.893421 | 2.50e-8 |
| intersection | 1151.999915 | 1152.000000 | 7.40e-8 |

差が2立体になるのは正しい答えです——切り手の半径が同じなので、A は帯を
まるごと失って上下に割れます。

#### 代価は測ってあります

- `boolean_envelope`: supported 44 / wrong-result 0 / エラー 1 で不変、最悪
  一致 6.68e-8 で不変。曲面評価 58,919,293 → **58,913,502 回**。
  **マーチングのニュートン反復は 1,232,730 回で1回も動いていません**
  ——45ケースには接点で終わる交線が1本も無いので、足した解きは一度も
  走りません。**通っている経路には触れていない、という実測です。**
- `ssi_probe` 3/3 clean、`mesh_watertight_probe` 全検体 open 0 / non-manifold
  0 / degenerate 0。
- `foreign_boolean_probe`（軸に平行、90演算）: ok 26 / NOCUT 4 / 拒否 0 /
  WRONG 0 / PANIC 0、恒等式の最悪残差 **4.32e-9**。**切り手を 27 度傾けた
  180演算**（`ZENITH_TILTED=1`）: ok 53 / NOCUT 7 / 拒否 0 / WRONG 0 /
  PANIC 0、残差 **7.88e-9** ——4-67 / 4-68 に記録されている値と**同じ**です。
- 全ワークスペースのテスト: **119 バイナリ・635 テスト・0 failed・警告 0**
  （exit 0）。

#### 残っていること

- `sphere × cylinder (eccentric intersection)` の**差**は、B-Rep は多様体
  ですが**メッシュが 13本非多様体**です。4-126 で閉じた一族の続きで、
  **この演算はいままで断られていたのでメッシュまで来ていませんでした**
  ——退行ではなく、新しく見えるようになった場所です。

  **場所は測ってあります。13本すべてが球の継ぎ目の1点に集まっています。**

  ```
  TESSWHY mesh non-manifold edge uses 3: (10.000000000,0,-0.000054622) -> ... length 4.879e-1
  TESSWHY mesh non-manifold edge uses 1: (10.000000000,0, 0.000004500) -> (10.000000000,0,0.000004235), length 2.647e-7
  TESSWHY mesh non-manifold edge uses 1: (10.000000000,0,-0.000054622) -> (10.000000000,0,-0.000056997), length 2.375e-6
  ```

  半径 10 の球の `(10, 0, 0)` は**継ぎ目（u = 0）の上**です。そこに `z` が
  **±5e-5 の幅に散った頂点**が並び、溶接の距離より広いので束ねられず、
  長さ 2.6e-7 の稜が1回だけ使われて残ります。**5e-5 という幅は、4-128 で
  直したのと同じ $\sqrt{\varepsilon}$ の桁です。** 継ぎ目の上で交線の端が
  同じように決まりきっていない可能性がありますが、**そこまでは測って
  いません**。次に触る人は、まずこの13本が**交線の端**なのか
  **テッセレーションの標本**なのかを分けてください——`ZENITH_TESS_WHY=1`
  の行だけでは、どちらとも読めます。

  → **分けました。4-129 にあります（B-Rep 側でした）。**
- `box × torus (top tangent plane)` の和と積は、まだ未実装で断られます。
- **これは全配置の証明ではありません。** 通ったのは測った2組で、曲面どうしの
  交差一般が通るようになったとは言えません。次は置き方を増やして測って
  ください（3-N-2）。

---

### 4-129. 継ぎ目の13本は B-Rep 側でした — ただし直せていません（2026/08/27）

4-128 の最後に「交線の端なのか、テッセレーションの標本なのかを分けて
ください」と書きました。**分けました。B-Rep 側です。**

#### 測ったこと

`sphere × cylinder (eccentric)` の差の結果から、`(10,0,0)` の 1e-3 以内に
ある **B-Rep の頂点**を数えると、**3つ**あります。

```
(10.000000000000, 0, 0.000000000000)     ← 真の点
(9.999999999998,  0, 0.000006352922)     ← 6.35e-6 ずれ
(9.999999999838,  0,-0.000056996766)     ← 5.70e-5 ずれ
```

メッシュの49頂点は、この3点のあいだに張られた**長さ 2.6e-7 の稜を刻んだ
標本**でした。つまり**テッセレーションは悪くありません。**

`(10,0,0)` が何なのかも測りました。球（半径 10、原点）の法線は `(1,0,0)`、
円柱（半径 6、軸 `(4,0)`）の法線も `(10-4,0,0)/6 = (1,0,0)` で、**平行です**
——**接点**です。しかも球のパッチの角（`u` の境界）でもあります。

交線の候補を面の組ごとに出すと、そこで終わる組が4つありました。

| 面の組 | 端の位置（真の点からの距離） | どう終わったか |
| :--- | ---: | :--- |
| a6 × b3 | 1.29e-7 | `tangency`（接点へ着地。4-128 の解き） |
| a7 × b3 | 1.05e-9 | 同上 |
| a0 × b0 | **5.70e-5** | `boundary`（縁に着いて終わり） |
| a1 × b0 | **6.35e-6** | 同上 |

**4-128 で入れた接点への着地は効いています**（下2桁が 1e-9〜1e-7）。効いて
いないのは、**縁に歩いて乗ったところで止まった**2組です。`at_edge` で
止まるとき、**縁に着いたことは確かでも、縁の「どこに」着いたかは
$\sqrt{arepsilon}$ でしか決まっていません。**

#### 試して、戻したこと（**3つとも実装には入っていません**）

1. **4-82 が名指ししていた、もう1つの着地口（`run()` の `!inside(settled)`
   側）に極値の解を入れる。** → `LANDWHY(B)` が**1度も出ませんでした**。
   この配置ではその口を通りません。**確かめられないものは入れない**ので
   戻しました（4-82 が同じ理由で入れなかったのと同じ判断です）。
2. **縁に着けなかったときの受け皿として、接点への着地を足す。** → 6回
   発火し、`1.29e-7 → 5.27e-9` のように**小さい数は動きました**が、
   **13本は 13本のまま**で、`box × torus (top tangent plane)` にも効かず、
   **結果を1つも変えませんでした**。戻しました。
3. **`at_edge` で止まるとき、接点のそばなら解き直す。** → 悪い端は
   `5.70e-5 → 7.56e-15` になりましたが、**もう1組が `6.35e-6` から
   `3.64e-5` へ悪化**し、**3演算とも `REFUSED` に退行しました**。戻しました。

**3つ目は、部分的に良くなった数字だけ見て採ってはいけない例です。** 端の
1つが 15桁合っても、別の端が動いて縫合が壊れれば、演算そのものが通らなく
なります。**採否は「結果が通ったか」で見てください。**

#### 次に触る人へ

分かっているのはここまでです。

- **直す場所は `at_edge` で止まる経路**です（`ssi_march.rs` の `run()`）。
  ただし 3 のやり方（その場で解き直す）は退行します。
- **4組がそれぞれ別々に解いている**のが根にあります。同じ接点で終わる弧は
  同じ点に着くべきなので、**面の組ごとに解かず、接点を1度だけ解いて全部の
  弧をそこへ着地させる**ほうが筋かもしれません。**測っていません。**
- 効果は `ZENITH_CONTACT_FILTER="eccentric"` の**メッシュ非多様体の本数**で
  見てください。B-Rep の頂点が3つ→1つになれば 0 になるはずですが、
  **それも測っていません。**

---

### 4-130. 接触の記録を消すと、断れなくなります（2026/08/27）

残る未実装は `box × torus (top tangent plane)` の和と積の2演算だけだったので、
そこを見ました。**機構は分かりました。直し方は間違えました。**

#### 分かったこと

あぶれた稜を名指しすると、2つの兆候が出ました。

1. **トーラスの上端の接触円が、交線として作られていました。** トーラス
   （中心 z=16、管半径 4）の z の範囲は 12〜20 で、箱の上面 z=20 を
   **一度も横切りません**。上端の半径 12 の円で触れているだけです。
   実測: あぶれた稜のうち `(-2,10,20) → (-1.526, 6.662, 20)` は、
   $(x-10)^2+(y-10)^2 = 143.8 pprox 144 = 12^2$ で**円の上**でした。
2. **その円が箱の側面を横切る点が、両側で食い違っていました。**
   真の値は $y = 10 - \sqrt{144-100} = 3.3667504$ です。A 側は
   `3.36655` と `3.36743`、B 側は `3.36661`（しかも別の座標に 9e-5 の
   ずれつき）。**1e-4 の桁**で、4-128 / 4-129 と同じ族です。

#### やってみて、戻したこと

**「端から端まで接しているだけの交線候補を落とす」を入れました**
（`edge_is_a_tangential_contact`）。規約 3-1 は「接触は、それ自体では位相を
作らない」なので、筋は通っています。

効果もありました。

| | 前 | 入れた後 |
| :--- | ---: | ---: |
| トーラスの和 縫合の非多様体 | 24 | **0** |
| 同 あぶれ | 48 | 36 |
| `contact_placement_probe` 断られる演算 | 5 | **3** |

**この「5 → 3」に飛びついてはいけません。** 減った2件を開いたら、
`box × cylinder (tangent)` の**差**が `ok` になっていました。**そこは
断るのが正しい行です**——壁の厚みが 0 になって材料が2つに割れるので、
多様体 B-Rep では表せません。プローブの冒頭にも「ここが `ok` に変わったら
誤答です」と書いてあります。

**接触の記録は、断るための証拠でした。** それを入口で落とすと、割れることが
検出できなくなり、**もっともらしい立体が返ります**。断るより悪い結果です。

常設テスト `a_tangent_contact_that_pinches_the_material_is_refused_by_name`
がそのまま落ちました。**ゲートが効いた実例なので、ここに残します。**

#### 次に触る人へ

- **入口で落とす方向はやめてください。** 接している曲線が「材料を割るか」は
  下流の非多様体判定が決めています。判定より前に消すと、判定できません。
- 落とすなら**判定のあと**、割らないと分かった接触だけです。**どこでそれが
  分かるかは、まだ見ていません。**
- 残る 1e-4 のずれ（`3.36655` / `3.36661` / `3.36743`）のほうは、4-129 と
  同じ「縁に着いたが、縁のどこかが決まっていない」です。`LANDWHY(A)` は
  ここでも **72 回中 72 回「extremum did not converge」**でした。
  `newton_to_bound_extremum` が接点の近くで解けていないのが根です。
  **中心差分の標本が領域の外へ出ているせいかと思って片側差分にしましたが、
  1件も変わりませんでした**（これも戻しました）。`bound_speed` が
  `tangent()` 経由なので、接点の近くで向きが雑音に埋もれているほうが
  疑わしいですが、**測っていません。**

---

### 4-131. 曲面どうしが「触れている」置き方を5つ測りました（2026/08/27）

4-130 の最後に「次に測るなら」と3つ書いたので、そのとおりにしました。
今日これで3回目です——**この手はまだ当たります。**

| 足した置き方 | 触り方 | 結果 |
| :--- | :--- | :--- |
| `sphere × sphere (externally tangent)` | 点（曲面どうし） | 3演算とも正しい |
| `cylinder × cylinder (tangent along a line)` | 線（曲面どうし） | **断り文がおかしい** |
| `sphere × cylinder (tangent along the equator)` | 円（曲面どうし） | 3演算とも正しい |
| `cylinder × cylinder (crossed, nearly equal radii)` | 交わる（寸法比だけ変える） | 3演算とも正しい |
| `torus × cylinder (through the tube)` | 交わる（どちらもパッチ） | **メッシュが大きく壊れる** |

#### 正しかったもの

- **球2つを外接**させると、和は**立体2つ**を返します。規約どおり
  「接触はそれ自体では位相を作らない」で、1点で触れていても繋ぎません。
  差は A そのまま、積は立体 0 です。
- **球と同軸で同半径の円柱**（赤道の円まるごとで接する）は、和が円柱、
  差が立体 0、積が球。球が円柱に含まれている、という答えとして正しい。
  **4-130 のトーラスと同じ「曲線に沿った接触」ですが、相手が曲面だと
  通ります。** 落ちるのは平面が相手のときでした。
- **半径をわずかに変えた直交2円柱**（6 と 5.9）は3演算とも通ります。
  4-128 の「**等半径だけが落ちていた**」の裏取りです。

#### 断り文がおかしい（新しい欠陥）

`cylinder × cylinder (tangent along a line)` の**和**が断られます。
半径 6 の円柱2本を 12 離すと、母線 `x=6, y=0` の1本で触れます。
**真の和は非多様体です**——その1本を4枚の面が共有するので、多様体 B-Rep
では表せません。**断るのは正しい。**

**問題は断り方です。** 断り文は「まだ実装していない」と言っています。

```
Exact B-Rep boolean is not implemented yet; ... selected face stitching has
0 unmatched edge uses, 20 non-manifold edge uses, ...
```

**あぶれた稜は 0 で、非多様体の稜が 20 と、そこまで測れています。** つまり
「答えが非多様体だ」と分かっているのに、「未実装」と言っています。

**この区別はこのリポジトリの規約そのものです**（プローブの冒頭）。
「未実装」の断りは直したぶんだけ `ok` に変わりますが、「答えが非多様体」の
断りは**変わってはいけません**——変わったら誤答です。いまの文面だと、次に
読む人が「実装すれば通る」と思って**通してしまう**危険があります。
`box × cylinder (tangent)` の差は場所を名指ししているので、**同じ扱いに
できるはず**です。

#### 断り文がおかしい理由も測りました

断り方を直す前に、**なぜ名指しできていないのか**を測りました。名指しの
断りは [`crate::contact`] が出します——交線のまわりに円周を置いて、材料が
いくつに分かれているかを数える仕組みです。`ZENITH_CONTACT_WHY=1` を付けても
**1行も出ませんでした**。測る前に降りています。

交線の候補を長さつきで並べたら、理由が出ました。

```
tol.linear = 1.000e-6
intersection edge candidates: 8
  a0 b4 length 3.161e-4  (6.0000, 0.0000,-20.0000) -> (6.0000, 0.0003,-20.0000)
  a0 b5 length 5.197e-4  (6.0000, 0.0000, 20.0000) -> (6.0000, 0.0005, 20.0000)
  ... （8本とも同じ）
```

**8本とも、蓋の縁（z = ±20）にある長さ 3e-4〜5e-4 の破片です。**
**触れている母線そのもの（x=6, y=0, z が -20 から 20 まで、長さ 40）は、
交線として1本も作られていません。**

作られない理由は 4-130 と同じです。**そこは端から端まで接しているので、
法線の外積が消え、マーチングが進めません。** 交線が無ければ、円周を置く
場所もありません。

`pinch_along_edge` は円周の半径を**稜の長さ × 1e-2** で取り、
`tol.linear × 100 = 1e-4` 以下なら測りません。破片は 3e-4 なので半径は
3e-6 で、**この足切りに掛かります。** 測らないので `None` が返り、
「未実装」の文面に落ちます。

**つまり、断り文を直すには接触曲線そのものを作れる必要があります。**
文面だけ書き換えるのは、測っていないことを測ったように言うことになるので、
やっていません。

#### メッシュが大きく壊れる（新しい欠陥）

`torus × cylinder (through the tube)`（トーラスの穴 8・外 16 の中を半径 9 の
円柱が通り、管に食い込む）は、**B-Rep は3演算とも多様体**ですが、
**メッシュが非多様体 74 / 108 / 64 本**です。今日いちばん大きい数字です。

**どちらもパッチに割れている曲面どうし**という、いちばん重い組み合わせです。
4-126 で 0 まで落とした一族が、この形では戻っています。

**場所も機構も測りました。** 悪い稜はすべて `z = ±2.645751311`、軸からの
距離 9 の上にあります。$\sqrt{7} = 2.6457513110645906$ で、これは
**円柱と管の交わりの円そのもの**です（$(9-12)^2 + z^2 = 16 \Rightarrow
z^2 = 7$）。

4回使われている稜の三角形を開くと、正体が出ます。

```
mesh non-manifold edge uses 4: (0.588624685,-8.980730534,2.645751311)
                            -> (0.390884339,-8.991507651,2.645751337)
  triangle  82620, third (-0.000000000,-9.000000000,2.645751311), area 1.267e-3
  triangle  82621, third ( 0.390884483,-8.991507715,2.645751416), area 1.061e-8
  triangle 141683, third ( 0.194635771,-8.997895138,2.645751285), area 4.260e-4
  triangle 141684, third ( 0.390884479,-8.991507622,2.645751101), area 2.344e-8
```

**まともな三角形が2枚と、面積 1e-8 の薄片が2枚**です。薄片の第3頂点は
稜の端そのもので、違うのは `z` だけ——`2.645751101` / `2.645751285` /
`2.645751311` / `2.645751337` / `2.645751416` と、**3.2e-7 の幅に5つ**
並んでいます。

**溶接の距離は `WELD_TOLERANCE = 1e-7` です。3.2e-7 はその外側なので
束ねられません。** 束ねられないまま残った薄片が、稜を3回・4回使います。

**溶接を緩めるのは、この文書が名指しで止めている手です**（`WELD_TOLERANCE`
のコメント。「ここより細かい三角形は溶接で潰れ、`weld` が外す。外した跡は
そのまま穴になる」）。緩めれば別のところが潰れます。

**「上流だろう」と書きかけて、測ったら違いました。**

最初は「同じ円を面ごとに別々に当てはめているので値がずれている」と見ました
（4-80 に「同じ弧を共有していても、面ごとに別の `Edge` の実体を持つことが
ある」とあるので）。**測ったら、B-Rep は正確でした。**

```
edges on the circle: 24, distinct positions: 12
  2 uses, faces [17, 18], z spread 0.000e0: ["-2.645751311065", "-2.645751311065"]
  2 uses, faces [10, 18], z spread 0.000e0: ["-2.645751311065", "-2.645751311065"]
  ... （12箇所とも同じ）
sqrt(7) = 2.645751311065
```

**24本の稜は12箇所にちょうど2本ずつ、値は `±√7` の倍精度そのもの、
ずれは 0 です。** B-Rep は1ビットも狂っていません。

**散らばりはテッセレーションが作っています。** 面の境界の点は稜から来るので
正確ですが、**面の中を割るときに作る点**が、境界の点から 1.7e-7 のところに
落ちています（実測: 薄片の第3頂点 `(0.390884483, -8.991507715, 2.645751416)`
と稜の端 `(0.390884339, -8.991507651, 2.645751337)` の距離）。
`WELD_TOLERANCE = 1e-7` のすぐ外側なので束ねられず、面積 1e-8 の薄片が
残ります。

これは 4-123（「溶接で消える中点を、細分が作っていました」）と同じ場所の、
**潰れずに生き残るほう**です。

#### 直しました（4-131 の続き）

**溶接は緩めていません。** 緩める手は `WELD_TOLERANCE` のコメントが名指しで
止めています。触ったのは**細分が新しい点を作らない距離**だけです
（`REFINEMENT_CLEARANCE`）。

歯止め自体は 4-123 で既に入っていました。**しきい値が `WELD_TOLERANCE`
ちょうど（1e-7）だったので、1.7e-7 のこの対には掛かりませんでした。**
境界の点は稜から取った 3D 点で上書きされるので、面の中の評価とはこの桁で
ずれます。**そのずれより広く取る**必要がありました。

**割らずに置くだけなので、形は変わりません。** 細分は品質のための段です。

倍率は**測って決めました**。1つの値で決めないのは、このリポジトリで実際に
やらかした失敗だからです（4-34）。

| `WELD_TOLERANCE` の倍率 | `torus × cylinder` 和/差/積 | `sphere × cylinder` 和/差/積 |
| ---: | :--- | :--- |
| 1（手を付ける前） | 74 / 108 / 64 | 0 / 13 / 0 |
| 2 | 1 / 21 / 15 | 3 / 13 / 0 |
| 4 | **0 / 0 / 0** | **3** / 13 / 0 ← 和が悪化 |
| **8** | **0 / 0 / 0** | **0 / 13 / 0** |

**4 で止めていたら、片方を直して片方を壊していました。** 2 は元より悪い
ところがあります。**「良くなった数字だけを見て採らない」がここでも要りました**
（5章）。

残るのは `sphere × cylinder (eccentric)` の**差**の 13本だけです。あれは
4-129 で測ったとおり**別の機構**（B-Rep 側で継ぎ目に頂点が3つ）なので、
ここでは動きません。

#### いまの状態

`contact_placement_probe` は **25配置・75演算**になりました。

| | 4-128 の後 | いま |
| :--- | ---: | ---: |
| 立体を返す演算 | 55 | **69** |
| 断られる演算 | 5 | **6**（うち**4件は規約どおり**、未実装は2件） |
| B-Rep 非多様体 | 0 | **0** |
| メッシュ非多様体の演算 | 1 | **1**（見つけた時点では 4。同じ日に3つ直しました） |

**増えた演算は、いままで測っていなかった形です。退行ではありません。**

---

### 4-132. **測定そのものが揺れていました**（2026/08/27）

**この節は、今日いちばん重いものです。** 直した欠陥より先に読んでください。

#### 見つかり方

まだ1件も無かった曲面の組み合わせ（円錐×円柱、円錐×円錐、円錐×球、
トーラス×球、トーラス×トーラス）を5つ足して測っていたら、**さっき 0 だった
ケースが 6 になっていました**。

疑って、同じバイナリ・同じ入力で `contact_placement_probe` を通しで2回
走らせました。

```
< sphere x cylinder (eccentric intersection) union   ok  1   0 / 6
> sphere x cylinder (eccentric intersection) union   ok  1   0 / 0
```

**89演算は完全に一致し、1演算だけが実行ごとに変わりました。**

#### 原因

`surface_tess.rs` の細分に、こう書いてありました。

```rust
let mut split_edges: HashSet<(usize, usize)> = HashSet::new();
...
for edge in split_edges {          // ← ハッシュの反復順
    ...
    // ここで weld_grid を見て「作るか、割らずに置くか」を決め、
    // 作ったら weld_grid を更新する
}
```

**中点を1つ作るたびに格子が変わり、次の判定がそれを見ます。つまり結果は
順序に依存します。** `HashSet` の反復順は Rust の既定のハッシャがプロセス
ごとに種を変えるので、**実行のたびに変わります。**

**4-131 で歯止めを 1e-7 から 8e-7 に広げたことで、この順序依存が見える
ようになりました。** 衝突が増えたぶん、順序で分かれる場面が増えたからです。
**広げたこと自体は正しく、隠れていた欠陥が出ただけです。**

もう1つ、**通し実行と単独実行でも結果が違いました**（単独では 5回とも 0）。
こちらは `zenith_topo` の `FACE_ID_GEN` などが**プロセスで通し番号**なので、
前に何ケース走ったかで `id` が変わり、ハッシュの分布が変わるためです。
**根は同じで、「ハッシュの反復順が答えを決めていた」**という1つの欠陥です。

#### 直したこと

添字で並べてから回すだけです。

```rust
let mut split_edges: Vec<(usize, usize)> = split_edges.into_iter().collect();
split_edges.sort_unstable();
```

**細分の結果は変わりません**（同じ辺の集合を、決まった順で処理するだけ）。
変わるのは「実行をまたいで同じ答えになる」ことです。

**確かめました。** `contact_placement_probe` を通しで**3回**走らせて、
**3回とも1バイトも違いません**（`diff` が空）。直す前は同じことをすると
1演算が違いました。

#### 同じ日に足した組み合わせ（4-132 の入口になったもの）

| 足した組み合わせ | 結果 |
| :--- | :--- |
| `cone × cylinder (crossed)` | 3演算とも多様体 |
| `cone × cone (apex to apex)` | 和は**立体2つ**。点接触は繋がない。正しい |
| `cone × sphere (inscribed, tangent along a circle)` | **差で検証ゲートが誤答を捕まえた**（**4-147 で解決**。差 1981.297767 が真値と一致し、空洞1つを持つ立体が返る） |
| `torus × sphere (biting the tube)` | 3演算とも多様体 |
| `torus × torus (crossed axes)` | **3演算とも未実装として断られる**（未解決） |

**`cone × sphere` の差**は、体積が **A + B**（2207.492438 =
2094.395102 + 113.097336）になっていました。円錐の中に球が入っているので
差は 1981.298 のはずです。**球が凹（空洞）ではなく凸として足されています。**
`BooleanResultVerifier` が捕まえて断ったので誤答は出ていませんが、**選び方か
向きのどちらかが間違っています。** ここは体積が閉じた式で書けるので、
直したかどうかがすぐ分かります。

**`torus × torus`**（軸を直交させ、芯の円が `(±12,0,0)` で交わる）は、
交線16本・あぶれ14本・非多様体0 です。あぶれた14本は**端点のずれでは
ありません**——4-128 のときと違って中点がまったく別で、**相手のいない弧**
です（面片が足りない）。すべて接点 `(±16,0,0)` / `(±8,0,0)` に接して
います。2つのトーラスはそこで**接している**（法線が両方 `(1,0,0)`）ので、
交線がその点を通ります。

同じ形の欠陥が他に無いかも見ました。`zenith_tess` と `brep_intersection` の
`HashMap` / `HashSet` は 30 箇所ありますが、**反復しているのはここだけ**で、
残りは `contains` / `get` / `entry` です。

#### なぜこれが重いのか

**このリポジトリは「実測を書く」で回っています。** 揺れる測定を実測として
書くと、次に読む人が再現できません。**今日の 4-116 以降の数字も、この
ケースについては書いた時点の実行の値でした。**

`boolean_envelope` は仕事量に「deterministic; compare these across runs,
not the seconds」と書いてあります。**そう書いてある以上、揺れるものを
残してはいけません。**

**ゲートとしても壊れています。** CI が実行のたびに違う答えを出すなら、
赤も緑も意味がありません。1演算だけとはいえ、**そこが赤になる日と
ならない日がある**ということです。

---

### 4-133. 空洞の向きが揃っていませんでした — 「円錐から中の物を引く」が通らなかった（2026/08/28）

4-132 で足した `cone × sphere (inscribed)` の**差**が、検証ゲートに落ちて
断られていました。体積が **A + B**（2207.492438 = 2094.395102 + 113.097336）
です。差なのに球が足されています。

#### 接触は引き金ではありませんでした

「内接（接している）から失敗するのだろう」と思って、**触れないところまで
小さくして測りました。**

| 配置 | 結果 |
| :--- | :--- |
| `box - sphere` (r=5、完全に内側) | **ok**、7476.401224（＝ 8000 − 523.6）|
| `cone - sphere` (r=1.5、**触れてもいない**) | **REFUSED**、2108.53 = A + B |
| `cone - sphere` (r=2.70 / 2.97 / 内接) | **REFUSED**、いずれも A + B |
| `box - sphere` (r=10、6面に接する) | **REFUSED**、12188.79 = A + B |

**「円錐から中の立体を引く」が一般に通っていませんでした。** 接触は
関係なく、**箱と小さい球の1通りだけが通っていた**という状態です。

#### 原因

入れ子そのものは正しく作られていました（立体1つ・空洞シェル1つ）。
**空洞シェルの向きだけが逆**でした。

| 配置 | 空洞シェル単体の符号つき体積 |
| :--- | ---: |
| `box - sphere` (r=5) | **+523.598776**（外向き） |
| `cone - sphere` (r=1.5) | **-14.137167**（内向き） |
| `box - sphere` (r=10) | **-4188.790205**（内向き） |

`MassCalculator::compute_from_brep` にはこう書いてあります——「空洞シェルは
外殻と同じ向きで保持されるため、**寄与を反転して足す**」。**その約束が
守られていませんでした。** 差の B 側は面を反転して採るので
（`reverse_orientation`）、空洞になる塊は内向きで出てくることがあります。
内向きのまま入れると**符号が2回変わり**、`A - B` が `A + B` になります。

**コメントに書いてある約束は、検査ではありません。** 書いてあったのに
守られておらず、通っていたのは1通りだけでした。

#### 直したこと

`nest_cavity_shells_into_solids` で、空洞にする塊の符号つき体積が負なら
**面を反転してから入れます**。形は変わりません——同じ面を、約束どおりの
向きにするだけです。

並べ替えも直しました。**符号ではなく大きさで並べます**——内向きの塊は
符号つき体積が負なので、符号で並べると「いちばん小さい」ことになって、
入れ子の親子が取り違えられます。

#### 効果（実測）

**6配置すべてが閉じた式に一致しました。**

| 配置 | 実測 | 閉じた式 |
| :--- | ---: | ---: |
| `cone - sphere` (内接 r=3) | 1981.297767 | 1981.297767 |
| `cone - sphere` (r=2.97) | 1984.656871 | 1984.656871 |
| `cone - sphere` (r=2.70) | 2011.947145 | 2011.947145 |
| `cone - sphere` (r=1.5) | 2080.257935 | 2080.257935 |
| `box - sphere` (r=5) | 7476.401224 | 7476.401224 |
| `box - sphere` (r=10、接する) | 3811.209795 | 3811.209795 |

`contact_placement_probe` は **81 ok / 9 refused**（前は 80 / 10）。
通しで2回走らせて1バイトも違いません。

#### 代価は測ってあります

- `boolean_envelope`: supported 44 / wrong-result 0 / エラー 1、最悪一致
  6.68e-8、曲面評価 **58,905,657** ——**直す前と1つも違いません**。
- `foreign_boolean_probe`: ok 26 / NOCUT 4 / 拒否 0 / WRONG 0 / PANIC 0、
  残差 4.32e-9。`mesh_watertight_probe` 全検体クリーン。
- `boolean_contained_cavity_test.rs` に4本追加。**`volume > 0` では
  捕まらない**ので（`A + B` にも体積はあります）、全部閉じた式と、
  **空洞シェル単体の符号が正であること**で押さえています。

#### 総当たりで掃きました

「1通りだけ通っていた」ものだったので、**総当たりで測り直しました**——
外側4種（箱・円柱・球・円錐）× 内側4種（箱・円柱・球・円錐）× 3演算 =
**48通り**。内側はどれも原点まわりの小さい立体で、**どこにも触れません**。

**48通りすべてが閉じた式に一致しました**（和 = 外側、差 = 外側 − 内側、
積 = 内側）。`boolean_containment_matrix_test.rs` として常設にしました
（実測 24秒）。

**この表は直す前に落ちることを確かめてあります。** 向きを揃える1行を
`if false` にして走らせると、円錐が外側の差が軒並み落ちます
（`refused: ... difference volume 16805.426302 exceeds operand A
16755.160819` など）。**緑を信用せず、落ちることを見てから採りました。**

---

### 4-134. **同じ立体どうしの和が、誤答を返していました**（2026/08/28）

4-133 の総当たりが効いたので、**同じ手をもう2軸に当てました**——「完全に
離れている」と「まったく同じ立体どうし」。5種（箱・円柱・球・円錐・
トーラス）× 3演算です。

#### 離れているほうは 15/15 でした

和は立体2つ・体積が2つぶん、差は A そのもの、積は立体0。**全部正しい。**

#### 同じ立体どうしは、3/15 しか正しくありませんでした

| 形 | 和 | 差 | 積 |
| :--- | :--- | :--- | :--- |
| 箱 | ok | ok | ok |
| 円柱 | REFUSED | REFUSED | REFUSED |
| **球** | **誤答（立体2つ・体積 1047.19755 = 2倍）** | REFUSED | REFUSED |
| 円錐 | REFUSED | REFUSED | REFUSED |
| **トーラス** | **誤答（立体2つ・2842.44607 = 2倍）** | REFUSED | REFUSED |

**`A ∪ A` が、重なった立体を2つ返していました。** 検証ゲートも通ります
——和の体積は A より大きく、内外判定の384点もすべて一致するからです
（重なった2つの立体は、どの点から見ても「中は中」です）。**位相だけが
違い、位相を見る検査がここにはありませんでした。**

#### 原因は2つありました

**1. 曲面の重なりが、一度も解消されていませんでした。**

`coincident_face_direction` は最初の数行で `FaceGeometry::Plane` 以外を
`None` にしていました。つまり**同じ場所に乗る曲面の片を落とす処理は、
平面にしか掛かっていません**。

**2. そもそも「相手の境界の上にある」と判定できていませんでした。**

`classify_face_against_mesh` は、点と**相手のメッシュ**との距離で
`Boundary` を決めます。**平面ならメッシュは厳密ですが、曲面は弦で近似
されている**ので、面の上の点でも 1e-4 より離れます。

実測（同じトーラス2つの和、`ZENITH_SELECT_WHY=1`）:

```
SELECTWHY A 面 0 の片 id 1: Outside → 採る 代表点 (6.6158 7.9603 1.8641)
SELECTWHY A 面 1 の片 id 2: Inside  → 落とす 代表点 (3.9219 4.7190 2.3506)
```

**16面のどれも `Boundary` になっていません。** 管の外側の片は `Outside`、
内側の片は `Inside` と判定され、和では外側の8枚しか採られません。

**箱だけが通っていたのは、平面のメッシュが厳密だからです。**

#### 直したこと（2つ）

1. `coincident_face_direction` が平面でないときに
   `curved_coincident_face_direction` へ回します。判定は平面側と**同じ
   厳しさ**です——境界の標本の重心と広がりが相対 1e-6 で一致し、面の中の
   代表点が相手の曲面の上に乗っていること。向きはその点の法線で決めます。
2. `classify_face_against_mesh` が、**メッシュで決まらないときだけ**
   B-Rep の面へ厳密に当てます（`distance::boundary_projections`）。
   **遠い面では走りません**——メッシュとの距離が形の 1% より遠ければ、
   そこは相手の境界ではありません。

#### 効果（実測）

| | 前 | いま |
| :--- | ---: | ---: |
| 同じ立体どうし 15演算のうち正しいもの | **3** | **13** |
| **誤答（もっともらしい立体を返した）** | **2** | **0** |
| 断られるもの | 10 | 2（円柱と円錐の差。**誤答ではありません**） |

離れている15演算は 15/15 のまま。

#### 代価は測ってあります

- `boolean_envelope`: supported 44 / wrong-result 0 / エラー 1、最悪一致
  6.68e-8 ——**不変**。曲面評価 58,905,657 → **58,918,536**（+0.02%）。
  厳密な当てはめは**メッシュで決まらず、かつ形の 1% 以内**のときしか
  走らないので、ほとんど増えません。
- `contact_placement_probe` 81 ok / 9 refused、B-Rep 非多様体 0、
  メッシュ非多様体 1演算 ——**不変**。
- `mesh_watertight_probe` 全検体クリーン。

---

### 4-135. 裏返しの立体が、打ち消し合って検査を通っていました（2026/08/28）

4-134 の最後に「まだ掃いていない軸」を3つ書いたので、その1つ——**同じ立体を
公差の前後だけずらす**——を測りました。ずらし幅は `1e-9 / 1e-7 / 1e-5 /
1e-3 / 1e-1` の5段階、形は閉じた式が厳密に書ける2つ（箱を x 方向、円柱を
軸方向）で、和・差・積の30通りです。

#### 箱は5段階とも完璧でした

`1e-9` と `1e-7` では薄片が公差に埋もれるので、差は立体0・積は元のまま——
**それが正しい答え**です。`1e-5` 以上は薄片がそのまま出ます
（`0.001000` / `0.100000` / `10.000000`、どれも閉じた式ちょうど）。

#### 円柱の `1e-5` だけが壊れていました

```
cylinder(r5,h10) 方向 z  1e-5 difference  **BAD**  2  -0.000000  期待 0.000785
        solid 0: faces 6, volume  7.854e-4
        solid 1: faces 6, volume -7.854e-4
```

**同じ薄片が2つ返り、片方が裏返しでした。** 体積は `+7.854e-4` と
`-7.854e-4` で、**合計するとちょうど打ち消し合います。**

#### なぜ検査を通っていたのか

`BooleanResultVerifier` は「結果の体積が正か」を**合計**で見ていました。
打ち消し合うと合計はほぼ 0 になりますが、しきい値
（`tol.linear` の3乗 = 1e-18）をわずかに上回るので通ります。
**1つずつ見ていなかったのが穴です。**

#### 直したこと

返ってきた立体を**1つずつ**測り、体積が正でなければ断ります。

**向きの揃った B-Rep なら体積は必ず正です**——空洞は `inner_shells` に
別に持ち、`compute_from_brep` が引きます（4-133）。裏返しや潰れた立体は、
それ自体が誤りです。

#### 効果（実測）

30通りのうち **`**BAD**` が 1 → 0**、断りが 4 → 5。**誤答が断りに
変わりました。**

#### 代価は測ってあります

- `boolean_envelope`: supported 44 / wrong-result 0 / エラー 1、最悪一致
  6.68e-8、曲面評価 58,918,536 ——**すべて不変**。**45ケースの中に裏返しの
  立体を返しているものは1つもありません。**

#### 残っていること

円柱を軸方向に `1e-5` ずらした和・差・積は、**3演算とも断られます**。
真の答え（厚み 1e-5 の円板）は返せていません。**断るのは誤答よりましと
いうだけで、通ったわけではありません。** 箱の同じ配置は通るので、
**曲面の薄い重なりだけが落ちます。**

---

### 4-136. 掃いて何も出なかったところ（2026/08/28）

4-133〜4-135 で総当たりが3回当たったので、**残りの軸も掃きました。
こちらは何も出ませんでした。** 出なかったことも実測なので残します。

#### 半分だけ重ねる — 12/12

同じ形を、体積の半分ほどが重なる位置に置きます。閉じた式が厳密に書ける
4通り（箱を x に 5、円柱を軸方向に 5、球を x に 5 と 3）× 3演算です。
球の重なりは

$$V = rac{\pi}{12}(4r + d)(2r - d)^2$$

で、$d 	o 0$ で $rac{4}{3}\pi r^3$ に戻ります（**この一致もテストに
入れました**——期待値を出す道具が狂っていたら、その先の一致に意味は
ありません）。

**12通りすべて一致**（例: 球を 3 ずらした積は実測 295.047878 に対して
閉じた式 295.047910）。`boolean_half_overlap_test.rs` として常設。

#### 引数の順序 — 食い違い 0

**和と積は可換**なので、`A op B` と `B op A` は同じ答えのはずです。
**閉じた式が要らない検査**なので、閉じた式の書けない配置にも使えます。

- 上の4配置 × 2演算: **8/8 一致**（`boolean_half_overlap_test.rs`）
- `contact_placement_probe` の **30配置 × 2演算 = 60通り**:
  **食い違い 0 件**（`ZENITH_CONTACT_SWAP=1`。演算が倍になるので既定は
  切ってあります）

**片方だけ返るものもありませんでした。** 断る配置は、入れ替えても断ります。

#### 掃いた軸の一覧（2026/08/28 時点）

| 軸 | 通数 | 結果 |
| :--- | ---: | :--- |
| 中に入っている（4形 × 4形 × 3演算） | 48 | 48/48（**直した後**。4-133） |
| 離れている（5形 × 3演算） | 15 | 15/15 |
| まったく同じ（5形 × 3演算） | 15 | 13/15（**直した後**。残り2件は断り。4-134） |
| 公差の前後だけずらす（2形 × 5段 × 3演算） | 30 | 誤答 0（**直した後**。5件は断り。4-135） |
| 半分重ね（4配置 × 3演算） | 12 | 12/12 |
| 引数の順序（34配置 × 2演算） | 68 | 68/68 |
| 自分の軸まわりに回して重ねる（4形 × **5角度** × 3演算） | 60 | **60/60**（4-137 で誤答 0、4-139 で断りも 0） |
| 桁の離れたスケール（5段 × 3演算） | 15 | 15/15 |
| 入れ子3段（空洞の中の島） | 3 | 3/3 |
| 結合則（4組 × 2演算） | 8 | 食い違い 0、**片方だけ返る 4**（4-138） |
| 面の一部だけが接する（5配置 × 3演算） | 15 | 15/15（4-140） |
| 同軸で一部だけ重なる（4配置 × 3演算） | 12 | 12/12（4-140） |
| **他カーネルの立体どうし**（8検体 × 3演算） | 24 | **パニック1件**（4-141。直した後は 4検体が3演算そろい、恒等式 1.03e-7） |
| 他カーネルの立体どうし・**異なる検体**（10検体・45組） | 45 | **誤答1件**（4-142。`cylinder × elliptic_prism` で残差 1.2e-2）。**4-143 で解決**——残差 **9.234e-10**。原因は earcut の出力を面積で検算していなかったこと。3演算そろったのは 23組で、うち 22組は恒等式を満たす |

**上の3つは、そのあと全部掃きました**（4-137 と下記）。

- **同じ形を回してから重ねる** → **誤答が2件出ました**（4-137）
- **スケールの桁を離す**（辺 1e2〜1e6 の箱の中に半径 1 の球）→ **15/15**。
  体積の比が最大 1e18 対 4.19 でも、積は 4.188790 のまま出ます。
  `boolean_validation` のコメントが心配していたところは、通っています
- **入れ子3段**（箱40 ⊃ 箱20 ⊃ 箱8）→ **3/3**。`A - B` は空洞つきの
  立体1つ（56000.000000）、そこに島を足すと**重ならない立体2つ**
  （56512.000000）、島は空洞＝材料でないところにいるので積は空

**次に掃くなら**（まだ手つかず）:

- ~~**部分的に重なる面**~~ → 4-139 と 4-140 で片づきました。**通らない
  実例は、いまのところありません**
- **3つ以上の立体の連鎖**を、順序を変えて突き合わせる（`(A∪B)∪C` と
  `A∪(B∪C)`）。結合則は閉じた式が要らない検査です
- ~~**他カーネルが書いた立体どうし**~~ → 掃きました。**本番経路のパニックが
  1件出ました**（4-141。空洞のある立体で添字が範囲外）

---

### 4-137. 二重被覆が「妥当な閉シェル」として通っていました（2026/08/28）

4-136 で「まだ掃いていない軸」に**同じ形を回してから重ねる**と書いたので、
測りました。**形は完全に同一**（自分の軸まわりに回すだけ）なので、和も積も
元の立体、差は空です。4形（円柱・球・円錐・トーラス）× 3角度（30°・90°・
180°）× 3演算 = 36通り。

#### 角度で結果が変わりました

| 角度 | 結果 |
| :--- | :--- |
| 90° / 180° | 円柱・球・円錐・トーラスとも和と積は正しい |
| **30°** | **球とトーラスの和が体積2倍**（1047.197551 / 2842.446068）|

**90° と 180° で通るのは、パッチの境がちょうど重なるからです。** 球は
8パッチ、トーラスは16パッチなので、90° も 180° も境が一致します。
**30° では、A の1枚が B の2枚にまたがって部分的に重なります。**
面の重なりの判定は「面全体の一致」（重心と広がり）なので、部分的な
重なりは捕まりません——これはコードのコメントに前から書いてあります
（4-124）。

#### 返っていたものは「二重被覆」でした

最初は **16面（8 + 8）が1つの立体**として返っていました。驚いたのは、
その立体が**すべての検査を通る**ことです。

```
solid 0: 外殻 16 面, 空洞 0, valid true, 非多様体の稜 0, 重複面 0, errors 0
```

**稜はどれもちょうど2回使われています。** A の8枚は自分たちで閉じ、
B の8枚も自分たちで閉じているからです。**`validate_closed` は
「シェルが繋がっているか」を見ていません。**

内外判定も通ります（重なった2つは、どの点から見ても「中は中」）。
体積の上限も、和なら `va + vb` にちょうど収まるので通ります。
**3つの検査が3つとも、すり抜けます。**

#### 直したこと（2つ）

**1. 稜の照合に中点を足しました**（`connected_piece_groups`）。

端点だけで照合していたので、**球の経線8本がすべて同じ端点の対**（北極と
南極）を持ち、経度の違う稜が1本と数えられていました。だから別々の閉じた
曲面が「繋がっている」ことになり、1つの立体になっていました。中点を鍵に
足すと、正しく**2つの立体**に分かれます。

**2. 返す立体どうしが重なっていないことを確かめます**（`BooleanResultVerifier`）。

ブーリアンの答えは「互いに交わらない立体の集まり」です。2つ以上返る
ときだけ、共通の境界箱に Halton 列で 512点撒いて、**両方の中に入る点が
1つでもあれば断ります**。境界の上は数えないので、触れているだけの2つは
通ります。

#### 効果（実測）

36通りのうち **誤答 2 → 0**、断り 14 → 16。**誤答が断りに変わりました。**

#### 代価は測ってあります

- `boolean_envelope`: supported 44 / wrong-result 0 / エラー 1、最悪一致
  6.68e-8、曲面評価 58,918,536 ——**すべて不変**。
- `mesh_watertight_probe` 全検体クリーン。
- 重なりの検査は**2つ以上返ったときだけ**走ります。

#### 残っていること

**部分的な面の重なりは、まだ解消できません。** 30° で回した球やトーラスの
和は「断る」だけで、真の答え（元の立体そのもの）は返せていません。
直すには、**A のパッチを B のパッチの境で刻んでから**重なりを見る必要が
あります（4-124 に同じことが書いてあります）。

→ **この段落は2つとも外れました。** 30° の和は 4-139 で**刻まずに**通る
ようになり（占める場所が同じなら答えは決まる）、「部分的な重なりが穴だ」
のほうは 4-140 で**測ったら通っていました**。

---

### 4-138. 結合則は破れていませんが、順序で通ったり通らなかったりします（2026/08/28）

最後の軸として**結合則**を測りました。`(A op B) op C` と `A op (B op C)` は
同じ答えのはずです（和と積）。**閉じた式が要らない検査**なので、答えの
分からない形にも使えます。4組（箱3つ／箱・円柱・球／球3つ／箱・円錐・円柱）
× 2演算です。

#### 数値の食い違いは 0 でした

両方返った組は**すべて一致**しました（例: 箱・円錐・円柱の和が
9026.253600 対 9026.253600、積が 39.856568 対 39.856568）。

#### ただし、**4件が「片方だけ返る」**です

| 組 | 演算 | `(A op B) op C` | `A op (B op C)` |
| :--- | :--- | ---: | :--- |
| 箱3つ | 和 | 2000.000000 | **断られる** |
| 球3つ | 和 | 1075.835063 | **断られる** |
| 球3つ | 積 | 122.781502 | **断られる** |
| 箱・円柱・球 | 和 | **断られる** | **断られる** |

断り文はどれも「まだ実装していない」です。**2回目の演算は、1回目の結果
（面の増えた立体）に当たります。** 括弧の位置が変わると、2回目に渡る
立体の形が変わるので、通る／通らないが変わります。

**これは実務で効きます。** 履歴の順序を入れ替えただけで通らなくなる、
ということだからです。**誤答ではありませんが、数として残しておきます。**

`boolean_associativity_test.rs` は「**両方返ったのに答えが違う**」ときだけ
赤にします（実測 210秒）。

---

### 4-139. 同じ場所を占めるなら、面を刻み直さなくても答えは決まります（2026/08/28）

4-137 で「部分的に重なる面がいちばん大きい穴」と書きました。**回り道が
ありました。**

#### 何が引っかかっていたか

同じ立体を自分の軸まわりに回して重ねると、**形は1ミクロンも変わりません**。
変わるのはパッチの境だけです。それでも 7°・30°・45° では断られていました
（90°・180° はパッチの境がちょうど重なるので通ります）。

**面を刻み直して重なりを解消する**のが正攻法ですが、そこまでしなくても
**占める場所が同じなら答えは決まります**——和も積も元の立体、差は空です。

#### 入れたもの

`occupy_the_same_region`。互いに「相手の中か境界の上」なら、同じ場所です。
判定そのものは既にありました（`solid_is_inside_or_on_boundary`）。

**足切りで2回つまずきました。両方とも測って直しました。**

1. **`Solid::bounding_box` は使えません。** 制御点から取るので、同じ形でも
   回すと広がります。実測: 半径 5 の円柱を 30 度回すと `±5` が
   **`±6.830127019`** になります。メッシュの実寸に変えました。
2. **`classify_face_against_solid` は呼ぶたびに相手を丸ごと張り直します。**
   面ごとに呼ぶと面の枚数だけ張ります（16面のトーラスなら16回）。
   メッシュを使い回す口（`classify_face_against_solid_mesh`）を足して、
   1回に済ませました。

#### 効果（実測）

4形（円柱・球・円錐・トーラス）× 5角度（7°・30°・45°・90°・180°）×
3演算 = **60通りが全部正しくなりました**（断り 36 → **0**、誤答 0）。

| 角度 | 前 | いま |
| :--- | :--- | :--- |
| 7° / 30° / 45° | 3演算とも断られる（球とトーラスは 4-137 の前は**誤答**） | **3演算とも正しい** |
| 90° / 180° | 正しい | 正しい |

#### 代価は測ってあります

- `boolean_envelope`: supported 44 / wrong-result 0 / エラー 1、最悪一致
  6.68e-8 ——**不変**。曲面評価 58,918,536 → **59,217,048（+0.51%）**、
  立体まるごとのテッセレーションは 82 → 168（演算ごとに2回）。
  **足切りは毎回走るので、そのぶんです。**

#### 残っていること

**部分的に重なる面そのものは、まだ解消できません。** 上は「同じ場所を
占める」という**特別な場合**を、面を刻まずに済ませたものです。

→ **「だから通らない」は外れました**（4-140）。刻む仕組みが無くても、
部分的に重なる配置は通ります——ブロックの部分的な面接触 15/15、同軸円柱の
部分的な側面重なり 12/12、どれも閉じた式ちょうどです。**通らない実例は、
いまのところ持っていません。**

---

### 4-140. 「部分的な面の重なりは通らない」を測ったら、通っていました（2026/08/28）

4-137 と 4-139 で「部分的に重なる面がいちばん大きい穴」と2回書きました。
**測っていませんでした。** コードのコメント（4-124）が「いまの判定は
面全体の一致なので、部分的な重なりは捕まりません」と言っているのを、
そのまま「通らない」と読んでいたわけです。

**測ったら、通りました。**

#### ブロックを横に並べて、ずらす — 15/15

面 `x = 10` の**一部だけ**が接する配置です。`(dy, dz)` を
`(0,0) / (5,0) / (5,5) / (9,0) / (2.5,7.5)` と振って、和・差・積。
**どれも閉じた式ちょうど**（和 2000、差 1000、積 空）。

#### 同軸の円柱で、側面や蓋が一部だけ重なる — 12/12

| 配置 | 和 | 差 | 積 |
| :--- | ---: | ---: | ---: |
| 同半径、B を軸方向に内包（r5 h10 ⊃ r5 h4 @3） | 785.39816 | **471.23890（立体2つ）** | 314.15927 |
| 同心の細い棒（r5 h10 ⊃ r3 h10） | 785.39816 | 502.65482 | 282.74334 |
| 同心で両端に出る棒（r3 h20 @-5） | 1068.14150 | 502.65482 | 282.74334 |
| 同心で中に埋まる短い棒（r3 h4 @3） | 785.39816 | 672.30083 | 113.09734 |

**全部、閉じた式ちょうどです。** 1件目は差が**上下の円板2つ**に割れます
——立体の数も合っています。

#### 何が言えるか

**「重なりを解消できない」ことと「答えが出せない」ことは別でした。**
面の重なりの解消は、同じ場所を**2枚とも採ってしまう**のを防ぐ仕組み
です。部分的に重なるだけの配置では、そもそも両方採る場面にならない
——分割と分類が先に片づけている——ということです。

**残っているのは、`4-139` で片づけた「同じ場所を占める」の周辺だけ**
かもしれません。**次に触る人へ: 通らない実例を持っていないなら、
「部分的な重なりが穴だ」と書かないでください。** ここで2回書いて、
2回とも実例がありませんでした。

`boolean_partial_face_contact_test.rs` として常設にしました（実測 12秒）。

---

### 4-141. **パニックしました** — 他カーネルの立体どうしで（2026/08/28）

4-136 に「まだ掃いていない軸」として書いた最後の1つ——**他カーネルが書いた
立体どうし**——を測りました。いままで測っていたのは「他カーネルの立体 ×
自作の切り手」だけです（`foreign_boolean_probe`）。

**5検体目で落ちました。**

```
thread 'main' panicked at crates\zenith_algo\srcrep_intersection.rs:388:22:
index out of bounds: the len is 6 but the index is 8
```

検体は `occ_reference_hollow_box`——**空洞のある立体**です。

#### 原因

交線の候補が持つ面の添字は、組み立てが `all_solid_faces`（**内側シェル
込み**）で作った並びを指します。ところが報告を組むところ
（`preparation_report_from_shell_assembly`）は
**`outer_shell.faces` だけ**を渡していました。中空の箱は外側 6 面 +
内側シェルなので、内側の面を指す添字 8 が範囲外になります。

**この経路は本番のブーリアンが通ります。** 「未実装」の断り文を組むために
毎回ここを通るので、**空洞のある立体を含むブーリアンが、断り文を組む
段でパニックしていた**ということです。

#### 直したこと（2つ）

1. **同じ並びを渡します。** 呼び側を `all_faces_of`（= `all_solid_faces`）に
   しました。
2. **添字で落ちないようにしました。** `faces[i]` を `faces.get(i)?` に
   変えて、合わない候補は捨てて先へ進みます。**カーネルがパニックするのは、
   誤答より悪い**ので、原因を直したうえで受け皿も置きます。

#### 測った結果（8検体）

| 検体 | 3演算 | 恒等式の残差 |
| :--- | :--- | ---: |
| `cylinder` | ok | 2.94e-12 / 2.68e-12 |
| `sphere` | ok | 1.03e-7 / 1.31e-9 |
| `torus` | ok | 6.28e-9 / 1.18e-8 |
| `stepped_shaft` | ok | 3.14e-12 / 3.51e-12 |
| `cone` / `hollow_box` / `plate_with_holes` / `chamfered_box` | 断られる | — |

**パニックは消えました。** 3演算そろった4検体は、包除の恒等式
（`|A∪B| + |A∩B| = |A| + |B|`）と分割の恒等式
（`|A\B| + |A∩B| = |A|`）を**最悪 1.03e-7** で満たします。

#### なぜ見つからなかったのか

**「他カーネルの立体」は測っていました。「他カーネルの立体どうし」は
測っていませんでした。** 片方が自作だと、自作側は面が素直なので、
この添字のずれに当たりません。**軸を1つ足しただけで、本番経路のパニックが
出ました。**

`boolean_foreign_pair_test.rs` として常設にしました。**第一の主張は
「落ちないこと」**で、恒等式はそのうえの確認です。

**重いテストです（実測 211秒）。** 3演算とも断られる検体（`cone` と
`chamfered_box`）は時間しか使わないので外しました（300秒 → 211秒）。
**空洞のある2つ（`hollow_box`、`plate_with_holes`）は残してあります**
——そこがパニックの現場だからです。

---

### 4-142. 他カーネルの立体どうしに、**誤答**が1件あります（2026/08/28。**4-143 で解決**）

4-141 で「同じ検体を2つ」測ったので、次は**異なる検体どうし**の総当たりに
掛けました。**10検体・45組**（中心を揃えて必ず重ねる）。

| | 値 |
| :--- | ---: |
| 組 | 45 |
| 3演算そろった組 | **23** |
| 3演算そろわなかった組（断りを含む） | 22 |
| **恒等式が破れた組** | **1** |

**恒等式の最悪残差 1.201e-2 は、その1組のものです。** 残る22組は恒等式を
満たします。

`occ_reference_cylinder` × `occ_reference_elliptic_prism`（中心を揃えて
重ねる）。恒等式の残差が **1.2e-2（1.2%）**です。

#### 測ったこと

| | 値 |
| :--- | ---: |
| A（円柱 r=10、高さ40） | 12566.370614 |
| B（楕円柱 a=12・b=7、高さ15） | 3958.406744 |
| **残差**（包除・分割とも同じ） | **150.951137** |

**残差は分割数を 24 / 48 / 96 と変えても 1ビットも動きません。**
テッセレーションの誤差ではなく、**幾何の誤り**です。

#### どの演算が誤っているか

断面（半径 10 の円と、半長軸 12・半短軸 7 の楕円）の重なりを**独立に
数値積分**して突き合わせました。

| 演算 | 実測 | 真値 | ずれ |
| :--- | ---: | ---: | ---: |
| 積 | 3561.762323 | 3561.762300 | **+0.000023** |
| 和 | 13113.966172 | 12963.015058 | **+150.951114** |
| 差 | 9155.559428 | 9004.608315 | **+150.951113** |

**積は正確です**（相対 6e-9）。**和と差だけが、まったく同じ量だけ
過大**です。

言い換えると:

- 和で「B のうち A の外にある部分」が 396.64 のはずが **547.60**
- 差で「A のうち B の外にある部分」が 9004.61 のはずが **9155.56**

**どちらも「相手の外」を 150.95 だけ多く数えています。** 積が正しいのに
和と差が同じ量だけ外れる、というのがこの欠陥の形です。

#### 検証ゲートを通ります

和は `|A| ≤ 和 ≤ |A|+|B|` に収まり（12566 ≤ 13114 ≤ 16525）、差は
`|A|` を超えません。内外一貫性の384点も通ります。**恒等式でしか
見えません。**

#### 機構は1つに絞れました（**分類ではなく、割り方**）

`ZENITH_SELECT_WHY=1` で採否を見たら、**円柱の側面の4分の1面が、
それぞれ2枚にしか割れていません。**

```
SELECTWHY A 面 0 の片 id 25 （2 枚中）: Outside → 採る 代表点 (9.6625 2.5760 11.8490)
SELECTWHY A 面 0 の片 id 26 （2 枚中）: Inside  → 落とす 代表点 (9.5614 2.9291 23.0859)
```

**2枚では足りません。** 中心を揃えると楕円柱は `z = 12.5〜27.5` を占め、
円柱の側面はその**上下の平面**で3つに切られます。さらに、半径 10 の円と
半長軸 12・半短軸 7 の楕円は `x = ±8.793, y = ±4.762` の**4点で交わる**
ので、側面は**周方向にも**切られます（4分の1面ごとに1本ずつ）。
本来は最大 6枚になるはずのところが、2枚です。

**交線は12本ちゃんと取れています**（上下の平面から8本、周方向に4本）。
**割りが1本ぶんしか効いていません。** あぶれた稜も非多様体も 0 なので
組み上がってしまい、**割られなかった面は代表点1つで丸ごと判定されます**
——そこで `Outside` に倒れた領域が、和と差の両方で 150.95 として出ます。

**だから積だけが正しいのです。** 積は「両方の内側」なので、倒れた領域は
どちらの結果にも入りません。

#### 次に触る人へ

- **再現は簡単です。** `occ_reference_cylinder` と
  `occ_reference_elliptic_prism` を読んで、境界箱の中心を揃えて重ねる
  だけです。`boolean_foreign_pair_test.rs` と同じ読み方で書けます。
- **見るのは「1枚の面を複数の交線で割る」ところ**です。分類は正しく
  動いています（代表点の位置と判定は合っています）。
- **楕円柱は他カーネルが書いた NURBS 面**です。自作の楕円柱でも同じに
  なるかを**まず分けてください**（4-141 の教訓）。
- **45組を最後まで回して、出たのはこの1組だけ**です。3演算そろった23組の
  うち、恒等式を満たさないのはここだけでした。残り22組は**断られる**ので
  （誤答ではありません）、そちらは別の問題です。

### 4-143. earcut の出したものを、面積で検算していませんでした（2026/08/28）

4-142 の誤答（他カーネルの円柱 × 楕円柱で恒等式の残差）を追い切りました。
**直りました。** 残差 2.406e-3 → **9.234e-10**。

| | 実測 | 真値 |
| :--- | ---: | ---: |
| 積 | 3561.762326 | 3561.762300 |
| 和 | **12963.015043** | 12963.015058 |
| 差 | **9004.608300** | 9004.608315 |

（4-142 の時点では 和 13113.97 / 差 9155.56 で、**+150.95** ずれていました。
その後の修理で **+30.23** まで下がっていたものを、ここで 0 にしました。）

#### 追い方（**面ごとの寄与まで割った**のが効きました）

まず再現を常設にしました（`foreign_cross_pair_probe`。4-142 の測定は
使い捨ての例で行われ、残っていませんでした）。そのうえで、

1. **差の立体の体積を、面ごとの寄与に割りました。** 発散定理なので和が
   体積になります。

   | 面 | 寄与 | 面積 |
   | ---: | ---: | ---: |
   | 0, 1, 3（NURBS） | 1846.123002 | 553.836900 |
   | **2**（NURBS） | **1876.353412** | **562.906024** |

   **差はちょうど 30.230410。体積の誤差そのものです。** 円柱の4分の1面は
   対称なので、4枚は同じでなければなりません
2. **入力の円柱を測りました。** 4面とも 628.318531、パラメータ面積 0.25。
   **入力は完璧です**
3. **片ごとに測りました。** 4面とも**パラメータ面積は同一**
   （Outside 0.219584 / Inside 0.030416）。なのに 3D 面積だけ面2 が
   562.906（他は 553.837）。**片の和も 628.32 に戻りません**（面2 は 637.39）
4. **uv のループを出しました。** 面0 と面2 で**完全に合同**（u が 0.5
   ずれただけ）。8稜、同じ形
5. **面積を積む側が見ている領域を測りました。**

   | 面2 の Outside 片 | 値 |
   | :--- | ---: |
   | p-curve が囲む uv 面積 | 0.219584 |
   | **三角形分割が覆う uv 面積** | **0.223069** |

6. **その中で切り分けました。**

   ```text
   TRIMAREA 標本した外周の面積 0.219583860（点 45）
   TRIMAREA   earcut 直後の面積 0.223069042（三角形 42）
   ```

**earcut が、多角形より広い三角形を返していました。** 裏返りではありません
——符号付きの和も 0.223069 です。

#### なぜ黙って通るのか

`trimmed_uv_triangulation` は**体積を積む経路**です
（`MassCalculator::compute_face_integral` はこの三角形分割を使います）。
そこが **earcut の出力を検査せずに信じていました**。

**この形は、閉じた式でも検証ゲートでも見えません。** 面は閉じていて、
非多様体でもなく、内外の判定も通ります。**恒等式でしか出ません。**

#### 直し方

**面積で検算し、合わなければ外周の始点を回して引き直します。** earcut の
出力は多角形をどの点から辿るかで変わるので、回すと通ります。回す量は
決め打ちの列なので、答えは実行ごとに変わりません（4-132 で測定の非決定性を
直したばかりなので、そこを戻さないようにしています）。

どの始点でも合わなければ、**いちばん近いものを採って先へ進みます**——
ここで断ると、いままで通っていた面まで落ちるからです。

#### 教訓

**4-122 と対になっています。** あちらは「安い物差し（パラメータ面積）が
正しい割りを捨てていた」で、こちらは「**その物差しを使う側の三角形分割が、
検算されないまま体積を積んでいた**」です。**同じ量を2通りで測れるなら、
測って突き合わせてください。** 突き合わせていなかった1か所が、そのまま
誤答になっていました。

#### 常設にしました

再現は `foreign_cross_pair_probe` として置き、CI のゲート一覧にも入れました。
**4-142 の測定は使い捨ての例で行われ、残っていませんでした**——同じことを
繰り返さないためです。

既定は**3検体・3組で 143 秒**、`ZENITH_PAIR_EXTRA=1` の**6検体・15組で
654 秒**です（どちらも実測）。CI の1本あたりの上限は 1200 秒なので広いほうも
入りますが、**このプローブは CI で毎回走る**ので既定は短いほうにしてあります。

**広いほうも掃きました**（2026/08/28）。**10組が3演算そろい、恒等式の破れは
0**（最悪 9.234e-10）。ただし**`hollow_box` を含む5組は、どれも3演算
そろいません**——空洞つきの立体だけが断られます。そこは 4-144 に。

#### 検証

| 対象 | 結果 |
| :--- | :--- |
| `boolean_envelope` | 44 / 0 / 1、解析解一致 **6.68e-8**（不変） |
| 全ワークスペースのテスト | **113 バイナリ・628 テスト、0 failed** |
| 常設ゲート | `mesh_watertight` / `contact_placement` / `face_split` / `slice` / `inertia` / `boolean_gate` とも exit 0 |
| `verify_mesh_exports.py` | **8/8** |
| `verify_showcase.py` | **54/54** |
| `freecad_cross_validate.py` | **27/27** |

### 4-144. 空洞のある立体は、**あらゆる相手との3演算が断られて**いました（2026/08/28）

4-143 のあと、掃く軸を広げました（`ZENITH_PAIR_EXTRA=1`、6検体・15組、
**654秒**）。**10組が3演算そろい、恒等式の破れは 0**（最悪 9.234e-10）。

**ただし `hollow_box` を含む5組は、どれも3演算そろいませんでした。**
空洞つきの立体だけが断られます。**中空部品は実務で普通に出てくる形**なので、
ここは実用性に直結します。

#### 断り文の中身

```text
selected face stitching has 4 unmatched edge uses, 0 non-manifold edge uses,
and 24 same-direction edge uses
```

**「同方向の稜」24件**。閉じた多様体では起こりえない形です。内側シェルの
6面 × 4稜 = 24 と数が合います。

`ZENITH_STITCH_WHY=1` に「同方向」も出すようにして位置を見ると、
`(6,8,±5)` や `(10,0,±5)` ——**円柱と空洞の壁の交線の上**でした
（6² + 8² = 100 なので半径10の円柱の上）。

#### 機構（測りました）

面の代表点から実効法線の向きへ出た点が立体の中か外かを測りました。

| シェル | 実効法線の向き |
| :--- | :--- |
| 外側 | 材料から**外**へ（正しい規約） |
| **内側（空洞）** | 材料の**中**へ（**規約が逆**） |

**切り手が空洞を貫くと、空洞の壁が外側の境界に繋がります。** そのとき
規約が逆のままだと巻きが食い違い、「同方向の稜」になります。

**`all_solid_faces` は外側と内側を1つの並びに潰していました**——どちらの
シェルから来たかが失われるので、揃えようがありませんでした。

#### 直し方

`face_comes_from_inner_shell` を足し、選択の段で**内側シェル由来の片は
向きを反転**させます（差の B 側反転と排他的論理和で合成）。

#### 効果（実測）

| 組 | 前 | 後 |
| :--- | :--- | :--- |
| `elliptic_prism × hollow_box` | 断り | **ok**（残差 1.025e-13） |
| `sphere × hollow_box` | 断り | **ok**（残差 1.673e-12） |
| `stepped_shaft × hollow_box` | 断り | **ok**（残差 7.239e-14） |
| `cylinder × hollow_box` | 断り | 断り（**同方向は 24 → 0**） |
| `torus × hollow_box` | 断り | 断り（同方向 0、あぶれ 28・非多様体 24） |

**狙った「同方向の稜」は全件 0 になりました。** 残る2組は別の形
（あぶれた稜と非多様体）なので、そこは次の一手です。

#### 残る2組は、**接触配置**でした（測りました）

`cylinder × hollow_box` のあぶれた稜を出すと、**z = −7.000000 と
z = −7.000001** の2本でした。**ちょうど 1e-6** です。

入力は綺麗です（読み込み直後・持ち方を揃えた後・中心を揃えた後とも
z = −7, −5, 5, 7 ちょうど）。選択された面片の稜にも `-7.000001` は
ありません。**交線そのものが、同じ稜を2通りに計算しています**——
`ZENITH_CAP_TRACE` の桁を上げると:

```text
稜 (0.000000000 -10.000000000 -7.000001000) -> (0.000000000 -10.000000000 7.000000000) 長さ 14.000001000
稜 (-0.000000000 -10.000000000 -7.000000000) -> (-0.000000000 -10.000000000 7.000000000) 長さ 14.000000000
```

**なぜそこだけ食い違うのか。** この配置は**接触**です。中心を揃えると、
半径 10 の円柱は箱の面 `y = ±10` に**線で接します**（x = 0 の母線で）。
`x` は ±15 なので、円柱は x 方向には収まっています。

**接触配置は、このリポジトリでいちばん難しい一族です**（3-N-1 の規約
「接触は、それ自体では位相を作らない」）。**4-144 で直した向きの件とは
別の話**なので、ここで分けて記録します。

#### 次に測るなら

接する母線の上で、交線が2通りに出ること自体を潰すところから。
`ZENITH_CAP_TRACE=1` は桁を上げてあるので、そのまま読めます。
**1e-6 ちょうど**なので、どこかに固定量が入っているはずですが、
`zenith_algo` と `zenith_geom` を「1e-6 を足し引きしている箇所」で
探しても見つかりませんでした（比較に使っているものばかり）。
**次は、交線を作る経路を1段ずつ測ってください。**

### 4-145. 探すための余裕が、答えの端点に残っていました（2026/08/28）

4-144 の最後に「1e-6 ちょうどなので固定量が入っているはずだが見つからない」
と書きました。**見つかりました。**

`clip_candidate_to_face_bboxes` は、交線を**2つの面の境界箱の重なり**で
切ります。重なりを取る `bbox_overlap` は、**全部の軸に一律で公差ぶんの
余裕**を当てていました。

```rust
a.min.z.max(b.min.z) - tol,
a.max.z.min(b.max.z) + tol,
```

`tol.linear` の既定は **1e-6** です。**そのまま −7 − 1e-6 = −7.000001** に
なります。

#### なぜそこだけ出るのか

この経路は「余裕なしの重なりで切り、駄目なら余裕つきで切る」という順です
（コメントにも「余裕を残したままだと交線が面の外へわずかにはみ出す」と
書いてありました）。**接触配置では、余裕なしの重なりは接している軸の厚さが
0 になります。** そこで `clip_line_to_bbox` が通らず、余裕つきの箱へ落ち、
**接していない軸（この場合 z）にも余裕が乗りました。**

半径 10 の円柱は箱の面 `y = ±10` に線で接します。接しているのは `y` だけ
なのに、`z` の端点まで 1e-6 ずれていました。**同じ稜を別の面の組から作った
ものは `z = −7.000000` なので、公差の外で食い違います。** あぶれた稜として
縫合が止まり、3演算とも断られていました。

#### 直し方

**余裕は、それが要る軸にだけ当てます**（`bbox_overlap_where_needed`）。
厚さが 0 の軸だけ広げれば、探すのに足りて、**答えは動きません**。

#### 効果（実測）

| 組 | 4-144 の後 | いま |
| :--- | :--- | :--- |
| `cylinder × hollow_box` | 断り | **ok**（残差 1.002e-13） |
| `elliptic_prism × hollow_box` | ok | ok |
| `sphere × hollow_box` | ok | ok |
| `stepped_shaft × hollow_box` | ok | ok |
| `torus × hollow_box` | 断り | 断り |

**空洞つきの立体は、5組中4組が通るようになりました**（着手前は 0組）。

#### **順序で通り方が変わります**（そして、断るほうが正しい）

常設テストを「4組そろう」に更新したら**落ちました**。プローブは4組そろうのに、
テストは3組です。**違いは引数の順序**でした。

| | 和 | 積 | 差 |
| :--- | :--- | :--- | :--- |
| `cylinder × hollow_box` | ok | ok | **ok** |
| `hollow_box × cylinder` | ok | ok | **断り** |

**断り文を読んだら、断るほうが正しい配置でした。**

```text
the true result is non-manifold at (-0.000000 -10.000000 -3.500000):
the material there is only in contact, falling into 2 pieces around the
intersection (measured on rings of radius 1.400e-1 and 1.400e-2)
```

中心を揃えると、半径 10 の円柱は箱の面 `y = ±10` に線で接します。**そこを
抜くと材料は接点だけで2つに分かれる**ので、真の答えが本当に非多様体です。
規約（3-N-1）どおり、**場所を名指しして断って**います。

**自分のテストの主張のほうが誤りでした。** 「4組そろう」ではなく、
「3組そろい、**残る1組は場所を名指しして断る**」が正しい主張です。
そう書き直しました（`refusal.contains("non-manifold at")` も検査します）。
逆向きが通ることも固定したので、4-145 が戻れば落ちます。

**「断られた」を見たら、まず正しい拒否かどうかを疑ってください。**
このリポジトリでは、断ることが答えである配置があります。

#### 教訓

**探すための余裕と、答えの精度は別のものです。** ここは「余裕なしを先に
試す」という形で一度分けてありましたが、**落ちた先で分けていませんでした**。
接触配置は「余裕なしが必ず落ちる」場所なので、そこだけ余裕つきの答えが
出ていました。**分岐で分けるなら、落ちた先も分けてください。**

### 4-146. 掃く軸を2つ足しました — **どちらも誤答 0**（2026/08/28）

4-143〜4-145 のあと、**まだ掃いていない軸**を足しました。`foreign_cross_pair_probe`
に `ZENITH_PAIR_ROTATE=<度>` を入れ、**片方を回してから重ねる**ようにしています
（それまでは軸に平行な配置だけでした。回す軸は `(1,2,3)` で固定）。

#### 軸1: 他カーネルの立体どうしを回して重ねる

| 回転 | 3演算そろった組 | 恒等式の破れ | 残差の最悪 |
| ---: | ---: | ---: | ---: |
| 13度 | 3 | **0** | 1.148e-9 |
| 31度 | 2 | **0** | 3.020e-11 |
| 47度 | 2 | **0** | 3.400e-11 |
| 71度 | 1 | **0** | 1.063e-11 |

**回すと断られる組は増えます**（3組そろうものが 3 → 1）。**返ってきたものは
全部正しい**——規約どおりです。

#### 軸2: **回した空洞つき立体**（今日開通したばかり）

空洞のある立体は 4-144 と 4-145 で通るようになったばかりなので、**回した
ものは誰も測っていませんでした。**

| 回転 | 3演算そろった組 | 恒等式の破れ | 残差の最悪 |
| ---: | ---: | ---: | ---: |
| 0度 | 4 | **0** | 1.673e-12 |
| 23度 | 2 | **0** | 4.545e-10 |
| 61度 | **3** | **0** | 2.153e-9 |

**61度では `torus × hollow_box` すら通ります**——軸に平行な配置では断られる
組です（4-145 の残り1組）。**置き方を変えると通る**という形なので、
断りは配置に固有で、形に固有ではありません。

#### この掃き出しで分かったこと

**誤答は1件も出ませんでした。** 4-133〜4-137 の掃き出しでは4件出たので、
**軸を足せば必ず出る、というものではありません**。ここは正直に書いておきます
——「掃けば出る」と期待して掃くと、出なかったときに掃き方を疑いたくなります。
**出なかったことも実測です。**

ただし**断られる組は増えます**。断りは赤にしない立て付けなので、
**「通る組が減ったこと」は、この掃き出しでは自動では捕まりません。**
数を主張に入れるなら、`contact_placement_probe` のように配置を固定して
から入れてください。

### 4-147. 4-132 の未解決が、**別の修理で直っていました**（2026/08/28）

4-146 のあと、引継書に「未解決」と残っている件を確かめ直しました。

#### `cone × sphere`（円錐に内接する球を引く）——**直っています**

4-132 の記録は「差の体積が **A + B**（2207.492438 = 2094.395102 + 113.097336）
になり、`BooleanResultVerifier` が捕まえて断った。球が凹（空洞）ではなく凸
として足されている」でした。

いま測ると:

| 演算 | 実測 | 真値 |
| :--- | ---: | ---: |
| 和 | 2094.395102 | 2094.395102（円錐そのもの） |
| 積 | 113.097336 | 113.097336（球そのもの） |
| **差** | **1981.297767** | **1981.297767** |

しかも差の結果は**空洞を1つ持つ立体**です——球が中に抜けた正しい形。

**直したのは 4-144 だと思われます**（内側シェルの向きを選択の段で揃える）。
4-132 の記述は「球が凹ではなく凸として足されている」で、**まさに空洞の
向きの話**でした。**別の入口から入って、同じ場所に当たっていた**ことに
なります。

#### `torus × torus (crossed axes)`——**まだ断られます**

3演算とも「未実装」で断られます（交線16本、あぶれ14、非多様体0）。
あぶれは端点のずれではなく**相手のいない弧**で、すべて接点
`(±16,0,0)` / `(±8,0,0)` に接しています。4-132 の記述のままです。

#### 教訓

**「未解決」と書いたものは、別の修理で直っていることがあります。**
今日は掃く軸を足しても誤答が出ませんでしたが（4-146）、**古い未解決を
測り直したら1件片づいて**いました。**掃くだけでなく、たまに棚卸しを
してください。**

### 4-148. 掃く軸をもう1本足しました（**測りかけです**。2026/08/28）

9-G の G1（新しい掃き軸を3本足して誤答が連続 0）の**2本目**です。

#### 足した軸: **ずらしてから重ねる**（`ZENITH_PAIR_SHIFT=<割合>`）

いままでの掃き出しは「中心を揃えて必ず重ねる」だけでした。**中心を揃えると
対称な配置ばかりになります**——対称性は交線を綺麗にするので、通りやすい側に
偏ります。境界箱に対する割合でずらすと、対称性が崩れて**片側だけが刺さる**
配置になります。

ずらす量は `(size.x * f, size.y * f * 0.5, size.z * f * 0.25)` で、3軸とも
違う割合にしてあります（同じ割合だと対角線上の対称性が残るため）。

#### 4点そろいました（2026/08/28 夕に測り足しました）

| ずらし | 3演算そろった組 | 恒等式の破れ | 残差の最悪 | 秒 |
| ---: | ---: | ---: | ---: | ---: |
| 0.15 | 3 / 3 | **0** | 3.054e-9 | 117 |
| 0.3 | 3 / 3 | **0** | 5.072e-9 | — |
| 0.45 | 3 / 3 | **0** | 1.617e-9 | 93 |
| 0.7 | 3 / 3 | **0** | 8.105e-9 | 51 |

すべて `ZENITH_PAIR_QUICK=1`（3組）です。**4点とも誤答 0** で、残差は
1.6e-9〜8.1e-9 に収まります。

**この節はもともと「1点だけ測って止まった」と書いてありました。**
残りの3点を測ったので、この見出しごと差し替えています。**測っていない
ことを測っていないと書いてあったので、拾えました。**

**ずらすほど速くなります**（117 → 93 → 51 秒）。重なりが浅くなると交線が
短くなるので、素直な向きです。**逆に言うと、深く重なる側のほうが仕事量が
多い**ので、この軸で誤答を探すなら 0.15 より小さい側を濃く測るほうが筋
です（0.0 は「中心を揃えて重ねる」そのもので、既に掃いてあります）。

```bash
for f in 0.15 0.45 0.7; do
  ZENITH_PAIR_SHIFT=$f ZENITH_PAIR_QUICK=1     cargo run --release -p zenith_algo --example foreign_cross_pair_probe
done
```

**`ZENITH_PAIR_QUICK=1` を付けているのは時間のためです**（3組143秒 ×
角度の数）。既定の15組で掃くと1点あたり 722 秒かかります。**当たりが出たら、
そこだけ広い設定で測り直してください。**

#### G1 の数え方について

**「軸を1本足して誤答0」を1本と数えます。** いま:

| 本 | 軸 | 結果 |
| ---: | :--- | :--- |
| 1 | 回して重ねる／回した空洞つき（4-146） | **誤答 0**（7点の実測） |
| 2 | ずらしてから重ねる（4-148） | **誤答 0**（4点の実測。0.15 / 0.3 / 0.45 / 0.7） |
| 3 | 鏡映してから重ねる（4-149） | **誤答 0**（4点の実測。x / y / z / tilted） |

**3本目の候補**（まだ掃いていない軸）:

- **片方を鏡映してから重ねる**（向きが裏返った立体との演算）
- **同じ演算を2回**（`(A ∪ B) ∪ B` が `A ∪ B` に戻るか）
- **他カーネルの立体と自作の立体を混ぜる**（`foreign_boolean_probe` は
  「読んだ立体 × 自作の切り手」だけで、恒等式では見ていません）

---

### 4-149. 3本目の掃き軸「鏡映してから重ねる」— 誤答 0（9-G の G1 達成、2026/08/28）

4-148 が名指ししていた3本目の候補のうち、**片方を鏡映してから重ねる**を
足しました（`ZENITH_PAIR_MIRROR=x|y|z|tilted`）。

| 鏡の法線 | 3演算そろった組 | 恒等式の破れ | 残差の最悪 | 秒 |
| :--- | ---: | ---: | ---: | ---: |
| x | 3 / 3 | **0** | 6.162e-10 | 79 |
| y | 3 / 3 | **0** | 9.234e-10 | 80 |
| z | 3 / 3 | **0** | 7.349e-9 | 81 |
| tilted (1,2,3) | 3 / 3 | **0** | 2.657e-9 | 108 |

すべて `ZENITH_PAIR_QUICK=1`（3組）。**4点とも誤答 0** です。

#### この軸が測っているものを、書く前に確かめました

**最初、「鏡映は面の向きを裏返すので、向きが逆の立体を食わせる軸になる」と
コメントに書きました。実装と違いました。**

`MirrorBuilder::mirror_solid` は `mirror_wire_reversed` を通しており、
**ループの巡回順序と辺の向きを反転**します。返るのは**向きの揃った立体**
です。実測でも `|B|` は鏡映の前後で同じ 3958.406744（正）でした。

**では何を測っているのか。** 制御点を鏡に映したうえで、**v 方向のノット列を
逆順に張り直した NURBS 面**です（`mirror.rs` の `reversed_knots`）。対称な
検体では形が元へ戻るので、**同じ形を別の作り方で組んだ B-Rep** になります。
面の張り方が変われば、交線の取り方も面の割り方も変わります。

**裏返しの立体を食わせる軸は、まだ掃いていません。** 4-135 で誤答が出たのは
そちらなので、**4本目の候補として残します**。

**コメントを読んで「そういう軸だ」と決めないでください。** 5章に同じ教訓が
あります（4-140）。今回は書いた直後に自分で引っかかりました。

#### G1 は達成しました

| 本 | 軸 | 点数 | 結果 |
| ---: | :--- | ---: | :--- |
| 1 | 回して重ねる／回した空洞つき（4-146） | 7 | **誤答 0** |
| 2 | ずらしてから重ねる（4-148） | 4 | **誤答 0** |
| 3 | 鏡映してから重ねる（4-149） | 4 | **誤答 0** |

**3本連続で誤答 0 です。** その前は掃くたびに出ていました（4-133〜4-137 で
4件、4-143 で1件）。**「出る確率が下がった」とは言えますが、「無い」とは
言えません**——15点はいずれも `ZENITH_PAIR_QUICK=1` の3組で、6検体15組の
広い設定では測っていません。

#### まだ掃いていない軸（4本目以降の候補）

- **裏返しの立体を食わせる**（上のとおり、鏡映では作れません）
- **同じ演算を2回**（`(A ∪ B) ∪ B` が `A ∪ B` に戻るか）。ブーリアンの
  **出力を入力に戻す**ので、読み込んだ B-Rep ではなく**自分が作った B-Rep**
  を試せます
- **他カーネルの立体と自作の立体を混ぜる**（`foreign_boolean_probe` は
  「読んだ立体 × 自作の切り手」だけで、恒等式では見ていません）

---

### 4-150. 表示メッシュが 17〜23 倍膨らんでいた原因は、幾何と関係のない条項でした（2026/08/28、9-G の G3）

**傾けたトーラス × 箱の差を 24分割で表示すると、三角形が 124,804 枚**でした。
同じ立体を格子経路で刻めば 3万枚ほどなので、4倍以上ふくれています。

#### 原因は1行でした

`triangle_needs_refinement` の先頭です。

```rust
if u_extent > cell_u || v_extent > cell_v { return true; }
```

三角形の **uv の範囲が格子1マスを超えたら、弦誤差に関係なく割ります**。
これは幾何の基準ではなく、パラメータの細かさを揃えるためのものです。
earcut が出す細長い片は uv で長いので、**3D では平らでも格子の細かさまで
割られ続けます**。

#### まず、全部外して測りました

| | 既定 | 条項なし |
| :--- | ---: | ---: |
| トリム面1枚の最大（`ZENITH_TRIM_WHY`） | 163,014 枚 | **22,770 枚** |
| `boolean_envelope` の曲面評価 | 59,001,475 | **52,234,483**（−11.5%） |
| 解析解との最悪一致 | 6.68e-8 | 6.64e-8（わずかに良い） |
| supported / wrong / エラー | 44 / 0 / 1 | 44 / 0 / 1 |

**それでもテストが1件落ちました。**

```
after 1 hole(s) the volume 94429.2035749507 should be 94429.2036732051
```

板に穴を1つ開けた体積が**相対 1.04e-9 動きます**（`boolean_chained_test`）。
**この条項は積分の精度を支えています。** 弦の検査は辺の中点3つしか見ない
ので、大きい三角形の上では足りません。

#### 経路を分けました

`refine_uv_triangulation_protected` に「掛けるかどうか」の引数を足し、
**答えを積む側だけ掛けます**。

| 呼び出し | 掛けるか | なぜ |
| :--- | :--- | :--- |
| `trimmed_uv_triangulation`（`face_uv_triangulation` 経由の積分、`tessellate_face`） | **掛ける** | 体積・面積がここで決まる |
| `stitched.rs` の `patch_mesh`（表示・STL） | **掛けない** | 弦誤差の基準はそのまま掛かるので形は保たれる |

#### 効果（実測、傾けたトーラス × 箱の差）

| | 前 | いま |
| :--- | ---: | ---: |
| 縫合メッシュ 24分割 | 124,804 枚 | **7,428 枚**（16.8分の1） |
| 縫合メッシュ 64分割 | 1,294,274 枚 | **56,592 枚**（22.9分の1） |
| 体積（24 / 64分割） | 6472.584432291 / 6472.584434791 | **同じ** |

**答えは1ビットも動きません。** `boolean_envelope` は supported 44 /
wrong 0 / エラー 1、最悪一致 6.68e-8、曲面評価 **59,001,475 回**で、
どれも既定と完全に一致します。テストは **129 バイナリ・654 テスト・
0 failed・警告 0**。`mesh_watertight_probe` は全検体 closed。

#### 代価も測ってあります（**悪くなったところがあります**）

`contact_placement_probe` の `sphere × cylinder (eccentric)` の差で、
**メッシュ非多様体が 13本 → 34本**に増えました。**演算の数は 1 のまま**
（81 ok / 9 refused、B-Rep 非多様体 0）ですが、増えたことは事実です。

原因はこの変更ではありません——**継ぎ目の頂点が 3つに割れている**という
B-Rep 側の欠陥（4-129）が、粗いメッシュで別の出方をしただけです。
**その演算はもともと STL に書けません**（0 でない時点で書けない）ので
実害は変わりませんが、G4 の数字としては悪化しています。

**中間はありませんでした。** 格子1マスを何倍まで許すかを振って測ると:

| 許す倍率 | 縫合メッシュ（24分割） | 偏心の差の非多様体 |
| ---: | ---: | ---: |
| 1（既定） | 124,804 | 13 |
| 2 | 36,326 | 34 |
| 4 | 11,508 | 34 |
| 8 | 7,442 | 34 |
| 外す | **7,428** | 34 |

**1 を超えた時点で 34 になります。** 途中で止めても得はないので、外しました。

#### 入れて、測って、外したもの

「弦の検査は辺の中点3つしか見ないので、**重心のたわみも見る**」を足しました。
理屈は立ちますが、**枚数が1枚も変わりませんでした**（縫合 7,428 枚で前後
同じ）。効果の測れないものは入れない、という決まりどおり外しました。

#### G3 の指標そのものに、取り違えがあります

**9-G の G3 は「トリム面1枚の三角形が 1万枚以下」で、163,014 枚という
数字は `ZENITH_TRIM_WHY=1`——つまり `trimmed_uv_triangulation`——で
測っています。** ところが**画面に出るのは縫合メッシュ**です。両者は別の
経路で、今回いじったのは後者だけなので、**G3 の数字は 163,014 のまま
動きません。**

**指標が、狙っているもの（「画面を回すたびに効く」）を測っていません。**
次に触る人は、まず**何を数えるかを決め直してください**。縫合メッシュで
数えるなら、いまの値は 24分割で 7,428 枚・64分割で 56,592 枚です。

---

### 4-151. G3 を、画面に出るもので数え直しました（2026/08/28）

4-150 で「G3 の指標が狙っているものを測っていない」と書いたので、
**数える場所を足して測り直しました**（`ZENITH_PATCH_WHY=1`。縫合経路の
`patch_mesh` が、面ごとの枚数を出します）。

#### 実測（傾けたトーラス × 箱の差、1面あたりの最大）

| 分割 | 1面の最大（縫合メッシュ） |
| ---: | ---: |
| 24 | **1,152 枚** |
| 48 | **4,020 枚** |
| 64 | 10,354 枚 |
| 96 | 20,298 枚 |

**常設30配置（`contact_placement_probe`、24分割、6,045面）での最大は
4,604 枚**です。

#### G3 は、常設ゲートの分割では達成しています

目標は「トリム面1枚の三角形が 1万枚以下」でした。**24分割で 4,604 枚、
48分割まで 1万枚を下回ります。** 64分割で 10,354 枚とわずかに超え、
96分割で 20,298 枚です。

**分割数が書かれていない目標だったので、ここで書き足します。**
常設ゲートと `mesh_watertight_probe` の既定は 24分割、`boolean_envelope`
の検算は 48分割です。**その範囲では達成、64分割以上は未達**、というのが
実測の状態です。

#### 古い数字（163,014）は、別の経路のものです

`ZENITH_TRIM_WHY=1` が出す値は `trimmed_uv_triangulation`——**積分と、
面を1枚だけ刻む経路**——のもので、いまも 163,014 枚のままです。
**そこは答えを積む場所なので、粗くしていません**（4-150）。

**2つの数字は別のものを測っています。取り違えないでください。**

| 口 | 何の枚数か | いま |
| :--- | :--- | ---: |
| `ZENITH_TRIM_WHY=1` | 積分と `tessellate_face` | 163,014 枚（24分割） |
| `ZENITH_PATCH_WHY=1` | **画面・STL に出る縫合メッシュ** | 1,152 枚（24分割） |

---

### 4-152. 縁に着いた場所を、接点で解き直す（2026/08/29。**4度目で通りました**）

4-129 で場所まで突き止めてあった欠陥です。`sphere × cylinder (eccentric)`
の差で、球の継ぎ目 `(10,0,0)` のまわりに **B-Rep の頂点が3つ**並びます。

| 面の組 | 端点の、接点までの距離 |
| :--- | ---: |
| face_a 0 × face_b 0 | **5.70e-5** |
| face_a 1 × face_b 0 | **6.35e-6** |
| face_a 6 × face_b 3 | 1.29e-7 |
| face_a 7 × face_b 3 | 1.05e-9 |

`(10,0,0)` は**接点でもあり、パッチの角でもあります**。後ろの2組は接点への
着地（4-128）が効いて倍精度まで詰まっていますが、前の2組は**歩いて縁に
乗ったところで止まって**おり、縁の上のどこに乗ったかが $\sqrt{arepsilon}$
でしか決まっていませんでした。

#### 直したこと

`run()` の `at_edge` の出口で、**接点のそばなら解き直します**。採るのは
次を全部満たすときだけです。

1. 正弦が `TANGENCY_SINE_LIMIT` を切っている（接点のそば）
2. **着いたときに縁に張り付いていた座標が、解き直しても同じ値のまま**
3. 領域の中にあり、歩幅の2倍より遠くへ飛んでいない

#### **2 を見つけるのに3回失敗しました**

| 試したこと | 結果 |
| :--- | :--- |
| 条件なしで解き直す | `face_a 1` が**赤道側の縁へ滑って** 6.35e-6 → 3.64e-5 に悪化、3演算とも断られる |
| 「どれかの縁の上にある」を条件に足す | **同じ**（滑った先も縁の上なので通ってしまう） |
| **「同じ縁に留まる」を条件にする** | `face_a 0` だけが直り、他は1つも動かない |

**「縁の上にある」と「同じ縁の上にある」は別です。** パッチの角の近くでは
縁が2本あるので、前者では滑る先を止められません。

#### 効果（実測）

| | 前 | いま |
| :--- | ---: | ---: |
| `face_a 0` の端点 | 5.70e-5 | **7.557e-15** |
| メッシュ非多様体（偏心の差） | 34本 | **10本** |

**0 にはなっていません。** 残るのは `face_a 1` の 6.35e-6 で、そこは条件 2 が
**正しく棄却**しています——解き直すと別の縁へ行くので、いまの解き方では
届きません。

#### 残り1点に、もう一手試して戻しました（2026/08/29）

**なぜ別の縁へ行くのかは分かりました。** 4本目の式は「外積を `onto` へ
落とした量が 0」ですが、**外積が 0 でなくても `onto` と直交すれば 0 に
なります**——偽の根です。実測で落ちた先 `(9.999999999912, 3.64e-5, 0)` は
**赤道の上**で、そこは接点ではありません（正弦はむしろ大きい）。

そこで「落とす向きを `heading` と x / y / z の4通り試し、**正弦が実際に
小さくなったものだけ採る**」を入れて測りました。

| | 前 | 入れた後 |
| :--- | ---: | ---: |
| 差のメッシュ非多様体 | 10本 | 10本（変わらず） |
| **積の**メッシュ非多様体 | **0本** | **4本（悪化）** |

**正弦が小さくなることは、正しい接点であることの十分条件ではありません。**
差は動かず積が悪くなったので、戻しました。

**次に試すなら、4×4 に押し込まずに解くことです。** 接点は
`p1 = p2`（3本）と `n1 × n2 = 0`（2本独立）の**5本**で決まる過剰決定系
なので、ガウス・ニュートンで最小二乗に解けば、向きを選ばずに済みます。
**射影で1本に潰すのをやめる**のが筋だと考えます（ただし未実装・未測定）。

#### 代価は測ってあります

`boolean_envelope` は supported 44 / wrong 0 / エラー 1、最悪一致 6.68e-8、
曲面評価 **59,003,683 回**（前 59,001,475、+0.004%）。`ssi_probe` 3/3 clean、
`mesh_watertight_probe` 全検体 closed、`contact_placement_probe` は
81 ok / 9 refused・B-Rep 非多様体 0。テストは **129 バイナリ・654 テスト・
0 failed・警告 0**。

---

### 4-153. 射影で1本に潰すのをやめたら、G4 が 0 になりました（2026/08/29）

4-152 の末尾に「次に試すなら、4×4 に押し込まずに解くこと」と書きました。
**そのとおりでした。**

#### 何が悪かったか

接点を解く4本目の式は「外積を `onto` へ落とした量が 0」でした。
**外積が 0 でなくても、`onto` と直交すれば 0 になります**——偽の根です。

実測（4-152）: 球の継ぎ目の端点から解くと、`(9.999999999912, 3.64e-5, 0)`
という**赤道の上の点**へ落ちました。そこは接点ではなく、正弦はむしろ
大きくなっています。

**向きを4通り試して「正弦が小さくなったものだけ採る」も測りました。**
差は 10本のまま、**積が 0 → 4本に悪化**しました（4-152）。
**正弦が小さくなることは、正しい接点であることの十分条件ではありません。**

#### 直したこと

接点が満たすのは **6本**です——`p1 - p2 = 0`（3本）と `n1 × n2 = 0`
（3本。独立なのは2本）。未知数は4つなので過剰決定系ですが、**真の接点
では全部同時に 0 になる**ので、最小二乗で解けば向きを選ばずに済みます。

`newton_to_tangency_least_squares` を足しました。ガウス・ニュートンで、
外積の3行は中心差分で作ります。`at_edge` の出口では**まずこちらで解き**、
届かなければ従来どおり接線向きへ落とす解き方に落とします。採否の条件
（同じ縁に留まる・領域の中・歩幅の2倍以内）は 4-152 のままです。

#### 効果（実測）

| | 4-152 | いま |
| :--- | ---: | ---: |
| `sphere × cylinder (eccentric)` 和 / 差 / 積 | 0 / 10 / 0 | **0 / 0 / 0** |
| `contact_placement_probe` メッシュ非多様体 | 1演算 | **0演算** |

**`contact_placement_probe` は 81 件が立体を返し、B-Rep もメッシュも
非多様体 0 になりました。9-G の G4 達成です。**

経過をまとめておきます——13本（4-129 で場所を特定）→ 34本（表示メッシュを
粗くして出方が変わった。4-150）→ 10本（縁に着いた場所を解き直す。4-152）
→ **0本**（射影をやめる。4-153）。

#### 代価は測ってあります

`boolean_envelope` は supported 44 / wrong 0 / エラー 1、最悪一致 6.68e-8、
曲面評価 **59,003,683 回**——4-152 と**1回も違いません**。追加の解きは
「接点のそばで縁に着いたとき」だけ走るので、45ケースには一度も掛かりません。
`ssi_probe` 3/3 clean、`mesh_watertight_probe` 全検体 closed。

#### 残っていること

**これは常設30配置・90演算の実測範囲です。** 接点で終わる交線は、置き方に
よってはまだ別の出方をするはずです。**`newton_to_tangency`（射影する
ほう）は接点で止まる側の着地でまだ使っています**——そこは倍精度まで
詰まっているので触っていませんが、同じ偽の根を踏む余地は残っています。

---

### 4-154. 面積分は 100% が解析曲面でした — 9-3 の問いに数字が出ました（2026/08/29）

9-3 は「**内部表現に解析曲面を持つか、まだ決めていない**」と書いていました。
9-1 は「ブーリアン1回の 90% は面積の検算で、1枚に三角形 4万〜16万枚。
**減らすべきは1回の枚数で、そこは手つかず**」と書いています。

**では、その三角形はどんな曲面の上に敷かれているのか。** 数えました
（`ZENITH_INTEGRAL_WHY=1`。`boolean_envelope` の45ケース）。

| 曲面の形 | 何か | 回数 | 三角形 | 割合 |
| :--- | :--- | ---: | ---: | ---: |
| NURBS 次数 2×2・制御点 3×3 | 球／トーラスの四半パッチ | 560 | 4,755,811 | **53.9%** |
| NURBS 次数 2×1・制御点 3×2 | 円柱／円錐の四半パッチ | 508 | 4,068,038 | **46.1%** |
| | **合計** | 1,068 | **8,823,849** | 100% |

**自由曲面は1枚もありません。** 平面はここに出てきません——既に
`add_affine_face` がトリム境界の線積分で解析的に積んでいるからです。

**つまり、8,823,849 枚すべてが「閉じた式のある曲面」の上に敷かれています。**
NURBS はいまや**入れ物でしかありません**。9-1 が「円柱の面積に三角形は
要りません」と書いたのは、この状況のことです。

#### 9-3 の問いに対して、これは何を言うか

9-3 は「解析曲面を持つと3つ効く（人工物が消える・性能の桁が変わる・
フィレットが書ける）。**ただし安くはない**」と書いていました。

**性能の側については、値段の見積もりが変わります。** 交線を閉じた式に
差し替えるのは確かに大仕事ですが、**面積分は交差とは別の段**です。
支持曲面を認識して積み方だけ差し替えるなら、**交差・トリム・分類の経路は
1行も触りません**。

**そして賞金は 90% のうちの 100% です。**

#### さらに切り分けました — **真の円柱だけで 44.8%**（2026/08/29）

`v` を動かしてヤコビアンが変わるかで、円柱と円錐を分けました。

| 曲面の形 | 何か | 回数 | 三角形 | 割合 |
| :--- | :--- | ---: | ---: | ---: |
| 2×2・3×3 | 球／トーラス | 560 | 4,755,811 | 53.9% |
| 2×1・3×2、v 方向一定 ○ | **真の円柱** | 448 | 3,953,790 | **44.8%** |
| 2×1・3×2、v 方向一定 × | 円錐 | 60 | 114,248 | 1.3% |

**単独でいちばん大きいのは真の円柱です。** 円錐は 1.3% しかないので、
最初の一手は円柱に絞るのが得です。

#### 円柱の閉じた式（導いて、実測で確かめました）

**パラメータは角度そのものではありません**（有理2次だから）。ですが写像は
閉じた式で書けます。四半円の制御点 `(1,0), (1,1), (0,1)`・重み
`1, w, 1`（`w = cos(Δ/2)`）に対して

```text
tan θ(u) = [2u(1-u)w + u²] / [(1-u)² + 2u(1-u)w]
```

実測で一致しました（`u = 0.25` → 21.598°、式も 21.598°）。ヤコビアンは
`|p_u × p_v| = r · θ'(u) · h` で、u に依存します（実測 339.4〜397.6）。

**面積**は `θ` に変数変換すると

```text
面積 = r · h · （(θ, v) 平面での領域の面積）
```

**体積の寄与**はもっと良くて、`v` に依存しません。軸の1点を原点、軸方向を
`a`、半径方向の単位ベクトルを `ρ̂(θ)` とすると

```text
p × の外積 = h · r · θ' · ρ̂(θ)
p · (p_u × p_v) = h · r · θ' · (O·ρ̂(θ) + r)      ← v が消える
体積の寄与 = (h·r/3) · ∬ (r + O_x cos θ + O_y sin θ) dθ dv
```

`O_x, O_y` は軸の1点の、`e1, e2` 枠での成分です。**どちらも (θ, v) 平面の
境界だけで決まります**——グリーンの定理で、面の中身は要りません。

**残差は境界の標本だけになります。** いまは「境界の標本誤差 ＋ 中身の弦
誤差」なので、**精度は上がるはず**です（ただし未測定）。

#### 順番の提案（判断です。数字ではありません）

1. **真の円柱の四半パッチ**（44.8%）。閉じた式は上に導いてあります。
   `recognize_cylinder_patch` が既にあるので、認識はそれを使えます
2. **円錐**（1.3%）。式は円柱と別（`r` が `v` で変わるので `v` が消えません）。
   **賞金が小さいので後回しでかまいません**
3. **球／トーラスの四半パッチ**（53.9%）。いちばん大きいですが、式も
   いちばん重くなります
4. 交線を閉じた式にするのは**そのあと**。段が別なので、順に測れます

**「はずです」と書いたところは、まだ測っていません。** 有理2次の
パラメータは角度そのものではないので、そこの写像で詰まる可能性が
あります。**やってみて測るまでは、見積もりです。**

---

### 4-155. 直線の向きには割らない — 安いほうから取りました（2026/08/29）

4-154 で「面積分の 100% は解析曲面」と分かったので、**解析積分を書く前に、
もっと安い一手があるか**を見ました。ありました。

パラメータ格子の条項は「三角形の uv 範囲が1マスを超えたら、**弦誤差に
関係なく**割る」です（4-150）。**曲面がその向きに直線なら、割っても何も
良くなりません**——求積の則はそこで厳密で、弦誤差もちょうど 0 です。

円柱・円錐の四半パッチは母線が直線なので、**v 方向がこれに当たります**。

#### 表現ではなく、形で見ます

NURBS の重みを覗くのではなく、**中点が両端の平均に一致するか**を数点で
測ります。効く条件そのものなので、こちらのほうが素直です。他の表現でも
そのまま効きます。

#### 効果（実測、`boolean_envelope` の45ケース）

| | 前 | いま |
| :--- | ---: | ---: |
| 円柱の面積分の三角形 | 3,953,790 | **3,128,920**（−21%） |
| 円錐 | 114,248 | 105,504（−8%） |
| **合計** | 8,823,849 | **7,990,235**（−9.4%） |
| 曲面評価 | 59,003,683 | **57,008,047**（−3.4%） |
| supported / wrong / エラー | 44 / 0 / 1 | **同じ** |
| 解析解との最悪一致 | 6.68e-8 | **同じ** |

テストは **129 バイナリ・654 テスト・0 failed・警告 0**。

#### **思ったほどは効きません。理由も分かりました**

21% です。10倍を期待していました。**u 方向の格子条項が支配的だから**です。

四半円柱（半径 6・高さ 40）の 48分割で、たわみの目標は
`対角 / (分割 × 8) = 40.9 / 384 = 0.1065`。半径 6 で弦誤差がこれに収まる
角度は約 21.6 度なので、**弦の基準だけなら 90 度を4分割で足ります**。
ところが格子条項は 48 列を要求します。**12倍の開きがそのまま残ります。**

**そこを外すと精度が落ちます**——外して測ったら、板に穴を1つ開けた体積が
相対 1.04e-9 動きました（4-150）。**だから解析積分が要るのです。**
u 方向を厳密に積めれば、そもそも列が要りません。

**この一手は、解析積分の代わりにはなりません。** 安いところを先に取った
だけです。本丸は 4-154 の順番どおりです。

---

### 4-156. 円柱の面を、中身を刻まずに積みました（2026/08/29）

4-154 の順番どおり、**真の円柱**（面積分の 44.8%）から手を付けました。

#### 入れたもの

`recognise_cylindrical_patch` と `loop_cylindrical_moments`。式は 4-154 に
導いたものです。

```text
p_θ × p_v = κ · ρ̂(θ)                ρ̂ = cosθ·e1 + sinθ·e2、|κ| = r·h/Δv
面積      = |κ| · ∬ dθ dv
体積の寄与 = s·ν·|κ|/3 · ∬ (r + O·e1 cosθ + O·e2 sinθ) dθ dv
```

`∬` はグリーンの定理で境界だけになります（`g=1 → G=θ`、`g=cosθ → G=sinθ`、
`g=sinθ → G=−cosθ`）。**u 方向には1回も刻みません。**

**認識は測って決めます。** 母線がどの `u` でも同じベクトルか、中点が両端の
平均か、3点から当てた円に他の標本が乗るか、その円が軸と直交するか——
**1つでも外れたら `None` を返し、従来どおり三角形で積みます**。法線の
向き `ν` も理屈で決めず、中央で測ります。

#### **面積と体積しか要らない呼び出しに限ります**

慣性まで解析で書くのは別の仕事で、まだやっていません。そこで
`SurfaceIntegral` に「慣性は要らない」旗を足し、`compute_face_integral` と
新しい `compute_volume_from_brep` で立てます。

**呼び手を調べたら、ブーリアンは体積しか読んでいませんでした**——
`boolean_validation.rs` の3か所も、`brep_intersection.rs` の並べ替えも
`.volume` だけです。そこを `compute_volume_from_brep` に替えました。

#### 効果（実測、`boolean_envelope` の45ケース）

| 面積分の内訳 | 回数 | 三角形 |
| :--- | ---: | ---: |
| 球／トーラス | 560 | 4,755,811 |
| 円柱（三角形で積んだ） | 184 | 1,930,266 |
| 円錐 | 60 | 105,504 |
| **円柱（解析。刻まない）** | **264** | **0** |
| **合計** | 1,068 | **6,791,581** |

**円柱 448 回のうち 264 回が解析に乗りました。** 三角形は 4-154 の時点の
**8,823,849 → 6,791,581（−23%）**です（うち −9.4% は 4-155 ぶん）。

残る 184 回は、慣性まで要る呼び出し（`compute_from_brep`）から来ています
——`boolean_envelope` が自分の検算で呼んでいるぶんです。**本番の口
（検証つきブーリアン）では、そこも解析に乗ります。**

#### 答えは動きません

| | 前 | いま |
| :--- | ---: | ---: |
| supported / wrong / エラー | 44 / 0 / 1 | **同じ** |
| 解析解との最悪一致 | 6.68e-8 | **同じ** |
| `foreign_boolean_probe` 恒等式の最悪残差 | 4.32e-9 | **同じ** |
| `mesh_watertight_probe` | 全検体 closed | **同じ** |
| テスト | 654 | **654（+3 で 657）・0 failed・警告 0** |

**解析と三角形の食い違いは 2.1e-14**（素の円柱・トリムされたボア壁とも）。
常設テストにしました（`analytic_cylinder_integral_test`）——**閉じた式
（四半パッチ `r·(π/2)·h`、貫通穴の側面 `2πrh`）と、三角形で積んだ答えの
両方**に当てています。自分の式を自分で検算しないためです。

#### `solve work` は動きません（**数え方の話です**）

`boolean_envelope` の「曲面評価」は 56,434,063 のままです。あの数は
`boolean_solids_exact_result_unverified` を囲んだ区間だけを数えていて、
**面積分は検算の段**なので入りません。**ここを測るなら三角形の枚数で
見てください**（`ZENITH_INTEGRAL_WHY=1`）。

#### 残っていること

- **球／トーラス（53.9%）が手つかず**です。いちばん大きく、式もいちばん
  重くなります
- **円錐（1.3%）** も未対応。`r` が `v` で変わるので `v` が消えません
- **慣性は解析で書いていません。** 重心・慣性テンソルが要る呼び出しは
  従来どおり三角形で積みます

---

### 4-157. 球とトーラスも、中身を刻まずに積みました（2026/08/29）

4-154 の順番の3番目、**球／トーラス（面積分の 53.9%）**です。

#### 測ったら、2つは同じものでした

`u` を動かした線がどんな形かを測ると:

| | `u` 等パラメータ線 |
| :--- | :--- |
| 球 | 球心を中心とする**半径 10 の円**（法線が `v` で回る） |
| トーラス | 芯の円の上を中心とする**半径 4 の円**（同上） |

**どちらも「半径一定の円弧を、軸まわりに回した面」**でした。**1つの
経路で両方入ります**（`RevolutionPatch`）。

#### 式（sympy と手計算の両方で確かめました）

子午線を `ρ = ρ_c + r cosψ`、`z = r sinψ` とすると

```text
面積 = ∮ G(ψ) dφ            G = r(ρ_c ψ + r sinψ)
```

体積はもう少し要ります。`p·(p_u × p_v)` の `p` は**世界の原点からの
位置**なので、軸の上の点から見たぶんに、**原点からのずれのぶん**を足します。

```text
体積の寄与 = s·σ/3 · [ ∮H dφ + O · (−e1∮Gz cosφ dφ − e2∮Gz sinφ dφ + a∮Gr dφ) ]
```

#### **2回間違えて、2回とも実測で捕まりました**

**1つ目: 球の面積が 0 になりました。**

p-curve のループは、**3D で潰れる辺を落として**返ってきます。実測で球の
パッチは **3区間**しかなく、極の辺がありません。グリーンの定理は閉じた輪を
要求するので、そこが抜けると答えが狂います。**しかも落ちていたのはちょうど
全部でした**——残りの辺では `G(ψ) = 0` だったので、合計が 0 になりました。

隙間があれば `(u, v)` の直線で埋めるようにしました。

**2つ目: 原点から動かすと体積が 6.67 倍狂いました。**

`p` は世界の原点からの位置なのに、軸の上の点から見たぶんしか積んで
いませんでした。**原点に置いた球とトーラスでは、その項がちょうど 0 なので
気づけません。** 動かした立体で測って初めて出ました。

**常設テストには、動かした立体を入れてあります。**

#### 効果（実測、`boolean_envelope` の45ケース）

| 面積分の内訳 | 回数 | 三角形 |
| :--- | ---: | ---: |
| 球／トーラス（三角形で積んだ） | 220 | 2,502,753 |
| 円柱（三角形で積んだ） | 184 | 1,930,266 |
| 円錐 | 60 | 105,504 |
| **円柱（解析）** | 264 | 0 |
| **回転面（解析）** | 340 | 0 |
| **合計** | 1,068 | **4,538,523** |

**1,068 回のうち 604 回が解析に乗りました（56.6%）。三角形は 4-154 の
時点の 8,823,849 から 4,538,523 へ、48.6% 減りました。**

#### 答えは動きません

| | 前 | いま |
| :--- | ---: | ---: |
| supported / wrong / エラー | 44 / 0 / 1 | **同じ** |
| 解析解との最悪一致 | 6.68e-8 | **同じ** |
| `foreign_boolean_probe` 恒等式 | 4.32e-9 | **同じ** |
| `contact_placement_probe` | 81 ok / 9 refused、非多様体 0 / 0 | **同じ** |
| `mesh_watertight_probe` | 全検体 closed | **同じ** |
| テスト | 657 | **130 バイナリ・657 テスト・0 failed・警告 0** |

解析と三角形の食い違いは **2.6e-14〜4.1e-14**（素の球・素のトーラス）、
トリムされた面で **3.6e-9 / 6.0e-8**。常設テスト
（`analytic_surface_integral_test`、7本）で、**閉じた式**（`4πR²/8`、
`4π²Rr`、`r(π/2)h`、`2πrh`）と**三角形の答え**の両方に当てています。

#### 残っていること

- **円錐（1.3%）** はまだ三角形です。`r` が `v` で変わるので `v` が
  消えず、式が別になります
- **慣性は解析で書いていません。** 重心・慣性テンソルが要る呼び出しは
  従来どおり三角形で積みます。45ケースで三角形を使う 464 回のうち、
  多くはそちらです
- **9-1 の「400倍」は、まだ測り直していません。** 枚数は半分になりましたが、
  それが対話性にどう効くかは別に測る必要があります

---

### 4-158. 円錐も入りました — 円柱の式が、そのまま一般形でした（2026/08/29）

4-154 の順番の最後、**円錐（面積分の 1.3%）**です。

#### 測ったら、円柱の親戚でした

| | `u` 等パラメータ線 | 母線（`v` 方向） |
| :--- | :--- | :--- |
| 円錐 | 軸まわりの円。**半径が `v` で線形**（10 → 5 → 0） | **直線**（中点のずれ 0） |
| 円柱 | 同じ円。半径は一定 | 直線 |

**円柱は「半径が変わらない円錐」**でした。式を書き下すと、半径を一定に
した瞬間に 4-156 の円柱の式へ**そのまま戻ります**。そこで**円柱の経路を
一般化**しました（`RuledRevolutionPatch`）——重複が減り、円錐が入ります。

```text
面積 = L · ε · ∮ ρ(v)·θ dv                 L = sqrt(dρ/dv² + dz/dv²)
体積 = s·σ·ε/3 · ∮ [A(v)·θ + B(v)(O·e1 sinθ − O·e2 cosθ)] dv
A(v) = ρ·(−dρ/dv·(O·a + z) + dz/dv·ρ)      B(v) = ρ·dz/dv
```

#### 潰れた端は、避けるか埋めるかします

真の円錐は**片方の端が1点**なので、そこで円を当てると失敗します。
**両端を避けて `w = 0.2 / 0.5 / 0.8` で当て、線形性を確かめてから
外挿**します。角を読む `v` も、半径がいちばん大きい端にします。

p-curve のループも、頂点の辺を落として返ります（球の極と同じ。4-157）。
**輪は 4-157 と同じやり方で閉じます。**

#### 効果（実測、`boolean_envelope` の45ケース）

| 面積分の内訳 | 回数 | 三角形 |
| :--- | ---: | ---: |
| 球／トーラス（三角形で積んだ） | 220 | 2,502,753 |
| 円柱（三角形で積んだ） | 184 | 1,930,266 |
| 円錐（三角形で積んだ） | 24 | 71,416 |
| **母線が直線の回転面（解析）** | 300 | 0 |
| **回転面（解析）** | 340 | 0 |
| **合計** | 1,068 | **4,504,435** |

**1,068 回のうち 640 回（59.9%）が解析に乗りました。** 三角形は 4-154 の
時点の 8,823,849 から **4,504,435（−49.0%）**です。

#### 答えは動きません

supported 44 / wrong 0 / エラー 1、最悪一致 6.68e-8、`foreign_boolean_probe`
恒等式 4.32e-9、`contact_placement_probe` 81 ok / 9 refused・非多様体 0 / 0、
`mesh_watertight_probe` 全検体 closed——**すべて不変**。テストは
**130 バイナリ・661 テスト・0 failed・警告 0**。

解析と三角形の食い違いは、真の円錐・円錐台・動かしたもの・穴あき箱・
円錐で削った箱のすべてで **2.1e-14 以下**。常設テストを 10本にしました
（`analytic_surface_integral_test`）——閉じた式は `πr√(r²+h²)`、
`π(r1+r2)√((r1−r2)²+h²)` を足しています。

#### **これで解析曲面は全部入りました**

4-154 で数えた3種（円柱 44.8%・球／トーラス 53.9%・円錐 1.3%）が揃い、
**面積分に出てくる曲面はすべて解析経路を持ちます**。

**残る 4,504,435 枚は、慣性まで要る呼び出しのものです**（`compute_from_brep`。
重心・慣性テンソルは解析で書いていません）。そこを書けば、45ケースの
面積分から三角形が**ほぼ消えます**。

---

### 4-159. 慣性を諦めた口へ全部通したら、三角形が 35分の1 になりました（2026/08/29）

4-158 で「残る 450万枚は慣性まで要る呼び出しのもの」と書きました。
**本当にそうかを確かめました。**

#### 呼び手を数えたら、ほとんど体積しか読んでいませんでした

`compute_from_brep(...)` の呼び出しを全部見ると、**読んでいるのはたいてい
`.volume` だけ**でした。慣性まで使うのは Python の口
（`zenith_py`）と、慣性のテストだけです。

残っていた `.volume` だけの呼び手を `compute_volume_from_brep` に替えました。

| 場所 | 何を読んでいたか |
| :--- | :--- |
| `interference.rs` の重なり体積 | `.volume` だけ |
| `boolean_envelope` の検算 | `.volume` だけ |

**`boolean_envelope` は測る側です。** ここが慣性まで積んでいたので、
**「カーネルの仕事」を測っているつもりで、プローブ自身の仕事を測って
いました。** 読む値は変わらないので、切り替えても測定の意味は同じです
（実測でも supported 44 / 0 / 1、最悪一致 6.68e-8 は不変）。

#### 効果（実測、`boolean_envelope` の45ケース）

| 面積分の内訳 | 回数 | 三角形 |
| :--- | ---: | ---: |
| 球／トーラス（三角形で積んだ） | 120 | 138,240 |
| 円柱（三角形で積んだ） | 84 | 96,768 |
| 円錐（三角形で積んだ） | 12 | 13,824 |
| **母線が直線の回転面（解析）** | 412 | 0 |
| **回転面（解析）** | 440 | 0 |
| **合計** | 1,068 | **248,832** |

**1,068 回のうち 852 回（79.8%）が解析。三角形は 4-154 の時点の
8,823,849 から 248,832 へ——35分の1（−97.2%）です。**

#### 答えは動きません

supported 44 / wrong 0 / エラー 1、最悪一致 6.68e-8、`foreign_boolean_probe`
恒等式 4.32e-9、`contact_placement_probe` 81 ok / 9 refused・非多様体 0 / 0、
`mesh_watertight_probe` 全検体 closed、`interference_probe` exit 0
——**すべて不変**。テストは **130 バイナリ・664 テスト・0 failed・警告 0**。

#### 一連の流れ（4-154 → 4-159）

| | 三角形 | 何をしたか |
| :--- | ---: | :--- |
| 出発点 | 8,823,849 | 4-154 で「100% が解析曲面」と測った |
| 4-155 | 7,990,235 | 直線の向きには格子条項を掛けない |
| 4-156 | 6,791,581 | 円柱を解析で積む |
| 4-157 | 4,538,523 | 球・トーラスを解析で積む |
| 4-158 | 4,504,435 | 円錐を解析で積む |
| **4-159** | **248,832** | 体積だけの口へ全部通す |

#### 残っていること

- **残る 248,832 枚は、本当に慣性が要る呼び出し**（`zenith_py` の物性値、
  慣性のテスト）と、`compute_from_brep` を通る少数の経路です。**そこを
  解析で書けば消えますが、賞金はもう小さい**です
- **9-1 の「対話性 400倍」は、まだ測り直していません。** 面積分の三角形は
  35分の1 になりましたが、**それが壁時計にどう出るかは別の話**です
  （9-1 のとおり、この環境の壁時計は 47% 揺れます）。**次に測るなら、
  そこを決めてからにしてください**

---

### 4-160. 点を1つ選ぶために、2万枚の三角形を作っていました（2026/08/29）

4-159 の末尾に「対話性はまだ測り直していない」と書きました。**測る前に、
測れるようにしました。**

#### まず、数え上げに載せました

3-1b は「**壁時計では判定できない。数え上げで測る**」と決めています。
ところが**面積分は数え上げに出ていませんでした**——`boolean_envelope` の
`solve work` は `boolean_solids_exact_result_unverified` を囲んだ区間だけで、
面積分は**検算の段**だからです（4-156 でそこを取り違えました）。

走行全体の数を1行足しました。

```text
face integrals: 2745 calls, 660 uv triangulations, N triangles   (whole run, deterministic)
```

**これで同じバイナリで前後を測れます**（`ZENITH_NO_ANALYTIC_FACE=1` が
「解析を入れる前」に相当）。

| | 解析あり | 解析なし |
| :--- | ---: | ---: |
| uv 三角化の回数 | 660 | 1,512 |
| uv 三角形 | 8,994,423 | 16,735,826 |

#### 数えたら、**97% は積分ですらありませんでした**

内訳を出したところ:

| | 回数 | 三角形 |
| :--- | ---: | ---: |
| 面積を積むため | 216 | 248,832 |
| **点を1つ選ぶため** | **444** | **8,745,591（97%）** |

`representative_face_point` と `spread_face_points` が、**面の中の点を
1つ選ぶためだけに、トリム領域を丸ごと三角化**していました。

#### 重かったのは細分ではなく、earcut のほうでした

最初「細分が要らないのだろう」と思って外しました。**1枚も減りません
でした。** 1回あたり約2万枚は、**earcut が出す枚数そのもの**です——
境界を「1点も落とさない」精度（`LoopFidelity::Exact`）で標本しているので、
折れ線が数千点になり、earcut がそのぶんの三角形を出します。

**点を1つ選ぶだけなら、そこまで要りません。** 表示用の適応標本
（`LoopFidelity::Display`）で足ります。平面側の
`planar_point_clear_of_holes` も、前から境界を8点で標本しています。

`face_uv_triangulation_for_point_picking` を足し、この2か所を移しました。

#### 効果（実測、`boolean_envelope` の45ケース）

| | 前 | いま |
| :--- | ---: | ---: |
| uv 三角形（走行全体） | 8,994,423 | **3,565,404**（−60%） |
| **解く段の曲面評価** | 55,540,495 | **51,942,445**（−6.5%） |
| supported / wrong / エラー | 44 / 0 / 1 | **同じ** |
| 解析解との最悪一致 | 6.68e-8 | **同じ** |

**解く段の数が動いたのは、これが初めてです。** 4-155〜4-159 は検算の段
だけを軽くしていました。ここは分類が通る道なので、解く段に出ます。

#### **これは挙動が変わる変更です**

標本する点が変わるので、**分類の答えが変わり得ます**。4-127 では代表点が
接触点そのものに落ちて誤分類になった、という例がありました。

だから広く測りました。

| ゲート | 結果 |
| :--- | :--- |
| 全テスト | **132 バイナリ・664 テスト・0 failed・警告 0** |
| `contact_placement_probe` | 81 ok / 9 refused、**B-Rep もメッシュも非多様体 0** |
| `foreign_boolean_probe` | ok 26 / NOCUT 4 / 拒否 0 / WRONG 0 / PANIC 0、恒等式 **4.32e-9** |
| `foreign_cross_pair_probe` | 3/3 そろい、恒等式の破れ 0、残差 9.235e-10 |
| `mesh_watertight_probe` | 全検体 closed |

**どれも動いていません。**

#### 残っていること

- **9-1 の「対話性 400倍」は、まだ測り直していません。** 数え上げは
  減りましたが、**壁時計は 3-1b のとおり使えません**。時間を主張するなら、
  まず**測り方を決める**必要があります
- 残る 3,565,404 枚のうち、点を選ぶためのぶんがまだ多いはずです。
  **そもそも三角化せずに点を取る**（p-curve から直接）ほうが筋ですが、
  まだやっていません

---

### 4-161. 点を選ぶのに、三角化そのものをやめました（2026/08/29）

4-160 で「境界を粗く標本すれば earcut が軽くなる」まで来ましたが、
それでも**1回あたり 7,469 枚**でした。**そもそも三角化が要りません。**

#### p-curve の多角形から直接選びます

平面側の `planar_point_clear_of_holes` は、前からこうしています——境界を
標本して uv の多角形にし、格子を走らせて**縁からいちばん遠い点**を採る。
曲面は **p-curve が最初から uv にある**ので、射影すら要りません。

`pcurve_uv_polygons` と `uv_points_clear_of_holes` を足し、
`representative_face_point` と `spread_face_points` の曲面の枝を移しました。
p-curve が無い面のために、三角化の受け皿は残してあります。

#### 効果（実測、`boolean_envelope` の45ケース）

| | 4-160 | いま |
| :--- | ---: | ---: |
| uv 三角化の回数 | 660 | **216**（積分のぶんだけ） |
| uv 三角形 | 3,565,404 | **248,832**（−93%） |
| **解く段の曲面評価** | 51,942,445 | **45,382,891**（−12.6%） |
| supported / wrong / エラー | 44 / 0 / 1 | **同じ** |
| 解析解との最悪一致 | 6.68e-8 | **同じ** |

**点を選ぶための三角化は 0 回になりました。**

#### **退行を出しました。測って直しました**

最初、境界の標本を **1区間あたり8点**にしました（平面側と同じ数）。
テストは全部通り、`contact_placement_probe` も
`foreign_cross_pair_probe` も動きませんでした。**それでも
`foreign_boolean_probe` が `ok 26 → 24`、`refused 0 → 6` になりました。**

誤答ではありません（WRONG 0・PANIC 0）が、**通っていた6演算が断られる**
のは退行です。多角形が粗いと、選んだ点が真の領域の外に落ちて分類が
変わります。

**1区間あたり 24点**に上げたら `ok 26 / refused 0` に戻り、仕事量は
1つも増えませんでした（45,382,891 のまま——多角形の標本は earcut に
比べれば無視できる）。

**平面側と同じ数で足りると思ったのが間違いでした。** 曲面の p-curve は
平面の直線より曲がるので、同じ点数では粗すぎます。

#### 広く測りました

| ゲート | 結果 |
| :--- | :--- |
| 全テスト | **130 バイナリ・664 テスト・0 failed・警告 0** |
| `foreign_boolean_probe` | ok 26 / NOCUT 4 / 拒否 0 / WRONG 0 / PANIC 0、恒等式 4.32e-9 |
| `contact_placement_probe` | 81 ok / 9 refused、B-Rep もメッシュも非多様体 0 |
| `foreign_cross_pair_probe` | 3/3、恒等式の破れ 0、残差 9.235e-10 |
| `mesh_watertight_probe` | 全検体 closed |

#### ここまでの積み上げ（4-154 → 4-161）

| | 解く段の曲面評価 | uv 三角形（走行全体） |
| :--- | ---: | ---: |
| 4-153（解析を入れる前） | 59,003,683 | 16,735,826 |
| **4-161（いま）** | **45,382,891** | **248,832** |
| | **−23.1%** | **−98.5%** |

**答えは1つも動いていません。**

---

### 4-162. 面積分から三角形が消えました — 断り文を読んだだけでした（2026/08/29）

4-161 のあと、残っていたのは **216 回・248,832 枚**でした。「慣性まで要る
呼び出しのぶんだろう」と書いていましたが、**確かめたら違いました。**

#### 数える前に、断り文を読みました

`compute_from_brep`（慣性まで積む口）は、カーネル本体からもう呼ばれて
いません（残るのは `regularize.rs` のテスト補助だけ）。**では 216 回は
どこから来るのか。**

解析経路が断った理由を出す口を足して数えたら、**216 件すべてが同じ**
でした。

```text
ANALYTICWHY p-curve が無い    216
```

**形が認識できなかったのでも、境界積分が通らなかったのでもありません。**
`face.pcurves` が `None` だったので、入口で諦めていただけでした。

#### 三角化の経路は、前からその場で導出していました

`face_uv_triangulation_inner` には、こう書いてあります——

> p-curve を保持していない面は、**その場で導出してから**使う。以前は
> ここで諦めて全矩形のグリッドに落ちていた。

**同じことを解析経路でもすればよいだけ**でした。`face.pcurves(&tol)` を
呼ぶ2行です。

#### 効果（実測、`boolean_envelope` の45ケース）

| | 4-161 | いま |
| :--- | ---: | ---: |
| uv 三角化の回数 | 216 | **0** |
| uv 三角形 | 248,832 | **0** |
| **解く段の曲面評価** | 45,382,891 | **43,946,059**（−3.2%） |
| supported / wrong / エラー | 44 / 0 / 1 | **同じ** |
| 解析解との最悪一致 | 6.68e-8 | **同じ** |

**45ケースの面積分から、三角形が1枚も無くなりました。**

#### ここまでの積み上げ（4-153 → 4-162）

| | 解く段の曲面評価 | uv 三角形（走行全体） |
| :--- | ---: | ---: |
| 4-153（解析を入れる前） | 59,003,683 | 16,735,826 |
| **4-162（いま）** | **43,946,059** | **0** |
| | **−25.5%** | **−100%** |

9-1 は「1枚の面を積むのに三角形 4万〜16万枚を使っています」と書いて
いました。**その枚数が 0 になりました。**

#### 広く測りました

| ゲート | 結果 |
| :--- | :--- |
| 全テスト | **130 バイナリ・664 テスト・0 failed・警告 0** |
| `foreign_boolean_probe` | ok 26 / NOCUT 4 / 拒否 0 / WRONG 0 / PANIC 0、恒等式 4.32e-9 |
| `contact_placement_probe` | 81 ok / 9 refused、B-Rep もメッシュも非多様体 0 |
| `foreign_cross_pair_probe` | 3/3、恒等式の破れ 0、残差 9.235e-10 |
| `mesh_watertight_probe` | 全検体 closed |

#### 残っていること

- **これは 45ケースでの話です。** 解析に乗らない曲面（自由曲面、
  `SURFACE_OF_REVOLUTION` の変わり種など）が来れば、そこは三角形で
  積みます。**受け皿は残してあります。**
- **慣性は解析で書いていません。** 重心・慣性テンソルが要る口
  （`zenith_py` の物性値）は従来どおりです
- **9-1 の「対話性 400倍」は、まだ測り直していません。** 3-1b のとおり
  壁時計は使えないので、**測り方を決めるところから**です

---

### 4-163. 「対話性の 400倍」を測り直しました — 24倍になりました（2026/08/29）

9-1 は「対話的なモデリングで許されるのは 0.1 秒級。**38秒はその 400倍**で、
これは調整で埋まる差ではないと見ています」と書いていました。**判断です。
測り直しました。**

#### 測り方を先に決めました

3-1b は「壁時計では判定できない。数え上げで測る」と決めています。
**その決まりは変えません。** ただし今回は「桁が変わったか」を問うので、
**同じバイナリで前後を2回ずつ**測り、**幅も一緒に出します**
（`ZENITH_NO_ANALYTIC_FACE=1` が「解析を入れる前」に相当）。

| | run1 | run2 |
| :--- | ---: | ---: |
| 解析あり | 24 秒 | 23 秒 |
| 解析なし | 70 秒 | 69 秒 |

**同じ設定の2回は 1秒差**でした（4% と 1.4%）。3-1b が記録した 47% ほどは
揺れていません。**ただし 3-1b は 5〜7分の走行での記録なので、揺れ方が
同じとは限りません。判定はこれまでどおり数え上げで行います。**

#### 45ケース全体

| | 解く段 | **検算の段** | 全体 |
| :--- | ---: | ---: | ---: |
| 解析なし | 18.6 s | **51.4 s** | 70.0 s |
| **解析あり** | 16.1 s | **6.9 s** | **23.0 s** |

**検算の段が 7.5分の1**になりました。9-1 が「ブーリアン1回の 90% は面積の
検算」と書いていた、その段です。

#### 1演算あたり（いちばん重い `sphere × sphere` の和）

| | 解く段 | 検算の段 | 合計 |
| :--- | ---: | ---: | ---: |
| 解析なし | 1.69 s | 13.55 s | **15.24 s** |
| **解析あり** | 1.50 s | 0.91 s | **2.41 s** |

#### 400倍 → 24倍

0.1 秒を目標とすると、**いちばん重い1演算で 24倍**です（9-1 の 38秒なら
380倍、この日の解析なしで 152倍）。

**「調整で埋まる差ではない」は、半分だけ当たっていました。** 調整では
埋まりませんでしたが、**積み方を変えたら 1桁動きました**。残る 24倍は、
まだ調整の範囲ではありません。

#### 何がまだ重いか（次に見るところ）

**いまは解く段のほうが重い**です（16.1 s 対 6.9 s）。9-1 が書いていた
「90% は面積の検算」という配分は**逆転しました**。

- 解く段 16.1 秒の中身は、まだ測っていません。**そこを数えるところから**
  です（`solve work` は 43,946,059 回の曲面評価と 1,232,730 回の
  マーチングのニュートン反復）
- 9-3 が挙げていた「**交線を閉じた式に**」は、まだ手つかずです。
  平面 × 円柱、平面 × 球、円柱 × 円柱には閉じた式があります

#### 9-1 の表は古くなりました

「**性能**: それでも対話的モデリングの 0.1 秒級には遠い」の行は、
数字を入れ替えました。**この節の他の行（フィレット・2Dスケッチ・検体の
数）は測り直していません。**

---

### 4-164. 解く段の 54% は「どこから始めるか」でした（2026/08/29）

4-163 で配分が逆転した（解く段 16.1 s 対 検算の段 6.9 s）ので、**解く段を
数えました。**

#### 数え上げに答えが書いてありました

`work_counter` の注記に、前からこうあります——

> `point_surface_coarse_searches`: 出発点を渡されずに全域を粗く見た回数。
> 1回につき 17x17 の格子と8段の詰めで **353 回の曲面評価**を払う。
> **費用のほとんどが「どこから始めるか」に消える**。ここが減れば全体が減る。

実測は **67,712 回**。353 を掛けると **約 2,390万回**で、解く段の曲面評価
43,946,059 回の **54%** です。

#### どの呼び手かを数えました

呼び出し口に印を付けて数えると、**2か所で 67%** でした。

| 呼び手 | 回数 |
| :--- | ---: |
| `face.rs` の境界点の検証 | 31,860 |
| `face.rs` の p-curve 導出 | 25,227 |
| 距離計算 | 7,704 |
| 交線の種を作る | 7,548 |
| 面分割 | 6,288 |

**どちらも、点を順に射影していく形**です。並んだ点は互いに近いので、
**直前の結果が良い出発点**になります。種を渡す口
（`point_to_surface_seeded`）は前からありました——**この2か所だけ渡して
いませんでした**（p-curve 導出の細分ループは、前から渡しています）。

#### **種を無条件に信じると壊れます。2回とも実測で捕まりました**

| 試したこと | 何が起きたか |
| :--- | :--- |
| 検証で種を無条件に信じる | 境界点が最大 **14.14 離れている**と報告され、45ケースの1件目で落ちた（誤差が 1.83 → 3.75 → 5.71 … と単調に伸びる形） |
| p-curve 導出で種を無条件に信じる | **`contact_placement_probe` が 81 ok → 75 ok**（6演算が断られる）。**テストは全部通っていました** |

**近い点に落ちるとは限りません。** 継ぎ目のそばでは別の枝へ引きずられ、
そのまま後続を巻き込みます。

#### 直し方は同じでした——**答えを測ってから信じる**

どちらの口も「点は面の上にあるはず」と分かっているので、**種の答えが
その範囲に収まったときだけ採り**、収まらなければ全域を見直します。
**収まらなかったときは元の値段に戻るだけ**で、悪くはなりません。

#### 効果（実測、`boolean_envelope` の45ケース）

| | 4-163 | いま |
| :--- | ---: | ---: |
| **解く段の曲面評価** | 43,946,059 | **30,894,913**（−29.7%） |
| 全域を粗く見た回数 | 67,712 | **31,062**（−54%） |
| supported / wrong / エラー | 44 / 0 / 1 | **同じ** |
| 解析解との最悪一致 | 6.68e-8 | **同じ** |

**恒等式の残差はむしろ良くなりました。**

| | 前 | いま |
| :--- | ---: | ---: |
| `foreign_boolean_probe` | 4.32e-9 | **1.98e-9** |
| `foreign_cross_pair_probe` | 9.235e-10 | **7.052e-10** |

`contact_placement_probe` 81 ok / 9 refused・非多様体 0 / 0、
`mesh_watertight_probe` 全検体 closed、テストは
**130 バイナリ・664 テスト・0 failed・警告 0**。

#### 積み上げ（4-153 → 4-164）

| | 解く段の曲面評価 | uv 三角形 |
| :--- | ---: | ---: |
| 4-153 | 59,003,683 | 16,735,826 |
| **4-164** | **30,894,913** | **0** |
| | **−47.6%** | **−100%** |

#### 残っていること

- **まだ 31,062 回**が全域を粗く見ています。上の表の残り（距離計算、
  交線の種、面分割）です。**同じ手が効くかは、それぞれ測ってください**
- `point_to_surface` の格子そのもの（17x17 ＋ 8段）は触っていません。
  **種が無いときの値段**は変わっていません

---

### 4-165. 射影の値段を下げようとして、**分類が細部に依存している**と分かりました（2026/08/29。測って戻しました）

4-164 のあと、まだ **31,062 回**が全域を粗く見ています。種を持てない
呼び出し（距離計算、交線の種、面分割）が残るので、**種が無いときの値段
そのもの**を下げられるか試しました。

#### 試したこと

`point_to_surface` は 17x17 の格子（289 回）＋8段の詰め（64 回）＝
**353 回**の曲面評価を払います。**格子を粗くして、詰めを増やす**——
1段ごとに目盛が半分になるので、段を足せば同じところへ届くはずです。

9x9（81 回）＋10段（80 回）＝ 161 回。

| | 前 | 試した後 |
| :--- | ---: | ---: |
| 解く段の曲面評価 | 30,894,913 | **24,918,362**（−19.3%） |
| `contact_placement_probe` | **81 ok** | **78 ok**（3演算が断られる） |

#### **粗さのせいではありませんでした**

格子を振って測ると、こうなりました（詰めは10段のまま）。

| 格子 | 結果 |
| ---: | :--- |
| 8 | 78 ok |
| **10** | **81 ok** |
| 12 | 78 ok |
| 14 | 78 ok |
| **16（元の値）** | **78 ok** |

**元の格子のままでも、詰めを 8段 → 10段 に増やすだけで 81 → 78 になります。**
つまり**詰めを増やすほうが悪くなる**。10 で 81 に戻るのは、たまたま
良いところへ落ちているだけです。

#### 本当の所見

**射影の答えの細部で、通っていた演算が断られます。**

**※ ここで「分類が依存している」と書きましたが、外れでした**——4-166 で
多数決にしても揺れは止まらず、断り文を読むと**組み立てが閉じない**ほう
でした。訂正は 4-166 にあります。

**そこを直さずに値段だけ下げると、必ずどこかが落ちます。** 戻しました。

#### 次に触る人へ

- **`point_to_surface` の 16 と 8 は動かさないでください。** 動かすなら、
  まず**分類が射影の細部に依らないようにする**ほうが先です
- 値段を下げたいなら、**種を渡せる呼び出しを増やす**ほうが安全です
  （4-164 の手。答えを測ってから信じる形にすれば、悪くなりません）
- 残る 31,062 回の内訳は 4-164 の表にあります

---

### 4-166. 「分類が細部に依存している」は**外れ**でした — 揺れているのは組み立てです（2026/08/29）

4-165 で「分類が射影の答えの細部に依存している」と書きました。**判断です。
測ったら違いました。**

#### 直そうとしたこと

`classify_face_against_mesh` の最後の内外判定は、**代表点1つ**で決めて
います。1点が動けば裏返るので、散らした点の**多数決**にすれば頑健に
なるはず——と考えて入れました。

#### 揺れは止まりませんでした

射影の詰めを 8段 → 9段 に増やすと、**多数決を入れても入れなくても**
`contact_placement_probe` は **81 ok → 78 ok** になります。仕事だけが
2% 増えました（30,894,913 → 31,527,229）。

**効果の測れないものは入れない**ので、戻しました。

#### 断り文を読んだら、場所が違いました

新たに断られる3演算は `sphere × cylinder (eccentric)` の和・差・積で、
理由は**「まだ実装していない」**——つまり**組み立てが閉じない**ほうです。
分類の誤りではありません。

```text
REFUSEWHY sphere x cylinder (eccentric intersection) union: Exact B-Rep boolean is not implemented yet
REFUSEWHY sphere x cylinder (eccentric intersection) difference: ...
REFUSEWHY sphere x cylinder (eccentric intersection) intersection: ...
```

**この配置は 4-152 / 4-153 で直したところです。** 交線が接点で終わり、
A 側と B 側の端が突き合うかどうかで縫合が決まります。射影が変わると
**端の位置がわずかに動いて、突き合わなくなる**。

#### 分かったこと

**接点の着地は、まだぎりぎりです。** 4-153 で「射影で1本に潰さず
過剰決定系で解く」ようにして 0 になりましたが、**入口の射影が変わると
崩れる**程度の余裕しかありません。

**性能側を動かしたいなら、直す場所はここです**——分類ではなく、
**接点の着地の頑健さ**。

#### 次に触る人へ

- **`point_to_surface` の 16 と 8 は、まだ動かさないでください**（4-165）。
  動かすと `sphere × cylinder (eccentric)` が落ちます
- 落ちる理由は分類ではありません。**`newton_to_tangency_least_squares` が
  どこから始めても同じ点に着くか**を測るところからです
- 4-165 に「分類が細部に依存している」と書きましたが、**外れです。**
  この節で訂正しました

---

### 4-167. 揺れの場所を、端点まで詰めました（2026/08/29。直っていません）

4-166 で「揺れているのは組み立て」まで来たので、**どこがどう動くか**を
端点で測りました。

#### 測り方

射影の詰めの段数を口にして（一時的に足して、測ったあと外しました）、
`sphere × cylinder (eccentric)` の交線の端を、継ぎ目 `(10,0,0)` の
まわりで並べます。

| | 詰め 8（通る） | 詰め 9（落ちる） |
| :--- | :--- | :--- |
| 継ぎ目のそばの端点 | **4本** | **3本** |
| ずれ | 2.5e-15 / 3.9e-13 / **1.3e-7** / 1.05e-9 | 1.5e-7 / 3.0e-8 / 7.5e-8 |

**弧が1本、継ぎ目から離れた場所へ行っています**（1e-3 の窓に入らなく
なる）。残った3本も、倍精度級（1e-13）から 1e-7 級に落ちています。

つまり**辿り方そのものが変わっています**。入口の射影が変わると種が動き、
弧の分かれ方まで変わる、ということです。

#### 試して外したもの（2つとも効果 0）

| 試したこと | 結果 |
| :--- | :--- |
| 分類を多数決にする（4-166） | 揺れは止まらず、仕事が 2% 増える |
| 接点の着地の条件を「同じ縁の上」に緩める | 揺れは**1つも**止まらない |

**どちらも戻しました。**

#### いま言えること

- **揺れているのは、接点で終わる弧の「分かれ方」です。** 端の位置の
  精度だけではありません（1本が丸ごと別の場所へ行きます）
- **`newton_to_tangency_least_squares` は、届いたところでは効いています**
  （2.5e-15、3.9e-13）。問題は**そこへ届く弧が変わる**ことです

#### 次に触る人へ

**直す筋は「接点を先に決める」だと考えます**（判断です。まだ測って
いません）。

いまは弧を辿ってから端で接点を解いています。順序を変えて、**面の組
ごとに接点を先に1回解き**、辿りの端をそこへ合わせれば、弧の分かれ方が
変わっても端は動きません。4-129 の末尾に同じことを書いています。

**ただし、端だけ動かすと曲線と食い違います。** そこをどう繋ぐかが本題で、
そこは測っていません。

- **`point_to_surface` の 16 と 8 は、まだ動かさないでください**
  （4-165、4-166）。ここが片づくまで、性能側は動かせません

---

### 4-168. 揺れの正体は掴みました。**刻みの手直しでは直りません**（2026/08/29）

4-167 の続きです。**弧がどこで止まっているか**を出しました
（`ZENITH_MARCH_WHY`。一時的に足して、測ったあと外しました）。

#### 止まる場所が分かりました

`sphere × cylinder (eccentric)` の交線を辿ると、継ぎ目のまわりで8本が
終わります。

| 詰め | 終わり方 |
| :--- | :--- |
| 8（通る） | 8本とも `(10,0,0)` から **5e-7 以内**。4本は接点で、4本は縁で終わる |
| 9（落ちる） | **1本だけ `(9.999999882, -0.001191467, -0.000972829)`**——接点の **1.5e-3 手前**で縁に着き、`tangency false` |

**その1本が、縫合の穴になります。**

#### なぜ解き直しが走らないか

`at_edge` の出口は `sine < TANGENCY_SINE_LIMIT`（1e-4）を入口にしています。
接点の 1.5e-3 手前では正弦が約 **1.3e-4** で、**線のすぐ外**です。
だから解き直しが走らず、端が 1.5e-3 ずれたまま残ります。

**しきい値のすぐ外で止まる弧がいる**、というのが正体です。

#### 5つ試して、5つとも外れました

| # | 試したこと | 結果 |
| ---: | :--- | :--- |
| 1 | 分類を多数決にする（4-166） | 揺れ止まらず、仕事 +2%。**戻した** |
| 2 | 接点の着地を「同じ縁の上」に緩める（4-167） | **1つも**止まらない。戻した |
| 3 | 入口を 100 倍広げる（1e-4 → 1e-2） | 止まらない。戻した |
| 4 | 「本当に接点か」を正弦 1e-9 で測って採る | 3 と併せて効果 0。戻した |
| 5 | `travel <= 歩幅×2` を 10倍・50倍に緩める | **どれも変わらない**。戻した |

**入口を広げても採られません。** 3 と 5 が効かないので、落ちているのは
`stays`（縁に張り付いた座標のビット一致）か、**最小二乗の解がそもそも
届いていない**かのどちらかです。**そこは測っていません。**

#### 次に触る人へ — **刻みではなく、順序を変えてください**

いまは**弧を辿ってから、端で接点を解いて**います。だから「どこから来たか」
で答えが変わります。

**接点は、面の組ごとに1点だけです。** 実測（4-168）: 偏心する球×円柱で
接点を持つ曲面の組は 8組あり、**8組とも同じ `(10,0,0)`** でした。13分割の
格子で正弦 2.8e-16 まで出ます。

```text
face_a  0 face_b 0 (10.0000,0.0000,0.0000) 正弦 3.553e-16
face_a  6 face_b 3 (10.0000,-0.0000,0.0000) 正弦 2.809e-16   …8組とも同じ点
```

**順序を変える**——面の組ごとに接点を先に1回解き、辿りの端をそこへ
合わせる。そうすれば弧の分かれ方が変わっても端は動きません。

**ただし端だけ動かすと曲線と食い違います。** そこをどう繋ぐか（曲線を
張り直すのか、端の区間だけ作り直すのか）が本題で、**そこは測って
いません**。

- ~~**`point_to_surface` の 16 と 8 は、まだ動かさないでください**~~
  → **4-169 で片づきました。** 揺れは消えています

---

### 4-169. 揺れは消えました。**当たりだけを弾く検査**が原因でした（2026/08/29）

4-168 の「順序を変える」を実装しました——`settle_ends_on_tangency` を
`ssi_march.rs` に置き、`march()` が `worst` を出す前に、**弧の両端が接点の
近くなら、そこを最小二乗で接点へ解き直します**。

端を動かしてよい理由は測ってあります。曲線は `marched.points` から
`fit_curve` で張られ、Edge の頂点は `curve.evaluate(t0/t1)` から来ます。
**点を動かせば曲線の端も一緒に動く**ので、4-168 が案じた「端だけ動かすと
曲線と食い違う」は起きません。

#### **1度も走っていませんでした**

最初の実装は、基準（81件）を保つだけで揺れが止まりませんでした。
**「効かなかった」と書く前に、走った回数を数えました**（3-1b）。

```text
=== 詰め 8 ===   68 正弦が大きい / 8 接点でない inf / 4 接線が取れない
=== 詰め 9 ===   68 正弦が大きい / 12 接点でない inf
```

**「合わせた」が 0 行。** そして落ちた理由は `inf` ——「接点でない」の
測り値が無限大でした。

原因は検査そのものです。着地点の正弦を `Self::tangent` で測っていました。
**`tangent` は外積を正規化して返すので、接点ちょうどでは `None` を返します**
（外積が 0 だから）。それを `unwrap_or(INFINITY)` で「接点でない」に
落としていました——**当たりだけが弾かれる検査**でした。

外積の長さを直に取る（`normal_cross(...).norm()`）ように変えると、合わせが
走り出しました（詰め8で 7件、詰め9で 11件）。

#### 実測

`contact_placement_probe` を、射影の詰め回数を変えて揺すった結果:

| 詰め回数 | 4-168 まで | **4-169** |
| ---: | ---: | ---: |
| 8（既定） | 81 ok / 9 断り | **84 ok / 6 断り** |
| 9 | 78 | **84** |
| 10 | 78 | **84** |
| 12 | 78 | **84** |

**揺れが消え、既定でも 3演算増えました。**

**断られる 6件の内訳**（**書いてから数え直しました**。最初「4-132 の
トーラス×トーラス」と書きましたが、**外れです**——トーラス×トーラスは
3演算とも通っています）:

| 件 | 断り方 |
| :--- | :--- |
| 箱×円柱（接する）差 | **正しい断り**。壁の厚みが 0 になる |
| 箱×円柱（継ぎ目 33度）差 | **正しい断り**。場所を名指ししている |
| 箱×トーラス（上面が接する）差 | **正しい断り**。場所を名指ししている |
| 箱×トーラス（上面が接する）和 | **穴**。未実装（あぶれた稜 48、非多様体 24） |
| 箱×トーラス（上面が接する）交 | **穴**。未実装（あぶれた稜 56） |
| 円柱×円柱（母線で接する）和 | 断るのは正しい（答えが本当に非多様体）が、**断り文が「未実装」**。場所を名指しすべき（4-131） |

**本当の穴は 2件**（箱×トーラスの和と交）、**文言の誤りが 1件**、
残り 3件は規約どおりです。

---

### 4-170. 断り文が「未実装」になる理由。**稜が短すぎて測れません**（2026/08/29）

4-169 で残った「文言の誤り 1件」——円柱×円柱が母線で接する和——を測りました。
答えは本当に非多様体（母線1本を4枚の面が共有する）なので、`contact` の
`find_result_pinch` が場所を名指しできるはずでした。**していません。**

#### 最初の見立ては外れました

「和では材料が輪の全周を埋めるので、`union_arcs` が繋げてしまい 1個と
数える」——**そう考えて、測りました。**

```text
CONTACTWHY 稜 8 本を測ります
CONTACTWHY   稜の長さ 3.161081e-4 半径 3.161081e-6 閾値 1.000000e-4
CONTACTWHY   稜の長さ 5.197072e-4 半径 5.197072e-6 閾値 1.000000e-4  …8本すべて同様
```

**輪を1度も張っていません。** `pinch_along_edge` は入口で抜けています——
輪の半径は稜の長さの 1/100 で、8本とも `3e-6` 前後。公差 `1e-6` の 3倍
しかなく、内外の判定が雑音になります。

**閾値は動かしません。測らないのが正しい**です。

#### 本題は、稜がそもそも無いこと

接する母線は、円柱の長さぶん（**約 40**）の1本の直線であるはずです。
実際に来ているのは **長さ 3〜5e-4 の破片が 8本**。辿りが接線の上で
進めていません。

接する母線の上では **n1×n2 が至るところ 0** です。4-169 で直したのは
「接点が1点だけ」の場合で、そこでは接点の両側に普通の交線があります。
こちらは**交線まるごとが接している**ので、辿る向きが最初から決まりません。
**別の問題です。**

#### 次に触る人へ

- 閾値（`length * 1e-2`、`tol.linear * 100.0`）を下げても直りません。
  **短い稜を測れるようにするのではなく、長い稜が出るようにする**話です
- 接する交線（母線・円まるごと）は、辿りではなく**認識**で出すのが筋です
  ——円柱2本の軸が平行で距離が半径の和なら、接する母線は解析的に1本
  求まります。**そこは測っていません**
- 箱×トーラスの上面接触（和・交、あぶれた稜 48/56）も、**半径 12 の円
  まるごとで接する**配置です。同じ族の可能性があります → **4-171 で
  測りました。違う族でした**

---

### 4-171. 残る 2件は、断りではなく**縫合の落ち**です（2026/08/29）

4-170 で「箱×トーラスの上面接触も、母線で接するのと同じ族かもしれない」
と書きました。**測ったら違いました。**

```text
CONTACTWHY 稜 40 本、長さ 3.449e0〜6.659e0、輪の半径の下限 1.000e-4
CONTACTWHY (19.2908 17.5948 20.0000) t=0.25 r=0.0479: pieces coarse 1
CONTACTWHY (18.4853 18.4853 20.0000) t=0.50 r=0.0479: pieces coarse 1
CONTACTWHY (17.5948 19.2908 20.0000) t=0.75 r=0.0479: pieces coarse 1   …40本すべて 1
```

**接する円は、ちゃんと稜として出ています。** 長さ 3.4〜6.7 の稜が 40本。
座標を確かめると `(18.4853, 18.4853, 20)` は中心 `(10,10)` から 12.0——
**接する半径 12 の円の上**です。母線のときの「長さ 3e-4 の破片 8本」
とは、まるで違います。

**そして材料は、どの稜のまわりでも 1塊です。** つまり——

> **和の答えは多様体で、返せるはずのものです。**

断られているのは規約のせいではありません。**縫合が落ちている**だけです
（あぶれた稜 和 48 / 交 56、非多様体 24）。

#### 分かったことの位置づけ

- 接する**円**は辿れます。接する**母線**（直線）は辿れません（4-170）。
  「接する交線は全部だめ」ではありません
- 残る「本当の穴 2件」は、**契約上の断りではなく実装の落ち**です。
  直せば 84 → 86 になります
- 次に測るのは、**あぶれた 48本がどこの稜か**です → **4-172 で測りました**

---

### 4-172. あぶれた稜の場所。**接する円が壁と交わる点で砕けています**（2026/08/29）

`ZENITH_STITCH_WHY` であぶれた稜を出しました。**2つの群に分かれます。**

**群1: 壁の上の長い稜**（長さ 3.42 / 6.65 / 6.93）。箱の側面
`x=0, x=20, y=0, y=20` の上にあります。

**群2: `z=20` ちょうどの、極端に短い破片**（長さ **0.000197** と
**0.000681**）。

```text
(0, 16.632909334, 20)  長さ 0.000680552
(0, 16.633347946, 20)  長さ 0.000196671      …壁ごとに2本ずつ、8か所
```

#### **破片の間に、答えが落ちています**

接する円は中心 `(10,10)`、半径 12、`z=20`。これが壁 `x=0` と交わる点は

```text
y = 10 + sqrt(144 - 100) = 16.633249581
```

破片は `16.632909334` から `16.633544617` までを覆っていて、
**真の交点 16.633249581 は、その2本のちょうど間にあります**（確かめ済み）。

つまり——**接する円が壁と交わるところで、辿りが砕けています。** 円の
途中では長さ 3.4〜6.7 の立派な稜が出ている（4-171）のに、交わる点の
まわり 0.0006 だけが、繋がらない破片2つになります。

#### 4-170 との関係

母線のときは**交線まるごと**が接していて、全長が破片でした。今回は
**円は辿れていて、他の交線と出会う1点だけ**が砕けます。**同じ「接して
いる場所で辿りが止まる」でも、規模が違います。**

そして 4-169 で入れた `settle_ends_on_tangency` は、**弧の端を接点へ
合わせる**ものでした。ここで要るのは「弧の端を、**別の交線との交点**へ
合わせる」ことです。**近いが、同じではありません。**

#### 次に触る人へ

- 直す先は縫合ではなく**辿りの端**です。あぶれているのは、交点をまたぐ
  0.0006 の区間が2つに割れて、どちらも相手を見つけられないからです
- **交点は先に解けます。** 接する円（解析的に出る）と壁の平面の交わりは
  連立2式で1点に決まります。辿りの端をそこへ合わせれば破片は消えるはず
  です。**そこは測っていません**
- 直れば 84 → 86。残りは規約どおりの断り 3件と、母線の 1件（4-170）です

---

### 4-173. **同じ稜が、2つの版で出ています**（2026/08/29）

4-172 の続き。あぶれた 48本を長さで数えました（出力は「選んだぶん」と
「蓋つき」で2回出るので、半分にしてあります）。

| 本数 | 長さ | 何か |
| ---: | ---: | :--- |
| 8 | 6.654184 | 壁の上の長い稜 |
| 8 | 6.928203 | 同上 |
| **8** | **3.423521** | 壁の上の稜 |
| **8** | **3.423580** | **↑と同じ場所の稜** |
| 8 | 0.000681 | 交点のそばの破片 |
| 8 | 0.000197 | 同上 |

#### **中点が同じで、長さだけ違います**

```text
(-0.000000000, 1.649413331, 19.865609770)  長さ 3.423521533
(-0.000000000, 1.649413331, 19.865609770)  長さ 3.423580637
```

**中点は 9桁まで一致**しています。同じ物理的な稜が、**長さの違う2つの版**
として出ています。差は **5.9e-5**——端 1つあたり 3e-5 です。

縫合の照合 `same_undirected_stitch_edge` は `tol.linear`（**1e-6**）で
測ります。**3e-5 は、その 30倍です。**——だから 8組すべてがあぶれます。

#### これで全部つながります

- 破片の長さ 0.00068 と 0.0002 は、**2つの版の食い違い**そのものです
- 辿りの精度が接点のそばで落ちるのは既知です（接点は二重根なので
  **√ε の精度**しか出ません）。1e-9 の残差なら位置は 3e-5——**実測と
  一致します**
- 普段の 84演算で縫合が通るのは、接していない交線では両側の版が 1e-6 の
  内側で一致するからです

#### 次に触る人へ

**公差を緩めてはいけません。** 1e-6 → 1e-4 にすれば通りますが、それは
「別の稜を同じと見なす」窓を 100倍に開けることです（4-71 で球の経線が
まさにそれで潰れました）。

**版を1つにするのが筋です。** → **4-174 で、どちらの版が誰のものか
測りました。**

---

### 4-174. 2つの版は、**A の版と B の版**でした（2026/08/29）

4-173 の「そこは測っていません」を測りました。あぶれた稜に、どちらの
立体の面片から来たかを付けます（`StitchEdgeUse::operand`。**照合には
使いません**——診断だけです）。

```text
  8 A 3.423521…    ←  箱の面片が持っている版
  8 B 3.423580…    ←  トーラスの面片が持っている版（同じ稜）
 16 A 6.654184     ←  箱だけ
 16 B 6.928203     ←  トーラスだけ
 16 A 0.000681 / 0.000197  ←  交点そばの破片。**全部 A**
```

**きれいに割れています。** 同じ物理的な稜について、**箱が持つ版と
トーラスが持つ版が別物**で、5.9e-5 食い違っています。

交線は面の組ごとに1回求めているはずなのに、面片が持つ段になると
**両者が自前の版を持っています**。どちらかで、面の上に引き直されて
いるということです（p-curve から曲線を張り直すと、面が違えば結果も
違います）。

そして交点そばの破片 16本は**全部 A** です——箱の側だけが、交点の
まわりを余分に刻んでいます。

#### 次に触る人へ

- 探す先は**面を切る段**です。交線 `Edge` が両方の面片に**同じ物**として
  渡っているか、それとも面ごとに引き直されているかを見てください
- 引き直されているなら、**引き直さないようにするのが直し**です。公差を
  緩めるのではありません（4-173）
- `ZENITH_STITCH_WHY` は A/B を出すようになりました。**この1行が、
  4-172 と 4-173 で2度かかった推測を1回で終わらせました**

---

### 4-175. **一本化の通しは既にあります。** 届いていないだけです（2026/08/29）

4-174 の「面を切る段を見てください」を辿りました。**探していたものは
既にありました**——`unify_coincident_edges` が、隣り合う面片が別々に
作った同じ稜を一本化します。

届いていない理由は、**門の公差**です。

| 門 | 公差 | 今回の食い違い |
| :--- | ---: | ---: |
| `unify_coincident_edges` の端点 | `tol.linear` = **1e-6** | **3e-5** |
| `curves_coincide` の途中の点 | `tol.linear * 10` = 1e-5 | （端点で落ちるので届かない） |
| `same_undirected_stitch_edge` の端点 | `tol.linear` = **1e-6** | **3e-5** |

**そして順番も効きません。** `unify_coincident_edges` が走るのは
**立体を組む段**で、縫合が「閉じた多様体か」を判定した**後**です
（`brep_intersection.rs:835`）。断りを決めているのは縫合の門のほうです。

#### **公差を上げるのは、直しではありません**

1e-6 → 1e-4 にすれば両方の門を通ります。**通してはいけません。**
3e-5 は誤差であって、そこに正しい答えがあるわけではないからです。窓を
広げれば、**別の稜どうしも同じと見なされ始めます**（4-71）。

#### 本当の直しは、4-169 と同じ薬です

食い違っているのは**端点だけ**です（中点は 9桁一致、4-173）。そして
その端点は、**接する円が箱の壁と交わる点**です。この点は3つの条件で
決まります——

```text
面 A の上   /   面 B の上   /   壁の平面の上
```

**未知数2つに式3つ。過剰決定で、位置が残差ではなく形で決まります。**
4-169 で接点に効いたのと同じ構図です（あちらは 6式4未知数）。ここを
先に解いて両側の端をそこへ合わせれば、**A の版と B の版は同じ点に
落ちます**。3e-5 は消えます。

- 実装は `settle_ends_on_tangency`（`ssi_march.rs`）の隣に置くのが
  自然です。あちらは「端を接点へ」、こちらは「端を**交点**へ」
- **まだ書いていません。** 直れば 84 → 86

#### **ただし、交点を解くには接する円が要ります**

その交点を過剰決定で解こうとすると、条件の1つが「`z=20` の平面の上」
になります。**トーラスはその平面に接している**ので、そこは二重根です
——また √ε に戻ります。

抜け道は、**接する円のほうを解析的に持つこと**です。トーラス（芯 12、
管 4、中心面 `z=16`）が `z=20` に接する線は、**中心 `(10,10)`、半径 12
の円**です。円として持てば、壁 `x=0` との交わりは

```text
y = 10 ± sqrt(144 - 100) = 16.633249581…
```

と**倍精度いっぱいで**出ます。二重根を通りません。

#### **残る2つの穴は、同じ一手で閉じます**

| 穴 | 接している線 | いま |
| :--- | :--- | :--- |
| 円柱×円柱（母線、4-170） | **直線**（母線） | 長さ 3e-4 の破片 8本 |
| 箱×トーラス（上面、4-171〜175） | **円**（半径 12） | 辿れるが端が 3e-5 ずれる |

どちらも「**接する交線を、辿りではなく認識で出す**」で閉じます
（4-170 で書いた筋です）。回転面と平面・回転面どうしの接触は、
軸と半径の関係だけで線が決まります。**辿る必要がありません。**

**次に触る人へ: これを1つ作れば 84 → 86 になり、母線の断り文も
場所を名指しできるようになります。** 別々の2件だと思って別々に
手を付けないでください。 → **4-176 で、半分は既にできていました。**

---

### 4-176. **接する円は、元から厳密に出ていました**（2026/08/29）

4-175 の見立て「接する円を解析的に持てば直る」を実装しにいきました。
**書き上げてから、走った回数を数えました**（4-169 の教訓）。

**全 90演算で 0回。** 一度も走りませんでした。**外しました。**

#### 既にあったもの

`iso_section_along`（`brep_intersection.rs`）が、軸に垂直な平面と回転面の
交わりを**等パラメータ線として厳密に**取り出します。接している場合も
通ります——パッチの端がちょうど平面に乗るので、挟み撃ちが端に収束します。

実測（箱の上面 × トーラス、40組が `Plane x Nurbs` で来る）:

```text
40 円柱でない → 等パラメータ線へ
72 手前: 断面だけで決まらない（= 36組が両向きとも外れ）
 → 残る 4組が、両向きの片方で通っている
```

**その4組が、トーラスの上端の四半パッチです。** 半径 12 の接する円は、
4本の等パラメータ線として**倍精度で**出ていました。

#### では 3e-5 はどこから来ているのか

**壁の交線の端**です。トーラスと箱の側面（`x=0` など）の交わりは、平面が
軸と平行なので等パラメータ線になりません——辿りに回ります。その弧の端が
`z=20`、つまり**接している高さ**に届いたところで、√ε の誤差を負います。

4-173 で見た「A の版 3.423521 / B の版 3.423580」は、まさに**壁の上の
稜**でした（中点の `z` は 19.8656）。接する円ではありません。

#### **見立ての誤り方**

4-175 で「接する円を解析的に持てば、壁との交わりが倍精度で出る」と
書きました。**円のほうは最初から厳密でした。** 足りないのは、
**辿った弧の端を、その厳密な円へ着地させること**です。

- 探す先は `settle_ends_on_tangency`（4-169）の隣です。あちらは「端を
  接点へ」。要るのは「**端を、隣の厳密な交線へ**」
- 端が乗るべき円は、`iso_section_along` が既に返しています。**同じ面の
  組の隣**（トーラスの同じパッチ × 箱の上面）から取れます
- **まだ書いていません**

#### **直す先の数字**（これで合っているか確かめられます）

あぶれた稜（A の版）の、接する高さ側の端:

```text
STITCHWHY unmatched A (16.633446254 0.000000000 19.999999999) -> …
```

| 量 | 実測 | あるべき値 | ずれ |
| :--- | ---: | ---: | ---: |
| `z` | 19.999999999 | 20 | **1.0e-9** |
| `y`（壁 `y=0` の上） | 0.000000000 | 0 | **厳密** |
| `x` | 16.633446254 | `10+√44` = 16.633249581 | **1.97e-4** |
| `(10,10)` からの距離 | 12.000108716 | 12 | **1.09e-4 外** |

**平面の上にも壁の上にも乗っているのに、トーラスの上にありません。**
接する円から 1.1e-4 外にいます。

要る解は**1未知数1式**です——「厳密な円の上」かつ「壁 `y=0` の上」で
`x = 10 + √44`。二重根を通らないので、倍精度いっぱいで出ます。
**この 16.633249581 が出れば直ったということです。** → **4-177 で
出しました（が、それだけでは足りませんでした）。**

---

### 4-177. 縁で終わる端を、縁へ厳密に置いて解き直す（2026/08/29）

`at_edge` の解き直しは、**接点のときだけ**走っていました（`sine_here <
TANGENCY_SINE_LIMIT` の中）。普通の縁抜けでは何もしていません——張り付いた
座標は `1e-9` の margin で「縁の上」と判定されるだけで**厳密に縁の値では
なく**、残りの座標はその中途半端な位置に合わせて決まっています。

`settle_end_on_bound`（`ssi_march.rs`）を足しました。張り付いた座標を
**その縁の値そのもの**に置き、残りを `p1 = p2` の3式から解き直します
（正規方程式の、固定した座標の行と列を単位にする）。**接している方向を
通らない**ので二重根になりません。

#### 実測

狙った端点:

| | 実測 | 厳密値との差 |
| :--- | ---: | ---: |
| 従来 | 16.633446254 | **1.97e-4** |
| **4-177** | **16.633249610** | **2.9e-8** |

**6800倍**近づきました（厳密値 `10+√44` = 16.633249581）。

代償はほぼありません——面の評価は **8,584,260 → 8,584,692（+432回、
+0.005%）**。

門はすべて保たれています: 84 ok / 6 断り（変わらず）、130本 / 664件 /
失敗 0 / 警告 0、恒等式の破れ 0・残差の最悪 7.052e-10、メッシュ全閉。

#### **それでも断りは減りません**

あぶれた稜は **48本のまま**です。A の版 3.423521 と B の版 3.423580 の
食い違いは**そのまま残っています**。

**一度読み違えました。** `head` で切った一覧に長い稜が出てこなかったので
「長い稜は直った」と書きかけました。**数え直したら 48本のままでした。**
切った一覧を「無い」の根拠にしてはいけません。

#### 残っているもの

直ったのは**片方の端だけ**です。同じ稜のもう片方は、まだ
`16.633446306` にいます（破片の長さ 0.00068 → 0.00034 に半減したのが
その証拠——**片端ぶんだけ縮みました**）。

- なぜ片方だけなのか。**測っていません**。辿りの向きで、片方の端は
  `at_edge` を通らずに終わっている可能性があります（閉じた弧、あるいは
  最大点数で打ち切られた側）→ **4-178 で測りました。外れです**
- そこが分かれば 84 → 86 に届くはずです

---

### 4-178. もう1つの着地口は、**もともと正確でした**（2026/08/29）

`run` には縁で終わる道が2つあります。4-177 で直したのは `at_edge` の
ほうで、もう1つは `landed = Some(trial)` の着地口です。そこには
**コードが予告を書いていました**——

> **こちらの着地口には、まだ極値の解を入れていません。** …同じ形の欠陥が
> 出たら、`land_on_crossed_bound` と同じものをここにも入れてください。

出たと思って、入れました。**そして測りました。**

```text
着地口で合わせた: 8853 件
そのうち 1e-9 を超えて動いたもの: 0 件
```

**8853回すべて、動く距離は 1e-9 以下**でした。この着地口は
**もともと正確**です。あぶれた稜は 1ビットも変わらず（48本のまま、
長さも同一）、仕事も残差も試験も完全に同じ。**外しました。**

#### 4-177 の見立ては外れでした

「破片が半分になったのだから、もう片方の端は別の口から出ている」と
書きました。**違いました。** もう片方の端は、この着地口からも来て
いません。

**どこから来ているのかは、まだ分かっていません。** 次に測るべきは
「あぶれた稜のもう片方の端が、そもそも `run` のどの出口で決まって
いるか」です。閉じた弧、最大点数での打ち切り、あるいは `march` の
外（`fit_curve` の後の分割）——**そこは測っていません**。

#### 学んだこと

**コードに書かれた「ここにも入れてください」は、予告であって根拠では
ありません。** 入れる前に、その口が本当にずれているかを測ってください。
今回は 8853件の全部がもともと 1e-9 以内で、入れる理由がありませんでした。

---

### 4-179. 継ぎ目に、**3つの違う値が並んでいます**（2026/08/29）

壁 `x=0` の、接する高さのそば（`y ≈ 16.6332`、`z = 20`）を全部並べました。

| `y` の値 | 誰の端か | 厳密値との差 |
| ---: | :--- | ---: |
| 16.632909332 | 長い弧 6.654523 の端 | **−3.40e-4** |
| **16.633249610** | 破片2本が共有する端 | **+2.9e-8** |
| 16.633446282 | 長い弧 3.423521 の端 | **+1.97e-4** |
| *16.633249581* | *厳密値 `10+√44`* | — |

**本来なら1点に集まるべきところに、3点あります。** そして間を長さ
0.000340 と 0.000197 の破片2本が埋めています——**その2本があぶれて
います**。

```text
A (0, 16.633249610, 20) -> (0, 16.632909332, 20)   長さ 0.000340
A (0, 16.633446282, 20) -> (0, 16.633249610, 20)   長さ 0.000197
```

#### 4-177 が直したのは、**破片の端だけ**でした

真ん中の 16.633249610 は 4-177 で出た値です。**長い弧2本の端は直って
いません。**

長い弧2本はどちらも**トーラス × 壁**の交線で、接する高さで終わります。
そこが `√ε` のままです。破片のほうだけ厳密になったので、**破片は
「長い弧の端」と「厳密な点」の食い違いそのもの**になりました
（0.000340 = 3.40e-4、0.000197 = 1.97e-4。**表の差とぴったり一致
します**）。

#### 次に触る人へ

- **狙いは 3点を1点にすることです。** 厳密な値は分かっています——
  `10 + √44 = 16.633249581`
- 長い弧の端が縁の解き直し（4-177）を受けていないのはなぜか。
  **測っていません。** `run` のどの出口で終わっているかから確かめて
  ください（4-178 で、もう1つの着地口ではないことは分かっています）
- 3点が1点になれば破片は消え、あぶれも消えるはずです。**84 → 86**

---

### 4-180. **この継ぎ目は、残差では決して決まりません**（2026/08/29）

3点を1点にする前に、「ニュートンで解き直せばいい」が**そもそも成り立つか**
を測りました。**成り立ちません。**

継ぎ目 `(10+√44, 0, 20)` での法線:

```text
トーラスの法線   [0, 0, 1]
上面 z=20 の法線 [0, 0, 1]      外積 0.000e+00  ← 縮退
壁 x=0 の法線    [1, 0, 0]      外積 1.000      ← 横断
```

**トーラスの法線と上面の法線が、そこで完全に一致します。** 3つの条件
（トーラスの上・上面の上・壁の上）で点を決めようとすると、**行列の階数が
2 しかありません**。どんな解き方をしても、縮退した向きの位置は残差から
決まりません——`√ε` はそこから出ています。

#### だから「解き直す」では届きません

4-177 の `settle_end_on_bound` が破片の端しか直せなかったのも、これで
説明がつきます。**縮退していない向き**（縁に張り付いた座標）は直せますが、
**縮退している向き**は直せません。

- 4-165〜4-168 で6つの手を試して戻したのも、4-176 で書いた枝が空振り
  したのも、**同じ壁**です
- **公差を緩める話でもありません。** そこに答えが無いのだから、窓を
  広げても入ってきません

#### 唯一の抜け道

**縮退した2つを、先に解析的に1つにまとめること。** トーラスと上面の
縮退した組は、**接する円**（中心 `(10,10,20)`、半径 12）です。円にして
しまえば、残る条件は壁1つ——**1未知数1式**で、横断的です。

```text
円 ∩ 壁 x=0  →  y = 10 ± √44   （倍精度いっぱい）
```

そして**その円は、`iso_section_along` が既に厳密に返しています**
（4-176）。**作る必要はありません。使えばいいだけです。**

#### 次に触る人へ

やることは1つです——**辿った弧の端が、隣の面の組の厳密な交線のそばに
あるなら、その交線と自分のもう一方の面との交わりとして解き直す。**

- 「厳密かどうか」は測れます。等パラメータ線は両方の面から `1e-15`、
  辿った曲線は `1e-9` です。**旗を立てる必要はありません**
- **端を動かすと曲線と食い違います。** 4-169 は `fit_curve` の前に点を
  動かしたので済みました。ここは既に曲線になった後なので、**そこをどう
  するかが本題**です。まだ答えを持っていません

---

### 4-181. 継ぎ目は**選べません。作るしかありません**（2026/08/29）

4-180 の「3点を1点に」を、**選ぶ**やり方で実装しました。継ぎ目に集まった
端をまとめ、いちばん良いものへ残りを揃える——旗を立てず、測って選ぶ形です。

**2つのものさしを試して、2つとも駄目でした。**

#### ものさし1: 面への距離の合計

```text
JOINTWHY 僅差 最良 1.066e-14  次点 1.138e-14
JOINTWHY 僅差 最良 1.198e-14  次点 1.389e-14   …20の固まり全部
```

**どの端も面から `1e-14` の内側**です。2e-4 の位置ずれが**まったく
見えません**。4-180 の縮退そのもの——面への距離は、縮退した向きの位置を
測れません。**書く前に分かっていたはずのことでした。**

#### ものさし2: 隣の交線への距離（自分の曲線は除く）

縮退しないので、こちらは差が出ました。

```text
JOINTWHY 僅差 最良 1.086e-4  次点 1.965e-4
```

**しかし、いちばん良い端でも `1.086e-4` です。** 0 になる端が**1つも
ありません**。

#### なぜ 0 が無いのか

**曲線どうしが食い違っているからです。** 継ぎ目に集まる3本の弧は、
それぞれ自分の端で `√ε` ずれています。正しい継ぎ目はどの曲線の上にも
乗っていません——**候補の中に正解が無い**のです。

> **選ぶ方式は、原理的に届きません。** 継ぎ目は、そこにある点から
> 選ぶのではなく、**解析的な円と壁から作る**しかありません。

#### 残っている唯一の道

```text
接する円（iso_section_along が厳密に返している）  ∩  壁の平面
  → 1未知数1式、横断的 → 倍精度いっぱい
```

やることは決まっています。**足りないのは「どの候補が厳密な等パラメータ
線か」を知る手段**です。4-181 で分かったとおり、**測っても分かりません**
（面への距離は全部 1e-14）。**作られ方を覚えておく**しかありません
——`FaceIntersectionCandidate` に「解析的に出したもの」の印を持たせる、
というのが素直な形です。

**印を立てるのは、この文書がずっと避けてきたことです。** ただし今回は
「印の代わりに測る」が**使えないと測った上での**判断です。

#### 学んだこと

**縮退した向きは、どんな残差でも測れません。** 4-180 で書いたことを、
4-181 で自分がもう一度踏みました。設計の前に、**そのものさしが問題の
向きを見られるか**を確かめてください。

---

### 4-182. 継ぎ目を**作りました**。84 → 85（2026/08/29）

4-181 の結論どおりに実装しました。

1. `FaceIntersectionCandidate` に `analytic` を持たせる。解析的に出した
   交線（平面どうし、平面×円柱の各経路、等パラメータ線）は `true`、
   辿って出したものは `false`
2. 辿って出した弧の端が、**面を1枚だけ共有する解析的な交線**のそばに
   あるなら、その交線と**3枚目の面**との交わりとして解き直す
   （`solve_curve_on_plane`。1未知数1式）
3. 端は制御点ごと動かす（クランプした NURBS は端の制御点をそのまま通る）

#### **旗を立てたことについて**

この文書は「旗を立てず、測る」を通してきました。**ここは例外です。**
ただし——**測っても分からないと測った上での**判断です（4-180、4-181）。
接する所では、辿って出した端も解析的に出した端も面から `1e-14` の内側で、
出来上がったものからは区別できません。**作られ方を覚えておくしか
ありません。**

#### 実測

```text
JOINTWHY (0, 16.633446282, 20.0) -> (0, 16.633249723, 20)  動き 1.966e-4
JOINTWHY (0, 16.632909332, 20.0) -> (0, 16.633249452, 20)  動き 3.401e-4
```

**3つ並んでいた値（4-179）が、1点に集まりました。** 厳密値
`10+√44 = 16.633249581` との差は **1.4e-7**（従来 1.97e-4 と 3.40e-4）。
16回作り直しています。

| | 従来 | **4-182** |
| :--- | ---: | ---: |
| 立体を返した演算 | 84 | **85** |
| 断り | 6 | **5** |

**箱×トーラス（上面が接する）の交**が通るようになりました。
**本当の穴は 2件 → 1件**です。

門はすべて保たれています——B-Rep 非多様体 0 / メッシュ非多様体 0、
`foreign_cross_pair_probe` 15組・恒等式の破れ 0・残差の最悪 7.052e-10、
メッシュ全閉、**130本 / 664件 / 失敗 0 / 警告 0**。面の評価は
**8,584,692 で変わらず**（増えていません）。

#### 残る 5件

| 件 | 断り方 |
| :--- | :--- |
| 箱×円柱（接する）差 | 規約どおり |
| 箱×円柱（継ぎ目 33度）差 | 規約どおり |
| 箱×トーラス（上面接触）差 | 規約どおり |
| **箱×トーラス（上面接触）和** | **穴。まだ縫えない** |
| 円柱×円柱（母線で接する）和 | 断りは正しいが文言が「未実装」（4-170） |

#### 次に触る人へ

- **和だけが残っています。** 交が通って和が通らないので、**選び方の側**に
  まだ何かあります。あぶれた稜を数え直すところから
- 3枚目の面が**平面のときだけ**扱っています。曲面どうしの継ぎ目は
  **測っていません**——`solve_curve_on_plane` を曲面へ広げる話です
- 円柱×円柱の母線（4-170）は、**接する線が辿れていない**ので別口です
  → **和のほうは 4-183 で測りました**

---

### 4-183. 和が通らない理由。**自分の規約を破っています**（2026/08/29）

4-182 のあと、和のあぶれた稜は **0本**、同方向も **0本**になりました。
残っているのは**非多様体の稜**だけです。そこには出す口が無かったので
足しました（`STITCHWHY non-manifold`）。

```text
32 本すべて  x4     ← 1本の稜を、4枚の面片が共有している
```

場所を測ると:

```text
(1.514718539, 1.514718601, 20)   (10,10) からの距離 12.000000079
(1.514718539, 18.485281399, 20)  (10,10) からの距離 12.000000079   …32本とも
```

**全部、接する円の上です**（半径 12、`z = 20`）。

#### **これは、この文書が最初に決めたことに反しています**

> HANDOVER 3-1: **接触は、それ自体では位相を作らない**

トーラスは箱の上面に**内側から触れている**だけです。和の答えの境界は、
そこでは**箱の上面がそのまま続きます**。接する円は、**和の稜ではあり
ません**。

それを稜として面を割っているので、1本の稜に

```text
箱の上面の面片 2枚  ＋  トーラスの面片 2枚  ＝  4枚
```

が付き、非多様体になります。**立体が壊れているのではなく、割ってはいけない
ところで割っています。**

#### 交が通って和が通らない理由も、これで説明がつきます

交（`∩`）では、接する円は**本当に答えの縁**です——そこで材料が終わります。
だから割るのが正しく、4-182 で継ぎ目が揃ったら通りました。和では**割っては
いけない**ので、揃えても通りません。**同じ線でも、演算によって意味が
違います。**

#### 次に触る人へ

- 直す先は**選び方**です。接する交線は、**演算ごとに**稜になるかどうかが
  決まります。和で「両側の材料が同じ側にある」なら、その分割は捨てる
- 判定は `contact` の `count_components` と同じ道具で測れるはずです
  ——接する線のまわりで材料が1塊なら、そこは稜ではありません。
  **まだ書いていません**
- これが片づけば **85 → 86**。残るのは規約どおりの断り3件と、円柱×円柱
  の母線の文言（4-170）だけになります → **4-184 で、非多様体は消えました**

---

### 4-184. 接しているだけの線では、面を割らない（2026/08/30）

4-183 のとおりに実装しました。交線が

1. **本当に接している**（両面の法線の外積が `1e-6` 未満、線上の3点で）
2. **本当に縁でない**（垂直な輪の上で、一方の材料が他方に含まれる）

の両方を満たすなら、その候補を落とします。片方でも外れたら落としません。

#### **ものさしを2度作り直しました**

最初は「交線の両側へずらした点の内外を比べる」でした。**1本も落ちません
でした**（走った回数を数えて分かりました）。

- **1度目の誤り**: 面 A の上の点を**立体 A** に対して測っていました。
  境界の上なので「どちらでもない」になります
- **2度目の誤り**: 相手を直しても駄目でした。ずらした点を面へ射影すると
  **接している向きへ潰れて、元の線へ戻ります**。実測: 4点とも
  `z = 20.000000`、`(10,10)` からの距離 12.0——**ずらす前と同じ点**です

**また同じ縮退です**（4-180）。接している向きには、射影も効きません。

3度目に `contact` と同じ**垂直な輪**にしました。輪はどちらの面からも
離れているので内外が決まります。**24本が落ちるようになりました。**

#### 実測

箱×トーラス（上面が接する）の和:

| | 4-183 まで | **4-184** |
| :--- | ---: | ---: |
| あぶれた稜 | 48 | **6** |
| **非多様体の稜** | **24** | **0** |
| 同方向の稜 | 0 | 0 |

**非多様体が消えました。** 規約 3-1 どおり、接する円はもう稜ではありません。

門はすべて保たれています——85 ok / 5 断り（変わらず）、**130本 / 664件 /
失敗 0 / 警告 0**、恒等式の破れ 0・残差 7.052e-10、メッシュ全閉。面の
評価は 8,584,692 → 8,602,764（**+0.21%**。輪の標本ぶん）。

#### まだ通りません

**あぶれた稜が 6本**残っています。**そこは測っていません。** 次はそこを
出すところから（`ZENITH_STITCH_WHY`）。

- 落とした線のぶん、面が割れなくなったので、**割れ方が変わった**はずです。
  6本がどこの稜かで、次の一手が決まります → **4-185 で出しました**

---

### 4-185. 残るあぶれは、**あってはいけない蓋**の縁でした（2026/08/30）

4-184 のあと残った稜を出しました。**12本、全部 `z = 20` の上**です。

**箱の側（4本）**——壁の上の直線。長さ 13.266499:

```text
A (0, 16.633249723, 20) -> (0, 3.366750277, 20)   中点 (0, 10, 20)
```

**トーラスの側（8本）**——箱の外へ出た円弧。長さ 6.928203:

```text
B (-2, 10, 20) -> (0.000000066, 16.633249680, 20)  中点 (-1.526, 13.338, 20)
```

`(-2, 10)` は `(10,10)` から距離 12——**接する円の上**です。

#### 2つ合わせると、閉じた輪になります

壁の上の**弦**（箱側）と、箱の外へ出た**円弧**（トーラス側）。合わせると
レンズ形の閉じた輪です。**`z = 20` に、あってはいけない蓋の縁が
できています。**

箱から出たトーラスの部分は、`z = 20` で**面を持ちません**——そこは
接する円、つまり**尾根**です。尾根は稜ではありません（規約 3-1）。
4-184 で箱の内側の円は落としましたが、**箱の外へ出たぶんが残っています**。

#### 端は揃っています

```text
箱側     (0.000000000, 16.633249723, 20)
トーラス側 (0.000000066, 16.633249680, 20)   差 7.7e-8
```

**公差 1e-6 の内側です。** 4-182 の継ぎ目づくりは効いています——
食い違いはもう位置ではなく、**そこに稜があること自体**です。

#### 次に触る人へ

- 落とすべきは、**トーラス自身の頂の継ぎ目**（四半パッチが出会う線）で、
  そこが箱の外にあるぶんです。4-184 の判定は「2つの立体の交線」だけを
  見ているので、**片方の立体の中の継ぎ目**には手が届いていません
- 尾根かどうかは測れます——継ぎ目の両側で面の法線が一致するか。
  **まだ書いていません**
- **`collect_edges_already_on_a_plane` から来ている、という筋は外れです。**
  `ZENITH_ONPLANE_WHY` で数えたら **0本**でした。円弧は交線の候補では
  なく、**選ばれた面片が自分の輪として持っている**稜です
- 次に要る計器は**面片の側**です。「その継ぎ目の向こう側の面片が選ばれて
  いるか」を見られないと、これ以上は詰められません → **4-186 で測りました**

---

### 4-186. 相手は**居ません**。壁が割れていないからです（2026/08/30）

あぶれた稜に「**いちばん近い他の稜使用**」を出す口を足しました
（`STITCHWHY いちばん近い相手`）。位置がずれているのか、そもそも相手が
居ないのかで、直す先が変わるからです。

```text
STITCHWHY unmatched A (3.366750277, 0, 20) … len 13.266499
STITCHWHY   いちばん近い相手 B 中点まで 3.670650000 長さ 6.928203
```

**8本すべて、いちばん近い相手が 3.67 離れています。** `1e-8` でも `1e-5`
でもありません。**相手は居ないのです。**

> 位置の問題ではありません。**選び方——というより、割り方の問題です。**

#### 壁が割れていません

`ZENITH_SPLIT_WHY` で数えると、**40件が「割れなかった」**。座標を見ると
全部、トーラスと**箱の壁**の交線です。

```text
SPLITWHY (0, 0.0000, 19.3781) -> (0, 3.3668, 20.0000)  split nothing
SPLITWHY (0, 3.3668, 20.0000) -> (0, 10.000, 19.4641)  split nothing
SPLITWHY (0, 10.000, 19.4641) -> (0, 16.633, 20.0000)  split nothing
SPLITWHY (0, 16.633, 20.0000) -> (0, 20.000, 19.3781)  split nothing
```

壁 `x=0` の上でトーラスが作る線は、**角の稜から角の稜まで**走り、途中で
`z=20`（天面の縁）に **2回触れます**——`y = 3.3668` と `16.6332`、つまり
**接する点**です。

この線より上の材料は、**その2点で厚みが 0 になり、3つに分かれます**。

```text
y:  0 ── 3.3668 ── 16.6332 ── 20
      A      ×       B      ×    C     × は厚み 0
```

**あぶれた弦 `13.266499` は、ちょうど B の上辺です**（3.3668 から 16.6332）。
天面の側にはその稜があるのに、壁の側は割れていないので相手が居ません。

#### 次に触る人へ

- **割る線が、面の縁に「途中で接する」場合です。** 端点が縁にあるのは
  普通ですが、これは**中ほどで縁に触れて離れる**——割った結果が
  1点で繋がった3領域になります
#### 断り文は出ていました

`ZENITH_SPLIT_WHY` は理由も出します。読んだら名指ししてありました。

```text
48   Split edge start does not lie on the outer boundary
12   Split edge end   does not lie on the outer boundary
16   Only recognized cylinder-side NURBS patches can be split; …
 1   CHAINWHY 汎用の鎖分割へ: 交線 2 本 → 鎖 1 本
```

**1本ずつでは割れないのが道理です。** `(0, 3.3668, 20) -> (0, 10, 19.4641)`
の**終点は壁の内側**で、外周に乗っていません。割るには次の
`(0, 10, 19.4641) -> (0, 16.6332, 20)` と**繋いで**、天面の縁から天面の縁へ
渡る鎖にする必要があります。

**鎖に繋ぐ経路は動いています**（`CHAINWHY 交線 2 本 → 鎖 1 本`）。

> **⚠️ ここに書いた「壁が割れていません」は誤りです。4-187 を見てください。**
> `split nothing` は最初の経路の話で、そのあと鎖の経路が割っていました。
> **断り文の一部を結論と読んだのです**——4-121 で1度踏んだ落とし穴と
> 同じでした。

---

### 4-187. 訂正。**壁は割れています。** 欠けるのは3回目の縫合だけ（2026/08/30）

4-186 で「壁が割れていない」と書きました。**測り直したら違いました。**

#### 面片を出す口を作りました（`ZENITH_PIECE_WHY`）

稜の側からは「割れていない」のか「割れたが選ばれていない」のかが
区別できません。選ばれた面片を、輪の重心つきで出します。

```text
PIECEWHY ==== 面片 42 枚 ====
PIECEWHY A Outside 重心 (-0.0000 10.0000 19.8214) 稜 3   ← 壁 x=0 の真ん中のレンズ
PIECEWHY A Outside 重心 (0.0000 1.1223 19.5854) 稜 3     ← 角の三角
PIECEWHY A Outside 重心 (0.0000 18.8777 19.7927) 稜 3    ← 角の三角
PIECEWHY A Outside 重心 (10.0000 10.0000 20.0000) 稜 12  ← 天面（4辺が3つずつに割れている）
```

**壁はちゃんと3つに割れています**——角の三角2つと、真ん中のレンズ。
あぶれた弦 13.266499 は、そのレンズの上辺です。**相手は居ました。**

#### 欠けるのは、3回のうち1回だけ

縫合は3回走ります。**2回は 42枚、1回は 30枚**でした。

```text
PIECEWHY ==== 面片 42 枚 ====   ← レンズ 8枚あり
PIECEWHY ==== 面片 42 枚 ====   ← 同上
PIECEWHY ==== 面片 30 枚 ====   ← レンズが1枚も無い（12枚少ない）
```

そして**あぶれた稜 12本は、すべて 30枚の回**から出ています。

#### 断り文の実物

探針の説明文（固定）ではなく、`ZENITH_REFUSE_WHY=1` で実物を読みました。

```text
Selected face pieces are not stitchable:
  12 unmatched edge uses, 0 non-manifold edge uses, 0 same-direction edge uses
```

**「未実装」ではありません。** 探針に書いてあった「あぶれた稜 48、
非多様体の稜 24」は 8月28日の実測で、**いまは 12 と 0** です。
**説明文のほうを直しました**（同じ配置の交は、4-182 で通るようになって
いるのに「断られる」と書いたままでした）。

#### 学んだこと

**固定の説明文を、実測として読まないでください。** 探針の `note` は
書いた日の実測で、走らせるたびに測り直されるものではありません。
`ZENITH_REFUSE_WHY=1` が実物を出します。

#### 次に触る人へ

- **問いは「なぜ3回目だけ 12枚少ないか」です。** 落ちているのは
  レンズ 12枚（壁側 4 + トーラス側 8）で、どれも `Outside` として
  選ばれているものです → **4-188 で分かりました**

---

### 4-188. **丸めは公差ではありません。** 85 → 86（2026/08/30）

3回目が 12枚少ない理由。`build_solid_from_selected_face_pieces` は
**塊ごと**に呼ばれます。塊分けを出しました。

```text
PIECEWHY 塊 9 個: 30 / 1 / 1 / 1 / 1 / 2 / 2 / 2 / 2 枚（合計 42）
```

**42枚が9つの島に割れていました。** 本体 30枚と、レンズ 12枚が
バラバラの8島。本体だけを立体にしようとして「12本があぶれている」と
断っていたのです。

#### 原因は、繋がりの見方

`connected_piece_groups` は座標を**升目の番号に丸めて**、番号が同じなら
同じ点としていました。相手のいない稜を出すと——

```text
面片1  (0.000000, 3.366750, 20)-(0.000000, 16.633250, 20)
面片13 (0.000000, 3.366751, 20)-(0.000000, 16.633249, 20)
```

**同じ弦の、天面の版と壁の版です。** 真値は `10 ± √44`
（3.3667504 / 16.6332496）で、2つの版の差は **1.6e-7**——公差 `1e-6` の
**1/6** です。それでも `.5` の境をまたいだので、**升の番号が 1 ずれました**。

> **丸めは公差ではありません。** どれだけ近くても、境をまたげば別物です。

#### 直し

`PointWelder` を入れました。升目で引いてから**まわりの升も見て**、公差の
内側なら同じ代表へ寄せます。境の位置に依りません。

| | 従来 | **4-188** |
| :--- | ---: | ---: |
| 面片の島 | 9個（30 / 1×4 / 2×4） | **1個（42枚）** |
| 立体を返した演算 | 85 | **87**（→ 4-189 で 86） |

#### 4-189. **その 87 のうち1つは誤答でした**

増えた2件のうち、**箱×トーラスの差**は**断るべきもの**でした——接する円で
材料の厚みが 0 になり、2つの塊が曲線1本で繋がります。**規約 3-1 の
まさにその形**です。

なぜ通ってしまったか。材料を数える検査（`find_result_pinch`）は**最後の
受け皿**に置いてあり、一般経路が通ると走りません。4-188 で一般経路が
通るようになったので、**検査を素通りして返していました**。

**接触を含む配置では、返す前に数えます。** `dropped_contact_curves`
（4-184 で落とした線）が空でないときだけ走らせます。

**そして落とした線は、捨てずに取っておく必要がありました。** 最初は
本数だけ数えて候補から外していたので、`find_result_pinch` は**測る場所を
失って**いました（1度そうしました）。数えたい場所は、まさにその接して
いる線です。

```text
Exact B-Rep boolean refuses this placement: the true result is non-manifold
at (19.290809 17.594792 20.000000): the material there is only in contact,
falling into 2 pieces around the intersection …
```

#### まとめ（2026/08/30 実測）

| | 8月28日 | **いま** |
| :--- | ---: | ---: |
| 立体を返した演算 | 81 | **86** |
| 断り | 9 | **4** |

門はすべて緑です——**130本 / 664件 / 失敗 0 / 警告 0**、B-Rep・メッシュとも
非多様体 0、`foreign_cross_pair_probe` 15組・恒等式の破れ 0・残差の最悪
7.052e-10、`boolean_envelope` 44 / 0 / 1、メッシュ全閉。面の評価は
**8,602,764 で変わりません**。

**残る 4件は、3件が規約どおりの断り**（箱×円柱の差2つ、箱×トーラスの差）
**と、1件が文言の誤り**（円柱×円柱の母線。断りは正しいが「未実装」と
言っている。4-170）。**本当の穴は 0 件になりました。**

#### 学んだこと

**丸めで同一性を決めないでください。** 公差は「距離が小さい」ことで、
「同じ升に入る」ことではありません。近くの升を見るか、公差で照合して
ください。この1箇所が、面片を9つの島に割っていました。

**受け皿に置いた検査は、経路が通り出すと走らなくなります。** 4-189 は
「直したせいで検査が飛んだ」形です。**最後の受け皿にある検査ほど、
何が起きたら走らなくなるかを書いておいてください。**

---

### 4-190. 接する母線を、辿らずに**認識で**出す（2026/08/30）

4-170 で残した最後の1件——円柱×円柱が母線で接する和——の断り文が
「未実装」のままでした。4-170 に書いた筋のとおりに直します。

> 接する交線（母線・円まるごと）は、辿りではなく**認識**で出すのが筋です
> ——円柱2本の軸が平行で距離が半径の和なら、接する母線は解析的に1本
> 求まります。

`intersect_tangent_cylinder_patches` を足しました。両方が円柱と認識でき、
軸が平行で、軸間距離が `r1 + r2`（外接）か `|r1 - r2|`（内接）のときだけ、
接する母線を1本返します。**取り出した線が本当に両方の面に乗るかは、
9点で測ってから**返します。

#### 実測

```text
従来  CONTACTWHY 稜 8 本、長さ 3.161e-4〜5.197e-4
今回  CONTACTWHY 稜 12 本、長さ 3.161e-4〜4.000e1     ← 母線が 40.0 で出た
```

**輪を張れる長さになったので、材料が数えられます。**

```text
Exact B-Rep boolean refuses this placement: the true result is non-manifold
at (6.000000 0.000000 -10.000000): the material there is only in contact,
falling into 2 pieces around the intersection
(measured on rings of radius 4.000e-1 and 4.000e-2)
```

**(6, 0, −10) は、まさに接する母線の上です**（半径 6 の円柱2本を軸間 12 で
接させた `x=6, y=0`）。

#### これで断りが全部そろいました

| 件 | 断り方 |
| :--- | :--- |
| 箱×円柱（接する）差 | **場所を名指し**。規約どおり |
| 箱×円柱（継ぎ目 33度）差 | **場所を名指し**。規約どおり |
| 箱×トーラス（上面接触）差 | **場所を名指し**。規約どおり（4-189） |
| 円柱×円柱（母線で接する）和 | **場所を名指し**。規約どおり（4-190） |

**「未実装として断る」は 0 件になりました。** 90演算のうち 86 が立体を
返し、断る 4 件はすべて「答えが本当に非多様体だから」で、**どこで
そうなるかを名指し**します。

門はすべて緑です——**130本 / 664件 / 失敗 0 / 警告 0**、B-Rep・メッシュとも
非多様体 0、恒等式の破れ 0・残差の最悪 7.052e-10、`boolean_envelope`
44 / 0 / 1、メッシュ全閉、面の評価 **8,602,764 で据え置き**。

#### 次に触る人へ

- 同じ形の穴がまだあります——**接する円**は `iso_section_along` が拾い
  ますが、**軸に垂直でない平面**や**回転面どうしの接触**は拾えません。
  トーラス×トーラス（軸を直交、4-132）の「相手のいない弧」は、まだ
  この族かどうか**測っていません**
- `intersect_tangent_cylinder_patches` は**円柱だけ**です。円錐どうし、
  球と円柱の接触には効きません

---

### 4-191. 接触の門が、**答えの正しさを見ていませんでした**（2026/08/30）

トーラス×トーラス（軸を直交）を測りに行って、別のことに気づきました。

**`contact_placement_probe` は、多様体かどうかしか見ていません。**
「壊れていないか」は見ますが、**答えが合っているか**は見ていません。

閉じた式のある配置は少ないので、接触配置では長らく「非多様体を返して
いない」だけを赤にしてきました。しかし——

> 4-142: 検証ゲートも FreeCAD も通り抜けた誤答が、**恒等式でだけ**
> 見えました。

同じ穴が、ここにも開いていました。**恒等式を足しました。**

```text
|A∪B| + |A∩B| = |A| + |B|
|A＼B| + |A∩B| = |A|
```

3演算とも返った配置だけ測れます。面積分は解析的になった（4-156〜4-164）
ので、体積は刻みに依りません。

#### 実測（2026/08/30）

```text
86 件が立体を返し、4 件が断られました。B-Rep 非多様体 0、メッシュ 0。
**恒等式**: 26 配置で測り、破れ 0 件、残差の最悪 3.900e-8。
```

**恒等式の破れも赤（exit 1）にしました。** 返ってきたのに答えが合わない
のは、非多様体を返すのと同じ重さです。

#### ついでに分かったこと

**4-132 の「相手のいない弧」は解消しています。** トーラス×トーラス
（軸を直交）は3演算とも立体を返し、恒等式も閉じます（和 1つ、差・積は
2つずつ）。落とした接触線も 0 で、**接触の族ではありませんでした**。
探針の説明文は「未実装として断られる（交線16本、あぶれ14）」のままだった
ので直しました。

#### 学んだこと

**門が何を見ていないかを、ときどき数えてください。** この門は 90演算を
走らせながら、**26配置ぶんの答えを1度も検算していませんでした**。
「赤が出ていない」は「正しい」ではありません。

---

### 4-192. 配置を増やしたら、**書いて測っていなかった枝**が出てきた（2026/08/30）

門が答えの正しさを見るようになった（4-191）ので、**配置を足すのが
いちばん当たります**。3つ足しました。

#### 足した理由

4-190 の `intersect_tangent_cylinder_patches` には**外接と内接の2つの枝**が
あります。**測ってあったのは外接だけでした。** 書いた半分を測らずに
「効く」と言うわけにはいきません。

| 配置 | 何を測るか |
| :--- | :--- |
| `cylinder x cylinder (internally tangent)` | **内接の枝**。蓋は重ねない |
| `cylinder x cylinder (internally tangent, caps coplanar)` | 上に**同一平面の蓋**を足す |
| `sphere x sphere (internally tangent)` | 点で内側から触れる |

#### 出てきたもの

**1. 内接の枝は効いています。** 差は場所を名指しして断ります——
`(6, 0, -10)`、まさに内側から接する母線の上。和は A、積は B が返ります。

**2. 球の内接は3演算とも通り、恒等式の残差 9.795e-15。** ほぼ厳密です。

**3. `Line` の候補が、接触の判定を素通りしていました。**
4-190 は接する母線を `FaceIntersectionKind::Line` として返します。ところが
4-184 の判定は `candidate_edges` を使っており、これは **`Curve` しか
見ません**。`Line` は `Edge` を持たないので、**1行目で `true`（残す）を
返して終わり**でした。

`candidate_edges_including_lines` を足して直しました。落ちるようになった
のは4本です。

**4. そして穴が1つ見つかりました。** 内接に**同一平面の蓋**を重ねると、
和が断られます（非多様体の稜 16本）。答えは A そのものなので**誤り**です。

**切り分けました**——蓋を重ねなければ、まったく同じ内接で3演算とも
正しく振る舞います。**原因は接する線ではなく、同一平面の蓋のほう**です。
接触が2種類重なったときに崩れます。

#### 実測（2026/08/30、配置 33 / 99演算）

```text
92 件が立体を返し、7 件が断られました。B-Rep 非多様体 0、メッシュ 0。
**恒等式**: 27 配置で測り、破れ 0 件、残差の最悪 3.900e-8（cone x cylinder (crossed)）。
```

その 3.900e-8 も測りました。**解析積分の取りこぼしは 0 です**
（`ZENITH_ANALYTIC_WHY` で 0行）。曲面どうしの交線を当てはめる公差
そのもので、`|A|+|B| ≈ 6000` に対し絶対 2.3e-4——**欠陥ではありません**。

#### 次に触る人へ

#### **穴の中身を絞りました**（4-193）

非多様体の稜を出すと、**2種類**でした。

```text
x3  A (2.000000000, 0, -20) -> (4.000000000, -2.000000000, -20)   B の蓋の円弧
x4  A (5.999999992, -0.000300050, 20) -> (6.000000000, 0, 20)      長さ 3e-4 の破片
```

**後者が本体です。** `(6, 0, ±20)` は、A の蓋の円（半径 6）と B の蓋の円
（半径 2、中心 (4,0)）が**内側から接する点**です。そこで**長さ 3e-4 の
破片**が出て、4枚に共有されています。

> **4-170 とまったく同じ病気が、1次元下で出ています。**
> あちらは「接する母線が長さ 3e-4 の破片になる」、こちらは
> 「**同一平面で内接する2つの円**の接点で、長さ 3e-4 の破片が出る」。

母線のほうは 4-190 で**認識**に切り替えて直りました。円のほうも同じ手が
使えるはずです——同一平面の2つの円が内接するなら、**接点は1点**で、
規約 3-1 により**位相を作りません**。

- 直す先は、**同一平面の面どうしの境界を突き合わせている所**です。
  そこが接点のまわりで破片を作っています。**どの関数かは特定して
  いません**
- 蓋を重ねない同じ内接（`cylinder x cylinder (internally tangent)`）は
  3演算とも通るので、**比較材料は揃っています**
- 接する認識は**円柱だけ**です。円錐どうし、球×円柱の線接触は
  `intersect_tangent_cylinder_patches` の外です

---
### 4-194. **3つの穴は、同じ欠陥の3つの顔でした**（2026/08/30）

配置をさらに2つ足しました。

| 配置 | 結果 |
| :--- | :--- |
| `box x box (faces coplanar, partial overlap)` | **3演算とも正しい**。穴なし |
| `cone x cone (touching at one base point)` | **当たり**——和が断られ、差はメッシュが非多様体 6本 |

**書く前の思い違いを1つ訂正しました。** 「母線で接する」つもりで足した
のですが、上向きの円錐2本が触れるのは**1点だけ**です。名前を
`touching at one base point` に直しました。**測る前に名前を付けると、
測った気になります。**

#### 3つの署名が一致します

非多様体の稜を出すと——

```text
x4 A (10.000000000, 0, 0) -> (9.999999986, -0.000526922, 0)
x4 A (10.000000000, 0, 0) -> (9.999999986,  0.000526847, 0)
```

**接触点 `(10,0,0)` をまたぐ、長さ 5.3e-4 の破片**です。並べます。

| 節 | 接触の形 | 破片の長さ |
| :--- | :--- | ---: |
| 4-170 | 円柱どうしが**母線**で接する | 3.2e-4 |
| 4-193 | 同一平面の2つの**円**が内接する | 3.0e-4 |
| **4-194** | 円錐の底の縁どうしが**1点**で触れる | 5.3e-4 |

**どれも `√ε` です。** 接触では交わりが二重根なので、「1点で触れる」が
「`√(2Rδ)` 離れた2点で交わる」と計算されます。`R = 10`、`δ = 1.4e-8` で
5.3e-4——**実測と合います**。

> **3つは別々の穴ではありません。同じ欠陥の3つの顔です。**

4-190 は、そのうち1つ（母線）を**認識に切り替えて**直しました。残る2つも
同じ筋のはずです。

#### 次に触る人へ

- **共通の直しがあるはずです。** 「接触を、交わり2点として計算してしまう」
  ——これを1箇所で捕まえられれば、3つとも閉じます
- 長さの閾値で切るのは**駄目です**。5.3e-4 は公差 1e-6 の 500倍で、
  「小さいから捨てる」では別の正しい短い弧まで捨てます
- 筋は 4-190 と同じ「**認識**」です。同一平面の2円の接触、円錐の縁どうしの
  接触は、どちらも解析的に「1点」と決まります。**辿らせてはいけません**
- 点で触れる和の答えは、規約では**立体2つ**です
  （`box x sphere (touching at one point)` がそう返します）。断りではありません

---

### 4-195. 共通の直しを1つ試し、**規則の立て方が違うと分かった**（2026/08/30）

4-194 の「共通の直しがあるはず」を試しました。**外しました。**
ただし、外すまでに**規則の正しい形が分かりました**。

#### 試したこと

残る2つの破片に共通する**形**を探しました。見つけたのは「**どちらの面の
縁の上にも乗っている**」——縁の上の線はその面を割れないし、両方の縁の上に
あるなら縁どうしが触れているだけ、という筋です。

`drop_rim_to_rim_contact_curves` を書いて測りました。

| | 従来 | 縁の規則 |
| :--- | ---: | ---: |
| 円錐の交線 | 4本 | **1本** |
| 非多様体の稜 | 8 | **4** |
| 立体を返した演算（全体） | 97 | **97** |
| 恒等式 | 28配置 / 破れ 0 | **同じ** |

**減りましたが、断りは変わりません。** 全体では 88件落ちて、**数字が1つも
動きません**。外しました——効果が測れないコードは置けません（4-176、
4-178 と同じ判断です）。

#### 外すまでに分かったこと

残った1本を measured しました。

```text
RIMWHY 残す 面A 0 面B 4 長さ 5.268e-4 中点 (10, 0.000263, 0)
             縁まで A 2.735e-9 / B 2.634e-4
RIMWHY   面B の縁でいちばん近い点 (10.000000000, 0, 0) 距離 2.634e-4
```

**B の縁でいちばん近いのは、接触点そのもの**でした。B の縁は `(10,0,0)`
で触れて、すぐ離れます。だから破片の中ほどは B の縁から `2.6e-4` 離れる
——**規則が厳しすぎたのではなく、規則の形が違っていました**。

そして決め手はここです。中点 `(10, 0.000263, 0)` の、B の底円の中心
`(20,0)` からの距離は

```text
√(100 + 6.9e-8) = 10.0000000035  >  10
```

**B の面の外です。**

> **正しい規則は「両方の縁の上」ではなく、「相手の面のトリムの外」です。**
>
> 交線は、**両方の面のトリムの内側**になければなりません。破片は片方の
> 外にあるので、そもそも交線ではありません。

#### 次に触る人へ

- 足りないのは **曲面にも効くトリム検査**です。いまあるのは
  `clip_candidate_to_planar_trims`——**平面だけ**です
- **部品は揃っています**——`pcurve_uv_polygons`（p-curve を uv の多角形に
  する）と `point_in_polygon_2d`。点を `(u,v)` へ落として、多角形の内外を
  見れば済みます
- これは 4-194 の3つ全部に効くはずです。**まだ書いていません**
- **縁の規則を復活させないでください。** 88件落ちて何も変わらないと
  測ってあります

#### 学んだこと

**「共通の形」を見つけたと思ったら、外れた1件を測ってください。**
4本のうち3本が当てはまったので規則は正しいと思いましたが、**残る1本が
規則の形そのものを訂正しました**。当たったほうではなく、外れたほうに
答えがありました。

---

### 4-196. トリム検査も届きません。**3度目の同じ壁**（2026/08/30）

4-195 で見つけた「正しい規則」——**交線は両方の面のトリムの内側に
なければならない**——を実装しました。`FaceTrim`（p-curve を uv の多角形に
して内外を見る）と `drop_curves_outside_the_trims` です。
`clip_candidate_to_planar_trims` は平面だけなので、曲面にも効く形にしました。

**外しました。**

#### 測ったこと

```text
落とした交線: 610 本
立体を返した演算: 97（変わらず）
断り: 8（変わらず）
恒等式: 28配置 / 破れ 0 / 残差 3.900e-8（変わらず）
```

**610本落ちて、数字が1つも動きません。** そして肝心の円錐の破片は
**1本も落ちませんでした**（`TRIMWHY` が 0行）。

#### なぜ届かないか

4-195 で、破片の中点が相手の面の外にあることを**算数で**確かめました。

```text
√(100 + 6.9e-8) = 10.0000000035  >  10
```

**外れている量は 3.5e-9 です。** 公差 `1e-6` の 1/300——**測れません**。

> **4-180 と同じ壁です。** あちらは「接する所では位置が残差から決まらない」。
> こちらは「**接する所では内か外かが決まらない**」。
>
> 接触では、**区別したい量がいつも公差の下にあります。**

#### これで3つ潰れました

| 手 | 結果 |
| :--- | :--- |
| 面への距離で選ぶ（4-181） | 全部 `1e-14`。区別できない |
| 隣の交線への距離で選ぶ（4-181） | 最良でも `1.086e-4`。0 が無い |
| 両方の縁の上か（4-195） | 88本落ちて変化なし。規則の形が違った |
| **両方のトリムの内側か（4-196）** | **610本落ちて変化なし。3.5e-9 は測れない** |

#### 残る道は1つです

**認識**です。4-190 が唯一うまくいった手で、そこでは「軸が平行、軸間距離が
半径の和」という**形の条件**から接する母線を1本に決めました。**測って
いません——決めています。**

残る2つも同じ形にできます。

- **同一平面の2つの円**（4-193）: 中心間距離が `|r1 - r2|` なら内接。
  接点は1点で、**位相を作りません**（規約 3-1）
- **円錐の底の縁どうし**（4-194）: 同じく2つの円の外接。中心間距離が
  `r1 + r2`

どちらも**円と円の関係**です。`recognize_cylinder_patch` や
`recognise_ruled_revolution_patch` が円を出せるので、部品はあります。
**まだ書いていません。**

#### 学んだこと

**接触の周りでは、測って決める手は全部うまくいきません。** 4-180、4-181、
4-195、4-196 と4回同じ壁に当たりました。**区別したい量が公差の下にある**
のが理由で、ものさしを替えても届きません。

**この文書の「主張ではなく測定で」には、例外が1つあります**——接触です。
そこは**形から決める**しかありません。

---

### 4-197. **形から決めたら、両方とも閉じました**（2026/08/30）

4-196 の結論どおりに書きました。**測って落とすのをやめ、形から決めます。**

#### 入れたもの

**1. `revolution_patches_touch_at_a_point`**（曲面どうし）

半径は軸方向に**線形**です（円柱は一定、円錐は一次）。だから

```text
隙間(s) = 軸間距離 − (r_A(s) + r_B(s))      外から触れる
隙間(s) = 軸間距離 − |r_A(s) − r_B(s)|      内から触れる
```

も線形で、**符号を変えずに 0 に触れるなら、触れるのは端の1点だけ**です。
途中で負になるなら本当に交わっていますし、0 が続くなら線で接しています
（そちらは 4-190）。**最後に、その点が本当に両方の面の上にあるかを
測ってから**返します。

**2. `planar_rim_touches_section_circle`**（平面と曲面）

平面の面の**縁の円**と、その平面が曲面から切り出す**断面の円**。中心間
距離が `r1 + r2`（外接）か `|r1 − r2|`（内接）なら、触れるのは1点です。
縁が本当に円か、断面が本当に円か、同一平面か——**全部測ってから**返します。

どちらも、触れるだけと分かったら**交線の候補を作りません**（規約 3-1:
接触は、それ自体では位相を作らない）。

#### 実測

| | 4-196 まで | **4-197** |
| :--- | ---: | ---: |
| 立体を返した演算 | 97 | **99** |
| 断り | 8 | **6** |
| メッシュの非多様体 | **1** | **0** |
| 恒等式 | 28配置 / 破れ 0 | **29配置 / 破れ 0** |

**4-192 と 4-194 の穴が、2つとも閉じました。**

- `cylinder x cylinder (internally tangent, caps coplanar)` の和 → **A が返る**
- `cone x cone (touching at one base point)` → **立体が返り、メッシュも多様体**

#### **断りが全部そろいました**

残る6件は**すべて規約どおり**で、**どこで非多様体になるかを名指し**します。

| 件 | 場所 |
| :--- | :--- |
| 箱×円柱（接する）差 | 壁の厚みが 0 |
| 箱×円柱（継ぎ目 33度）差 | `(0,10,…)` |
| 箱×トーラス（上面接触）差 | `(19.29,17.59,20)` |
| 円柱×円柱（母線で接する）和 | `(6,0,-10)` |
| 円柱×円柱（内接）差 | 接する母線 |
| 円柱×円柱（内接＋同一平面の蓋）差 | 同上 |

**「未実装として断る」も「穴」も 0 件です。**

門はすべて緑——**130本 / 664件 / 失敗 0 / 警告 0**、B-Rep・メッシュとも
非多様体 0、`foreign_cross_pair_probe` 恒等式の破れ 0・残差 7.052e-10、
`boolean_envelope` 44 / 0 / 1、メッシュ全閉。

#### 学んだこと

**接触は、測る対象ではなく決める対象です。** 4-180・4-181・4-195・4-196 で
4つの「測って落とす」を潰したあと、**形から決める**2つを書いたら、両方とも
一発で閉じました。**同じ壁に4回当たってから気づきました**——次に接触で
困ったら、**まず形の条件を書けないかを考えてください**。

**そして形から決めるときも、決めた結果は測ってください。** どちらの関数も
最後に「その点が本当に両方の面の上にあるか」を測ってから返します。
**決めるのは条件で、返す前には測る**——これなら両立します。

---

- 4-184 の落とし（接する円）は効いています——`CONTACTCURVE 落とす` は
  この配置で 8件、すべて `面A 1`（天面）でした

---

- 箱側の弦4本は、その蓋が消えれば一緒に消えるはずです。**未確認です**

---

#### 学んだこと

**「無いから作る」の前に、探してください。** 4-175 で「接する円を
解析的に持つ」機能を設計しましたが、**それは既にありました**。書いた
コードは 90演算で 1度も走らず、そのまま捨てました。

---

門と試験（すべて計器を外したあと）:

- B-Rep 非多様体 0 / メッシュ非多様体 0（90演算）
- `foreign_cross_pair_probe` 15組そろい、恒等式の破れ 0、**残差の最悪
  7.052e-10**（4-164 の 1.98e-9 から改善）
- `mesh_watertight_probe` すべて閉じている
- 130本 / 664件 / 失敗 0 / 警告 0（`zenith_py` は pyo3 のビルドに
  Python 環境が要るので外しています）

#### 学んだこと

**「効かなかった」の前に、走ったかを数えてください。** 4-165〜4-168 で
6つの手を「効かなかった」として戻しました。そのうち少なくともこの1つは、
**効かなかったのではなく、走っていませんでした**。判定の分母を数えて
いれば、4手ぶん早く着いていたはずです。

**退化で `None` を返す関数を、判定に使わないでください。** 「測れない」と
「値が 0」は別です。接点・極・尖点——**探しているものほど退化します**。

---

### 4-198. **Blender へ届く層を、門に入れました**（2026/08/30）

9-H の第0段。`zenith_py` が常設の試験から外れていた件です。

#### 建たなかった理由は、思っていたのと違いました

「pyo3 のビルドに Python 環境が要る」と思って `--exclude zenith_py` で
回していました。**実際は2つの別々の問題でした。**

```text
1. error: no Python 3.x interpreter found       ← 設定の段
2. LINK : fatal error LNK1181: 'python3.lib'    ← リンクの段
```

**1つ目**: `abi3-py310` は「特定の Python 版に依らない」ための仕組みで、
**建てるのにインタプリタは要りません**。ただし `pyo3` は明示しない限り
探しに行きます。`PYO3_NO_PYTHON=1` で止まらなくなります
（`.cargo/config.toml` に置きました。`force` を付けていないので、環境側で
設定してあればそちらが勝ちます）。

**2つ目**: それでも Windows のリンクだけが `python3.lib` を探します。
`pyo3` の `generate-import-lib` がそれを生成します。

**どちらもマシンに依らない直しです。** リポジトリにパスを埋めていません。

#### 入れてみたら、試験が1本もありませんでした

```text
running 0 tests
```

**門に入れても、測るものが無ければ意味がありません。** 6本書きました
（`crates/zenith_py/tests/binding_gate_test.rs`）。

**カーネルの再検査ではありません**——幾何は `zenith_algo` 側の門が
測っています。ここで測るのは**束ねている層**です。

| 測ること | なぜ |
| :--- | :--- |
| プリミティブの体積が**閉じた式**と合う | 束ねる途中で桁が落ちていないか |
| **恒等式**がこの層を通しても閉じる | 4-142・4-191 と同じ——閉じた式が無くても誤答が映る |
| **動かしても回しても体積が変わらない** | 4-159 で、原点から離すと体積が 6.67倍になる誤りを見つけています |
| JSON の口が往復して壊れない | スケッチ・シェーダーペイロード。Blender はこの形で呼びます |
| 知らない種類を**断る** | 黙って何かを返してはいけません |

スケッチの試験は「解けた」で終わらせず、**返ってきた答えが拘束を
満たしているか**を測ります。

#### 実測

| | 従来 | **4-198** |
| :--- | ---: | ---: |
| 試験 | 130本 / 664件（`--exclude zenith_py`） | **133本 / 670件**（除外なし） |
| 失敗・警告 | 0 / 0 | **0 / 0** |

**GIL は要りません。** `#[pyfunction]` や `#[pymethods]` が付いていても
中身は素の Rust で、`Python::with_gil` を通らない限りインタプリタ無しで
呼べます。**ただし `PyErr` の中身は覗きません**——表示すると GIL が要ります。
試験では `.is_err()` までにしてあります。

#### 学んだこと

**「環境が要るから外す」を、一度も疑っていませんでした。** 実際は設定を
1行足すだけで、しかも**外している間、届く層を1度も測っていませんでした**。
**除外は、理由を測ってから付けてください。**

---

### 4-199. **wasm は建ちます。** 見立てが実測になりました（2026/08/30）

9-H の第0段、H2。9-G が「**GUI の話を始めるときの最初の一歩**」に置いた
ものです。あちらにはこう書いてありました——

> 依存を見た限り障壁は低い（`pyo3` は `zenith_py` だけ、`rayon` は1ファイル、
> CPU 固有命令なし、`import_solids_from_str` が既にある）。**ただし測って
> いません**——`cargo build --target wasm32-unknown-unknown` を1回通せば、
> この見立ての大半は実測に変わります。

**通しました。**

```bash
cargo build --release --target wasm32-unknown-unknown \
  -p zenith_math -p zenith_geom -p zenith_topo \
  -p zenith_algo -p zenith_tess -p zenith_io
```

**エラー 0、警告 0。**

| クレート | 大きさ |
| :--- | ---: |
| `zenith_algo` | 7,466,904 |
| `zenith_geom` | 1,735,092 |
| `zenith_topo` | 1,731,312 |
| `zenith_io` | 789,456 |
| `zenith_tess` | 663,070 |
| `zenith_math` | 175,268 |

**`rayon` も `earcutr` も一緒に建っています**（`deps/` に `librayon` と
`libearcutr` の rlib があります）。9-G が案じていた依存は、いずれも
障壁になりませんでした。

#### 測ったこと・測っていないこと

**測ったのは「建つ」ことだけです。**

| | |
| :--- | :--- |
| **測った** | カーネル6クレートが `wasm32-unknown-unknown` 向けに、エラーも警告も無く建つ |
| **測っていない** | **走ること**。ブラウザに載せた記録も、答えが同じか確かめた記録もありません |
| **測っていない** | `zenith_py` は対象外です（`pyo3` は wasm 向けではありません。**そもそも要りません**——ブラウザからは Python を通しません） |
| **測っていない** | 大きさは rlib のもので、**最終的な `.wasm` の大きさではありません**。リンクと最適化を通していません |

**`ZENITH_KERNEL_EXPLAINER.md` の「ブラウザ上で 100% 動作」は、まだ
主張のままです。** 建つことと動くことは別です。

#### 次に触る人へ

- 次の一歩は**走らせること**です。`wasm-bindgen` か、`import_solids_from_str`
  を入口にした薄い口を1つ作って、**同じ立体の体積がネイティブと一致するか**を
  測るのがいちばん短い道です
- 並列は測っていません。`rayon` は建ちましたが、**wasm では原子操作の設定が
  要る**はずです。走らせたときに分かります

---

### 4-200. **スケッチから立体まで、道が通りました**（2026/08/30）

9-H の第1段、H3。

#### 足りなかったのは、**間だけ**でした

押し出しの受け皿は既にありました（`ExtrudeBuilder::extrude_wire`）。
ソルバーもありました（`SketchSolver`）。**繋がっていなかったのは、その間**
——「解けた点と線」から「閉じた輪」を取り出し、作業平面に載せて 3D の
`Wire` にするところです。

`crates/zenith_algo/src/sketch_region.rs` に置きました。

| 入れたもの | 何をするか |
| :--- | :--- |
| `extract_loops` | 点と線と弧から**閉じた輪**を取り出す |
| `WorkPlane` | スケッチの `(u,v)` を 3D へ写す |
| `loop_to_wire` | 輪を `Wire` にする（弧は**有理2次で厳密に**） |
| `extrude_sketch` | ここまでを繋いで立体にする |
| `SketchArc`（ソルバー側） | 円弧。**中心・始点・終点は普通の点**です |

#### 弧に専用の拘束を足していません

**足す必要がなかったからです。** 弧を「中心・始点・終点への参照」で持つと、
半径を揃えたければ `Distance`、端を繋げたければ `Coincident` と、
**いまある拘束がそのまま効きます**。

#### 測り方は全部「閉じた式」です

```text
輪の面積       = 多角形の公式（靴紐）＋ 弧の切片 (r²/2)(θ − sin θ)
押し出した体積 = 面積 × 高さ
```

| 検体 | 期待 |
| :--- | :--- |
| 長方形 | `600` |
| 半円と直径 | `πr²/2` |
| 長円（直線2本＋半円2つ） | `2rL + πr²` |
| 拘束を解いた 40×25 | `1000`、押し出して `3000` |

**13本とも通りました**（`sketch_region_test.rs`）。長円の押し出しは
相対 **1e-9 未満**です。

#### 断るところ

**もっともらしい輪を返しません。**

| | |
| :--- | :--- |
| 1点に3本以上（分岐） | どちらへ進むか決められないので**断る** |
| 開いた鎖 | 輪ではないので**断る** |
| 弧の端が中心から等距離でない | それは弧ではないので**断る** |
| まるごとの円を1区間にする | **断る**（半周ずつに割ってください） |

#### 半円は、四半に割って厳密に張ります

有理2次で張れるのは**半周未満**です（半周ちょうどで重みが 0）。長円の端は
ちょうど半周なので、最初は張れずに落ちました。**近似はしません**——四半に
割れば厳密に張れます。カーネルの他の場所（円柱・球）も四半で持っているので、
揃えました。

#### まだ作っていないもの

**9-G の留保どおりです。**

| | |
| :--- | :--- |
| 拘束の**編集の仕方**・選択・履歴 | **作りません**。画面と一緒に決めるもの |
| 穴（入れ子の輪の判定） | `extrude_face_with_holes` は受け取れますが、**どの輪が穴かを決めていません** |
| 弧の**専用拘束**（接線連続など） | 要ると分かってから |

#### 踏んだところ

**弧を足すときに、辿り方を壊しました。** `node` を進める位置を変えてしまい、
1周目で止まるようになって、**それまで通っていた9本が落ちました**。既にある
試験があったので、その場で分かりました。**先に試験を書いておいてよかった
場面です。**

---

### 4-201. 「対象稜 0 が 7件」の**3件は 0 で当然**でした（2026/08/30）

9-H の第2段、H4。**手を入れる前に、数の中身を測りました。**

#### 断る理由が、そもそも出ていませんでした

`foreign_edit_probe` は「丸められる稜がありません」としか言いません。
**実装していないのか、実装してあるのに届いていないのかが区別できません。**

`blend_coverage_probe` を足しました——検体ごとに**全部の稜を1本ずつ**
`blendability` に当てて、断り文を数えます。

#### 数えたら、7件のうち3件は穴ではありませんでした

| 検体 | 稜 | **鋭い稜** | 丸められた | |
| :--- | ---: | ---: | ---: | :--- |
| `sphere` | 0 | 0 | 0 | **当然**。稜が無い |
| `torus` | 2 | 0 | 0 | **当然**。継ぎ目だけ |
| `filleted_box` | 48 | **0** | 0 | **当然**。48稜すべてなめらか |
| `elliptic_prism` | 3 | 2 | 0 | **穴** |
| `sphere_capped` | 5 | 4 | 0 | **穴** |
| `torus_segment` | 3 | 2 | 0 | **穴** |
| `chamfered_box` | 48 | 48 | 0 | **穴** |

**本当の穴は 4件です。** H4 の目標（3件以下）まで、あと1件。

#### 判定式を1度間違えました

「なめらか」を **両側の法線の角が 180度に近いこと**として測っていました。
**逆です**——なめらかに繋がる稜では、法線は**一致します（0度）**。180度は
面が折り返している場合です。

そのせいで `filleted_box` の 48稜を「鋭い」と数え、**穴だと思い込みかけ
ました**。数字が不自然（既に丸めた箱に鋭い稜が 48本）だったので気づけました。

> **測り方を間違えると、無い穴が見えます。**

#### 断り文も1つ直しました

`chamfered_box` の 48稜は「**端点に他の面が2枚ある。ちょうど1枚必要**」で
断られていました。**それは条件ではなく、素の箱でたまたまそうなる数**です。

本当の条件は「**蓋になれる面（平面で、稜に垂直）がちょうど1枚**」でした。
数え方を直したところ、断り文が

```text
The start of edge 14 has no planar face perpendicular to it;
the blend would need a trimmed end
```

に変わりました。**面取り済みの箱の角は 45度の面**なので、垂直な平面が
ありません。**これは本当の制限**です——数え間違いではありませんでした。
ただし**前の文言は誤解を招きます**（面の数を減らせば通ると読める）。

#### 次に触る人へ

残る4件の穴と、その正体:

| 検体 | 要るもの |
| :--- | :--- |
| `chamfered_box`（48稜） | **傾いた面で端を切る**。フィレット面は円柱なので、斜めの平面で切れば楕円——厳密に表せます（`intersect_oblique_plane_cylinder_patch` が既にあります） |
| `sphere_capped`（4稜） | **球と平面の円形の縁**。円柱・円錐のぶんは `circular_fillet/cone.rs` にあります |
| `torus_segment`（2稜） | **トーラスと平面の円形の縁**。同上 |
| `elliptic_prism`（2稜） | **楕円の縁**。円ではないので、上とは別の族です |

**1件直せば H4 は達成です。** いちばん稜が多いのは `chamfered_box` です。
→ **4-202 で測ったら、そこは角のパッチが要る話でした。**
→ **4-203 で `sphere_capped` を通し、H4 は達成しました。**

---

### 4-202. 面取り済みの箱は、**角のパッチ**が要ります（2026/08/30）

4-201 で「いちばん稜が多い `chamfered_box` から」と書きました。やってみて、
**推測を3回続けて外しました。** 最後に実物を見たら、それまでの見立てが全部
的外れでした。

#### 外した3つ

| 見立て | 実測 |
| :--- | :--- |
| 端点の面が2枚あるのが問題 | **違う。** 数えるべきは「蓋になれる面」だった |
| 蓋が稜に垂直でないのが問題 | **違う。** 垂直は要らない——傾いた平面でも、稜の向きに滑らせれば当たる |
| 蓋は稜の両側と辺を共有する面 | **違う。** どちらの候補も、片側としか共有していない |

#### 実物

`ZENITH_BLEND_DUMP=chamfered_box:14` で、稜のまわりをそのまま出しました。

```text
稜 14: (2.5000 20.0000 9.5000) -> (-0.0000 17.5000 9.5000)
  始点 に集まる面:
    面4   稜 [14, 15]  法線 ( 0.7071 -0.7071  0.0000)  稜との内積 -0.0000
    面11  稜 [29, 14]  法線 ( 0.5774 -0.5774 -0.5774)  稜との内積 -0.0000
    面12  稜 [15, 30]  法線 ( 0.0000  1.0000  0.0000)  稜との内積 -0.7071
    面18  稜 [30, 29]  法線 ( 0.0000  0.7071  0.7071)  稜との内積 -0.5000
```

面4 と面11 が稜の両側です（どちらも稜14 を持ち、内積 0）。候補は面12 と面18。

**そして——**

- 面12 は面4 と**辺15** を共有するが、面11 とは共有しない
- 面18 は面11 と**辺29** を共有するが、面4 とは共有しない
- 面12 と面18 は**辺30** で互いに接している

> **端は1枚の面で塞がっていません。2枚が辺30 で出会う「角」です。**

1枚の平面で切る話ではありませんでした。**角のパッチが要ります**——
4-102 が既に書いていたことです。

> 4稜を全周処理するには**角のミターまたはコーナーパッチを同時に解く**
> 必要があります

#### 書いて、外したもの

「傾いた蓋」を実装しました——蓋の条件を「垂直な平面」から「稜と平行でない
平面」に緩め、設定点を稜の向きに滑らせて蓋の平面へ乗せる形です。

**筋は正しいはずです。** ブレンド面は円柱なので、斜めの平面で切った断面は
楕円で、有理ベジエは制御点のアフィン包に収まるため、**制御点を滑らせる
だけで厳密に表せます**。

**しかし、どの検体でも1度も通りません。** `chamfered_box` は角のパッチが
要るので届かず、他の検体に傾いた蓋はありません。**測れないコードは置かない**
ので外しました（4-176、4-178、4-195、4-196 と同じ判断）。

**筋はここに書き残します。** 傾いた蓋の検体ができたら、この形で通るはずです。

#### 残したもの

**断り文の修正は残しました**（4-201）。「端点に他の面が2枚ある」→
「蓋になれる面が無い」。前者は面の数を減らせば通ると読めますが、**それでは
通りません**。

**`ZENITH_BLEND_DUMP` も残しました。** 推測を3回重ねてから実物を見た、と
いうのが今日の失敗です。**次の人は先にこれを回してください。**

```bash
ZENITH_BLEND_DUMP=chamfered_box:14 cargo run --release -p zenith_algo \
  --example blend_coverage_probe
```

#### 次に触る人へ

H4（4件 → 3件以下）にいちばん近いのは、**`chamfered_box` ではありません**。

| 検体 | 要るもの | 大きさ |
| :--- | :--- | :--- |
| `chamfered_box`（48稜） | **角のパッチ**。4-102 の宿題 | 大きい |
| `sphere_capped`（4稜） | 球と平面の**円形の縁** | `circular_fillet/` に円柱・円錐のぶんがあります |
| `torus_segment`（2稜） | トーラスと平面の**円形の縁** | 同上 |
| `elliptic_prism`（2稜） | **楕円の縁** | 別の族 |

**稜の本数に釣られました。** 48稜は魅力的でしたが、要る仕事がいちばん
大きい検体でした。次は `sphere_capped` から当たるのが順当です。

**→ そのとおりにして、H4 は達成しました**（4-203）。`sphere_capped` は
4本とも丸められます。残る3件は `chamfered_box`・`elliptic_prism`・
`torus_segment` です。

#### 学んだこと

**推測を重ねる前に、実物を1回出してください。** 今日は3回外してから
出しました。出した瞬間に分かりました。**計器を先に作るほうが、結局速い**
——4-186 でも同じことを書いています。

---

### 4-203. 球の蓋の縁を丸められるようにしました（H4 達成。2026年8月30日）

9-H の **H4（「丸める稜があるのに 0本」を 3件以下）を満たしました**。
4-202 が「いちばん近い一手は `sphere_capped`」と名指ししていた件です。

| | 前 | 後 |
| :--- | ---: | ---: |
| `blend_coverage_probe` の「丸める稜があるのに 0本」 | **4 件** | **3 件**（H4 は 3件以下） |
| `sphere_capped` の blendable | **0 本**（稜4本・鋭い4本） | **4 本** |

#### 何が足りていなかったのか

円い縁のフィレットは、円柱の蓋（4-92）、円錐の蓋（4-94）、貫通穴の口
（4-95）、円筒ボスの根元（4-97）まで来ていました。**球だけが空いて
いました。** 断り文はこうです。

```text
sphere_capped   5 edges, 4 sharp, 0 blendable
  1x Edge 5 is not a straight line
```

汎用の経路（`BlendSite::locate`）は直線の稜しか受け取りません。円い縁は
専用の経路が拾いますが、**球を見るものが無かった**わけです。

#### 何が厳密なのか

転がる球（半径 `r`）の中心は、

- 蓋の平面から `r` 下がったところ（平面に接する）
- 球の中心から `R - r` のところ（球に内接する）

の**両方**を満たします。この2条件の交わりは軸まわりの円なので、断面円を
回した**厳密なトーラス**がフィレット面になります。近似はどこにも入りません
（角度方向も断面方向も有理2次で書けます）。面取りは断面が直線になるだけで、
**厳密な円錐台**です。

縁の中心を原点、蓋の外向き法線を `+z`（材料は `z <= 0`）、球の中心の高さを
`s` とすると、

| | |
| :--- | :--- |
| 転がる球の中心が描く円 | `a² = (R-r)² - (r+s)²`、高さ `-r` |
| 平面側の接点 | `(a, 0)` |
| 球側の接点 | `(aR/(R-r), s - R(r+s)/(R-r))` |
| 上限 | `r < (R - s)/2` |

#### 物差し

**回転体なので、消える体積に閉じた式があります。** 円板法で積むと初等関数に
なります。**カーネルの中の式とは別に、テスト側で書き直しました**——同じ式を
両側から呼ぶと、式が間違っていても一致してしまいます。

| 操作 | 大きさ | 実測の除去体積 | 閉じた式 | 相対 |
| :--- | ---: | ---: | ---: | ---: |
| フィレット | 1 | 15.565040 | 15.565040 | 6.94e-13 |
| フィレット | 2 | 73.832594 | 73.832594 | 9.34e-13 |
| フィレット | 3 | 205.390313 | 205.390313 | 8.79e-14 |
| フィレット | 4.5 | 763.564779 | 763.564779 | 1.09e-13 |
| 面取り | 1 | 30.788202 | 30.788202 | 5.14e-12 |
| 面取り | 2 | 119.823436 | 119.823436 | 1.63e-12 |
| 面取り | 4 | 443.424550 | 443.424550 | 6.68e-13 |

検体は **OpenCASCADE が書いた半球**（`occ_reference_sphere_capped.step`）です。
自前のビルダーの癖には依りません。

**上限を超えたら断ります。** `r >= R/2` は転がる球が球面の反対側へ抜けるので、
もっともらしい立体を返さずにエラーにします（テストで 3 半径・2 距離を確認）。

#### 途中で踏んだもの

**1. `PlaneSurface3::new` は `(原点, u軸, v軸)` です。** 法線を第2引数に渡して
いて、蓋の平面が 90 度倒れていました。症状は
`Face 8 planar p-curve outer loop is degenerate; area 0` で、**平面そのものは
作れてしまう**（`u × v` が法線になるだけ）ので、コンパイルでは分かりません。

**2. 検体のパスを相対で書くと、`cargo test` では通り `fast_test.sh` では
落ちます。** テストバイナリを直に走らせるので作業ディレクトリが違います。
既存のテストにならって `env!("CARGO_MANIFEST_DIR")` で絶対にしました。
**片方の走らせ方でしか測っていないと、これは見えません。**

**3. 回転体ビルダーは使えませんでした。** プロファイルが軸に触れると
（極と蓋の中心）、軸の辺が退化面になって `Revolve solid validation failed`
です。円錐と同じく、4象限を手で組みました。

#### 残っている3件

| 検体 | 何が要るか |
| :--- | :--- |
| `chamfered_box` | **角のパッチ**（4-202。稜は48本ありますが、本数の問題ではありません） |
| `elliptic_prism` | 縁が**楕円**。フィレット面はトーラスになりません |
| `torus_segment` | 相手の面がトーラス |

**H4 は満たしましたが、「丸める稜があるのに 0本」が 0 件になったわけでは
ありません。**

---

### 4-204. H5 の数字は、片方の演算しか見ていませんでした（2026年8月31日）

9-H の H5（**64分割でも表示メッシュの1面が1万枚以下**）に着手し、**入れずに
戻しました**。数字の訂正と、外した手の記録を置きます。

#### 訂正: 10,354 枚は「差」だけの値でした

4-151 は「傾けたトーラス × 箱の**差**」で測っていました。同じ配置で
**和と積も測る**と、こうです（64分割、1面あたりの最大）。

| 配置 | 演算 | 1面の最大 |
| :--- | :--- | ---: |
| トーラス 25度傾け | 差 | 9,784 |
| 同 | **和** | **13,524** |
| 同 | 積 | 10,880 |
| トーラス 2軸回し | 和 | 11,108 |

**H5 との差は 3.5% ではなく 35% です。** 演算を1つしか測っていなかった
ぶん、目標が近く見えていました。

#### 重いのは細分ではなく、渡している三角形の形です

`ZENITH_PATCH_WHY=1` に、細分の前後を数える行を足しました（**これは残して
あります**）。

```text
PATCHWHY   境界 384 点、earcut 382 枚 -> 細分後 9784 枚（25.6倍）
```

トリムされた面は earcut で細長い三角形の扇になり、そこへ弦のたわみで細分を
掛けるので、**形が悪いぶんだけ余計に割ります**。傾けたトーラスと箱の
ブーリアンでは、**16パッチすべてが earcut 経路**で、構造格子は 0 回でした。

#### 入れて、測って、戻したもの: 内側の辺の入れ替え（Delaunay flip）

点を足さず境界も動かさずに三角形の形を整える手です。**効きました**——
最悪面 **13,524 → 11,608（−14.2%）**。

**それでも入れていません。`contact_placement_probe` のメッシュ非多様体が
0件 → 3件になったからです**（`box × torus` の和で 12 / 24 / 57 本）。
B-Rep もテストも全部緑のまま、**表示メッシュだけが壊れます**。

原因を3回推測して3回とも外しました。

| 推測 | 対策 | 結果 |
| :--- | :--- | :--- |
| 溶接で同じ点になる頂点をまたいでいる | 四隅の 3D 距離を見て弾く | **変わらず 3件** |
| earcut が向きの揃わない片を返している | 2枚とも同じ向き・面積を要求 | **変わらず 3件** |
| uv で凸でも 3D で畳まれる | 入れ替え後の法線が元と同じ側かを見る | **変わらず 3件** |

**分からないものは入れない**、というのがこのリポジトリの規則なので戻しました。
壊れた表示メッシュを返すのは、遅いことより悪い失敗です。

#### 途中で自分がやらかしたこと（2つとも実測で見つけました）

**1. 非決定性を持ち込みました。** 入れ替えの順序を `HashMap` の反復に任せて
いたので、同じ入力で枚数が走るたびに変わりました（52,332 / 52,364 / 52,294）。
**4-132 が「今日いちばん重い所見」として直した欠陥と同じものです。** 辺を
並べ替えてから回すようにして、3回とも1バイト違わなくなりました。

**2. 「無効化して測る」が効いていませんでした。** `perl` の置換が黙って
失敗しており、**入れ替えを止めたつもりの測定を2回**、実際は入れたまま
取っていました。そのせいで「入れ替えは原因ではない」と一度誤って結論して
います。**置換したら、置換できたかを必ず確かめてください**（`assert` する
か、`grep` で見る）。

#### 次に触る人へ

**枚数を減らすなら、細分ではなく最初の三角形分割を変えることです。**
入れ替えは点を足さないので、できることに限りがあります（−14%）。1万枚を
切るには、**境界に沿った三角形分割**（内部に点を足す、制約付き Delaunay）が
要ります。OCCT が `BRepMesh_Delaun` でやっているのと同じ方向です（4-76）。

**入れるときは、`contact_placement_probe` のメッシュ非多様体を必ず見て
ください。** テストもB-Repも緑のまま、そこだけが壊れます。

---

### 4-205. 掃き出しを4つ足したら、3つ出ました（2026年8月31日）

「掃き出しは段に入れず、ずっと並行して回す」という決まりに従って、
`contact_placement_probe` に配置を4つ足しました。**4つで3つ出ました。**

選んだ理由は2つです。

- **`cone × torus` は、35配置の表に1件もありませんでした**（他の10組はすべて
  あります）。**組の抜けは、置き方の抜けより見つけやすい**ので先に埋めます
- 交線が**極や頂点を通る**置き方は、この日の別の作業で3件続けて欠陥が出た
  族です（4-78、4-79、4-80）

#### 出たもの

| 配置 | 演算 | 結果 |
| :--- | :--- | :--- |
| `cone × torus`（縁を管に当てる） | **和** | **断られる（「未実装」）** |
| 同 | 差 | ok。**メッシュ非多様体 3本** |
| 同 | 積 | ok。**メッシュ非多様体 4本** |
| `cone × torus`（頂点を穴に通す） | 3演算 | ok、非多様体 0 |
| `sphere × cylinder`（軸が両極を通る） | 3演算 | ok、非多様体 0 |
| `cone × cylinder`（頂点が軸の上） | 3演算 | ok、非多様体 0 |

**極・頂点を通る3配置は、3演算とも通りました。** 4-78〜4-80 で直したものが
効いています。出たのは**組そのものが無かった** `cone × torus` のほうでした。

#### 「未実装として断るのは 0 件」は、35配置での話でした

4-197 の時点で「断る 6 件はすべて『答えが本当に非多様体だから』で、**未実装
として断るは 0 件**」と書きました。**配置を4つ足したら 1 件出ました。**

書いてあったことが誤りだったのではありません。**測った範囲での話だった**、
ということです。同じ書き方をするときは、範囲を添えてください。

#### いまの数字

| | 前（35配置） | 後（39配置） |
| :--- | ---: | ---: |
| 演算 | 105 | **117** |
| 立体を返す | 99 | **110** |
| 断る | 6 | **7**（うち**1件は未実装**） |
| B-Rep 非多様体 | 0 | **0** |
| メッシュ非多様体 | 0 | **2 演算**（3本・4本） |
| 恒等式 | 29配置・破れ 0 | **32配置・破れ 0**、最悪 3.900e-8 |

**門は緑のままです**（B-Rep 非多様体と恒等式の破れだけが赤）。メッシュと
断りは数えて印字するだけなので、4配置を常設に入れても赤にはなりません。
**見えるところに置いておくために入れました。**

#### 次に触る人へ

`cone × torus`（縁を管に当てる）の和が入口です。再現はこれだけです。

```rust
let cone = PrimitiveBuilder::make_cone(10.0, 0.0, 20.0)?;   // 底 r=10、高さ 20
let torus = translate(make_torus(12.0, 4.0)?, (10.0, 0.0, 0.0));
// union だけが「未実装」で断られる。差と積は返るが、メッシュが 3 / 4 本非多様体
```

差と積が返るのに和だけ断られるので、**交線は取れていて、組み立てだけが
足りていない**はずです（`ZENITH_SPLIT_WHY=1` から入ってください）。

---

### 4-206. 面積を囲む片の中に、切れ目が1本混ざっていました（2026年8月31日）

4-205 で掃き出しが出した「`cone × torus` の和が未実装として断られる」を
直しました。**「未実装として断る」は、また 0 件に戻りました。**

#### 何が起きていたか

断り文の内訳は `unmatched 0 / 非多様体 8` ——**取りこぼしではなく、面片が
多すぎる**ほうです（4-74 と同じ読み）。稜を座標つきで出すと、2本が4回ずつ
使われ、**同じ片が同じ稜を2回**使っていました。

```text
NM (-2.8000 9.6000 0.0000) -> (-6.0000 0.0000 0.0000)  used by 4: A#5 B#10 B#10 B#13
```

その片の輪を出すと、**行って戻っています**。

```text
piece #10 (B) outer 7 edges
  [3] (-2.800 9.600 0.000) -> (-6.000 0.000 0.000)   mid (-5.2407 4.8705 0)
  [4] (-6.000 0.000 0.000) -> (-2.800 9.600 0.000)   mid (-5.2407 4.8705 0)
```

**4-74 で入れた判定は「片ごと丸ごと面積 0 か」なので、これは掛かりません。**
片は面積を囲んでおり、その中に切れ目が1本混ざっているだけだからです。

3-1 の規約——**接触は、それ自体では位相を作らない**——がここでもそのまま
実装になります。隣り合う2本が同じ弧を逆に辿っていたら、その2本を落とします。
輪は閉じたままで、面の形も動きません（出て戻るだけなので、前後の稜は
もともと繋がっています）。

#### 見分け方で2回外しました

| 見分け方 | 結果 |
| :--- | :--- |
| 向きの印（`Forward` / `Reversed`）が逆かどうか | **外れ**。割った結果は両側が別々の `Edge` の実体を持ち、印は揃っているとは限りません |
| `same_edge_geometry`（同じ t で標本を突き合わせる） | **外れ**。同じ弧でも媒介変数の取り方が違うと一致しません（実測: 中点は小数4桁まで同じなのに不一致） |
| **端点が入れ替わっていて、中点が同じ** | **当たり**。縫合の突き合わせ（`same_undirected_stitch_edge`）と同じ見方です |

#### 置く場所も1回外しました

最初は `face_encloses_no_area` の隣（刻み込みの**前**）に置きました。
**動きませんでした**——診断を出したら、畳み込みが見ているのは4〜6辺の輪
だけで、**問題の7辺の輪は刻み込み（imprint）のあとに出来ていました**。
刻み込みの後ろへ移して当たりました。

**「入れたのに効かない」ときは、入れた場所が見ているものを数えてください。**

#### 実測

| | 前 | 後 |
| :--- | ---: | ---: |
| `contact_placement_probe` | 110 ok / **7 断り** | **111 ok / 6 断り** |
| うち「未実装」の断り | **1** | **0** |
| 恒等式 | 32配置・破れ 0 | **33配置・破れ 0**、最悪 3.900e-8 |
| B-Rep 非多様体 | 0 | **0** |
| メッシュ非多様体 | 2演算 | **2演算**（そのまま。差 3本・積 4本） |

`cone × torus` の体積が恒等式に乗ります。

```text
union 5395.304159  difference 1605.376070  intersection 489.019037
A + B = 和 + 積:  5884.323192 vs 5884.323196   残差 5.787e-10
A     = 差 + 積:  2094.395102 vs 2094.395106   残差 1.813e-9
```

動いていないこと: テスト 203バイナリ / 681件 / 失敗 0、警告 0、
`boolean_envelope` 44 / 0 / 1・解析解 6.68e-8、`foreign_boolean_probe`
90/90・refused 0・WRONG 0、`foreign_cross_pair_probe` 15組・破れ 0。

#### 残っているもの — 3つの仮説を潰したところまで

**同じ配置の差と積で、メッシュが非多様体のままです**（3本・4本）。B-Rep は
多様体なので、これは表示側の宿題です。**次の人が同じ道を通らないように、
測って消したものを置きます。**

出ている稜はこれです（積、24分割）。

```text
used 1x  (-2.800000  9.600000 0.000000) -> (-2.800000 -9.600000 0.000000)  len 1.920e1
used 1x  (-2.800000  9.600000 0.000000) -> (-2.800000  9.600000 0.000000)  len 1.071e-7
used 1x  (-2.800000 -9.600000 0.000000) -> (-2.800000 -9.600000 0.000000)  len 1.330e-7
```

**同じ座標に見える2点が 1.07e-7 と 1.33e-7 離れています。** `WELD_TOLERANCE`
は 1e-7 なので、**すぐ外側**です。束ねられず、まわりの稜が対を失います。

消えた仮説:

| 仮説 | 実測 | 判定 |
| :--- | :--- | :--- |
| B-Rep の頂点がずれている | 3面が同じ点を **4.4e-15** で共有（稜 145 / 108 / 124） | **違う** |
| 隣り合う面が同じ稜を違う数で刻んでいる（4-83 の機構） | **全面・全稜とも 24 刻み**で揃っている | **違う** |
| 片方の面だけが境界の点を落としている | 落ちるのは各稜の step 0 だけ（継ぎ目の重複除去）。対称 | **違う** |

**残る問いは1つです**——同じ稜を同じ割合で標本しているのに、**どの段で
面ごとに 1.33e-7 ずれるのか**。境界の点は稜の 3D 点で上書きされるはずなので
（`REFINEMENT_CLEARANCE` の注記）、そこが効いていない経路がある、という
のがいまの見立てです（**これは推測です。測ってから直してください**）。

再現:

```bash
# cone(10, 0, 20) と torus(12, 4) を (10,0,0) に置いた積・差、24分割
cargo run --release -p zenith_algo --example contact_placement_probe
```

---

### 4-207. 置き方を3つ変えたら、原因が「境界の辺 1本」まで絞れました（2026年8月31日）

4-205 で足したばかりの `cone × torus` に、**置き方だけ変えた配置を3つ**
足しました。「同じ形を置き方だけ変えて測る」は、この日いちばん効いた手です
（5章）。

#### 実測

| 配置 | 和 | 差 | 積 |
| :--- | :--- | :--- | :--- |
| 管を立てて縁の内側に通す | ok 0本 | ok 0本 | ok 0本 |
| **底の平面から 3 持ち上げる** | ok **9本** | ok **9本** | ok **21本** |
| 円錐を穴の中に立てる | ok 0本 | ok 0本 | ok 0本 |

（数字はメッシュ非多様体の本数。**B-Rep はすべて多様体、恒等式も 36配置で
破れ 0** です。）

#### 「同一平面だから」という読みは消えました

4-205 の配置（トーラスの赤道と円錐の底が同一平面）で 0 / 3 / 4 本でした。
**持ち上げて同一平面をやめたら、9 / 9 / 21 本と悪くなりました。**

**接触の特殊さが原因ではありません。** `cone × torus` の交線一般の話です。

#### 原因は「境界の辺が1本足りない」ところまで絞れました

持ち上げた配置の和で、対を失った稜は**9本すべて `z = 0`**（円錐の底面）に
あり、**どれも1回しか使われていません**——重なりではなく**穴**です。

`ZENITH_EARCUT_WHY=1` を足して数えました（**この口は残してあります**）。

```text
EARCUTWHY   境界の辺 145 本のうち 1 本が三角形分割に無い（三角形 142）
```

**2枚の面が、それぞれ境界の辺を1本だけ落としています。** リングの連続する
2点を結ぶ辺は三角形分割に必ず現れなければならず、現れないと隣の面から見て
相手のいない稜になります。

earcut は共線・重複の点を自分で落とすので、それを測って挿し戻す段
（`reinsert_dropped_boundary_points`、4-86）と、耳を修理する段
（`repair_boundary_ears`）が既にあります。**そこを通っても 1本だけ残ります。**

#### 消えた仮説（4-206 の3つに続いて）

| 仮説 | 実測 | 判定 |
| :--- | :--- | :--- |
| 赤道と底が同一平面だから | 持ち上げると**悪化**（3/4 → 9/21） | **違う** |
| B-Rep の頂点がずれている | 4.4e-15 で一致（4-206） | **違う** |
| 刻み数が面ごとに違う | 全面 24 で揃う（4-206） | **違う** |
| 片側だけ境界の点を落としている | 落ちるのは各稜の step 0 だけ。対称（4-206） | **違う** |

#### その1本を名指ししました

`ZENITH_EARCUT_WHY=1` に、**欠けた辺そのもの**を出す行を足しました。

```text
EARCUTWHY   境界の辺 145 本のうち 1 本が三角形分割に無い（三角形 142）
EARCUTWHY     欠けた辺 [110]->[111]  uv (0.537833400,0.721106841)->(0.537833400,0.701185901)
EARCUTWHY       前後との外積: 前 -4.733e-29、後 -8.360e-16
EARCUTWHY       3D (-3.354459,6.013024,0.000000)->(-3.155074,6.437551,0.000000)
```

**uv の u が完全に同じで、前の点との外積が 4.7e-29**——つまり
`[109] [110] [111]` は**共線**です。earcut の `filterPoints` が真ん中を
落とすのは、まさにこの形です（4-86）。

#### なぜ挿し戻しで戻らないのか

**`reinsert_dropped_boundary_points` は「どの三角形にも使われていない点」を
探します。** ところがこの `[110]` は**使われて**いて、無いのは**辺のほう**
です。だから走査に引っかかりません。

#### またいでいる三角形を出したら、**三角形が丸ごと1枚無い**だけでした

推測せずに現物を出しました。

```text
欠けた辺 [110]->[111]
  三角形 42:  [109] [110] [121]   <- a を使う唯一の三角形
  三角形 141: [120] [121] [111]   <- b を使う
```

**`a` も `b` も、同じ頂点 `[121]` と辺を共有しています。** その間の三角形
`[110][111][121]` だけが無く、そこが穴になっていました。またいでいる
三角形を割る必要はなく、**足りない1枚を足すだけ**です。

`fill_missing_boundary_edges` を入れました——欠けた境界の辺 `(a, b)` に
ついて、**`a` とも `b` とも辺を共有している頂点 `c`** を探して `(a, b, c)`
を足します。点は1つも増えず、覆えていなかったところを覆うだけです。
見つからなければ何もしません。

**実測: 境界の欠けは 0 本になり、メッシュ非多様体は 5演算 → 4演算。**
残る4演算は別の機構です（同じ口で数えると欠けは 0 なので、**境界の辺が
足りないほうではありません**）。

#### 数字

| | 前 | 後 |
| :--- | ---: | ---: |
| `contact_placement_probe` | 39配置117演算 111 ok / 6 断り | **42配置126演算 120 ok / 6 断り** |
| 恒等式 | 33配置・破れ 0 | **36配置・破れ 0**、最悪 3.900e-8 |
| B-Rep 非多様体 | 0 | **0** |
| メッシュ非多様体 | 2演算 | **5演算**（新しく足した配置のぶん） |

**メッシュの数が増えたのは、欠陥が増えたからではなく、測る配置を増やした
からです。** 門は緑のまま（B-Rep と恒等式だけが赤）で、見えるところに置いて
おくために入れました。

---

### 4-208. 残る4演算の機構は「稜の曲線の端が、頂点とずれている」でした（2026年8月31日）

4-207 で「境界の辺が1本足りない」ぶんは埋まり、同じ口で数えると欠けは 0 本
です。**残る4演算には名前が付いていませんでした。** 名前が付いたので置きます。

#### 実測（`cone_torus_mesh_probe` を足しました。**この口は残してあります**）

| 配置 | 演算 | 前 | 後 |
| :--- | :--- | ---: | ---: |
| 縁を管に当てる（4-205） | 差 | 3本 | **0本** |
| 同 | 積 | 4本 | **0本** |
| 底の平面から 3 持ち上げる（4-207） | 和 | 3本 | **0本** |
| 同 | 積 | 16本 | **0本** |

#### 潰した仮説（推測ではなく、全部その場で測りました）

| 仮説 | 実測 | 判定 |
| :--- | :--- | :--- |
| 4-207 で入れた `fill_missing_boundary_edges` が三角形を重ねている | 止めると **9 / 9 / 21 本に悪化** | **違う**（あれは効いている） |
| 片側だけが刻みを1つ飛ばす T字 | 26本のうち **5本だけ**。しかも双子を直したら一緒に消えた | **主因ではない** |
| B-Rep の頂点に双子がある | 1e-5 以内の対 **0組** | **違う** |
| 同じ場所を通る稜が2本ある | 端点が一致して中間だけずれる対 **0組** | **違う** |

**T字は 1e-9 では1本も見つかりませんでした。** 片側が刻みを飛ばすと、その
弦は曲線の膨らみ（サジッタ）ぶん外れます——実測 **6.2e-3**。**判定の閾値を
幾何から決めないと、あるものを「無い」と読みます。**

#### 名前が付きました

非多様体の稜のそばには、**1.069e-7 〜 1.330e-7 離れた頂点の対**が必ず居ま
した。`weld` は **1e-7** で束ねるので、**すぐ外側**です。束ねられないまま
2点として残り、そこが穴になります。

`ZENITH_REFINE_WHY=1` に「溶接距離のすぐ外の対」を出す行を足して数えると、
**対はどれも「境界と境界」**でした（細分の点ではありません。4-131 とは別）。

では、境界の点がなぜ2つになるのか。境界の標本は**稜の曲線**から取ります
（`oriented.evaluate_normalized`）。その端を頂点の位置と突き合わせました。

```text
稜の端 120 箇所、曲線と頂点の差は最大 1.128e-7、溶接距離より大きいもの 4 箇所
```

**メッシュの双子の距離と、同じ数字です。** 隣り合う2本の稜がそれぞれ自分の
曲線を評価するので、その継ぎ目に「同じはずの点」が2つできていました。

#### 直した場所（表示側です）

`boundary_rings` で、**両端だけ頂点の位置を使う**ようにしました。頂点は稜
どうしで共有されているので、2本の稜が同じ点を出し、既にある重複判定が片方
を落とします。隣の面も同じ頂点へ寄せるので、継ぎ目は開きません。

**まず表示側で塞ぎ、そのあと上流を直しました**（下）。表示側の寄せは残して
あります——上流が将来また崩れても、表示メッシュは穴を開けません。上流の値
そのものも常設テストで押さえてあります
（`the_gap_between_an_edge_curve_end_and_its_vertex_stays_where_it_was_measured`、
いま 3.972e-15、閾値 1e-9）。

#### 上流も直しました（同じ日）

**対症のままにしませんでした。** `edge_end_gap_probe` を足して、組を変えて
掃いたら、**`cone × torus` だけの話ではありません**でした。

```text
稜の端 5292 箇所のうち、溶接の距離 (1e-7) を超えているもの 54 箇所。
最大 1.330e-7（cone x torus (rim on the tube) / union）
```

`box × cylinder` に 8 箇所、`cylinder × cone` に 2 箇所あります。**どちらも
ありふれた組**です。

原因は `sew` にありました。**縫うときに、離れた頂点を1つに束ねてから稜へ
入れ直すのに、曲線を置き去りにしていました**——`Edge { .. }` を直接書いて
いて、頂点だけ差し替わっていたからです。`Edge::with_vertices` を足して、
束ねた位置へ曲線の端も一緒に動かすようにしました。

```text
稜の端 5292 箇所のうち、溶接の距離 (1e-7) を超えているもの 0 箇所。
最大 3.972e-15（box x sphere / union）
```

**1.330e-7 → 3.972e-15。** 丸め誤差の桁です。

#### 払った代償（測りました）

**体積が 1e-10 の桁で動きます。** 曲線の端を 1e-7 動かすので当然ですが、
`foreign_cross_pair_probe` の残差は**両方向に動きました**。

| 組 | 前 | 後 |
| :--- | ---: | ---: |
| `cylinder+elliptic_prism` | 6.525e-10 | **1.980e-10** |
| `cylinder+hollow_box` | 4.803e-13 | **1.824e-10** |
| `elliptic_prism+stepped_shaft` | 6.228e-12 | **2.286e-10** |
| `sphere+hollow_box` | 5.666e-11 | **4.735e-10** |

**破れは前も後も 0 組**で、最悪は 7.052e-10 → **9.429e-10**。公差 (1e-6) の
3桁下なので入れましたが、**「良くなっただけ」ではありません**。

**表示メッシュの枚数も動きました**（`face_budget_probe` を足して測りました。
64分割、1面あたりの最大）。

| 演算（25度傾けたトーラス × 箱） | 前 | 後 |
| :--- | ---: | ---: |
| 和 | 8,318 | 9,650 |
| 差 | 9,784 | **14,996** |
| 積 | 11,108 | **8,200** |

**最悪は 11,108 → 14,996 で、H5 は遠くなりました。** 分割を 48 / 64 / 96 と
変えても伸び方はなめらかなので、閾値をまたいだのではなく、曲線が動いたぶん
そのままです。**表示の重さと引き換えに、穴の出どころを断ちました。**

#### 数字

| | 前 | 後 |
| :--- | ---: | ---: |
| `contact_placement_probe` | 42配置126演算 120 ok / 6 断り | **同じ** |
| **稜の端と頂点の差** | 最大 1.330e-7、溶接距離超え 54箇所 | **最大 3.972e-15、0箇所** |
| `foreign_cross_pair_probe` | 15組・破れ 0、最悪 7.052e-10 | 15組・破れ 0、**最悪 9.429e-10** |
| `boolean_envelope` | supported 44 / wrong 0 / error 1 | **同じ** |
| B-Rep 非多様体 | 0 | **0** |
| **メッシュ非多様体** | **4演算** | **0演算** |
| 恒等式（接触配置） | 36配置・破れ 0、最悪 3.900e-8 | **同じ**（36配置・破れ 0、最悪 3.900e-8） |
| テスト | 135 / 687 / 失敗 0 / 警告 0 | **136 / 689 / 失敗 0 / 警告 0** |

**常設テストは、直す前に落ちることを確かめてから入れました**（寄せを外すと
`a_cone_meeting_a_torus_tessellates_into_a_manifold_mesh` が落ちます）。

---

### 4-209. 内側の対角線を Delaunay へ入れ替えました。1面の最大が 23% 減（2026年8月31日）

**4-204 がここで表示メッシュを3件壊して戻しています。** 壊れる形に名前が
付いたので、禁則を3つ足して入れました。**64分割の1面あたり最大 14,996 →
11,484 枚（−23%）**、掃き出しのメッシュ非多様体は **0 件のまま**です。
**H5（1万枚以下）にはまだ 15% 足りません。**

#### 素で入れ替えると、H5 には届きます（が、壊れます）

| 配置（64分割、1面あたりの最大） | 入れ替えなし | 入れ替えあり |
| :--- | ---: | ---: |
| 25度傾けたトーラス × 箱 / 和 | 9,650 | **7,646** |
| 同 / 差 | **14,996** | **5,266** |
| 同 / 積 | 8,200 | **6,210** |
| 2軸に回したトーラス × 箱 / 和 | 6,626 | **7,048** |
| 同 / 差 | 5,980 | **4,660** |
| 同 / 積 | 6,230 | **4,454** |

**最悪 14,996 → 7,646 枚。H5 は 10,000 以下なので、6演算すべてが通ります。**

**それでも入れていません。** `contact_placement_probe` のメッシュ非多様体が
**0件 → 3件**になるからです（`box × torus (inclined 25deg)` の和 6本、
`box × cylinder (both turned)` の積 4本、`box × cone (generatrix in a face)` の
積 3本）。**4-204 と同じ3件です。** 口は `ZENITH_DELAUNAY_FLIP=1` で開けて
あります。

#### 4-204 の3つの推測は、どれも幾何の話でした。**壊れているのは位相です**

4-204 は「溶接で同じ点になる頂点をまたいでいる」「向きの揃わない片」
「3D で畳まれる」を試して3つとも外しました。今回は**測って**、次のところ
まで絞れました。

| 測ったこと | 結果 |
| :--- | :--- |
| 入れ替えが境界の辺を消していないか | **消していない**（`FLIPWHY` は無言。入れ替えは境界に触れません） |
| 壊れた稜のまわりに何があるか | 面 750 の中に `A—M—B` の連なりと**弦 `A—B` の両方**があり、隣の面 753 は `A—B` しか持っていない |
| その欠陥は入れ替えのせいか | **違う**。入れ替えを外しても、その面は**境界の辺を2本落としたまま**でした（点 `[1]` `[2]` が**どの三角形にも使われていない**）。**両側の面が同じ点を落として、たまたまつじつまが合っていただけ**です |

**入れ替えは、先にあった食い違いを露わにするだけです。**

#### 落ちた境界の点を戻す道を2つ足しました（これは入れました）

どちらも点を増やさず、既にある境界の点を使うだけです。

- **`split_edge_carrying_point`**: 使われていない境界の点が**乗っている辺**を
  探して、その辺を持つ三角形を**全部**割る（片方だけ割ると面の中に T字が
  できます）
- **`force_missing_boundary_edges`**: それでも欠ける境界の辺を、**横切って
  いる辺を入れ替えて**通す（制約付き三角形分割の教科書どおり）。凸でない
  四角形は入れ替えられないので、進まなくなったら諦めます

**実測: `box × cone` の面で、境界の辺 53 本のうち欠け 2 本 → 0 本。**
掃き出しの全42配置126演算で、欠けは 0 本です。

#### それでも 3件は直りません

境界の辺が全部そろっても、`box × cone` の積は壊れたままです。**面 750 は
点 `M` を持ち、面 753 は持っていません**——同じ B-Rep の稜を共有している
のに、**面ごとの境界の点列が食い違っています**。

**B-Rep の側は3つとも潰しました**（`edge_end_gap_probe` で測りました）。

| 疑い | 実測 |
| :--- | :--- |
| 稜の途中に別の稜の頂点が乗っている（T字の頂点） | **0 個** |
| 同じ場所を通る稜が2本ある | **0 組**（完全に一致する2本も見るように判定を直したうえで） |
| 稜どうしが部分的に重なっている | **0 組** |

**B-Rep はきれいです。食い違いはテッセレーションの側で作られています。**

`ZENITH_RING_WHY=1` を足して、壊れる2枚を名指ししました。

```text
RINGWHY 面 658 のリング: 点 96、稜 2169 x24、稜 2170 x24、稜 2171 x24、稜 2172 x24
RINGWHY 面 661 のリング: 点 96、稜 2178 x24、稜 1027 x24、稜 2182 x24、稜 2171 x24
```

**2枚は稜 2171 を、どちらも 24 刻みで共有しています。** 落とした点も
`ZENITH_DROP_WHY=1` で比べました——**どちらも各稜の step 0 だけ**で、対称です
（4-206 で確かめたのと同じ）。**境界の取り方は一致しています。**

**入れ替えのあとの uv はきれいです**（`FLIPWHY`）——境界の辺の欠け 0 本、
潰れた三角形 0 枚。**つまり食い違いは、入れ替えのあとの細分で作られています。**

**外した手**（測って戻しました）: 入れ替えにも「境界の折れ線の上を走る弦は
作らない」判定（4-84）を掛けてみましたが、**1本も弾かれず、枚数も 7,646 で
1枚も変わりませんでした**。効果の測れないものは入れません。

#### 名前が付きました: **溶接が、向きの違う双子を「重複」として外していた**

食い違っている点 `M` は `A` と `B` のちょうど中点なので、まず**細分が作った
中点**を疑いました。座標で待ち伏せる口（`ZENITH_SPLIT_WATCH`）を足して
測ったら、**1件も掛かりません**でした。**違います。**

次に、境界の点そのものを座標で追いました（`ZENITH_RING_WATCH`）。

```text
RINGWATCH 面 658 が 稜 2171 の step 21/24 として出した (18.145711119,8.333445396,20)
RINGWATCH 面 661 が 稜 2171 の step  3/24 として出した (18.145711119,8.333445396,20)
```

**両方の面が `M` を出しています**（向きが逆なので step は 21 と 3）。境界の
取り方は一致していて、`A—M` も `M—B` もどちらの面にもあります。

では誰が消したのか。溶接の内訳を、入れ替えの有無で比べました。

```text
入れ替えあり: weld が外した三角形: … 同じ3頂点の重複 1 …
入れ替えなし: weld が外した三角形: … 同じ3頂点の重複 0 …
```

**溶接が1枚外していました。** 一直線の境界で面と面が出会うところでは、
**両側の面が同じ3頂点の薄片を1枚ずつ出します**。閉じた立体としては2枚とも
要ります——**向きが逆で、それぞれ別の側を向いている**からです。ところが
突き合わせの鍵が3つの添字を**並べ替えた**ものだったので、向きの違う双子が
「重複」として外され、その2辺が相手を失っていました。

**鍵を、向きを保ったまま回すだけに直しました**（いちばん小さい添字が先）。
同じ向きの重複はこれまでどおり外れます。**`A—M` と `M—B` の穴は消えました。**

#### 壊れる形は2つでした

残ったのは **`A—B` が4回使われる**形です。現物を出しました。

```text
mesh non-manifold edge uses 4: (18.008,8.333,20) -> (18.283,8.342,20)
  三角形 35:  第3点 (18.146,8.333,20.0)  面積 5.904e-4   <- 箱の面の薄片
  三角形 735: 第3点 (18.146,8.333,20.0)  面積 5.904e-4   <- 円錐の面の薄片（同じ形）
  三角形 65:  第3点 (17.733,8.359,20.0)  面積 4.714e-3   <- 箱の面の隣
  三角形 762: 第3点 (17.306,6.988,17.5)  面積 3.899e-1   <- 円錐の面の隣
```

**この配置は、円錐の母線が箱の面にまるごと乗っています**（そういう配置を
選んで測っています）。接している線のまわりでは、**箱の面と円錐の面が同じ
場所を覆う**ので、両側が同じ薄片を1枚ずつ持ちます。立体としてはそこが
**厚みのない刃**になっていて、「稜はちょうど2枚」という数え方に乗りません。

**外した手**: 潰れた耳を外す判定を緩めてみました（`span² × 2e-14` →
`1e-9`）。**1枚も増えず、3件とも変わりません**——薄片の面積は 5.9e-4 で、
外すほど潰れてはいないからです。戻しました。

**入れ替えが作った欠陥ではありません**（入れ替えなしでは、2枚の面が
たまたま違う切り方をしていて表に出ないだけです）。とはいえ**入れ替えが
露わにする**ので、入れ替え側で作らないようにしました。

#### 禁則を3つ足して、入れました

| 禁則 | 中身 | 効き |
| :--- | :--- | :--- |
| **境界に沿った耳** | リングの連続する3点だけでできた三角形を作らない | 3件 → **1件** |
| **境界の折れ線の上を走る弦** | 4-84 と同じ判定 | 単独では 0（耳と組で要ります） |
| **まっすぐな境界の点をとばす弦** | 同じリングの上で間を2点以上とばし、**とばした点が弦から弦長の 5% 以内**なら作らない | 1件 → **0件** |

3つめの閾値は振って決めました——**0.02 以上で3件とも 0、0.01 では 1 件
戻ります**。枚数がいちばん少ないのは 0.05（11,484 枚。0.02 では 11,984 枚）
なので 0.05 にしています。

**とばす弦を「間が2点以上なら一律に禁じる」ともやってみました**が、
この段では点がすべて境界の点なので**入れ替えが1本も通らず、枚数が
14,996 に戻りました**。近さの条件が要ります。

止めるときは `ZENITH_NO_DELAUNAY_FLIP=1`。

#### 数字（すべてこの日の実測）

| | 前 | 後 |
| :--- | ---: | ---: |
| **1面あたり最大（64分割）** | 14,996 | **11,484**（−23%） |
| 同・6演算の合計 | — | 6演算とも減（例: 積 8,200 → 6,618） |
| `contact_placement_probe` | 120 ok / 6 断り、B-Rep 0、メッシュ **0** | **同じ** |
| 恒等式（接触配置） | 36配置・破れ 0、最悪 3.900e-8 | **同じ** |
| `foreign_cross_pair_probe` | 15組・破れ 0、最悪 9.429e-10 | **同じ** |
| `boolean_envelope` | supported 44 / wrong 0 / error 1 | **同じ** |
| テスト | 136 / 689 / 失敗 0 / 警告 0 | **同じ** |

**H5 は未達です**（11,484 > 10,000）。残りを詰めるなら、4-204 が書いたとおり
**境界に沿った三角形分割**（内部に点を足す制約付き Delaunay）が要ります。
入れ替えは点を足さないので、ここが限界です。

#### 数字

| | 入れ替えなし（いま） | 入れ替えあり |
| :--- | ---: | ---: |
| `contact_placement_probe` | 120 ok / 6 断り、B-Rep 0、**メッシュ 0** | 120 ok / 6 断り、B-Rep 0、**メッシュ 3件** |
| 恒等式 | 36配置・破れ 0、最悪 3.900e-8 | 同じ |
| テスト | **136 / 689 / 失敗 0 / 警告 0** | （測っていません） |
| 1面あたり最大（64分割） | 14,996 | **7,646** |

---

### 4-210. 入れ替えを細分の**途中**にも挟んだら、H5 が達成しました（2026年8月31日）

9-H の **H5（64分割で1面あたり 10,000 枚以下）を満たしました**。

| | 前（4-209） | 後 |
| :--- | ---: | ---: |
| **64分割の1面あたり最大** | **11,484 枚** | **3,854 枚（−66%）** |
| 同・いちばん重い立体の総数 | 38,476 枚 | **22,892 枚（−40%）** |
| H5（10,000 以下） | **未達（残り 15%）** | **達成**（上限まで 2.5 倍の余裕） |

#### 何が足りなかったのか

4-209 は入れ替えを**細分の前に1回だけ**掛けていました。それで
14,996 → 11,484 枚（−23%）まで来ましたが、そこで止まっていました。

**最長辺二分は、割るたびに細長い片を作り直します。** 最初に形を整えても、
1回割った先はまた形が悪い。悪い形のまま次の通しに入るので、同じたわみに
届くまでの割り回数が余分にかかります。

**1通しごとに整えれば、次の通しで割る本数が減ります。** 入れ替えは点を
1つも増やさず、境界にも触れないので、**同じ領域を、より少ない三角形で
同じたわみに収められます**。

止めるときは `ZENITH_NO_FLIP_BETWEEN=1`（効きを測り直すための口です）。

#### 壊していないこと（すべて実測）

| 物差し | 前 | 後 |
| :--- | :--- | :--- |
| テスト | 137 バイナリ / 692 件 / 失敗 0 | **同じ** |
| コンパイラ警告 | 0 | **0** |
| `contact_placement_probe` | 120 ok / 6 断り、B-Rep 非多様体 0、**メッシュ非多様体 0** | **同じ** |
| 同・恒等式 | 36配置・破れ 0、最悪 3.900e-8 | **同じ** |
| `mesh_watertight_probe` | すべて閉じている | **同じ** |
| `boolean_envelope` | 44 / wrong 0 / error 1、解析解 6.68e-8 | **同じ** |
| 同・**仕事量** | 30,621,637 回 / 1,233,834 反復 / 7,548 辿り | **1桁も動かない** |
| `foreign_cross_pair_probe` | 15組・破れ 0・最悪 9.429e-10 | **同じ** |
| 決定性 | — | **3回とも同一** |

**ブーリアンの仕事量が1桁も動いていない**のが、「これは表示側だけの変更」
という証拠です（内外判定はメッシュを使うので、そこが動けば数が変わります）。

#### 門にしました

`face_budget_probe` は**報告するだけ**でしたが、**達成したので赤にしました**
（10,000 枚を超えたら `exit 1`）。両方向で確かめてあります——いまは exit 0、
`ZENITH_NO_FLIP_BETWEEN=1` を付けると exit 1 です。

**通ったものは門にする。** そうしないと、次の変更が静かに戻します。上限を
上げるときは、なぜ上げるのかを 9-H に書いてからにしてください。

#### 4-204 との関係

**私は 8月31日の朝に、この入れ替えを「点を足さないのでここが限界」と
書きました**（4-204）。**そこが違いました**——限界だったのは「入れ替えの
効果」ではなく、**掛ける回数**でした。1回では足りず、通しごとに掛ければ
まだ 66% 落ちます。

4-209 が「詰めるには境界に沿った三角形分割（内部に点を足す制約付き
Delaunay）が要る」と書いていたのも、**この目標については要りませんでした**。
より細かい目標を置くなら、そのときに要ります。

---

### 4-211. 手薄な組を5つ厚くしたら、**今回は1件も出ませんでした**（2026年8月31日）

掃き出しを続けました。42配置のうち、`torus × torus`・`torus × sphere`・
`torus × cylinder`・`cone × sphere` は**それぞれ1つずつ**しかありません。
トーラスは16パッチで継ぎ目も多く、この日いちばん欠陥が出ている面です。
**薄いところから厚くする**、で5つ足しました。

| 配置 | 和 | 差 | 積 |
| :--- | :--- | :--- | :--- |
| トーラス2つ、同じ軸で芯の円だけ違う | 1つ | 1つ | 1つ |
| トーラスの穴に球を通す | 1つ | 1つ | 1つ |
| トーラスの穴に半径 9 の棒を通す | 1つ | 1つ | 1つ |
| 円錐の頂点に球を載せる | **2つ** | 1つ | 空 |
| トーラス2つを横に並べ、管を1点で触れさせる | **2つ** | 1つ | 空 |

**15演算すべてが立体を返し、B-Rep もメッシュも非多様体 0、恒等式も破れ 0**
でした。

#### 打率が落ちました

| 日 | 足した配置 | 出た欠陥 |
| :--- | ---: | ---: |
| 8月30日 | 5 | 3 |
| 8月31日（4-205） | 4 | 3 |
| 8月31日（4-207） | 3 | 1 |
| **8月31日（ここ）** | **5** | **0** |

**「打率が落ちるまでは掃き出しを回す」と 9-4 に書いてあります。** 今回
初めて 0 でした。次に足すときは、**同じ組の置き方を変える**より、**まだ
無い組み合わせや、いままでと違う軸**（大きさの桁を変える、自分の出力を
入力に戻す、など）を探すほうが良さそうです。ただし**1回の 0 で決めない
でください**——1回ぶんの標本です。

#### 「点で触れているだけ」は立体2つで返します

円錐の頂点に球を載せた和と、トーラス2つを横に並べた和は、**立体を2つ**
返します。点で繋がっているものを1つの多様体 `Solid` にはできないので、
これは規約（3-1「接触は、それ自体では位相を作らない」）どおりです。
**恒等式は閉じます**——点の体積は 0 なので。

#### 書いた配置が、狙いと違っていました

**最初、「棒を穴に通す」を半径 7 で書きました。** トーラスの穴の半径は
`12 - 4 = 8` なので、**触れてすらいません**。3演算とも「通った」と出ますが、
測っていたのは**離れた2つの立体**でした。

半径 9 に直して、管の内側へ全周で食い込むようにしました。

**配置を足すときは、狙った接触が本当に起きているかを数で確かめてください。**
「通った」だけでは、何も当てていない配置と区別が付きません。

---

### 4-212. 掃き出しの軸を「大きさの桁」に変えたら、すぐ出ました（2026年8月31日）

4-211 で**置き方を変える掃き出しが初めて 0 件**になったので、軸を変えました。
**大きさの桁**です。

#### なぜこの軸か

このカーネルの公差は**絶対値**です——`Tolerance::linear` が 1e-6、メッシュの
溶接が 1e-7。ところが実務のモデルは mm だけではありません。時計の部品なら
0.1 mm 級、建築なら 10 m 級です。**同じ形でも、桁が変われば公差との関係が
変わります。**

`scale_sweep_probe` を足しました。5配置 × 7桁を、**相対の恒等式**で見ます
（体積そのものは `s³` で変わるので比べても意味がありません）。

#### 1つめ: 100倍にすると断られました（**直しました**）

5配置のうち**3配置が、100倍で3演算とも断られました**。断り文はこれです。

```text
Solid validation failed: Face 0: outer loop edge 5 p-curve differs from
3D edge by 1.008478e-5
```

**絶対公差でした**（`distance > tol.linear`、1e-6）。100倍の模型で 1e-5 は、
**相対では 1e-7**——1倍のときに通っている値と同じです。**桁を変えただけで
断られる**のは、公差の付け方の問題です。

4 m の部品に 1e-6 を要求するのは、相対 2.5e-10 を要求することで、NURBS の
当てはめはそこまで出ません。

**ワイヤの大きさ（境界箱の対角）に比例させました。** `max(1.0)` を掛けて
あるので、**小さいほうへは効きません**——1 未満で比例させると、いま通って
いるものが通らなくなります。ここは「大きい模型で緩める」だけの直しです。

| | 前 | 後 |
| :--- | ---: | ---: |
| 25通り（5配置 × 5桁）のうち断られた演算 | **9** | **0** |

#### 2つめ: 0.02 と 0.01 の間で、恒等式が6桁跳びます（**未修理**）

`torus × cylinder`（穴に棒を通す）だけ、小さい側で残差が跳びます。

| 桁 | 相対の破れ |
| ---: | ---: |
| 0.2 | 1.878e-13 |
| 0.1 | 2.375e-13 |
| 0.05 | 2.856e-12 |
| 0.02 | 2.074e-12 |
| **0.01** | **1.368e-6** |
| 0.005 | 8.912e-6 |

**公差の床なら、滑らかに上がるはずです。** 0.02 → 0.01 で 6桁跳ぶのは、
**どこかに絶対の閾値があって、そこを跨いでいる**ということです。他の4配置は
0.005 まで 1e-7 以下で通るので、**この組に固有**です。

**門は 0.1 までにしました。** 0.02 以下は表示だけで、赤にしません——
**直っていないものを赤で常設すると、赤に慣れてしまいます**。名前を付けて
見えるところに置く、が このリポジトリのやり方です。

##### そのあと測ったこと（同じ日の夜）

**1. 積分の誤差ではありません。** 分割数を 32 → 64 → 128 と上げても、
残差は **1.368e-6 のまま1桁も動きません**。**形そのものが違います。**

| 桁 | 32分割 | 64分割 | 128分割 |
| ---: | ---: | ---: | ---: |
| 1 | 2.185e-13 | 9.967e-13 | 5.707e-12 |
| 0.015 | 1.325e-7 | 1.325e-7 | 1.325e-7 |
| 0.01 | 1.368e-6 | 1.368e-6 | 1.368e-6 |

**2. 単調ではありません。** 0.020／0.018／0.016 は 1e-12 台、**0.015 と
0.014 が 1.3e-7**、0.013／0.012 はまた 1e-12 台、**0.011 と 0.010 が
1.37e-6**。**閾値をひとつ跨いでいるのではなく、飛び飛びに切り替わります。**

**3. 交線の当てはめ許容を大きさに比例させてみましたが、効きませんでした。**
`intersect_nurbs_patches` の `deviation_limit` は `tol.linear` の絶対値
（1e-6）で、0.01 倍の模型では相対 3e-6 まで粗くなります。桁が合うので
疑いましたが、**比例させても残差は 1桁も動きませんでした**。**測れない
変更は入れない**ので戻しました。**次の人は、ここを候補から外して構いません。**

#### 3つめ: `cone × sphere` は 0.005 で断られます（**4-224 で解消**）

こちらは断りなので赤にしません。**桁の端で断るなら、それは分かったほうが
良い事実です。**

#### 掃き出しの軸について

| 軸 | 足した数 | 出た欠陥 |
| :--- | ---: | ---: |
| 置き方（8/30） | 5 | 3 |
| 置き方（4-205） | 4 | 3 |
| 置き方（4-207） | 3 | 1 |
| 置き方（4-211） | 5 | **0** |
| **大きさの桁（ここ）** | **5配置 × 7桁** | **2**（1つは修理済み） |

**軸を変えたら、また出ました。** 「打率が落ちた」のは**その軸が枯れた**の
であって、カーネルが枯れたのではありません。次の軸の候補は、**自分の出力を
入力に戻す**（ブーリアンの結果をもう一度切る）と、**桁の違う2つを組み合わせる**
（大きい板に小さい穴）です。

---

### 4-213. 3本目の軸「自分の出力を入力に戻す」で、また出ました（2026年8月31日）

掃き出しの軸を、もう一度変えました。

| 軸 | 結果 |
| :--- | :--- |
| 置き方（4-205、4-207、4-211） | 3/5 → 3/4 → 1/3 → **0/5**（枯れた） |
| 大きさの桁（4-212） | **2件**（1件は修理済み） |
| **自分の出力を入力に戻す（ここ）** | **2件**（未修理） |

**枯れるのは軸であって、カーネルではありません。** 3本とも、軸を変えた
最初の回で出ています。

#### なぜこの軸か

実務のモデルは、**ブーリアンの結果をもう一度切ります**。ところが常設の
検体は、ほとんどが**ビルダーが作ったばかりの立体**どうしです。
ブーリアンの出力は別物で、

- 面が分割されていて、境界がパラメータ矩形に沿っていない
- 稜が交線の当てはめで、真円でも直線でもない
- 同じ弧に別々の `Edge` の実体が並ぶことがある（4-80）

`chained_boolean_probe` は既にありますが、そちらは**平面だけの穴あき板**で、
**曲面が交わった結果を戻していません**。`rechained_boolean_probe` を足しました。

#### 実測（5連鎖）

| 連鎖 | 1段目の面数 | 2段目 | 恒等式 |
| :--- | ---: | :--- | ---: |
| （円柱 − 円柱）を**箱**で切る | 10 | **0/3 断り** | — |
| （球 − 円柱）を球で切る | 12 | 3/3 | 2.334e-10 |
| （トーラス − 円柱）を**箱**で切る | 20 | **0/3 断り** | — |
| （円錐 + 球）を円柱で切る | 13 | 3/3 | 4.052e-13 |
| （箱 − 円柱）をトーラスで切る | 10 | 3/3 | 4.245e-14 |

**通った3つは恒等式が閉じています**（最悪 2.334e-10）。**曲面で切るのは
通り、箱で切るのが通りません。**

#### どこで止まっているか

断り文は「未実装」で、内訳はこうです。

```text
11 intersection edges, 5 batch-split faces, 8 applied batch splits,
8 skipped batch splits
```

**8本のうち8本の切り込みが当たっていません。** `ZENITH_SPLIT_WHY=1` で
理由を見ると、専用の経路が全部断っています。

```text
SPLITWHY (10.0000 0.0000 14.0000) -> (8.0000 6.0000 20.0000) split nothing
  Split edge endpoints do not match cylinder side boundaries;
  Cylinder-side split edge must be a ruling along the patch axis;
  Section edge is not a circle about a...
```

切り込みは**1段目の交線の断片**なので、線織でも円でもありません。

汎用の面分割器に1本ずつ渡すと、こう言います。

```text
A1 (0.00 10.00 25.00)->(-5.00 8.66 25.00): the splitting curve ends 5.176e0 away from the boundary
A5 (-5.00 8.66 40.00)->(-5.00 -8.66 40.00): ok, 2 片, 残差 7.057e-15
```

**端が面の境界から 1〜5 離れています**——**断片なので、繋いでからでないと
境界から境界へ届きません**。カーネルは鎖にまとめる段を持っています
（`group_edges_into_chains`）ので、**1本ずつ渡した上の測り方は、そのままでは
カーネルの断り方と同じではありません**。ここは次の人が確かめるところです。

#### 鎖にまとめてから測り直したら、機構が名指しできました

カーネルと同じく**面ごとに集めて鎖で渡す**と、こうなります。

| 面 | 切り込み | 結果 |
| :--- | ---: | :--- |
| A1、A2（曲面、稜7本） | 2本 | **ok**（2片、残差 1.6e-12） |
| A5（平面） | 1本 | **ok**（残差 7.1e-15） |
| **A0、A3（曲面、稜7本）** | 1本 | **端が境界から 1.000 離れている** |
| **A7、A8（曲面、稜4本）** | 2本 | **端が境界から 0.544 離れている** |

**端の位置を測ると、片側だけが浮いています。**

```text
A0  切り込みの始点 (10.0000 0.0000 25.0000)  境界まで 1.0000
    切り込みの終点 ( 0.0000 10.0000 25.0000)  境界まで 0.0000
```

`(10, 0, 25)` は半径10の円柱の上、角度 0 度の位置です。**そこは支持
パッチ（四半円）の縁**で、切り込みはそこで止まっています。ところが
**面の実際のトリム境界は、そこにありません**——1段目の円柱どうしの差が
その角度あたりを削っており、面の境界は削られた縁（交線）まで後退して
います。その差が **1.0** です。

**つまり: 2段目の交線が「支持曲面のパッチの縁」で止まり、「面のトリム
境界」に着いていません。** 1段目の出力は、支持パッチとトリム境界が
食い違う（パッチの一部しか使っていない）ので、そこが出ます。ビルダーの
出力では両者がほぼ一致するため、この軸でなければ出ません。

#### 「切り詰めれば通る」ところまで、裏を取りました

**推測で終わらせないために、手で切り詰めて測りました。**

まず、切り込みが本当に面からはみ出しているかを数えます。A0 の切り込みを
65 点で標本し、各点を面のトリム領域（p-curve の多角形）で内外判定します。

```text
A0: 標本 65 点のうち面の中は 49 点（15..63）
```

**先頭の 23% が面の外**です。1段目が削った切り欠きの中を走っています。

次に、内側の区間だけを**厳密な部分曲線**（`split_at` を2回）で取り出して
割り直します。

| 渡したもの | 端が境界から |
| :--- | ---: |
| そのまま | **1.000** |
| 内側だけに切り詰め | **0.222** |

**切り詰めると 1/5 まで縮みます。** 残っている 0.222 は、**私の走査が 65 点と
粗いぶん**（1 目盛が弧長で約 0.24）です。交差点を挟み込んで求めれば 0 に
なります。

**当てはめ直してはいけません。** 最初は標本点から3次で当てはめ直しましたが、
`the splitting curve leaves the face by 2.677e-3` で断られました——**測って
いたのは自分の当てはめ誤差**です。`split_at` で厳密に切ってください。

#### 実装してみて、**もう1段先に壁がある**と分かりました（入れていません）

上の手順どおりに、**どの経路も外れたときの受け皿**として実装し、測りました
（通っている経路の費用が増えないよう、失敗の後にだけ走らせています）。

| 段階 | 端が境界から | 結果 |
| :--- | ---: | :--- |
| 切り詰めない | **1.000** | 断られる |
| 標本 65 点で切り詰め | 0.222 | 断られる |
| **挟み込みを入れる** | **1.592e-3** | 断られる（許容は 2e-4） |
| **境界の多角形を 24 → 96 点に** | 届く | **割れる（2片）。ただし面積の残差 1.209** |

**端は境界に着くようになりましたが、割った片の面積が合いません。**
残差 1.209 は、片が領域として正しく取れていないということです。

**つまり、壁は「切り詰め」ではなく、その先の「領域の取り方」にあります。**
切り込みが**曲がったトリム境界**（1段目の交線）に着いたとき、境界の巡回を
2本に割るところが正しく回れていない、というのがいまの見立てです
（**これは推測です。measure してから直してください**）。

**動かないものは入れない**ので、実装は戻しました。上の数字は、次に触る人が
同じところまで一気に来られるように置いてあります。

#### 次の一手

**`FaceSplitter::split_by_chain` が、曲がった境界に着いた切り込みで
領域をどう取っているか**を見てください。面積の残差 1.209 が出た時点の
片を2枚とも取り出して、どちらがどれだけ余分か／足りないかを測るのが
入口です。

**→ 測りました。見立ては外れです（4-214）。巡回は正しく、間違っていたのは
検算のほうでした。**

なお `clip_candidate_to_face_bboxes` は境界箱までしか見ていません。
切り詰めそのものを入れるなら、そこが場所になります。

**通っている A1・A2 との違いも手がかりです**——あちらは切り込みが2本
届いていて、鎖にすると境界から境界へ渡れています。

**通っている A1・A2 との違いも手がかりです**——あちらは切り込みが2本
届いていて、鎖にすると境界から境界へ渡れています。

**門にはしていません。** 断ることは赤にしない、という決まりどおりです
（`rechained_boolean_probe` は恒等式の破れと非多様体だけを赤にします。
いまは 0 件で緑）。


### 4-214. 残差 1.209 の正体は、**割り方ではなく検算のほう**でした（2026年8月31日）

4-213 の「次の一手」どおり、**残差 1.209 が出た時点の片を2枚とも取り出して
測りました**。見立て（「領域の取り方が悪い」）は**外れていました**。

#### 片の中身

`(円柱 − 円柱)` の A0 面（元のパラメータ面積 568.798344666）を、切り詰めた
切り込み `(9.434 3.316 25.000) -> (0.000 10.000 25.000)` で割った結果です。

```text
片 0（稜 6 本）                          片 1（稜 5 本）
 (9.434 3.316 25) -> (8 6 20)            (0 10 25) -> (0 10 40)
 (8 6 20)         -> (10 0 14)           (0 10 40) -> (10 0 40)
 (10 0 14)        -> (10 0 0)            (10 0 40) -> (10 0 26)
 (10 0 0)         -> (0 10 0)            (10 0 26) -> (9.434 3.316 25)
 (0 10 0)         -> (0 10 25)           (9.434 3.316 25) -> (0 10 25)
 (0 10 25)        -> (9.434 3.316 25)
```

**正しく割れています。** 片0が下側（1段目の欠き取り `(10 0 26)->(8 6 20)->
(10 0 14)` を抱えたほう）、片1が上側。どちらも閉じていて、重なっていません。

#### なのに、面積が両方とも同じ値でした

```text
片 0: 面積 628.318530718（元の 1.1046 倍）
片 1: 面積 628.318530718（元の 1.1046 倍）
```

**2枚が9桁まで同じ**で、その値は `2π × 10 × 40 ÷ 4 = 628.3185307`——
**四半円柱パッチの、トリムを無視した丸ごとの面積**です。元の面（568.798）は
1段目で欠けた後の姿ですから、**検算が「欠けも切り込みも無い素のパッチ」を
2回測っていた**ことになります。`(628.32 × 2 − 568.80) ÷ 568.80 = 1.209`。
**残差 1.209 はここから出ていました。**

#### なぜそうなるか（3段の落ち方）

```text
片の p-curve: **取れない**
  -> Edge 223 projection to NURBS surface exceeds tolerance;
     max distance 1.895788e-5
```

1. 切り込みの稜（1段目の交線を切り詰めたもの）が、**乗っているはずの円柱から
   1.895788e-5 浮いています**。`Tolerance::linear` は 1e-6 なので、
   `Face::pcurves` が断ります。
2. `parameter_areas` は**1枚でも取れなければ `None`** を返します
   （`face_split.rs:139`）。
3. 受け皿の `parameter_area_check` は「**測れないなら、高いほうで測ります。
   断りません**」として `compute_face_integral` に落ちます
   （`face_split.rs:126`）。ところがそこは、**p-curve が無ければトリムを
   知りません**——`add_surface` はパラメータ矩形の求積に落ち、
   **パッチ全体**を返します。

**「高いほうで測る」つもりの受け皿が、実際には「何も測っていない数」を
返しています。** しかもその数は黙って門を通り、**正しく割れた結果を
落としていました。**

#### 名前を付けます

| | |
| :--- | :--- |
| **欠陥 A** | 面積の受け皿が、トリムを読めないときに**素のパッチを測る**。測れないなら「測れない」と言うべきところ |
| **欠陥 B** | ~~1段目の交線が、乗っているはずの円柱から 1.9e-5 浮いている~~ **→ 取り下げました（4-215）。交線は 101 本すべて 5e-7 以下で合っています。浮かせていたのは、切り込みの端を境界へ吸着させる段でした** |

**A のほうが重い**と見ています（**B はそもそも存在しませんでした。4-215**）。A は
**間違った数を、正しい数の顔で返す**からです。このリポジトリで
いちばん避けたい種類の欠陥で、**測っていないのに測ったことにしている**
検算は、他の場所にも同じ形で潜んでいる可能性があります。

#### 直していません

門の振る舞いがカーネル全体で変わる場所なので、**測って名前を付けるまで**に
しました。次の人へ:

1. `parameter_area_check` の `None` 経路が、**いま実際にどれだけ走っているか**
   を数えてください（`compute_face_integral` に落ちた回数）。走っていないなら
   直すのは安全、走っているなら**そこで通っている門を先に洗う**必要があります。
2. そのうえで、受け皿は**数を返さず断る**か、少なくとも
   `ParameterAreaCheck::parametric == false` を**門の側が見る**ようにする、が
   筋だと思います（いまその旗は付いていますが、残差の判定は旗を見ていません）。
3. B は 4-212 の「絶対公差」と同じ家族です。`pcurves` の許容も、
   4-212 で `face.rs` に入れた**大きさに比例させる**やり方が効くかもしれません。

**4-213 の見立て「領域の取り方が悪い」は取り下げます。** 巡回は正しく
回っていました。**推測を書いたら、次の人が最初にやることは、その推測を
測って潰すことです**——今回はそれで1手で当たりました。


### 4-215. 交線は合っていました。噛み合っていないのは**2つの門の許容**でした（2026年8月31日）

4-214 で欠陥を2つ挙げました。**B（交線が曲面から 1.9e-5 浮いている）は
取り下げます。** 測ったら、交線は合っていました。

#### まず、交線がどれだけ浮いているかを測りました

`intersection_fit_probe` を足しました。交線を 33 点で標本して、**両側の
支持曲面まで**の距離を測ります。

| 段 | 交線 | 最悪の浮き | 1e-5 超え |
| :--- | ---: | ---: | ---: |
| 1段目（ビルダーの出力どうし、8配置） | 58 本 | **4.737e-7** | **0 本** |
| 2段目（ブーリアンの結果を切る、3連鎖） | 43 本 | **1.936e-7** | **0 本** |

**101 本すべてが 5e-7 以下**です。とくに 4-214 で問題になった
`(円柱−円柱) を箱で切る` の2段目は **1.176e-14**——ほぼ機械精度でした。

#### `split_at` も無罪でした

次に疑ったのは、切り詰めに使う `split_at` です。交線を 0.25 / 0.5 / 0.7 の
位置で割り、さらにもう一度割って、そのつど曲面までの距離を測りました。

```text
face            元のずれ            前半            後半         前半の前半
A0             3.978e-15     7.105e-15     7.957e-15     7.957e-15
A1             1.066e-14     7.105e-15     1.066e-14     7.332e-15
...（8面 27 通り、すべて 1e-14 台）
```

**二度割っても増えません。** `split_at` は厳密です。

#### 浮かせていたのは、**端の制御点を掴んで動かす**段でした

`split_by_chain` は、切り込みの端を境界の着地点へ**吸着**させます
（`snapped_onto_boundary` → `move_traversal_end`）。やっていることは
**端の制御点を目標点へ置き直す**ことです。

同じ交線の端の制御点を、**半径の外向き**（曲面から離れる向き）へ動かして
測りました。

| 動かした量 | 動かす前の浮き | 動かした後の浮き |
| ---: | ---: | ---: |
| 1e-6 | 3.997e-15 | **1.000e-6** |
| 1e-5 | 3.997e-15 | **1.000e-5** |
| 1e-4 | 3.997e-15 | **1.000e-4** |
| 4e-4 | 3.997e-15 | **4.000e-4** |
| 1e-3 | 3.997e-15 | **1.000e-3** |

**動かした量ちょうど浮きます**（1:1）。端の制御点は曲線の端そのものなので、
当たり前といえば当たり前です。

**ただし、これは上限です。** 実際の吸着先は**面のワイヤの頂点**で、そこは
**曲面の上**にあります。そこで、**曲面に沿って**動かした場合も測りました。

| 動かした量 | 周方向（円柱の周りへ） | 周方向（曲面へ引き戻す） | **軸方向**（線織に沿って） |
| ---: | ---: | ---: | ---: |
| 1e-6 | 2.500e-7 | 2.500e-7 | **7.161e-15** |
| 1e-5 | 2.500e-6 | 2.500e-6 | **7.161e-15** |
| 1e-4 | 2.500e-5 | 2.500e-5 | **7.324e-15** |
| 4e-4 | 1.000e-4 | 9.999e-5 | **7.216e-15** |
| 1e-3 | 2.500e-4 | 2.500e-4 | **7.324e-15** |

**向きで別物です。**

- **線織（母線）に沿って動かすぶんには、まったく浮きません。** 円柱は
  その向きに直線なので、端の制御点を滑らせても曲線は面の上に残ります。
- **周方向は、動かした量の 0.25 倍浮きます。** 曲がっている向きなので、
  端を動かすと端の内側が弦のほうへ落ちます。
- 目標点を曲面へ引き戻しても変わりません（2.500e-6 のまま）。**端点が
  曲面の上にあることと、曲線が曲面の上にあることは別**です。

**0.25 倍というのが、噛み合わせを決める係数です。**

**この 0.25 は曲率に依りません。** 半径 1 / 2 / 5 / 10 / 40 の円柱で、
動かす量 1e-6 と 1e-4 の両方で測って、**10 通りすべてが 0.2500 ちょうど**
でした。有理2次の弧なので、端の制御点の基底が真ん中で **B₀(½) = 1/4** に
なる、その 1/4 です。**曲率のきつい面で悪化する、という心配は要りません**
（次数が上がれば別の値になります。いまの交線は2次です）。

#### 噛み合っていない2つの許容

ここが本題です。**同じパイプラインの2つの門が、桁のちがう許容を持って
います**——片方は面の大きさに比例し、もう片方は絶対値です。

| 門 | 式 | 効き方 |
| :--- | :--- | :--- |
| 切り込みの端を吸着してよい量（`split_by_chain`） | `tol.linear * 10 * boundary_extent(wire).max(1.0)` | **面の大きさに比例** |
| 曲線が曲面に乗っていると認める量（`Face::pcurves`） | `tol.linear.max(1e-6) * 10` = **1e-5** | **絶対値。面の大きさに依らない** |

`boundary_extent` は「ワイヤの最初の頂点からの最大距離」です。この面では
**29.5973**、つまり吸着の許容は **2.960e-4**。上の 0.25 倍を掛けると、
**曲がった向きに 4e-5 より大きく吸着した時点で、p-curve の 1e-5 を超えます**
——**許容 2.960e-4 は、その 7.4 倍**です。

4-214 で出た浮き **1.895788e-5** を 0.25 で割ると **7.6e-5**。許容の
2.960e-4 の内側なので、**割る側は当然のように通し、p-curve 側が断った**
——数が合います。

**割る側が「十分近い」と認めて曲がった向きに 4e-5 より大きく吸着すると、
その結果の片は p-curve が取れなくなります。**
そこから先は 4-214 のとおりです——面積の検算が受け皿へ落ち、**トリムを
知らない素のパッチ**を測り、**正しく割れた結果が捨てられます**。

**これは私の実験に固有ではありません。** 切り込みの端が境界から 1e-5 より
遠いところに来る割り方——つまり**難しいほうの割り方**が、そのまま落ちます。

#### 直したこと（欠陥 A のほう）

**「測れなかった」と言うようにしました。** 元は測れたのに片だけ p-curve が
取れないとき、`checked_areas` はこう断ります。

```text
split_by_chain: piece 0 has no readable p-curve, so this split cannot be
checked (the 3D fallback would measure the untrimmed patch)
```

**断る点は前と同じです。変えたのは、言っていることを本当にしたところ**です。
4-213 では「面積の残差 1.209」を読んで「領域の取り方が悪い」と推測し、
正しい巡回を疑いに行きました。**嘘の数は、次に読む人の時間を奪います。**

安全であることは数えました。**受け皿（`3d*`）に落ちた分割は、常設の掃き出し
1,005 回のうち 0 回**です（`contact_placement_probe` の 885 回と
`scale_sweep_probe` の 120 回。`ZENITH_AREA_CHECK_WHY=1` の `3d*` 印を
数えました。この口を付けると 3D の面積を毎回積むので**とても重く**、
`contact_placement_probe` は最後まで流していません——885 回で打ち切りです）。
**いま通っているものは、どれもこの経路を通っていません。**

#### 直していないこと（許容の噛み合わせ）

**吸着の許容を 1e-5 まで締める**のが素直に見えますが、**やっていません**。
`snapped_onto_boundary` のコメントにある 3-N-1（球を極を通る弧で割る）は、
**弧の端が極から 1.669e-5 ずれていて、それを吸着して通しています**。締めると
そこが落ちます。**いま通っているものを落とす直しは、落ちる先を測ってから**
です。

#### 実際どれだけ吸着しているか（`ZENITH_SNAP_WHY=1` で測りました）

口を足して、`contact_placement_probe`（47配置・141演算）を通しで流しました。

| 吸着量 | 回数 |
| :--- | ---: |
| 1e-9 未満 | **1,517** |
| 1e-9〜1e-7 | 69 |
| 1e-7〜1e-6 | 24 |
| 1e-6〜1e-5 | 0 |
| **1e-5 以上** | **0** |

**1,610 回すべてが 2.714e-7 以下**でした。**いま通っているものは、
噛み合わせの穴に一度も落ちていません。**

**つまりこれは潜在的な罠です。** 壊れるのは周方向 4e-5 からで、実測の
最大はその **147 分の 1**。許容 2.960e-4 のほうは、**実際に使われている
量の 1,000 倍**を認めています。

#### 次の人へ

**→ 1 は入れました（4-216）。落ちたものは1つもありません。**

1. **吸着の許容に天井を付ける**のが素直です。上の分布からすると、
   4e-5 あたりの天井は**いま通っているものを1つも落としません**
   （最大 2.714e-7 に対して 147 倍の余裕）。`snapped_onto_boundary` の
   コメントにある 3-N-1 の 1.669e-5 も、その天井の内側です。
   **さらに安全側に振るなら、天井を `on_surface_limit`（1e-5）そのものに
   する**手もあります——「面に乗っていると言える距離より遠くへは吸着
   しない」という筋の通し方で、係数がいくつでも成り立ちます。実測の最大
   2.714e-7 に対して 37 倍の余裕があるので、こちらでも落ちないはずです。
   ただし 3-N-1 の 1.669e-5 は**この天井には収まりません**。もっとも、
   **3-N-1 は `contact_placement_probe` の配置として入っており**（同 216 行）、
   その通しでの吸着最大が 2.714e-7 でした——**1.669e-5 はもう起きていない**
   可能性が高いです。通しテストで確かめてから決めてください。
2. あるいは**吸着ではなく当てはめ直し**——端を動かしたあと、曲線を曲面へ
   引き戻す。ただし 4-213 の教訓どおり、**当てはめ直したら自分の当てはめ
   誤差を測ることになる**ので、そこは慎重に。
3. `on_surface_limit` を 4-212 と同じく**大きさに比例**させる手もあります。
   ただしこちらは「面の上に乗っているか」の判定なので、緩めると
   **乗っていない曲線を通します**。1 と 2 を先に。

#### コメントが1つ、実物と違っていました

`snapped_onto_boundary` にこう書いてあります。

> クランプされた B-spline は、**端の制御点がそのまま端点**です。そこを
> 差し替えれば、**曲線の途中は動かさずに**端だけを合わせられます。

**前半は正しく、後半は正しくありません。** 端点は確かに合いますが、
**端の1区間ぶんは動きます**——上の表の 0.25 倍がそれです。直線の向き
（線織）に動かすときだけ、途中が動きません。コメントを実測に合わせました。

#### 測り方について

**推測を3つ潰して、4つめで当たりました**（交線の当てはめ → `split_at` →
吸着）。**どれも「疑わしい」と思っただけでは分からず、測ったら1回で
片が付きました。** 4-214 で「B のほうが軽い」と書いたのも外れです——
B は存在しませんでした。

`intersection_fit_probe` は**門にしていません**。いまは 0 本なので門に
できますが、**まだ1回しか流していない**数字です。


### 4-216. 噛み合わせに天井を付けました（2026年9月1日）

4-215 で見つけた「2つの門の許容が噛み合っていない」ほうを塞ぎました。

#### 何をしたか

`split_with_ordered_cut` の `limit` に天井を1つ足しただけです。

```rust
let limit = (tol.linear * 10.0 * scale).min(SNAP_CEILING);  // SNAP_CEILING = 4e-5
```

この `limit` は2つのことを決めています——**切り込みの端が境界からどれだけ
離れていても受け取るか**と、**受け取った端をどれだけ動かして境界に合わせるか**
です。後者が曲線を曲面から浮かせるので、**浮きが `Face::pcurves` の許容
（絶対 1e-5）を超えない範囲**に収めます。

浮きは吸着量の **1/4**（4-215 の実測。曲率に依らず 0.2500 ちょうど）なので、
`1e-5 ÷ 0.25 = 4e-5` が天井です。

#### 落ちたものは1つもありません

| | 前 | 後 |
| :--- | :--- | :--- |
| 通しテスト | 137 バイナリ / 692 件 / 失敗 0 | **同じ** |
| 警告 | 0 | **0** |
| `contact_placement_probe` | 41配置の恒等式で破れ 0、最悪 3.900e-8 | **同じ** |
| `scale_sweep_probe` | 破れ 0、非多様体 0、断り 3、最悪 8.912e-6 | **同じ** |
| `rechained_boolean_probe` | 破れ 0、非多様体 0、2段目の断り 6 | **同じ** |

**当たり前の結果です。** 4-215 で `ZENITH_SNAP_WHY=1` を流したとき、
吸着 1,610 回の最大が **2.714e-7**——天井の 147 分の 1 でした。**先に分布を
測ってあったので、入れる前から落ちないと分かっていました。**

#### 大きさの桁について

**天井は絶対値です。** 4-212 で公差を「大きさに比例」させたのと逆向きに
見えるので、断っておきます。

比例させてよいのは**その門が単独で完結している**ときです。ここは違います
——吸着した結果を受け取るのは `Face::pcurves` で、**そちらの許容が絶対
1e-5** だからです。**比例のほうだけを大きくすると、割る側が通したものを
p-curve 側が断る**、という 4-214 の形になります。

`scale_sweep_probe` を 100 倍・10 倍でも流して、**断りが増えていない**
（3件のまま、いずれも 0.005 の小さい側）ことを確かめました。

`on_surface_limit` をいつか大きさに比例させるなら、**この天井も一緒に
動かしてください**。定数のところにそう書いてあります。

#### 残っているもの

**`(円柱−円柱) を箱で切る` は、まだ通りません**（4-213）。天井は
「割る側が通したものを p-curve 側が断る」経路を塞いだだけで、**そもそも
切り込みが境界まで届いていない**という元の問題は残っています。4-213 の
実測では端が **1.000** 離れており、天井とは桁が3つ違います。

そちらの入口は 4-213 のままです——**断片の交線を鎖にまとめる段
（`group_edges_into_chains`）に何が渡っているか**を見てください。


### 4-217. 切り込みをトリム境界まで厳密に切り詰める（2026年9月1日）

4-213 の壁——**曲面のブーリアン結果を箱で切ると断られる**——を、もう一段
掘りました。4-213 の見立て（「断片だから鎖にしないと届かない」）も**外れ**
でした。

#### 鎖は、ちゃんとできています

`ZENITH_SPLIT_WHY=1` で見ると、こうです。

```text
CHAINWHY 汎用の鎖分割へ: 交線 2 本 → 鎖 1 本
CHAINWHY   鎖 2 本: the splitting curve ends 5.438e-1 away from the boundary
```

**2本はちゃんと1本の鎖になっています。** それでも端が 0.54 余ります。
つまり**断片だからではありません**。

#### 余っているのは、切り込みが境界の外まで伸びているからです

2段目の交線は、**支持パッチの縁**まで伸びます。ところが面の実際の境界は、
1段目で削られた分だけ内側へ後退しています。その差が **0.54〜1.00** です。

**3D で「境界までの距離が 0 になるところ」を探して、そこで切りました。**

| やり方 | 端が境界からどれだけ離れるか |
| :--- | ---: |
| 切り詰めない | 1.000 |
| UV の 96 角形で挟む（4-213） | 1.9e-5（**吸着で曲線が浮いて p-curve が壊れる**。4-215） |
| **3D で距離を黄金分割で詰める（ここ）** | **5.6e-10** |

切るのは `split_at` で厳密に。**当てはめ直しません**——当てはめ直すと自分の
当てはめ誤差を測ることになります（4-213 の教訓）。

面ごとに測ると、こうなりました。

| 面 | 前 | 後 |
| :--- | :--- | :--- |
| A0 | 端が 1.000 離れている | **2片、残差 2.175e-9** |
| A3 | 端が 1.000 離れている | **2片、残差 2.175e-9** |
| A5 | （通っていた） | 2片、残差 7.057e-15 |

#### 切ってよいのは、**鎖の自由端だけ**でした

最初は「境界に乗っていない端」を全部切ろうとして、**ドリル穴の壁で何も
できませんでした**。鎖はこうなっています。

```text
(-5.0000 3.3166 25.0000) -> (10.0000 3.3166 25.0000)
(-5.0000 3.3166 25.0000) -> (-5.0000 0.0000 26.0000)
```

`(-5, 3.3166, 25)` は**2本が共有する継ぎ目**で、境界から 3.464 離れています。
**そこは境界の上にある必要がありません。** ところが「境界に乗っていない端」
という条件だと、そこも切ろうとして、両端の交点が同じ1点になり、
「切れない」と返していました。

**鎖にまとめてから、鎖の両端（自由端）だけを切る**ように直しました。
自由端は「その点を使っている稜が鎖の中に1本しかない」端です。

| | 稜ごとに切る | **鎖の自由端だけ切る** |
| :--- | ---: | ---: |
| 切り詰めが走った回数 | 6 | **12** |
| そのうち断られた回数 | 0 | **0** |

#### どこに入れたか

`split_face_by_edges` の受け皿の中、**素のままの鎖を試したあと**です。

```text
1. 専用の経路（円柱の線織、円の断面 …）
2. 鎖にまとめて汎用の面分割器
3. **ここ**（切り詰めて、もう一度 2 を）
4. 面の内側で閉じた輪
5. 穴を横切る切り込み
```

**推測を含むもの（どの交点で切るか）は、素のままで通るものの後ろに置く**
——4-213 で決めた順序どおりです。切り詰めが素の割り方を横取りすることは
ありません。

#### 落ちたものは1つもありません

| | 前 | 後 |
| :--- | :--- | :--- |
| 通しテスト | 137 バイナリ / 692 件 / 失敗 0 / 警告 0 | **同じ** |
| `contact_placement_probe` の恒等式 | 41配置、破れ 0、最悪 3.900e-8 | **同じ** |
| `scale_sweep_probe` | 破れ 0、非多様体 0、断り 3、最悪 8.912e-6 | **同じ** |
| `face_budget_probe`（H5） | 最悪 3,854 枚 | **同じ** |
| `rechained_boolean_probe` で切り詰めが走った回数 | — | **12 回、断られたのは 0 回** |
| `rechained_boolean_probe` | 破れ 0、非多様体 0、2段目の断り 6 | **同じ** |

#### 壁は動きました——次は**蓋**です

`(円柱−円柱) を箱で切る` は **0/3 のまま**ですが、**断りの中身が変わりました。**

| | 4-213 の時点 | いま |
| :--- | ---: | ---: |
| 当たった分割 | 8 | **14** |
| 外れた分割 | 8 | 8 |
| **蓋のループ** | 0 | **0** |
| **蓋の面** | 0 | **0** |
| 縫合の相手なし稜（差） | — | 8 |

**分割はもう主因ではありません。** 蓋が1枚も作れていないので、選んだ面片
だけでは殻が閉じません（差で相手なし 8、和では非多様体 12）。

**次の人へ**: `CapBuilder` に**何が渡っていて、なぜループにならないか**を
見てください。交線 11 本のうち、蓋の材料になるはずのものがどれで、どこで
落ちているか。入口は「蓋のループが 0 本」という一点です。

なお、この面の境界は**ちゃんとそこにあります**——`(9.43398, 3.31662, 25)`
での境界までの距離を測ると、丸め誤差の範囲でした。**「1段目が境界を作って
いない」という筋は消えました。**


### 4-218. 蓋が1枚も作れない理由を、材料まで降りて見ました（2026年9月1日）

4-217 で分割が 8 → 14 に増え、断りが**蓋の生成**へ移りました。`ZENITH_CAP_TRACE=1`
で、蓋の材料をそのまま出しました。

#### 材料（`(円柱−円柱) を箱で切る` の差、z = 25 の蓋）

```text
稜 (10.000000 0.000000 25) -> ( 0.000000 10.000000 25)  長さ 14.142136
稜 ( 0.000000 10.000000 25) -> (-5.000000  8.660254 25)  長さ  5.176381
稜 (-5.000000 -8.660254 25) -> ( 0.000000 -10.000000 25)  長さ  5.176381
稜 ( 0.000000 -10.000000 25) -> (10.000000  0.000000 25)  長さ 14.142136
稜 (-5.000000  3.316625 25) -> (10.000001  3.316625 25)  長さ 15.000001
稜 (-5.000000 -3.316625 25) -> (10.000001 -3.316625 25)  長さ 15.000001
cap group face_b 0: 6 edge(s) in -> 0 loop(s), 6 skipped
```

**6本入って、ループ 0 本。** 理由は2つあります。

#### 1つめ: 稜どうしが**互いを刻んでいません**

下2本（ドリル穴の壁の切り口、`y = ±3.316625` の直線）は、上4本のうち
円柱の弧と**交わります**。円 `x² + y² = 100` 上で `y = 3.316625` になるのは
`x = 9.433981`——弧の**途中**です。

**そこに頂点がありません。** 端で繋がった稜だけを追ってループを作るやり方
なので、途中で交わっていても見えません。`split_by_curves` の注記に
「交線どうしが面の内側で交わる場合は、まだ扱えない。先に交点で互いを刻む段
（imprint）が要る」と書いてあるのと**同じ穴**です。蓋の側にもあります。

なお直線の端が `10.000001` になっているのは、**その稜が切り詰められていない**
からです。4-217 の切り詰めは**面分割**の受け皿に入れたもので、蓋の材料には
効いていません。切り詰めれば `9.433981` に着き、上の交点と同じ点になります
——**そこが手がかりです**。

#### 2つめ: **切り手自身の稜が材料に入っていません**

z = 25 の蓋の本当の輪郭は、こうなるはずです。

```text
円柱の弧 → 穴の壁の直線 → **箱の x = -5 の縁** → 穴の壁の直線 → 円柱の弧 …
```

`(-5, 8.660254, 25)` から `(-5, 3.316625, 25)` へ渡る辺は、**箱の底面と
箱の側面が作る、箱自身の稜**です。交線ではないので、いまの材料には入って
いません。**交線だけでは、この蓋は閉じません。**

#### だから、順に2つ要ります

1. **蓋の材料の imprint。** 同一平面上の稜どうしなので、**2D の問題**です
   （空間の一般の曲線どうしより、ずっと素直です）。交点で互いを刻んで
   頂点を作る。
2. **切り手自身の稜を材料に足す。** 蓋の平面の上に乗っている、相手の立体の
   稜のうち、**選ばれた領域の側にあるもの**を拾う。

**1 だけでは足りません。** 1 をやっても `(-5, 8.660254, 25)` と
`(-5, 3.316625, 25)` の間が空いたままなので、輪になりません。

#### 門にはしていません

`rechained_boolean_probe` は断りを赤にしない決まりのままです（4-213）。
いまも **0/3 のまま**で、緑です。

**→ どちらも要りませんでした（4-219）。** 蓋がそもそも不要で、切り手の平面が
割れれば片が蓋になります。**この節の見立ては外れです。** 以下は当時の記録
として残します。

**次の人へ**: 上の 2 のほうが先だと思います。**材料が足りないまま
imprint を書いても、輪にはなりません**。まず「蓋の平面に乗っている相手の
稜」を拾って材料に入れ、そのうえで交点で刻んでください。`ZENITH_CAP_TRACE=1`
が材料をそのまま出すので、**足りているかどうかは1行で分かります**。


### 4-219. 4-213 の壁が抜けました（2026年9月1日）

**`(円柱−円柱) を箱で切る` が通るようになりました。**

| | 4-213 | いま |
| :--- | :--- | :--- |
| `(円柱−円柱) を箱で切る` | **0/3 断り** | **3/3、恒等式 1.688e-9、非多様体 0** |
| 2段目の断り（5連鎖） | 6 | **3** |

#### 4-218 の見立ては、また外れでした

4-218 では「蓋の材料が足りない」「imprint が要る」と書きました。**どちらも
要りませんでした。** 蓋は**そもそも要らなかった**のです——切り手の平面が
ちゃんと割れれば、その片が蓋の役をします。

#### 本当の原因は、**早すぎる打ち切り**でした

箱の底面（z = 25）に届く交線は6本です。

```text
(10.0000 0.0000 25) -> ( 0.0000 10.0000 25)   円柱の弧
( 0.0000 10.0000 25) -> (-5.0000  8.6603 25)
(-5.0000 -8.6603 25) -> ( 0.0000 -10.0000 25)
( 0.0000 -10.0000 25) -> (10.0000  0.0000 25)  円柱の弧
(-5.0000  3.3167 25) -> (10.0000  3.3167 25)   穴の壁の切り口
(-5.0000 -3.3167 25) -> (10.0000 -3.3167 25)   穴の壁の切り口
```

上4本は鎖になり、**残差 2.526e-16 で綺麗に割れます**。ところがそこで
`applied > 0` になり、**関数はそのまま返っていました**。下2本（1本ずつの鎖）
は**一度も試されません**。

しかも下2本は、境界を **0.536 追い越して**います（`x = 10` まで伸びているが、
円柱の弧に当たるのは `x = 9.434`）。試しても、そのままでは当たりません。

**2つが重なっていました。**

1. 当たった鎖があると、**残りを試さずに返す**
2. 残りは**境界を追い越している**ので、そのままでは当たらない

#### 直したこと

**当たった鎖を記録しておき、当たらなかった鎖を 4-217 の切り詰めにかけてから
当て直します。** 鎖の長さで絞りません——輪を閉じるには2本以上が要りますが、
**切り詰めたあとは1本でも境界から境界へ渡れます**。

同じ穴が「一部だけ当たったとき」の受け皿にもあったので、そちらにも足しました。

#### 落ちたものは1つもありません

| | 前 | 後 |
| :--- | :--- | :--- |
| 通しテスト | 137 バイナリ / 692 件 / 失敗 0 / 警告 0 | **同じ** |
| `contact_placement_probe` | 141演算で 135 ok / 6 断り、恒等式 41配置で破れ 0（最悪 3.900e-8） | **同じ** |
| `scale_sweep_probe` | 破れ 0、非多様体 0、断り 3（最悪 8.912e-6） | **同じ** |
| `face_budget_probe`（H5） | 最悪 3,854 枚 | **同じ** |
| `intersection_fit_probe` | 101本すべて 5e-7 以下 | **同じ** |
| **`rechained_boolean_probe`** | **2段目の断り 6** | **3** |

#### 教訓

**「当たった」で止めない。** この関数は5つの経路を順に試しますが、どれも
**1つでも当たったらそこで返す**建て付けでした。**面を割るのは「1本当たれば
終わり」ではありません**——届いている交線は全部当てないと、殻が閉じません。

4-213 から 4-219 まで、**推測を4回外しました**（当てはめ誤差 → `split_at`
→ 領域の取り方 → 蓋の材料）。当たったのはどれも**測ったあと**です。
**推測を書くのはよいのですが、次に読む人が最初にやることは、その推測を測って
潰すことだと分かるように書いてください。**

#### 残っているもの

`(トーラス−円柱) を箱で切る` は **0/3 のまま**です。**門にはしていません**
（断りは赤にしない決まり。4-213）。次はそこです。


### 4-220. 蓋の材料の重複除去。「自分の出力を入力に戻す」軸が緑になりました（2026年9月1日）

> **番号について。** この節は**現在地点から「4-221」としても参照されています**
> （「4-221 で 0 件」「4-221 で枯れた」「4-217〜4-221」）。**4-221 という
> 見出しはありません。** 中身はこの 4-220 です。番号を振り直すと既存の参照が
> 全部ずれるので、**ここに注記だけ置きます**。

**`rechained_boolean_probe` の2段目の断りが 6 → 0 になりました。**

| 連鎖 | 4-213 | いま |
| :--- | :--- | :--- |
| （円柱 − 円柱）を箱で切る | **0/3 断り** | **3/3、恒等式 1.688e-9** |
| （球 − 円柱）を球で切る | 3/3 | 3/3、2.334e-10 |
| （トーラス − 円柱）を箱で切る | **0/3 断り** | **3/3、恒等式 1.178e-13** |
| （円錐 + 球）を円柱で切る | 3/3 | 3/3、4.052e-13 |
| （箱 − 円柱）をトーラスで切る | 3/3 | 3/3、4.245e-14 |

**15演算すべてが立体を返し、恒等式が閉じ、非多様体 0** です。

#### トーラスのほうは、4-219 とは別の型でした

`(トーラス−円柱) を箱で切る` は、面が**1枚も割れていません**でした
（`0 batch-split faces`）。ところがこれは正しいのです——箱の面 `y = 0` は
トーラスの**継ぎ目**とちょうど重なっており、割る必要がありません。

失敗していたのは**蓋**です。`4 cap loops, 0 cap faces`。

#### 輪が折り返していました

`ZENITH_CAP_TRACE=1` に「輪が平面と読めない理由」を出す口を足して、輪の点を
そのまま並べました。

```text
 0 (16.0000 0.0000 0.0000)
 8 (12.0000 0.0000 4.0000)
16 ( 9.0000 0.0000 2.6458)   ← ここで折り返す
17 ( 9.3126 0.0000 2.9627)
24 (12.0000 0.0000 4.0000)
31 (15.9332 0.0000 0.7279)
```

**同じ2本を往復しているだけ**でした。囲む面積が 0 なので、Newell 法の法線が
取れず、「平面と読めない」で落ちていたのです。

原因は **4-80 と同じ**——**同じ弧が別々の `Edge` の実体で2本来ます**。
分割の側は `deduplicate_split_edges` を通していますが、**蓋の側は通して
いませんでした**。

#### 直したこと

蓋の材料にも、同じ重複除去を通しました。1行です。

| | 前 | 後 |
| :--- | ---: | ---: |
| 蓋の材料 | 24 本 | **10 本** |
| 輪 | 4 本（すべて折り返し） | **2 本** |
| 蓋の面 | **0 枚** | **2 枚** |

#### 落ちたものは1つもありません

| | 前 | 後 |
| :--- | :--- | :--- |
| 通しテスト | 137 バイナリ / 692 件 / 失敗 0 / 警告 0 | **同じ** |
| `contact_placement_probe` | 141演算で 135 ok / 6 断り、B-Rep・メッシュとも非多様体 0、恒等式 41配置で破れ 0（最悪 3.900e-8） | **同じ** |
| `scale_sweep_probe` | 破れ 0、非多様体 0、断り 3（最悪 8.912e-6） | **同じ** |
| `face_budget_probe`（H5） | 最悪 3,854 枚 | **同じ** |
| **`rechained_boolean_probe`** | **2段目の断り 6** | **0** |

#### 診断を2つ足しました

「蓋 0 枚」の内訳が、これまで1段も見えませんでした。`ZENITH_CAP_TRACE=1` に
3か所足してあります。

1. 輪は見つかったが**閉じたワイヤにならない**
2. 輪は閉じたが**平面と読めない**（点をそのまま並べます。折り返しはここで見えます）
3. 面は作れたが**p-curve の検算が通らない**

**「0 枚」という数だけでは、どこで落ちたか分かりません。**

#### 軸について

| 軸 | 足した数 | 出た欠陥 | いま |
| :--- | ---: | ---: | :--- |
| 置き方（4-205、4-207、4-211） | 12配置 | 7 | 枯れた |
| 大きさの桁（4-212） | 5配置 × 7桁 | 2 | 1件が未修理（0.01 の跳び） |
| **自分の出力を入力に戻す（4-213〜4-220）** | **5連鎖** | **2** | **どちらも修理済み。緑** |

**この軸は、いま 0 件です。** 4-211 のときと同じで、**枯れたのは軸であって
カーネルではありません**。

#### 厚くしてみました（5 → 10 連鎖）

「1回ぶんの標本で決めない」ので、**その場で厚くして測りました**。

| 足した連鎖 | 結果 |
| :--- | :--- |
| **3段**（円柱−円柱 → 箱 → 箱） | 3/3、2.095e-10 |
| **3段**（球−円柱 → 球 → 円柱） | 3/3、8.419e-9 |
| **桁混在**（400 の板に半径 2 の穴 → 箱） | 3/3、1.206e-13 |
| トーラス2つ → 円柱 | 3/3、**1.906e-7** |
| 円錐−円柱 → 球 | 3/3、1.137e-10 |

**10連鎖・30演算すべてが緑**、断り 0、非多様体 0。残差の最悪は
**1.906e-7**（トーラス2つを円柱で切る）で、門の 1e-6 の内側です。

**3段でも桁を混ぜても出ませんでした。** この軸は本当に枯れています。
次は**別の軸**を探してください（4-212 の「大きさの桁」に残っている
0.01 の跳びは、まだ未修理です）。


### 4-222. 小さい桁で崩れているのは、**積だけ**でした（2026年9月1日）

4-212 の未修理（`torus × cylinder` の恒等式が 0.02 と 0.01 の間で6桁跳ぶ）を
絞りました。

#### まず、測り方を疑いました——**無罪でした**

体積は三角形で積んでいるので、小さい模型では相対誤差が上がるはずだ、と
考えました。溶接（1e-7）も細分（8e-7）も**絶対値**だからです。

素のトーラスと素の円柱を、閉じた式と突き合わせました。

| 桁 | トーラスの相対の差 | 円柱の相対の差 |
| ---: | ---: | ---: |
| 1 | 4.248e-14 | 6.523e-14 |
| 0.1 | 3.293e-14 | 1.169e-14 |
| 0.01 | 6.442e-14 | 1.755e-14 |
| 0.005 | 6.442e-14 | 1.755e-14 |

**全桁で機械精度**です。解析の面積分に乗るので、三角形の細かさに依りません。
**測り方は無罪**で、崩れているのはブーリアンの出力そのものです。

#### 相似な形なので、**s³ 倍が正解**です

桁を変えても形は相似なので、`scale = 1` の答えの `s³` 倍が厳密な基準に
なります。**閉じた式が要りません。**

| 桁 | **差**のずれ | **積**のずれ | **和**のずれ |
| ---: | ---: | ---: | ---: |
| 0.1 | 1.570e-8 | 2.880e-7 | 4.096e-9 |
| 0.05 | 3.019e-8 | 5.540e-7 | 7.879e-9 |
| 0.02 | 6.038e-8 | 1.108e-6 | 1.576e-8 |
| 0.015 | 8.862e-9 | 2.727e-6 | 2.313e-9 |
| 0.012 | 2.020e-8 | **3.708e-7** | 5.272e-9 |
| **0.01** | 3.919e-8 | **2.574e-5** | 1.023e-8 |
| 0.008 | 1.195e-7 | 2.426e-5 | 3.118e-8 |
| 0.005 | 6.902e-8 | **1.737e-4** | 2.066e-8 |

**差と和は、どの桁でも 1e-7 以下**です。**崩れているのは積だけ**。

#### 滑らかでもありません

0.015 で 2.727e-6 まで上がったあと、**0.012 で 3.708e-7 に戻り**、0.01 で
2.574e-5 に跳びます。**単調ではない**ので、「公差の床にじわじわ近づいて
いる」のとは違います。**桁によって、出てくる形そのものが変わっている**と
見るのが自然です。

面の数はどの桁でも **20 枚**で同じでした。**枚数では違いが出ません。**

#### 次の人へ

**積だけを見てください。** 差と和が全桁で綺麗なので、**交線も分割も
選択も、大筋では効いています**。積のときだけ違うのは、

- **選ぶ片が違う**（`select_operand_faces_after_batch_split` の内外判定）
- **蓋の向き**（積では両方の立体の内側を採るので、向きが差と逆）

のどちらかの周りです。**0.012（良い）と 0.01（悪い）で、選ばれた面片を
並べて比べる**のが入口です——面の枚数は同じ 20 枚なので、**どの片が
選ばれたか**を見る必要があります。

**門は 0.1 までのままです**（4-212）。直っていないものを赤で常設すると、
赤に慣れてしまいます。


### 4-223. 4-212 の欠陥に名前が付きました——**交線の絶対公差**（2026年9月1日）

4-222 で「崩れているのは積だけ」まで絞りました。もう一段降りて、機構が
分かりました。

#### 選び方は正しい。位相も同じ

積の結果を、桁ごとに並べて比べました（大きさで正規化しています）。

| 桁 | 立体 | 面 | 重心の例 | 面積/s² |
| ---: | ---: | ---: | :--- | ---: |
| 1 | 1 | 12 | (4.5238, −5.0550, 1.4180) | 37.869133 |
| 0.012 | 1 | 12 | (4.5238, −5.0550, 1.4180) | 37.869140 |
| 0.01 | 1 | 12 | (**4.5237**, −5.0550, 1.4180) | 37.869118 |

**選ばれた片も、面の枚数も、並びも同じ**です。**内外判定は無罪**でした。
ところが**重心が 4.5238 → 4.5237**——相対 2e-5 で、体積のずれ 2.574e-5 と
同じ桁です。

#### 三角形の細かさも無罪

刻みを 16 / 32 / 64 / 128 と上げて測りました。

| 桁 | 16 | 32 | 64 | 128 |
| ---: | ---: | ---: | ---: | ---: |
| 0.01 | 2.574e-5 | 2.574e-5 | 2.574e-5 | 2.575e-5 |
| 0.005 | 1.737e-4 | 1.737e-4 | 1.737e-4 | 1.737e-4 |

**まったく動きません。** 回転面は解析で積むので、刻みに依りません。

#### 交線そのものが、**桁に依らない絶対の誤差**を持っています

交線を 72 点で標本し、大きさで正規化して、`scale = 1` の形からの
**最近傍距離**（順序に依らない測り方）を取りました。

| 桁 | 正規化したずれ | **絶対のずれ** |
| ---: | ---: | ---: |
| 0.1 | 6.504e-5 | **6.504e-6** |
| 0.02 | 1.471e-4 | **2.941e-6** |
| 0.012 | 4.250e-4 | **5.100e-6** |
| 0.01 | 1.584e-4 | **1.584e-6** |
| 0.005 | 3.086e-4 | **1.543e-6** |

**絶対のずれは、模型が 20 倍小さくなっても 1.5e-6〜6.5e-6 のまま**でした。

#### **この測り方は当てになりません**（後で気づきました。訂正します）

両方を「各稜の `k/8` の位置」で標本して最近傍距離を取っていますが、
**媒介変数の取り方が桁で違えば、同じ割合は同じ場所を指しません**。最近傍
距離は、**幾何のずれと、標本位置のずれの両方**を拾います。

実際、4-224 で床を外したあとに測り直すと、**正規化したずれは 0.012 以下で
1.5e-4 前後にほぼ一定**でした。ところが同じ桁の恒等式の残差は 1.4e-6 です
——**100 倍違います**。**曲線が本当に 1.5e-4 ずれているなら、体積はもっと
崩れるはず**なので、**この数字は誤差ではなく、標本の取り方の差を見ています**。

**次の人へ: この節の「1.5e-6〜6.5e-6」を信じないでください。** 曲線どうしを
比べるなら、**弧長で標本する**か、**片方の点からもう片方の曲線への最短距離**
（`ExtremumEngine::point_to_curve`）で測ってください。`intersection_fit_probe`
が支持曲面まで測るのに使っているやり方が、そのまま使えます。

**分かっているのは、「積だけが崩れる」「選び方も位相も三角形の細かさも
無罪」（4-222）までです。**

**積がいちばん先に見えるのは、3演算のうち体積がいちばん小さいから**
だろう、とは言えます（桁 1 で 195.9 に対し、差 3594・和 13773）。同じ
絶対誤差なら、積では 18 倍大きく見えます。**ただしこれは推測です**——
上の測り方が当てにならないので、「同じ絶対誤差」であること自体を
まだ測れていません。

**単調でないこと**（0.012 が 0.01 より良い）も、**まだ説明が付いていません**。
「公差の帯の中で止まる場所が成り行きだから」と書きたくなりますが、
**それも上の数字に寄りかかった推測**です。

#### 直していません

**`tol.linear` を模型の大きさに比例させる**のが筋ですが、**カーネル全体の
公差**なので、ここを触ると全部が動きます。4-212 で `Face::pcurves` の許容を
比例させたのとは規模が違います。

#### 2回試して、**どちらも空振りでした**（入れていません）

いちばん近そうな2か所を、**小さい模型のときだけ厳しく**してみました
（どちらも `min` を取る形で、緩める方向には効きません）。

| 触ったところ | 0.01 | 0.005 |
| :--- | ---: | ---: |
| 手を入れる前 | 1.368e-6 | 8.912e-6 |
| **当てはめの許容**（`deviation_limit = tol.linear`） | 1.368e-6 | 8.932e-6 |
| **辿る段の収束判定**（`fit_all_branches` に渡す `Tolerance::linear`。`ssi_march` の `gap.norm() <= tol.linear`） | **1.368e-6** | **8.912e-6** |

**2つめは1桁も動きません**——手を入れる前と**同じ数字**です。

`torus × cylinder` に解析の交線経路はありません（`analytic_ssi` にあるのは
平面×平面／平面×球／平面×円柱／球×円柱の同軸だけ）。それでも辿る段の
公差が効かないので、**この2つは犯人ではありません**。

**戻しました。** 効かない変更を入れると、次の人が「ここは見た」と思って
しまいます。

#### 次の人は、まず**経路を確かめて**ください

**→ 矛盾は解けました（4-224）。(b) でした**——`surface_patch_extent` に
**1.0 の絶対の床**があり、桁 0.005 のパッチが「大きさ 1.0」と報告されて
いました。だから `extent * 1e-7` は 1e-7 にしかならず、効かなかったのです。
以下は当時の記録です。

**矛盾が1つ残っています。それがいちばん強い手がかりです。**
`fit_all_branches` は **`deviation <= deviation_limit` のときだけ枝を採り**ます
（`ssi_march.rs`）。だから許容を 100 分の1にすれば、返る曲線は 100 倍良く
なるはずです。**ところが数字が変わりませんでした。** 辻褄が合いません——
**(a) この面の組の交線は、そこから来ていない**か、**(b) `extent` が思って
いる値ではない**か、**(c) 断られて別の経路に落ちている**か、のどれかです。
**そこを先に潰してください。**

**「絶対公差のどこか」までしか分かっていません。** 私は場所を2回外しました。
**先に `ZENITH_SSI_WHY=1` で、この面の組の交線が本当に辿る段から出ているかを
確かめてください。** 公差をいじる前に、そこです。

そのあとの候補は、種の探索（`find_seeds` の格子）、歩き幅
（`first_step` の下限が `tol.linear * 100.0` と絶対値。ただし差し渡し 0.12 の
模型でも `extent * 0.1 = 0.012` のほうが大きいので、**効いていないはず**）、
稜の端点の作り方、`Regularizer::hold_like_our_own` の丸め、です。

#### 次の人へ、順に

1. **どこまで比例させるかを決める。** 立体1つの中でも、大きい面と小さい面が
   あります。「模型の大きさ」を何で測るか（立体の境界箱？ 面の境界箱？）を
   先に決めてください。**4-216 の天井のように、噛み合う相手がいます**——
   比例させるなら、`on_surface_limit` も `SNAP_CEILING` も一緒に動かす
   必要があります。
2. **決めたら `scale_sweep_probe` の門を 0.02 以下まで広げる。** いまは
   0.1 までです。広げられたら、それがそのまま証拠になります。

**門は 0.1 までのままにしてあります。** 直っていないものを赤で常設すると、
赤に慣れてしまいます。**名前は付きました**——「交線の絶対公差」です。


### 4-224. パッチの大きさに **1.0 の床**が置いてありました（2026年9月1日）

4-223 で公差を2回いじって、2回とも数字が動きませんでした。**理由が分かり
ました。私が掛けていた「パッチの大きさ」が、嘘だったからです。**

#### 見つけ方

`ZENITH_SSI_WHY=1` に、**許容とその元になった大きさ**も出すようにしました。
「何本辿れたか」だけでは、公差をいじっても動かない理由が分かりません。

```text
桁 1     : パッチの大きさ 4.197618e1、許容 1.000e-6（相対 2.382e-8）、歩幅 4.198e0
桁 0.005 : パッチの大きさ 1.000000e0、許容 1.000e-6（相対 1.000e-6）、歩幅 1.000e-1
```

**桁 0.005 で「大きさ 1.0」** です。実際の差し渡しは 0.21 前後——**5倍近く
大きく報告されています**。

```rust
fn surface_patch_extent(surface: &NurbsSurface3) -> f64 {
    ...
    worst.max(1.0)   // ← ここ
}
```

**絶対の床でした。** 床の意図は「潰れたパッチで 0 を返さない」ことですが、
**絶対値で置くと、小さい模型で嘘になります**。

#### 何が起きていたか

`first_step = (extent * 0.1).max(tol.linear * 100.0)` なので、

| 桁 | 本当の大きさ | 報告された大きさ | 最初の歩幅 |
| ---: | ---: | ---: | ---: |
| 1 | 41.98 | 41.98 | 4.198 |
| 0.005 | 約 0.21 | **1.0** | **0.1**（**模型の半分**） |

**模型の半分の歩幅で交線を辿り始めていました。**

4-223 で私が入れた `deviation_limit = tol.linear.min(extent * 1e-7)` も、
**実際には 0.21 ではなく 1.0 を掛けていた**ので、1e-7 にしかならず、
ほとんど効きませんでした。**空振りの原因もここです。**

#### 直したこと

床の意図（潰れているかどうか）だけを残し、**大きさは測ったとおり**返します。

```rust
if worst > 0.0 { worst } else { 1.0 }
```

#### 小さい桁の断りが消えました

| `scale_sweep_probe` | 前 | 後 |
| :--- | ---: | ---: |
| **断り** | **3** | **0** |
| 恒等式の破れ（門の内） | 0 | 0 |
| 0.01 の残差 | 1.368e-6 | 1.367e-6 |
| 0.005 の残差 | 8.912e-6 | 9.038e-6 |

**35通り（5配置 × 7桁）すべてが 3演算とも立体を返します。** 4-212 で
「`cone × sphere` は 0.005 で断られます」と書いた件は、これで解消です。

#### 恒等式の破れは**残っています**

0.01 と 0.005 の残差はほとんど動きません。**歩幅ではありませんでした。**
門は 0.1 までのままです。

**次の人へ**: 断りが消えたので、**残っているのは「返ってきたのに答えが
合わない」だけ**になりました。4-223 の測り方（相似な形なので `s³` 倍が
基準）がそのまま使えます。**交線の絶対のずれ 1.5e-6〜6.5e-6 がどこで
入るのか**を、こんどは**大きさが正しく報告される状態で**追ってください。

#### 落ちたものは1つもありません

| | 前 | 後 |
| :--- | :--- | :--- |
| 通しテスト | 137 バイナリ / 692 件 / 失敗 0 / 警告 0 | **同じ** |
| `contact_placement_probe` | 141演算で 135 ok / 6 断り、非多様体 0、恒等式 41配置で破れ 0（最悪 3.900e-8） | **同じ** |
| `rechained_boolean_probe` | 10連鎖・30演算すべて緑（最悪 1.906e-7） | **同じ** |
| `face_budget_probe`（H5） | 最悪 3,854 枚 | **同じ** |

#### 教訓

**「最低これくらい」を絶対値で書くと、小さい模型で嘘になります。**
4-212（p-curve の許容）、4-216（吸着の天井）に続いて3件目です。
**下限も上限も、大きさに関わる量は、何を基準にした値なのかを書いてください。**


### 4-225. 測り直したら、**恒等式は交線の精度に鈍い**と分かりました（2026年9月1日）

4-223 の測り方を退けたので、**正しい物差し**で測り直しました。点から曲線への
最短距離（`ExtremumEngine::point_to_curve`）です。媒介変数の取り方に依りません。

#### 交線のずれ（`torus × cylinder`。桁 1 の形と比べます）

| 桁 | 正規化した最大ずれ | 絶対の最大ずれ |
| ---: | ---: | ---: |
| 1 | 3.560e-15 | 3.560e-15 |
| 0.1 | **4.501e-6** | 4.501e-7 |
| 0.02 | 1.966e-5 | 3.932e-7 |
| 0.012 | 1.662e-5 | 1.994e-7 |
| 0.01 | **2.140e-5** | 2.140e-7 |
| 0.005 | 2.140e-5 | **1.070e-7** |

（桁 1 は自分自身と比べているので、物差しが正しいことの確認です。）

**絶対でも相対でもありませんでした。** 模型が 20 倍小さくなると、
**相対のずれは 4.5 倍に増え、絶対のずれは 4 分の1に減ります**。4-223 で
「絶対公差のどこかで止まっている」と書いたのは**外れ**です。

#### そして、**恒等式はこのずれに鈍い**

ここがいちばん大事な発見です。

| 桁 | 交線の相対のずれ | 恒等式の残差 |
| ---: | ---: | ---: |
| 0.1 | **4.501e-6** | **2.375e-13** |
| 0.01 | 2.140e-5 | 1.367e-6 |

**桁 0.1 では、交線が相対 4.5e-6 ずれているのに、恒等式は 2.4e-13 で
閉じています。** 7桁の開きです。

**当たり前でした。** 恒等式 `V(A−B) + V(A∩B) = V(A)` は、**両方の演算が
同じ交線を使う**ので、交線がどこにあろうと**誤差は打ち消し合います**。

**つまり、恒等式が破れるのは「交線が不正確だから」ではありません。**
破れるのは、**差と積で違うものを使ったとき**だけです。

#### だから、次に見るところが変わります

**探すのは「差と積で食い違うもの」です。** 候補:

- **片の選び方**（`select_operand_faces_after_batch_split` の内外判定）が、
  差と積で別の答えを出している
- **蓋**が、差と積で別の形になっている
- どちらかの演算だけが**別の経路**（受け皿）に落ちている

4-222 で「選ばれた片も面の枚数も並びも同じ」と測りましたが、**あれは積だけ**
を見ています。**差と積を並べて、同じ面片の同じ部分を使っているかを見て
ください。**

**これまで4-212 から 4-224 まで、ずっと「交線の精度」を疑ってきました。
その筋は、いまの測定で消えました。**

#### 差と積を並べました——**積だけが崩れています**

同じ組を、桁 1 と 0.01 で、**それぞれの演算の体積そのもの**（`s³` で
正規化）で比べました。

| 演算 | 桁 1 | 桁 0.01 | 相対のずれ | 面 |
| :--- | ---: | ---: | ---: | ---: |
| 差 | 3594.074360863 | 3594.074177871 | **5.09e-8** | 20 → 20 |
| **積** | 195.853729156 | 195.848729901 | **2.55e-5** | 12 → 12 |

**面の枚数は同じ**です。差は 5e-8 で合っているのに、積だけが 500 倍崩れて
います。**恒等式の残差 1.367e-6 は、この2つを足したものとぴったり合います**
（(3594×5.09e-8 + 195.85×2.55e-5) ÷ 3789.9 = 1.37e-6）。

#### 担いでいるのは、**トーラス側の面**でした

積の 12 枚を、桁 1 と 0.01 で面積どうし並べました。

| 面 | 面積/s² | 相対のずれ |
| :--- | ---: | ---: |
| **円柱側 4 枚**（74.806856） | 変わらず | **0.000e0**（全桁一致） |
| **トーラス側 8 枚**（37.869133） | 37.869117〜129 | **1.1e-7〜4.2e-7** |

**円柱側は1桁も動きません。トーラス側だけがずれます。**

ただし**面積のずれ（4e-7）より、体積のずれ（2.55e-5）のほうが 64 倍大きい**
ので、**面積ではなく、面がどこにあるか**（形と位置）が効いています。体積は
発散定理で `x·n` を積むので、位置に効きます。

**差も同じトーラスの面を使っていて、そちらは 5e-8 です。** 違うのは
**トリムの仕方**——積のほうは、レンズ状の小さい片です。

#### トリム境界を、パラメータ空間で並べました

積のトーラス片（稜4本）の外周 p-curve を 16 点で標本し、桁 1 と 0.01 で
比べました。**パラメータ空間なので、桁で正規化する必要がありません。**

| 面 | UV の最大ずれ |
| ---: | ---: |
| 0〜2 | 8.331e-6 |
| 4〜6 | 9.191e-6 |
| 8〜11 | 8.397e-6 |
| 3、7 | 1.090（**対応が取れていません**——並べ方の都合で、桁ごとに違う面と
突き合わせています。**欠陥ではありません**） |

**取れた 10 枚は、どれも 8.3e-6〜9.2e-6** です。UV は 0〜1 に正規化されて
いるので、これは**パッチに対する相対のずれ**です。

#### ここまでで言えること／言えないこと

**言えること**（測った）:

- 崩れているのは**積だけ**。差は 5.09e-8（4-222、上）
- 積の**円柱片は全桁一致**、**トーラス片だけ**が 1.1e-7〜4.2e-7（面積）
- そのトーラス片の**トリム境界が、パラメータ空間で 8〜9e-6 ずれている**

**言えないこと**（この時点では推測でした。**下で測りました**）:

- **差も同じ交線を使っています。** それでも差が 5e-8 で済むのは、
  **差の面が大きく、同じ境界のずれが体積に効きにくいから**——という筋。

#### 測りました。**1回で決まりました**

差の曲面片についても、同じ比較をしました。

| 演算 | 曲面片 | 対応の取れた枚数 | **UV の最大ずれ** |
| :--- | ---: | ---: | ---: |
| 差 | 20 枚 | 20 | **9.191e-6** |
| 積 | 12 枚 | 10 | **9.191e-6** |

**まったく同じ値**です。**トリムのずれは、差と積で違いません。**

#### これで筋が通りました

| | |
| :--- | :--- |
| トリム境界のずれ | 桁 0.01 で **9.2e-6**（UV。パッチに対する相対）。**差でも積でも同じ** |
| 差への効き方 | 体積 3594 に対し **5.09e-8** |
| 積への効き方 | 体積 **195.85** に対し **2.55e-5** |

**同じずれが、体積の小さい積では 500 倍大きく効きます。** 積は円柱と
トーラスの管が重なるレンズ状の小さい立体で、**表面積に対して体積が小さい**
——境界が少し動くと、体積の割合が大きく変わります。

**4-225 の冒頭で「恒等式は交線の精度に鈍い」と書いたのは、言い過ぎでした。**
正しくは、**「同じずれでも、演算によって効き方が 500 倍違う」**です。
打ち消し合うのは、**両方が同じ大きさのときだけ**でした。

#### だから、直すところは1つです

**トリム境界の精度を、模型の大きさに対して一定にすること。** いまは桁が
小さくなると相対の精度が落ちます（桁 0.1 で 4.5e-6、0.01 で 2.1e-5。4-225 の
交線の測定）。

**4-223 で「交線の絶対公差」と書いた筋は、正しい物差しで測り直しても
生きています。** 外れていたのは**数字と、直す場所の当て方**でした
（4-224 のパッチの大きさの床が、公差をいじっても効かない理由でした）。

#### 3度目も空振りでした（**当てはめの許容ではありません**）

4-224 で床を外したので、こんどは `extent` が正しく報告されます。その状態で
`deviation_limit` を**パッチの大きさに比例**させて測りました
（`tol.linear.min(extent * 2.4e-8)`。桁 1 で 1e-6 と同じになる係数）。

| | 0.01 | 0.005 | 断り |
| :--- | ---: | ---: | ---: |
| 手を入れる前 | 1.367e-6 | 9.038e-6 | **0** |
| 比例させたあと | **1.368e-6** | 8.954e-6 | **3**（**戻った**） |

**恒等式は動かず、断りだけが戻りました。** 直っていないどころか、悪く
なっています。**戻しました。**

**`deviation_limit` は犯人ではありません**——床を外して正しい `extent` を
掛けても、です。これで**3回**外しました（許容 → 辿る段の収束判定 → 正しい
`extent` を掛けた許容）。

#### 次の人へ

**トリム境界の 9.2e-6 が、どの段で入るのかを直接測ってください。**
交線そのもの（`edge_candidates` の曲線）は 4-225 で測れています
（桁 0.01 で相対 2.1e-5）。**次に見るのは、その曲線から p-curve を作る段**
です——`project_edge_to_nurbs_pcurve` は、

- `on_surface_limit = tol.linear.max(1e-6) * 10.0`（**絶対**）
- 標本の置き方が「弦が稜からどれだけ外れるか」（**絶対**の判定）

と、**絶対の量が2つ**あります。**公差をいじる前に、まず
「交線の 2.1e-5 が、p-curve では 9.2e-6 になっている」という対応が
本当かを確かめてください。** 私は3回、確かめずに公差をいじって外しました。

#### 門は動かしていません

`scale_sweep_probe` は 0.1 までが門のままです。0.02 以下は表示だけ。

---

### 4-226. 「未実装として断る」を赤ゲートに格上げしました（2026年9月1日、H7 達成）

**長らく文書の主張であって、ゲートではありませんでした。**

`contact_placement_probe` は断りを1つに数えていて、**「答えが本当に非多様体
だから断った」のか「実装が足りなくて断った」のか**を分けていませんでした。
前者は規約どおり（HANDOVER 3-1。`ok` に変わったら誤答です）、後者は欠陥です。

#### 分け方は、断り文がもともと持っていました

`BooleanEngine` は2種類を返します。

| 断り文 | 意味 | 扱い |
| :--- | :--- | :--- |
| `... refuses this placement: <座標>` | **答えが本当に非多様体**。どこで材料が割れるかを名指しする | 緑（規約どおり） |
| `... is not implemented yet; ...` | **実装が足りない** | **赤** |

数えて、後者が 1 件でもあれば `exit 1` にしました。**配置ごとに名前も出します**
（`GATE ERROR: <配置> <演算> は「未実装」として断られました`）。

#### 実測（47配置・141演算）

```text
135 件が立体を返し、6 件が断られました（うち**「未実装」として断ったもの 0 件**）。
**B-Rep が非多様体だったもの 0 件**、メッシュが非多様体だったもの 0 件。
**恒等式**: 41 配置で測り、破れ 0 件、残差の最悪 3.900e-8
```

**断りの 6 件は全部が「答えが本当に非多様体だから」**です。正しい断りが赤に
ならないことも、接線の配置1つで確かめました（`box × cylinder (tangent)` の
差は断られますが exit 0）。

#### なぜ「断りを 0 にする」を目標にしなかったか

**6 件は減らす対象ではありません。** 答えのほうが非多様体なので、`ok` に
変わったらそれは誤答です。**守るべきは「未実装で断らない」ほう**で、そこは
配置を足すたびに顔を出します——4-205 で 1 件出て、同じ日に直しました
（4-206）。**そのとき鳴る場所が無かった**ので、ここで作りました。

---

### 4-227. **4度目の空振り。p-curve を 100 倍細かく取っても、跳びは動きません**（2026年9月1日）

4-225 は「直すところは1つ——**トリム境界の精度を、模型の大きさに対して一定に
すること**」で終わっています。**そこを直接やって、外しました。**

#### やったこと

`project_edge_to_nurbs_pcurve` の細分は、弦が稜から `tol.linear`（**絶対
1e-6**）以上離れている区間を割ります（[`crates/zenith_topo/src/face.rs`](crates/zenith_topo/src/face.rs)）。
**絶対値なので、模型が 100 分の1になると相対では 100 倍粗くなります。**
向きは 4-225 の測定（桁 0.01 で UV に 8〜9e-6）と一致します。

そこで **稜の長さの 1e-7 倍**を床にしました（桁 1 の検体でちょうど 1e-6 に
なる係数——**いまの大きさでは何も変わりません**）。

#### 実測（`torus × cylinder`。この組だけが跳びます）

| 桁 | 前 | 後 |
| ---: | ---: | ---: |
| 0.1 | 2.375e-13 | **2.726e-10**（**悪化**） |
| 0.02 | 2.139e-12 | **5.240e-10**（**悪化**） |
| **0.01** | **1.367e-6** | **1.381e-6**（**動かず**） |
| 0.005 | 9.038e-6 | 8.516e-6 |

**値段**: この1配置だけで **20分29秒**。基準は5配置ぜんぶで約13分です。

#### 言えるようになったこと

**p-curve の細分は、0.01 の跳びの律速ではありません。** 100 倍細かく取っても
残差が動かないので、**トリム境界のずれは別のところで決まっています**。
候補は、4-225 が測った「UV で 8〜9e-6」を作っている段——**交線そのものの
当てはめ**か、**面を割るときの吸着**（`face_split` の `SNAP_CEILING`）です。

**桁 0.1 と 0.02 が悪化したのも所見です。** 細かく取ると良くなる、という
単純な話ではありません。標本を増やすと p-curve の節点が増え、**当てはめの
形そのものが変わります**。

#### この筋での空振りは4回目です

| | 手 | 結果 |
| :--- | :--- | :--- |
| 4-223 | 交線の当てはめ誤差を測る | **測り方が当てにならなかった**（訂正済み） |
| 4-224 | パッチの大きさの床を外す | 断りは 3 → 0。**恒等式は動かず** |
| 4-225 | `deviation_limit` をパッチの大きさに比例 | 恒等式は動かず、**断りが 3 に戻った**（戻した） |
| **4-227** | **p-curve の細分を稜の長さに比例** | **恒等式は動かず、良い桁が悪化。20分/配置**（戻した） |

**4つとも「公差を相対にする」型です。** 次に触る人は、**別の型**を試して
ください——たとえば「差と積で**違うもの**を使っていないか」（4-225 が最後に
示した筋）。**公差をいじる手は、ここまで4回とも空振りです。**

#### 残したもの

`scale_sweep_probe` に **`ZENITH_SCALE_FILTER`** を足しました。桁の小さい
ところは1回が重い（この日は 20 分）ので、**追っている組だけを回せる**ように
しておきます。

---

### 4-228. **掃き出しの4本目を開けたら、初回で2つ当たりました**（2026年9月1日、H8 着手）

9-H の H8——**読んだ立体（STEP）を切る**——に着手しました。常設の検体
（`tests/fixtures/occ_reference_*.step`）は**このカーネルが書いたものを
OCCT が読み直した**ものです。ここで読むのは **OCCT が配っている実物**
（`reference/OCCT/data/step/`）——誰かの設計データです。

**`read_and_cut_probe` を足しました。1回目で、切るところまで行けませんでした。**

#### 当たり1: `SEAM_CURVE` を読めていませんでした

```text
screw.step       **読めません**: Unsupported curve entity SEAM_CURVE (#961)
linkrods.step    **読めません**: Unsupported curve entity SEAM_CURVE (#131)
```

**2つとも、1本目の稜で止まっていました。** `SEAM_CURVE` は ISO 10303-42 で
`surface_curve` の subtype で、**属性の並びは同じ**です（違うのは「閉じた
曲面の継ぎ目に乗っていて、同じ曲面を2回指す」という制約だけ）。同じ扱いに
しました。

**閉じた曲面（円柱・球・トーラス）の継ぎ目にある稜はみなこれ**なので、実物の
データには普通に出ます。**自分が書いた STEP には出ません**——だから今まで
見えていませんでした。

#### 当たり2: **ファイルが申告している不確かさを読んでいませんでした**

`SEAM_CURVE` が通ると、次は検証で止まります。中身を見たら、2つは別の話でした。

| 検体 | ファイルの申告 | 外れの大きさ | 読み |
| :--- | ---: | ---: | :--- |
| `linkrods.step` | **2e-5** | 5〜9e-6 | **そのファイルの中では正しい**。こちらが 1e-6 で測って断っていた |
| `screw.step` | 1e-6 | **10.18 / 74〜86** | 申告どおりに測っても大きすぎる。**こちらの読み違い** |

読み込んだ立体は `Tolerance::default()`（1e-6）で検査していました。
**書いた側の精度を超える主張はできません。** 申告のほうが粗ければそちらに
合わせるようにしました（`UNCERTAINTY_MEASURE_WITH_UNIT` を読み、単位を掛け、
**天井 1e-3 mm** で頭打ち）。

**実測: `linkrods` の違反が 255 → 122 件。** 残る 122 件は **9.0e-5**——
**申告の 2e-5 を 4.5 倍超えています**ので、そこは読み違いです。

#### 途中で自分がやらかしたこと

**実体名を、引数の中から探していました。** `UNCERTAINTY_MEASURE_WITH_UNIT`
は `#18622 = UNCERTAINTY_MEASURE_WITH_UNIT(...)` と**実体名で**来るのに、
複合実体の中だけを見ていたので**1件も拾えず**、「直したのに数字が動かない」
と一度書きかけました。名前と中身の両方を見るようにしました。

#### 門への影響（測りました）

| | 前 | 後 |
| :--- | ---: | ---: |
| `foreign_cross_pair_probe` | 15組・破れ 0、最悪 9.429e-10 | **同じ** |
| テスト | 137 / 692 / 失敗 0 / 警告 0 | **同じ** |

**この変更は、自分が書いた STEP にも効きます**（こちらの書き手は 1e-5 を
申告するので、読み直しの検査が 1e-6 → 1e-5 になります）。**緩めたぶんで
何かを見落としていないか**を上の2つで測って、どちらも動きませんでした。

#### 当たり3: **稜の頂点で曲線を切り詰めていませんでした**

`screw` の 10.18 を、推測せずに名指ししました。`ZENITH_STEP_WHY=1` を足して、
**稜ごとに「曲線の端が頂点からどれだけ離れているか」**を出します。

```text
STEPWHY EDGE_CURVE #336 曲線 #339 SURFACE_CURVE same_sense=true 端のずれ 始 0.000000e0 終 1.018357e1
STEPWHY EDGE_CURVE #426 曲線 #429 SURFACE_CURVE same_sense=true 端のずれ 始 1.018357e1 終 0.000000e0
```

**2本だけでした。** 中の 3D 曲線は B スプライン（`#340`）で、**その稜は曲線を
途中までしか使いません**。`EDGE_CURVE` の頂点は「曲線のどこからどこまでを
使うか」を決めるのに、**切り詰めていませんでした**。

**自分が書いた STEP では、稜が曲線を丸ごと使います。** だからこれまで合って
いました。**実物のデータはそうではありません。**

頂点の位置で切り詰めるようにしました（`split_at` で前後を落とす。端のずれが
公差の中なら**何もしません**——丸ごと使う稜がほとんどです。頂点が曲線の上に
無い稜は、切り詰めても直らないので**そのまま下流の検査へ**渡します）。

**実測: 端のずれのある稜が 2 → 0 本、`screw` の違反が 229 → 225 件。**

#### まだ切れていません

**H8 の本題（切って恒等式を見る）には、まだ届いていません。** 残っているのは
**曲面側**です。

| 検体 | 残り | 中身 |
| :--- | ---: | :--- |
| `screw.step` | **225 件** | 境界の点が**面から 83 外れる**。曲線は直したので、**面の読み違い**（`Face 3`／`Face 4`） |
| `linkrods.step` | **122 件** | 境界の点が**面から 9.0e-5 外れる**。申告 2e-5 の **4.5 倍** |

#### 当たり4（未修理）: **`master_representation` を見ていませんでした**

`ZENITH_STEP_WHY=1` を面の側にも伸ばして、名指ししました。

```text
STEPWHY ADVANCED_FACE #709 曲面 #482 TOROIDAL_SURFACE 境界の点が曲面から最大 8.716487e1（27 / 27 点が外れ）
STEPWHY ADVANCED_FACE #944 曲面 #153 CONICAL_SURFACE  境界の点が曲面から最大 2.370054e1（117 / 117 点が外れ）
STEPWHY 曲面 #482 TOROIDAL_SURFACE は境界に合わせたパッチを作れず、決め打ちのパッチに落ちました
STEPWHY   トーラス（芯 8.250000、管 54.873719）の上に乗らない点: 芯からの距離 51.779581（差 3.094138e0、許す幅 5.487372e-2）
```

**境界の点が、そもそもその曲面の上に乗っていません**（0.38〜3.09 外れ）。
解析パッチを作れないので**決め打ちの 90度 x 90度パッチ**に落ち、そこに面が
乗っていないので全点が外れます。**公差の話ではありません。**

なぜ乗っていないのか。**`master_representation` を読んでいませんでした。**

```text
screw.step    .PCURVE_S2. が 22 件（.CURVE_3D. は 0 件）
linkrods.step .PCURVE_S2. が 108 件（.CURVE_3D. は 0 件）
```

`SURFACE_CURVE(name, curve_3d, associated_geometry, master_representation)` の
最後の欄は、**どれが正か**を言います。**2つのファイルとも、全部の稜で
「p-curve が正」**と宣言しています。**3D 曲線のほうは近似**で、ねじ山のような
曲線では大きく外れます。こちらは**常に 3D 曲線を使って**いました。

**自分の書き手は `.PCURVE_S1.` しか出しません。** だからこの欄を読まなくても
今まで困りませんでした。**実物のデータは違います。**

**やってみて、戻しました。** p-curve を正として読む経路を書きました——
ISO 10303-42 の式で曲面を評価する係（平面・円柱・円錐・球・トーラス）、
2D の `LINE` と `B_SPLINE_CURVE_WITH_KNOTS` を読む係、撒いて当てはめる係。
**実測: `screw.step` の違反が 225 → 271 件に増え、稜の端が 21.4 ずれる**
という新しい欠陥まで出ました。

#### なぜ外したか（次に書く人へ）

**2D の `LINE` は範囲を持ちません。** 実物はこうです。

```text
#966 = PCURVE('',#153,#967)
#967 = DEFINITIONAL_REPRESENTATION('',(#968),#972)
#968 = LINE('',#969,#970)          <- 2D の直線
#969 = CARTESIAN_POINT('',(6.28318530718,0.E+000))
#970 = VECTOR('',#971,1.)          <- 向き (0,1)、長さ 1
```

`t = 0〜1` で撒くと、**曲面の上を長さ 1 だけ進んだ線**になります。**その稜が
どこからどこまでかは、`EDGE_CURVE` の頂点が決めます**。撒く前に、**頂点に
対応する 2D のパラメータを求める**必要があります（3D の頂点を曲面のパラメータ
へ写し、2D 曲線の上でその点に最も近いパラメータを探す）。

**B スプラインのほうは範囲を持ちます**（節点ベクトル）ので、そちらだけなら
通ったかもしれません。**`screw.step` は 22 件中 12 件が `LINE`** です。

**手順を書き直します。**

| 順 | やること |
| ---: | :--- |
| 1 | **2D 曲線の「稜に当たる範囲」を先に決める**。頂点を曲面のパラメータへ写し、2D 曲線上で最も近いパラメータを両端ぶん求める |
| 2 | その範囲で撒いて、曲面の式で 3D に移し、通る曲線を当てはめる |
| 3 | 当てはめた曲線の端が頂点と合うことを**その場で確かめる**（合わなければ 3D 曲線へ戻す） |
| 4 | 読めたら `read_and_cut_probe` がそのまま切って恒等式まで測ります |

**曲面の式（ISO 10303-42）は、書いてみて確かめた範囲では次のとおりです。**
円錐は `半径 = R + v·tan(α)`、トーラスは `(R + r·cos v)` と `r·sin v`、球は
`R·cos v` と `R·sin v`。**これは残しておきます**——書き直すときの出発点に
なります。

#### 手順どおりに書き直して、**2度目も外しました**

上の4段のとおりに書き直しました（頂点を曲面のパラメータへ写し、2D 曲線の上で
当たるパラメータを求め、その範囲で撒く）。**進みましたが、足りません。**

| 回 | やったこと | 実測 |
| ---: | :--- | :--- |
| 1 | `t = 0〜1` で撒く | `screw` 225 → **271**（悪化）、稜の端が 21.4 ずれる |
| 2 | 頂点を曲面のパラメータへ写して範囲を決める | 端の**片方は 1.6e-11 で一致**。**もう片方が 17.5 ずれる** |
| 3 | 合わせるのを**3D の距離**にする（角度の巻き対策） | 端は合うようになったが、`screw` **225 のまま**、`linkrods` 122 → **236**（悪化） |

**2回目で分かったこと**: パラメータ空間で合わせると**角度の巻き**で外します
——`atan2` は `(-π, π]` を返しますが、p-curve は `u = 6.283`（2π）に置かれて
いることがあります。**3D の距離で合わせれば、どの巻きでも同じ点は同じ点です。**

#### **3回目で、もっと大事なことが分かりました**

**曲線を直しても `screw` は 225 のままです。** あの 225 件は
「境界の点が**曲面から** 83 外れる」で、**曲面の側の話**だからです。

`torus_patch_for_boundary` が境界の点を載せられずに `None` を返すと、
**決め打ちの 90度 x 90度パッチ**に落ちます。**面がそこに乗っていなければ、
曲線がどれだけ正しくても全点が外れます。**

**つまり H8 の残りは、曲線ではなく曲面です。**

**これは測って確かめました。** 3回目の形（p-curve を正として読み、3D の距離で
合わせる）を入れた状態で、断られた稜は **22 本中 1 本だけ**——つまりほとんどの
稜が p-curve から作られています。**それでもトーラスの外れは 0.377 / 3.094 の
まま、1桁も動きませんでした。** 曲線の出どころを変えても、あの面の境界点は
同じところに来ます。

| 順 | やること |
| ---: | :--- |
| 1 | **面の支持曲面を、STEP の解析曲面そのままで持つ**（いまは境界に合わせた NURBS パッチを作り、作れなければ決め打ちに落ちる）。9-3「内部表現に解析曲面を持つか」の判断が、ここに効きます |
| 2 | そのうえで p-curve を正として読む（**3D の距離で合わせる**。上の3回目の形） |
| 3 | 読めたら `read_and_cut_probe` がそのまま切って恒等式まで測ります |

**9-3 を「まだ決めていない」と置いたままにしてきましたが、H8 はそこに
ぶつかりました。** 実物の STEP を読むには、解析曲面を持つほうが要ります。

#### 次に触る人へ

**手順は上の「なぜ外したか」に書き直しました。**

#### 門への影響（もう一度測りました）

| | 前 | 後 |
| :--- | ---: | ---: |
| `foreign_cross_pair_probe` | 15組・破れ 0、最悪 9.429e-10 | **同じ** |
| テスト | 137 / 692 / 失敗 0 / 警告 0 | **同じ** |

---

### 4-229. **H6 の律速を、はじめて数字で押さえました**（2026年9月1日）

4-212 から 4-227 まで、`scale_sweep_probe` の「0.02 と 0.01 の間で 6桁跳ぶ」を
5回追って、**公差を相対にする手は4回とも空振り**でした（4-223、4-224、4-225、
4-227）。**測るところを変えました。**

#### 同じ形の桁を変えて、交線の当てはめだけを測る

`intersection_fit_probe` に、跳ぶ組（`torus × cylinder`）を **桁 1 / 0.1 /
0.01** で並べました。この口は**交線が支持曲面からどれだけ浮いているか**しか
見ないので、ブーリアンの他の段が混ざりません。

| 桁 | 絶対の誤差（worst A） | **模型に対する相対** |
| ---: | ---: | ---: |
| 1 | 3.226e-8 | 3.2e-8 |
| 0.1 | 2.893e-7 | **2.9e-6** |
| 0.01 | 1.462e-7 | **1.5e-5** |

**絶対でも相対でもありません。** 模型が 10 分の1になると**絶対値が 9 倍**に
増え、100 分の1では 4.5 倍のまま留まります。**相対では、桁 1 から 0.01 まで
で 500 倍粗くなります。**

#### これが 4-225 の数字と噛み合います

4-225 は「トリム境界が UV で **8〜9e-6** ずれる」と測っていました。**上の
1.5e-5（相対）と同じ桁**です。トリム境界は交線から作るので、**交線の相対
精度がそのまま出ています**。

そして 4-227 で「p-curve の細分は律速ではない」と分かっています——**細分を
100 倍細かくしても残差が動かなかった**のは、**元の交線がその精度しか持って
いない**からでした。筋が通りました。

#### だから、次に見るのは交線を作る段です

**公差を掛ける場所ではありません**（4回そこで外しました）。**交線そのものを
作る段**——追跡の歩幅、収束の判定、当てはめの節点の置き方——のどれが、
模型が小さくなると相対的に粗くなるのかを、**この口で1つずつ測れます**。

**`intersection_fit_probe` は交線だけを見ます。** ブーリアン全体を回さずに、
手を入れた効果がその場で分かります——`scale_sweep_probe` は1配置 20 分
かかりますが（4-227）、こちらは全体で数秒です。

#### この日の空振りを、まとめて置きます

| | 手 | 結果 |
| :--- | :--- | :--- |
| 4-223 | 交線の当てはめ誤差を測る | 測り方が当てにならなかった（訂正済み） |
| 4-224 | パッチの大きさの床を外す | 断り 3 → 0。**恒等式は動かず** |
| 4-225 | `deviation_limit` を比例させる | 動かず、断りが戻った |
| 4-227 | p-curve の細分を比例させる | 動かず、良い桁が悪化、1配置 20 分 |
| **4-229** | **測るところを変えた** | **律速が数字になった** |

**5回目で当たったのは、直す手ではなく測る口でした。**

---

### 4-230. **交線の精度は、跳びの原因ではありませんでした**（2026年9月1日）

4-229 で速い口ができたので、**その場で手を入れて測りました**。結果は
**この一族（4-212 〜 4-229）を終わらせるもの**でした。

#### やったこと

`intersect_nurbs_patches` の `deviation_limit`（**絶対 1e-6**）を、パッチの
大きさに比例させました（`tol.linear.min(extent * 1e-7)`）。歩幅はここを切る
まで半分にされるので、**小さい模型ほど細かく辿ります**。

#### 交線は 20 倍良くなりました

| 桁 | 前 | 後 |
| ---: | ---: | ---: |
| 1 | 3.226e-8 | 3.226e-8 |
| 0.1 | 2.893e-7 | 1.543e-7 |
| **0.01** | **1.462e-7** | **7.243e-9** |

**相対では 1.5e-5 → 7.2e-7。** 狙ったとおりです。

#### **恒等式は1桁も動きませんでした**

| 桁 | 前 | 後 |
| ---: | ---: | ---: |
| 0.01 | 1.367e-6 | **1.368e-6** |
| 0.005 | 9.038e-6 | **9.014e-6** |

**疑っていた量を 20 倍良くして、症状がまったく動きませんでした。**
これは「効かなかった」ではなく、**「原因ではない」の証明**です。

#### 値段は 3.2 倍でした

`contact_placement_probe` が **13分 → 42分12秒**。緑のままですが（135 ok /
6 断り、非多様体 0、恒等式 41配置で破れ 0）、**症状が直らないのにこの値段は
払えません。戻しました。**

#### この一族は、ここで閉じます

| | 疑い | どう潰したか |
| :--- | :--- | :--- |
| 4-223 | 交線の当てはめ誤差 | 測り方が当てにならなかった（訂正） |
| 4-224 | パッチの大きさの床 | 断りは直ったが恒等式は動かず |
| 4-225 | `deviation_limit`（face_split） | 動かず、断りが戻った |
| 4-227 | p-curve の細分 | 動かず、良い桁が悪化 |
| **4-230** | **`deviation_limit`（交線を作る段）** | **20 倍良くしても動かず** |

**5回とも「交線まわりの精度」を疑って、5回とも外しました。**
**交線の精度は、この跳びの原因ではありません。** そう言い切れるのは、
4-230 で**疑った量そのものを大きく動かして、症状が動かなかった**からです。

#### 次に見るところ

**残っているのは、交線から先です。**

| 候補 | なぜ |
| :--- | :--- |
| **体積の積分** | 恒等式は体積で測ります。積分の側が桁で崩れていれば、形が正しくても破れます。**まだ一度も疑っていません** |
| 片の選び方 | 4-222 で「選ばれた片も面の枚数も並びも同じ」と測りましたが、**積だけ**を見ています |
| 蓋 | 差と積で別の形になっていないか |

**いちばん安いのは体積の積分**です。同じ立体を桁だけ変えて、`s³` で正規化した
体積が一致するかを見れば、**ブーリアンを回さずに**分かります。

---

### 4-231. **体積の積分も無罪でした。ついでに「B-Rep を縮められない」ことが分かりました**（2026年9月1日）

4-230 で「交線の精度は原因ではない」と確定したので、**まだ一度も疑って
いなかった側**——体積の測り方——を測りました。

#### まず、測ろうとして分かったこと

**このカーネルは B-Rep を縮められませんでした。**

```text
torus  scale 0.1  縮められません: B-Rep transform must be rigid; axis 0 is scaled by 0.1
```

`BrepTransform::transform_solid` は**剛体変換しか受け付けません**でした。
**模型の拡大縮小は基本の操作です**（単位を変える、他所のデータに合わせる、
図面の縮尺を変える）。**同じ日に直しました**——4-232。

#### 測り方を変えて、測りました

素形状は**桁ごとに作れば厳密に相似**なので、それで測ります
（`mass_scale_probe` を足しました）。**体積は `s³` 倍になるはず**です。

| 形 | 桁 1 → 0.005 の最悪のずれ |
| :--- | ---: |
| トーラス | 2.184e-14 |
| 円柱 | 4.771e-14 |
| 球 | 1.867e-14 |
| 円錐 | 3.257e-15 |
| 箱 | 3.032e-16 |

**丸め誤差の桁です。体積の積分は、桁で崩れません。**

#### これで、残るのは1つだけです

| 疑い | 判定 |
| :--- | :--- |
| 交線の精度 | **無罪**（4-230。20 倍良くしても症状が動かない） |
| 体積の積分 | **無罪**（4-231。1e-14 で一致） |
| **ブーリアンが作る形そのもの** | **残った** |

**桁を変えると、ブーリアンが別の立体を作っています。** 4-225 が測ったとおり
——差は 5.09e-8 で合うのに、**積だけが 2.55e-5 崩れます**。面の枚数は同じ
（12 → 12）で、円柱側の4枚は全桁一致、**トーラス側の8枚だけ**がずれます。

**交線が同じで、積分も同じなら、違うのは「どこを切って、どの片を採るか」
です。** 次に見るのはそこです——`select_operand_faces_after_batch_split` の
内外判定と、蓋の作り方。

---

### 4-232. **B-Rep を一様に拡大縮小できるようにしました**（2026年9月1日）

4-231 で測ろうとして見つけた欠落です。**模型の拡大縮小は基本の操作**
（単位を変える、他所のデータに合わせる、図面の縮尺を変える）なのに、
`BrepTransform::transform_solid` は**剛体変換しか受け付けません**でした。

#### 断っていた理由は、非一様のほうにだけ当てはまります

文面はこうでした。

> Non-rigid transforms are rejected because they would turn circles into
> ellipses and offsets into non-offsets, which the current geometry
> recognizers do not model.

**一様な拡大縮小は、円を楕円にしません。** 平面は平面のまま、直交は直交の
まま、有理の重みも変わりません。**理由が当たるのは非一様のほうだけ**でした。

#### 直したこと

`ensure_rigid` を **`ensure_similar`** にしました——**一様なら通し、その倍率を
返します**。非一様（軸ごとに倍率が違う）と、鏡像（右手系が壊れる）は
**いままでどおり断ります**。

**公差も倍率で動かします。** 縮めた模型に元の公差を残すと、相対ではそのぶん
緩くなります（`Face::tolerance`、`Edge::tolerance`、`Vertex::tolerance`）。

`BrepTransform::scale_solid(solid, factor)` を足しました。

#### 実測

`mass_scale_probe` に「縮めたほう」の列を足して、**作り直しと縮めたのを
並べました**。

| 形 | 作り直し（桁 0.005） | **縮めた**（桁 0.005） |
| :--- | ---: | ---: |
| トーラス | 2.184e-14 | **2.208e-14** |
| 円柱 | 4.771e-14 | **4.771e-14** |

**同じ精度です。** 体積はちょうど `s³` 倍になります。

#### テストを書き換えました（意図を変えたので）

`test_rigid_transform_preserves_brep_and_rejects_scaling` は「拡大縮小は
**断る**」を固定していました。**意図的に置かれたテスト**なので、消さずに
**書き換え**ました——名前を
`test_similarity_transform_preserves_brep_and_rejects_non_uniform_scaling` に
変え、

- 一様な拡大縮小が**通る**こと
- 体積がちょうど **`s³` 倍**になること
- 側面が**2倍の半径の真円柱のまま**であること
- **非一様は、いまも断る**こと

を押さえます。**テスト 137 / 692 / 失敗 0 / 警告 0**（据え置き）。

#### まだできないこと

**鏡像**（`x` を反転する）は断ります。左手系になると面の向きの意味が
変わるので、**向きを付け替える段が要ります**。要るようになったら測って
から入れてください。

---

### 4-233. **崩れているのはブーリアンそのもの**（2026年9月1日）

4-232 で B-Rep を縮められるようになったので、**同じ形を2通りに用意して**
比べられるようになりました。

- **小さく作る**（寸法を `s` 倍して作る）
- **作って縮める**（桁 1 で作って `scale_solid`）

**どちらも厳密に相似**です。`scale_origin_probe` を足して測りました。

| 桁 | 作る（残差） | 縮める（残差） | 作る（積の体積） | 縮める（積の体積） |
| ---: | ---: | ---: | ---: | ---: |
| 1 | 4.758e-13 | 4.758e-13 | 195.853729156 | 195.853729156 |
| 0.1 | 4.893e-13 | 5.739e-13 | 0.195853786 | 0.195853792 |
| 0.02 | 1.212e-12 | 1.190e-12 | 0.001566832 | 0.001566831 |
| **0.01** | **3.710e-7** | **3.711e-7** | 0.000195849 | 0.000195849 |
| **0.005** | **2.452e-6** | **2.430e-6** | 0.000024477 | 0.000024477 |

**2つの道が、同じところで同じだけ破れます。** 積の体積も一致します。

#### つまり

| 疑い | 判定 |
| :--- | :--- |
| 交線の精度 | **無罪**（4-230） |
| 体積の積分 | **無罪**（4-231） |
| 素形状の作り方 | **無罪**（4-233。縮めても同じ） |
| **ブーリアンそのもの** | **これです** |

**小さい模型では、ブーリアンが別の立体を作ります。** 交線は同じ、積分も同じ、
入力も同じ——違うのは**そこから先**、「どこを切って、どの片を採って、どう
蓋をするか」だけです。

#### 次に触る人へ

**絶対値の公差を、選ぶ段と蓋の段で洗ってください。** 交線を作る段は
4-230 で潰しました（そこを 20 倍良くしても動きません）。**まだ見ていないのは
その先**です。

`scale_origin_probe` は**桁ごとに2通りを並べる**ので、手を入れた効果が
「作る／縮める」の両方で同じように動くかまで見えます。**片方だけ動いたら、
その手は形を変えています。**

---

### 4-234. **誤差は1枚に集まっていません。全面に等しく散っています**（2026年9月1日）

4-233 で「崩れているのはブーリアンそのもの」まで来たので、**その中のどこか**を
面ごとに名指ししました（`piece_diff_probe`）。

#### 測り方

4-232 で縮められるようになったので、**同じ立体を2通り**用意します。

- 桁 1 で切ってから縮める（**正しいほう**。桁 1 の恒等式は 4.758e-13）
- 桁 0.01 で切る（**崩れるほう**）

面ごとに**体積への寄与**（発散定理の項）を出し、いちばん近い面どうしで
突き合わせます。**面積ではなく寄与**で見るのは、面がどこにあるかが効くから
です（4-225）。

**口が信用できるかを先に確かめます**——面ごとの寄与を足すと立体の体積に
なるはずで、実測は **1.5e-12** で一致しました。

#### 実測（桁 0.01）

| 演算 | 寄与の差の合計 | 立体の体積 | **相対** | いちばん大きい1枚 |
| :--- | ---: | ---: | ---: | ---: |
| 積 | **3.118e-10** | 1.958e-4 | **1.592e-6** | 13.5%（12枚中） |
| 差 | **3.108e-10** | 3.594e-3 | **8.648e-8** | 13.5%（20枚中） |

**絶対の誤差は、2つの演算でほぼ同じです**（3.118e-10 と 3.108e-10）。
**違うのは体積だけ**——積は差の 18 分の1なので、同じ誤差が 18 倍効きます。

**そして、1枚に集まっていません。** いちばん大きい面でも合計の 13.5% で、
**全面に等しく散っています**。

#### これで分かること

**「どこか1か所で切り損ねている」のではありません。** 全部の面が、**同じ
くらいずつ**ずれています。これは**面をどう作るか（トリム）に、模型の大きさに
依らない絶対のずれがある**という形です。

4-225 が「同じずれが、体積の小さい積では 500 倍効く」と書いたのは正しく、
**そのずれが全面に等しく乗っている**ことまで、ここで数字になりました。

#### 消えた仮説（この日6つ目）

**片を選ぶ段の公差**を疑って、2か所を模型の大きさで頭打ちにしました
（`classify_face_against_mesh` の `tol.linear * 100.0`（絶対 1e-4）と、
`mesh_extent(mesh).max(1.0)` の床——4-224 と同じ形です）。

**残差は 1バイトも動きませんでした**（3.710e-7 / 3.711e-7 のまま）。戻しました。

#### 消えた仮説（7つ目。**コードを変えずに潰しました**）

**吸着**（`split_by_chain` が切り込みの端を境界へ寄せる段）を疑いました。
天井 `SNAP_CEILING = 4e-5` は**絶対値**で、`boundary_extent(...).max(1.0)` の
**床**まで付いています（4-224 と同じ形）。差し渡し 0.4 の模型では、窓が
2.5 倍広くなります。**全面に等しく乗る絶対のずれ**の形にぴったりです。

**その前に測りました。** `ZENITH_SNAP_WHY=1` で数えると——

```text
この配置では、吸着は 0 回
```

**一度も起きていません。** 直す前に測ったので、コードを触らずに済みました。

#### 次に触る人へ

**探すのは「全面に等しく乗る、絶対のずれ」です。** 1枚だけ壊れている、では
ありません。`piece_diff_probe` は数秒で回るので、手を入れて**その 3.1e-10 が
動くか**をその場で見られます。

**まだ測っていない口**: p-curve の**射影の精度**（`tol.parametric`）、
面積分がトリム境界を読むところ、`settle_seam_parameters`。**どれも「全面に
乗る」形をしています。**

**手を入れる前に、その段が実際に走っているかを数えてください**——7つ目は
それで済みました。

---

### 4-235. **絶対のずれは p-curve にありました**（2026年9月1日）

4-234 で「全面に等しく乗る、絶対のずれ」まで来たので、**面のトリム境界を
決めているもの**——p-curve——を測りました。**コードは変えていません**
（`validate_pcurves` がもともと持っている数字です）。

#### 実測（`piece_diff_probe` に足しました）

| 桁 | p-curve が稜から離れる量 | 桁どおりなら |
| ---: | ---: | ---: |
| 1 | 1.906e-8 | — |
| **0.01** | **1.078e-6** | 1.906e-10 |

**5,700 倍です。** 模型が 100 分の1になったのに、**絶対値のほうが 57 倍
増えました**。4-234 が言う「全面に等しく乗る絶対のずれ」の正体は、これです
——**トリム境界は p-curve で決まり、面積分はそれを読みます**。

#### 2つ試して、2つとも外しました

| 手 | 実測 |
| :--- | :--- |
| `on_surface_limit`（種を渡した射影を信じる線引き。**絶対 1e-5**）を曲面の大きさで頭打ち | **`extent × 1e-6` では、桁 0.01 のブーリアンが断られます**（4-214 が書いた綱引き——締めると割り方の p-curve が取れなくなる）。**`extent × 1e-5` では数字が1つも動きません** |
| 細分の止めどころ（`deflection`。**絶対 1e-6**）を稜の長さに比例（4-227 の手を、今度は正しい量で） | **p-curve のずれは 1.078e-6 のまま動かず**。面ごとの寄与は積で 3.118e-10 → 2.528e-10（−19%）、**差では 3.108e-10 → 3.173e-10（悪化）** |

**どちらも戻しました。**

#### なぜ動かなかったか——**測っていたのは保存済みの p-curve でした**

`Face::pcurves` は、面が**すでに持っている** p-curve があればそれを返します。

```rust
pub fn pcurves(&self, tol: &Tolerance) -> Result<FacePcurves, String> {
    if let Some(pcurves) = &self.pcurves {
        return Ok(pcurves.clone());
    }
    ...
```

**つまり 1.078e-6 は、ブーリアンが面に付けたときの値**です。`project_edge_to_nurbs_pcurve`
を締めても、**その面に付ける段が別の道なら効きません**。

#### 次に触る人へ（**ここが入口です**）

1. **ブーリアンが面に p-curve を付けているのはどこか**を突き止める
   （`face.pcurves = Some(...)` を書いているところ）
2. そこが**どの許容で作っているか**を見る。**絶対値なら、それが 4-234 の
   「全面に等しく乗るずれ」です**
3. `piece_diff_probe` は**数秒**で、`p-curve が稜から離れる量`と
   `寄与の差の合計`を両方出します。**その 1.078e-6 が動くか**で判定できます

**恒等式（20分）で判定しないでください。** 4-227 はそれで「効かない」と
書きましたが、あのとき**測る量が違っていました**。

---

### 4-236. **p-curve は 9 点の折れ線でした。ただし辻褄が1つ合っていません**（2026年9月1日）

4-235 の入口（「面に p-curve を付けている段を突き止める」）をたどりました。

#### 付けているのは `Face::new` です

```rust
FaceGeometry::Nurbs(_) => {
    if let Ok(pcurves) = face.derive_nurbs_boundary_pcurves(&Tolerance::default(), 8) {
        face.pcurves = Some(pcurves);
    }
}
```

**面を1枚作るたびに、`Tolerance::default()`（絶対 1e-6）で付けます。**
模型の大きさは見ていません。**4-234 の「全面に等しく乗る」形と一致します。**

#### 何で止まっているか（`ZENITH_PCURVE_WHY=1` を足しました）

```text
PCURVEWHY 稜 96 点 9（上限 4096）、止めどころ 1.000e-6、射影の最悪 1.211e-11、受け入れ幅 1.000e-5
```

| 見たもの | 実測 |
| :--- | :--- |
| 点の数 | **9 点**（上限 4096 には遠い） |
| 射影の精度 | **1.2e-11 〜 1.3e-9**（十分） |
| 止めどころ | **1.000e-6**（絶対値） |
| 最悪を出す面 | **曲面**（平面ではない） |

**p-curve は 9 点の折れ線で、止めているのは弦の許容です。** 点数の上限でも、
射影の精度でもありません。

#### 合っていないこと（**次に読む人はここから**）

**止めどころを締めても、`validate_pcurves` の数字が動きません。**

4-235 で `deflection` を稜の長さに比例させました（桁 0.01 で 1e-8。**100 倍
細かい**）。点は増えるはずですが、**p-curve が稜から離れる量は 1.078e-6 の
まま**でした。

**どちらかが間違っています。**

- 細分の判定（`surface.evaluate(弦の中点) と 稜` の距離）と、
  `validate_pcurves` が測っている量が**別のもの**
- あるいは、測っている面の p-curve が**別の道で付けられている**

**まずここを合わせてください。** 合わないまま公差をいじると、4-227 と
同じ空振りになります。`ZENITH_PCURVE_WHY=1` と `piece_diff_probe` で、
**同じ稜について2つの数字を並べる**のが早いはずです。

---

### 4-237. **辻褄が合いました。p-curve は折れ線で、点数の上限に当たっています**（2026年9月1日）

4-236 の「止めどころを締めても数字が動かない」を解きました。**点数の分布を
数えただけ**です。

#### いまの分布（`ZENITH_PCURVE_WHY=1`）

```text
608 本  9 点
176 本  967 点
116 本  101 点
 92 本  102 点
```

**9 点で済む稜（直線）と、967 点まで割っている稜（曲線）があります。**
4-236 で「9 点」と書いたのは、**先頭だけを見ていたから**でした。

#### 締めたときに何が起きるか

止めどころを稜の長さに比例させる（桁 0.01 で 1e-8。**100 倍細かい**）と——

```text
416 本  9 点
400 本  4096 点   <- **上限**
176 本  967 点
```

**400 本が上限 4096 点に当たります。** 目標に届かないまま打ち切られるので、
**ずれは 1.078e-6 のまま**でした。**公差が効かなかったのではなく、点数が
足りなかった**のです。

#### だから、直し方は「もっと割る」ではありません

**p-curve は 1 次（折れ線）です。**

```rust
// 1次のクランプ節点列。節点をとった辺のパラメータをそのまま使う。
```

折れ線で弦のずれを 100 分の1にするには、**点が 10 倍**要ります（弦の誤差は
間隔の 2 乗で落ちるため）。967 点 → 9,670 点で、上限のほうが先に来ます。

**3 次にすれば、同じ精度が 1/10 以下の点で届きます**（誤差は間隔の 4 乗）。
**次に触る人は、点を増やす前に次数を上げてください。**

#### 積み上がった数字（H6 の現在地点）

| | 実測 |
| :--- | :--- |
| 恒等式の破れ（桁 0.01） | 相対 1.6e-6 |
| 誤差の散り方 | **全面に等しく**（最大の1枚で 13.5%。4-234） |
| p-curve が稜から離れる量 | 桁 1 で 1.906e-8、**桁 0.01 で 1.078e-6**（4-235） |
| p-curve の点数 | 9 / 101 / 102 / **967**（4-237） |
| 締めたとき | **400 本が上限 4096 に当たる**（4-237） |
| p-curve の次数 | **1（折れ線）** |

**無罪が確定しているもの**: 交線の精度（4-230）、体積の積分（4-231）、
素形状の作り方（4-233）、片を選ぶ段の公差（4-234）、吸着（4-234。走ってすら
いない）、射影の精度（4-236。1.2e-11〜1.3e-9）。

---

### 4-238. **3次にしても動きませんでした。測っている数字のほうを疑うべきです**（2026年9月1日）

4-237 の「点を増やす前に次数を上げる」を、そのままやりました。

#### やったこと

`project_edge_to_nurbs_pcurve` が返す p-curve を **1 次 → 3 次**にしました。
媒介変数は**標本を取った稜のパラメータのまま**（弦長で割り当て直すと、
`validate_pcurves` も `boundary_rings` も「同じ割合の点どうし」を比べられなく
なります）。節点は媒介変数の移動平均（de Boor A9.1）。当てはめられなければ
1 次に戻します。

#### 実測

| | 前（1次） | 後（3次） |
| :--- | ---: | ---: |
| p-curve が稜から離れる量（桁 0.01） | 1.078e-6 | **1.078e-6**（1桁も動かず） |
| 面ごとの寄与の差（積） | 3.118e-10 | **8.833e-10**（**悪化**） |
| 同（差） | 3.108e-10 | 3.076e-10 |

**戻しました。**

#### ここで立ち止まります

**`1.078e-6` が、4回連続で1桁も動きません。**

| 手 | 結果 |
| :--- | :--- |
| 止めどころを 100 倍細かく（4-235） | 1.078e-6 |
| `on_surface_limit` を締める（4-235） | 1.078e-6（さらに締めると断られる） |
| 点数の上限に当たっていると突き止めた（4-237） | — |
| **次数を 1 → 3**（4-238） | **1.078e-6** |

**表し方を変えても、点を増やしても、同じ値です。** これは「その量が
p-curve の細かさで決まっていない」ということです。**測っている数字のほうを
疑ってください。**

`validate_pcurves` は、**同じ割合の点どうし**を比べます。

```rust
let uv = pcurve.curve.evaluate(t_min + normalized_t * (t_max - t_min));
let point_from_pcurve = surface.evaluate(uv.x, uv.y);
let point_from_edge = edge.evaluate_normalized(normalized_t);
```

**`pcurve` の媒介変数の割合と、稜の割合が同じである**ことを前提にしています。
節点は稜のパラメータで置いてあるので**そのはず**ですが、**そこがずれていれば、
p-curve がどれだけ正確でも、この距離は縮みません。**

**次にやること: 割合の対応そのものを測ってください。** 同じ稜について、
`pcurve.evaluate(t)` を曲面に載せた点と、`edge.evaluate_normalized(t)` を、
**t を細かく振って**並べる。ずれが**t の位置で系統的に動く**なら、対応の
ほうが原因です。**その1回の測定で、この 4回の空振りが片づきます。**

---

### 4-239. **1回で決まりました。浮いているのは p-curve ではなく稜です**（2026年9月1日）

4-238 の処方どおり、**割合の対応そのもの**を測りました。同じ稜について2つ
並べます——**同じ割合で比べた距離**と、**稜のどこでもよいとしたときの最短
距離**。対応がずれているだけなら、後者は小さくなるはずです。

```text
割合の対応: 同じ割合での最悪 1.078e-6、**稜までの最短の最悪 1.078e-6**（桁 0.01）
```

**同じ値です。対応の問題ではありませんでした。**

#### だから、こう読みます

`surface.evaluate(p-curve(t))` は、**稜からどうやっても 1.078e-6 離れます**。
p-curve は曲面の上の点しか作れないので、これは

> **稜そのものが、支持曲面から 1.078e-6 浮いている**

ということです。**p-curve をどれだけ細かく・高い次数にしても、浮いた稜には
近づけません。** 4-235・4-237・4-238 の3回が動かなかった理由が、これで
説明できます。

#### 数字のつながり

| | 実測 |
| :--- | ---: |
| 交線が支持曲面から浮く量（桁 0.01） | **1.462e-7**（4-229。`intersection_fit_probe`） |
| **結果の立体の稜が浮く量**（桁 0.01） | **1.078e-6**（4-239） |

**7倍違います。** `intersection_fit_probe` が測っているのは**交線**で、結果の
立体には**それ以外の稜**（元の立体から切り取られた稜、蓋の縁）も入ります。
**浮いているのはそちらです。**

#### 次にやること（**ここが入口です**）

1. **どの稜が 1.078e-6 浮いているのかを名指しする**。交線から来た稜か、
   元の立体の稜か、蓋の縁か。`piece_diff_probe` に「割合の対応」を出す係が
   あるので、そこに**稜の出どころ**を足せば分かります
2. その出どころの段が、**どの許容で稜を作っているか**を見る
3. 判定は `piece_diff_probe`（数秒）。**その 1.078e-6 が動くかどうか**です

**4-215 が「浮かせていたのは `split_by_chain` の吸着だった」と書いています。**
ただし 4-234 で**この配置では吸着が0回**と測れているので、**別の段**です。

---

### 4-240. **訂正。稜の浮きは 1.894e-7 で、p-curve のずれ 1.078e-6 とは別物です**（2026年9月1日）

4-239 で「稜そのものが 1.078e-6 浮いている」と書きました。**測り直したら
違いました。訂正します。**

#### まず、自分の測り方を1回外しています

稜ごとの浮きを、**曲面を 33×33 の格子で撒いて**測りました。出た値は
**5.886e-3**——**格子の目そのもの**です（差し渡し 0.4 ÷ 32 の半分）。
**順位まで当てになりません**（長い稜ほど格子に当たりやすい）。

**厳密に射影して測り直しました**（`ExtremumEngine::point_to_surface`）。

```text
稜 210: 浮き 1.894e-7、長さ 0.141357、次数 3、共有 2 面
稜 211: 浮き 1.368e-7、長さ 0.141357、次数 3、共有 2 面
```

**1.894e-7 です。** `intersection_fit_probe` の 1.462e-7（4-229）と同じ桁で、
**交線の精度そのもの**です。

#### だから、4-239 の読みは行きすぎでした

| 量 | 実測 |
| :--- | ---: |
| 稜が支持曲面から浮く量 | **1.894e-7** |
| p-curve を曲面に載せた点と稜の距離 | **1.078e-6** |

**5.7 倍の開きがあります。** 稜の浮きだけでは説明が付きません。**残りは
p-curve 側**——標本と標本の**あいだ**のずれです。

**4-239 の「p-curve をどれだけ細かくしても近づけない」は誤りです。**
近づける余地は **5.7 倍ぶん**あります。

#### では、なぜ細かくしても動かなかったのか

4-237 で測ったとおり、**締めると 400 本が上限 4096 点に当たります**。
**上限が先に来るので、細かくできていません。** 4-238 で次数を 3 にしたのは
方向として正しいのですが、**全体を1本で補間した**ので暴れました（積の寄与が
3.118e-10 → 8.833e-10）。

#### 次に触る人へ

**5.7 倍ぶんは、p-curve の当てはめで詰められます。** ただし——

- **点を増やす道は上限（4096）で塞がっています**
- **1本で全体を補間する道は暴れます**（4-238 の実測）

**区分ごとに 3 次で当てはめる**（節点をまたいで滑らかさを要求しない、
局所的な当てはめ）が、残っている道です。**判定は `piece_diff_probe` の
「割合の対応」の行で、数秒で出ます。**

#### この日の教訓（もう1つ）

**測り口を足したら、その口が飽和していないかを最初に確かめてください。**
格子で測った 5.886e-3 は、**きれいに揃った数字**でした——4本の稜がまったく
同じ値です。**同じ値が並んだら、測っているのは対象ではなく物差しです。**

---

### 4-241. **4回空振りした理由が分かりました。直していた関数は、半分の稜しか作っていません**（2026年9月1日）

4-235・4-237・4-238 と、`project_edge_to_nurbs_pcurve` を3回直して、
**3回とも 1.078e-6 が1桁も動きませんでした**。理由が分かりました。

#### p-curve を作る道は3本あります

```rust
fn match_nurbs_boundary_pcurve(...) {
    if let Ok(curve) = match_nurbs_outer_boundary_pcurve(...) { return Ok(curve); }
    if let Ok(curve) = match_affine_patch_pcurve(...) { return Ok(curve); }
    project_edge_to_nurbs_pcurve(...)   // <- 直していたのはここだけ
}
```

**近道が2本、先に立っています。** `ZENITH_PCURVE_WHY=1` に、**どの道で
作ったか**を出す行を足して数えました。

```text
1408 本  パッチの縁に合わせた
 992 本  射影して当てはめた
```

**私が直していたのは、992 本のほうだけ**でした。

#### ただし、いちばん悪い稜は射影の道でした

いちばん悪い稜を名指しして（`piece_diff_probe` に足しました）、突き合わせます。

```text
いちばん悪い稜は 213
PCURVEPATH 稜 213 : 射影して当てはめた
PCURVEWHY  稜 213 点 102、止めどころ 1.000e-6、**射影の最悪 1.078e-6**
```

**`射影の最悪` が 1.078e-6**——**標本した点そのものが、曲面から
1.078e-6 離れています**。p-curve の当てはめ方（次数・点数）を変えても
動かないのは当然でした。**動かすべきは、標本した点の側**です。

#### **合っていないことが1つ残ります（次に読む人はここから）**

同じ稜 213 を、面の曲面へ**厳密に射影**して測ると **1.368e-7** です
（257 点で標本。`piece_diff_probe` の「稜ごとの浮き」）。

| 測り方 | 値 |
| :--- | ---: |
| p-curve を作る段が報告する `射影の最悪` | **1.078e-6** |
| こちらが同じ稜を同じ面の曲面へ射影した値 | **1.368e-7** |

**8 倍違います。同じ稜、同じはずの曲面です。**

考えられるのは——

- **別の面の曲面**へ射影している（稜は2枚の面に共有されています）
- p-curve を作るときの曲面が、**面が最後に持っている曲面と違う**
  （途中で作られて捨てられた面かもしれません）

**この1つを合わせれば、H6 は「標本した点をどう作るか」に絞れます。**
`ZENITH_PCURVE_WHY=1` は面の番号を出していないので、**そこに面の番号を
足す**のが次の一手です。

#### 試して戻したもの

| 手 | 実測 |
| :--- | :--- |
| p-curve を**区分ごとの 3 次**にする（4-240 の処方） | 1.078e-6 のまま、寄与も同じ |
| 近道（パッチの縁）の受け入れを曲面の大きさで頭打ち | **内訳が 1408 / 992 のまま動かず**（近道の稜はそれだけ正確） |

**どちらも戻しました。**

#### 測り口の直し（4-240 の測り方も甘かった）

稜ごとの浮きを **33 点**で標本していました。**257 点**にすると 1.894e-7 →
**2.239e-7** に上がります。**足りない標本は、小さいほうに嘘をつきます。**

---

### 4-242. **跳びは「だんだん悪くなる」ではなく「切り替わる」でした**（2026年9月1日）

4-241 で「標本した点の側を動かすべき」と分かったので、4-230 の手
（`deviation_limit` をパッチの大きさに比例）を、**今度は正しい数字で**
測り直しました。

#### 効きました——**38 倍**

| 演算 | 前 | 後 |
| :--- | ---: | ---: |
| 面ごとの寄与の差（積） | 3.118e-10 | **8.092e-12** |
| 同（差） | 3.108e-10 | **8.040e-12** |
| 同（和。この回に追加） | — | 8.041e-12 |

**桁 0.01 で切った立体が、桁 1 で切って縮めたものと 38 倍よく一致します。**
4-230 で「効かない」と書いたのは、**恒等式でしか見ていなかった**からでした。

#### それでも恒等式は動きません

| 桁 | 前 | 後 |
| ---: | ---: | ---: |
| 0.01 | 3.710e-7 | **3.711e-7** |
| 0.005 | 2.452e-6 | 2.446e-6 |

**3演算とも 8e-12 で一致しているのに、恒等式は 3.7e-7 破れます。**
立体が「正しいほうとずれている」せいではありません。

#### 絶対値に直したら、形が変わりました

恒等式の残差は**相対**です。`|A| + |B| = 13968.69 × s³` を掛けて絶対に直します。

| 桁 | 相対 | **絶対** |
| ---: | ---: | ---: |
| 1 | 4.758e-13 | 6.65e-9 |
| 0.1 | 6.084e-13 | 8.5e-12 |
| 0.02 | 1.178e-12 | **1.3e-13** |
| **0.01** | 3.711e-7 | **5.2e-9** |
| 0.005 | 2.446e-6 | 4.3e-9 |

**0.02 まではきれいに小さくなり、0.01 で跳ね上がります。**

**だんだん悪くなっているのではありません。切り替わっています。**
公差がじわじわ効いているなら、絶対値は滑らかに増えるはずです。**1桁のあいだに
5 桁跳ぶのは、条件分岐が裏返った形**です。

#### 次に触る人へ（**探し方が変わります**）

**「公差を比例させる」はもうやめてください。** この筋で **6回**外して
います（4-223、4-224、4-225、4-227、4-230、4-234）。**4-242 で分かったのは、
探すのが「値」ではなく「分かれ道」だ**ということです。

- 桁 0.02 と 0.01 の**あいだ**で、`if` の向きが変わる場所を探す
- `boolean_solids_exact_result` の中で、**受け皿（fallback）へ落ちる条件**が
  いちばん怪しい——落ちれば答えの作り方がまるごと変わるので、**絶対値が
  跳ぶ**説明になります
- `ZENITH_SSI_WHY=1`、`ZENITH_REFUSE_WHY=1` を桁 0.02 と 0.01 で流して、
  **出力を diff する**のがいちばん早いはずです

#### 入れなかったもの

**`deviation_limit` の比例は戻しました。** 38 倍良くなりますが、**恒等式は
動かず**、`contact_placement_probe` が **13分 → 42分**（3.2 倍。4-230）
です。**症状が直らないのにこの値段は払えません。**

---

### 4-243. **分かれ道はありませんでした。位相は同じで、数字だけが跳びます**（2026年9月1日）

4-242 の処方（「桁 0.02 と 0.01 のあいだで `if` が裏返る場所を探す」）を
やりました。**見つかりませんでした。**

#### 交線の段は、両方の桁で同じ振る舞いです

`ZENITH_SSI_WHY=1` を両方で流し、**数字を伏せて diff** しました（枝の本数、
落とした理由、パッチの大きさの読み）。

```text
（差分なし）
```

#### 結果の位相も同じです

| 演算 | 桁 0.02 の面数 | 桁 0.01 の面数 |
| :--- | ---: | ---: |
| 積 | 12 | 12 |
| 差 | 20 | 20 |
| 和 | 26 | 26 |

**面の数も同じ、SSI の振る舞いも同じ**です。**受け皿へ落ちてはいません。**

#### そして、面ごとの一致は「悪いほう」が 0.02 です

| 桁 | 面ごとの寄与の差（絶対） | 恒等式の残差（絶対） |
| ---: | ---: | ---: |
| 0.02 | **1.430e-9** | **1.3e-13** |
| 0.01 | 3.118e-10 | **5.2e-9** |

**逆です。** 桁 0.02 のほうが「正しいほう」から遠いのに、**恒等式はきれいに
閉じます**。桁 0.01 はよく一致しているのに**破れます**。

**この2つは連動していません。** 立体が基準からどれだけずれているかと、
3演算のつじつまが合うかは、別のことでした。

#### 何が残るか

恒等式が破れるのは、**3つの演算の誤差が打ち消し合わなくなる**ときです。
桁 0.02 では 1.4e-9 のずれが3演算で**きれいに相殺**し、桁 0.01 では 3e-10 の
ずれが**相殺しません**。

**次に見るのは「どの演算が、他の2つと違う向きにずれているか」です。**
`piece_diff_probe` は3演算を別々に出すので、**同じ面が3演算でどうずれるかを
並べれば**分かります（いまは演算ごとに合計を出しているだけです）。

#### この日ここまでで分かったこと（H6）

| | |
| :--- | :--- |
| **無罪** | 交線の精度、体積の積分、素形状の作り方、片を選ぶ段、吸着、射影の精度、**位相の分かれ道**（4-243） |
| **効くが直らない** | `deviation_limit` の比例（面ごとの一致が 38 倍。恒等式は動かず。値段 3.2 倍） |
| **残り** | **3演算のあいだで誤差が相殺しなくなる理由** |

---

### 4-244. **破れはトーラスの境界から来ています**（2026年9月1日）

4-243 で「3演算のあいだで誤差が相殺しなくなる理由を見よ」となったので、
**破れをどちらの立体の境界から来たかに割りました**（`identity_split_probe`）。

#### 筋

`|A∪B| + |A∩B| = |A| + |B|` が成り立つのは、**A の境界が和と積で相補的に
使われるから**です（B の外側は和へ、内側は積へ）。だから

> **A から来た面の寄与を、和と積で足すと `|A|` になるはず**

です。ならなければ、**A の境界の切り分けが相補になっていません**。

#### 実測

| 桁 | **A 側（トーラス）の残り** | 恒等式の破れ |
| ---: | ---: | ---: |
| 1 | 4.025e-10 | -8.480e-9 |
| 0.02 | **4.940e-15** | -6.839e-14 |
| **0.01** | **-5.182e-9** | **-5.182e-9** |
| 0.005 | -4.267e-9 | -4.262e-9 |

**桁 0.01 と 0.005 で、A 側の残りが恒等式の破れとぴたり一致します。**

**破れは、トーラスの境界から来ています。** 和と積で、トーラスの面が
**相補的に切り分けられていません**——足りないぶんも余ったぶんも、そのまま
恒等式に出ます。

#### この口の限界（正直に書きます）

**B 側（円柱）の列は当てになりません。** 円柱の面は切り分けたあとに
**パッチが張り直される**ので、制御点が元と一致せず、「出どころ不明」に
丸ごと入ります。**A 側だけを読んでください**——トーラスの面は制御点が
そのまま残るので照合できます。

（桁 1 で A 側 4.0e-10、破れ -8.5e-9 と合わないのは、そのぶんが B 側または
蓋にあるからです。**小さい桁では A 側が支配的**になります。）

#### 次に触る人へ

**トーラスの面が、和と積でどう切り分けられているかを見てください。**

- 和に入るトーラス片と、積に入るトーラス片の**トリム境界が同じか**
  （同じ交線から作られるはずです）
- 片方に**入っていない部分**がないか（足すと `|A|` にならないのは、
  そういうことです）

`identity_split_probe` は数秒で回り、**桁を変えても A 側の残りが破れを
追いかける**ので、手を入れた効果がそのまま見えます。

---

### 4-245. **16 枚のうち 8 枚ちょうどが、相補になっていません**（2026年9月1日）

4-244 で「破れはトーラスの境界から」と割れたので、**どのパッチか**を数えました
（`identity_split_probe` に足しました）。曲面ごとに、**元の面の寄与**と、
**和・積に入った片の寄与の合計**を比べます。

#### 実測

| 桁 | 相補でないパッチ | 1枚あたりのずれ | A 側の残り |
| ---: | ---: | ---: | ---: |
| 1 | **8 / 16** | 5.7e-11 | 4.025e-10 |
| **0.02** | **0 / 16** | — | **4.940e-15** |
| 0.01 | **8 / 16** | 6.804e-10 | -5.182e-9 |
| 0.005 | **8 / 16** | 約 1.0e-9 | -4.267e-9 |

**8 枚ちょうど**です。16 枚の半分。しかも**1枚あたりのずれがほぼ同じ**
（桁 0.01 では上位3枚が 6.804e-10 で揃います）。**8 × 6.8e-10 = 5.4e-9** で、
恒等式の破れ **5.182e-9** とほぼ合います。

#### 桁 0.02 だけが 0 枚です

**きれいに 0**（残り 4.940e-15）。**この桁では、切り分けが厳密に相補**に
なっています。**桁 1 でも 8 枚崩れています**（ずれが小さいので恒等式では
見えないだけ）。

**つまり「小さい模型で壊れる」のではありません。** ずっと 8 枚崩れていて、
**桁 0.02 のときだけ、たまたま揃っている**——そう読むほうが素直です。

#### 次に触る人へ

**16 枚のうち、どの 8 枚か**を見てください。トーラスは 4×4 のパッチで
できています。**円柱に切られる側の半分**なのか、**v 方向の半分**なのかで、
疑う場所が変わります。

- 切られる側の半分なら、**切り分けそのもの**（片の選び方・トリム）
- v 方向の半分なら、**継ぎ目**（`settle_seam_parameters` の側）

`identity_split_probe` はパッチごとのずれを出すので、**そこに曲面の位置
（制御点の重心など）を添えれば**、どの 8 枚かが分かります。**それが次の
1行です。**

---

### 4-246. **切られたパッチが、ぜんぶ相補になっていません**（2026年9月1日）

4-245 の「その 8 枚はどこか」を名指ししました。

```text
ずれ 5.713e-11  パッチの中心 (-6.222222, -6.222222,  2.666667)
ずれ 5.700e-11  パッチの中心 (-6.222222,  6.222222,  2.666667)
ずれ 5.645e-11  パッチの中心 ( 6.222222, -6.222222,  2.666667)
ずれ 5.143e-11  パッチの中心 (-6.222222,  6.222222, -2.666667)
…（8枚とも |z| = 2.666667）
```

**8 枚とも `|z| = 2.667`** です。トーラスは 4×4 のパッチで、これは**管の
上下の四半分**——**切る円柱（半径 9）が通る側**です。`z = 0` の内外の四半分
（残る 8 枚）は**切られないので、丸ごとどちらかへ行き、当然そろいます**。

**つまり「切られたパッチが、ぜんぶ相補になっていない」**のでした。

#### 符号が手掛かりです

A 側の残りは**負**です（桁 0.01 で -5.182e-9）。**片の合計が、元の面より
小さい**——**切り口で材料が減っています**。和の片と積の片が、**切り口で
ぴったり合っていません**。

#### いちばんありそうな筋（**まだ測っていません**）

**切り口の p-curve が、片ごとに別々に作られています。**

面を作るのは `Face::new` で、**面1枚につき1回** p-curve を導出します
（4-236）。和へ行く片と積へ行く片は**別々の `Face`** なので、**同じ切り口を
2回、別々に近似**します。p-curve は 9〜967 点の折れ線（4-237）なので、
**2つの近似は一致しません**——その差が、そのまま隙間になります。

**桁 0.02 で 0 枚だったのは、たまたま2つの近似が同じ点列になったから**、と
読めば辻褄が合います。

#### 次に触る人へ（測り方まで書きます）

1. **同じ切り口の稜が、和の片と積の片で同じ p-curve を持っているか**を測る。
   `Face::pcurves` を両方から取り出して、**同じ稜 id の p-curve の制御点を
   比べる**だけです
2. 違っていたら、**切り口の p-curve を1回だけ作って共有する**のが直し方です
3. 判定は `identity_split_probe`（数秒）。**「相補になっていないもの 8 枚」が
   0 枚になるか**で分かります

**この筋は、まだ推測です。** 上の 1 を測ってから手を入れてください——
この日、推測で手を入れて **7 回**外しています。

---

### 4-247. **その測り方は成り立ちませんでした**（2026年9月1日）

4-246 の処方（「同じ稜 id の p-curve を、和の片と積の片で比べる」）を
やりました。**測れませんでした。**

```text
和と積で共有する稜 0 本のうち、p-curve が違うもの 0 本
```

**和と積は別々の実行です。** それぞれが自分で稜を作るので、**番号は最初から
共有していません**。「同じ稜を2回近似している」かどうかは、**番号では
確かめられません**。

#### そして、幾何で比べた測定は既にあります

4-225 が、**差と積のトリム境界を UV で比べて** **どちらも 9.191e-6** と
測っています。**2つの近似は同じ**でした。

**だから 4-246 の筋（片ごとに別々の近似ができて食い違う）は、支持されません。**

#### 残る読み方

片は**同じ切り口**に切られているのに、**足すと元より小さい**（桁 0.01 で
1枚あたり 6.804e-10）。同じ境界を使っているなら、**足りないぶんはどちらの
片にも入っていない**——つまり

> **トリムした面の積分が、切り口のそばで取りこぼしている**

という形です。**4-231 で「積分は桁で崩れない」と測りましたが、あれは
トリムしていない素の立体**です。**トリムした面の積分は、まだ測って
いません。**

#### 次に触る人へ（測り方）

**1枚のパッチを、任意の曲線で2つに切って、足し合わせるだけ**です。

1. トリムしていないパッチの寄与を測る
2. 同じパッチを2つの領域に切り、それぞれの寄与を測る
3. **足して 1 と合うか**

合わなければ、**トリムした面の積分**が原因です。**ブーリアンを回さずに
測れます**（4-231 と同じ手口で、こんどはトリムを入れて）。

**この日の推測はここまでで 8 回外しています。** 上の 1〜3 を測ってから
手を入れてください。

---

### 4-248. **メッシュの物差しでは分解できません。次に測るものを絞りました**（2026年9月1日）

4-247 の「トリムした面の積分が取りこぼしているのでは」を、**別の物差しで
突き合わせて**確かめようとしました。

| 桁 0.01 | 解析（p-curve を読む） | メッシュ（三角形） | 差 |
| :--- | ---: | ---: | ---: |
| 和 | 1.377283437549e-2 | 1.374166089438e-2 | **-0.23%** |
| 積 | 1.958487299007e-4 | 1.934901288760e-4 | **-1.20%** |

**メッシュ側は弦誤差で 0.2〜1% ずれます。** 探しているのは **6.8e-10**
（相対 3e-6）なので、**この物差しでは分解できません**。突き合わせには
使えません。**probe の出力に、そう書いておきました。**

#### 筋を詰めました

トリムした面の積分は、境界の周回積分です。片 1 の境界は
**（パッチの縁の一部）＋（切り口）**、片 2 は**（縁の残り）＋（切り口の逆
向き）**。足すと**切り口の寄与は打ち消し合う**——**2つの切り口が同じ曲線
なら**。

**だから、足りないぶん（6.8e-10）は「2つの切り口の p-curve の差」です。**

4-225 は「差と積のトリムのずれはどちらも 9.191e-6」と測りましたが、それは
**それぞれが基準からどれだけずれているか**であって、**2つが互いに同じか**
ではありません。**同じ大きさずれていても、向きが違えば打ち消し合いません。**

#### 次に測ること（**これが最後の1つです**）

**同じトーラスのパッチについて、和に入った片と積に入った片の「切り口」を
UV で直接比べる。**

1. 曲面の指紋（制御点）で、和の片と積の片を突き合わせる（`identity_split_probe`
   にその係があります）
2. それぞれの境界ループから、**パッチの縁に乗っていない区間**＝切り口を選ぶ
3. 両方を UV で標本して、**点ごとの差**を出す

**差が 0 なら**、打ち消し合うはずなので別の原因です。**差が 6.8e-10 に見合う
大きさなら、そこが原因**——直し方は「切り口の p-curve を、2つの片で共有する」
です。

**稜の番号は共有していません**（4-247）ので、**幾何で突き合わせてください**。

---

### 4-249. **原因を掴みました。和の片と積の片が、別の切り口に切られています**（2026年9月1日）

4-248 の「最後の1測定」をやりました。**同じトーラスのパッチについて、和に
入った片と積に入った片の切り口を、UV で幾何的に突き合わせます**（稜の番号は
共有していないので。4-247）。

#### 実測

| 桁 | **切り口の UV 最大差** | A 側の残り | 恒等式の破れ |
| ---: | ---: | ---: | ---: |
| 1 | **2.070e-10** | 4.025e-10 | -8.480e-9 |
| 0.02 | **2.072e-10** | 4.940e-15 | -6.839e-14 |
| **0.01** | **7.890e-6** | **-5.182e-9** | **-5.182e-9** |
| 0.005 | **1.863e-5** | -4.267e-9 | -4.262e-9 |

**桁 1 と 0.02 では 2e-10 で一致しているのに、0.01 で 4 桁跳んで 7.9e-6、
0.005 で 1.9e-5 です。** 恒等式が破れる桁と、ぴったり重なります。

**2回流して 1バイトも違いません。** 揺れではなく、決まった差です。

#### これで筋が通ります

トリムした面の積分は境界の周回積分で、片1の境界は（パッチの縁の一部）＋
（切り口）、片2は（縁の残り）＋（切り口の逆向き）。**2つの切り口が同じ曲線
なら、足したとき打ち消し合います。** 同じでなければ、**その差がそのまま
残ります**——それが 1枚あたり 6.8e-10、8枚で 5.4e-9、恒等式の破れ 5.182e-9 です。

**和と積は別々の実行**なので（4-247）、**それぞれが切り口を作り直します**。
桁が小さいと、その2つが 7.9e-6 も離れます。

#### 直し方の候補（**まだ測っていません**）

| 案 | 中身 |
| :--- | :--- |
| **切り口を共有する** | 交線を1回だけ作り、和・差・積で**同じ曲線を使い回す**。`boolean_solids_exact_result` は演算ごとに交差からやり直しています |
| **切り口を決定的にする** | 作り直してもよいが、**同じ入力なら同じ曲線**になるようにする。いまも決定的（2回流して同一）なので、**演算の種類で分岐している**ところを探す |

**後者のほうが軽いはずです。** 2回流して同じなのに、**和と積で違う**のだから、
**演算の種類が交線の作り方に効いている**——そこを見つければ済みます。

#### この日の追跡（H6）

| 段 | 記録 |
| :--- | :--- |
| 交線の精度・体積の積分・素形状・片の選び方・吸着・射影の精度・位相の分かれ道 | **全部無罪**（4-230〜4-243） |
| 破れはトーラスの境界から | 4-244 |
| 相補でないのは 16 枚中 8 枚 | 4-245 |
| その 8 枚は「切られたパッチ」 | 4-246 |
| 番号では比べられない／メッシュでは分解できない | 4-247、4-248 |
| **切り口そのものが違う** | **4-249** |

**推測で手を入れて 8 回外し、測り口を 8 つ足して、原因に届きました。**

---

### 4-250. **交線は同じでした。分かれるのは片を切り出すところです**（2026年9月1日）

4-249 で「和の片と積の片が別の切り口に切られている」と分かったので、
**演算の種類がどこに効いているか**をコードで追いました。

#### `op` が届く先は1か所だけです

```rust
let mut edge_candidates =
    Self::intersection_edge_candidates_from_face_pairs(face_pair_candidates, tol);   // ← op は無い
…
let selection = Self::selected_face_pieces_from_candidates(
    solid_a, solid_b, edge_candidates.clone(), op, tol,                              // ← ここだけ
);
```

**交線の候補は、`op` を使う前に作られます。** 和でも積でも**同じ交線**です。
`op` が渡るのは**片の選び方**だけ。

#### だから、分かれるのはその先です

同じ交線から出発して、**片を切り出すところ**で2つが分かれています。候補は
2つ。

| 候補 | 中身 |
| :--- | :--- |
| **片ごとに稜を切り詰める** | 切り口の稜を、その片の境界に合わせて詰める段。**詰め方が片によって違えば、p-curve も違います** |
| **p-curve を片ごとに作り直す** | `Face::new` は面1枚につき1回 p-curve を導出します（4-236）。**同じ 3D 稜なら同じ p-curve になるはず**なので、**稜のほうが違っている**と読むのが自然です |

**後者の読みが正しければ、探すのは「切り口の稜が、片ごとに別の形になる場所」**
です。`split_by_chain` の切り詰めと、`snapped_onto_boundary` の吸着が
そこにあります（4-215、4-217、4-219 が触っています）。

#### 次に測ること

**同じ切り口の 3D 稜を、和の片と積の片で幾何的に比べる**——`identity_split_probe`
の「切り口の突き合わせ」を、**UV ではなく 3D で**やるだけです。

- **3D でも違う**なら、稜そのものが片ごとに違う（切り詰め・吸着の段）
- **3D では同じ**なら、p-curve の作り方が片ごとに違う（射影の段）

**この1回で、直す場所が2つに1つまで決まります。**

---

### 4-251. **同じ 3D 稜、同じ曲面、違う p-curve**（2026年9月1日）

4-250 の1測定をやりました。切り口を **UV と 3D の両方**で突き合わせます。

| 桁 | UV の最大差 | **3D の最大差** |
| ---: | ---: | ---: |
| 1 | 2.070e-10 | **0.000e0** |
| 0.02 | 2.072e-10 | **0.000e0** |
| 0.01 | **7.890e-6** | **0.000e0** |
| 0.005 | **1.863e-5** | **0.000e0** |

**3D の稜は、どの桁でも完全に同一です。** 切り詰めも吸着も**していません**
——和の片と積の片は、**同じ 3D 曲線**を境界に持っています。

**違うのは p-curve だけ**です。**同じ稜を、同じ曲面へ写して、違う答えに
なっています。**

#### なぜそうなるか（**4-241 が答えを持っています**）

p-curve を作る道は**3本**あります。

```rust
if let Ok(curve) = match_nurbs_outer_boundary_pcurve(...) { return Ok(curve); }  // 近道1
if let Ok(curve) = match_affine_patch_pcurve(...) { return Ok(curve); }          // 近道2
project_edge_to_nurbs_pcurve(...)                                               // 射影
```

**近道1は「その稜がパッチの縁に乗っているか」で分かれます**（受け入れは
**絶対 1e-5**。4-241）。**片によって、この判定が裏返ります**——和の片では
縁に乗っていると認められ、積の片では認められない、ということが起こりえます。
**認められれば uv では直線、認められなければ折れ線**なので、**7.9e-6 の差**が
出ます。

**桁 1 と 0.02 で差が 2e-10 なのは、両方が同じ道を通っているから**、
**0.01 で跳ぶのは、片方だけが近道に落ちるから**——この読みなら、すべての
数字が揃います。

#### 次に測ること（**推測を1回で潰せます**）

`ZENITH_PCURVE_WHY=1` は**どの道で作ったか**を出します（4-241 で足しました）。
**切り口の稜について、和の片と積の片で道が違うか**を見るだけです。

- **道が違う**なら、**近道の受け入れ（絶対 1e-5）が原因**です。曲面の
  大きさで頭打ちにすれば揃います（4-241 で試したときは**内訳が動かなかった**
  ので、**切り口の稜だけ**に効かせる必要があるかもしれません）
- **道が同じ**なら、同じ道が同じ入力で違う答えを返していることになるので、
  **射影の種**（`seed`）を疑ってください

#### この日の到達点

**8 回の空振りと 9 つの測り口を経て、H6 は「同じ稜・同じ曲面から、違う
p-curve が出る」まで来ました。** 直す場所は、あと1測定で決まります。

---

### 4-252. **同じ切り口が、和と積で別の道を通っていました**（2026年9月1日、H6 の原因）

4-251 の1測定をやりました。`ZENITH_PCURVE_WHY=1` に**稜の中点**を添えて
（番号は実行ごとに変わるので。4-247）、**同じ切り口が和と積で同じ道を通って
いるか**を数えます。

```text
PCURVEPATH (-0.180000000,0.000000000,0.000000000) : パッチの縁に合わせた
PCURVEPATH (-0.180000000,0.000000000,0.000000000) : 射影して当てはめた
PCURVEPATH (-9.000000000,0.000000000,0.000000000) : パッチの縁に合わせた
PCURVEPATH (-9.000000000,0.000000000,0.000000000) : 射影して当てはめた
…（**9 か所**）
```

**同じ中点の切り口が、片方では近道（パッチの縁）、片方では射影で作られて
います。** 近道は **uv で直線**、射影は**折れ線**です。**同じ 3D 稜から、
違う p-curve が出ます**——4-251 で測ったとおりです。

#### これで全部つながります

| 段 | 記録 |
| :--- | :--- |
| 恒等式が桁 0.01 で破れる | 4-212 |
| 破れはトーラスの境界から | 4-244 |
| 相補でないのは**切られた 8 枚** | 4-245、4-246 |
| **3D の稜は同一、p-curve だけが違う** | 4-251 |
| **同じ切り口が、和と積で別の道を通る** | **4-252** |

**トリムした面の積分は周回積分なので、2つの片の切り口が同じ曲線なら
打ち消し合います。** 道が違えば曲線が違い、**打ち消し残りがそのまま恒等式の
破れになります**（1枚 6.8e-10 × 8枚 = 5.4e-9、実測の破れ 5.182e-9）。

#### なぜ道が分かれるのか

**近道は「その稜がパッチの縁に乗っているか」で決まります**（受け入れは
**絶対 1e-5**。4-241）。**片のパッチは、演算ごとに張り直されます**
（4-244 で「円柱の面は制御点が元と一致しない」と測っています）。
**張り直されたパッチが少し違えば、判定が裏返ります。**

#### 直し方（次に触る人へ）

| 案 | 中身 |
| :--- | :--- |
| **判定を大きさに合わせる** | 近道の受け入れ（絶対 1e-5）を、パッチの大きさで頭打ちに。4-241 で試したときは内訳が動きませんでしたが、**あれは全部の稜を見ていました**。**切り口の稜だけ**で数え直してください |
| **道を1本に決める** | 切り口の稜は**必ず射影で作る**。近道は「元からパッチの縁だった稜」にだけ使う。**判定が裏返る余地が消えます** |
| **パッチを張り直さない** | 演算ごとに同じパッチを使えば、判定も揃います。いちばん深い直し方です |

**判定は `identity_split_probe`（数秒）です**——「切り口の突き合わせ」の
**UV の最大差が 2e-10 に落ちるか**、「相補になっていないもの」が **0 枚**に
なるか。

#### この日の H6

**推測で 8 回外し、測り口を 9 つ足して、原因に届きました。** 直す手は
まだ入れていません——**入れる前に、上の判定で効くかを測ってください**。


### 4-253. H6 の3案のうち、**2つを潰しました**（2026年9月1日）

4-252 が置いた3案を、`identity_split_probe`（数秒）で順に測りました。
**入れる前に測る**、という 4-252 の指示どおりです。

#### 案2「道を1本に決める」——**逆に壊れます**

「切り口の稜は必ず射影で作る」を試すため、**近道を止める口**を足しました
（`ZENITH_NO_PCURVE_SHORTCUT=1`）。

| | 相補でない面 | UV の最大差 | **3D の最大差** |
| :--- | ---: | ---: | ---: |
| いま（近道あり） | 8 枚 | 7.890e-6 | **0.000e0** |
| **近道なし** | 8 枚 | **5.379e-1** | **6.588e-2** |

**桁が5つ悪化し、3D まで狂います。** 近道は継ぎ目の稜をパッチの縁に
留めておく役をしていて、そこを射影に回すと**反対側の縁へ落ちます**。
**「全部射影」は誤りです。**

#### 案1「判定を大きさに合わせる」——**きわどい受け入れが1件もありません**

近道の受け入れは**絶対 1e-5** です（`distance > tol.linear.max(1e-6) * 10.0`）。
「小さい模型では緩すぎて、切り口の稜まで拾っているのでは」と考えて、
**受け入れた距離を出す口**を足しました（`ZENITH_PCURVE_WHY=1` の
`PCURVEEDGE`）。

| 受け入れた距離 | 件数 |
| :--- | ---: |
| **1e-14 未満** | **2,432** |
| 1e-14 以上 | **0** |

**2,432 件すべてが機械精度**（最大 4.039e-15）です。閾値は必要の**9桁ぶん
緩い**のに、**きわどい受け入れは1件もありません**。**閾値を下げても、
下げなくても、通る稜は同じです。** 案1 は無関係でした。

#### 射影の粗さでもありません

`piece_diff_probe` が出す1行が目を引きます。

```text
p-curve が稜から離れる量: 桁 1 で 1.906e-8、桁 0.01 で 1.078e-6
（桁 1 を 0.01 倍すると 1.906e-10）
```

**相似なら 1.906e-10 のはずが 1.078e-6——5,600 倍**悪い。射影の折れ線を
決める `deflection = tol.linear`（**絶対 1e-6**）が効いている、と読めます。

**パッチの大きさに比例させて測りました**（`tol.linear.min(patch_span * 1.7e-7)`。
桁 1 で同じ値になる係数、緩める方向には効かない形）。

| | UV の最大差（桁 0.01） |
| :--- | ---: |
| 前 | 7.890e-6 |
| 比例させたあと | **7.890e-6** |

**1桁も動きません。** **戻しました**——効かない変更を入れると、次の人が
「ここは見た」と思ってしまいます。

#### 残るのは案3

**「パッチを演算ごとに張り直さない」**だけが残りました。4-252 の見立て
（張り直されたパッチが少し違うので判定が裏返る）は、**上の2つを潰した
ことで、消去法でも支持されます**。ただし**まだ直接は測っていません**。

**次の一手**: **同じ切り口の稜1本について、和と積で使われたパッチの
制御点を並べてください。** 違っていれば案3で確定です。違っていなければ、
**判定でも粗さでもパッチでもない**ことになり、見立てを立て直す必要が
あります。`ZENITH_PCURVE_WHY=1` は既に**稜の中点**を出すので（4-252）、
同じ切り口を照合する手がかりはあります。

#### 足した口（2つ、どちらも残してあります）

| 口 | 何が見えるか |
| :--- | :--- |
| `ZENITH_NO_PCURVE_SHORTCUT=1` | 近道を止める。**答えが変わります**——速くするための口ではありません |
| `ZENITH_PCURVE_WHY=1` の `PCURVEEDGE` | 近道が**受け入れた距離**と、パッチの差し渡し |


### 4-254. **4-252 の見立ては、指紋の見誤りでした**（2026年9月1日、訂正）

4-253 で案1・案2 を潰し、残った案3（パッチが演算ごとに張り直される）を
確かめようとして、**4-252 そのものが成り立っていない**と分かりました。

#### まず、パッチの指紋を添えました

`PCURVEPATH` に**パッチの四隅と中心**を添えて、道が分かれた 9 本について
パッチを突き合わせました。**2 種類**出ました——案3 の裏付けに見えます。

ところが中身を見ると、こうです。

```text
中点 (0.000000000, 9.000000000, 0.000000000)
  パッチ A: (9,0,-20) (0,9,-20) (9,0,20) (0,9,20)
  パッチ B: (0,9,-20) (-9,0,-20) (0,9,20) (-9,0,20)
```

**張り直された同じパッチではありません。隣り合う別の四半パッチです。**
稜はその2枚の**継ぎ目**なので、**両方に現れて当たり前**です。

#### 指紋が足りていませんでした

`PCURVEPATH` の指紋は**稜の中点だけ**でした（4-252）。ところが、

- 継ぎ目の**全長の稜**（`(0,9,-20)`→`(0,9,20)`、中点 `(0,9,0)`）
- それを**割った稜**（`(0,9,-a)`→`(0,9,a)`、中点も `(0,9,0)`）

は、**同じ指紋になります**。前者は近道（パッチの縁に corner から corner まで
一致）が通り、後者は通らないので射影に落ちます。**当たり前の振る舞いです。**

**稜の長さを指紋に足して数え直しました。**

| 指紋 | 種類 | **道が分かれたもの** |
| :--- | ---: | ---: |
| 中点だけ（4-252） | 405 | **9** |
| **中点＋長さ**（ここ） | 414 | **0** |

**0 本です。** **同じ稜が別の道を通ることはありません。**

#### だから、4-252 の「原因」は取り下げます

> **同じ切り口が、和と積で別の道を通っています**

**これは成り立ちません。** 道は稜ごとに一意で、分かれて見えたのは
**別々の稜を同じものと数えていた**からです。

**4-253 で案1・案2 を潰したのは有効**です（あちらは閾値と道そのものを
直接測っています）。**案3 も、この測り方では支持されません。**

#### では、何が残るのか

**`identity_split_probe` が出す破れは本物です。**

```text
切り口の突き合わせ: 8 枚で比べ、UV の最大差 7.890e-6、3D の最大差 0.000e0
トーラスの面 16 枚のうち、相補になっていないもの 8 枚
```

**3D は完全に同一で、UV だけが 7.890e-6 違います。** 同じ 3D 曲線が違う uv に
写るなら、**2つの片が同じ曲面を別のパラメータで持っている**ということです
——**面の側**の話で、**稜の p-curve の作り方の話ではありません**。

**次の一手**: **その 8 枚について、和の片と積の片の「曲面そのもの」を
突き合わせてください**（次数・ノット・制御点・重み）。`piece_diff_probe` が
「円柱の面は切り分けたあとにパッチが張り直されるので制御点が元と一致しない」
と既に測っています（4-244）。**同じことがトーラス側でも起きているか**を、
そこで確かめます。

#### 教訓

**幾何で照合するときは、照合の鍵が一意かを先に確かめてください。**
4-247 で「番号は実行ごとに変わるので幾何で照合する」と決めたのは正しかった
のですが、**選んだ鍵（中点）が一意ではありませんでした**。**一意でない鍵は、
無い鍵より悪い**——「分かれている」という、**存在しない現象**を作ります。

`PCURVEPATH` の指紋には**長さを足してあります**。


### 4-255. 曲面も同一でした。**残るのは p-curve だけ**（2026年9月1日）

4-254 で「稜の p-curve の作り方ではなく、面の側だ」と書いたので、そこを
測りました。和の片と積の片で、**トーラスの曲面そのもの**を突き合わせます
（中心で対応を取り、次数・パラメータ範囲・制御点・重みを比べる）。

| 桁 | 突き合わせた枚数 | **制御点・重みの最大差** | パラメータ範囲が違うもの |
| ---: | ---: | ---: | ---: |
| 1 | 8 枚 | **0.000e0** | **0 枚** |
| 0.01 | 8 枚 | **0.000e0** | **0 枚** |

**完全に同一**です。**案3（パッチが演算ごとに張り直される）も、トーラス側
では成り立ちません。**

#### ここまでで、外側は全部潰れました

| 見たもの | 結果 | 記録 |
| :--- | :--- | :--- |
| 3D の稜 | **同一** | 4-251 |
| 近道の受け入れ閾値 | きわどい受け入れ **0 件** | 4-253 |
| 道が分かれるか | **0 本**（指紋の見誤りだった） | 4-254 |
| 射影の粗さ（絶対公差） | 比例させても**動かない** | 4-253 |
| **曲面そのもの** | **同一** | **ここ** |

**同じ曲面の上に、同じ 3D 曲線があって、p-curve だけが uv で 7.890e-6
違います。** 曲面が同じで曲線が同じなら、正しい uv は1つに決まります。
**つまり、どちらか一方の p-curve が、単に粗いのです。**

#### 次の一手（1つに絞れます）

**その 8 枚について、和の片と積の片の切り口の p-curve を、
「制御点の数・ノット」まで並べてください。**

- **数が違うなら**、片方が細かく割られている＝**粗さの差**です。
  そのときは `project_edge_to_nurbs_pcurve` の割り方（`deflection`）が
  効くはずですが、**4-253 で比例させても動きませんでした**——なので、
  **p-curve がその場で作られていない**（面が持っている `pcurves` を
  そのまま使っている）疑いが強い。`Face::new` は p-curve を**先に導出して
  保持します**。**いつ・どの入力で作られたものかを見てください。**
- **数もノットも同じなら**、粗さではなく**当てはめの答えそのもの**が
  違うことになります。

**推測を書きません。** ここまでで、外側の候補は全部測って潰しました。
残っているのは**上の1点だけ**です。


### 4-256. 切り口の p-curve を、**形まで**並べました（2026年9月1日）

4-255 が置いた「制御点の数とノットまで並べる」をやりました。

#### まず、向きの取り違えではないことを確かめました

和の片と積の片は、**共有する切り口を逆向きに辿ります**（端が入れ替わって
います）。同じ割合どうしで引き算すると、それだけで大きな数が出ます。

| | 同じ向きで測る | **向きを合わせて測る** |
| :--- | ---: | ---: |
| 桁 1 | 1.000e0 | **4.742e-10** |
| 桁 0.01 | 1.000e0 | **7.890e-6** |

**向きを合わせても 7.890e-6 は残ります。本物です。**（4-254 で指紋の
見誤りをしているので、ここは先に潰しました。）

#### 形は同じ。ずれ方が違います

| | 次数 | 制御点 | ノット | 始 | 終 |
| :--- | ---: | ---: | ---: | :--- | :--- |
| 桁 1 和 | 1 | 967 | 969 | (0.462166600, 1) | (0.462166600, 0) |
| 桁 1 積 | 1 | 967 | 969 | (0.462166600, 0) | (0.462166600, 1) |
| 桁 0.01 和 | 1 | 102 | 104 | (0.462158203, 1) | (**0.462150318**, 0) |
| 桁 0.01 積 | 1 | 102 | 104 | (0.462158203, 0) | (**0.462150313**, 1) |

**次数も制御点の数もノットの数も同じ**です。**粗さの差ではありません。**

**桁 1 では、u が両端とも 0.462166600 で一定**——p-curve は uv の**真っ直ぐな
縦線**です。**桁 0.01 では u が 0.462158203 から 0.462150318 へ動きます**
（**7.885e-6**）。これが観測している 7.890e-6 と一致します。

#### 向きで符号が変わります

- 和: `v = 1 → 0`、`u: .203 → .318`
- 積: `v = 0 → 1`、`u: .203 → .313`

**どちらも「自分の始点」で u = .462158203、「自分の終点」で u ≒ .46215031**
です。

**→ ここから先の読み（「u の流れが辿る向きについてくる」）は外れでした
（4-257）。** 反転して測っても符号は変わりません。両方が「自分の始点で .203」
なのは、**和と積の切り口が別々の稜の実体だから**です（4-80）。

**これが H6 の残りの正体です。** 曲面も 3D の稜も同一（4-251、4-255）、
道も一意（4-254）、粗さも同じ。**違うのは「どちらの端から作ったか」だけ**
です。

#### 次の一手（1つ）

**`project_edge_to_nurbs_pcurve` が、始点から順に作っているかを見てください。**
p-curve の標本は稜の媒介変数に沿って置かれ、各点は
`ExtremumEngine::point_to_surface` で uv を得ます。**前の点を種にしているなら、
誤差は始点から一方向に積み上がります**——それが「向きで符号が変わる」形です。

測り方: **同じ 3D 稜を、向きを反転させて2回 p-curve にして、u の流れを
並べる。** 反転で符号が反転すれば確定です。**数秒で付きます。**

**推測は書きません。** 上の表までが測った事実です。


### 4-257. 向きではありませんでした。**切り口は別々の稜の実体**です（2026年9月1日、訂正）

4-256 が置いた1点——「向きを反転すれば u の流れの符号が反転するか」——を
測りました。

#### 反転しません

同じ面の外周ワイヤを逆向きにした写しを作り、同じ稜の p-curve を作り直しました。

| 桁 | 順に辿る | 逆に辿る |
| ---: | ---: | ---: |
| 1 | +5.052e-12 | +5.051e-12 |
| 0.01 | **−7.885e-6** | **−7.890e-6** |

**符号は変わりません。** **u の流れは、辿る向きの性質ではありません**——
**稜そのものの性質**です。4-256 の「辿る向きについてくる」は取り下げます。

#### では、なぜ 4-256 では両方が「自分の始点で .203」だったのか

**別々の稜の実体だったからです。**

```text
和 : 稜id  69  向き Reversed  始 (0.090,0.000,0.026457513) 終 (0.000,0.090,0.026457513) 制御点 16
積 : 稜id 141  向き Forward   始 (0.090,0.000,0.026457513) 終 (0.000,0.090,0.026457513) 制御点 16
```

**3D の始点・終点・制御点の数はまったく同じで、id だけが違います**（4-80 の
「同じ弧に別々の実体」）。**それぞれが自分の始点から p-curve に射影される**
ので、どちらも「自分の始点で u = .462158203」になります。

**向きの話に見えていたのは、別実体を1つと数えていたためです。**（4-254 と
同じ形の取り違えを、また1つ潰しました。）

#### いま分かっていること

| | |
| :--- | :--- |
| 3D の稜 | **同じ形**（始点・終点・制御点の数が一致）。**ただし別の実体** |
| 曲面 | **完全に同一**（4-255） |
| p-curve の形 | 次数・制御点の数・ノットの数が**同じ**（4-256） |
| p-curve の**両端** | **5e-9 まで一致**（4-256 の表） |
| p-curve の**途中** | **7.890e-6 違う**（向きを合わせて測った値。4-256） |

**両端は合っていて、途中だけが違います。** 本当の p-curve は uv の真っ直ぐな
縦線です（桁 1 で確認済み）。**2本の折れ線が、その縦線の左右に振れています。**

#### 次の一手

**振れの中身を見てください。** 具体的には、**同じ 3D 曲線を2つの実体それぞれ
から射影したときの、標本点（ノット）の位置**です。

- **ノットの位置が違うなら**、標本の置き方が実体ごとに変わっている
  （置き方は「弦が稜からどれだけ外れるか」で決まります）。
- **ノットの位置が同じで uv だけ違うなら**、`ExtremumEngine::point_to_surface`
  が同じ点に対して違う uv を返している——**種の違い**です。

**4-253 で `deflection`（弦の許容）を厳しくしても動かなかった**ので、
**後者（種）の疑いが強い**のですが、**測っていません**。**推測です。**


### 4-258. **標本点そのものが 10 倍ずれています**（2026年9月1日、H6）

4-257 の1点——「ノットの位置が違うのか、同じ点に違う uv が返っているのか」
——を測りました。**どちらでもなく、もっと単純でした。**

#### 2つの実体は、完全に同じ曲線です

和と積の切り口の稜（別実体）について、制御点の指紋を取りました。

| 桁 | 和 | 積 |
| ---: | ---: | ---: |
| 1 | 7.573752792626412e2 | **7.573752792626412e2** |
| 0.01 | 2.199701253482313e0 | **2.199701253482313e0** |

**15 桁まで一致**。**同じ曲線の別実体**です（4-80）。**実体の違いは原因では
ありません。**

#### p-curve の外れには、**下限**があります

p-curve は必ず曲面の上にあります。**稜そのものが曲面から浮いていれば、
その分は、どんなに細かく割っても縮みません。** 両方を測りました。

| 桁 | p-curve が稜から外れる量 | **稜が曲面から浮く量**（下限） | 比 |
| ---: | ---: | ---: | ---: |
| 1 | 7.050e-8 | **6.527e-8** | **1.08 倍** |
| 1（別の稜） | 1.215e-7 | **1.214e-7** | **1.00 倍** |
| **0.01** | **1.078e-6** | **1.026e-7** | **10.5 倍** |
| **0.01（別の稜）** | **1.078e-6** | **1.665e-7** | **6.5 倍** |

**桁 1 では、下限にぴたりと届いています**——p-curve はこれ以上よくなり
ようがない、正しい姿です。**桁 0.01 では、下限の 10 倍で止まっています。**

#### 細分では縮みません

弦の許容（`deflection = tol.linear`、**絶対 1e-6**）をパッチの大きさに
比例させて、制御点を増やして測りました。

| | 制御点 | p-curve が稜から外れる量 |
| :--- | ---: | ---: |
| いま | 102 | 1.078e-6 |
| 比例させた | **4096**（上限に到達） | **1.078e-6** |

**40 倍に増やしても 1 桁も動きません。** **折れ線の粗さではありません**
——**標本点そのものが 1.078e-6 ずれています**。

#### だから、残るのは射影の1点です

標本点の uv は `ExtremumEngine::point_to_surface(point, surface, 32,
tol.parametric)` が返します。**稜は曲面から 1.026e-7 しか浮いていないのに、
その uv を曲面に戻すと 1.078e-6 ずれる**——**射影が最近点を外している**、
ということです。

**次の一手**: **標本点1つで、射影の答えを直接確かめてください。**
`point_to_surface` が返した uv の点と、格子で総当たりした最近点を比べる。
差が 1e-6 の桁で出れば確定です。**数秒で付きます。**

見るべき引数は2つあります。

- `32`（初期格子の粗さ）——**絶対ではなく個数**なので、桁には依らないはず
- `tol.parametric`（**1e-7 の絶対値**。パラメータ空間の収束判定）——
  パッチの 3D の差し渡しが 0.0628 なら、1e-7 は 3D で 6.3e-9。**これだけでは
  1.078e-6 を説明できません。**

**説明が付いていないので、推測は書きません。** 上の表までが測った事実です。


### 4-259. **H6 達成。収束判定に次元がありました**（2026年9月1日）

4-258 で「標本点そのものが下限の 10 倍ずれている」まで絞れたので、射影の
中を見ました。**1行でした。**

#### 原因

`ExtremumEngine::refine_surface_projection` の収束判定です。

```rust
let f = diff.dot(&su);          // 残差 · ∂S/∂u
let g = diff.dot(&sv);
if f.abs() < tolerance && g.abs() < tolerance { break; }
```

`f` は「**長さ × 長さ/パラメータ**」の量です。これを**絶対の 1e-7**
（`tol.parametric`）と比べているので、**到達できる残差は
`tolerance / |∂S/∂u|`**——**パッチが小さいほど大きくなります**。

トーラスの四半パッチで確かめました。

| 桁 | `|∂S/∂u|` | 予想される残差 | **実測** |
| ---: | ---: | ---: | ---: |
| 1 | 6.28 | 1.6e-8 | **1.741e-8** |
| 0.01 | 0.0628 | 1.6e-6 | **1.546e-6** |

**予想と実測が 10% 以内で一致します。** 点はトーラス上に厳密に乗っている
のに（閉じた式で 1.830e-8）、射影は 1.546e-6 離れた点を返していました。

#### 直し

**微分の大きさで割って、長さの次元に戻します。**

```rust
let scale_u = su.norm().min(1.0);
let scale_v = sv.norm().min(1.0);
if f.abs() < tolerance * scale_u && g.abs() < tolerance * scale_v { break; }
```

**`min(1.0)` を掛けてあるので、大きいパッチでは判定が変わりません**——
いま通っているものの要求は動きません。**小さいパッチのときだけ厳しく**なります。

#### 効き方

| 測り口 | 前 | 後 |
| :--- | ---: | ---: |
| 射影の余分（桁 0.01） | 1.527e-6 | **3.807e-8** |
| `identity_split_probe` の UV 差（0.01） | 7.890e-6 | **2.069e-10** |
| 同（0.005） | 1.863e-5 | **2.069e-10** |
| 相補になっていない面（0.02 / 0.01 / 0.005） | 5 / 8 / 8 | **0 / 0 / 0** |
| **恒等式（`torus × cylinder`、桁 0.01）** | **1.367e-6** | **3.959e-13** |
| **同（桁 0.005）** | **9.038e-6** | **1.660e-12** |

#### H6 を達成しました

**`scale_sweep_probe` の門を 0.005 まで下げました。**

| | 前 | 後 |
| :--- | :--- | :--- |
| 門の範囲 | 0.1〜100（0.02 以下は表示だけ） | **0.005〜100（全部が門の中）** |
| 恒等式の破れ | 0（門の中） | **0** |
| 非多様体 | 0 | **0** |
| **断り** | 0 | **0** |
| 残差の最悪 | 8.912e-6 | **7.394e-8** |

#### 落ちたものは1つもありません

| | 前 | 後 |
| :--- | :--- | :--- |
| 通しテスト | 137 バイナリ / 692 件 / 失敗 0 / 警告 0 | **同じ** |
| `contact_placement_probe` | 141演算で 135 ok / 6 断り（未実装 0）、非多様体 0、恒等式 41配置で破れ 0（最悪 3.900e-8） | **同じ** |
| `rechained_boolean_probe` | 10連鎖・30演算すべて緑（最悪 1.906e-7） | **同じ** |
| `face_budget_probe`（H5） | 最悪 3,854 枚 | **同じ** |
| `intersection_fit_probe` | 1e-5 超え 0 本 | **同じ** |

#### たどり方（4-229 〜 4-259）

**推測を外した回数を数えると、この件だけで 11 回**です。当たったのは
どれも**測ったあと**でした。とくに最後の3手が効きました。

1. **4-258**「p-curve の外れには**下限**がある」——稜が曲面から浮いている量。
   **桁 1 では下限にぴたり届き、桁 0.01 では 10 倍**。**比べる相手を作った**
   ことで、初めて「悪い」と言えるようになりました。
2. **細分を 102 → 4096 に増やして 1 桁も動かない**と確かめた。これで
   「折れ線の粗さ」が消え、**標本点そのもの**に絞れました。
3. **閉じた式（トーラスの厳密な距離）を基準に置いた**。総当たりの格子は
   重すぎて（10 分で終わらない）測れませんでしたが、閉じた式なら一瞬です。
   **「厳密に 1.830e-8 なのに射影は 1.546e-6」**という形にできて、初めて
   射影を疑えました。

**「悪い」と言うには、比べる相手が要ります。** 4-229 から 4-258 まで、
**比べる相手を作れないまま数字だけを見ていた**ので、11 回外しました。

#### 落とし穴として

**次元のある量を、絶対の公差と比べていました。** `f = 残差 · ∂S/∂u` の
単位は「長さ²/パラメータ」です。**単位が合っていない比較は、模型の大きさが
変わった瞬間に意味を変えます。**

同じ形の間違いを、この2日で4件直しています。

| どこ | 何が絶対だったか |
| :--- | :--- |
| 4-212 | p-curve の許容 |
| 4-216 | 吸着の天井（p-curve 側と噛み合っていなかった） |
| 4-224 | パッチの大きさの床（1.0） |
| **4-259** | **射影の収束判定（次元が違った）** |

**公差を書くときは、単位を確かめてください。**


### 4-260. H8 の壁は「9-3 の判断」ではなく、**STEP の解析曲面の復元**でした（2026年9月1日）

H6 が片づいたので（4-259）、残る H8 を測り直しました。4-228 は「残りは曲面側で、
9-3 の判断にぶつかっている」と書いていましたが、**もっと手前で止まっています**。

#### 検体は2つとも、**読む段**で落ちます

| ファイル | 落ち方 |
| :--- | :--- |
| `screw.step` | 外殻の検証で **225 件**。境界が自分の曲面から**最大 87** 離れる |
| `linkrods.step` | 外殻の検証で **122 件**。最大 **4.1e-4** |

**桁が3つ違います。別々の話**です。`linkrods` のほうは実務のファイルにある
程度の粗さ（1e-4 級）ですが、`screw` の **87** は部品の寸法そのものです。

#### `screw.step` は、曲面の型で結果が分かれます

面を1枚ずつ読んで測りました（`import_face_from_str`）。

| 曲面の型 | 制御点 | 境界の外れ |
| :--- | ---: | ---: |
| `CYLINDRICAL_SURFACE` | **9 × 2**（全周） | **6.342e-15** |
| `CONICAL_SURFACE` | 3 × 2（四半周） | **1.299e1**、**2.381e1** |
| `TOROIDAL_SURFACE` | 3 × 3（四半周） | **8.215e1**、**8.311e1**、**8.742e1** |
| `PLANE` | — | （平面のまま） |

**円柱だけが正しく、円錐とトーラスは四半周のパッチになっています。**

#### 決め打ちのパッチに落ちています

`get_surface_for_boundary` は**境界に合わせて**解析パッチを作る建て付けで、
作れなければ `get_surface` の**決め打ち（90度 × 90度）**に落ちます。
`ZENITH_STEP_WHY=1` が言います。

```text
STEPWHY 曲面 #482 TOROIDAL_SURFACE は境界に合わせたパッチを作れず、
        決め打ちのパッチに落ちました（境界の点 72 個）
STEPWHY   トーラス（芯 8.250000、管 54.873719）の上に乗らない点:
        芯からの距離 51.779581（差 3.094138e0、許す幅 5.487372e-2）
```

**制御点 3×3 は、この決め打ちの 90 度パッチです。** 面はそこに乗っていないので、
境界が 87 離れます。

#### トーラスの形が変わっています

```text
TOROIDAL_SURFACE('', #483, 8.25, 54.8737186...)
```

**芯の円 8.25 に対し、管が 54.87**——**管のほうが大きい**（自己交差する
「紡錘トーラス」）。ねじ山の側面を表すのに使われる形で、**常設の検体には
1つもありません**。

境界の点は、芯から **51.78** と **54.50** のところにあり、管の 54.87 から
**3.094** と **0.377** ずれています。許す幅は **5.487e-2**（管の 1e-3）。

**「点が乗っていないから作れない」という判定までは正しく動いています。**
分からないのは、**なぜ乗っていないか**です。

#### 次の一手（順に）

1. **境界の点から、トーラスを当てはめ直してください。** 軸はそのままに、
   芯と管を最小二乗で解く。**出てきた値が 8.25 / 54.87 と違うなら、
   置き方（`AXIS2_PLACEMENT_3D`）か単位の読みが違います**。同じなら、
   境界の B-spline（`B_SPLINE_CURVE_WITH_KNOTS` が 27 本。ねじの螺旋の
   当てはめ）が**本当にその程度ずれている**ということで、**許す幅のほうを
   考え直す**話になります。
2. **円錐が落ちた理由が出ていません。** トーラスは「乗らない点」を出すのに、
   円錐は1行も出しません。**診断の穴です。** `cone_patch_for_boundary` にも
   同じ口を足してください。
3. `linkrods.step`（最大 4.1e-4）は**別件**として分けてください。こちらは
   ファイルが申告する不確かさの範囲かもしれません。

#### H8 の見立てを直します

4-228 の「9-3 の判断にぶつかっている」は、**まだそこまで行っていません**。
**いま止まっているのは STEP の解析曲面の復元**で、9-3（内部表現に解析曲面を
持つか）とは別の話です。**円柱は同じ道で正しく復元できている**ので、
**設計の問題ではなく、円錐とトーラスの実装の問題**です。


### 4-261. 円錐の**頂点の向こう側**（2026年9月1日、H8）

4-260 で「円錐が落ちた理由が1行も出ていない」と書いたので、まず口を足しました。
**一発で出ました。**

```text
STEPWHY   円錐（基準半径 7.500000、勾配 1.000000）の上に乗らない点:
          軸方向 -15.000000 での半径 7.500000
          （あるべき値 **-7.500000**、差 1.500000e1、許す幅 7.500000e-3）
```

**あるべき値が負**です。

#### STEP の円錐は無限で、頂点の向こう側も面です

`半径 + 軸方向 × 勾配` は、頂点を越えると**負**になります。そこは
**反対の葉**で、幾何としては半径 `|...|`、角度が π ずれた円錐面です。
判定が符号を見ていなかったので、**半径 7.5 の点を「あるべき値 −7.5」と
比べて差 15 で断って**いました。

#### 直し

**境界の点が全部向こう側のときだけ、裏返します。** 半径と勾配の符号を反転し、
角度を π ずらせば、あとの組み立ては何も変わりません。

**跨いでいる面は、これまでどおり断ります**——頂点を含むので、1枚の回転パッチ
では表せません。

#### 効き方

| `screw.step` の円錐面 | 前 | 後 |
| :--- | :--- | :--- |
| #944 | 制御点 **3 × 2**、境界の外れ **2.381e1** | **9 × 2**、**1.569e-11** |
| #1151 | 制御点 **3 × 2**、境界の外れ **1.299e1** | **9 × 2**、**8.548e-12** |
| **外殻の検証エラー** | **225 件** | **72 件** |

**3 × 2 は決め打ちの 90 度パッチ**でした（4-260）。境界に合わせたパッチが
作れるようになったので、**全周（9 × 2）**になり、外れが 12 桁縮みました。

#### 落ちたものは1つもありません

| | 前 | 後 |
| :--- | :--- | :--- |
| 通しテスト | 137 バイナリ / 692 件 / 失敗 0 / 警告 0 | **同じ** |
| `scale_sweep_probe` | 破れ 0・非多様体 0・断り 0（最悪 7.394e-8） | **同じ** |
| `contact_placement_probe` | 135 ok / 6 断り（未実装 0）、非多様体 0 | **同じ** |
| `rechained_boolean_probe` | 30演算すべて緑 | **同じ** |

#### 残り

`screw.step` の 72 件は**トーラス**です（芯 8.25 / 管 54.87 の紡錘型）。
境界が管から **3.094** と **0.377** ずれています（許す幅 5.487e-2）。
**4-260 の「次の一手」の 1 番**——境界からトーラスを当てはめ直して、
置き方か単位の読みが違うのかを確かめる——が、そのまま残っています。

#### 教訓

**「理由が出ない」は、それ自体が欠陥です。** トーラスには理由を出す口が
あって、円錐には無かった。**片方だけ調べて「円錐も同じだろう」と書かずに、
口を足したら1分で分かりました**（4-260 で「診断の穴」と書いておいたのが
効きました）。


### 4-262. トーラスは**ファイルの側が合っていません**（2026年9月1日、H8）

4-260 の「次の一手」の 1 番——境界からトーラスを当てはめ直す——をやりました。

#### 面の頂点が、そのトーラスの上にありません

`screw.step` の面 `#709`（`ADVANCED_FACE('',(#710),#482,.F.)`、曲面は
`TOROIDAL_SURFACE('',#483,8.25,54.873718663856)`）の頂点を、
`#483` の枠（原点 `(-17.898, -0.826, -46.314)`、軸 `-Z`）で測りました。

| 頂点 | 位置 | 軸からの半径 | 軸方向 | **芯からの距離** |
| :--- | :--- | ---: | ---: | ---: |
| #337 | (-17.898, -2.076, 7.732) | 1.250 | -54.045 | **54.496560** |
| #427 | (-27.820, -2.076, 5.436) | 10.000 | -51.750 | **51.779581** |
| #715 | (-17.898, -10.826, 5.436) | 10.000 | -51.750 | **51.779581** |

**管の半径は 54.873719** です。**0.377 と 3.094 ずれています**（0.7% と 5.6%）。

**公差の話ではありません。**

#### 当てはめ直しても合いません

境界を 99 点で標本すると、芯からの距離は **51.78〜54.50**——**ばらつき 2.72**
です。軸を固定して芯の半径を当てはめ直しても、**最悪の外れは 2.17 までしか
下がりません**（そのときの芯は 0.0、STEP は 8.25）。

**どんなトーラスにも乗りません。**

#### 制御点で比べてはいけません

途中、B スプラインの**制御点**を測って「51.78〜54.50 だから外れている」と
書きかけました。**間違いです**——次数 3 の制御点は曲線の上にありません。
**上の表は頂点（`VERTEX_POINT`）で測っています**。頂点は必ず曲面の上に
あるはずの点なので、そこがずれていれば本物です。

**→ 「ファイルの側が合っていない」は取り下げます（4-264）。** 合って
いなかったのは**こちらの測り方**でした——符号付きの半径で測ると、頂点は
管の半径と小数第6位まで一致します。

#### だから、次は2つに分かれます

**(a) 読み方が違う。** `AXIS2_PLACEMENT_3D` の解釈（原点・軸・基準方向の
並び）は確かめましたが、**紡錘トーラス（管 > 芯）を OCCT がどう使うか**は
確かめていません。STEP の仕様に当たる必要があります。

**(b) 落ち方を直す。** いまは**決め打ちの 90 度パッチ**に落ちて、境界が
**87 離れた面**が出来ます。これは 3-2 の「もっともらしい答えより、
できないと言う」に反します。**作れないなら、その面を断るべき**です。
少なくとも「決め打ちのパッチに落ちた」ことが**呼ぶ側に返っていません**。

**(b) は (a) と独立に、いますぐできます。** そして (b) をやると、
`read_and_cut_probe` の断り文が「検証エラー 72 件」から
「**この曲面は復元できない**」に変わり、**次の人が (a) に直行できます**。

#### まだ測っていないこと

`linkrods.step`（最大 4.1e-4）は**触っていません**。桁が3つ違うので、
別の話として残してあります。


### 4-263. 復元できない曲面を、**復元できないと言う**（2026年9月1日、H8）

4-262 の (b) を入れました。

#### 前は、壊れた面が黙って出来ていました

境界に合わせた解析パッチが作れないと、**決め打ちの 90 度パッチ**に落ちます。
面がそこに乗っていなければ、**境界が丸ごと外れた曲面**がそのまま下流へ行き、
検証が「境界が曲面から外れている」を 72 件並べる、という形でした。

**読めなかったことが、読めなかったとして出てきません。** 追いかける人は
幾何を疑うことになります（4-260 で、実際に私がそうしました）。

#### 測ってから断ります

決め打ちに落ちたあと、**境界の点がその曲面に乗っているかを測ります**。
乗っていなければ、**曲面を名指しして断ります**。

```text
前: Solid validation failed with 72 outer-shell errors
    (Face 3: Face 4 boundary point on outer loop is off surface by 8.31e1; ...)

後: Surface #482 TOROIDAL_SURFACE could not be rebuilt to cover its face:
    the boundary is off the fallback patch by 8.749340e1
    (allowed 1.322876e-2 for an extent of 13.228757)
```

**許す幅は境界の大きさに比例**させました（`境界の差し渡し × 1e-3`）。
絶対値で書くと、模型の大きさが変わった瞬間に意味が変わります
（4-259 で同じ形の欠陥を直したばかりです）。

#### いま通っているものは動きません

**測ってから断る**ので、決め打ちのパッチでも面が乗っているなら、これまで
どおり通ります。

| | 前 | 後 |
| :--- | :--- | :--- |
| 通しテスト | 137 バイナリ / 692 件 / 失敗 0 / 警告 0 | **同じ** |
| `scale_sweep_probe` | 破れ 0・非多様体 0・断り 0（最悪 7.394e-8） | **同じ** |
| `contact_placement_probe` | 135 ok / 6 断り（未実装 0）、非多様体 0 | **同じ** |
| `rechained_boolean_probe` | 30演算すべて緑 | **同じ** |
| `foreign_cross_pair_probe` | 破れ 0 | **同じ** |

#### H8 の残り

`screw.step` は**まだ読めません**。ただし止まっている場所が名指しされました
——`TOROIDAL_SURFACE`（紡錘型。管 54.87 > 芯 8.25）の復元です。
**4-262 の (a)**——OCCT が紡錘トーラスをどう使うかを STEP の仕様に当たる——が、
そのまま次の一手です。

`linkrods.step`（最大 4.1e-4）は**別件**のまま残っています。

#### 教訓

**「読めなかった」と「読んだけれど答えが変」は、別の出口から出さないと
いけません。** ここでは前者が後者の顔をして出ていました。**下流の検証が
拾ってくれるから誤答にはならない**——それは正しいのですが、**追う人の
時間を奪います**。


### 4-264. 紡錘トーラスの**軸をまたいだ側**（2026年9月1日、H8）

4-262 で「ファイルの側が合っていない」と書きました。**間違いでした。**
合っていなかったのは、こちらの測り方です。

#### 符号付きで測ると、ぴたり合います

`screw.step` のねじ山の面 `#709` の頂点を、符号を付けて測り直しました。

| 頂点 | 軸からの半径 | 軸方向 | 符号なしで測ると | **符号付きで測ると** |
| :--- | ---: | ---: | ---: | ---: |
| #427 | 10.00 | -51.750 | 51.779581 | **54.873719** |
| #337 | 1.25 | -54.045 | 54.496560 | **54.873719** |

**管の半径 54.873719 と、小数第6位まで一致します。**

#### なぜ符号が要るのか

管が芯より大きいトーラス（`54.87 > 8.25`）は自分と交わり、**断面の円が軸を
またぎます**。またいだ先の点は、符号付きの半径が**負**です。

ところが 3D の点から測れるのは**符号なしの距離**なので、素直に
`|半径| − 芯` で測ると別の値になります。**円錐の「頂点の向こう側」
（4-261）と、まったく同じ形の見落とし**でした。

`screw.step` は**円錐でも紡錘トーラスでも、軸や頂点をまたいだ側**を使って
います。ねじ山とはそういう形です。

#### 直し

**またいだ側に全部あるときだけ、芯の符号を反転して角度を π ずらします。**
またいでいる面（軸の上の点を含む）は、これまでどおり断ります。

**一度、半径も一緒に反転して書きました。** 打ち消し合って**数字が1桁も
動きません**でした。`|r| − (−芯) = |r| + 芯` が、符号付きで見た
`|(−|r|) − 芯|` と同じになる——**反転するのは片方だけ**です。

#### 効き方

| | 前 | 後 |
| :--- | ---: | ---: |
| トーラス面の境界の外れ | **8.749e1** | **2.946e-4** |
| `screw.step` の検証エラー | 225 → 72（4-261） | **18** |

**5桁縮みました。**

#### 落ちたものは1つもありません

| | 前 | 後 |
| :--- | :--- | :--- |
| 通しテスト | 137 バイナリ / 692 件 / 失敗 0 / 警告 0 | **同じ** |
| `scale_sweep_probe` | 破れ 0・非多様体 0・断り 0（最悪 7.394e-8） | **同じ** |
| `contact_placement_probe` | 135 ok / 6 断り（未実装 0） | **同じ** |
| `rechained_boolean_probe` | 30演算すべて緑 | **同じ** |
| `foreign_cross_pair_probe` | 破れ 0（最悪 9.429e-10） | **同じ** |
| `face_budget_probe`（H5） | 3,854 枚 | **同じ** |

#### 残っているのは、**ファイルの粗さ**です

`screw.step` の残り 18 件は **2.946e-4** の桁です。ところがこのファイルは、
自分で**不確かさ 1e-6** と宣言しています。

| ファイル | 宣言した不確かさ | 実際の外れ | 比 |
| :--- | ---: | ---: | ---: |
| `screw.step` | 1e-6 | **2.946e-4** | **294 倍** |
| `linkrods.step` | 2e-5 | **4.1e-4** | **20 倍** |

**どちらも、自分の申告より粗い**です。ねじ山の稜は螺旋を B スプラインで
当てはめたもので、その当てはめ誤差がそのまま出ています。

**構造の欠陥は2つとも塞ぎました**（円錐の第2葉、紡錘トーラスの内枝）。
**残るのは方針の判断**です——「他所のファイルの粗さを、どこまで受けるか」。

**選択肢は3つ**あります。

| 案 | 中身 |
| :--- | :--- |
| **申告を信じない** | ファイルの宣言ではなく、**実測した外れ**を面ごとの公差にする |
| **読むときだけ緩める** | 検証の公差を、読み込み経路でだけ大きくする。**下流は元のまま** |
| **断り続ける** | 「このファイルは自分の申告より粗い」と言って断る。**正しいが、実務のファイルはほとんど読めなくなります** |

**まだ決めていません。** 3-2（もっともらしい答えより、できないと言う）と、
H8 の目標（読んだ立体を切る）が、ここで正面からぶつかります。**決めるのは
判断であって、測定ではありません。**

#### 教訓

**「相手のデータが悪い」と書く前に、自分の測り方を疑ってください。**
4-262 で「ファイルの側が合っていない」と書きました。**頂点が 0.7% と 5.6%
ずれる、どんなトーラスにも乗らない**——数字は全部正しかったのに、
**測り方が符号を落としていました**。

同じ日に、同じ形で2回やっています（円錐 4-261、トーラス 4-264）。
**またいだ側は、符号なしの距離では見えません。**


### 4-265. 「位相が不正」と言っていたものは、**位相は無傷**でした（2026年9月2日、H8）

4-264 で構造の欠陥を塞いだので、残る「他所のファイルの粗さをどこまで受けるか」
という判断を、**決める前に測りました**。

#### 読み込みの公差を上書きする口を足しました

`ZENITH_IMPORT_TOL` です。**答えが変わります**——決める前に測るための口です。

| ファイル | 既定（申告どおり） | **1e-3 まで受けたら** |
| :--- | :--- | :--- |
| `screw.step` | 検証エラー **18 件** | **3 件**（しかも別の型——面の向きの不一致） |
| `linkrods.step` | 検証エラー **122 件** | **読めます**（立体 1 個、面 37、体積 3.897551） |

#### 読めた `linkrods.step` の位相は、無傷でした

| 検査 | 結果 |
| :--- | ---: |
| 開いたワイヤ | **0** |
| 相手のいない稜 | **0** |
| **非多様体** | **0** |
| 同じ向き / 重複した稜 / 重複した面 / 潰れた面 / 潰れた稜 | すべて **0** |
| 平面の向きの不一致 | **0** |
| 稜の端と頂点の差 | **0**（最大 **8.951e-16**） |
| p-curve の不一致 | **0**（最大 1.006e-6） |
| **境界が曲面から外れ** | **255 件（最大 4.145e-4）** |

**落ちているのは最後の1つだけ**です。**幾何の公差の話で、位相ではありません。**

#### 断り文が嘘をついていました

ブーリアンの入口はこう言っていました。

```text
Exact B-Rep boolean input A is not topologically valid
```

**位相は無傷なのに「位相が不正」**です。読む人は位相を疑いに行きます
（4-263 とまったく同じ形）。**どの検査で落ちたかを言う**ように直しました。

```text
Exact B-Rep boolean input A does not validate as a closed shell:
boundary points off their surface 255 (worst 4.145e-4)
```

#### 私も同じ間違いをしました

最初、稜の使われ方を**端点だけ**で数えて「**10 本が3回以上使われている**」と
読みました。**間違いです**——同じ2点を結ぶ別の弧は普通にあります（4-65）。
カーネルの検証は中点も見ており、**非多様体 0** が正しい答えでした。

**端点だけで稜を数えない。** この落とし穴は 4-65 に書いてあり、それでも
踏みました。

#### 判断の材料（まだ決めていません）

| 分かったこと | 数字 |
| :--- | :--- |
| **申告された不確かさは当てにならない** | `screw.step` は 1e-6 申告で実際 2.946e-4（**294 倍**）、`linkrods.step` は 2e-5 申告で 4.145e-4（**20 倍**） |
| **1e-3 まで受ければ読める** | `linkrods.step` は立体になり、**位相も無傷** |
| **残るのは1つの検査だけ** | 「境界が曲面から外れる」。最大 4.145e-4 |

**3-2（もっともらしい答えより、できないと言う）との折り合いが要ります。**
いま言えるのは、**「読めない」の中身が公差1つに絞れた**ということです。

**次に決める人へ**: 立体は面ごとに `tolerance` を持っています。**読んだ
ファイルの粗さを、その値に入れて持ち歩く**のが筋に見えます——「全体の公差を
緩める」のではなく、「この面はここまでしか信用できない」と**書いておく**。
ただし下流（分割・縫合・ブーリアン）がその値を見ているかは**測っていません**。

#### 落ちたものは1つもありません

| | 前 | 後 |
| :--- | :--- | :--- |
| 通しテスト | 137 バイナリ / 692 件 / 失敗 0 / 警告 0 | **同じ** |
| `scale_sweep_probe` | 破れ 0・非多様体 0・断り 0 | **同じ** |
| `contact_placement_probe` | 135 ok / 6 断り（未実装 0） | **同じ** |
| `rechained_boolean_probe` | 30演算すべて緑 | **同じ** |
| `foreign_boolean_probe` | 変わらず | **同じ** |


### 4-266. **面が、自分の粗さを持ち歩く**（2026年9月2日、H8）

4-265 で「残るのは公差1つ」まで絞れたので、そこを直しました。**全体を緩める
のではなく、その面がどこまで正しいかを測って持たせます。**

#### 直した2か所

| どこ | 前 | 後 |
| :--- | :--- | :--- |
| `Face::validate_boundary_on_surface` | `tol.linear` だけを見る | **`tol.linear.max(self.tolerance)`** |
| STEP の取り込み | 面の公差に **1e-6 を決め打ち** | **境界の外れを実測して持たせる**（下限 1e-6、上限 1e-3） |

**`regularize.rs` が稜で同じことをしています**（`tol.linear.max(edge.tolerance)`）。
新しい考え方ではありません。

**空試験をしました。** 検証側だけ直して取り込み側を 1e-6 のままにすると、
**数字が1つも動きません**。ビルダーの出力（公差 1e-6）に影響が無いことは、
これで確かめてあります。

**上限は 1e-3 です。** 超える面は公差の話ではなく**別の曲面**なので、
これまでどおり名指しして断ります（4-263）。

#### 読めるようになりました

| ファイル | 前 | 後 |
| :--- | :--- | :--- |
| `screw.step` | 読めない（検証エラー 18 件） | **立体 1 個、面 16、体積 2240.815696** |
| `linkrods.step` | 読めない（検証エラー 122 件） | **立体 1 個、面 37、体積 3.897551** |

**OCCT が配る実物の STEP が2つとも読めて、切れます。**

| ファイル | 和 | 差 | 積 |
| :--- | :--- | :--- | :--- |
| `screw.step` | ok（**メッシュ非多様体 242 本**） | **検証で断り**（体積が合わない） | ok |
| `linkrods.step` | ok | ok | ok（**メッシュ非多様体 2016 本**） |

#### 新しい欠陥が3つ見えました

**読めるようになったので、その先が初めて見えています。**

1. `screw.step` の**和がメッシュ非多様体 242 本**
2. `linkrods.step` の**積がメッシュ非多様体 2016 本**
3. `screw.step` の**差が体積の検証で断られる**

**これは軸を足したときに毎回起きることで、H8 の値打ちそのものです。**

#### 検体の選び方に穴があります

`linkrods.step` の恒等式が **0.000e0** と出ますが、**意味がありません**——
切り手の箱が部品を丸ごと含んでいて、**差が空**（立体 0、体積 -0.000000）に
なっているからです。`0 + |A| - |A| = 0` は、何も測っていません。

**4-211 と同じ間違い**です（「棒を穴に通す」を半径 7 で書いて、触れてすら
いなかった）。**切り手を選び直してください**——部品を横切って、差も積も
中身が残る位置に。

#### 落ちたものは1つもありません

| | 前 | 後 |
| :--- | :--- | :--- |
| 通しテスト | 137 バイナリ / 692 件 / 失敗 0 / 警告 0 | **同じ** |
| `scale_sweep_probe` | 破れ 0・非多様体 0・断り 0（最悪 7.394e-8） | **同じ** |
| `contact_placement_probe` | 135 ok / 6 断り（未実装 0）、非多様体 0 | **同じ** |
| `rechained_boolean_probe` | 30演算すべて緑 | **同じ** |
| `foreign_cross_pair_probe` | 15 組そろい、破れ 0（最悪 9.429e-10） | **同じ** |
| `face_budget_probe`（H5） | 3,854 枚 | **同じ** |

#### H8 について

**「読んだ立体（STEP）を切る」は通りました。** ただし目標は
**「切って恒等式の破れ 0」**です。いまは

- 切り手が悪くて**恒等式が測れていない**（上記）
- **メッシュ非多様体が出ている**

ので、**達成とは書きません**。次にやることは2つです。

1. **切り手を選び直す**（恒等式が意味を持つ位置に）
2. **メッシュ非多様体 242 / 2016 本**を追う。`contact_placement_probe` で
   同じ数字を 4-208 が 0 にしているので、**その道具がそのまま使えます**


### 4-267. **中身の無い恒等式**を、緑と数えていました（2026年9月2日、H8）

4-266 で読めるようになった `linkrods.step` の恒等式が **0.000e0** と出ます。
**何も測っていません。**

#### 測ってみると、答えのほうが間違っています

| | |
| :--- | :--- |
| 部品の bbox | z **[-13.604, 14.922]** |
| 切り手の箱 | z **[-6.473, 7.790]**（差し渡しの半分） |
| 積 | **3.897551 = 部品の体積そのもの** |
| 差 | **立体 0、体積 -0.000000** |

**→ これは誤診でした（4-269）。取り下げます。** はみ出していません——
`Solid::bounding_box()` が**曲面の制御点まで含む**ので、部品の広がりを
14 倍に見積もっていました。本当の広がりは z [0.000, 2.000] で、**切り手が
丸ごと飲み込んでいた**だけです。以下は当時の記録として残します。

**部品は切り手の z 範囲からはみ出しているのに、積が部品全体を返しています。**
分類が「全部が切り手の内側」と言っている、ということです。

体積そのものは疑いませんでした——**2つの物差しで確かめてあります**
（解析 3.897551 / メッシュ 3.963953、三角形 57,904 枚。1.7% 差）。

#### 恒等式は、この誤りを捕まえられません

差が空で積が A 丸ごとなら、

```text
|A＼B| + |A∩B| - |A| = 0 + |A| - |A| = 0
```

**必ず閉じます。** 答えが間違っていても閉じます。**恒等式は、2つの答えが
互いに辻褄が合っているかしか見ていません。**

#### 掃き出しに、そう言わせました

差が空かつ積が A 丸ごとのときは、こう出します。

```text
**この恒等式は中身がありません**——差が空で積が A 丸ごとなので、必ず閉じます。
**切り手が部品を丸ごと含んでいるか、分類が間違っています。**
```

**4-211 と同じ形の落とし穴**です（「棒を穴に通す」を半径 7 で書いて、
触れてすらいなかった。3演算とも「通った」と出ていました）。

**「通った」と「測れた」は違います。**

#### 次の人へ

1. **`linkrods.step` の分類を追ってください。** 部品が切り手からはみ出して
   いるのに全部が内側と判定されています。**入口は「面片をどちらに割り当てて
   いるか」**——`select_operand_faces_after_batch_split` です。
2. **切り手も選び直してください。** いまは「差し渡しの z 半分」ですが、
   読んだ立体は形がまちまちです。**部品の重心を通る平面で切る**ほうが、
   どんな形でも中身のある検算になります。
3. `screw.step` の**メッシュ非多様体 242 本**（和）と `linkrods.step` の
   **2016 本**（積）は、まだ触っていません。

#### H8 について

**「読んで切る」は通りましたが、「恒等式の破れ 0」はまだです。**
いま分かっているのは、**恒等式が閉じていても中身が無い場合がある**という
ことで、これは 9-4 の採点そのものに関わります。**`contact_placement_probe`
など他の掃き出しにも、同じ形の見逃しが無いかを確かめる価値があります。**


### 4-268. 主力の掃き出しにも、同じ見逃しがありました（2026年9月2日）

4-267 で「恒等式が閉じていても中身が無いことがある」と分かったので、
**他の掃き出しにも同じ見逃しが無いか**を数えました。9-4 の採点そのものの話です。

#### `contact_placement_probe` に 1 件

41 配置のうち **1 件**が「差が空・積が A 丸ごと」でした。

```text
IDENTITYWHY sphere x cylinder (tangent along the equator)
            **中身がありません**（差が空、積が A 丸ごと）
```

**これは欠陥ではありません。** 半径 10 の球と同じ半径の同軸円柱で、
**球が円柱に含まれています**——注記に「実測: 立体0つ（球は円柱に含まれる）」
と、当時からそう書いてあります。**差が空になるのが正しい姿**です。

**ただし、そのぶん恒等式は何も測っていません。**

#### 「41 配置で破れ 0」は、実質 40 配置でした

そう表示するようにしました。

```text
**恒等式**: 41 配置で測り、破れ 0 件、残差の最悪 3.900e-8（cone x cylinder (crossed)）。
**中身の無いもの 1 件**（差が空で積が A 丸ごと。必ず閉じるので採点になりません。4-268）。
```

#### 同じ形が、片方では無害で、片方では欠陥を隠していました

| どこ | 差が空になる理由 | 判定 |
| :--- | :--- | :--- |
| `contact_placement_probe` の球×円柱 | **本当に含まれている** | 無害。数えなければよい |
| `read_and_cut_probe` の `linkrods.step` | **分類が間違っている**（部品は切り手からはみ出している） | **欠陥**（4-267） |

**恒等式だけでは、この2つを区別できません。** どちらも `0 + |A| - |A| = 0`
です。**区別するには、外側から見た事実**（部品の bbox が切り手からはみ出して
いる）が要ります。

#### 次の人へ

**「何件で測った」と書くときは、そのうち何件が本当に測れているかも書いて
ください。** 掃き出しの数は、**採点できた数**であって、**流した数**では
ありません。

まだ入れていないもの:

- `scale_sweep_probe`、`rechained_boolean_probe`、`foreign_cross_pair_probe`
  にも同じ数え方を入れる価値があります（**まだ入れていません**）
- **含まれている配置を、恒等式の分母から外す**か、**別の検算**（含まれて
  いるなら積は A に一致するはず、という等式）を当てる

#### 落ちたものは1つもありません

通しテスト **137 バイナリ / 692 件 / 失敗 0 / 警告 0**。
`contact_placement_probe`・`read_and_cut_probe`・`scale_sweep_probe`・
`rechained_boolean_probe` はすべて緑のまま。


### 4-269. `bounding_box()` は立体の広がりではありません。掃き出しが2時間20分止まりました（2026年9月2日）

**4-267 の「分類が間違っている」は誤診です。取り下げます。**

#### 誤診の中身

`linkrods.step` を切ると差が空・積が A 丸ごとになるので、
「部品が切り手からはみ出しているのに全部が内側と分類されている」と書きました。
**はみ出していませんでした。**

| `linkrods.step` | x | y | z |
| :--- | :--- | :--- | :--- |
| `Solid::bounding_box()` | [-3.273, 9.468] | [-14.383, 19.751] | **[-13.604, 14.922]** |
| **境界（ワイヤ）だけ** | [3.125, 8.145] | [2.500, 4.000] | **[0.000, 2.000]** |

**z の差し渡しが 14 倍違います。**

`Solid::bounding_box()` は、面の `bounding_box()` を集めます。そして
`Face::bounding_box()` は、**NURBS の面では曲面の制御点まで**含みます
（`bbox.extend_bbox(&nurbs.bounding_box())`）。**トリムされた自由曲面**
（この部品は次数 6x10 を 6 枚持ちます）では、**トリムの外まで大きく張り出し
ます**。

掃き出しはそこから切り手を作っていたので、**箱が部品を丸ごと飲み込んで**
いました。差が空・積が A 丸ごとは、**正しい答え**です。

**カーネルの面の対の判定は、`face_boundary_bbox`（境界だけ）を使っています**
——そちらが正しいやり方です。私が `Solid::bounding_box()` を「立体の広がり」
と読んだのが間違いでした。

#### 切り手を境界から作ったら、**返らなくなりました**

直すと、`linkrods.step` の切り手は 5.020 x 1.500 x 2.000 になります
（前は 12.742 x 34.134 x 28.526）。**本当に交線を辿ることになります。**

| | 時間 |
| :--- | ---: |
| `screw.step` 和 / 差 / 積 | **24.0 / 20.7 / 7.6 秒** |
| **`linkrods.step` の3演算** | **2時間20分回っても返りません** |

**自由曲面（次数 6x10）を箱で切るのが重い**、というのが H8 の次の壁です。

#### 掃き出しが止まったら、掃き出しではありません

2時間20分のあいだ、**画面には1行も出ていませんでした**（出力が溜め込まれる
ため）。**止まっているのか進んでいるのかが分かりません。**

2つ直しました。

1. **走る前に、走ることを言う**（`print!` + `flush`）。どの演算で止まったかが残ります
2. **演算ごとに待ち時間の上限**（既定 120 秒。`ZENITH_READ_CUT_BUDGET` で変更）。
   返らないものは「**n 秒で返りません**」と出して次へ進みます

**別の糸で走らせて、待つのをやめる**やり方です。糸は止められないので走り
続けますが、プロセスが終われば消えます。

これで掃き出し全体が **230 秒**に収まります。**4-252 の「判定は数秒で付く」
にはまだ遠い**ので、そこは次の宿題です。

#### 黙って落ちる出口を2つ塞ぎました

`intersect_face_supports` の曲面×平面の腕には、**理由を出さずに組を捨てる**
出口があります（`planar_rim_touches_section_circle`）。捨てられた組は交線を
1本も出さないので、**「交線 0 本」という事実だけ**が残ります。
`ZENITH_SSI_WHY=1` で言うようにしました。`planar_face_as_patch` が失敗する
出口も同じです。

**今回は、どちらも鳴りませんでした**——それ自体が測定です（対はもっと手前、
境界の bbox が重ならない段で落ちていました）。

#### 教訓

**同じ夜に3回、同じ間違いをしました。**

| | 疑ったもの | 実際 |
| :--- | :--- | :--- |
| 4-262 → 4-264 | 「ファイルの側が合っていない」 | **こちらの測り方が符号を落としていた** |
| 4-267 → ここ | 「分類が間違っている」 | **こちらが `bounding_box()` を読み違えていた** |
| （4-265 の途中） | 「稜が3回以上使われている」 | **端点だけで数えていた**（4-65 に書いてある） |

**「向こうが悪い」と書く前に、自分の物差しを疑うこと。** 3回とも、
**測り直したら5分で分かりました**。

#### 落ちたものは1つもありません

通しテスト **137 バイナリ / 692 件 / 失敗 0 / 警告 0**。
`contact_placement_probe`（41配置、破れ 0、中身の無いもの 1 件）、
`scale_sweep_probe`（破れ 0・非多様体 0・断り 0）、
`rechained_boolean_probe`（30演算すべて緑）、`foreign_cross_pair_probe` は
すべて前と同じ。


### 4-270. 重いのは**辿り**ではなく、**点→曲面の射影**でした（2026年9月2日、H8）

4-269 で「自由曲面を箱で切ると返らない」と分かったので、**壁時計ではなく
仕事量**で測りました（`zenith_geom::work_counter`）。

#### `linkrods.step` の和（切り手は境界から作った実寸の箱）

| 経過 | 曲面評価 | 辿り | 辿りのニュートン | 種探し | **点→曲面の射影** |
| ---: | ---: | ---: | ---: | ---: | ---: |
| 10 秒 | 28,575,251 | **0** | 0 | 0 | 202,379 |
| 30 秒 | 75,558,115 | 256 | 118,725 | 13 | 635,926 |
| 60 秒 | **255,364,878** | **256** | **118,725** | 13 | **1,905,098** |

**30 秒から 60 秒のあいだ、辿りは 1 回も増えていません**（256 のまま）。
増えているのは**曲面評価**（75M → 255M）と**点→曲面の射影**（636k → 1.9M）
です。

**辿り（SSI）は終わっています。重いのはその後です。**

#### 数が合います

`ExtremumEngine::point_to_surface` は、17x17 の粗い格子（289 回）＋詰め
＋ニュートンで、1回あたり **130 回前後**の曲面評価を使います。

```text
1,905,098 回 × 134 ≒ 2.55 億 = 実測の曲面評価 255,364,878
```

**曲面評価のほぼ全部が、点→曲面の射影から出ています。**

#### なぜ 190 万回も射影するのか

**まだ測っていません。** 候補は、

- **p-curve の導出**（`project_edge_to_nurbs_pcurve`）。次数 6x10 の面では
  折れ線が細かくなり、標本ごとに射影します
- **境界が曲面に乗っているかの検証**（`validate_boundary_on_surface`）
- **面片の内外判定**

**次の人へ**: `point_to_surface` の呼び出し元を数えてください。
`work_counter` は**呼ばれた回数**しか持っていないので、**どこから呼ばれたか**
を分ける口が要ります。それが分かれば、**190 万回のうち何割がどの段か**が
出ます。**そこが H8 の次の一手**です。

#### 測り方について

**壁時計で「重い」と言っても、次に何をするかが決まりません。** 仕事量で
測ったら、**辿りが 30 秒の時点で終わっている**——つまり SSI をいくら速く
しても効かない——ことが1回で分かりました。

**4-77 以来のやり方どおり**です（`work_counter` は環境で 47% ぶれる壁時計の
代わりに入れたもの）。


### 4-271. 射影の**9割は p-curve の導出**でした（2026年9月2日、H8）

4-270 で「重いのは点→曲面の射影」まで絞れたので、**呼び出し元を分ける口**を
足しました。`work_counter` は呼ばれた回数しか持っていなかったので、
どこから呼ばれたかを数えられませんでした。

#### 口を2つ足しました

| 数える口 | どこ |
| :--- | :--- |
| `pcurve_projections` | `project_edge_to_nurbs_pcurve`（p-curve を作る） |
| `boundary_check_projections` | `distance_to_surface`（境界が曲面に乗っているか） |

#### 測った結果

`linkrods.step` の和（切り手は境界から作った実寸の箱）:

| 経過 | 射影 合計 | **うち p-curve** | 境界の検証 | その他 |
| ---: | ---: | ---: | ---: | ---: |
| 30 秒 | 431,382 | **357,463（83%）** | 3,522 | 70,397 |
| 60 秒 | 1,057,769 | **947,549（90%）** | 3,522 | 106,698 |

**9割が p-curve の導出**です。境界の検証は 3,522 回で**増えていません**
（一度作れば終わりなので当然です）。「その他」は `zenith_algo` と
`zenith_tess` から呼ばれるぶんです。

#### だから、見るところは1つです

**`project_edge_to_nurbs_pcurve` が、次数 6x10 の面で標本を置きすぎています。**

その関数は「弦が稜からどれだけ外れるか」で標本を足していきます
（`deflection = tol.linear`、上限 `MAX_POINTS = 4096`）。自由曲面では
**1本の稜に何百点も**置かれ、**1点ごとに射影**します。

**次の人へ**: **1本の稜あたり何点置いているか**を数えてください
（`pcurve_projections ÷ 稜の本数`）。4096 に張り付いているなら、
**上限に当たっているのに満たせていない**——4-258 で見た「細分しても縮まない」
と同じ形かもしれません。**そこを先に確かめてください。**

**速くする前に、そもそも必要な点数かを確かめること。** 4-160 で
「細分が重いのだろう」と書いて外し、実際は earcut だった、という前例が
あります。

#### いま入れたのは数える口だけです

**振る舞いは何も変えていません。** 数えるだけです。

---

### 4-272. **どの計算に何秒使ったか**を、必ず出すようにしました（2026年9月2日）

計算が数時間返らない、という目に何度か遭っています（4-269 は **2時間20分**）。
そのとき知りたいのは2つです。

- **どの計算で止まっているのか**
- それは**遅いだけ**なのか、**進んでいない**のか

**時間だけでは後者が分かりません。** 10 分返らなくても、仕事量が増え続けて
いるなら進んでいます。増えていないなら、**収束しない輪の中**です。

#### 足したもの

**`zenith_geom::progress`**（新しい口）。

| | 中身 |
| :--- | :--- |
| `Heartbeat` | 一区切りに付ける拍動。**経過した秒数と、その間に増えた仕事量**を一定間隔で出し、落ちるときに合計を出す |
| `timed(label, f)` | 一区切りを測って、値と秒数を返す |

**`work_counter` の差分と一緒に出す**のが要点です。曲面評価・射影・辿り・面の
積分が**どれも増えていなければ「進んでいません」と言います**。

#### 数え上げとの使い分けは変えていません

`work_counter` の冒頭には「**速くなったかを壁時計で判定してはいけない**」と
書いてあります（同じ仕事が同じ時間で終わらないため）。**そこは変えていません。**
ここで壁時計を使うのは**別の目的**——**終わらないものを見つける**ためで、
比較のためではありません。文書にもそう書いてあります。

#### 掃き出しに付けました

| 口 | 出るもの |
| :--- | :--- |
| `contact_placement_probe` | 1演算ごとの秒数。最後に**合計と、重い順に5件** |
| `scale_sweep_probe` | **桁ごとの秒数**（3演算の合計）を表の列に |

どちらも `ZENITH_SLOW_SECONDS` を超えた演算を、その場で `SLOW ...` と
名指しします（既定は 30 秒／60 秒）。

#### 出しただけで、1つ分かりました

`cone × sphere` を桁で振ると、**大きい桁ほど遅い**と分かりました。

| 桁 | 100 | 10 | 1 | 0.1 | 0.02 | 0.01 | 0.005 |
| ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| 秒 | **46.5** | 37.1 | 18.4 | 11.9 | 8.8 | **3.7** | 7.4 |

**12 倍の開き**です。同じ形を大きさだけ変えているので、**仕事量が桁に依って
いる**ことになります。公差が絶対値なので、大きい模型ほど相対的に細かく
刻んでいる——という筋が立ちますが、**まだ測っていません。推測です。**

#### 途中でやらかしたこと

拍動の糸を `sleep` で刻んで止めていたので、**落ちるたびに最大 0.2 秒待って**
いました（終了 0.40 秒 と `SLOW` の 0.2 秒 が食い違って気づきました）。
141演算なら 28 秒になります。**待たずに起こす**ようにしました。

**測る口が測る対象を遅くしていないか**は、付けたあとに必ず見てください。

---

### 4-273. **届かない目標を追って、稜あたり 4,000 回射影していました**（2026年9月2日、H8）

4-271 の宿題（**1本の稜あたり何点置いているか**）を数えました。

#### 実測（`linkrods.step`、90 秒ぶん）

| 点数 | 本数 |
| ---: | ---: |
| 129 | 91 |
| 513 | 222 |
| 1025 | 37 |
| **4096（上限）** | **482** |

**482 本が上限に張り付いています。** 1点ごとに射影を1回払うので、
**結論の出ていない稜に稜あたり 4,000 回**——4-271 で「射影の 9割が p-curve の
導出」と測った、その中身です。

#### 直したもの その1: **答えが決まったら止める**

この関数は最後に `max_distance > on_surface_limit` なら `Err` を返します。
`max_distance` は最大値なので**減りません**——**一度超えたら、あと何点
置いても結論は変わりません**。止めずに割り続けていました。

**周回の先頭に置いただけでは、1本も減りませんでした**——1周のうちに上限まで
行けてしまうからです。**割るたびに見る**ようにして直りました。

#### 直したもの その2（**本命**）: **届かない目標を追わない**

止めるようにしても、**上限に張り付く稜は 2,525 本**残りました。中を見たら、

| | 本数 |
| :--- | ---: |
| 射影が受け入れ幅 1e-5 を**超えている**（＝上の修理が効く） | **23** |
| 射影が **1e-5 の内側**（＝正しく射影できている） | **2,502** |

**割るのが無駄だったのではありません。割っても届かないのです。**

割って縮むのは**弦の誤差**です。`strayed` は「uv の中点を曲面に写した点」と
「稜の中点」の距離なので、**稜そのものが曲面から離れているぶんが床**に
なります。稜が曲面から 8e-6 離れていれば、弦を 1e-6 にすることは**原理的に
できません**。

止めどころを `deflection.max(稜と曲面の隔たり × 1.05)` にしました。
**自分で作った立体では隔たりがほぼ 0 なので、何も変わりません。**

#### 実測（同じ 90 秒）

| | 前 | 後 |
| :--- | ---: | ---: |
| 上限 4096 に張り付いた稜 | **2,525 本** | **0 本** |
| その稜が置く点数 | 4096 | **163**（25 分の1） |
| 90 秒で導出できた p-curve | 134,055 | **266,044**（約2倍） |

#### 門（すべて据え置き）

| | |
| :--- | :--- |
| テスト | **137 バイナリ / 693 件 / 失敗 0 / 警告 0** |
| `contact_placement_probe` | 47配置141演算で **135 ok / 6 断り**（未実装の断り 0）、B-Rep・メッシュとも非多様体 0、恒等式 41配置で破れ 0（最悪 3.900e-8）、中身の無いもの 1 件 |

#### 4-272 の口が、初日から効きました

掃き出しの時間の内訳が出るようになったので、**どこに消えているか**が
分かります。

```text
**時間**: 演算 141 件で合計 961.8 秒（1件あたり平均 6.82 秒）。重い順に:
     115.8 秒  box x torus (top tangent plane) union（全体の 12.0%）
      63.7 秒  box x torus (top tangent plane) difference（全体の 6.6%）
      26.5 秒  torus x torus (crossed axes) difference（全体の 2.8%）
```

**1配置で全体の 18.6%** を使っています。次に速さを詰めるなら、
**まずこの配置**です。

---

### 4-274. **一度作った p-curve を、訊かれるたびに作り直していました**（2026年9月2日、H8）

4-273 で `linkrods.step` が動くようになったので、**拍動**（4-272）を
`read_and_cut_probe` にも付けて中を見ました。

#### まず、止まってはいませんでした

```text
PROGRESS [linkrods.step union] 経過 20 秒: 曲面評価 +9172271、射影 +218745（うち p-curve +107509）、辿り +118725、面の積分 +18  進んでいます
PROGRESS [linkrods.step union] 経過 40 秒: 曲面評価 +42563611、射影 +732624（うち p-curve +714217）、辿り +0、面の積分 +0  進んでいます
PROGRESS [linkrods.step union] 経過 60 秒: 曲面評価 +37666015、射影 +645348（うち p-curve +629090）、辿り +0、面の積分 +0  進んでいます
```

**最初の 20 秒で辿りも積分も終わり、あとはずっと p-curve の射影**です
（20秒あたり 60〜70万回）。**収束しない輪ではありません。**

#### 数えたら、作り直していました

`Face::pcurves` は `&self` を取ります。`pcurves` 欄が空だと**訊かれるたびに
作ります**。数える口を足しました（`pcurve_derivations` と
`pcurve_cache_hits`）。

```text
p-curve 作り直し +4493（持っていた +666）
```

**作り直し 4,493 回に対し、持っていたのは 666 回。** 1回あたり射影を
約 62 回払うので、**その時間のほとんどが作り直しに消えていました。**
`zenith_algo` からは `attach_*_pcurves` を**一度も呼んでいません**でした。

#### 直したもの: 面が、一度作ったものを覚える

`Face` に `OnceLock<FacePcurves>` を足しました。**同じ面からは同じものしか
出ない**ので、覚えても答えは変わりません。形の一部ではないので、
**比較にも直列化にも入れません**。

**複製で落としたら、効きませんでした**（作り直し 5,532、持っていた 9,069）。
この経路は面をよく複製します。**既にある `pcurves` 欄は複製している**ので、
扱いを揃えました。

#### 実測（`linkrods.step` の和、20〜40 秒の窓）

| | 手を入れる前 | 覚える（複製で落とす） | **覚える（複製でも持つ）** |
| :--- | ---: | ---: | ---: |
| p-curve 作り直し | 4,493 | 5,532 | **3,205** |
| 持っていた | 666 | 9,069 | **16,505** |
| p-curve の射影 | 263,620 | 263,620 | **173,356**（−34%） |

**残る 3,205 回は、本当に新しく作られた面**（割った断片）です。そこは
作り直しではありません。

#### 門（すべて据え置き）

| | |
| :--- | :--- |
| テスト | **137 バイナリ / 693 件 / 失敗 0 / 警告 0** |
| `contact_placement_probe` | 47配置141演算で **135 ok / 6 断り**（未実装 0）、B-Rep・メッシュとも非多様体 0、恒等式 41配置で破れ 0（最悪 3.900e-8） |
| 同・時間 | 961.8 秒 → 974.9 秒（**測定の揺れの範囲**。この経路は面を複製しないので、効きません） |

#### `screw.step` が切れるようになりました

4-273 と合わせて、**和 2.1 秒・積 1.5 秒で立体が返ります**（差は検証で
断られます）。**H8 で初めて、読んだ立体が切れました。**

| 検体 | 和 | 差 | 積 |
| :--- | :--- | :--- | :--- |
| `screw.step` | **ok**（体積 8972.57、メッシュ非多様体 242本） | 断られた（検証） | **ok**（体積 1660.93、非多様体 0） |
| `linkrods.step` | まだ返りません | 同 | 同 |

**恒等式はまだ測れません**（3演算そろわないため）。次は `screw` の差
（検証の中身）と、`linkrods` に残る 3,205 回の作り直しです。

---

### 4-275. `screw.step` の差が断られる中身——**符号付きの和だけが合っています**（2026年9月2日、H8）

4-274 で `screw.step` の和と積は通るようになりました。**残る差**を見ます。

#### まず、断り文が読めていませんでした

probe が **90 字で切り詰めて**いて、肝心の末尾が消えていました。

```text
断られた: Exact B-Rep boolean produced a result that fails verification: volume A=2240.815694, B=839
```

**直す側が読むのは末尾です。** 切り詰めをやめ、`; ` で改行するようにしました。

```text
Exact B-Rep boolean produced a result that fails verification:
  volume A=2240.815694, B=8392.677697, result=579.890214 over 2 solid(s)
0 invalid shell(s)
membership 0 of 362 classified samples disagreed (384 drawn):
  result solid 1 encloses volume -2.380901e2, which is not positive:
  it is inside-out or collapsed
```

**内外判定は 362 標本すべて一致しています。** 殻も 0 件が不正。**落ちているのは
「立体 1 が裏返っている」の1点だけ**です。

#### 体積の算術が、そこだけ合っています

| | |
| :--- | ---: |
| 立体 0 | **+817.980336** |
| 立体 1 | **−238.090119** |
| 符号付きの和 | **579.890217**（検証の report と一致） |
| 積（別に通っている） | 1660.925480 |
| **和 ＋ 積** | **2240.815694** |
| **A** | **2240.815694** |

**5桁ではなく、表示した全桁が一致します。** 偶然ではありません。

#### しかし、空洞ではありませんでした

「立体 1 は空洞（内側の殻）なのに、別の立体として返している」と読めます。
**測ったら違いました。**

| 立体 | 面 | 範囲 | 相手の中か |
| ---: | ---: | :--- | :--- |
| 0 | 10 | (-22.898, -5.826, -34.564)〜(-12.898, 4.174, 0.436) | **いいえ** |
| 1 | 12 | (-27.898, -10.826, -33.644)〜(-7.898, 9.174, 7.731) | **いいえ** |

**立体 1 は立体 0 より広く**（x も y も）、**重心はどちらも相手の中に
ありません**。空洞なら材料の中に無ければおかしいので、**この読みは外れ**です。

**稜の上の点で試して一度間違えました**——境界の点は包含の判定に使えません。
重心で取り直しています。

#### いま言えること／言えないこと

**言えること**（測りました）:

- 断りの理由は**1点だけ**（裏返り）。内外判定も殻も通っている
- **符号付きの和が、恒等式を全桁で満たす**
- 2つの殻は**入れ子でも、重心どうしの重なりでもない**

**言えないこと**（推測です。**測ってから書いてください**）:

- 立体 1 が「二重に覆っている片」なのか（4-137 で一度見た形です）
- それとも積 1660.925480 の側にも打ち消し合う誤りがあるのか

**次に測ること**: 2つの殻が**面を共有しているか**（同じ面が両方に入って
いれば二重被覆です）。`ZENITH_FACE_OWNER_WHY` と同じ要領で、殻ごとの面の
番号を並べれば分かります。

#### probe に足したもの（残してあります）

- **断り文を切り詰めない**（`; ` で改行）
- 裏返りで断られたときだけ、**検証を通さない口で取り直して**、立体ごとの
  体積・面の数・内側の殻・範囲・**相手の中にあるか**を出す

---

### 4-276. **検証の内外判定が、この立体では当てになりません**（2026年9月2日、H8）

4-275 の宿題を2つ潰しました。ひとつは空振り、**もうひとつが重い所見**です。

#### 二重被覆ではありませんでした

2つの殻が同じ面を共有していれば二重被覆です（4-137 で一度見た形）。
番号で突き合わせました——**共有 0 枚**。違います。

#### 「どちらの総体積が正しいか」を、ブーリアンに依らず決めようとしました

符号付きの和（**579.89**）と絶対値の和（**1056.07**）のどちらが |A＼B| なのかは、
**A と B だけを見れば決まります**。A の範囲に格子で点を撒き、**A の中で B の外**
の割合を数えれば済みます（種を使わないので決定的です）。

**その口が、自分で「使えない」と言いました。**

```text
**点で数えた体積**: A 3820.503（面積分では 2240.816、ずれ 70.5%）、A＼B 2138.097
**この数え方は使えません**——メッシュに穴があると光線の数え方が狂います
```

**A 自身が 70.5% ずれます。** 点の内外は光線の交差回数で決めるので、
**メッシュに穴があると数が合いません**。`screw.step` の表示メッシュは
非多様体（4-274 で和が 242 本）なので、当然でした。

**口を足したら、まず自分を検算させること。** ここは A の体積が面積分で
分かっていたので、その場で気づけました。

#### そして、これは 4-275 の読み直しを迫ります

**ブーリアンの検証も、同じ数え方を使っています**（`is_point_inside_mesh`）。

```text
membership 0 of 362 classified samples disagreed (384 drawn)
```

**この「0 件不一致」は、この立体では意味がありません。** 内外判定が 70% 
外れる相手で「全部一致した」と言っているので、**通っていること自体が
情報になっていない**のです。

**4-275 で「内外判定も殻も通っている。落ちているのは裏返りの1点だけ」と
書いたのは、書き直します。** 正しくは:

| 検査 | この立体での意味 |
| :--- | :--- |
| 殻が閉じているか | **有効** |
| 体積が正か（裏返り） | **有効**（面積分なのでメッシュに依りません） |
| 内外の標本が一致するか | **無効**（穴あきメッシュの上で数えています） |

#### 次に触る人へ

**H8 の本題（読んだ立体を切って恒等式を見る）には、順番があります。**

1. **読んだ立体のメッシュを水密にする**（`screw.step` で非多様体 242 本）。
   ここが直らないと、**内外判定を使う検査が全部あてになりません**
2. そのうえで 4-275 の裏返りを見る

**1 を飛ばして 2 をやると、当てにならない数字の上で推測することになります。**
今日はそれを1回やりかけました。

#### probe に足したもの（残してあります）

- **殻どうしで同じ番号の面を数える**（二重被覆の判定）
- **点で数えた体積**と、**面積分との突き合わせ**。ずれが 2% を超えたら
  **「この数え方は使えません」と自分で言います**

---

### 4-277. **読んだ立体のメッシュに穴が開く理由**を名指ししました（2026年9月2日、H8）

4-276 で「まずメッシュを水密にする」と順番を決めたので、そこから始めました。

#### 潰した候補（どれも実測）

| 疑い | 実測 | 判定 |
| :--- | :--- | :--- |
| 稜が面どうしで共有されていない | **全 37／114 本がちょうど2面**、1面だけのものは 0 本 | **違う** |
| 解析曲面が別の道でメッシュ化されている | 面の種類は**平面と NURBS だけ**（縫合が扱える） | **違う** |
| 面が「共有しない経路」へ落ちている | `ZENITH_TESS_WHY` の既存の口は**1件も鳴らない** | **鳴っていなかっただけ** |
| 落とす境界の点が面ごとに違う | 落ちるのは各稜の step 0 だけ | **違う** |
| 殻どうしが同じ面を持つ（二重被覆） | 共有 0 枚 | **違う** |

#### 診断の無い分岐がありました

穴はすべて**1枚の面だけが出した稜**で、本数は 24／48／96 と**刻み数の倍数**
です。そこで座標で待ち伏せると（`ZENITH_RING_WATCH`）、

```text
RINGWATCH 面 2 が 稜 1 の step 23/24 として出した (-26.987162494,0.423702928,5.720052044)
```

**面 2 しか出していません。** 稜 1 は面 2 と面 12 が共有しているのに、
**面 12 は 1点も出していません**でした。

`tessellate_face_stitched` には受け皿へ落ちる道が3本あり、**2本には診断が
ありましたが、1本には無い**——`face.pcurves()` が `Err` を返す道です。
口を足したら、すぐ出ました。

```text
RINGWHY 面 10 は p-curve を作れないので共有しない経路へ: Edge 7 projection to NURBS surface exceeds tolerance; max distance 1.998029e-4
RINGWHY 面 12 は p-curve を作れないので共有しない経路へ: Edge 1 projection to NURBS surface exceeds tolerance; max distance 7.281697e-5
```

**これが原因です。** 稜が曲面から **7.3e-5〜2.0e-4** 離れている面は
**p-curve を作れず**（受け入れ幅は**絶対 1e-5**）、**黙って**共有しない経路へ
落ちます。そこは面ごとに独立に標本するので、隣と点が合わず穴になります。

**4-266 は検証側だけを面の粗さに合わせていました。** p-curve を作る側は
全体の公差のままで、**同じ面が「検証は通るのに p-curve は作れない」**と
いう食い違いになっていました。

#### 直そうとして、2回とも読めなくなりました（**入れていません**）

| 手 | 実測 |
| :--- | :--- |
| `tol.linear` を面の粗さまで上げる | **両方のファイルが読めなくなる**。`tol.linear` は**受け入れ幅と、どこまで細かく作るかの両方**を決めるので、粗い p-curve ができて今度は検証が落ちる（`differs from 3D edge by 1.6e-4 (allowed 1.3e-4)`） |
| **受け入れ幅だけ**を面の粗さに合わせる | **やはり読めなくなる**。作れるようになった p-curve を、検証が**もっと厳しい物差し**（面の粗さの2倍）で比べるため |

**筋は通っていますが、片側だけ動かすと必ずもう片側で落ちます。**

#### 次に触る人へ

**受け入れ幅と検証の物差しは、同じものでなければなりません。** いまは
別々に決まっていて、その隙間に「作れないから黙って落ちる」面が入ります。

- `match_nurbs_boundary_pcurve` の `on_surface_limit`（**絶対 1e-5**）
- `Face::validate_boundary_on_surface` の許容（**`tol.linear.max(self.tolerance)`**。4-266）

**揃えるなら、p-curve 自身の近似誤差も勘定に入れる必要があります**——
稜が 7e-5 外れている面で作った p-curve は、**3e-4 ずれます**（折れ線の誤差が
上に乗ります）。**「稜の外れ」と「p-curve の粗さ」は別の量**で、いまはどちらも
1つの数字で見ています。**そこを分けるのが本筋**だと思います。

#### 残したもの

- **p-curve が作れず受け皿へ落ちる面を名指しする**（`ZENITH_RING_WHY`／`ZENITH_TESS_WHY`）
- probe に**水密の検算**（穴と重なりの本数、穴の一例の座標）、**稜を使う面の一覧**、
  **面の種類の内訳**

---

### 4-278. **物差しを揃えたら、隠れていた検査が動き出しました**（2026年9月2日、H8）

4-277 が「受け入れ幅と検証の物差しは同じものでなければならない」で終わって
いたので、そこを揃えました。**入れていませんが、分かったことが2つあります。**

#### 揃えた2か所

| どこ | 前 | 後 |
| :--- | :--- | :--- |
| `Face::validate_pcurves` | `tol.linear` だけ（4-266 は**姉妹関数だけ**を直していました） | `tol.linear.max(self.tolerance)` |
| p-curve を作るときの受け入れ幅 | **絶対 1e-5** | `tol.linear.max(self.tolerance) * 10` |

**細かさ（`deflection`）は `tol` のままにしました。** 両方緩めると粗い
p-curve ができて検証が落ちます（4-277 で実測済み）。

#### 効きました

| ファイル | 前（検証の誤り） | 後 |
| :--- | ---: | ---: |
| `screw.step` | 46 件 | **3 件** |
| `linkrods.step` | 90 件 | **12 件** |

#### 残った 3 件は、**別の欠陥**でした

```text
Face 3 planar p-curve loop is inconsistent with face orientation; oriented area -8.537417e-1
Face 4 planar p-curve loop is inconsistent with face orientation; oriented area -8.537417e-1
Face 5 planar p-curve loop is inconsistent with face orientation; oriented area -8.220581e-1
```

**p-curve の輪の巻き方向が、面の向きと合っていません**（4-46 の族）。

**これは新しく壊れたのではありません。** その検査は

```rust
let Ok(pcurves) = face.pcurves(tol) else { return; };
```

で始まります。**p-curve が作れない面は、黙って検査を飛ばしていました。**
作れるようにしたので、**初めて動いた**のです。

**「読めていた」の中身が変わります**——これまでは、平面の輪の巻き方向を
**この3枚については一度も見ていませんでした**。

#### 入れていない理由

**揃えると、2つのファイルが読めなくなります。** 残る 3 件で断られるからです。
**門が緑にならないものは入れない**、という決まりに従いました。

#### 追記: **その3件は、読んだあとには出ません**（4-279）

巻き方向を probe で測る口を足して、**HEAD の状態で数えました**。

```text
screw.step     平面の輪: **向きが合わないもの 0 枚**
linkrods.step  平面の輪: **向きが合わないもの 0 枚**
```

**0 枚です。** つまり 4-278 の 3 件は**取り込みの検証の時点**でだけ出ます
——probe が見ているのは `Regularizer::hold_like_our_own` を通した**あと**の
立体で、そこでは既に揃っています。

**取り込み側を測らないと、この 3 件は追えません。** 平面の p-curve は公差に
依らないので、**物差しを変えて 3 件が出るようになった理由もまだ不明です**。

#### 次に触る人へ（**ここから続けられます**）

1. **取り込みの検証の時点で**、平面の輪の巻き方向を出す
   （`crates/zenith_topo/src/shell.rs` の `oriented_area <= tol.parametric`）。
   probe の口は `Regularizer` の**あと**を見ているので、そこでは 0 枚です。
   STEP は面の向きを `.T.`／`.F.` で持ち、**曲面の側にも向きがあります**。
   二重に適用していないか、3D のワイヤの巻き方向と平面の法線を突き合わせて
   確かめてください
2. そのうえで 4-278 の2か所を揃えれば、**穴 326／1330 本**が減るはずです
   （p-curve が作れれば、共有しない経路へ落ちなくなるので）

**この順番でないと進みません。** 揃える前に穴を追うと、原因が
「作れないから落ちている」ところで止まります（4-277）。

---

### 4-280. **取り込みが持たせた p-curve が、3D のワイヤと逆向き**でした（2026年9月2日、H8）

4-279 の宿題（取り込みの検証の時点で巻き方向を測る）をやりました。
**4-278 を入れられない理由が、これで名前を持ちました。**

#### 訂正（4-281）: **この3枚は平面ではありません**

**下の読みは誤りです。** 検証の文面が `planar p-curve loop` と書いていたので
平面だと思って進めましたが、**その検査は NURBS の面も見ます**。実測で出た
3枚は**すべて NURBS** でした（4-281 で確かめました）。

**平面についての測定（10枚すべて規約どおり）は正しく、そして無関係でした。**
文面は直しました。以下は、その取り違えを含んだままの記録です。

#### まず、3D のワイヤは正しく巻いています（**平面の話。この件とは無関係でした**）

STEP の規約はこうです——境界は**面の法線**まわりに反時計回り。面の法線は
`same_sense` が `.F.` なら曲面の法線の逆。したがって**曲面の法線まわりでは、
`.T.` は反時計回り、`.F.` は時計回り**。

取り込みの時点で、平面の面すべてについて突き合わせました。

```text
ORIENTWHY 面 #14: same_sense .F.、巻きと曲面の法線の内積 -0.9xxx → 規約どおり
（10 枚すべて「規約どおり」）
```

**3D のワイヤは正しい**のです。

#### 検証が見ているのは、別のものでした

```text
ORIENTWHY 面 3: p-curve は **ファイル／取り込みが持たせたもの**、符号付き面積 -8.537417e-1、面の向き 逆、ワイヤの法線と平面の法線の内積 +0.966209
```

**検証は p-curve の符号付き面積で見ます。** そしてその p-curve は
**その場で投影したものではなく、取り込みが持たせたもの**でした。

**3D のワイヤと、持たされた p-curve の巻きが逆**です。同じ面について、

| 見るもの | 言っていること |
| :--- | :--- |
| 3D のワイヤ（取り込み時に測定） | 規約どおり |
| 取り込みが持たせた p-curve | **逆** |

#### なぜ今まで見えなかったか

その検査は `let Ok(pcurves) = face.pcurves(tol) else { return; };` で始まります。
**平面の面では p-curve は作れる**ので、この検査自体は前から動いていました。
ただし**読めていた**（＝検証を通っていた）——なぜなら、**4-278 を入れる前は
別の誤りが先に 46 件出ていて**、この 3 件はその中に埋もれていたからです。
4-278 で 46 → 3 になって、**初めて単独で見えました**。

#### 次に触る人へ

**直す場所は、取り込みが p-curve を持たせるところです。**

1. `crates/zenith_io/src/step_import.rs` で、面に `pcurves` を持たせている
   ところを探す（**ファイルの p-curve をそのまま使っているのか、こちらで
   投影しているのか**）
2. `FACE_BOUND` の旗で**ワイヤを逆順にしたとき、p-curve も一緒に逆にして
   いるか**を確かめる。**ワイヤだけ反転させれば、ここがずれます**
3. 直ったら 4-278 の2か所を揃える。**穴 326／1330 本が減るはず**です

**旗は2つあります**（`ADVANCED_FACE` の `same_sense` と `FACE_BOUND` の向き）。
取り込みは後者だけをワイヤに適用し、前者は `Face::orientation` に入れています。
**p-curve 側にどちらが効いているかを、まだ誰も確かめていません。**

#### 残したもの

`ZENITH_ORIENT_WHY=1` で、

- **取り込みの時点**: 面ごとに `same_sense` と、3D の巻きと曲面の法線の内積、
  規約どおりかどうか
- **検証の時点**: 符号付き面積、面の向き、3D の巻きとの内積、そして
  **その p-curve が持たされたものか、その場で投影したものか**

---

### 4-281. **「planar」と書いてある検査が、NURBS の面も見ていました**（2026年9月2日、H8）

4-280 で「取り込みが持たせた p-curve が 3D のワイヤと逆向き」と書きました。
**平面の面の話だと読んでいましたが、違いました。**

#### 取り違えの中身

断り文はこうです。

```text
Face 3 planar p-curve loop is inconsistent with face orientation
```

`planar` と書いてあります。ところが**この検査は `Plane` と `Nurbs` の
両方を見ます**（`validate_planar_face_orientation`）。種類を出したら、

```text
ORIENTWHY 面 3（**NURBS**）: 符号付き面積 -8.537417e-1、面の向き 逆
ORIENTWHY 面 4（**NURBS**）: 符号付き面積 -8.537417e-1、面の向き 逆
ORIENTWHY 面 5（**NURBS**）: 符号付き面積 -8.220581e-1、面の向き 逆
```

**3枚とも NURBS** でした。4-280 で測った「平面は 10 枚すべて規約どおり」は
**正しく、そしてこの件とは無関係**です。

**断り文の名前を、実物に合わせました**（`planar` / `NURBS` / `surface`）。

#### では何が起きているのか

NURBS の面では、**UV の向きはこちらが決めます**（STEP の解析曲面を
復元するとき、パッチをどう張るかで u と v の取り方が変わります。4-260〜4-264）。

- 面は `.F.`（逆）
- p-curve の輪は UV で**反時計回り**（面積 +0.85）
- 規約は「逆の面なら時計回り」

**復元したパッチの UV の向きが、STEP の曲面のそれと合っていない**——という
筋です。**まだ測っていません。推測です。**

#### 次に触る人へ

1. `get_surface_for_boundary` が STEP の解析曲面から NURBS パッチを張る
   ところで、**u と v の取り方**を見る（円柱・円錐・トーラスで別々です）
2. 復元したパッチの `∂S/∂u × ∂S/∂v` が、**STEP の曲面の法線と同じ側**を
   向いているかを測る。**逆なら、そこが原因**です
3. 直したら 4-278 の2か所を揃える。**穴 326／1330 本が減るはず**です

#### 今日の教訓

**断り文の名前を信じて、種類を確かめませんでした。** 「planar」と書いてあれば
平面だと思うのが普通ですが、**そこで 1 手ぶん無駄に**しています。
**名前と実物は、その場で突き合わせてください。**

---

### 4-282. **曲面の上の輪の巻きを、平面のやり方で測っていました**（2026年9月2日、H8）

4-281 で「復元したパッチの UV の向きを測れ」と書いたので、測りました。
**測り方が間違っていて、1手ぶん遠回りしました。**

#### 測って、そのまま信じかけたもの

取り込みの時点で、面ごとに「境界の巻き」と「パッチの法線」を突き合わせる口を
足しました。

```text
ORIENTWHY 曲面 #709（TOROIDAL_SURFACE）: same_sense .F.、巻きと **パッチの** 法線の内積 +0.9999 → **規約が破れています**
（4枚が「破れている」、33枚が「規約どおり」。破れた3枚はすべて TOROIDAL_SURFACE）
```

**「トーラスの復元だけが UV を裏返している」と読めました。** そこで、
**張ったパッチの法線を測って、裏返っていたら小円を逆に回して張り直す**
という直し方を入れました。

#### **裏返っていませんでした**

```text
ORIENTWHY   トーラスの復元: 法線と外向きの内積 +1.0000（そのまま）
（12枚すべて +1.0000）
```

**パッチの法線は、はじめから外向きです。** 張り直しは1度も起きません。

#### 測り方が不正でした

巻きを `Σ aᵢ × aᵢ₊₁`（頂点の外積の和＝ベクトル面積）で測っていました。
**これは平らな輪でしか意味を持ちません。** トーラスの面の境界は曲面の上を
うねるので、ベクトル面積はどこを向くか決まりません。

**「+0.9999 だから規約が破れている」は、読み取れない数字を読んだものでした。**
口ごと外しました（平面のものは有効なので残してあります）。

#### いま言えること

| | |
| :--- | :--- |
| 復元したトーラスのパッチの法線 | **外向き**（12枚すべて +1.0000）。正しい |
| 平面の面の巻き | **10枚すべて規約どおり**（4-280） |
| 曲面の面の巻き | **まだ測れていません**（平面のやり方は使えません） |
| 検証が落ちる3枚 | **NURBS**（4-281）。UV の符号付き面積が `.F.` の面で正 |

#### 次に触る人へ

**曲面の上の輪の巻きは、UV で測ってください。** 3D のベクトル面積は使えません。
検証（`validate_planar_face_orientation`）が既に UV でやっているので、
**そちらが正しく、取り込み側に同じものを持ち込もうとしたのが間違い**でした。

残る問いは1つです——**`.F.` の面で、UV の輪が反時計回りになるのはなぜか**。

- p-curve は `Face::new` が**その場で投影して**持たせています
  （ファイルの p-curve ではありません。4-280 の「取り込みが持たせたもの」は
  この意味です）
- 投影は 3D のワイヤを UV へ写します。**写像の向きは、パッチの UV の取り方で
  決まります**
- **`.F.` の面では、STEP のワイヤは曲面の法線まわりに時計回り**のはずです

**この3つのうちどれが破れているかを、UV で1枚ずつ確かめるところから**です。

---

### 4-283. 矛盾が**1点に絞れました**（2026年9月2日、H8）

4-282 で「曲面の輪は UV で測れ」と決めたので、UV で測りました。
**これで、食い違っている場所が1つに定まりました。**

#### 測ったこと（3枚とも同じ）

| 見たもの | 実測 |
| :--- | :--- |
| `ADVANCED_FACE` の `same_sense` | **`.F.`** |
| `FACE_BOUND` の旗 | **`.T.`**（＝こちらはワイヤを反転していません） |
| 復元したパッチの UV の手 | **右手系**（`∂u×∂v` と法線の内積 **+1.0000**） |
| 復元したパッチの法線 | **外向き**（トーラスの芯から外へ。+1.0000） |
| p-curve の輪の符号付き面積 | **正**（＝UV で反時計回り） |

#### なぜ矛盾なのか

STEP の規約はこうです。

1. 境界は、**面の法線**まわりに反時計回り
2. 面の法線は、`same_sense` が `.F.` なら**曲面の法線の逆**

したがって `.F.` の面では、**曲面の法線まわりに時計回り**。UV が右手系なので、
**符号付き面積は負**になるはずです。

**測ったのは正でした。**

#### 消えた容疑者（すべて実測）

| 疑い | 実測 | 判定 |
| :--- | :--- | :--- |
| トーラスのパッチの UV が裏返っている | 右手系（+1.0000） | **違う** |
| パッチの法線が内向き | 外向き（+1.0000。12枚すべて） | **違う** |
| `FACE_BOUND` の旗でこちらが反転している | 旗は `.T.`。反転していない | **違う** |
| p-curve がファイル由来 | `Face::new` が**その場で投影**している | **違う** |
| 平面の面の巻きが崩れている | 10枚すべて規約どおり | **違う**（無関係） |

#### 残っているのは1つです

**こちらが持っているワイヤの向きが、ファイルに書いてある向きと同じか。**

投影は向きを変えません。パッチも正しい。旗も反転していない。**残るのは
「`EDGE_LOOP` を読んで `Wire` にするまでの間に向きが変わっていないか」**
だけです。

#### 追記: **3枚とも「紡錘トーラス」でした**

生テキストを開いたら、いきなり出ました。

```text
#709 = ADVANCED_FACE('',(#710),#482,.F.);
#482 = TOROIDAL_SURFACE('',#483,8.25,54.873718663856);
#803 → #392 = TOROIDAL_SURFACE('',#393,8.25,54.873718663856);
#874 →  #81 = TOROIDAL_SURFACE('',#82,8.25,54.873718663856);
```

**大半径 8.25 に対して、小半径 54.87。** 小さいほうが大きいので、これは
**紡錘トーラス**（自分自身を貫くトーラス）です。**4-264 で扱ったのと同じ族**
です。

**私の「外向き」の測り方が、そもそも当てはまりません。** 芯の円（半径 8.25）
から外へ、という測り方をしましたが、**紡錘トーラスでは面が軸をまたぐ**ので、
そこでは「芯から外」が曲面の法線の向きと一致しません。

**つまり 4-283 の表の「パッチの法線は外向き（+1.0000）」も、この3枚については
読めない数字でした。** 4-282 と同じ間違いを、**別の場所でもう一度**やって
います——**ふつうの形を前提にした測り方を、そうでない形に当てた**のです。

#### 次に触る人へ

**まず、紡錘トーラスで「曲面の法線」がどちらを向くかを決めてください。**
ISO 10303-42 の `toroidal_surface` は `major_radius < minor_radius` を
禁じていません。**そこが決まらないと、`same_sense` の意味も決まりません。**

そのあとで、`screw.step` の面 `#709` について

1. `EDGE_LOOP` の `ORIENTED_EDGE` の並びと `.T.`／`.F.` を生テキストで書き出す
2. こちらの `Wire` の稜の並びと向きを出して、**1本ずつ突き合わせる**

診断の口は `ZENITH_ORIENT_WHY=1` に揃えてあります（取り込み・検証の両方）。

**4-264 を先に読んでください。** 紡錘トーラスの軸をまたいだ側は、
そこで一度扱っています。

#### 直った先に何があるか

これが直れば **4-278 の2か所を揃えられます**。そうすると、

- `screw.step` の穴 **326 本**、`linkrods.step` の **1330 本**が減るはず
- 内外判定を使う検査が**初めて意味を持ちます**（4-276）
- **H8 の本題**（読んだ立体を切って恒等式を見る）に、ようやく手が届きます

---

### 4-284. **紡錘トーラスを裏返すと、法線の側も裏返っていました**（2026年9月2日、H8）

4-283 で「3枚とも紡錘トーラス」と分かったので、そこから追いました。

#### 見つけたこと

4-264 は、紡錘トーラスの「軸をまたいだ側」を**芯の符号を反転して角度を π
ずらす**ことで同じ道に乗せました。**形はそれで合います。**

**しかし `∂u×∂v` の向きが逆になります。** `same_sense` は「曲面の法線に
対して」決まるので、そこが逆だと**境界の巻きの規約がひっくり返り**、
`p-curve loop is inconsistent with face orientation` で断られます。

**裏返した面は、ちょうど 3 枚**でした（`ORIENTWHY` で数えました）。
4-283 で断られた 3 枚と、**同じ 3 枚**です。

#### 直し

**小円を逆に回して打ち消します。** 形は同じで、向きだけが戻ります。

```rust
let (start_minor, minor_sweep) = if flip {
    (start_minor + minor_sweep, -minor_sweep)
} else {
    (start_minor, minor_sweep)
};
```

#### 途中で外したもの

**「張ったパッチの法線を測って、裏返っていたら張り直す」**という自動修正を
先に入れていました。**外しました**——その判定は「芯から外へ」で測っており、
**紡錘トーラスでは意味を持ちません**（面が軸をまたぐため）。**4-282 と同じ
間違いです。** しかも自動修正は、`flip` に基づく正しい反転を**打ち消して**
いました（内積が −1.0000 になって張り直され、元へ戻る）。

**ふつうの形を前提にした測り方を、そうでない形に当てる**——この日 2 回
やりました。

#### いまは何も変わりません（空試験）

| | 前 | 後 |
| :--- | ---: | ---: |
| テスト | 137 / 693 / 失敗 0 / 警告 0 | **同じ** |
| `screw.step` の穴 | 326 本 | **同じ** |
| `linkrods.step` の穴 | 1330 本 | **同じ** |

**効くのは、4-278（物差しを揃える）と一緒に入れたときだけ**です。
p-curve が作れないうちは、この 3 枚はそもそも検査に届きません。

#### 一緒に入れたら、こうなりました（**入れていません**）

4-278 の2か所を当てて測った値です。

| | 前 | **両方入れたとき** |
| :--- | ---: | ---: |
| `screw.step` | 読める、穴 **326 本** | 読める、穴 **14 本**（三角形 15678 → 13570） |
| `linkrods.step` | 読める、穴 1330 本 | **読めません**（12 件） |

**`screw.step` の穴が 23 分の1になります。** ですが `linkrods.step` が
読めなくなるので、まだ入れられません。

#### `linkrods` に残る 12 件は、あと 2.5% です

```text
Face 6: outer loop edge 5 p-curve differs from 3D edge by 5.635772e-4 (allowed 5.500000e-4)
```

**2.5% の超過**です。原因は 4-277 で書いたとおり——**測る量と検査する量が
違います**。取り込みは「境界が曲面からどれだけ外れているか」を測って持たせ、
検証は「p-curve と 3D の稜の差」を見ます。**p-curve は折れ線なので、自分の
近似誤差がそこに乗ります。**

**「両方測って大きいほうを持たせる」を試して、外しました。** 測る用の面を
緩くすると、**その面の p-curve が粗くなり、測定値も一緒に膨らみます**
（4-273 で `deflection` を射影の隔たりで床にしたため）。**測り方が自分の
答えを変えてしまう**ので、この形では決まりません。

#### 次に触る人へ

**「稜の外れ」と「p-curve の粗さ」を、別々の数字で持たせてください。**
1つの数字に押し込む限り、どちらかで必ず落ちます。

- 面が持つのは**稜の外れ**（いまと同じ）
- p-curve の粗さは、**作った p-curve から測って別に持つ**
- 検証は**その和**と比べる

**そこが決まれば、4-278 と 4-284 を一緒に入れられます。** `screw.step` の
穴は 326 → 14 本になり、**H8 の本題に手が届きます**。

---

### 4-285. **物差しを2つに分けたら、両方読めて穴が 1/23 と 1/39 になりました**（2026年9月2日、H8）

4-284 が「**稜の外れ**と **p-curve の粗さ**を、別々の数字で持たせてください」で
終わっていたので、そのとおりにしました。**通りました。**

#### 直した3か所

| どこ | 中身 |
| :--- | :--- |
| `Face` に `pcurve_tolerance` を足す | **稜の外れ（`tolerance`）とは別の量**。既定 0 で、読み込みだけが実測して持たせます |
| 検証の物差し | `tol.linear.max(self.tolerance + self.pcurve_tolerance)`。**2つの和**です |
| p-curve を作るときの受け入れ幅 | `tol.linear.max(self.tolerance) * 10`。**粗さは足しません**——足すと「粗いから広がり、広がったからもっと粗くなる」輪になります（4-284 で実測） |

#### 最後の1歩は「**標本の数を揃える**」でした

2つに分けても、まだ `linkrods.step` が 12 件で落ちました。取り込みは
p-curve の差を **1e-12** と測るのに、検証は **5.6e-4** と言います——**9 桁**の
食い違いです。

**標本の数が違いました。** 検証は **37 点**、取り込みは **8 点**。
**少なく取れば小さく出ます。**

**測る量を揃えても、測り方が違えば意味がありません。** 揃えたら通りました。

#### 実測

| | 前 | **後** |
| :--- | ---: | ---: |
| `screw.step` | 読める、穴 **326 本**（三角形 15678） | 読める、穴 **14 本**（三角形 13570） |
| `linkrods.step` | 読める、穴 **1330 本**（三角形 51438） | 読める、穴 **34 本**（三角形 46568） |
| テスト | 137 / 693 / 失敗 0 / 警告 0 | **同じ** |

**23 分の1と 39 分の1**です。

#### ここまでの道のり（4-277 から 9 手）

| | やったこと | 結果 |
| :--- | :--- | :--- |
| 4-277 | 穴の原因を名指し（p-curve が作れず黙って落ちる） | 診断 |
| 4-278 | 物差しを揃える | **読めなくなる**（3件）ので入れず |
| 4-279 | 巻きを probe で測る | 読んだあとは 0 枚。取り込み側を見るべきと判明 |
| 4-280 | 取り込み側で測る | **平面だと誤読**（文面が `planar`） |
| 4-281 | 種類を確かめる | **NURBS** だった。文面を直す |
| 4-282 | 曲面の巻きを 3D で測る | **測り方が不正**（平らでない輪にベクトル面積） |
| 4-283 | UV で測り、矛盾を1点に絞る | **3枚とも紡錘トーラス** |
| 4-284 | 裏返しの打ち消し | 単独では効かない（空試験） |
| **4-285** | **物差しを2つに分け、標本を揃える** | **両方読めて、穴が 1/23 と 1/39** |

**同じ間違いを2回しました**——「ふつうの形を前提にした測り方を、そうでない形に
当てる」（4-282 と 4-284）。**測る口を足したら、まずその口自身を検算してください。**

#### 残っているもの

**穴はまだ 14 本と 34 本あります。** 0 ではありません。ここが 0 になれば、
**内外判定を使う検査が初めて意味を持ちます**（4-276）。


### 4-286. 読んだ立体のメッシュの穴を、**2つの機構**まで下ろしました（2026年9月2日、H8）

4-285 が「穴はまだ 14 本と 34 本ある」で終わっていたので、そこからです。
**`screw.step` は 0 本になりました。**

#### 記録が指していた入口は、もう塞がっていました

4-277 は「p-curve を作れず黙って共有しない経路へ落ちる面」を原因として
名指ししていました。**測ったら 0 件**です（4-285 の物差し分けで塞がって
いました）。**それでも穴は残っていたので、別の機構です。**

#### 帰属させると、面をまたいでいませんでした

`ZENITH_FACE_OWNER_WHY=1` で数えると、**34 本すべてが1枚の面（面 21）の中**
でした。稜の共有でも、刻み数の食い違いでもありません——**その面の三角形分割
そのものが割れています**。

途中で `RINGWHY` の行を `cut -c1-200` で切って読み、「同じ稜を x16 と x1 で
標本している」と誤読しました。**切り詰めの跡でした**（`x16` の途中）。
**切って読んだ行から結論を出さないでください。**

#### 機構1: **内側の一直線の上に、T 字の継ぎ目**

面積で突き合わせる口を足しました（`ZENITH_EARCUT_WHY`）。多角形と三角形の
和は一致（差 6.3e-15）——**覆えてはいます**。ところが**境界でないのに1回しか
使われない辺が 6 本**あり、**すべて `v = 0.5` の直線上**でした。

```text
相手のいない辺 (0.016345,0.500000)-(0.492646,0.500000)   ← 跨ぐ辺
相手のいない辺 (0.016345,0.500000)-(0.019901,0.500000)   ← その一部
相手のいない辺 (0.019901,0.500000)-(0.023430,0.500000)   ← その一部
```

**長い辺と、その上に乗る短い辺が同居**しています。片側は三角形1枚、もう
片側は細かい三角形が並ぶので、そこが縫合されません。

境界の辺には手当てが3段あります（4-85、4-86、4-207、4-209）。**内側には
無く、黙って通っていました。**

`repair_interior_t_junctions` を足しました——1回しか使われていない**境界で
ない**辺の上に頂点が乗っていたら、その三角形を割ります。**壊れている辺だけ**
を触るので、正しく張れている面には掛かりません。

**許容を辺の長さで正規化して、5本取りこぼしました。** 短い辺ほど厳しくなり、
**点が詰まっているところ**——いちばん見つけたい場所——で効きません。
**領域の大きさ**基準に直しました。

#### 機構2: **uv で潰れた三角形を、出口で捨てていました**

`push_with_uv_winding` は、uv の符号付き面積がちょうど 0 の三角形を捨てて
いました。

```rust
if signed == 0.0 { return; }   // ← 捨てていた
```

ところが同じ面には「**uv では面積 0 なのに、3D では合わせて 0.005672 の面積を
持つ**」三角形が 41 枚あります。**実測で 10 枚捨てて、穴が 14 本**でした。

uv から向きが決まらないだけなので、**3D の法線から決めます**。頂点の法線は
既に面の向きを織り込んである（`oriented_normal`）ので、それと揃えれば
`forward` を別に見る必要はありません。

#### 実測

| | 穴 |
| :--- | ---: |
| `screw.step`（素の読み込み、16分割） | **34 → 0** |
| `screw.step`（`Regularizer` 経由、24分割。掃き出しと同じ道） | **14 → 0** |
| `linkrods.step`（同上） | 34 → **33** |

**2つの機構は、経路によって出方が違いました。** 素の読み込みでは機構1が、
`Regularizer` を通したものでは機構2が効きます。**片方だけ測っていたら、
もう片方を見落としていました。**

#### 測る道を揃えるのに手間取りました

一時プローブが `screw.step` を **10 面**、掃き出しが **16 面**と報告して
食い違いました。掃き出しは `Regularizer::hold_like_our_own` を通していた
からです。**同じ立体を測っていると思い込むと、数字が合わない理由を探して
時間を使います。** 揃えたら再現しました。

#### 残っているもの

`linkrods.step` は **33 本**。`EMITDROP` は 0 件なので機構2ではなく、
T 字の修理も当たりません（辺の上に頂点が無い＝**片側の三角形がそもそも
無い**）。**3つ目の機構**です。次はそこから。


### 4-287. 3つ目の機構——**輪が uv で自分と交わっている**（2026年9月2日、H8）

4-286 で `screw.step` は 0 本になり、`linkrods.step` に 33 本が残りました。
**そちらは別の機構でした。**

#### まず、症状が違います

| | `screw.step` | `linkrods.step` |
| :--- | ---: | ---: |
| 穴 | 0 | **33** |
| **重なり（3回以上使われる稜）** | 0 | **12** |
| `EMITDROP` | 0 | **0** |
| 内側の T 字 | 直った | **修理が当たらない** |

**穴だけでなく、重なりが出ています。** T 字の修理が当たらないのは、
**その辺の上に頂点が無い**——片側の三角形がそもそも無いからです。

#### 数えたら、面積が 1.57 倍でした

`ZENITH_EARCUT_WHY` に足した面積の突き合わせ（4-286）が効きました。

```text
面積 多角形 0.787012715 / 三角形 1.234632104（差 5.688e-1）
境界の連続対 528 のうち辺になっていないもの 11
```

**三角形が多角形の 1.57 倍を覆っています。** 重なっている、ということです。
**下流の手当てをいくら足しても直りません**——入力が多角形になっていません。

#### 輪が、自分と交わっていました

交差を数える口を足しました。

```text
**輪が uv で自分と交わっています**: 2 組。
例 [245/528](0.000000,0.101754)-[246/528](1.000000,0.096399)
   と [377/528](1.000000,0.091043)-[378/528](1.000000,0.096399)
```

**境界の標本 245 と 246 の間で、u が 0.000000 から 1.000000 へ飛んでいます。**
連続する2点なので、**1本の p-curve が継ぎ目をまたいでいる**ということです。

区間の刻みは `[48, 48, 24, 24, 24, 24, 24, 24, 24, 48, ...]` で、累積すると
245 と 246 は**同じ区間（8 本目、240..264）の中**です。**稜1本の p-curve が、
途中で継ぎ目を飛び越えています。**

#### 継ぎ目の手当ては、ここまで届いていません

`settle_seam_axis`（`face.rs`）は、**ちょうど端に乗っている標本**を、いちばん
近い内側の標本の側へ寄せます。**途中で飛び越える標本の列**は想定していません。

#### どう直すか（まだ入れていません）

**筋としては、継ぎ目で稜を割るのが正しい**です。閉じた曲面を1枚の面で表す
とき、その面の境界は継ぎ目を2度たどります。**継ぎ目を横切る稜は、B-Rep と
して割れているべき**で、割れていなければ p-curve は必ずどこかで飛びます。

手当ての置き場所は2つあります。

| 置き場所 | 中身 | 見立て |
| :--- | :--- | :--- |
| **取り込み（`zenith_io`）** | 継ぎ目を横切る稜を、継ぎ目で2本に割る | **筋が良い。** 下流全部が直る |
| 三角形分割（`zenith_tess`） | 飛びを見つけたら、その面は共有しない経路へ落とす | **対症療法。** 穴は残るが、**重なりは消える**（いまは 1.57 倍を出しています） |

**まだ決めていません。** 取り込み側は影響範囲が広いので、**先に「飛んでいる
稜が何本あるか」を数えてから**にしてください。いまは 1 面・2 組だけです。

#### 入れたもの（診断だけ）

`ZENITH_EARCUT_WHY` に、**輪が自分と交わっているか**を数える口を足しました。
交差する組の数と、**最初の1組を輪の位置つき**で出します。

**振る舞いは変えていません**——`ZENITH_EARCUT_WHY` が無ければ1行も走りません。

#### 教訓

**「穴」と「重なり」は別の症状です。** 4-286 まで穴だけを数えていたので、
`linkrods` が別の機構だと気づくのが遅れました。**重なりは、入力が多角形に
なっていない印**です。


### 4-288. **読んだ立体のメッシュが水密になりました**（2026年9月3日、H8）

4-287 で「輪が uv で自分と交わっている」と分かったので、そこを直しました。
**`screw.step` も `linkrods.step` も、穴 0・重なり 0 です。**

#### まず、数えました

**直す場所を決める前に、どれだけ広いかを数えます。**

| | 面 | 飛んでいる面 | 区間 | 最大の段差 |
| :--- | ---: | ---: | ---: | ---: |
| `screw.step` | 10 / 16 | **0** | 0 | 0.000 |
| **`linkrods.step`** | 37 | **1** | **2** | **1.000**（＝周期そのもの） |
| ビルダーの円柱・球・トーラス・円錐 | 6〜16 | **0** | 0 | 0.000 |

**1ファイル・1面・2区間だけ**でした。段差がちょうど周期 1.000 倍なのは、
**継ぎ目の反対側に置かれている**ということです。

#### 2回、場所を外しました

| 試したこと | 結果 |
| :--- | :--- |
| `settle_seam_axis` に「標本の列を連続にする」後段を足す | **1桁も動かず。戻した** |
| 細分で足した点にも継ぎ目の寄せを掛ける | **1桁も動かず。戻した** |

どちらも「標本の列」を直す手です。**この稜は丸ごと継ぎ目に乗っている**ので、
標本ごとの寄せ（`settle_seam_axis`）は**最初に「全部が端に居る」で早期に
戻ります**。担当は区間ごとに扱う `settle_seam_segment_axis` のほうでした。

**効かない変更は入れずに戻しています。** 入れると、次の人が「ここは見た」と
思ってしまいます。

#### 原因は、判定の1行でした

`settle_seam_segment_axis` は「寄せ先が曖昧な区間」をこう決めていました。

```rust
at(min) || at(max)   // 全部の制御点が min 側、または 全部 max 側
```

**`linkrods` の区間は、両端に混ざって乗っています**（一部が u=0、残りが u=1）。
どちらの条件にも当たらないので**寄せる対象から漏れ、そのまま残っていました**。

「**どの制御点も min か max のどちらかに乗っている**」も曖昧と数えるように
しました。混ざっている区間は、まさに継ぎ目の上で飛んでいる区間です。

#### 実測

| | 前 | 後 |
| :--- | ---: | ---: |
| `screw.step` の穴 | 14 | **0** |
| `linkrods.step` の穴 | 34 | **0** |
| `linkrods.step` の重なり | 12 | **0** |
| `linkrods.step` の三角形 | 46,517 | **32,998** |

**三角形が 29% 減ったのは、1.57 倍の重なりが消えたから**です（4-287）。

#### 9月2〜3日の3手で、穴が消えました

| | 機構 | 効いた先 |
| :--- | :--- | :--- |
| 4-286 その1 | 内側の一直線の上の T 字継ぎ目 | `screw`（素の読み込み）34 → 0 |
| 4-286 その2 | uv で潰れた三角形を出口で捨てていた | `screw`（`Regularizer` 経由）14 → 0 |
| **4-288** | **継ぎ目に混ざって乗る区間を、寄せる対象から漏らしていた** | **`linkrods` 34 → 0** |

**3つとも別の機構で、経路によって出方が違いました。**

#### 落ちたものは1つもありません

| | 前 | 後 |
| :--- | :--- | :--- |
| 通しテスト | 137 バイナリ / 693 件 / 失敗 0 / 警告 0 | **同じ** |
| `contact_placement_probe` | 135 ok / 6 断り（未実装 0）、B-Rep・メッシュとも非多様体 0 | **同じ** |
| `scale_sweep_probe` | 破れ 0・非多様体 0・断り 0（最悪 7.394e-8） | **同じ** |
| `rechained_boolean_probe` | 30演算すべて緑（最悪 1.906e-7） | **同じ** |
| `face_budget_probe`（H5） | 3,854 枚 | **同じ** |
| `foreign_cross_pair_probe` | 15組そろい、破れ 0（最悪 9.429e-10） | **同じ** |
| `mesh_watertight_probe` | すべて閉じている | **同じ** |

#### H8 について

**「読んだ立体を切る」の土台は揃いました**——2ファイルとも読めて、切れて、
**表示メッシュが水密**です。**目標の「恒等式の破れ 0」はまだ**で、残るのは

- `linkrods.step` の演算が返らない（射影の 9 割が p-curve の導出。4-271）
- `screw.step` の差が体積の検証で断られる（4-266）

の2つです。

#### 教訓

**「全部が片側」と「全部がどちらかの側」は違います。** 判定を書くとき、
**中間の形**（混ざっている）を落としていないか確かめてください。ここでは
その1行のために、面1枚の三角形分割が丸ごと壊れていました。


### 4-289. **閉じた1本の交線**を、3か所が揃って弾いていました（2026年9月3日、H8）

4-288 のあと、`read_and_cut_probe` の数字が大きく動きました。**まずそこを
記録します**（私の手ではなく、継ぎ目の直しの効果です）。

| | 4-287 の時点 | 4-288 のあと |
| :--- | :--- | :--- |
| `screw.step` の3演算 | 24.0 / **断り** / 7.6 秒 | **2.2 / 2.8 / 1.4 秒。3演算とも ok** |
| **`screw.step` の恒等式** | 測れず | **2.848e-13 / 2.874e-13** |
| `linkrods.step` の3演算 | **2時間20分で返らず** | **80 秒で返る**（ただし断り） |

**継ぎ目の1行を直したら、返らなかった演算が 80 秒になり、断られていた差が
通り、恒等式が閉じました。** 4-287 の時点では「速度」と「体積の検証」を
別々の宿題として数えていましたが、**どちらも同じ原因の症状**でした。

#### 残った `linkrods` を掘りました

断り文は `0 cap loops, 0 cap faces`。蓋の材料を出すと、10 本のうち **4 本が
「長さ 0」**でした。

**接触の記録かと思いましたが、違いました。** 曲線としての広がりを測ると
**1.118 / 2.121 / 1.463 / 0.622** あります——**閉じた輪**です。平面が丸棒を
横切ってできる、端が同じ点の円でした。

**広さも数えました**（直す前に）。

| | 交線 | うち端が同じもの |
| :--- | ---: | ---: |
| `screw.step` | 8 | 0 |
| **`linkrods.step`** | 20 | **8** |
| ビルダー（箱×円柱、円柱×円柱、トーラス×円柱） | 8 / 8 / 8 | **0** |

**1ファイルだけ**です。

#### 同じ前提が、3か所にありました

| 場所 | 条件 | 症状 |
| :--- | :--- | :--- |
| `collect_closed_intersection_edge_loops` | `loop_edges.len() >= 3` | 輪として拾われない（**0 loops**） |
| `order_edges_into_closed_wire` | `edges.len() < 3` で断る | 輪は拾えたがワイヤにならない（**4 failed**） |
| `CapBuilder::make_planar_cap_with_holes` | 各稜の**始点だけ**を集める | 点が1つで法線が取れない（**Degenerate planar wire**） |

**1つ直すたびに、次が同じ理由で断りました。** 3段とも「輪は3本以上の稜で
できている」と決め打っています。**同じ前提が何か所にあるかは、直してみるまで
分かりませんでした。**

直し方はどれも同じ形です——**閉じた1本で、曲線として広がりがあるなら、
それは輪です**。広がりを確かめてから通すので、点で触れているだけのものは
これまでどおり落ちます（3-1 の規約）。

| `linkrods.step` | 前 | 後 |
| :--- | ---: | ---: |
| 蓋のループ | **0** | **8** |
| 蓋の面 | **0** | **4** |

#### それでも通りません

```text
selected face stitching has 56 unmatched edge uses
```

**蓋 4 枚では埋まりません。** 次の壁はここです。材料 10 本のうち **6 本が
まだ skipped**（鎖にならない断片）なので、そこから見てください。

#### 落ちたものは1つもありません

| | 前 | 後 |
| :--- | :--- | :--- |
| 通しテスト | 137 バイナリ / 693 件 / 失敗 0 / 警告 0 | **同じ** |
| `contact_placement_probe` | 135 ok / 6 断り（未実装 0）、非多様体 0 | **同じ** |
| `scale_sweep_probe` | 破れ 0・非多様体 0・断り 0 | **同じ** |
| `rechained_boolean_probe` | 30演算すべて緑 | **同じ** |
| `face_budget_probe`（H5） | 3,854 枚 | **同じ** |
| `foreign_cross_pair_probe` | 15組、破れ 0 | **同じ** |
| `mesh_watertight_probe` | すべて閉じている | **同じ** |

#### 教訓

**「n 本以上」と書いた条件は、境目の形を落としていないか疑ってください。**
円は1本で輪になります。3か所とも、書いたときは「輪＝多角形」と思っていた
はずです。

**そして、同じ前提は固まって存在します。** 1か所直して通らなかったとき、
「別の原因だ」と考える前に**同じ条件を探して**ください——今回は3回続けて
同じ形でした。


### 4-290. 「メッシュ非多様体 45 本」は、**刻みで変わる別の物差し**でした（2026年9月3日、H8）

`read_and_cut_probe` は `screw.step` について、同じ行で
**「表示メッシュ 穴 0 本」**と**「メッシュ非多様体 45 本」**を並べて出して
いました。**物差しが2つ混じっています。**

| 何を測っているか | 使う口 | 刻み |
| :--- | :--- | ---: |
| 表示メッシュの穴 | `tessellate_solid_stitched` | **24** |
| 結果の「メッシュ非多様体」 | `tessellate_solid` | **16** |

#### 刻みで変わります

| 刻み | 穴 | 重なり | 計 |
| ---: | ---: | ---: | ---: |
| 16 | 3 | 42 | **45** |
| 24 | 0 | 0 | **0** |

しかも **45 本は読んだ立体そのものに出ています**——和も差も積も、同じ 45 本を
そのまま引き継いでいるだけです。**ブーリアンの欠陥ではありません。**

```text
読んだ立体そのもの   面 16、三角形 7092、穴 3 本、重なり 42 本
union の立体 0       面 26、三角形 9284、穴 3 本、重なり 42 本
difference の立体 0  面 12、三角形 5172、穴 3 本、重なり 42 本
```

#### 16 分割で残る理由

面 21（ねじ山）の `v = 0.5` の上に、`[9, 10, 11]` のような**ほぼ一直線の
細片**があります。辺 `(9,11)` が1回しか使われず、それを割るべき点 `10` は
**その三角形自身の頂点**です。4-286 の T 字修理は「割る相手がいない」と
正しく判断して手を出しません。

**許容の問題ではありません。** 診断で数字を出せるようにしました。

```text
T 字を直せない (0.019901,0.500000)-(0.016345,0.500000):
  いちばん近い頂点は辺から 1.498e-10（許容 1.414e-9）
```

**内側に居ます。** 見つけられないのではなく、**見つけたものが自分自身**です。

#### 直していません

細片そのものを扱う必要があります。**4-286 の機構2（uv で潰れた三角形を
捨てない）と正面からぶつかる**ので、慎重にやってください——あちらは
「uv で潰れていても 3D で面積を持つ三角形は捨てない」でした。ここの細片は
**3D でも潰れて**います（3点が同じ稜の上に並んでいます）。**区別できる量が
要ります。**

#### 掃き出しの物差しを揃えました

同じ立体について2つの数字を並べているのは、それ自体が読みにくさです。
いまは「45 本」だけを見て「ブーリアンが壊した」と読めてしまいます
——**私はそう読みました。**

**刻みを1か所（`display_params()`）にまとめ、表示メッシュと結果の検査で
同じ物差しを使う**ようにしました。

| `screw.step` | 前 | 後 |
| :--- | ---: | ---: |
| 和 / 差 / 積 のメッシュ非多様体 | 45 / 45 / 0 | **0 / 0 / 0** |

**これで `screw.step` は H8 の目標を満たします。**

| | |
| :--- | :--- |
| 読み込み | ok（面 16、体積 2240.815696） |
| 表示メッシュ | **穴 0・重なり 0** |
| 和 / 差 / 積 | **3演算とも ok、メッシュ非多様体 0** |
| **恒等式** | **2.848e-13 / 2.874e-13** |

**16 分割で残る 45 本は消えていません**——測っていないだけです。上の細片の
話は、そのまま次の宿題として残ります。**「見えなくなった」と「直った」を
混ぜないでください。**

#### 入れたもの（診断だけ）

T 字修理が直せなかった辺について、**いちばん近い頂点が辺からどれだけ外れて
いるか**と**そのときの許容**を出すようにしました。「乗っていない」のか
「許容が厳しすぎる」のかは、距離を見ないと分かりません。

**振る舞いは変えていません**——`ZENITH_EARCUT_WHY` が無ければ走りません。
テスト 137 バイナリ / 693 件 / 失敗 0 / 警告 0。


### 4-291. `linkrods` の縫合——**公差ではありません**（2026年9月3日、H8）

`screw.step` が通ったので（4-290）、残る `linkrods.step` を測りました。
断り文はこれです。

```text
selected face stitching has 56 unmatched edge uses,
0 non-manifold edge uses, and 0 same-direction edge uses
```

#### 相手までの距離に、間がありません

`ZENITH_STITCH_WHY=1` は、相手のいない稜ごとに**いちばん近い相手までの距離**
を出します。2,372 件を集計しました。

| 距離 | 件数 |
| :--- | ---: |
| **ちょうど 0** | **4** |
| 1e-6 〜 0.01 | **0** |
| 0.01 〜 0.1 | 1,006 |
| **0.1 以上** | **1,362** |

中央値 **0.113**、最大 **0.545**。

**公差を緩めても届きません。** 1e-6 と 0.01 の間が**丸ごと空**なのが、その
証拠です——公差の問題なら、そこに山ができます。

#### つまり、両側が違う割り方をしています

相手のいない稜は**すべて A 側**（読んだ立体）で、長さは 0.02〜0.46。近くに
似た稜はあるが**別の稜**、という形です。同じ面を両側から割ったときに、
**片方だけが割れた**か、**割る位置が違った**ということになります。

#### 次の人へ

**まず「どの面の片が、どの面の片と合っていないか」を出してください。**
いまの `STITCHWHY` は稜の座標しか言いません。**面の番号**（と、それが
どちらの立体から来たか）を添えれば、1回で場所が決まります。

そのうえで、

| 見るところ | 何を確かめるか |
| :--- | :--- |
| `select_operand_faces_after_batch_split` | **採られた片の数**が、両側で釣り合っているか |
| `collect_batch_splits_for_faces` | **同じ交線が、A と B の両方の面を割っているか**（片方だけなら、そこが原因） |

**蓋（4-289 で 4 枚）では埋まりません。** 56 本に対して桁が違います。

#### 数字の出どころ

- `screw.step`: 3演算とも ok、恒等式 2.848e-13 / 2.874e-13（4-290）
- `linkrods.step`: 3演算とも 80 秒で返り、**3つとも断り**。分割は 22/22 当たり、蓋は 8 ループ 4 面


### 4-292. 端点のつながり方で、**割り方の食い違い**だと確かめました（2026年9月3日、H8）

4-291 で「公差ではない」と分かったので、もう一段測りました。相手のいない稜の
**端点に、他の稜が何本つながっているか**です。閉じた殻なら、どの頂点にも
**偶数本**が集まります。

| （始点, 終点）につながる本数 | 件数 |
| :--- | ---: |
| **(1, 1)** | **952** |
| (1, 3) | 532 |
| (3, 1) | 490 |
| (5, 1) | 112 |
| (3, 3) | 98 |
| (1, 5) | 70 |
| (3, 5) | 42 |
| **(0, 0)** | **36** |

**ほとんどが奇数**です。そして **36 件は両端とも 0 本**——**どこにも
つながっていない稜**が出ています。

#### これで筋が決まりました

**位置の話ではありません**（4-291 で距離に間が無いことを確かめました）。
**割り方の食い違い**です——ある面が切られたのに、その稜を共有する隣の面が
切られていない（あるいは違う位置で切られた）ため、**片側は2本、もう片側は
1本**になっています。T 字の頂点です。

**(0, 0) の 36 件**は、それより悪い形です。**面片が丸ごと浮いている**か、
**相手が選ばれなかった**か。

#### 次の人へ

**要るのは imprint（互いを刻む段）です。** `split_by_curves` の注記に
「交線どうしが面の内側で交わる場合は、まだ扱えない。先に交点で互いを刻む段
（imprint）が要る」と書いてあるのと**同じ話が、面と面の間でも起きています**。

順に:

1. **`(0, 0)` の 36 件から見てください。** いちばん壊れ方が大きく、
   数が少ないので追いやすいはずです。どの面片から来た稜かが分かれば、
   選ばれ方の問題か、割られ方の問題かが決まります。
2. ~~**`StitchEdgeUse` に面の番号を持たせてください。**~~ **入れました。**
   測ると、相手のいない稜は**67 枚の面**にまたがっていました（すべて A 側。
   いちばん多い面で 154 本、次いで 84 本が4枚、70 本が4枚）。

   **1枚が壊れているのではなく、広く起きています。** 「特定の面を直せば
   済む」型ではありません——**割り方そのものが両側で揃っていない**という
   上の見立てと合います。

#### 入れたもの（診断だけ）

`ZENITH_STITCH_WHY` に2つ足しました。

1. **端点につながる他の稜の本数**。相手までの距離だけでは「位置がずれている」
   のか「そもそも割られていない」のかが分かりません
2. **その稜を出した面の番号**（`StitchEdgeUse::face_id`）。座標だけでは
   **どの面の片が合っていないか**が言えません

**振る舞いは変えていません。**

---

### 4-293. **`(0,0)` の 36 件は「閉じた稜」でした。ただし直す場所はそこではありません**（2026年9月3日、H8）

4-292 が「入口は `(0,0)` の 36 件」で終わっていたので、そこから開けました。

#### 正体

```text
unmatched A 面 539 (7.969923711 3.125 0.5) -> (7.969923711 3.125 0.5) mid (7.530076289 3.125 0.5) len 0.000000000
```

**始点と終点が同じ**です。`len` は弦の長さなので 0 になりますが、中点は別の
場所にあります——**閉じた稜**（円）です。端点で相手を探す縫合では、
**端につながる他の稜が 0 本**になるのは当たり前でした。

**192 本の相手のいない稜のうち、24 本**がこれです（12.5%）。

#### 中点では見分けられません

閉じた稜どうしを突き合わせるとき、いまの判定は**始点・終点・中点**を使います。
**中点は輪のどこから始めるかで動きます**——実測: 同じ円のはずの2本で
**0.177** 離れていました。**公差の問題ではありません。**

そこで**始め方に依らない目印**（標本の重心＝円なら中心、折れ線で測った弧長）で
見分けるようにして、測りました。

#### **効果は 0 でした。戻しました**

| | 前 | 後 |
| :--- | ---: | ---: |
| 相手のいない稜 | 56 本 | **56 本** |
| `(0,0)` の件数 | 36 | **36** |

**判定は1度も発火しません**でした。`ends_match` の時点で落ちています——
つまり**同じ演算の中に、その円を持つ面が1枚しかありません**。

#### 面の番号を演算ごとに並べたら、はっきりしました

```text
U 539  U 540      （和の面）
D 618  D 619  D 620  D 621   （差の面）
I 697  I 698  I 699  I 700   （積の面）
```

**演算ごとに別々の面**です。座標が同じ円が複数の演算に出てくるので、
`sort -u` でまとめると「2枚が同じ円を持っている」ように見えますが、
**1つの演算の中では1枚しかありません**。

**相手の面（穴の壁）が、その円を出していない**——ここが本当の欠陥です。

#### 次に触る人へ

**閉じた稜の見分け方は、直しても効きません**（測りました）。見るべきは
**「穴の壁の面が、口の円を境界として持っているか」**です。

- 面 539 の2本の円は **z = 0.5 の平面**にあり、半径が違います（弧長 1.373 と
  2.468）。**穴の口**です
- 壁の面はふつう、その円を**継ぎ目で2本の弧に割って**持ちます。**1本の閉じた
  円 対 2本の弧**なら、いまの判定では永久に合いません
- **まずそれを確かめてください**——壁の面の稜を出して、口の円の上に乗って
  いるかを見る

**残りの 168 本（閉じていないほう）も、まだ見ていません。** そちらのほうが
数は多いので、先にそちらを見るという選び方もあります。

---

### 4-294. **H8 が「いちばん難しい置き方」だけを測っていました**（2026年9月3日）

4-293 が「壁の面が口の円を持っているか」で終わっていたので、座標で待ち伏せる口
（`ZENITH_STITCH_WATCH="x,y,z"`）を足して見ました。**そこで別のことが
分かりました。**

#### 切り手が、必ず端の特徴に接していました

`z = 0.5` の面のまわりを出すと、こうです。

```text
A 面 539 (7.969924 3.125 0.5) -> 同じ点   mid (7.530076 3.125 0.5)   ← 閉じた円（半径 0.220）
A 面 539 (8.145285 3.125 0.5) -> 同じ点   mid (7.354715 3.125 0.5)   ← 閉じた円（半径 0.395）
B 面 531 (8.145285 3.125 0.5) -> (8.145285 2.500 0.5)                ← 切り手の稜
```

**`8.145285 = 7.75 + 0.395285`。** 切り手の面が、穴の口の円に**ちょうど接して
います**。

偶然ではありません。**切り手は立体の囲みから作っていました**——
`bbox.min` から `span` ぶんの箱なので、側面は `bbox.min.x` と `bbox.max.x`、
つまり**モデルの境界面そのもの**です。**そこは必ず端の特徴に接します。**

**接触はこのカーネルがいちばん手を焼く配置**です（3-1、3-N で 40 件以上の
記録があります）。**H8 は、いちばん難しい置き方だけを測っていました。**

#### 普通の置き方を既定にしました

側面を内側へ 3% 寄せ、切る高さも半分ちょうどから外します（0.47）。
**難しい置き方は `ZENITH_CUT_TANGENT=1` で戻せます**——捨てません。

#### そうしたら、`linkrods` が返らなくなりました

| 置き方 | `linkrods` の和 |
| :--- | :--- |
| 接する（前） | 80 秒で返る（断られる） |
| 普通（後） | **300 秒でも返らない** |

**接するほうが速いのは、交線が短いから**です。普通に切ると面をたくさん割ります。

#### 律速を数字で追いました（4-272 の拍動）

```text
経過 30 秒: 曲面評価 +5410818、射影 +122366（… 粗い全域 +1528）、辿り +191838
経過 60 秒: 曲面評価 +32247357、射影 +81500（… 粗い全域 +77674）、辿り +0
経過 90 秒: 曲面評価 +40996679、射影 +99822（… 粗い全域 +99822）、辿り +0
```

**60 秒以降、辿りも積分も 0 で、射影の 100% が「粗い全域」**（種を渡さない
探索。1回につき曲面評価 353 回）。

#### 呼び元を、9 か所ぜんぶ数えて突き止めました

**推測していません。** 数える口を足しては測る、を繰り返しました。

| 疑った所 | 実測 |
| :--- | ---: |
| 面の法線／片の法線／種探し | **0** |
| 面を切る段／接する曲面 | 13／0 |
| 交線への着地／当てはめの測定 | 0／0 |
| **距離の `support_foot`** | **53,879〜66,288** |

**`classify_face_against_mesh` が「近くに境界があるか」を見るのに、
`boundary_projections` を呼んでいました**——**すべての面へ全域射影して、
いちばん近い所を全部集める**関数です。要るのは「近いものが1つでもあるか」
だけでした。

**途中で1度、数え口を流用して読み違えました**（`seed_on_patch` と共用したので、
別の関数の 24 回を「上流が呼ばれている」と読みかけました）。**数え口は
共用しないでください。**

#### 直し

`has_boundary_within(point, solid, limit)` を足しました。**答えは同じ**で、

- **面の囲みで先に捨てる**（囲みは面を含むので安全）
- **見つかったら止める**

#### 実測

| | 前 | 後 |
| :--- | ---: | ---: |
| `linkrods` の和（普通の置き方） | **300 秒でも返らない** | **131.9 秒で返る** |
| 同・差／積 | 同上 | 146.6／147.0 秒 |
| `screw` の恒等式 | 3.845e-13 / 3.899e-13 | **同じ** |
| `contact_placement_probe` の合計時間 | 1453.4 秒 | **1225.7 秒（−16%）** |
| テスト | 137 / 693 / 失敗 0 / 警告 0 | **同じ** |
| 掃き出しの中身 | 135 ok / 6 断り、非多様体 0、恒等式 41配置で破れ 0 | **同じ** |

**答えは1つも動かず、時間だけが減りました。**

#### 残っているもの

`linkrods` は**返るようになりましたが、まだ断られます**（縫合）。普通の置き方
でも接する置き方でも断られるので、**接触が原因ではありません**。

---

### 4-295. **選び方は正しく、割り方が食い違っています**（2026年9月3日、H8）

4-294 で `linkrods` が返るようになったので、断りの中身に入りました。

#### 相手のいない稜は、全部 A 側です

和で 64 本（うち閉じた稜 8 本）。**B（切り手）からは 1 本も出ません。**
切り手の箱は問題なく縫えています。

#### 選び方は正しい

`ZENITH_SELECT_WHY=1` で採否をそのまま出しました。

| | 採る | 落とす |
| :--- | ---: | ---: |
| A | 39（Outside） | 24（Inside） |
| B | 14（Outside） | 2（Inside） |

**和の規約どおり**です（互いの外側だけを採る）。**落としすぎても、採り
すぎてもいません。**

#### 相手のいない稜は、**切り口の上にありません**

切断面は `z = 1.0`（模型の z 幅 2.0 の半分）です。ところが相手のいない稜の
z は **0.0、0.3499、0.5999、1.3499、1.5、1.5999** に散っていて、
**1.0 は1本もありません**。

**つまり、切った跡が合っていないのではありません。** 合っていないのは
**A が元から持っている稜**です。

#### だから、見るところはここです

**同じ稜を共有する2枚の面が、その稜を別々に割っています。**

- 面 F1 と F2 が稜 E を共有している
- 割るとき、F1 は E を 3 つに、F2 は 2 つに割る——といった食い違いがあると、
  どちらの片も相手が見つかりません
- 4-292 が「端点につながる稜の本数が奇数ばかり」と測ったのは、**この形の
  現れ方**です

#### 次に触る人へ

**「稜 E を、どこで割ったか」を面ごとに出す口が要ります。** いまは
`ZENITH_STITCH_WHY` が「合わなかった稜」を出しますが、**割った点そのもの**は
見えません。

- 割る前の稜の番号と、割った点の並びを、**面ごとに**出す
- 同じ稜を共有する2枚で、その並びが一致しているかを見る

**一致していなければ、割る段で「稜ごとに1度だけ割って、両方の面へ配る」
必要があります。** 面ごとに独立に割ると、必ずこうなります。

#### 追記: **刻み込みを増やしても効きません**（同じ日）

「割った点を両方へ配る」を実際にやりました。いまの刻み込みは**交線の端点だけ**を
配っています（`imprint_points`）。そこへ**全部の片の頂点**を足して、互いに
刻み込むようにしました。

**効果は 0 でした**（56 本のまま）。戻しました。

**つまり「配り忘れた点がある」のではありません。** 相手の稜に**乗っていない**
ので刻めない、という形です。

**手掛かり**: 相手のいない A の稜（長さ 0.08）に対し、診断は**中点がぴったり
一致する（0.000000000）B の稜、長さ 1.500000000** を「いちばん近い相手」として
出します。**長さが 19 倍違います。** 中点だけが合うというのは、
**同じ線の上に無い**か、**片方が相手の内側に丸ごと入っている**かのどちらかです。
**そこを次に見てください。**

#### 測った値（すべてこの日の実測）

| | |
| :--- | :--- |
| 和の相手のいない稜 | **64 本（全部 A）** |
| うち閉じた稜 | 8 本 |
| 採否 | A 39/24、B 14/2（規約どおり） |
| 切断面 | `z = 1.0`。**相手のいない稜に z=1.0 は 1 本も無い** |

---

### 4-296. **`screw.step` の表示メッシュは、刻みを変えると壊れます**（2026年9月3日）

4-290 が「16 分割では非多様体が 45 本残る。掃き出しは 24 分割で測るので見えて
いない」と書いていたので、**刻みを振って測りました**。

#### 実測（読んだ立体そのもの。ブーリアンを回す前）

| 分割 | 三角形 | 穴 | 重なり | |
| ---: | ---: | ---: | ---: | :--- |
| 8 | 2,246 | 0 | 0 | |
| 12 | 4,272 | 0 | 0 | |
| **16** | 7,092 | **3** | **42** | **壊れる** |
| **20** | 9,796 | **7** | **19** | **壊れる** |
| 24 | 13,580 | 0 | 0 | |
| **32** | 24,454 | **8** | **113** | **壊れる** |
| **48** | 51,290 | **118** | **162** | **壊れる** |

**24 分割が緑だったのは偶然でした。** 掃き出しはそこだけを見ています。

`linkrods.step` は **8〜48 のどれでも 0／0** です。壊れるのは `screw` だけ。

#### 壊れているのは 2 枚の面です

`ZENITH_FACE_OWNER_WHY=1` で数えました。

```text
面 [Some(21)] が出した稜 256 本      ← 48 分割
面 [Some(9), Some(21)] が出した稜 24 本
```

- **面 21**: NURBS **9x2**、稜 16 本（ねじ山の面）
- 面 9: 平面、稜 5 本

**面 21 が単独でほとんどを出しています。**

#### 既知の族ではありません

| 疑い | 実測 | 判定 |
| :--- | :--- | :--- |
| 境界の辺が欠ける（4-207、4-277） | 340 本のうち **1 本**だけ | **違う**（256 本を説明できません） |
| 面が共有しない経路へ落ちる（4-277） | **0 枚** | **違う** |
| 溶接が三角形を外している | **同じ3頂点の重複 4〜17 枚**、flap 0〜3 | **ここが手掛かり** |

**重なり（稜が3回以上使われる）が主**です——穴より多いので、**同じところを
2度覆っています**。

#### 追記: **継ぎ目でした**（同じ日）

`ZENITH_REFINE_WHY=1` で、溶接が束ねる対を出しました。

```text
溶接で束ねられる対 9（境界どうし 9、境界と細分 0、細分どうし 0）、uv の隔たりは最大 1.000e0
溶接で束ねられる対 5（境界どうし 0、境界と細分 0、細分どうし 5）、uv の隔たりは最大 1.000e0
```

**uv で 1.0 離れた点どうしが、3D では同じ点**です。パラメータ範囲は 0〜1 なので、
これは**両端**——**継ぎ目**です。面 21 は回転で作った面（ねじ山）で、
**u の方向に閉じています**。

`linkrods` に同じ報告は出ません。**壊れるのは継ぎ目のある面だけ**です。

**「24 分割だけ緑」は、継ぎ目の上に標本がちょうど乗るかどうかで決まっていた**
——という筋が立ちます。**まだ測っていません。**

#### 消えた仮説（測って戻しました）

**`fill_missing_boundary_edges` が既に覆えている所へ足している**、という読みで、
足す前に「重心が既存の三角形の内側か」を見て見送るようにしました。

| 分割 | 前 | 後 |
| ---: | :--- | :--- |
| 12 | 0／0 | **0／11**（壊れた） |
| 24 | 0／0 | **0／11**（壊れた） |
| 48 | 118／162 | **140／182**（悪化） |

**見送ると別の所が壊れます。** あの段は必要な仕事をしています。**犯人では
ありません。**

#### 次に触る人へ

**継ぎ目から見てください。** 面 21 を 32 分割で1枚だけ張って、

- 重複三角形が**溶接の前に**何枚あるか
- それは earcut の出力か、細分が作ったか、`fill_missing_boundary_edges`（4-207）が
  足したものか

**`fill_missing_boundary_edges` は「覆えていない隙間を埋める」ので、既に
覆えているところに足すと重なります。** 4-209 でその可能性に触れていますが、
**まだ測っていません。**

#### なぜ大事か

**利用者は刻みを自分で選びます。** 24 だけが緑という状態は、**実質的に
壊れている**のと同じです。掃き出しが 24 固定なのは、**測り方の穴**です。

---

### 4-297. **刻みを振る門を足しました。自作の立体は全部の刻みで水密です**（2026年9月3日）

4-296 で「掃き出しが 24 分割だけを見ているのは測り方の穴」と書いたので、
**塞ぎました**。

#### 足したもの: `mesh_density_probe`

素形状 5 つとブーリアンの結果 3 つを、**10 通りの刻み**（6・8・10・12・16・20・
24・32・48・64）で測ります。**2 のべき乗だけにしていません**——16 と 24 で答えが
違ったのが、まさにそこだったからです。

見るのは「ちょうど2枚の三角形に共有されていない稜」の本数を、**穴（1回）**と
**重なり（3回以上）**に分けたものです。**壊れていれば `exit 1`**（自分で作った
立体なので、赤にします）。

#### 実測

```text
subject                        6       8      10      12      16      20      24      32      48      64  verdict
torus                          -       -       -       -       -       -       -       -       -       -  ok
cylinder                       -       -       -       -       -       -       -       -       -       -  ok
cone                           -       -       -       -       -       -       -       -       -       -  ok
sphere                         -       -       -       -       -       -       -       -       -       -  ok
box                            -       -       -       -       -       -       -       -       -       -  ok
torus - rod [0]                -       -       -       -       -       -       -       -       -       -  ok
torus + rod [0]                -       -       -       -       -       -       -       -       -       -  ok
box x tilted torus [0]         -       -       -       -       -       -       -       -       -       -  ok

8 件中 0 件が、どこかの刻みで壊れます。
```

**自分で作った立体は、10 通りの刻みすべてで水密**です。**壊れるのは読んだ
ファイルの、継ぎ目のある面だけ**（4-296）——ここまでが裏の取れた範囲です。

#### 消えた仮説（4-296 の続き。測って戻しました）

**4-285 で「向きの違う双子を残す」ようにしたのが、継ぎ目で効いている**、
という読みで、溶接の鍵を並べ替え（4-285 の前の形）に戻して測りました。

| 分割 | いま | 並べ替えに戻すと |
| ---: | ---: | ---: |
| 32 | 穴 8／重なり 113 | 穴 6／重なり **98** |
| 48 | 穴 118／重なり 162 | 穴 119／重なり **161** |

**ほとんど変わりません。** 4-285 は犯人ではありません。戻しました。

#### 面 21 の形（次に触る人へ）

```text
TESSWHY face 21: ring 点数 [408], 稜ごとの刻み [24, 24, 24, 24, 24, 24, 24, 48, 24, ...]
```

**稜 16 本のうち 1 本だけ刻みが倍**です（24 に対し 48）。`balance_opposite_edges`
が倍にしています。**そこが継ぎ目かどうかを、まず見てください。**

---

### 4-298. **earcut が、交わっていない輪から裏返った三角形を返します**（2026年9月3日）

4-297 で「継ぎ目から見てください」と書いたので、そこから追いました。
**原因の1つ手前まで来ましたが、直せていません。推測を3回外しています。**

#### 壊れている場所

`screw.step` を 32 分割で張ったときの、稜が3回以上使われる所です。

```text
使用 3 回: (-25.293213,-2.076297,2.936330) -> (-24.972124,-3.317959,2.936330) 長さ 1.283e0
使用 3 回: (-25.293213,-2.076297,2.936330) -> (-24.973831,-3.313107,2.936330) 長さ 1.277e0
```

**同じ点から出る、長さ 1.283 と 1.277 の二重の稜**です。終点は 0.005 離れて
います。

- 始点は**境界の頂点**（稜 5・9・12 が集まる所。`ZENITH_RING_WATCH` で確認）
- **2つの終点は境界の点ではありません**——細分が作った点です

#### earcut 自身が裏返った三角形を返しています

段ごとに向きを数えました。

```text
TESSWHY   earcut 直後の向き: 正 113 枚、負 4 枚
TESSWHY   **向きがばらけています**: 正 1994 枚、負 61 枚   ← 細分のあと
```

**earcut を出た時点で、既に 2〜5 枚が逆向き**です。細分はそれを増やします。

**逆向きの三角形は、同じところを2度覆います。** 重なり 113 本の出どころは
ここだ、という筋が立ちます。

#### 輪は交わっていません

earcut は「交わらない多角形」を前提にします。**輪の自己交差を数えたら、
92 回の測定すべてで 0 回**でした。

**入力は正しいのに、出力の向きがばらけています。**

#### 外した手（3つとも測って戻しました）

| 手 | 実測 |
| :--- | :--- |
| `fill_missing_boundary_edges` が既に覆えている所へ足している → 見送る | **12 と 24 分割が新たに壊れ**、48 も 118/162 → 140/182 に**悪化** |
| 4-285 の「向きの違う双子を残す」が効いている → 溶接の鍵を元に戻す | 32 分割で 113 → 98、48 で 162 → 161。**ほとんど変わらない** |
| **裏返った三角形を earcut の直後に外す** → 挿し戻しと穴埋めに任せる | **1つも変わらない**（全分割で同じ数字） |

**3つ目が変わらないのは、外している三角形が重なりの原因ではない**ということ
です。**まだ結びついていません。**

#### 追記: **重なりの現物を名指ししました**（4-299）

「重なっている稜を出している三角形を名指しする」を実装しました
（`ZENITH_OVERLAP_WHY=1`。**1枚の面の中で**3回以上使われる稜を、
三角形の番号と**その点が境界か細分か**つきで出します）。

```text
OVERLAPWHY 1枚の中で3回以上使われる稜 47 本（境界の点は 0..272）
OVERLAPWHY   稜 [11](境界) - [12](境界) を 3 枚が使う: [10, 1422, 1777]
OVERLAPWHY     三角形 10:   [11](境界) [12](境界) [13](境界)
OVERLAPWHY     三角形 1422: [653](細分) [11](境界) [12](境界)
OVERLAPWHY     三角形 1777: [658](細分) [11](境界) [12](境界)
```

**境界の稜 `[11]-[12]` を、3 枚が使っています。** 1枚の面の中では、境界の稜は
**ちょうど1枚**が使うはずです。

もう1つの形も出ました。

```text
OVERLAPWHY   稜 [11](境界) - [656](細分) を 3 枚が使う: [186, 206, 1670]
OVERLAPWHY     三角形 206:  [656] [11] [657]
OVERLAPWHY     三角形 1670: [657] [11] [656]   ← 同じ3点の逆向き
```

**同じ3点の三角形が、1枚の面の中に2枚**あります（折り返し）。

#### 4つ目の外した手

**1枚の面の中の重複を畳む**（同じ3点なら1枚にする）を入れて測りました。
**面をまたぐ双子（4-285）とは別の話**なので、筋は通っています。

| 分割 | 前 | 後 |
| ---: | :--- | :--- |
| 32 | 穴 8／重なり 113 | 穴 9／重なり **112** |
| 48 | 穴 118／重なり 162 | 穴 116／重なり **160** |

**ほとんど変わりません。** 折り返しは**主因ではありません**。戻しました。

**主因は1つ目の形**——**境界の稜を、細分が作った三角形が余分に使っている**
ほうです。

#### 次に触る人へ

**「境界の稜を、1枚を超える三角形が使っている」を入口にしてください。**

- 境界の稜は `protected` で守られている（細分は割らない）はずです。
  **割られていないのに、その稜を使う三角形が増えている**のはなぜか
- 三角形 1422 と 1777 は、どちらも**細分の点1つと境界の点2つ**でできています。
  **細分が三角形を割るとき、境界の稜を持つ側を2枚とも残していないか**

`ZENITH_OVERLAP_WHY=1` は残してあります。

#### そのほかに分かっていること

**面 21 を、32 分割で1枚だけ張って現物を見てください。** ここまでで

- 輪は交わっていない（0 回）
- earcut の出力に逆向きが 2〜5 枚ある
- その逆向きを外しても、重なりの数字は動かない

が分かっています。**次は「重なっている稜を出している三角形3枚」を名指しして、
それぞれがどの段で作られたかを見る**のが早いはずです。**逆向きかどうかでは
なく、重なりそのものを追ってください。**

診断の口は残してあります（`ZENITH_SEAM_WHY=1` で壊れた稜の座標、
`ZENITH_TESS_WHY=1` で段ごとの向きと輪の自己交差）。

**`ZENITH_SEAM_WHY` は、まず「何個の点につながっているか」を言います**
（4-302）。**1点なら、その頂点のまわりだけの話**——実測はそうでは
ありませんでした。

| 分割 | 壊れた稜 | つながっている点 |
| ---: | ---: | ---: |
| 16 | 45 本 | **40 個** |
| 20 | 26 本 | 24 個 |
| 32 | 121 本 | 110 個 |
| 48 | 280 本 | **268 個** |

**稜とほぼ同じ数の点に散っています。** 1か所の星型ではありません——
**面 21 の縁に沿って、broadly に起きています**。

**私は一度「全部同じ1点から出ている」と読みかけました。** 先頭の4本が
たまたま同じ頂点だっただけで、**並び順でそう見えていた**のです。
**数えてから言ってください。**

---

### 4-300. **重なりは「修復の段」で生まれています**（2026年9月3日）

4-299 が「境界の稜を、1枚を超える三角形が使っているのを入口に」で終わって
いたので、**段の前後で数えて挟み撃ちにしました**。

#### 挟み撃ち

1枚の面の中では、**境界の稜はちょうど1枚**が使うはずです。そこを段ごとに
数えました（`ZENITH_OVERLAP_WHY=1`）。

| 段 | 2枚以上に使われている境界の稜 |
| :--- | ---: |
| **earcut の直後** | **0 本**（1件も出ません） |
| **修復の段のあと** | **8〜55 本** |

**earcut はきれいです。重なりは、そのあとの修復の段で生まれています。**

修復の段はこれだけあります（この順）。

1. `reinsert_dropped_boundary_points`（4-86）
2. `repair_boundary_ears`
3. `drop_flat_boundary_triangles` ＋ 挿し戻し
4. `insert_unused_boundary_points_on_straddled_edges`（4-285）
5. `fill_missing_boundary_edges`（4-207）
6. `force_missing_boundary_edges`（4-285）

**どれかが、既に覆えている所へ三角形を足しています。**

#### 「穴埋めを見送る」は既に外しています

4-298 で `fill_missing_boundary_edges` に「重心が既存の三角形の内側なら
見送る」を入れて測り、**12 と 24 分割が新たに壊れた**ので戻しました。
**その見送り方が雑だった**のか、**犯人が別の段**なのかは、まだ分かりません。

#### 追記: **段を割ったら、別物だと分かりました**（同じ日）

6 つの段のあいだに数える口を置いて、1回で割りました。

| 段 | 境界の稜の重なり |
| :--- | ---: |
| 1 挿し戻しのあと | **0 本** |
| 2 耳の修理のあと | **0 本** |
| 3 潰れた耳を外したあと | **0 本** |
| 4 乗った点の挿し戻しのあと | **0 本** |
| 5 穴埋めのあと | **0 本** |
| **修復の段のあと**（＝`force_missing_boundary_edges` の直後） | **8〜55 本** |

**`force_missing_boundary_edges`（4-285 で私が入れた段）が作っています。**

#### **ところが、止めても立体の数字は変わりません**

| 分割 | そのまま | `force` を止める |
| ---: | :--- | :--- |
| 32 | 穴 8／重なり 113 | 穴 9／重なり **113** |
| 48 | 穴 118／重なり 162 | 穴 118／重なり **162** |

**面ごとの「境界の稜の重なり」と、立体の「稜が3回以上使われる」は、別物**
でした。前者は `force` が作っていますが、**後者の原因ではありません**。

**5 つ目の外した手**です。

#### 次に触る人へ（**ここから続けられます**）

**立体の重なりを、面をまたいで追ってください。** 面ごとに見ているうちは
届きません——`ZENITH_FACE_OWNER_WHY=1` は「どの面が出したか」を言うので、
**3 枚が別々の面から来ているのか、1 枚の面から 2 枚来ているのか**を先に
決めるべきでした。

**面をまたいで数えました**（`ZENITH_FACE_OWNER_WHY=1`、48 分割）。

```text
面 [Some(21)]              が出した稜 36 本   ← 1枚の中で重なっている
面 [Some(9), Some(21)]     が出した稜 16 本
面 [Some(21), Some(36)]    が出した稜 20 本
面 [Some(21), Some(37)]    が出した稜 31 本
面 [Some(21), Some(38)]    が出した稜 18 本
```

**面 21 が、隣（9・36・37・38）と重なっています。** 1枚の中の重なり（36 本）
より、**またいだ重なりのほうが多い**（85 本）。

**面 21 の張った範囲が、隣の面の territory にはみ出している**——という形です。
トリムは p-curve で決まるので、**面 21 の p-curve が、真のトリムより外へ
出ていないか**を次に見てください（面 21 が持ち歩く粗さは 1.000e-6 で、
緩くはありません）。

参考までに、**6 つの段の前後で同じ数え方をする**手順はこうです。 口は
`count_boundary_overuse(&triangles, "段の名前")` として置いてあり、
**2 か所（earcut の直後と修復のあと）で呼んでいます**。あいだに 4 回足せば、
**どの段が増やしているかが1回で分かります**。

**私は一度そこで失敗しています**——行番号で挿し込もうとして、`ring_ranges` の
見えない場所に置いてしまい、**1件も鳴りませんでした**。**呼び出しの直後に、
名前で置いてください。**

#### 今日ここまでで分かっていること（`screw.step` の表示メッシュ）

| | |
| :--- | :--- |
| 壊れる刻み | 16・20・32・48（8・12・24 は水密） |
| 壊れる面 | 面 21（NURBS 9x2、ねじ山）が単独でほとんど |
| 輪の自己交差 | **0 回** |
| earcut の直後 | 向きのばらけ 2〜5 枚、**境界の稜の重なり 0 本** |
| 修復の段のあと | **境界の稜の重なり 8〜55 本** |
| 細分のあと | 向きのばらけ 61 枚まで増える |
| 自作の立体 | **10 通りの刻みすべてで水密**（4-297） |

**外した手は 4 つ**（4-298、4-299 に記録）。

---

### 4-301. **安くした道を、常設テストで固定しました**（2026年9月3日）

4-294 で `has_boundary_within` を足し、**すべての面へ全域射影して全部集める**
道（`boundary_projections`）から切り替えました。**速いほうが安全なのは、
答えが同じときだけ**なので、そこを常設で押さえます。

#### 足したもの

`distance.rs` に単体テストを2本。**両方の道に同じ問いを投げて、答えが一致
することだけ**を見ます。

| テスト | 中身 |
| :--- | :--- |
| `the_cheap_near_boundary_test_agrees_with_the_thorough_one` | トーラスに対し、**7x7x5 の格子 × 5 通りの上限 = 875 通り**。**1点だけ通しても意味がありません**（4-115 の「範囲を振る」） |
| `it_also_agrees_on_a_solid_with_a_cavity` | **空洞のある立体**（箱から箱を引いたもの）。内側の殻を見落とすと、**中に居る点で答えが変わります** |

**上限も振ります。** 上限の前後で答えが変わる点こそ、**囲みで捨てる判断が
効くところ**です。

#### 入れる前に、落ちることを確かめました

囲みの判定をわざと壊す（`outside.norm() > limit` を `limit * 0.5` にする）と、
**空洞つきのほうが落ちます**。

```text
thread 'distance::near_boundary_tests::it_also_agrees_on_a_solid_with_a_cavity' panicked
test result: FAILED. 1 passed; 1 failed
```

**通ることを確かめただけのテストは、通り続けるだけです。**

#### 数字

| | 前 | 後 |
| :--- | ---: | ---: |
| テスト | 137 / 693 / 失敗 0 / 警告 0 | **137 / 695 / 失敗 0 / 警告 0** |

---

### 4-303. **刻み込みの受け入れ幅は、犯人ではありませんでした**（2026年9月3日、H8）

4-295 の「相手のいない A の稜（長さ 0.08）に対し、**中点がぴったり一致する
B の稜、長さ 1.5**」を追いました。

#### 読み方をまず確かめました

診断の `いちばん近い相手 B 中点まで 0.000000000 長さ 1.500000000` は、
**中点どうしの距離が 0** で、**相手の弦の長さが 1.5** という意味です
（コードで確認）。

**A の短い片が、B の長い稜のちょうど真ん中に乗っています。**
「A は割ったのに B は割っていない」という形です。

#### 刻み込みは、既に全部の頂点を集めていました

4-295 の追記で「全部の片の頂点を互いに刻み込む」を試して効果 0 でしたが、
**理由が分かりました**——`imprint_vertices_on_edges` は、渡された点に加えて
**すべての面のすべての稜の端点を自分で集めています**（`add_point` の 2 つ目の
ループ）。**私の追加は、既にあるものを二重に渡していただけ**でした。

#### では、なぜ刻めないのか——測りました

点を稜の上に載せる判定（`curve_parameter_of_point`）は、**外れが
`tol.linear * 10`（絶対 1e-5）を超えたら諦めます**。読んだ立体は面が
**5.6e-4 まで粗さを持つ**ので（4-285）、**そこに届いていないのでは**、と
読みました。

**外れました。** 諦めた 9,166 件の分布はこうです。

| | |
| :--- | ---: |
| 最小 | **1.760731e-2** |
| 中央 | 5.367115e-1 |
| 最大 | 9.983004e-1 |
| **1e-5 と 1e-3 のあいだ** | **0 件** |

**惜しかったものは1件もありません。** 諦めているのは「そもそも別の場所に
ある点」だけで、**受け入れ幅を緩めても何も変わりません**。

#### 次に触る人へ

**刻み込みは、正しく動いています。** A の短い片が B の長い稜に刻まれないのは、
**その点が B の稜の上に無い**からです（外れが 1.76e-2 以上）。

**つまり、A と B は「同じ線」を共有していません。** 中点だけが一致して、
**線そのものがずれています**。次は

- A の短い片の**端点**が、B の稜からどれだけ離れているか
- その 2 本が**本当に同じ交線から出たのか**

を見てください。**「割り方の食い違い」ではなく「別の線」かもしれません。**

#### 残したもの

`ZENITH_IMPRINT_WHY=1` で、**受け入れ幅のすぐ外にいるもの**（`tol.linear` の
1000 倍まで）だけを出します。**全部出すと 41,672 行**になり、そのほとんどは
関係ありません。

---

### 4-304. **継ぎ目をまたいで一周する切り込み**（2026年9月3日、H8）

4-303 の「A の短い片は B の稜の上に無い」を受けて、**割れなかった側**から
入り直しました。`linkrods.step` の断りを、**鎖ごとに**数え直します。

#### まず、測り方に穴がありました

`ZENITH_SPLIT_WHY=1` の断り文は**面片ごと**に出ます。鎖は割ったあとの片
すべてに当て直されるので、**「別の片に属する鎖」も必ず1回は「境界から
離れている」と断られます。**

その1行を数えると、外れがこう散って見えます。

| 外れ | 件数（3演算ぶん） |
| ---: | ---: |
| 0.090 〜 0.465 | 42 |
| **1.086 〜 3.318** | **39** |

**「交線が途中で切れている」と読みかけました。** 差し渡し 5.02 の部品で
3.3 は、SSI が止まっているとしか読めません。

**鎖ごとに1行にまとめたら、3.3 は消えました。** 知りたいのは「この鎖は
**どこかで**当たったか」で、面片ごとの1行ではありません。

#### 当たらなかった 15 本の内訳

| 断り方 | 本数 | 意味 |
| :--- | ---: | :--- |
| `the splitting curve ends X away from the boundary` | 8 | 外れ **0.103 〜 1.086** |
| `both ends of the splitting curve land at the same place` | 4 | **閉じた輪** |
| `a cut made of 4 curves has 0 loose ends, not two` | 3 | **閉じた輪** |

**7 本が閉じた輪です。** どちらの文も「境界に届かない」ではなく、
**輪が閉じている**という意味でした。

#### 外した手（1つ目）——「穴を開ければいい」

平面には**面の内部で閉じる輪を刻む**経路があります
（`split_planar_face_by_interior_loop`）。**NURBS には無い**ので、
そこだと読んで書きました。uv の取り方だけが違う、100 行ほどのものです。

**1本も当たりませんでした。** しかも `linkrods` の3演算が **105 秒 → 220 秒**
になりました（面の外周 16 点を毎回 NURBS へ射影するため）。

**止めたのは、自分で入れた門でした**——`Imprinting a loop that crosses the
surface seam is not implemented`。**そしてその門が正しかった**のです。

**閉じた輪は穴ではありませんでした。** 円柱を一周する交線は、uv では
`v = 一定` の**まっすぐな線**で、`u` の端から端まで届きます。面は
**穴が開くのではなく、上下2つに分かれます**。

**書いたものは戻しました。** 残したのは診断だけです。

#### では、なぜ「両端が同じ場所」なのか——測りました

閉じた曲面から来た面は、境界に**継ぎ目の辺を2本**持ちます。3D では同じ線
なので、そこを通る点は**巡回上の2か所**に居ます。`locate_on_wire` は
**いちばん近い1か所しか返しません**。

断り文に「その点は境界の辺**何本**の上に居るか」を足して数えました。

```text
both ends of the splitting curve land at the same place
  (start sits on 2 boundary edges, end on 2)   ← 12 件すべて
```

**2本です。** 「同じ場所」ではなく、**2か所あるのに1つしか見ていません**でした。

#### 直したもの

`locate_on_wire_avoiding` を足し、両端が同じ所に落ちたときだけ、
**もう一方の継ぎ目の辺**へ片端を移します。`limit` より遠ければ移しません
——**近いから選ぶ**のであって、**他が無いから選ぶ**のではありません。

実測（`ZENITH_SPLIT_WHY=1` の `SEAMSPLITWHY`）:

```text
境界の辺 8 本、from 0.2950 to 0.2950 → ずらし先 Some(5.705)
境界の辺 8 本、from 0.7650 to 0.7650 → ずらし先 Some(5.235)
```

**割合が 1.0 に足ります**（0.295 + 0.705、0.765 + 0.235）。向きの逆な
継ぎ目の対で、狙ったとおりの場所です。

#### 結果——**前進の途中です。断りは減っていません**

| | 前 | 後 |
| :--- | ---: | ---: |
| `both ends land at the same place` | 12 件 | **0 件** |
| 次の門 `piece 0 encloses no area` | 0 件 | **12 件**（1.110e-16 of 1.000e0） |
| **相手のいない稜** | 64 / 64 / 60 | **64 / 64 / 60** |

**`linkrods.step` はまだ断られます。** 壁が1枚ぶん奥へ動いただけです。

#### 次の門を測りました

```text
piece 0 encloses no area (1.110e-16 of 1.000e0,
  uv の広がり u 1.000e0 v 7.050e-1, 外周の稜 7 本)
```

- **`u` は全域（1.0）、`v` は 0.705。** 矩形としての大きさは足りています
- **外周の稜は 7 本。** 境界 8 本のうち 6 本＋切り込み 1 本——**巡回の
  取り方は狙いどおり**です
- それでも囲む面積は **1.110e-16**

**潰れているのではなく、符号で打ち消し合っています。**

**ここで一度「潰れているだろう」と書き、`u` の広がりが 1.0 だったので
「予想が外れた」と書きました。それも間違いでした**——潰れ方の予想が
外れただけで、**原因の予想（継ぎ目が同じ `u` に落ちている）は当たって
いました**。`ZENITH_UV_DUMP=1` で点の並びを出すと、そのまま見えます。

#### 現物（`ZENITH_UV_DUMP=1`）

```text
UVDUMP 辺 0: (1.0000 0.2350) -> (1.0000 0.1175) -> (1.0000 0.0000)
UVDUMP 辺 1: (1.0000 0.0000) -> (0.8750 0.0000) -> (0.7500 0.0000)
UVDUMP 辺 2: (0.7500 0.0000) -> (0.6250 0.0000) -> (0.5000 0.0000)
UVDUMP 辺 3: (0.5000 0.0000) -> (0.3750 0.0000) -> (0.2500 0.0000)
UVDUMP 辺 4: (0.2500 0.0000) -> (0.1250 0.0000) -> (0.0000 0.0000)
UVDUMP 辺 5: (1.0000 0.0000) -> (1.0000 0.1175) -> (1.0000 0.2350)
UVDUMP 辺 6: (1.0000 0.2350) -> (0.5000 0.2350) -> (0.0000 0.2350)
```

**飛んでいるのは1か所だけです。** 辺 4 は `u = 0` で終わり、辺 5 は
`u = 1` から始まります。**辺 5 は `u = 0` 側の継ぎ目**なのに、`u = 1` に
落ちています。だから巡回は「下辺で 1 → 0、継ぎ目で戻る、切り込みで
1 → 0」となり、**往復して面積が消えます**。

#### 3D の閉性では、これは絶対に見つかりません

`piece 0` の 3D ワイヤは**閉じています**。`u = 0` と `u = 1` は 3D で
**同じ線**なので、辺 4 の終点と辺 5 の始点は**同一の点**です。
`is_closed(tol)` は何も言いません。**壊れているのは uv の側だけ**です。

**「閉じているから正しい」は、継ぎ目のある面では成り立ちません。**

#### 直しました——**相手のいない稜が 64／64／60 → 52／52／48**

読み違えていたのは**どこが曖昧か**でした。継ぎ目の側そのものではなく、
**どちらの巡回に、どちら向きの切り込みを付けるか**です。

普通はそれが 3D の端点で決まります。**継ぎ目でずらしたときだけ決まりません**
——切り込みの両端は 3D で**同じ点**なので、**どちらの組み合わせでもワイヤは
閉じます**。片方は正しい2領域、もう片方が上の「往復して面積 0」です。

**閉性では選べないので、面積で選びます。** どちらも通らなければ、
**最初の組み合わせの理由で断ります**——「試したが両方だめ」を「割れた」に
化けさせません。

| | 前 | 後 |
| :--- | ---: | ---: |
| `piece 0 encloses no area` | 12 件 | **0 件** |
| 向きの入れ替えが効いた回数 | — | **18 回** |
| **相手のいない稜**（和／差／積） | 64 / 64 / 60 | **52 / 52 / 48** |
| 面片 | 41 / 24 / 21 | **49 / 32 / 19** |

**`linkrods.step` はまだ断られます。** 減りましたが 0 ではありません。

#### 次に触る人へ

残る 52 本は、**まだ測っていません**。9/3 の内訳（閉じた輪 7 本、境界から
0.103〜1.086 が 8 本）は**この修理の前の数字**です。**まず数え直して
ください**——4-291 の数字を引きずって 4-304 を読み違えたのと同じ轍です。

そのうえで、下の一族はまだ塞がっていません。

**曖昧なのは 2 つあって、片方しか手当てされていません。**

| | 曖昧さ | いまの値 | 正しい値 |
| :--- | :--- | ---: | ---: |
| **辺 5**（継ぎ目） | 丸ごと継ぎ目の上 | `u = 1` | **`u = 0`** |
| **辺 6**（切り込み） | **両端が継ぎ目の上** | `1 → 0` | **`0 → 1`** |

`settle_seam_segment_axis`（4-288）は**丸ごと端に張り付いている区間**しか
曖昧と見ません。**辺 6 は中が全域に散っているので曖昧に数えられず**、
`1 → 0` のまま残ります。すると辺 5 の寄せ先を決めるとき、隣が
`0`（辺 4）と `1`（辺 6）で**割れます**——平均は 0.5 で、意味がありません。

つまり**辺 5 だけを直しても、飛ぶ場所が辺 5→辺 6 に移るだけ**です。
**両端が継ぎ目に乗る区間**（一周する切り込みがまさにそれ）も曖昧として
扱い、**巡回の連続性で 2 つ同時に決める**必要があります。

**`is_closed` の隣に、uv の連続性を見る検査を置いてください。** それが
無いあいだ、この一族は毎回「面積が 0」まで進んでから初めて見えます。
上の修理は**面積で選び直しているだけ**で、**uv の巡回が飛んでいること
そのものは、まだ誰も検査していません**。

#### 今日の外し方（2つ）

1. **「穴を開ければいい」**——NURBS に内部の輪を刻む経路を 100 行書いて、
   **1本も当たらず**、3演算が **105 秒 → 220 秒**。戻しました
2. **「p-curve が uv で潰れている」**——潰れていませんでした。ただし
   **原因の読み（継ぎ目が同じ `u` に落ちている）は当たって**おり、
   「外れた」と書いた**その記述のほうが間違い**でした

**測る前に書かないでください。** そして**書いたものを、測ったあとに
読み直してください。**

#### 残したもの

| 口 | 中身 |
| :--- | :--- |
| `ZENITH_READ_CUT_ONLY=linkrods` | **1ファイルだけ回します。** 診断を振るたびに `screw` の 8〜48 分割を待たなくて済みます（1回あたり 2 分）。**門は既定のまま全部回します** |
| `CHAINWHY 鎖 N（M 本）` | **鎖ごとに1行。** 当たった回数か、いちばん惜しい外れ。上の落とし穴を二度踏まないための口です |
| `STITCHWHY   その相手の弦まで` | 相手のいない稜の**始点・終点・中点**が、いちばん近い相手の弦からどれだけ離れているか。**中点の一致は「同じ線の上」を意味しません** |
| `SEAMSPLITWHY` | 継ぎ目のずらし先。境界の辺の本数と、移す前後の巡回座標 |

#### 門は全部そのまま

| | |
| :--- | :--- |
| テスト | **137 / 695 / 失敗 0 / 警告 0** |
| `screw.step` | 3演算 ok、恒等式 **3.845e-13 / 3.899e-13** |
| `contact_placement_probe` | 41 配置、破れ 0（最悪 3.900e-8） |
| `scale_sweep_probe` | 破れ 0・非多様体 0・断り 0（最悪 7.394e-8） |
| `rechained_boolean_probe` | 10 連鎖 30 演算すべて ok（最悪 1.906e-7） |
| `foreign_cross_pair_probe` | 15 組、破れ 0（最悪 9.429e-10） |
| `mesh_watertight_probe` | すべて閉じている |

---

### 4-305. **閉じた輪は切り込みではなく、穴でした**（2026年9月4日、H8）

#### まず、測り方の穴を1つ塞ぎました

`read_and_cut_probe` の**既定の予算が 120 秒**でした。`linkrods` は
**131.7 / 144.4 / 141.8 秒**で返って断りを返すのに、**「120 秒で返りません」
と表示され、断りの中身が見えていませんでした。**

**300 秒に上げました。** 上限は、いま掛かる時間の 2 倍くらいに置いて
ください——**詰めすぎると、遅いだけのものが「止まった」に化けます。**

引継書の「**3演算とも 80 秒で返る**」も**古い数字**でした。80 秒は
**接する置き方**（`ZENITH_CUT_TANGENT=1`）のもので、4-294 で既定が
普通の置き方に変わってからは当てはまりません。入口の行を直しました。

#### 外した手: 閉じた輪を、境界の上の頂点でほどく

4-304 で「当たらなかった鎖のうち **7 本が閉じた輪**」と分かっていました。
輪が**面の境界（継ぎ目）を通っていれば**、それは穴ではなく面を2つに分ける
切り込みで、境界の上の頂点でほどけば `order_chain` を通せます。

**書いて測ったら、何も動きませんでした。**

| | 手の前 | 手の後 |
| :--- | ---: | ---: |
| 相手のいない稜 | 164 | **164** |
| 選ばれた面片（和／差／積） | 45 / 28 / 19 | **45 / 28 / 19** |
| 当たった分割・見送った分割 | 25 / 14 | **25 / 14** |
| 断り文 | | **1バイト違わず** |

**ほどく場所が1つも見つからなかった**からです。**捨てました。**

#### なぜ見つからないのか——数字が出ました

修理の代わりに**測り口を残しました**（`ZENITH_CLOSED_CHAIN_WHY=1`）。
閉じた鎖について、**その頂点が面の境界からどれだけ離れているか**を出します。

```text
CLOSEDCHAIN 鎖 4 本: 境界までいちばん近い頂点で 3.450000e-1
```

**0.345 ちょうど。** 12 回とも同じ値で、**どの頂点も同じだけ離れています。**

**つまり、この輪は本当に面の内部で閉じています。** 切り込みではなく
**穴**です。面を2つに割る話ではなく、**内側のワイヤを1本足す**話でした。
`split_by_chain` は「面を2つに割る」しか知らないので、**どう直しても
ここは通りません。**

#### 縫合の外れも、丸い数字でした

`ZENITH_STITCH_WHY` の内訳で、いちばん多い外れが **0.176776695** で
**222 本**。これは **0.25/√2 ちょうど**です。始点も終点も同じ距離で、
中点だけ遠い——**A の稜は B の弦と平行にずれています。**

**公差の話ではありません**（4-303 で幅を緩めても何も変わらないと測って
あります）。**A と B は、0.25 ずつずれた別の線です。**

#### 次に触る人へ

**次の一手は「面の内部で閉じる輪を、穴として入れる」**です。これは
公差でも並べ方でもありません。

**→ 訂正**（4-306）。**「`FaceSplitter` が持っていない機能」と書きましたが、
4-50 からあります**（`split_by_interior_loop`）。呼ばれてもいて、
**交線を丸ごと渡していた**のが誤りでした。**「無い」と書く前に、探して
ください。**
平面には経路がありますが、**NURBS にはありません**（`face_split.rs` の
`order_closed_loop` のあたり）。

**「切り込みとして通す」道はもう歩かないでください**——上の表のとおり、
1バイトも動きません。

---

### 4-306. **内側の輪も、鎖ごとに渡さないと当たりません**（2026年9月4日、H8）

4-305 で「閉じた輪は切り込みではなく穴だった」と分かりました。では穴として
入れる機能を書くのか——**書く必要はありませんでした。もうあります。**

`FaceSplitter::split_by_interior_loop` は 4-50 からあり、
`brep_intersection.rs` の「最後の受け皿」から呼ばれています。**なのに
一度も当たっていませんでした。**

#### なぜ当たらなかったか

**交線を丸ごと渡していた**からです。

```rust
crate::FaceSplitter::split_by_interior_loop(face, split_edges, tol)
//                                                ^^^^^^^^^^^ 32 本まとめて
```

`linkrods.step` の 32 本は **4 つの輪**に分かれています。まとめて渡すと
`order_closed_loop` は必ず落ちます。**鎖ごとに渡さないと当たりません。**

**4-304 で `split_by_chain` に対して直したのと、同じ誤りです。** 片方を
直したときに、**隣の呼び口を見ませんでした。**

#### 直した結果

| | 前 | 後 |
| :--- | ---: | ---: |
| 当たった分割 | 25 | **37** |
| 相手のいない稜 | 164 | **143** |
| 選ばれた面片（和／差／積） | 45 / 28 / 19 | **46 / 29 / 19** |

**閉じた輪 12 本が全部入りました**（25 + 12 = 37）。**`linkrods` はまだ
断られます**——残り 143 本は別の一族です。

#### 次に触る人へ

**同じ形の誤りが他にもないか**を見てください。`split_edges` を丸ごと渡して
いる呼び口は、**鎖に分かれた入力に対して黙って落ちます**——断り文は出ますが、
「輪が1つも無い」ではなく「輪が繋がらない」と読めるので、**気づきにくい**
です。

---

### 4-307. **2つ目の穴は、入れると分割は増えるが縫合は悪くなります**（2026年9月4日、H8）

4-306 のあと、**閉じた輪 12 本のうち 9 本がまだ落ちて**いました。理由を
出す口を足したら（`ZENITH_SPLIT_WHY` に「内側の輪としても断られました」を
添えました）、**9 本とも同じ1行**でした。

```text
splitting a face that already has holes is not implemented
```

**1つ目の輪で穴を開けた面に、2つ目の輪が入れられません。**

#### 入れて、測りました

`split_by_interior_loop` の早期の断りを外し、**元からある穴は外側の片が
引き継ぐ**ようにしました（入れ子は `checked_areas` が見ます——別の判定を
足さず、もうある物差しに任せます）。

| | 4-306 の後 | 2つ目の穴も入れて |
| :--- | ---: | ---: |
| 当たった分割 | 37 | **41**（9 本のうち 4 本が入った） |
| **相手のいない稜** | **143** | **146** |
| 選ばれた面片（和／差／積） | 46 / 29 / 19 | 46 / 29 / **21** |

**分割は増えましたが、目標の数字は悪くなりました。**

#### 入れませんでした

**+3 が何なのかを言えないからです。** 面の番号は分割が増えるとずれるので、
**集合の差では追えません**（実測でそう読みかけました——`面 264` と
`面 270` は同じ面です）。

**理由が言えないまま目標を悪くする手は入れない**、という約束どおりです。
**診断だけ残しました**——次に来た人が、この1行を自分で見られるように。

#### 追記: 口は要りませんでした。**使い方の問題**でした

「面の番号に依らずに追える口を作れ」と書きましたが、**作る必要は
ありませんでした**。`STITCHWHY unmatched` は**座標を出しています**。
番号を落として比べるだけです。

```bash
grep "STITCHWHY unmatched" out.txt   | sed -E 's/.*unmatched (.) 面 [0-9]+ / /' | sort -u
```

**新しい測り口を足す前に、いまの出力を使い切ってください。** 私は
「番号でしか追えない」と書きかけました——**出ているものを見ていなかった
だけ**です。

#### そうしたら、+3 が読めました

稜の**のべ**ではなく**別々の稜**で数えると **96 → 100** です。

| | |
| :--- | :--- |
| **消えたもの** | **B 側 4 本**。`x = 3.275609` と `x = 7.994676` の**縦の稜** |
| **増えたもの** | **A 側 8 本**。**同じ x** の上 |

**2つ目の穴は、B 側の相手のいない稜を A 側に付け替えて、さらに 4 本
増やしています。** 進んでいません。**入れない判断は正しかった**です。

そして**場所が名指しできました**——`x = 3.275609` と `x = 7.994676`、
**円柱の継ぎ目**です。**次はそこを見てください。**

なお、この手（2つ目の穴）はもう書いてあります——`split_by_interior_loop`
の早期の断り2行を消して、穴を `outside` に配るだけです。**測り直しは 7 分。**

---

### 4-308. **相手のいない稜の3割は、切り口の上にあります**（2026年9月4日、H8）

4-307 の追記のやり方（面の番号を落として座標で数える）を、**いまの状態
そのもの**に当てました。**のべ 143 は、別々の稜で数えると 96 本**です。

| | 本数 |
| :--- | ---: |
| A 側 | **74** |
| B 側 | **22** |

#### 高さで分けると、山が2つあります

| z | 本数 | それは何か |
| ---: | ---: | :--- |
| **1.410000** | 14 | **切り手の上面** |
| **0.470000** | 16 | **切り手の底面** |
| 1.349930816 | 20 | 丸棒の接する円 |
| 0.599930816 | 4 | 同上（下側） |
| 1.599930816 / 0.349930816 | 各 1 | 同上 |
| 1.875 / 0.125 | 各 2 | |
| （両端の z が違う＝縦の稜） | **36** | |

切り手は**高さ 0.94**（`span.z * 0.47`）なので、**`z = 0.470` と
`z = 1.410` は切り手の底面と上面そのもの**です。**30 本、3 割が
切り口の上にあります。**

#### これは 4-293 を覆します

4-293 は「**切り口の上に1本も無い**（切断面 z=1.0 に 0 本）」でした。
**あれは切り口が z=1.0 だった頃の数字**です。**4-294 で切り手の置き方を
変えたのに、この結論を持ち越していました。**

**測り方を変えたら、その測り方に依っていた結論も測り直してください。**
4-304 でも同じことを書きました（「4-291 の数字を引きずって読み違えた」）。
**同じ轍です。**

#### 次に触る人へ

**まず `z = 0.470` と `z = 1.410` の 30 本**を見てください。切り口の上の
稜は、**A と B の両方が同じ平面を割って作るもの**なので、片方だけが持つ
のは筋が通りません。**いちばん筋の悪いところが、いちばん数が多い**です。

次が `x = 3.275609` / `x = 7.994676` の**円柱の継ぎ目**（4-307）。

数え方はこれです（**新しい口は要りません**）。

```bash
ZENITH_READ_CUT_ONLY=linkrods ZENITH_READ_CUT_BUDGET=900 ZENITH_STITCH_WHY=1   cargo run --release --example read_and_cut_probe 2>&1   | grep "STITCHWHY unmatched"   | sed -E 's/.*unmatched (.) 面 [0-9]+ / /' | sort -u
```

---

### 4-309. **切り口の稜と、数え間違い**（2026年9月4日、H8）

4-308 で名指しした稜を、そのまま読みました。

#### 確かなこと: **A は円を1本のまま持ち、B は割っています**

```text
A (4.392234284 3.250000000 0.470000000) -> (4.392234284 3.250000000 0.470000000)
                                            mid (3.357765716 3.250000000 0.470000000)
```

**始点と終点が同じ**で、中点が遠い——**閉じた円**です。**和・差・積の
どれでも 4 本**、`z = 0.470` と `1.410` に `x ≒ 3.875` と `x ≒ 7.75` で
2 本ずつ。**どれも A 側で、B 側に同じ円が1本もありません。**

4-303 の「**A と B は同じ線を共有していません**」が、ここで形になりました
——**片方は円を丸ごと1本の稜として持ち、もう片方は継ぎ目で割っています。**
どんな公差でも合いません。

切り口の上の割合は、演算ごとにこうです。

| | 相手のいない稜（別々の稜） | うち切り口の上 |
| :--- | ---: | ---: |
| 和 | 47 | **13** |
| 差 | 50 | **15** |
| 積 | 46 | **15** |

#### 外した読み: 「B が同じ稜を2度作っている」（**数え間違いでした**）

「差が **5e-9〜2.3e-8** の組が 4 つあり、公差 5e-6 の中にいるのに合って
いない」と書きかけました。**間違いです。**

**3演算の出力を1つのファイルに混ぜて数えていました。** 近すぎる2本は
**別々の演算のもの**で、**同時には存在しません**。演算ごとに分け直したら、
**どの演算でも近すぎる組は 0 でした。**

**「相手のいない稜は 96 本」も、混ぜた数字です。** 正しくは
**和 47 / 差 50 / 積 46**。

**同じファイルに 3 演算を出す口で数えるときは、必ず演算で分けてください。**

```bash
awk '/走らせています/{if(/union/)op="union"; else if(/difference/)op="difference";
     else if(/intersection/)op="intersection"} /STITCHWHY unmatched/{print op" "$0}' out.txt   | sed -E 's/^([a-z]+) .*unmatched (.) 面 [0-9]+ /  /' | sort -u
```

#### その判断は、もう決まっていました

「円を1本のまま持つのか、割るのか」を**設計の判断**と書きましたが、
**カーネル自身がすでに答えを持っています。**

```rust
// crates/zenith_algo/src/primitive.rs:179
// 4本の底面円弧 & 4本の天面円弧
```

`make_cylinder` も `make_cone` も、**円は 4 本の四半弧**で持ちます。
**閉じた円を1本の稜として持つほうが、規約から外れています。**

そして、この円は**ファイルから読んだものではありません**——`x ≒ 3.875`、
`z = 0.470` は**丸棒の円柱と切り手の底面の交線**で、**交線を作る側が
1本の閉じた輪として出しています**（4-305 で見た「閉じた鎖」がこれです）。

#### 待ち伏せて見た中身（`ZENITH_STITCH_WATCH="3.875,3.25,0.47"`）

和のとき、その場所にいるのはこれだけです。

```text
STITCHWATCH A 面 273 (4.392234 3.250000 0.470000) -> (4.392234 3.250000 0.470000)
                                                   mid (3.357766 3.250000 0.470000)
STITCHWATCH A 面 273 (4.392234 3.250000 0.470000) -> (4.392234 3.250000 0.000000)
                                                   mid (4.392234 3.250000 0.235000)
```

**閉じた円を持っている面は、和では 面 273 の1枚だけ**です。相手が
「少しずれた所に居る」のではなく、**そもそも居ません**。

**ここから先は、まだ測っていません。** 相手であるべき面（円柱側の片）が
**四半弧で持っているのか、そもそも作られていないのか**——**そこを先に
確かめてください。** 私は今日、機構を1つ言い当てそこねています（4-309 の
一族2）。**同じことを繰り返さないよう、測ってから書いてください。**

#### 次に触る人へ

**交線が閉じた輪になったら、四半弧に割ってから稜にしてください。**
規約はもう決まっているので、設計の判断は要りません。**割る場所は、
カーネルが素形状で使っているのと同じ 4 点**（`x = ±r`、`y = ±r` の側）
に取れば、素形状と読んだ立体で同じ形になります。

**これは 4-306 と同じ場所の話です**——4-306 は閉じた輪を**面に穴として
入れる**ところまで進めました。残っているのは**稜として持つ形**のほうです。

---

### 4-310. **相手が居ないのは、平面が輪を1つも入れていないからです**（2026年9月4日、H8）

4-309 の閉じた円について、**相手であるべき面を探しました。**

`ZENITH_STITCH_WATCH` の半径は 0.6、円の半径は 0.517 なので、**待ち伏せは
円を丸ごと覆っています**。そこに居るのは **面 273 の2本だけ**——
**円柱側も、四半弧すら持っていません。**

相手は **B 側の切り口の平面**のはずです。そこを出しました
（`ZENITH_SPLIT_WHY` の `PLANARLOOPWHY`）。

```text
PLANARLOOPWHY 平面の内側の輪: 稜 14 本 → 輪 2 個、輪に入らなかった稜 12 本
              （**輪はあるのに、余りがあるので丸ごと諦めます**）
PLANARLOOPWHY 平面の内側の輪: 稜 12 本 → 輪 2 個、輪に入らなかった稜 10 本
```

**輪は見つかっているのに、1つも入っていません。** `split_planar_face_by_interior_loops`
は「**輪に入らない稜が1本でもあれば丸ごと諦める**」ので、**閉じた輪と
開いた鎖が混ざった交線では、輪のほうも入りません。**

#### 緩めて測ったら、悪くなりました

| | 前 | 余りを許して |
| :--- | ---: | ---: |
| 相手のいない稜（和／差／積） | 47 / 50 / 46 | **55 / 56 / 54** |
| 当たった分割 | 37 | **29** |
| 選ばれた面片 | 46 / 29 / 19 | 35 / 29 / 19 |

**あの門は理由があってそこにいました。** この経路は「**輪だけ入れて早く
返る**」ので、緩めると**余った鎖が捨てられます**。**戻しました。**

#### 次に触る人へ

直すなら、**緩めるのではなく、余った鎖も同じ流れで当てる**ようにして
ください。形は 4-306 と同じです——**鎖ごとに当て、当たった片に次の鎖を
当て直す**。いまの平面の経路は「全部いっぺんに、さもなくば何もしない」
です。

**門を緩める前に、その門が何を守っているかを見てください。** 今日はこれで
2回目です（4-307 の2つ目の穴も、入れたら目標が悪くなりました）。

---

### 4-311. **「余った鎖も流す」を書いて測ったら、まだ悪いままでした**（2026年9月4日、H8）

4-310 で「**緩めるのではなく、余った鎖も同じ流れで当てる**」と書きました。
**書きました。測りました。まだ悪いです。**

作りはこうです。`split_planar_face_by_interior_loops` を本体と薄い皮に
分け、本体に「余りを許すか」を足して、許したときは**輪に入らなかった稜を
一緒に返す**ようにしました。呼び出し側（`split_face_by_edges`）では、
**輪だけ先に入れて、余りを下の1本ずつの流れに渡します**——捨てません。

| | 素のまま | 雑に緩める（4-310） | **余りを流す（これ）** |
| :--- | ---: | ---: | ---: |
| 相手のいない稜（和／差／積） | **47 / 50 / 46** | 55 / 56 / 54 | 51 / 51 / 50 |
| 当たった分割 | **37** | 29 | 36 |
| 選ばれた面片 | 46 / 29 / 19 | 35 / 29 / 19 | 40 / 31 / 21 |

**雑に緩めるよりは良く、素のままよりは悪い。** 入れませんでした。

#### 疑って、外れました

**「輪で穴が開いた片に、次の切り込みが入らないのでは」**と疑いました
（`splitting a face that already has holes is not implemented`、4-307）。

**数えたら 9 件で、変える前と同じ**でした。**原因ではありません。**

今日これで**3回目**です（4-309 の一族2、4-310 の門、これ）。
**思いついたら、書く前に数えてください。** 私は3回とも、数えたら外れて
いました。

#### 自分の手順どおりに数えたら、出どころが分かりました

演算ごとに、座標で突き合わせました（**追加の計算は要りません**——出力は
取ってあります）。

**手そのものは効いています。**

| **相手のいない閉じた円** | 素のまま | 余りを流す |
| :--- | ---: | ---: |
| 和 | A 4 | A **3** |
| 差 | A 4 | A **1** |
| 積 | A 4 | A **1** + B 3 |
| **合計** | **12** | **5** |

4-309 で名指しした一族が、**12 本から 5 本に減っています。** 積では
**B 側にも同じ円が現れました**——**狙いどおり相手が作られています。**

**代わりに壊れたのは、B の外周です。** 増えた稜は**ほぼ全部 B 側の
`z = 1.410`**（切り手の上面）で、こういう形をしています。

```text
B (7.994676166 2.545000000 1.410000000) -> (4.514905614 2.545000000 1.410000000)
B (4.514905614 2.545000000 1.410000000) -> (4.227334650 2.545000000 1.410000000)
B (4.227334650 2.545000000 1.410000000) -> (4.130880814 2.545000000 1.410000000)
B (4.130880814 2.545000000 1.410000000) -> (3.619119186 2.545000000 1.410000000)
```

**`y = 2.545` の1本の辺が、4 つに割れています。** 割れ目
（`x = 4.1309`、`4.2273`、`4.5149`）は**刻み込みの点**です。

**輪を入れた片の外周が、相手より細かく割れています。** 相手側は同じ辺を
別の割り方（あるいは1本のまま）で持っているので、合いません。

#### 次に触る人へ

**道は合っています。** 閉じた円は 12 → 5 に減りました。**残るのは、
輪を入れたときに外周を細かくしすぎない**ことです。

**`planar_region_face` ではありません**——そこは外周を
`face.outer_wire.clone()` のまま使います（`brep_intersection.rs:2380`）。
**書いてから確かめて、外しました。今日 4 回目です。**

細かくしているのは、**輪を入れたあとに当てる「余りの鎖」のほう**です。
輪を入れた片に鎖を当てると外周が割れますが、**相手の面は別の順番で
割られている**ので、割れ目が揃いません。

つまり問題は「輪を先に入れる」ことではなく、**輪を先に入れると、
そのあとの割り方が相手と揃わなくなる**ことです。**揃え方**を決めるのが
次の一手です——たとえば**両側で同じ順番に当てる**、あるいは**輪を入れる
前に刻み込みを済ませる**。

**測り方**（これは今日確かめたやり方です）:

```bash
# 演算ごとに、座標で（面の番号は分割が増えるとずれます）
awk '/走らせています/{if(/union/)op="union"; else if(/difference/)op="difference";      else if(/intersection/)op="intersection"} /STITCHWHY unmatched/{print op" "$0}' out.txt   | sed -E 's/^([a-z]+) .*unmatched (.) 面 [0-9]+ /  /' | sort -u
```

---

### 4-312. **4-306 を、全部の門で確かめました**（2026年9月4日）

4-306（内側の輪を鎖ごとに渡す）は、**すべてのブーリアンが通る道**を
変えています。**通しテストと掃き出しだけでは足りません。**

| 門 | 結果 |
| :--- | :--- |
| `cargo test --workspace --release` | **137 バイナリ / 695 件 / 失敗 0 / 警告 0** |
| `contact_placement_probe` | 47配置141演算、**135 ok / 6 断り（未実装 0）**、B-Rep・メッシュとも非多様体 0、恒等式 41 配置で破れ 0、最悪 **3.900e-8** |
| `mesh_density_probe` | **8 件中 0 件**がどこかの刻みで壊れる（10 通りの刻み） |
| `foreign_cross_pair_probe` | 15 組そろい、**破れ 0**、最悪 **9.429e-10** |
| `face_budget_probe` | 最悪 **3,854 枚**（H5 は 10,000 以下） |
| `rechained_boolean_probe` | **破れ 0、非多様体 0、断り 0**（全段） |
| `mesh_watertight_probe` | **すべて閉じている** |

**道を変える修理を入れたら、7つ全部を通してください。** 通しテストと
掃き出しだけでは、刻みを振ったとき・連鎖したとき・他カーネル由来の形の
ときが見えません。

---

### 4-313. **平面には専用の鎖の段があり、汎用の受け皿に来ません**（2026年9月4日、H8）

4-310 で「B の切り口の平面は輪を1つも入れていない」と分かりました。
**なぜ 4-306 の手当てが効かないのか**を読んで、構造が分かりました。

```text
split_face_by_edges
 ├ 内側の輪（全部いっぺんに、さもなくば何もしない）   ← 4-310 でここが諦めている
 ├ 1本ずつ当てる
 ├ **平面専用の鎖の段**（split_planar_face_by_edge_chain）  ← 平面はここで割れる
 └ 汎用の鎖の段（applied_split_count == 0 のときだけ）   ← 4-306 の手当てはここ
```

**平面は専用の段で割れてしまうので、汎用の段に一度も来ません。**

#### 平面の段にも同じ受け皿を置いて、測りました

| | 素のまま | 平面にも受け皿 |
| :--- | ---: | ---: |
| 相手のいない稜（和／差／積） | 47 / 50 / 46 | **47 / 50 / 46** |
| 当たった分割 | 37 | **37** |
| 選ばれた面片 | 46 / 29 / 19 | **46 / 29 / 19** |
| 相手のいない閉じた円 | 4 / 4 / 4 | **4 / 4 / 4** |

**1バイトも動きません。** `split_planar_face_by_edge_chain` は、これらの
鎖に対して**そもそも `Err` を返していません**——受け皿に来ないので、
置いても意味がありません。**入れませんでした。**

**構造は正しく読めましたが、手は空振りです。** 今日 5 回目です。

#### 次に触る人へ

**平面の輪が入る唯一の道は、いまのところ一番上の
「全部いっぺんに、さもなくば何もしない」だけ**です。そこを

- **緩める** → 悪くなる（4-310）
- **輪だけ入れて余りを流す** → 閉じた円は **12 → 5** に減るが、
  **外周の割れ方が相手と揃わなくなる**（4-311）

**4-311 の道が、いまのところいちばん近い**です。残っているのは
**「輪を先に入れたあと、相手と同じ割り方にする」**ことだけです。

---

### 4-314. **順番を逆にしても、輪は入りません**（2026年9月4日、H8）

4-311 の道（輪を先に入れて余りを流す）は、閉じた円を **12 → 5** に減らす
代わりに**外周の割れ方が相手と揃わなく**なりました。そこで**順番を逆に**
しました——**先に相手と同じ割り方で割り、そのあと輪を穴として足す**。
外周は今日までと同じ形のままになるはずです。

どの片に足すかは選ばず、**全部に試して面積が戻ったものだけ採る**ように
しました（`split_by_interior_loop` の物差しに任せ、別の判定を足さない）。

| | 素のまま | 割ってから穴を足す |
| :--- | ---: | ---: |
| 相手のいない稜（和／差／積） | 47 / 50 / 46 | **47 / 50 / 46** |
| 当たった分割 | 37 | **37** |
| 選ばれた面片 | 46 / 29 / 19 | **46 / 29 / 19** |
| 相手のいない閉じた円 | 4 / 4 / 4 | **4 / 4 / 4** |

**また1バイトも動きません。** 輪は、割り終わった片のどれにも入って
いません。**入れませんでした。**

#### 今日の空振りは6つです

**平面に輪を入れる道は、まだ1本も通っていません。** 通っているのは
**曲面側だけ**（4-306。当たった分割 25 → 37、相手のいない稜 164 → 143）。

| 試したこと | 結果 |
| :--- | :--- |
| 門を緩める（4-310） | **悪化**（47/50/46 → 55/56/54） |
| 輪だけ入れて余りを流す（4-311） | **閉じた円 12 → 5**、ただし外周が揃わず全体は悪化 |
| 平面の鎖の段に受け皿（4-313） | **動かず** |
| 割ってから穴を足す（4-314） | **動かず** |

#### 次に触る人へ

**4-311 だけが唯一、狙った数字を動かしました。** 残っているのは
**「輪を先に入れたあと、外周を相手と同じに割る」**ことだけです。

#### 相手も測りました

`ZENITH_STITCH_WATCH="6.0,2.545,1.41"` で、その辺に居るものを出しました
（**素のまま**の状態です）。

```text
B 面 300 (4.514906 2.545000 1.410000) -> (7.994676 2.545000 1.410000)
B 面 303 (7.994676 2.545000 1.410000) -> (4.514906 2.545000 1.410000)
```

**相手は B 自身の側面（面 303）で、辺を `x = 4.5149` から `7.9947` まで
丸ごと1本で持っています。** 面 300 と 303 で向きが逆、**ちゃんと組んで
います。**

**4-311 で増えた割れ目 `x = 4.2273` と `4.1309` は、平面側にしか
ありません。** 側面は割れていないので、合わなくなります。

#### 次に触る人へ

**揃え方は決まりました。** 輪を先に入れるなら、**外周に新しい頂点を
作らない**——さもなければ、**同じ頂点を側面にも入れる**。どちらかです。

`x = 4.5149` は**素のときから両側にある**割れ目なので、**そこまでは
揃っています**。増えるぶんだけが問題です。

---

### 4-315. **少しでも割れた面は、どの鎖の段にも来ません**（2026年9月4日、H8）

4-313／4-314 で「平面に輪を入れる手」を2つ置いて、どちらも動きませんでした。
**なぜ当たらないのかを測りました**——**置いた場所に、そもそも来ていません。**

段の入口に印を置いたら、**1行も出ませんでした。**

#### 構造はこうです

```text
split_face_by_edges
 ├ 内側の輪（全部いっぺんに、さもなくば何もしない）  ← 4-310 でここが諦める
 ├ 1本ずつ当てる                                     ← 切り口の平面はここで少し割れる
 ├ 平面専用の鎖の段   ─┐ どちらも `applied_split_count == 0` のときだけ
 └ 汎用の鎖の段       ─┘ ← 4-306 の手当てはここ
```

**少しでも割れた面は、鎖の段に来ません。** 実測（`ZENITH_LEFTOVER_WHY=1`）:

```text
LEFTOVERWHY 平面 面: 交線 14 本 → 当たった 3、当たらなかった 11、片 4 枚
            → **鎖の段へ行きません**（少しでも割れたので、ここで返ります）
LEFTOVERWHY 平面 面: 交線 12 本 → 当たった 4、当たらなかった 8、片 5 枚
            → **鎖の段へ行きません**
```

**切り口の平面は、14 本中 11 本・12 本中 8 本を当てられないまま返ります。**
**閉じた輪を穴として入れる機会が、一度もありません。**

曲面のほうは `当たった 0` が多く（27 件）、**ちゃんと鎖の段へ行きます**——
だから 4-306 は効きました。**平面と曲面で、通る道が違います。**

#### 正しい場所に置いて、それでも動きませんでした

**1本ずつ割ったあとの片**に、余りを鎖にまとめて穴として当てる受け皿を
置きました。**外周は今日までのままなので、4-314 で分かった「揃え方」の
条件を満たします。** 条件（`applied_split_count > 0` かつ余りあり）は
**満たされています**（3 > 0、余り 11 本）。

| | 素のまま | 正しい場所に受け皿 |
| :--- | ---: | ---: |
| 相手のいない稜（和／差／積） | 47 / 50 / 46 | **47 / 50 / 46** |
| 当たった分割 | 37 | **37** |
| 相手のいない閉じた円 | 4 / 4 / 4 | **4 / 4 / 4** |

**また動きません。**

#### そして、「入れられないから」ではありませんでした

断り文を出したら、こうです（`ZENITH_LEFTOVER_WHY=1`）。

| 何が起きたか | 件数 |
| :--- | ---: |
| `the cut is not a closed loop: ... is used 1 time(s), not 2` | **45** |
| **入った**（片 2 枚、面積残差 **2.015e-14** / **1.300e-16**） | **6** |
| `splitting a face that already has holes is not implemented` | 3 |

**6 件は、面積残差 1e-14 でちゃんと入ります。** 「入れられない」ので
はありません——**入れても、縫合の数字が動かない**のです。

**つまり、この 6 つの輪は、相手のいない閉じた円の相手ではありません。**
4-311 で減った 7 本（12 → 5）は、**もっと上の段（全部いっぺんに）で
入っていた輪**です。**ここではない。**

**私はここで「入れられない」と書きかけました。断り文を見たら違いました。
今日 8 回目です。**

#### 次に触る人へ

**ここまでで分かっていることを、順に並べます。**

1. 平面は鎖の段に来ない（**この記録**）
2. 1本ずつのあとに受け皿を置いても、輪は入らない（**この記録**）
3. 輪を先に入れれば入る——**閉じた円が 12 → 5**（4-311）。ただし
   **外周の割れ方が相手と揃わない**
4. 揃え方は決まっている——**外周に新しい頂点を作らない**か、
   **同じ頂点を側面にも入れる**（4-314）

**2 は「入らない」のではありません**——**6 件は入りますが、縫合の数字が
動きません**。**探す場所は、1本ずつのあとではなく、いちばん上の段
（全部いっぺんに、さもなくば何もしない）です。** 4-311 で閉じた円が
12 → 5 に減ったのは、そこで入った輪です。

**次の一手は 4-311 に戻ることです。** 残っている問題は1つだけ——
**輪を入れた片の外周に、新しい頂点を作らない**こと（4-314 で相手を
測ってあります）。

**今日はこの一族で 7 回空振りし、思いついた機構を 8 回外しました。** 数字は各記録にあります——
**同じ道を歩き直さないでください。**

---

### 4-316. **4-306 は、軽い失敗と重い失敗を取り替えていました**（2026年9月4日、H8）

4-315 が名指しした「なぜ入らないのか」を `ZENITH_LEFTOVER_WHY=1` で読み、
そこから**別の一族**に行き当たりました。

#### まず、記録どおりでした（自分の手で確かめました）

「余りを穴として入れても1バイトも動かない」——**本当でした。**
`ZENITH_LEFTOVER_APPLY=1` を書いて入れてみたら、**穴は 3 回入る**のに
（面積残差 2.015e-14 / 1.300e-16）、断り文は**1バイトも変わりません**。

**理由も分かりました。** 穴が入る面は `当たった 0` なので、そのあと
**鎖の段が `face.clone()` から作り直します**。私が入れた穴は、その時点で
捨てられていました。**既にある 4-306 と同じ場所を二重に叩いていただけ**です。
**書いたものは戻しました。**

#### そこで気づいたこと——**和に非多様体が 9 本あります**

| | 記録（4-315 までの要約） | 実測（9/4） |
| :--- | :--- | :--- |
| 相手のいない稜（和／差／積） | 47 / 50 / **46** | 47 / 50 / **42** |
| **非多様体**（和） | 触れていません | **9 本** |

**2回走らせて1バイト同じ**です。ゆらぎではありません。

**記録された `linkrods` の断りは、これまで全部「非多様体 0」でした。**

#### どの commit かを二分で挟みました

只 1 ファイル（`brep_intersection.rs`）しか動いていないので、そこだけ
戻して測れます。

| commit | 相手のいない稜 | 非多様体 |
| :--- | :--- | ---: |
| `3920f77`（4-305。診断のみ） | 56 / 56 / 48 | **0 / 0 / 0** |
| **`cd32fe9`（4-306。輪を鎖ごとに渡す）** | 47 / 50 / 42 | **9 / 0 / 0** |
| `46d814c`（いま） | 47 / 50 / 42 | 9 / 0 / 0 |

**4-306 ちょうどです。** それ以降は診断だけなので、数字が動きません。

**4-306 は「当たった分割 25 → 37、稜 164 → 143」の勝ちとしてだけ
書かれています。** 重い側が **0 → 9** になったことは、どこにも
書かれていませんでした。

**「良くなった数」を書くときは、「悪くなった数」も同じ行で数えてください。**
このリポジトリで非多様体は、相手のいない稜より**重い**扱いです——
掃き出しの門は、断ることを赤にせず、**非多様体を返すことを赤にします**。

#### 9 本の中身——**余っているのは A の1枚だけ**

`ZENITH_STITCH_WHY=1` に**面の番号**を足して読みました（4-292 と同じ流儀）。

```text
non-manifold x3 A 面 269 (3.2756 3.7008 0.470) -> (3.6191 3.9550 0.470)
non-manifold x3 B 面 292 (3.2756 3.7008 0.470) -> (3.6191 3.9550 0.470)
non-manifold x3 B 面 294 (3.6191 3.9550 0.470) -> (3.2756 3.7008 0.470)
```

**B は正常です。** 292 と 294 は**向き違いの対**——隣り合う2面が同じ稜を
共有する、当たり前の形です。3 本ともそうなっています
（295/298、304/305）。

**余分なのは A の 面 269、ただ1枚**です。3 本とも同じ面から来ています。

#### 素性を測ったら、思っていたものと違いました

「同一面の重なりだろう」と読みました。判定側（`resolve_coincident_face_pieces`）
のコメントが、自分で「**面全体の一致しか見ないので、部分的な重なりは
捕まりません**」と言っているからです。**外れました。**

| 面 | 種類 | 外周の稜 | 内側の輪 |
| ---: | :--- | ---: | ---: |
| **A 269** | **NURBS** | **18 本** | **1 個** |
| B 292 / 294 / 295 / 298 / 304 / 305 | **すべて平面** | 3〜8 本 | 0 |

**曲面と平面なので、同一面ではありません。** A 269 は**穴を1つ持った
大きな曲面**で、その外周が**B が自分たちで閉じている 3 本**を巻き込んで
います。

#### 場所は、4-307 が名指ししたところです

**`x = 3.275609`。** 4-307 の「**次はそこ**」と書かれた円柱の継ぎ目、
**そのもの**です。`z = 0.470` と `1.410` は切り手の底面と上面（4-308）。
**3つの記録が同じ点を指しています。**

#### 次に触る人へ

**A 269 がなぜ 18 本と穴1個になったのか**から見てください。**割られる
べきものが、割られずに巻き込まれています**——面が2つに分かれる代わりに、
**その3本を外周の一部として抱え込んで**います。

**これは割り方の一族ではありません。** 4-305 〜 4-315 が追ってきたのは
全部「輪が入らない」でしたが、これは**入ったあとの形**の話です。

そして**判断が要ります**——4-306 を残すか（軽い失敗が 9 本減って重い失敗が
9 本増える）、戻すか。**私は残しました**。門はどちらでも赤にならず、
`linkrods` はどちらでも断られるからです。**ただし、選んだことを
知らないまま進むのが、いちばん悪い**ので、ここに書いておきます。

#### 残したもの

| 口 | 中身 |
| :--- | :--- |
| `ZENITH_STITCH_WHY=1` の `non-manifold` 行 | **面の番号**が付きました。座標だけでは「どちらが余分か」が言えません |
| その次の行 | その面の**種類・外周の稜の本数・内側の輪の数**。相手が何枚に割れているかで、一致の判定（1枚 対 1枚）が届くかどうかが決まります |

---

### 4-317. **4-306 の穴が、そのまま 9 本の出どころでした**（2026年9月4日、H8）

4-316 で「A 269 がなぜ 18 本と穴1個になったのか」を次の一手に置きました。
**面の巡回をそのまま出したら、1回で分かりました。**

#### 内側の輪＝非多様体の稜、そのもの

```text
FACEDUMP 内側 0-0: (3.6191 3.9550 0.4700) -> (3.6191 3.9550 1.4100)  ← 非多様体
FACEDUMP 内側 0-1: (3.6191 3.9550 1.4100) -> (3.2756 3.7008 1.4100)  ← 非多様体
FACEDUMP 内側 0-2: (3.2756 3.7008 1.4100) -> (3.2756 3.7008 0.4700)  ← これだけ違う
FACEDUMP 内側 0-3: (3.2756 3.7008 0.4700) -> (3.6191 3.9550 0.4700)  ← 非多様体
```

**4-306 が刻んだ穴の 4 本のうち、3 本が 3 回使われています。**
残る 1 本は `x = 3.2756` の**継ぎ目の側**で、そこだけ数が合っています。

**外周 18 本のほうは、まったく無関係でした。** 切り口の平面（`z = 0.1250`
と `1.8750`）と、別の穴を回る往復で、**巻き込みはありません**。
「割られるべきものを抱え込んでいる」と読みかけましたが、**外れです**。

#### 演算ごとに姿が違うだけで、出どころは1つです

| 演算 | その輪を使っている面 | 数 |
| :--- | :--- | :--- |
| **和** | A **269**（穴）＋ B **292**・**294** | **3 回＝非多様体** |
| **差・積** | A **441** と A **591** | 2 回だが**どちらも相手なし** |

**441 と 591 は向き違いの対**です——`split_by_interior_loop` が返す
**内側の面と外側の面が、両方とも残って**います。**和では内側が落ちて**、
代わりに **B が 2 枚**入ります。

**B は 2 枚とも正しい**（向き違い＝隣り合う2面の共有）ので、
**和で余っているのは A の穴の縁**です。逆に言えば、**B はその窓を
自分だけで閉じて**おり、A に穴を開ける必要がありません。

#### いま言えること

**4-306 の穴は、開ける場所そのものが要検討です。** B が自分で閉じている
窓に A が穴を開けると、和では 3 回、差と積では「A の2枚が浮く」形に
なります。**どちらも、穴を開ける前には無かった**姿です
（4-316 の二分: `3920f77` は非多様体 0、`cd32fe9` で 9）。

#### 「選び方の間違いでは」も潰しました

`location`（内か外か）と向きの反転も出して測りました。

```text
面 269 NURBS 外周 18 内輪 1  位置 Outside 向き反転 false
面 292 平面   外周  7 内輪 0  位置 Outside 向き反転 false
面 294 平面   外周  3 内輪 0  位置 Outside 向き反転 false
（295 / 298 / 304 / 305 も全部 Outside）
```

**7 枚とも `Outside`、反転なし。** 和は `Outside` を採るので、**選び方は
規約どおり**です。B の 2 枚が両方残っているのは、**B 自身の殻の折れ目**
であって、内と外を取り違えたのではありません。

**つまり余っているのは、やはり A の穴の縁 1 本ぶん**です。

#### 次に触る人へ

**穴を開ける前に、相手がその窓を閉じているかを見てください。**
測り方は決まっています——輪の 4 本それぞれについて、**相手側の面が
何枚その稜を使うか**を数えれば足ります。**2 枚なら、開けてはいけません。**

そこが決まるまで、**4-306 を戻す判断はしないでください**。戻すと
軽い失敗が 9 本増えます（56/56/48 へ）。**選ぶ前に、選べるだけの数字を
揃えるほうが先**です。

#### 残したもの

| 口 | 中身 |
| :--- | :--- |
| `ZENITH_FACE_DUMP=269` | その番号の面の**外周と内側の輪を、点の並びのまま**出します。本数と種類だけでは、巻き込みなのか元からの形なのかが分かりません。**要約ではなく並びが要ります**（4-304 で同じことを学びました） |

---

### 4-318. **穴の縁の数え方が決まりました**（2026年9月4日、H8）

4-317 が次の一手に置いた「**開ける前に、相手がその窓を閉じているかを
数える**」を、実際に数えました。**規則は立ちましたが、そこへ着くまでに
2回外しています。**

#### 1回目の外し——輪ごとに合計した

最初は輪ごとに「相手側が何回使っているか」を合計しました。

```text
A 面 269 の内側の輪 0（4 本）: 相手側 7 回 → 相手が通り抜けています（開けてよい形）
```

**まさにその面が、非多様体を作っている面です。** 4 本で 7 回なら平均
1.75 回で、健全に見えます。**中身は「2 回が 3 本、1 回が 1 本」**でした。

**合計は、混ざったものを隠します。** 数える単位は**稜**で、輪ではありません。

#### 2回目の外し——自分側と相手側を混ぜた

稜ごとに数え直し、「**他が 2 回なら非多様体**」と書きました。
`contact_placement_probe`（**門は緑**）で確かめたら、**104 個の穴のうち
28 個が同じ形**でした。**反証です。**

原因は、直すときに**自分側と相手側の区別を落とした**ことでした。
**「自分 1／相手 1」は他が 2 回ですが、健全な形**です。

**直したつもりで、前より粗い数え方にしていました。**

#### 3回目の外し——「自分 0／相手 2」なら非多様体、とした

区別して数え直したら、`linkrods` の壊れている稜だけが「自分 0／相手 2」に
なり、**数まで一致**しました（3 本 × 3 回 = 9）。**そこで止めずに、
他の門でも見ました。**

| 緑の門 | 「自分 0／相手 2」の輪 |
| :--- | ---: |
| `screw.step` | **4 個** |
| `rechained_boolean_probe` | **8 個** |
| `foreign_cross_pair_probe` | **36 個** |

**全部緑です。** 規則は死にました。**数が一致したのは、一致しただけ**でした。

#### 生き残ったのは「揃っているか」だけです

| データ（すべて緑） | 輪 | **混ざり** |
| :--- | ---: | ---: |
| `contact_placement_probe` | 104 | **0** |
| `rechained_boolean_probe` | 147 | **0** |
| `foreign_cross_pair_probe` | 423 | **0** |
| `scale_sweep_probe` | 252 | **0** |
| `screw.step` | 18 | **0** |
| **合計** | **944** | **0** |
| **`linkrods.step`（断られる）** | 10 | **2** |

**944 個すべてが「1本あたりの形が1種類」**です。混ざっているのは
`linkrods` の **面 269 と面 441 だけ**——そして**その 2 つが、壊れている
2 つ**です。

- 面 269: 「自分 0／相手 1」1 本 ＋「自分 0／相手 2」3 本
  → **和の非多様体 3 本 × 3 回 = 9 回**（4-316 の実測と一致）
- 面 441: 「自分 0／相手 0」3 本 ＋「自分 0／相手 1」1 本
  → **差と積で浮く 3 本**（4-317）

**健全な穴の縁は、全部の稜が同じ使われ方をします。** 途中で変わるのは、
**輪の一部だけが相手と噛み合っている**ということです。

#### 区別して数え直したときの分布（参考）

| 形（1本あたり） | 自作の立体（緑、104 個） | `linkrods.step`（10 個） |
| :--- | ---: | :--- |
| 自分 0／相手 1 | 72 | 面 269 に 1 本 |
| 自分 1／相手 1 | 26 | — |
| 自分 1／相手 0 | 4 | **8 個すべて** |
| 自分 2／相手 0 | 2 | — |
| **自分 0／相手 2** | **0** | **面 269 に 3 本** |
| **自分 0／相手 0** | **0** | **面 441 に 3 本** |

（上の表の「自分 0／相手 2」は、緑のデータにも普通に出ます。**混ざり方の
ほうが効いています。**）

#### まだ言えないこと

**944 対 2 で残っていますが、それでも規則ではありません。** ここまでに
**3 回外している**ことを思い出してください——どれも「一致した」ところで
止めていたら、そのまま入れていました。

**縫合の前に計算できる数で、2種類の壊れ方が両方説明できる**のは事実です。

**いまは数えるだけで、動きは変えていません。** 断りの数字は 4-317 から
1バイトも動いていません。

#### 常設テストで固定しました——**わざと壊すと落ちます**

数える所を `hole_rim_use_shapes` として切り出し、テストから呼べるように
しました（`HoleRimUseShape::is_mixed`）。

`a_hole_rim_is_uniform_until_a_piece_goes_missing`——**板を貫く穴**
（10×10×4 の板を半径 2 の円柱で貫通）を差で作り、

1. **健全な差では、穴の縁が揃っている**ことを見て
2. **その縁に触れている面片を1枚落とし**、**混ざりが出る**ことを見ます

**入れる前に、落ちることを確かめました**（4-301 の流儀）。`is_mixed` を
`false` に固定すると:

```text
thread 'a_hole_rim_is_uniform_until_a_piece_goes_missing' panicked
  面片を1枚落としても混ざりが出ないなら、この見分け方は壊れ方を見ていません
  落とした面片: 6、縁: HoleRimUseShape { face_id: 39, edge_count: 4,
    histogram: {(0, 1): 4} }
test result: FAILED
```

**通ることを確かめただけのテストは、通り続けるだけです。**

| | 前 | 後 |
| :--- | ---: | ---: |
| テスト | 137 / 695 / 失敗 0 / 警告 0 | **137 / 696 / 失敗 0 / 警告 0** |

#### 残したもの

| 口 | 中身 |
| :--- | :--- |
| `ZENITH_HOLE_WHY=1` | 選ばれた面片の**内側の輪ごとに、稜1本あたり「自分側 / 相手側が何回使っているか」の分布**を出します。**合計ではなく分布**です——合計にすると、上の1回目と同じ外し方をします |

---

### 4-319. **4-306 が持ち込んだ重い失敗を、消しました**（2026年9月4日、H8）

4-318 で測り口が固まり、常設テストで**わざと壊すと落ちる**ことまで確かめた
ので、**使う**段へ進みました。

#### 4回目の外し——**組で揃っているか、では駄目でした**

4-318 の見分け方（`(自分, 相手)` の組が輪の中で揃っているか）を、そのまま
「揃わない穴は畳む」に使いました。**通しテストが 2 件落ちました。**

```text
revolved_ring::the_tilted_drill_bites_the_bore ... FAILED
revolved_ring::the_drilled_halves_add_back_up ... FAILED
  revolved_ring / tilted drill / Difference refused: ...
```

**これまで通っていた差が、断られるようになりました。**

測ったら、その輪はこうです。

```text
A 面 N の内側の輪 0（6 本）: 自分 0／相手 1: 3 本、自分 1／相手 0: 3 本
```

**組は混ざっていますが、合計はどの稜も 1** です。**誰が相手かは揃って
いなくて構いません。**

**4-318 の掃き出しは、この試験を含んでいませんでした**——944 個は5つの
「掃き出し」から取っており、`tilted_cutter_test` は入っていません。
**門を数える口と、常設テストは別の集合です。**

#### 揃えるべきは「合計」でした

| | 合計の集合 | |
| :--- | :--- | :--- |
| `tilted drill`（**通る**） | **{1}** | 組は混ざるが合計は揃う |
| `linkrods` 面 269（壊れる） | **{1, 2}** | 3 回使われる稜が 3 本＝**和の非多様体 9 回** |
| `linkrods` 面 441（壊れる） | **{0, 1}** | 誰も使わない稜が 3 本＝**差と積で浮く 3 本** |

`is_mixed` を**合計の集合**で見るように直したら、`tilted_cutter_test` は
**11 件すべて通り**ました。

#### 数字——**4-306 の直前より、全部良くなりました**

| | 4-306 の直前 | 4-306 〜 4-318 | **ここ** |
| :--- | :--- | :--- | :--- |
| 相手のいない稜（和／差／積） | 56 / 56 / 48 | 47 / 50 / 42 | **48 / 48 / 42** |
| **非多様体** | 0 / 0 / 0 | **9** / 0 / 0 | **0 / 0 / 0** |

**4-316 で見つけた「軽い失敗と重い失敗の取り替え」が解消しました。**
`linkrods.step` は**まだ断られます**——相手のいない稜 48 / 48 / 42。

#### 7 つの門すべてで確かめました（4-312 の流儀）

| | |
| :--- | :--- |
| テスト | **137 / 696 / 失敗 0 / 警告 0** |
| `screw.step` | 3演算 ok、恒等式 **3.845e-13 / 3.899e-13** |
| `contact_placement_probe` | 41 配置、破れ 0（最悪 3.900e-8） |
| `scale_sweep_probe` | 破れ 0・非多様体 0・断り 0（最悪 7.394e-8） |
| `rechained_boolean_probe` | 30 演算すべて ok（最悪 1.906e-7） |
| `foreign_cross_pair_probe` | 15 組、破れ 0（最悪 9.429e-10） |
| `mesh_watertight_probe` / `mesh_density_probe` | 緑 |

**`ZENITH_KEEP_MIXED_HOLES=1` で止められます。**

#### 次に触る人へ

**残るのは `linkrods` の相手のいない稜 48 / 48 / 42 だけ**になりました。
非多様体は 0 です。

**そして、この一族で外した回数を数えてください——4 回**です（輪ごとに
合計した／自分側と相手側を混ぜた／「自分 0／相手 2」を犯人とした／
組で揃っているかを直しに使った）。**どれも「一致した」ところで止めて
いたら、そのまま入れていました。** 4 回目は**通しテストが止めました**。

**掃き出しで測って、常設テストで確かめてください。** 片方だけでは足りない、
というのが今日いちばん高くついた学びです。

---

### 4-320. **残った 42 本を数え直したら、絵が変わっていました**（2026年9月5日、H8）

4-319 で非多様体が 0 になったので、**残る「相手のいない稜」を測り直しました**。
**4-293 の絵はもう当てはまりません。**

#### 4-293 との差

| | 4-293（9/3） | **いま**（9/5） |
| :--- | :--- | :--- |
| 相手のいない稜（積） | 48 本 | **42 本** |
| どちら側か | **全部 A 側** | **A 33 本 / B 9 本** |
| またがる面 | **67 枚** | **17 枚** |
| 端点のつながり | 奇数ばかり | **奇数ばかり**（変わらず） |

**面が 67 → 17 枚に絞れています。** 「1枚を直せば済む型ではない」という
4-292 の但し書きは、**まだ正しい**が、範囲はだいぶ狭くなりました。

#### 「片が丸ごと欠けている」ではありませんでした

**面の側から数える口**を足しました（`ZENITH_FLOAT_WHY=1`）。稜の側から
「42 本」と言われても、**その面の外周のうち何割が浮いているか**が分かりません。
外周が丸ごと浮いている片（＝相方が欠けている）と、一部だけの片
（＝割り方の食い違い）では、直す先が違います。

**最終選択には、丸ごと浮いた片が1つもありませんでした。**

```text
FLOATWHY A 面 591: 稜 4 本のうち 3 本が浮いている
FLOATWHY A 面 601: 稜 6 本のうち 5 本が浮いている
FLOATWHY A 面 599: 稜 4 本のうち 1 本が浮いている
FLOATWHY B 面 609: 稜 5 本のうち 4 本が浮いている
（17 枚すべて部分的。丸ごとは 0 枚）
```

**つまり「片が欠けている」のではなく、割り方の食い違いです。**

#### B 側に、はっきりした綻びが1つ出ました——ただし**読み方を1度外しました**

まず目に付いたのはこれです。

```text
面 607: … -> (7.994676184  2.814542974  1.410)
面 607: (7.994676184  2.821477274  1.410) -> …
```

**「同じ角が y で 0.0069343 離れた2値として存在している。対称性
（`2.814542960 + 3.435457040 = 6.250000`、円柱の中心 y = 3.125）から
2.8145 が正で 2.8215 が余計」と書きました。外れです。**

`ZENITH_STITCH_WATCH` でその座標を待ち伏せたら、**2値のあいだに稜があり、
しかも正しく合って**いました。

```text
面 607: (7.994676 2.814543 1.410) -> (7.994676 2.821477 1.410)   長さ 0.0069343
面 609: (7.994676 2.821477 1.410) -> (7.994676 2.814543 1.410)   ← その相手（向き違い）
```

**両方とも本物の頂点**で、短い稜がそれを繋いでいます。**重複ではありません。**

#### 本当の姿——**弧と、ほぼ直線が、同じ2点のあいだを走っています**

待ち伏せで全部出すと、面 607 は**3 辺**しかありません。

| 辺 | 弦 | **中点が弦の中点からどれだけ離れているか** | 相手 |
| :--- | ---: | ---: | :--- |
| A `(7.375249 2.999255)` → `(7.994676 2.814543)` | 0.646381 | **0.167882** | **無し** |
| B `(7.994676 2.814543)` → `(7.994676 2.821477)` | **0.006934** | — | 面 609（合っている） |
| C `(7.994676 2.821477)` → `(7.375249 2.999255)` | 0.644434 | **0.002193** | **無し** |

**「長さがほぼ同じだから、ほぼ同じ線だろう」と書きました。これも外れです。**
弦の長さは 0.646 と 0.644 でほぼ同じですが、**膨らみが 76.6 倍違います**。

- **A は弧**です。円弧と見ると**半径 0.395**（棒の断面と同じ桁）
- **C はほぼ直線**です（膨らみ 0.0022。ちょうど 0 ではありません）

**A と C は片端 `(7.375249 2.999255)` をぴったり共有**し、**もう片端が
0.0069343 ずれて**います。その隙間を辺 B が塞いでいます。

**弦の長さだけで「同じ線」と言ってはいけません**——**中点を見てください**。
このリポジトリは 4-65 で同じことを学んでいます（同じ2点を結ぶ別々の弧は
普通にあります）。**今日また踏みました。**

#### まだ言えないこと

**「片方が直線で代用されている」とは、まだ言えません。** 直線を作る道は
3つあり（`intersect_planes`、`intersect_tangent_cylinder_patches`、
`intersect_ruling_plane_cylinder_patch`）、**どれも正しい場面があります**
——平面が円柱の母線を含めば、交線は本当に直線です。`linkrods` は棒が
いろいろな向きに付いているので、**A と C が別々の棒から来ている**可能性が
あります。

**次は「A と C を、それぞれどの面の組が出したか」です。** そこが分かるまで、
原因は書きません。**この一族では、推論を今日だけで 2 回外しています。**

**この数は前から見えていました。** 4-304 で足した弦の測り口が
「**その相手の弦まで: 始点 0.000000000 終点 0.006934300**」と出しており、
**同じ 0.0069343** です。**出ていたのに、名前が付いていませんでした。**

場所は **`x = 7.994676`**——4-307 が名指しした2つの継ぎ目のうちの1つです
（もう1つは `x = 3.275609`。そちらは 4-317 で追いました）。

#### 次に触る人へ

**まず「A と C をどの面の組が出したか」を測ってください。**
それが決まるまで、下は**推測**です。

**0.0069343 という「ずれ」が、どちらの側の値なのか**も見てください。
`2.814542960 + 3.435457040 = 6.250000` の対称性は**面 609（平面、x 一定）
の上での話**で、そこには `2.821477` も乗っています。**対称なほうが正しい、
という推論は使えません**——両方が実在する頂点です。

**弦と弧の食い違いを疑っています**——半径 0.3105 の円弧を粗く折れ線に
すると、中央のたわみが 0.0069 前後になります。片方が真の曲線の上で、
もう片方が p-curve の折れ線の上なら、この形になります。**ただしこれは
まだ推測で、測っていません。**

**刻み込みでは埋まりません。** 受け入れ幅は `tol.linear * 10`（絶対 1e-5）
で、0.0069 はその **694 倍**です。ここを緩めるのは、**0.0069 を「同じ点」と
呼ぶ**ということで、部品の差し渡し 5.02 に対して大きすぎます。**直すのは
公差ではなく、2つの値が食い違う理由のほう**です。

**残りの内訳**（積の 42 本）:

| どこ | 本数 |
| :--- | ---: |
| 切り口の面（`z = 0.470` / `1.410`） | **17** |
| モデル自身の特徴の高さ（`z = 1.3499` / `0.5999` など） | **25** |

---

## 5. 踏んだ落とし穴（繰り返さないために）

### 同じ文言が入口と記録の両方にあるとき、置き換えは記録に当たります（2026年9月4日）

引継書は、**入口の一覧**と**記録の本文**に同じ文を持っていることが
あります。`assert count == 1` を付けて置き換えても、**当たったのが
どちらかは分かりません。**

実測でこうなりました（9/4）。入口の一覧に「今日やったこと」を4つ足した
つもりが、**4つとも記録の本文に当たっていました**。入口には古い断定
（「設計の判断です」——4-309 追記でもう決まっていた）が残ったままで、
**読んだ人が逆の結論に進むところ**でした。

**入口を直したら、入口を目で見てください。**

```bash
awk '/^> ### 9月4日にやったこと/,/^> ### 9月3日にやったこと/' HANDOVER.md
```


### 思いついたら、書く前に数えてください（2026年9月4日）

この日、**思いついた機構が8回とも外れました**。どれも**書いてから
数えて**気づいています。

| | 思いついたこと | 数えたら |
| :--- | :--- | :--- |
| 4-309 | 「B が同じ稜を2度作っている（差 1e-8、公差の中）」 | **3演算を混ぜて数えていた**。演算ごとには **0 組** |
| 4-310 | 「輪が入らないのは門が固いから。緩めればいい」 | **緩めたら悪化**（47/50/46 → 55/56/54）。門は余った鎖を守っていた |
| 4-311 | 「穴の空いた片に次の切り込みが入らないのでは」 | **9 件で、変える前と同じ**。原因ではない |
| 4-311 追記 | 「外周を細かくしているのは `planar_region_face` では」 | **そこは外周を `outer_wire.clone()` のまま使う**。読めば1分で分かった |
| 4-313 | 「平面の段にも 4-306 の受け皿を置けば入る」 | **1バイトも動かず**。そもそも `Err` を返していないので受け皿に来ない |
| 4-315 | 「1本ずつのあとに受け皿を置けば入る」 | **1バイトも動かず**。段には来ているのに数字が動かない |
| 4-315 追記 | 「`split_by_interior_loop` が入れられないのだろう」 | **6 件は面積残差 1e-14 で入る**。断り文を出せば1回で分かった |
| 4-305 | 「穴として入れる機能は、まだ無い」 | **4-50 からあった**（`split_by_interior_loop`）。**呼ばれてもいた**。`grep` 一回で分かった |

**8つとも、先に数える（読む）だけで済みました。** `awk` 一行、
`grep | wc -l`、あるいは**その関数を開く**。**どれも実装より安いです。**


### 3演算を1つのファイルに出す口で数えるときは、演算で分けてください（2026年9月4日）

`read_and_cut_probe` は和・差・積を**同じ出力に続けて**出します。
`grep ... | sort -u` で数えると、**3演算が混ざったまま**数えられます。

実測でこうなりました（4-309）。**「差が 5e-9〜2.3e-8 しかない稜が 4 組
あり、公差 5e-6 の中にいるのに合っていない」**と読みました。**間違い
です**——近すぎる2本は**別々の演算のもの**で、**同時には存在しません**。
演算ごとに分け直したら **0 組**でした。「相手のいない稜 96 本」も混ざった
数字で、正しくは **和 47 / 差 50 / 積 46** です。

**同じ形の間違いは 4-294 でもやりました**（数え口を2つの呼び出しで共用し、
24 件の出どころを読み違えた）。**「どこから来た数字か」を、数える前に
確かめてください。**


### 「残っているのは仕方のないぶん」と書く前に、断り文を数えてください（2026/08/29）

面積分の三角形を減らしていったとき、最後に 216回・248,832枚が残りました。
**「慣性まで要る呼び出しのぶんだろう」と書きました。推測でした。**

断った理由を出して数えたら、**216件すべてが「p-curve が無い」**でした
（4-162）。形の問題でも精度の問題でもなく、**入口で諦めていただけ**です。
しかも三角化の経路は、同じ状況で**前からその場で導出**していました。
直したのは2行です。

**「残りは仕方がない」は、いちばん検算されにくい文です。** 数えれば
1分で分かることを、書いて済ませていました。**残りを説明する前に、
残りを数えてください。**

同じ形の間違いを、この日だけで3回しています。

| どこ | 書いたこと | 測ったら |
| :--- | :--- | :--- |
| 4-160 | 「細分が重いのだろう」 | 細分を外しても**1枚も減らない**。重いのは earcut |
| 4-161 | 「平面側と同じ8点で足りるだろう」 | **6演算が断られる**。曲面の p-curve は平面より曲がる |
| 4-162 | 「残りは慣性のぶんだろう」 | **全件 p-curve が無いだけ** |

**3回とも、測る道具は既にありました。**


### 「片方が自作」だと当たらない欠陥があります（2026/08/28）

`foreign_boolean_probe` は「他カーネルの立体 × **自作の**切り手」を
30配置・180演算まで測っていて、**PANIC 0** でした。**それでも本番経路が
パニックしました**——両方とも他カーネルの立体にした瞬間です（4-141）。

自作の立体は面の並びが素直なので、**内側シェル込みの添字**という
ずれに当たりません。**片側を固定した測り方には、その側の性質が
入り込みます。**

**「A × B」を測るときは、A と B を入れ替えるだけでなく、両方を同じ
出所にした組も測ってください。**


### コメントを読んで「通らない」と決めないでください（2026/08/28）

コードのコメントに「いまの判定は面全体の一致なので、部分的な重なりは
捕まりません」とありました（4-124）。それを**そのまま「部分的に重なる
配置は通らない」と読んで、2回そう書きました**（4-137、4-139）。

**測ったら通りました**——ブロックの部分的な面接触 15/15、同軸円柱の
部分的な側面重なり 12/12、どれも閉じた式ちょうどです（4-140）。

**「この仕組みが捕まえない」と「答えが出せない」は別です。** 捕まえる
仕組みが要る場面に、そもそもならないことがあります。

**通らない実例を持っていないなら、「そこが穴だ」と書かないでください。**
書くなら、**その実例を一緒に置いてください。**


### 検査を3つ通っても、正しいとは限りません（2026/08/28）

半径 5 の球を z 軸まわりに 30 度回して和を取ると、**同じ球を2枚重ねた
「二重被覆」**が返っていました（4-137）。体積は真値の2倍です。

**それが、当時あった検査を3つとも通りました。**

| 検査 | なぜ通ったか |
| :--- | :--- |
| シェルが閉じているか（稜が2回使われるか） | A の8枚とB の8枚が**別々に**閉じている。稜はどれも2回 |
| 内外一貫性（384点） | 重なった2つは、どの点から見ても「中は中」 |
| 体積の上限（和は `va + vb` 以下） | 2倍は `va + vb` に**ちょうど**収まる |

**「検査が全部緑だから正しい」は言えません。** 通る形の欠陥は、必ず
あります。増やすべきは検査の数ではなく、**独立した角度**です——ここで
効いたのは「返す立体どうしは重なってはいけない」という、**それまで
どの検査も見ていなかった不変量**でした。

**閉じた式と突き合わせるのが、いちばん強い角度です。** この欠陥も、
最初に見つけたのは検査ではなく「和が元の立体になるはずなのに2倍」でした。


### 総当たりで掃いてください。1件ずつ足していると、穴が見えません（2026/08/28）

2026/08/28 に、**総当たりで3回掃いて3回とも欠陥が出ました。**

| 掃いた軸 | 掃く前の見え方 | 実際 |
| :--- | :--- | :--- |
| 中に入っている（4×4×3） | 「1通り緑だから通っている」 | **箱から小さい球を引く1通りだけ**が通っていた（4-133） |
| 離れている（5×3） | 測っていなかった | 15/15 正しい |
| **まったく同じ（5×3）** | 測っていなかった | **3/15。うち2件は誤答**（4-134） |

**同じ立体どうしの和が、重なった立体を2つ返していました。** 体積は2倍で、
それでも検証ゲートを通ります——和の体積は A より大きく、内外判定も
すべて一致するからです（重なった2つの立体は、どの点から見ても「中は中」）。

**配置を1件ずつ足す手は、今日3回当たりました。それでも総当たりのほうが
強いです。** 1件ずつだと「その1件」しか分かりません。総当たりなら
**通っている範囲の形**が分かります——「箱だけ」「平面だけ」のような
偏りは、並べて初めて見えます。

**軸を選ぶときは、真の答えが閉じた式で書けるところにしてください。**
上の3つはどれも、和・差・積の全部が閉じた式です。だから
`volume > 0` に逃げずに済みます。

まだ掃いていない軸（次に触る人へ）:

- **同じ形を、半分だけ重ねる**（A と B が半分ずつ重なる。和・差・積とも
  閉じた式）
- **入れ子3段**（A ⊃ B ⊃ C）
- **同じ立体を、公差より少しだけずらす**（1e-9、1e-7、1e-5 と振る）


### コメントに書いてある約束は、検査ではありません（2026/08/28）

`MassCalculator::compute_from_brep` に「空洞シェルは外殻と同じ向きで
保持されるため、寄与を反転して足す」と書いてありました。**書いてあった
のに、守られていませんでした**（4-133）。通っていたのは「箱から小さい球を
引く」1通りだけで、円錐からだと**触れてもいない**のに落ちます。

**片側にしか書いていない約束は破れます。** 守るほうのコードには何も
書いてありませんでした。約束を置くなら、**破れたら落ちるものを一緒に
置いてください**——ここでは「空洞シェル単体の符号つき体積が正であること」
をテストにしました。

**「1通りは通っている」を「通っている」と読まないでください。** この欠陥は
`test_exact_brep_boolean_returns_cavity_for_contained_difference` が緑
だったので、ずっと通っていることになっていました。**その1通りが、たまたま
向きの揃う配置でした。**


### 同じ入力で2回走らせてから、数字を書いてください（2026/08/27）

**測定が揺れていました。** 同じバイナリ・同じ入力で `contact_placement_probe`
を通したら、90演算のうち1演算だけが実行ごとに 6 と 0 を行き来しました
（4-132）。原因は `HashSet` の反復順が結果を決めていたことです。

**見つかったのは偶然です。** 別の作業のついでに「さっき 0 だったのに」と
気づいただけで、探していたわけではありません。**気づかなければ、揺れた側の
値を実測として書いていました。**

決まりを1つ足します。**新しい数字を文書に書く前に、同じものを2回測って
突き合わせてください。** 特に:

- 反復順が答えに効く場所（作りながら参照する格子、貪欲な選択）
- ハッシュ容器を**反復**しているところ（`contains` / `get` だけなら安全）
- プロセス通し番号（`FACE_ID_GEN` のような `id`）を鍵にしているところ

**通し実行と単独実行でも突き合わせてください。** 上のケースは、単独で
5回走らせるとどれも 0 で、通しで走らせると揺れました。`id` が変わると
ハッシュの分布が変わるからです。**「単独で再現しない」は、直った証拠では
ありません。**


### 良くなった数字だけを見て採らないでください（2026/08/27）

**「断られる件数が減った」も、良くなった数字です。** 5 → 3 に減ったので
通ったかと思ったら、減った2件は**断るのが正しい行**でした（4-130）。
`REFUSED` が減ることは、それ自体では前進ではありません。**どの行が
変わったかを開いてください。**


交線の端が **5.70e-5 → 7.56e-15** になりました。15桁です。**それでも入れて
はいけませんでした**——同じ変更で別の端が `6.35e-6 → 3.64e-5` に動き、
**3演算とも `REFUSED` に退行した**からです（4-129）。

端が1つ合っただけでは、縫合は閉じません。**採否は「結果が通ったか」で
見てください。** 途中の量（残差、端の位置、ずれの桁）は原因を絞るのには
要りますが、**採る理由にはなりません。**

同じ日に、逆向きの例も出ました。接点への着地の受け皿は6回発火して小さい数
を動かしましたが、**結果を1つも変えなかった**ので戻しました。**動いた数字
は、効いた証拠ではありません。**


### 出力が出来上がることは、出力が正しいことではありません（2026/08/27）

**PDF は出来ます。HTML も出来ます。STL も OBJ も書けます。それでも中身は
壊れていることがあります。**

同じ日に3つ出ました。

| どこ | 出来上がっていたが | 実際 |
| :--- | :--- | :--- |
| 非STEP出力 | 4形式とも問題なく書けていた | **形を見る検査が1つも無かった**。検証スクリプトは検査関数を呼んでいなかった（4-111） |
| 統合PDF | 5.2MB の立派な本が出来ていた | 本文が **663行落ちて**おり、数式の `\frac` `\approx` `\begin` が**制御文字**になっていた（4-114） |
| DXF | 図面として開ける | 外形が2つある断面で、2つ目が穴の層に落ちていた（4-112） |

**見分け方は、出力を作った側と別の道具で読み直すことだけです。** 自分が
書いたものを自分で読み返しても、同じ誤解が両側に乗るので何も出ません。
`tools/verify_iges.py` が OpenCASCADE に読ませているのが良い形で、
`tools/verify_mesh_exports.py` はまだ自前のパーサ止まりです（そこが弱いと
文書に書いてあります）。

**生成物の中に本文を書かないでください。** 差分がスクリプトの変更に見えるので
レビューに掛からず、他の文書と食い違っても誰も気づきません。今回の LaTeX の
破損は、Markdown として出した瞬間に見えました。

### `volume > 0` は、テストではなく願望です（2026/08/27）

**閉じた式が書ける形に `volume > 0` と書いてあったら、そこが穴です。**
8か所ありました。うち3か所が、抜き勾配が指定の半分になっている欠陥を
通していました（4-109）。1か所（掃引管）は**コメントが「経路長 × 断面積を
大きく外さない」と書いているのに、表明は `volume > 0` だけ**でした。
コメントは検査ではありません。

見分け方はひとつです。**「この立体を、寸法を半分にして作り直したら、この
表明は落ちますか」**と聞く。落ちないなら、それは測っていません。

閉じた式が本当に無い形（円 → 長方形 → 楕円のロフトなど）でも、**別の積み方で
積み直す**ことはできます。断面積をシンプソン則で積んで発散定理と突き合わせ
れば、面の向きや欠落は出ます。ただし**それは閉じた式の代わりではない**ので、
何が言えて何が言えないかをテストに書いてください。

### 許容を片側だけ締めるときは、ずれる向きを先に決めてください（2026/08/27）

出力メッシュの体積を B-Rep と突き合わせるとき、「曲面は内接するのでメッシュは
**小さく**出るはず」と決めて、大きい側だけ 1e-6 で締めました。**穴あき直方体と
ブーリアン結果が +6.9e-05 / +5.5e-05 で赤くなりました。**

外へ出っ張った面（円柱の側面、球）では確かに小さく出ますが、**内側へ食い込んだ
面（穴のボア）では、穴のほうが小さく刻まれるので立体は逆に大きく出ます。**
欠陥ではなく近似の向きで、片側に決めたこちらの誤りでした。

**凹凸が混じった立体では、帯は両側で取ってください。**

### 原点に置いて測ると通る欠陥がある（2026/08/25）

**同じ形を、置き場所だけ変えて測ってください。** 半径10の球を原点に置いて
面の上の点を射影すると 3.8e-15、`(20,10,10)` に置くと **0.446** でした
（4-79）。形は同じで、答えだけが違います。粗い格子の当たりがどこに落ちるかが
置き方で変わるためで、**原点で測っている限り一生出てきません。**

このリポジトリの検体は原点まわりに置いたものが多いので、**そこが穴になり
やすい**と思ってください。回帰テストは3つの置き方で測っています。

### 点を撒いて数える測り方は、刻みより細い構造を見落とす（2026/08/24）

**測っているつもりで、実は予想を書いていることがあります。** 接触の配置で
「答えが非多様体か」を測るのに、交線のまわりの円周に64点撒いて、答えの内側の
点がいくつの弧に分かれるかを数えました。**接触している3件すべてで「弧は1つ
＝多様体」と答えました。全部間違いです。**

材料が残るのは幅 **0.95 度**の帯で、64点の刻みは 5.6 度。**またいで通り過ぎて
いました**（4-74）。

刻みを細かくしても直りません。半径を縮めると帯も比例して細くなります。
直したのは**数え方のほう**で、A と B の内側の区間を別々に求めてから区間
どうしで演算すると、細い帯は引き算の結果として**そのまま出ます**。

**教訓**: 「標本を撒いて数える」を書く前に、**探しているものの大きさが刻みに
対してどれくらいか**を1行で見積もってください。見積もれないなら、その測り方は
まだ測定になっていません。同じ形の失敗が 4-34（メッシュの分割数を2つだけ見て
「閉じた」と報告した）にもあります。

### 予想を書くのはよい。書いた予想を、実測の代わりにしない（2026/08/24）

接触配置の規約を決めたあと、**「どれが多様体になり、どれが真に非多様体か」を
予想でこの文書に書きました。1日のうちに3回外しました。**

| 書いた予想 | 実測 |
| :--- | :--- |
| `box × sphere` の和は非多様体なので断るべき | 極を通らない置き方では**多様体の立体が返る** |
| `box × cylinder` 接線の和は非多様体なので断るべき | **多様体の立体が返る** |
| `box × sphere` が断られるのは継ぎ目との一致 | **極を通ること**。交線が極で点に潰れていた |

3つとも、**測る手段はすぐそこにありました**。「非多様体か」は、返ってきた
立体のメッシュで稜がちょうど2枚の三角形に共有されているかを数えれば決まり
ます。「なぜ断るのか」は、断った理由を出す口を1つ足せば出ます。

**予想そのものは書いてよい。** 何を測るかが決まるし、外れたときに何が
分かっていなかったかが残ります。危ないのは、**予想を表に入れて「規約に
照らした答え」のような顔をさせること**です。この文書はそれを一度やって、
「6件はすべて接線配置」という記述を長いあいだ残しました（3-1）。

書くなら、予想だと分かる場所に、予想だと分かる書き方で。

### 判定を二重に持たない（2026/08/25）

earcut は共線の境界点を**自分で**落とします。こちらでも共線を判定して外す手を
入れたら悪化しました（4-85）。**基準が2つあると、食い違ったところで壊れます。**

直ったのは、自分で判定するのをやめて**落とされた結果のほうを測って挿し戻した**
ときでした（4-86）。**相手がどう振る舞うかを推測せず、振る舞った結果を見て
ください。**

### uv で潰れていても、3D では潰れていない（2026/08/25）

境界が uv で直線だと、その上の点は一直線に並びます。earcut はそこに **uv では
面積 0 の三角形**を出します。**3D では一直線ではありません**——曲線の上の3点
だからです。

「uv で潰れているなら面のどこも覆っていない、落としてよい」——理屈は通って
いるように見えます。**実測では悪化しました**（4-85）。落とした三角形が境界を
繋いでいて、抜くと穴が開きます。

しかも**面積は良くなりました**（331.06 → 316.63）。**閉じ方は悪くなりました**
（5件 → 7件）。片方の量だけ見ていたら「直った」と判断していたところです。

**このあと「そもそも作らせない」も試して、これも悪化しました**（5件 → 6件）。
**2回外したら、前提のほうを疑ってください。** どちらの手も「その三角形は
要らない」を前提にしており、**その前提を測っていませんでした**（4-85）。

### 直す前に、切り離す（2026/08/24）

断面の周長が合わない件で、**「連結か積算のどちらか」までは分かっているのに、
どちらかを決めずに連結器を直しにいって外しました**（4-70）。測っても数字が
動かず、効かなかったので戻しました。

そのあと、**生成側が正しいことを数字で切り離してから**やり直したら、犯人は
1回で決まりました（4-73）。切り離しに使ったのはこれだけです。

- 線分の長さの**合計**（120.000000000 ちょうど → 生成側は無罪）
- 重複したキーの組（0 → 二重出力ではない）
- 載せ直した点の数（**2、蓋ごとに1点** → ここだ）

**候補が2つに絞れているときこそ、片方を潰す測定を先に書いてください。**
勘で当たれば速いように見えますが、外したときに「なぜ動かなかったのか」も
分からないまま戻すことになります。

### 面積が合っていても、周長は合っていない（2026/08/24）

同じ弦の上を点が滑ると、**面積は1桁も動かずに周長だけが伸びます**。
円柱の軸断面がそれで、面積は 1.4e-16 で厳密なのに周長は 0.43% 過大でした。

**1つの量が厳密だからといって、その形が正しいとは限りません。** 断面なら
面積・周長・ループ数を別々に見てください。体積が合っている立体で重心が
ずれていた例（4-33）、面積が合っている面で重心がずれていた例（4-72）も
同じ形です。

### 対称な置き方は、誤りを隠す（2026/08/24）

`inspect_face` が返していた重心は、原点からの四面体分割による**体積**の
重心でした。円柱の**下蓋は z = 0 なので、偶然正しく見えていました**。
上蓋（z = 40）で測って初めて (0, 0, 30) が出ました——ちょうど 3/4 です。

「通る値を1つ選ばない」（下記）と同じ話ですが、**通る場所を1つ選んでも
同じことが起きます**。原点・軸の上・対称面の上は、いちばん検体にしたく
なる場所であり、いちばん誤りを隠す場所です。

**ブーリアンの回転ボックス対応は、部品を1つずつ入れると必ず別のケースを壊しました。**
4回取り下げています。必要だったのは以下の4つ＋同一平面処理で、**同時に入れないと効きません**。

1. 連結分割（切り込みは複数の交線が内部の角で繋がったもの）
2. 境界沿い交線の除外（それは接触の記録であって切り込みではない）
3. 同一境界辺への着地の許可（両端間の経路はその辺の一区間）
4. 頂点の刻み込み（**2度「無意味」と判断して捨てた**。分割が正しくなって初めて効いた）

教訓として、**単独で効果が測れない部品でも、他が揃うまで判断を保留する**ほうが良い場合があります。
ただし取り下げの判断自体は正しく、そのたびに障壁の位置が1段深く特定できました。

**ゲートが落としたとき、疑うのはまず自分の期待値です。** トーラス × ボックスで
差と積が「解析解と一致」したのにゲートが弾き、誤検知だと思いました。実際は箱が
トーラスの横幅を覆っておらず、**私の解析解のほうが間違っていました**。
ゲートは 384 点の内外一貫性で見ているので、体積が合っていても形が違えば落ちます。
体積の一致は形の一致より弱い条件です。

**「いくつか乗っている」は「そういう形である」ではありません。** 円柱の認識は
「最大半径に16点以上乗っていれば円柱」でした。円錐の底円はちょうど最大半径に
乗るので、円錐が円柱として通ります。**全ての標本がそうであること**まで見ないと、
条件を満たす部分集合があるだけで通ってしまいます。

**探索そのものが粗いと、粗さがそのまま答えとして残ります。** 最近傍点探索は
16×16 の粗サンプリングから始めます。ニュートン法が動けなかったとき、そのまま
返すと格子間隔（半径10の球で 1.8）が答えになります。しかもそれは「もっともらしい
小さな値」なので、検査を通ってしまいます。**返す前に当たりの周りを詰めること。**

**「面積・体積がちょうど半分／2倍」は、たいてい継ぎ目の取り違えです。**
トーラスを1面で書いたファイルは、体積がちょうど半分になりました。縫い目上の点は
UV 領域の両端どちらにも写るので、p-curve から囲まれた領域を読むと、投影がどちらを
選ぶかで答えが割れます。**p-curve では原理的に決まらない**ので、位相で見ます
（ループ内のどの辺も2度現れるなら、その面は曲面全体）。断面積がちょうど2倍に
なったときと同じ性質の間違いです。

**絶対値を取ると、向きの誤りが見えなくなります。** `MassProperties` は体積の
絶対値を返していました。捨てていた符号こそが、シェルが外を向いているか内を
向いているかを示す唯一の量です。おかげで**3つのビルダーが裏返ったまま**通って
いました。`builder_audit` には「体積が正か」の検査が最初からあり、一度も
発火していません。**量を報告するとき、`abs` や `max(0.0)` で「もっともらしく」
しないこと。** 符号や範囲外の値は、たいてい情報であって汚れではありません。

**A と B が噛み合わないとき、直すべきが A だとは限りません。** 領域分割を
入れたら通っていた差と積が壊れ、「領域の巻き方に規約が要る」と診断して
取り下げました。**その診断は外れていました。** 領域は正しく、相手のトーラスが
裏返っていたのです。噛み合わせだけを見ていると「どちらかを相手に合わせる」
以上のことが言えません。**両方を外の物差し**（このときは発散定理の符号）
**に当てて**初めて、どちらが外れているか分かりました。

**比較相手を間違えると、欠陥が「仕様」に見えます。** 書き戻したファイルを
OpenCASCADE が 0.86% 高く測るのを、「OCC は有理パッチをそう測らない」と説明し、
比較相手を **OCC 自身の NURBS 化**に置きました。両方が同じくらい外れているので
一致して見えます。実際は、**自前ビルダーの有理パッチは 1e-11 で読まれていました**。
外れていたのは有理パッチ全般ではなく、我々が書いていた巻き付いた形だけです。
**「相手も同じくらい外れている」は、自分が正しい証拠ではありません。**
比較相手には、外れようのないもの（解析解）を置いてください。

**同じ量を2つの経路で出していると、片方だけ直っていることがあります。**
`face_uv_triangulation` は保持している p-curve のフィールドを見て、`plane_pcurves()`
は無ければ導出していました。同じ「その面の p-curve」を指す2つの経路が別々に
振る舞い、片方はトリムを失って面を3倍で返していました。**導出そのものは
正しかった**（結果は1点ずつ一致する）ので、幾何を疑っている限り見つかりません。
**値が食い違ったら、まず両者が同じ経路を通っているかを見てください。**

**当てに行くより、候補を並べて測るほうが早いことがあります。** 交線を辿る種の
選び方で3回外しました。①いちばん近い格子点 → 2面が触れている配置では接点が
いちばん近く、そこから始めても向きが定まらない。②いちばん条件の良い点 →
交線から遠い点を掴む。③「最も近い距離 ＋ 格子の目」の窓 → 片方が直って
もう片方が壊れる。**通ったのは、選ぶのをやめて候補を順に試し、結果を測って
採る形**でした。1つの規則で当てようとしている間は、直すたびに別の配置が
壊れます。

**残差が小さいことは、位置が決まっていることを意味しません。** 曲面同士が
接している場所では、「両方の曲面の上にある」という条件が交線上の位置を
一意にしません。等半径の直交円柱では、交線から **2.99e-5** 離れた点が両曲面を
**2.24e-11** で満たします。解の**条件数**（ここでは2つの法線のなす角）を見て、
悪いところでは答えずに断ってください。

**壁時計を物差しにすると、正しい改善が「効果なし」に見えます。** 失敗経路の
重複走査を落としたとき、45ケースの走査は 6分32.9秒 から 6分38.9秒 になりました。
差なし、むしろ悪化に見えます。ところが同じバイナリを2回走らせるだけで
**5分07秒 と 7分32秒**が出ます。振れ幅のほうが、測ろうとしている差より大きい。
実際には辿り開始回数が 18,840 → 11,928（−36.7%）減っていました。
**測ろうとしている差より測定の揺れが大きいなら、その物差しは使えません。**
決定的に数えられる量（`zenith_geom::work_counter`）に替えてください。
なお逆向きの誤りもあります——数が減っても時間が減るとは限りません。

**ゲートを通ったものだけを見せるゲートは、何も確かめていません。**
`boolean_envelope` の `gate` 列は、45ケースすべてで `pass` と出続けていました。
検証つきの入口（`boolean_solids_exact_result`）を呼んでいたからです。ゲートに
落ちた結果は `Err` になって「未実装」と同じ箱に入るので、**この列は原理的に
`REJECT` になれません**。同じ384点を2回払ってもいました。いまは `unverified` を
呼んで例が自分でゲートを掛け、`REJECT` は誤答として数えます。結果は 39/0/6 の
まま変わりませんでした——つまり「ゲート落ちは実際に無い」ことが、これで初めて
確かめられたことになります。

**厳密に一致するはずのものを「近い」で見ていないか。** 鏡映はアフィン変換
なので、写した立体の体積は元と**厳密に**一致します（実測で差 1e-15 未満）。
それを `< 1e-3` で見ていました。12桁ぶん何も見ていません。同じ型を探すなら、
平行移動・回転・鏡映・拡大縮小のあとで保存されるはずの量が候補です。

**同じ数字でも役割が逆のことがあります。** `boolean_verification_test` には
`(volume - one_sphere).abs() / one_sphere > 1e-3`（片方をそのまま返していないか）と
`(volume - expected).abs() / expected < 1e-3`（閉じた式と合っているか）が並んで
います。**前者は緩いほうが安全**で、後者は緩いと何も見ません。許容を機械的に
締めると前者を壊します。締める前に、その不等号がどちら向きの主張かを見て
ください。

**許容が実力より何桁も緩いテストは、通っていても見張っていません。**
他カーネルの6検体は 1e-3 で見ていましたが、実力は 7.5e-13 〜 2.4e-12 でした。
9桁ぶんの劣化を素通しします。期待値も OpenCASCADE が印字した4桁の数を写した
もので、**その literal 自体が 3e-8 の粗さ**を持ち込んでいました。閉じた式に
置き換えて 1e-11 まで締めています。同じ点検で見つかったもの:

| テスト | 旧許容 | 実測 | 新許容 |
| :--- | ---: | ---: | ---: |
| 他カーネル6検体の体積・面積 | 1e-3 | 7.5e-13 〜 2.4e-12 | 1e-11 |
| 球の赤道断面の面積 | 1e-2 | 4.83e-11 | 1e-10 |
| 円柱断面の面積 / 周長 | 1e-4 | 4.83e-11 / 2.41e-11 | 1e-10 |
| 穴あき箱の断面積 | 1e-4 | 4.62e-12 | 1e-11 |
| 鏡像後の体積の一致 | 1e-3 | < 1e-15 | 1e-12 |
| 球×球の和の体積 | 1e-3 | 1.25e-8 | 1e-7 |

**締める前に必ず実測してください。** 先に厳しくすると、実力不足なのか
テストが過剰なのかが区別できません。やり方は、許容を一度 1e-15 にして
失敗メッセージから実測値を読み、それを見てから決めることです。

**緩いままにしたものもあります。** 干渉判定の距離は `2e-2` ですが、これは
表面を三角形で測るぶん必ず長く出るという**向きの決まった**誤差で、
`>= truth - 1e-9` と `< truth + 2e-2` の**両側から挟んで**あります。緩さに
理由があり、下限が本体を押さえている設計です。**緩いこと自体が情報なら、
他と同じ値にして隠さないでください。**

なお NURBS円柱の体積はここで `2e-6` の許容を付けて「既知の残差」として
残していましたが、そのあと原因が経路の取り違えだと分かり（4-19）、
いまは値ではなく**収束の落ち方**を見るテストになっています。
**「既知の残差だから緩くしておく」と書いた瞬間に、それは調べられなく
なります。** 緩さの理由を書くのは、諦めるためではなく、後から疑える
ようにするためです。

**自分が作れるものを、自分で受け取れますか。** ブーリアンは空洞を持つ立体を
**返せる**のに、**受け取れません**でした（4-23）。返した立体を次の演算に渡すのは
実務のモデリングそのものなので、ここが切れていると使えません。出力に持たせた
性質のリストを作って、入力として受け取れるかを1つずつ確かめてください。

**ゲートが通したことは、正しいことではありません——特徴が小さければ。**
384点の内外一貫性は、境界箱の 0.6% しかない空洞の消失を通しました。落ちる
標本が2〜3点で、許容している食い違い 1% の中に収まるからです。**この検査が
見える特徴の大きさには下限があります。** 45ケースの走査で誤答ゼロが続いて
いたのは、空洞入りの検体が1つも無かったからでもあります。

**閾値に絶対値の定数が入っていたら、寸法の両端で何が起きるか考えてください。**
ゲートの緩衝には `.max(1.0)` という床がありました。**大きいモデルでは正しい
答えを弾き、小さいモデルでは検査そのものを恒真にします**（4-22）。どちらも
誤答としては現れないので、気づく機会がありません。床が要るなら、単位から
仮定せず公差から引いてください（ここでは `tol.linear` の3乗）。

**直したつもりの修正を、同じプローブにもう一度かけてください。** 比の正規化を
直した時点で私は直ったと思いましたが、床を残していたので **80000 対 0.008 では
再発**していました。配置を足しただけで出ました。**1つの検体で直ったことは、
その欠陥が消えたことではありません。**

**1つの閾値を2つの問いに使っていないか。** 検証ゲートは「体積の境界に
収まっているか」と「体積がゼロでないか」に同じ `eps` を使い、どちらも大きい
ほうの立体で正規化していました。前者はそれで正しく、後者は間違いです。
**正しい答えが弾かれても、それはエラーとして出るので誤答には見えません。**
対応表の数が減るだけで、原因は分かりません（4-22）。

**型が表現できないものは、実装が正しくても返せません。** 軸平行の箱の近道は
`Option<Solid>` を返し、`None` は「この近道の出番ではない」でした。空の答えを
置く場所が無いので、`A - A` はエラーになります。4-6 で一般経路を直したときに
同じ問題がここにもあったのに、**戻り値の型を見なかったので気づけません
でした**。「この原則は全ての経路に入っているか」を確かめるとき、実装だけで
なく**型が答えを表せるか**も見てください。

**素直な検体しか無い検査は、素直な欠陥しか見つけません。** 45ケースの走査は
桁の揃った配置ばかりで、上の2件はどちらも一度も出ませんでした。
`robustness_probe` に、桁差・自分自身との演算・ナノメートルの隙間・退化入力を
入れてあります。**答えられないこと（clean error）は健全な結果です。**
危険なのは誤答とパニックで、どちらも呼び出し側が区別できません。

**「速くなるはず」も測ってから言うこと。** 分割が費用の90%だと分かった
あと、減らす手を3つ試して**すべて外しました**（4-21）。1つ目は三角形を
半分にして評価を35%増やし、2つ目は評価を4分の1にしてブーリアンを壊し、
3つ目は評価を27%減らして**全期間ゼロだった誤答を2件出しました**。
**減らした量と払う費用は比例しません**し、余分に見える条件が実は答えを
支えていることがあります。

通ったのは性質の違う2つだけです。**射影の温め**（外れ方が安全側に固定
されている）と**判定の記憶**（同じ引数に同じ戻り値を返すので、ビット単位で
同一）。どちらも「何をするか」を変えていません。**何をするかを変える最適化は、
この領域では3回とも壊しました。**

**精度は「どこまで正確か」だけでなく「答えが出るかどうか」を決めます。**
ブーリアンは分割を採るかどうかを面積の和で判定するので、質量積分が
1e-14 から 1e-10 に落ちるだけで正しい分割が弾かれ、対応ケースが 39 から
31 に減りました。報告される体積の桁を見て「まだ十分」と判断すると、
実際には答えそのものが出なくなります。**精度を緩めるときは、その量を
何が読んでいるかを先に調べてください。**

**他カーネルの読みを診断器に写さないこと。** `mass_convergence` は
「OCC reads 768.397883」「OCC reads 1275.086556」を literal で持っていました。
実際の OCC はそれぞれ 770.039001732 と 1276.598256 で、自前の値と **2.01e-8 /
1.39e-6** で一致します。写した数字が古く、**存在しない 0.2% の食い違いを
報告し続けていました。** 突き合わせの本体（`freecad_cross_validate`）は
ずっと 2.01e-8 と言っていたのに、です。

写した数字は、対象が変わっても、カーネルが直っても、そのまま残ります。
しかも見た目は測定値と区別が付きません。**この手引きの「主張ではなく測定で
判断する」に素直に従った人ほど、この幻を追いかけます**（私は追いかけました）。
比較相手が要るなら、閉じた式を書くか、実際に両方を測るゲートを指してください。

**高次の則を使っていることは、高次で収束していることではありません。**
面の求積は6点・次数4の三角形則ですが、トリムされた面では2次でしか落ちて
いませんでした（4-20）。則の次数が効くのは被積分関数が滑らかな範囲の中だけ
で、B-spline のノット区間をまたぐとそこが崩れます。**収束の次数は、使って
いる則の次数からではなく、分割数を振って比を取って確かめてください。**
比がきっかり 4.00 で並んでいたら、何次の則を書いてあっても2次です。

**同じ機能が「掛かる面」と「掛からない面」に分かれていないか。**
ノット整合の格子は最初からありましたが、境界がパラメータ矩形の面にしか
掛かりませんでした。トリムされた面はそこから外れ、誰もそれに気づいて
いません——外れた側だけを測る検体が無かったからです。4-19 の厳密な線積分も
同じで、`Plane` にしか掛かっていませんでした。**「その機能はある」と
「この面に効いている」は別の主張です。**

**「既知の残差」と書いてあるものが、本当に届かないとは限りません。**
3-5 のトリム B-spline は残差として記録されていましたが、厳密な経路は最初から
あって、その面が入っていなかっただけでした（4-19）。**残差を疑う手は
「分割数を変えて動くか」です。** 動くなら求積の粗さで、細かくすれば届きます。
動かないなら偏りで、細かくしても永久に届きません。動かない量を見つけたら、
それは待つべきものではなく、経路を疑うべきものです。

**打ち消し合った小さい値を基準にすると、正しい修正が劣化に見えます。**
NURBS円柱の体積は 64分割で 1.38e-6、512分割で 8.43e-6 でした。細かいほうが
悪いのは、キャップの負の偏りと側面の正の求積誤差がその分割数で釣り合った
からです。偏りを直すと 64分割の値は 7.17e-6 に「悪化」します。
**符号の違う2つの誤差が乗っている量は、どの分割数で見たかで良し悪しが
入れ替わります。** 1点で引用せず、分割数を振って落ち方を見てください。

**プローブの測り方を間違えると誤診します。** このセッションで2度踏みました。

- 境界までの距離をサンプル点への距離で測り、「交線が面を横断していない」と
  誤読して、存在しない問題を追いかけました。線分距離で測り直すと gap は全て 0。
- 境界が曲面に乗っているかを 80×80 の格子で測って `0.41` を得ましたが、
  それは**格子間隔そのもの**でした（半径10の全周80分割 → 0.39）。粗い格子で
  当たりを付けてから局所的に詰め直すと `5e-13`。

**出た値が「測定の刻み幅くらい」なら、それは測定の限界であって欠陥ではない**
と、まず疑ってください。

**プローブが振る範囲に、本番の既定値が入っていますか。** `slice_robustness_probe`
は「分割数によらず閉じるか」を見るためのものなのに、振っていたのは 4〜32 で、
`slice_solid` が実際に使う 128 を一度も試していませんでした。だから
「0 failures」と言い続け、FreeCAD 側だけが断面の破れを見ていました。
**上限を上げるだけでは足りません。**失敗は分割数に対して単調とは限らず、
実測では 96・120・128・144・160・256 で落ちて 192 では通ります。既定値そのものを
列に入れてください（4-37）。

**`if let Ok(x) = f() { push(x) }` は、失敗を件数の減少に変えます。**
`CornerBlendN` はこれで、パッチを1枚も作れていないのに `Ok` を返していました。
呼び出し側から見ると「空の成功」なので、エラーとしては一度も現れません。
同じ形が `write_face` の `Option` にもありました（面が黙って消える）。
**捨てる前に、捨ててよいと言い切れるかを確かめてください。** 言い切れないなら
`?` で上げるほうが安全です。

**戻り値の型が失敗を表せないなら、その関数は失敗を隠します。**
`export_solid_to_string` は `String` を返すので、面が書けなくても伝える先が
ありません。4-6 と、上の「型が表現できないものは返せません」と同じ話が、
今度は入口ではなく**出口**で出ました。型を見るのは、実装を読む前です。

**空実装は `todo!()` の形をしているとは限りません。** 全ワークスペースで
`todo!` / `unimplemented!` / `FIXME` / `#[ignore]` は0件、警告も0件でしたが、
IGES は引数を読まずに固定文字列を返していました。手掛かりは
`fn export_solid_to_string(_solid: &Solid, ..)` の**アンダースコア1つ**です。
`grep "pub fn .*(_"` は1分で終わります。

**そのテストは、対象を通っていますか。** IGES のテストは**空のシェル**を渡して
`"S0000001"` が含まれるかを見ていました。エクスポータ側も引数を読んでいない
ので、両者は矛盾なく通り続けます。**形に依存しない検査は、形を作る実装を
検査していません。** 立体を変えたら出力が変わることを1本入れてください。

**比較相手を、相手の実装が書いたファイルに置いてください。** STEP の複合
エンティティの括弧抜けは、自前の出力どうしを比べているあいだは永久に見え
ません。OpenCASCADE が書いた同じ行を並べた瞬間に分かります。5章の
「比較相手を間違えると、欠陥が『仕様』に見えます」の裏返しで、**寛容な
読み手は欠陥を仕様に見せます**。

**単位が桁で外れたら、まず区切りの宣言を疑ってください。** IGES で箱が
ちょうど 25.4 倍になったのは、Global の1番目に書いた `1H,` が**値としての
カンマを含む**ためでした。フィールドを先にカンマで割る読み手はここで数を
取り違え、14番目の単位フラグを見失います。**「その値自身が区切り文字である」
宣言は、書かずに既定へ任せるほうが安全です。**

**2つの誤りが打ち消し合って、正しい数が出ていることがあります。**
スケッチの自由度解析は、階数を全変数で数え（過大）、式の本数も固定点の分まで
数えて（過大）、差を取ると正しく見えていました。片方だけ直すとテストが落ちます。
**落ちたテストの期待値が正しいとは限らず、直した側が正しいとも限りません。**
どちらも外の物差し（ここでは「階数は自由変数の本数を超えられない」という
不等式）に当ててから決めてください。

**公差で繋ぐしかない場面かどうかを、先に測ってください。** 断面の輪郭を
つなぐ溶接公差は、一致させたい隙間（最大 2.0e-6）と、別物として残したい
刻み幅（最小 1.0e-6）が**重なっていました**。どの値を選んでも壊れます。
重なっていると分かった時点で、公差の調整をやめて位相（メッシュの頂点・辺の
添字）で繋ぐ設計に移りました。**「良い公差が見つからない」は、公差で解く
問題ではないことの証拠です。**

**覆い漏らしを恐れて足した余裕が、そのまま答えに乗ることがあります。**
掃引面の媒介変数の範囲を、境界がちょうど収まるところから **2e-6 だけ広げ**
ました。その面の境界はパラメータ矩形そのものなので、広げたぶんが積分に入り、
側面積が 3.0e-6、体積が 1.3e-6 だけ大きく出ます。しかも**分割数を 32 から 256
まで振っても1桁も動きません**。同じ立体の平面キャップは 1.5e-14 で厳密な
ままだったので、幾何ではなく経路のほうが違うと分かりました（4-38）。
**安全側に丸めるときは、丸めたぶんを誰が読むかを先に調べてください。**

**対応するエンティティを、勘で選ばないでください。** 「実務のファイルには
回転面が出るはずだ」と考えて `SURFACE_OF_REVOLUTION` から書き始めるところ
でしたが、OpenCASCADE に実際に書かせてみると、母線を回した輪は
`CYLINDRICAL_SURFACE` に落ちていました。出てきたのは
`SURFACE_OF_LINEAR_EXTRUSION` と `ELLIPSE` です（4-38）。
**相手が何を書くかは、相手に書かせて数えるのがいちばん速い**です。
道具は `tools/occ_reference_swept.py` にあります。**ただし「1つの検体で
出なかった」を「要らない」と読まないでください。** ここで回転面が出なかった
のは母線が直線だったからで、スプラインの母線を回すと出ます。そちらは
インポーターが読めないままでしたが、**検体に1つも入っていなかったので
誰も気づけませんでした**（4-60）。

**症状を測ったからといって、原因まで測ったことにはなりません。**
3-Z には「同じ形なのにブーリアン結果が最大 17.5 倍重い」という**測った症状**と、
「原因は `SamplePlan::segments_for_edge` が稜ごとに倍々に刻むせいだ」という
**読んだだけの原因**が並べて書いてありました。表題は「測定済み・未着手」です。
数えてみると、稜の刻みは両者とも頼んだ分割数ちょうどで、ブーリアン側のほうが
稜は少ないくらいでした。本当の原因は構造格子を張れるかの判定のほうです（4-39）。

`segments_for_edge` は実際に倍々に刻むコードなので、読めばもっともらしく
見えます。**もっともらしさは、この repo では通貨になりません。** 原因を書く
ときは、それを何で数えたかも一緒に書いてください。数えていないなら「そう
見える」と書くこと。

**同じ形でも、支持曲面の広さは経路によって違います。** ブーリアンは元の立体の
曲面をそのまま持つので、全高 40 の円柱から高さ 20 の箱を削ると、ボア壁の面は
曲面の半分しか使いません。面の境界は領域の**内部**の等パラメータ線に乗ります。
「境界が領域の縁にあるか」で場合分けするコードは、**同じ形をビルダーで作った
ときだけ通り、ブーリアンで作ると通りません**。判定は領域全体ではなく、
境界自身が囲む範囲に対して行ってください（4-39）。

---
**「削り残さない」と「削った跡である」は別の主張です。** 歯元フィレットが
ホブの包絡線かどうかを測るとき、「どの工具円もフィレットに食い込まない」
だけでは足りません。歯底円をそのまま伸ばした形も食い込みません——工具が
触れないので。**フィレット上のどの点も、どれかの工具円に乗っている**ことを
別に測ってください（4-40）。片側だけの検査は、通っても何も言えません。

**閉じた式を持つ検体を、持たない検体で置き換えないでください。**
歯元をトロコイドにするとき、既定の `make_spur_gear`（歯元が半径方向の直線）は
そのまま残しました。その形には断面積の閉じた式があり、`builder_audit` の
外の物差しになっているからです（実測 1.99e-9）。トロコイド版に置き換えると、
物差しごと失います。**新しい形は、古い形を消さずに足してください。**

**検査のために内部を見せるほうが、検査を弱めるより良いことがあります。**
曲線が包絡線かどうかは、それが包む族を渡さなければ外から測れません。
`GearBuilder::root_fillet_generation` は工具の運動をそのまま返します。
公開 API が増えるのは代償ですが、**「体積が正である」だけの検査で
済ませる**よりは安いと判断しました（旧実装はそれで通っていました）。

**「無くても往復する」は「要らない」ではありません。** STEP に 2D の
トリム境界を書かない理由として「OCC 自身も出さないし、無くても往復する」と
書いてありました。どちらも事実です。往復するのは OpenCASCADE が 2D 境界を
**自分で求め直せる**からで、求め直さない読み手には何も残りません。
**相手ができることを、こちらがやらなくてよい理由に使わないでください**——
その相手しか試していないなら、なおさらです（4-41）。

**入れたものが、別の測っている量を動かしていないか。** p-curve を書くように
したら、球の書き戻し精度が 1.19e-13 から 1.71e-10 へ落ちました。ゲートは
1e-6 なので**素通り**します。気づいたのは、表の数字を前回の走行と見比べた
からです。**許容の中に収まっていることは、動いていないことではありません。**
実力が上がったら許容も締めてください（このとき 1e-6 → 1e-8 にしました）。

**「実装していない」を、空の成功で表さないでください。** `zenith_server` は
三角形0枚のメッシュ、要素0個の配列、0.0 が並んだ計測値を**成功として**
返していました。呼ぶ側から見ると「モデルが空」と区別が付きません。計測値の
0 に至っては、空ですらなく誤答です。名前を挙げて断れば、呼ぶ側は失敗として
扱えて、次に手を付ける人は何が空白かを一覧できます（4-41）。

**やらないと決めたことも、決めたと書いてください。** 解析曲面を STEP に
書き出す件は、認識の危うさ（5章の「円錐が円柱として通る」）、p-curve の
書き直しが要ること、いまの経路が緑であることを見て、**実装しないと決めました**。
決めずに残すと、次の人は同じ検討を最初からやり直します。やるときの筋道も
一緒に書いてあります（4-41）。

**「正しく測る」ようにしたら壊れることがあります。控えめな見積もりが仕事を
していないか、先に確かめてください。** テッセレーションの許容たわみは、立体の
寸法を**頂点だけ**から測っていました。曲がった形では実際より小さく出ます
（螺旋ばねは両端が同じ角度に来るので、頂点だけ見ると幅 2R が消える）。
稜の途中まで見て寸法を正しくしたところ、トーラスの詰まりは直りましたが、
ばねのメッシュ体積が**収束しなくなりました**（64分割で相対 5.85e-1、
128分割で -4.86e0）。目標が緩むとパッチの境界が巻きをまたいで切り取られます。
過小評価が安全側に効いていた場所でした。測り方は戻し、**潰れだけを見張る**
（頂点の箱が稜の箱の 1e-3 未満なら切り替える）ようにしています。

**別経路の数字が正しいと、出力の欠落は見えません。** 他カーネルが書いた全周の
円錐と半球は、**側面がメッシュから丸ごと消えていました**（三角形20枚、高さ 0）。
体積は面ごとの積分で出すので正しく、`foreign_reexport` も `verify_reexport` も
緑のままでした。見つかったのは、メッシュの境界箱を切り手の寸法に使ったときに
「高さ 0 の箱は作れない」と断られたからです。**その形の出力を、その形の出力
として測る**プローブが要ります。

**曖昧さの条件を粗く書くと、基準にすべきものまで曖昧になります。** 継ぎ目に
乗った p-curve をループの隣に合わせて寄せるとき、曖昧の条件を「各制御点が
min か max のどちらかにある」と書きました。円錐の底円の p-curve は u が 1 から
0 へ動く1次の直線で、制御点は2点ともちょうど端にあります。**基準にすべき区間
まで曖昧**になり、半球だけ直って円錐は直りませんでした。正しい条件は
「**全部が同じ側にある**」です。

**「返ってこない」は、まず刻み数を数えてください。** トーラスのテッセレーション
が4分割でも返りませんでした。分割数に依らないので遅いのではありません。段ごとに
即座に吐かせて降りると、境界の点列が 16384 点（継ぎ目の稜4本 × 上限 4096）で、
そこから先の earcut が二次で効いていました。**上限に張り付いていること自体が
合図**です。上限があるから止まらないだけで、上限に達したら結果は使い物に
なりません。

**絞り込みの箱は、標本ではなく上界で作ってください。** 面の境界箱を12点ずつの
標本から作っていました。曲がった境界は標本の間で外へ膨らむので、その箱は面より
**小さく**なります。有理曲線はパラメータが角度に比例しないので「端と真ん中を
取れば足りる」も成り立ちません。実測で、本当の y の最大 10 に対して箱は 9.993。
交線がその隙間に入り、**面の組ごと候補から消えました**。消えた組は「交わらない」
と区別が付きません。B-spline は制御点の凸包に含まれるので、制御点で測れば
必ず含みます。**大きすぎる方向に外れるのは無駄で済み、小さすぎる方向に外れると
答えが消えます。**

**同じ形を別の作り方でも用意して、並べてください。** 読んだ立体を切れないとき、
断り文には段ごとの件数しか出ません。同じ半径・同じ高さの円柱をビルダーでも
作って並べたら、形は同じで**持ち方が違う**ことが1行で分かりました
（3面/6稜使用 対 6面/24稜使用）。対照が無いと、原因を曲面の精度や交差の
アルゴリズムに求めてしまいます。

**「効くはずの手当て」を1つずつ試して、両方とも効かないことがあります。**
全周1枚を刻む、平面として持ち直す——どちらも単独では何も変わりませんでした
（正規化は割った面 0）。効いたのは**両方を、この順で**やったときだけです。
先に平面にしないと、キャップが p-curve に守られていて、共有している円が
刻めません。**片方を試して駄目だったことは、その方向が違うことの証拠には
なりません。**

**認識が危ういかどうかは、根拠で決まります。** 4-41 で解析曲面の書き出しを
見送った理由は「認識が当てずっぽうになる」ことでした（5章の円錐が円柱として
通った件）。一方、平面の認識は**当てていません**——B-spline は制御点の凸包に
含まれるので、制御点が1平面に乗れば曲面はその平面から出られない、という定理
です。「認識」という言葉でひとまとめにせず、**標本から当てているのか、
性質から決まるのか**で分けてください。

**検体が全部同じ条件だと、その条件を読んでいないことに気づけません。**
他カーネルの検体10本はすべてミリで書かれていました。インポーターは単位を
**1行も読んでいません**でしたが、10本とも通ります。インチのファイルを1本
足したら、体積が 16387 倍小さく返りました——閉じた多様体で、形も正しく、
大きさだけ違う。**恒等式も閉性検査も面積検査も、全部通ります。**
検体を並べるときは「何が同じか」を数えてください。同じものは測れていません。

**自分で作った検体は、外の物差しに通してから使ってください。** インチの
ファイルは手で作りました（数値をインチ値にして、単位の実体を差し替える）。
そのまま自分の読み手に食わせても、**ファイルが間違っているのか読み手が
間違っているのか分かりません**。OpenCASCADE に読ませて解析解どおりの大きさに
戻ることを先に確かめてあります。そこまでやって初めて、食い違いは読み手の
問題だと言えます。

**STEP のトークンと括弧の間には空白が入ります。** ファイルは 80 桁で折り返され、
折り返しがちょうどそこに来ることがあります。`GLOBAL_UNIT_ASSIGNED_CONTEXT(`
で探して当たらず、**単位の文脈が無いファイルに見えました**。実測でそうなって
いたのは、行を空白で連結してから探していたためです。トークンで探し、空白を
飛ばし、括弧の対応を数えてください。

**同じ量が複数の場所から入ってくるなら、全部の入口を数えてください。**
座標は `get_point` の1箇所を通りますが、半径は円・楕円・円柱・円錐・球・
トーラス・`VECTOR` の大きさと、7箇所に散っています。座標だけ単位を掛けると
**箱は通って円柱だけが壊れます**。平らな形だけで確かめると、その差は出ません。

**外の物差しにも、緩む領域があります。** このリポジトリは OpenCASCADE を
ゲートに使っていますが、**有理 B-spline 上の求積では OCC の数字が解析解より
0.078% 低く**出ます。自分で書いたファイルを自分で測ってそうなります。
その値を正解に据えたところ、**解析解ちょうどを返しているこちらが WRONG と
出ました**。外の物差しを使うときは、その物差しがその領域で強いかを先に
確かめてください。閉じた式があるなら、閉じた式のほうが上です。

**ファイルを見れば済むことを、API 経由で確かめないでください。**
上の件で、FreeCAD の Python から `surface.isURational()` を呼んで「7面とも
非有理」と読み、「非有理なら円は表せない、だから近似のはずだ」と考えを
進めました。**ファイルのテキストには `RATIONAL_B_SPLINE_SURFACE` と重み
`(1, 0.5, 1, 0.5, ...)` がそのまま並んでいます。** 一次資料が読める形で
手元にあるときは、それを読んでください。

**測って何も出なかったことも、記録に残してください。** 表現の3軸のうち、
単位では欠陥が出ましたが、複数立体と B-spline 化では出ませんでした。
「出なかった」を書かないと、次の人が同じ軸をもう一度掘ります。**測った範囲
そのものが、このリポジトリの資産です。**

**「前は通っていたのに拒否になった」を、回帰と決めつけないでください。**
`revolved_ring` を角の箱で削る配置で、まさにそう読んで**コードのコメントに
書きました**。OpenCASCADE に同じ配置を計算させると 30.109547 mm³ 削れます。
つまり以前の「元の体積のまま」は**誤答**で、いまの拒否のほうが正しい。
誤答が拒否に変わるのは改善です。**「通っていた答え」が正しかったかを、まず
外の物差しで確かめてください**（`tools/occ_cut_reference.py`）。

**検体を足したら、プローブの一覧にも足してください。** `revolved_ring` と
`elliptic_prism` は検体には入っていたのに `regularize_probe` の一覧に入って
おらず、正規化はこの2本で**一度も測られていませんでした**。そして測って
みたら、どちらも壊れていました。検体とプローブは別々に増えるので、片方だけ
増えます。

**「閉じている」と「演算に渡せる」は別です。** `regularize_probe` はシェルの
閉性だけを見ていて、`valid` と報告していました。ブーリアンの入口が見るのは
`is_topologically_valid` で、こちらのほうが厳しい。**閉じたまま無効**という
状態があり、閉性の列ではそれが見えません。列を1つ足すまで、測っていたのに
見えていませんでした。**検査は、後段が実際に要求するものと同じものを見て
ください。**

**同じ箱に2つの仕事をさせているなら、要求が逆を向いていないか確かめて
ください。** 面の境界箱は「候補の絞り込み」（外してはいけない）と「交線の
切り詰め」（緩めてはいけない）の両方に使われています。上界にしただけでは
片方しか満たしません。細分して**保証つきのまま締める**と両方満たせます。

**面のトリムを見る口が複数あるなら、全部に同じ規則を入れてください。**
平面のトリムは線分用と曲線用の2箇所で見ており、**どちらも外周ループしか
見ていませんでした**。穴の中も面の一部という扱いになり、穴を素通りするだけの
相手と交わっていることになります。片方だけ直すと、配置によって挙動が変わる
という、いちばん追いにくい形になります。

**「かもしれない」で通す入口は、後段が「無かった」と分かったときの戻り道を
用意してください。** ブーリアンの入口は境界箱と面の種類しか見ないので、
触れていない立体も通します。交線が1本も出なければ、答えは包含関係だけで
決まる——その経路は既にあったのに、そこへ戻る道がありませんでした。
**入口の近似は、後段で否定されたときに引き返せて初めて安全です。**

**離れた2つの立体の和は、1つではなく2つです。** 体積の合計だけを見ていると、
融合してしまっても気づけません。個数も見てください（`the_union_is_two_separate_solids`）。

**「Xを無視する」直しを入れたら、「Xのすぐ隣」が巻き添えになっていないかを
必ず測ってください。** 穴の中を面の外にしたので、穴の壁に**本当に届く**相手が
切れなくなっていないかを、太い棒で別に確かめています。除外の直しは、効き
すぎたことが症状として出にくいものです。

**「何件あるか」ではなく「何種類あるか」を先に測ってください。** 残っていた
23件の拒否を段の数字で並べたら、**23件すべてが同じ1箇所**（切り口の蓋が
作れていない）でした。先に分類しなければ、`revolved_ring` の6件を個別に追って
同じ結論に6回たどり着いていたはずです。数を減らす作業に入る前に、内訳を
出す道具を1本書くほうが速い（`foreign_boolean_stage_probe`）。

**同じ形をビルダーでも作って、両方落ちるか見てください。** 読んだ立体が
切れないとき、原因はファイルの持ち方だと考えがちです。同じ輪をビルダーで
組んでも同じように断りました。**読み込みは無関係**だと分かれば、以降は
ファイルを挟まない再現で追えます（`ring_corner_probe`）。

**診断は、本番と同じ入力で走らせてください。** `prepare_exact_boolean`（内訳を
返す口）は入力を整えずに数えており、演算のほうは入口で整えていました。同じ
配置で「6組・8稜・蓋2枚」と「4組・6稜・蓋0枚」に食い違い、**後者を見て23件を
分類していました**。診断が本番と違う道を通ると、出てくる数字は本物に見えて
中身が別です。**内訳を返す関数を足したら、本番と同じ前処理を通っているかを
最初に確かめてください。**

**切った出力を、そのままデータとして読まないでください。** 断り文を 130 文字で
切って表示し、そこに映っていなかった「蓋 2 枚」を「蓋 0 枚のまま」と読みました。
直したはずのものが直っていないという結論を、**表示の都合から**出しかけました。

**「N本あぶれた」で止まらず、その稜を名指ししてください。** 本数からは、
足りないのが面なのか、向きなのか、別の場所を指しているのかが分かりません。
面片を走査して**1回しか使われていない稜**を位置つきで出したら、あぶれていた
のは球面上の弧で、**球の面が1枚も割られていない**ことがその場で見えました
（`unmatched_edge_probe`）。中点の座標をノルムに入れるだけで、その点が
どちらの形の上にあるかが分かります。

**「割れない」の理由は、切り込みの形にあることがあります。** 面を割る口が
「境界から境界へ」しか受け付けないと、**面の内側で閉じたループ**は
「境界に届かない」と断られます。断られたことは記録に残りますが、残るのは
「割れなかった」であって「閉じたループだったから割れなかった」ではありません。
断り文が形を言わないなら、形を自分で見てください。

**プローブが公開 API と違う口を使っているなら、そう書いてください。**
`foreign_boolean_probe` は検証なしの口を使います（ゲートに拒否された結果でも
恒等式は測れるので、そのほうが情報が多い）。**そのぶん実際の API より甘い
数字が出ます。** 球の角は検証なしでは通り、検証つきでは落ちていたのに、
「3演算とも通った」と報告しました。いまは両方の数を出します。

**ゲートを緩める直しは、番人のテストが止めるまで気づけません。** 正しい答えが
ゲートに拒否されるのを直そうとして、**2回とも検査そのものを無効化**しました。
1回目は帯を三角形の大きさで取り（平面は粗く割ってもずれ 0 なのに）、2回目は
頂点法線で取りました（**溶接後の法線は平均されるので、直方体の角が「曲がって
いる」と出ます**）。どちらも `boolean_scale_and_empty_test` が捕まえています。

**正しい答えがたまに拒否されるほうが、誤答が通るより安全です。** 迷ったら
取り下げてください。ゲートは、誤答と利用者の間に立っている唯一のものです。

**同じ形のコードを新しく足したら、前に直した検査も一緒に持っていってください。**
4-46 で「割った断片の巻き方を面の向きと突き合わせる」を入れたのに、4-50 で
**新しい分割の口を足したとき、同じ検査を入れませんでした**。球では偶然合って
いて、円錐で合いませんでした。**「偶然合っていた」は、測るまで「合っている」と
見分けが付きません。** 分割・縫合・向きに触る口を足したら、既存の口が何を
確かめているかを見てから書いてください。

**拒否が減ると、隠れていた誤答が出てきます。** 恒等式は3つの演算が揃って
初めて組めます。`cone corner` は差がずっと誤答（削り残し 0.003239）でしたが、
積と和が拒否されていたので**恒等式が組めず、見えていませんでした**。
積と和を通した瞬間に残差が 8.38e-10 から 9.91e-7 に跳ねました。
**通る数が増えたときは、残差も一緒に見てください。** 増えた行が既存の誤りを
照らしていることがあります。

**標本は「ある」ことは示せますが、「ない」ことは示せません。** 差には
「中身が重なっていなければ A をそのまま返す」近道があり、その判定は共通の
境界箱に512点を撒くだけでした。重なりが 0.003239 mm³、箱が 216 mm³ の配置では
期待値 0.008点で、**まず当たりません**。そして「重なっていない」と決めて、
汎用経路が作れていた正しい答えを捨てていました。**標本による否定を、計算を
打ち切る根拠に使わないでください。** 使うなら最後の受け皿に置いて、本筋が
失敗したときだけ効かせます。

**「ゼロか」を相手の大きさで測らないでください。** 積が正かを見る閾値が
`1e-3 × min(V(A), V(B))` になっていて、真値 0.003239 に対して 0.729 でした。
訊きたいのは「この立体は何も囲んでいないのか」だけで、相手の大きさとは
関係ありません。`tol.linear` の3乗が数値的なゼロです。**相対で切ると、
小さい答えほど弾かれます**——薄い重なり（板と針、面取りのかかり、公差ぎりぎりの
嵌合）は実務にいくらでもあります。この判定はこれで3度目の修正です。


**未実装は、失敗の形を変えて出てきます。** 「上面の切り口が縫えない」という
症状の正体は、`Planar face splitting with inner wires is not implemented yet`
という**そのままの一行**でした。未整合辺3本の端点を並べて初めて「閉じた
三角形＝その面が割れていない」と分かり、そこで初めて分割器の中を覗きました。
**縫合の失敗を数えるだけでは、原因の段には届きません。** 段ごとに何が
返っているかを出させてください（`foreign_boolean_stage_probe`）。

**穴を配るときは、体積ではなく穴の有無を検査してください。** 割った片に穴を
「両方に入れる」「どちらにも入れない」をやると、**体積は合っていても穴が
消える／二重になる**ことがあります。`planar_split_with_hole_test` は
「穴が失われれば π·100·6 ぶん増える」という形で、体積とは別に見ています。

**同じ形を作る道が2つあるなら、直したほうを検査してください。** 輪は
読むと4面、ビルダーで組むと10面に割れます。直したのは読んだ形の経路なのに、
テストはビルダー製で書いてしまい、**直したはずの経路を一度も通って
いませんでした**。「同じ形だから同じはず」は、面の枚数が違う時点で崩れます。


**「交わらない」の中身を、必ず1段割ってください。** 平面と円錐の交線が出ない
のは、辿れないからではなく**当てはめが要求精度に届かない**からでした。辿った
点は両方の曲面から 4e-11 しか離れていないのに、そこに通した曲線のずれが
3.53e-5 で、要求は 1e-6。「交線 0」という表示は**この2つを区別しません**。

**収束の見切りは、序盤の減り方で入れてはいけません。** 「刻みを半分にして
0.9 倍に届かなければ諦める」を入れたところ、別の配置の交線が 12 本から 2 本に
減りました。3次補間の理屈（8分の1）は漸近の話で、序盤はそれよりずっと緩く
減ります。**止める条件は「届いたか」と「これ以上刻んでも伸びないか」だけ**に
してください。費用は点の上限が抑えます。

**恒等式は「何も起きていない」を見つけられません。** 切り手を丸ごと無視して
A をそのまま返すと、V(A-B) = V(A)、V(A^B) = 0、V(AuB) = V(A)+V(B) となり、
**2つの恒等式はどちらも成り立ちます**。トーラスを半分に切る配置が、残差
2.44e-12 の「ok」として1度も疑われずに並んでいました。**積が 0 の行は、
判定できていない行です。** `foreign_boolean_probe` はそれを NOCUT として
分けて数え、外の物差し（`occ_cut_reference.py`）へ送ります。

**2つのカーネルを並べる前に、箱が同じか確かめてください。** OCC の
`BoundBox` は密着していません——トーラスでは真値 ±16 に対して ±17.3183 と
返します。切り手は箱に対する比で置くので、そのままだと**別の配置**を測る
ことになり、積が 67.16 と 52.16 に分かれました。同じ箱で測れば一致します。
`cutter_placement_probe` が Zenith の使う箱を、`--box` にそのまま貼れる形で
出します。**外れているのが答えなのか物差しなのかは、合わせるまで分かりません。**

---

**曲線に直線を当てるところを、折れ線で代用しないでください。** p-curve と
直線の交点は、1スパンの1次・2次だけ閉じた式で解き、それ以外は 32 点の
折れ線に落ちていました。多スパンの3次では**弦と曲線のずれがそのまま交点の
ずれ**になり、同じスプラインの上の同じ点が、面をまたぐと 2.3e-3 食い違って
いました。本数は合っているのに輪が閉じない、という形で出ます。符号付き距離は
滑らかなので、**折れ線で挟んでから曲線そのものの上で二分**すれば、次数に
依らず丸め誤差まで詰まります。

**「未整合」と「非多様体」を同じ天秤に載せないでください。** 蓋を足すかを
`(未整合, 非多様体, 同方向)` の辞書順で決めていたので、**非多様体を6本
作ってでも未整合を2本減らす**選択が「改善」と判定されていました。未整合は
「まだ閉じていない」、非多様体は「壊れている」。足してよいのは、閉じる方向に
だけ動くときだけです。

**「1スパンだけ」の口を、境界の稜に使わないでください。** 面を割るときに
境界の稜を着地点で分ける処理が `split_bezier_at`（制御点が次数+1個ちょうど）
しか使っておらず、内部ノットを持つ稜——押し出したスプラインの輪郭——を
断っていました。**断られると面はそのまま残ります**。症状は「蓋が割れない」
ですが、原因は曲線1本の細分です。`split_at` はノット挿入で割るので多スパン
でも有理でも通ります。

**「鎖にまとめる」は「1本の鎖にする」ではありません。** 曲面に届いた交線を
丸ごと1本の切り込みとして渡していたので、出入り2箇所ぶんが届く面では
「端が4つある」と断られ、面はそのまま残っていました。端の繋がりで**鎖に
分けてから**、1本ずつ順に当ててください。

**断られたときは、断り文をそのまま出させてください。** 「面が割れない」の
中身は、この作業だけで「1スパンしか割れない」「端が4つある」「境界から
1.885 離れている」と3種類ありました。段の合計（applied splits）はどれも
同じ 0 に見えます。`intersection_edge_probe` は面ごとに断り文を出します。

**同じ欠陥は、分割器の数だけあります。** 「断片の輪を、元の面の巻き方に
合わせる」は 4-46 で `Regularizer` に、4-52 で `split_by_interior_loop` に
入れました。**円柱側面の分割器にはまだ入っていませんでした。** 断片の輪を
決め打ちの順で組む口を見つけたら、そこも同じです。裏向きの面——輪の穴の壁が
それです——でしか出ないので、表向きの検体では偶然合ったままになります。

**面積の突き合わせは、標本の密度を揃えてから。** 領域の和が元に戻るかを
見る検査が 5.3e-4 で落ちましたが、欠陥ではなく、比べている2つの多角形の
標本が 24 点と 48 点だったためです。**片側だけ密にすると、常に小さく出ます。**
揃えて密にしてから、帯は 1e-6 のままにしてください。緩めると領域の
取り違えが通ります。

**物差しが何も見つけなくなったら、物差しのほうを広げてください。** 他カーネル
立体のブーリアンは30配置とも通るようになりましたが、それは「欠陥が無い」では
なく「この30配置では出ない」です。検体を9つ足したら、**読めない曲面**が
すぐに1つ出ました。切り手を傾けたら、別の拒否が出ました。

**「対応している」と書いてあることと、測ってあることは別です。** 掃引面の
道具の説明には「OCC は回転面を選ぶ」と書いてありましたが、実際に書かれた
ファイルには円柱と平面しか入っておらず、`SURFACE_OF_REVOLUTION` は
**1つの検体にも入っていませんでした**。インポーターはそれを読めません。
対応範囲を主張する前に、その実体が検体に入っているかを数えてください。

**検体を作るときも、意図した形になっているか測ってください。** 曲がり管の
断面を軌道に直交させ忘れ、断面の平面が軸から 0.53 mm ずれた回転面になって
いました。管ではない別の形です。生成した検体は、書いたあと読み直して
どの実体が入ったかを数えます。

**期待値に相手の値をそのまま使わないでください。** 挽き物の体積は、こちらと
OpenCASCADE で 1.3e-5 違いました。刻みを振ってもこちらは 10 桁動きません
——**動かない差は求積の粗さではありません**。母線から直接積分したら、
外れていたのは OCC のほうでした。相手の値を期待値にすると、**相手の誤差を
仕様として焼き付けます**。

**独立とは、相手の測定を1つも通らないという意味です。** 上の突き合わせで、
最初はパップスの定理を使おうとして `Part.Face.Area` と `CenterOfMass` を
呼びました。それも OCC の求積を通るので独立ではなく、2.8e-4 ずれた 第3の
値が出ただけでした。曲線の評価だけで済む形に組み直して初めて決まりました。

**「軸に平行」は特別な場合です。** 30配置とも通ったのは、切り手が全部座標軸に
平行だったからでもあります。27 度傾けた配置を足したら、6検体が断りました。
平面同士の交線も等パラメータ線も、軸に平行なうちは特別に易しいままです。

**費用も測ってください。** 傾けた配置は答えこそ正しいものの、1件で15分
かかっていました。当てはめの刻みを半分ずつ試すたびに最初から辿り直して
いたためです。**2回測れば収束の次数が出ます**——理屈の4乗ではなく、
その場で測った次数から必要な刻みを予測すれば、同じ答えが 75 秒で出ます。
「返ってくる」と「使える」は別です。

**「交線が無い」を、1つの数で片付けないでください。** 中身は3つあります
——種が無い（本当に離れている）、種はあるが辿れない、辿って縁まで届いた。
本数だけを見ていると全部「0本」に見えます。実測では、離れている組が10、
**辿れない組が12**、届いた組が8でした。`march_stop_probe` が分けて出します。

**自分の見立ては、測る前に記録に書かないでください。** 4-61 に
「辿りが面の内側で止まっている」と書きましたが、測ると**8組とも縁まで
届いて**いました。私が端点として引用したのは辿りの終わりではなく、
**別の組に続く継ぎ目の点**でした。1つ道具を書けば分かったことです。

**「もう見た」を、候補の列の有無で判定しないでください。** 交わりを求めて
`Unsupported` になった組も、候補の列には入っています。素通しで数えると
全部「もう見た」になり、**追加の処理が一度も走りません**。実測でそうなって
いて、直すまで交線は1本も増えませんでした。**列に入っていることと、
中身があることは別です。**

**「全部だめだったときの受け皿」は、「1つでも通ったら要らない」ではありません。**
鎖にまとめて当て直す段を `applied_split_count == 0` で守っていたので、1枚の面に
2本の鎖が届いて片方が単独で当たると、**もう片方は一度も試されずに捨てられて**
いました。受け皿の条件は「まだ当たっていない稜が残っているか」であって、
「1本も当たらなかったか」ではありません。

**`git checkout <ファイル>` は、そのファイルの変更を全部捨てます。** 測って
悪くなった変更だけを取り下げるつもりで打ち、**同じファイルにあった、測って
良くなっていた変更も一緒に消しました**。幸い手順が残っていたので入れ直せ
ましたが、**良い変更は、次の実験を始める前にコミットしてください。**

**測って悪くなったら、理由が分からなくても取り下げてください。** クリップの
内外判定を折れ線から曲線に変える直しは、筋は通っているのに、測ると対象の
ずれが 5.073e-4 から 1.473e-3 へ**悪化**しました。理由は詰めていません。
筋が通っていることは、良くなっている証拠ではありません。

**厳密な口に見えても、中で何を呼んでいるかを見てください。** 内外判定を
折れ線から曲線に変えるつもりで、交点を厳密に解く関数に任せました。**その
関数は、区間ごとの内外を 32 点の折れ線で決めていました。** 48 点の折れ線を
1か所から追い出したつもりで、もっと粗いものを呼んでいたわけです。
「交点は厳密」と「内外は厳密」は別のことです。

**境界のすぐ近くを判定する処理は、境界のすぐ近くで壊れます。** 二分で境目を
詰めるとき、終盤の点は必ず境界のそばにあります。そこで「中点を見る」たぐいの
判定を使うと、いちばん精度が要る場面でいちばん外れます。**偶奇（交点の個数）
で決めれば、点が境界からどれだけ近くても効きます。**

**同じ点が2通りの値で出ていないか、数えてください。** 円錐の上面の切り口の
端は、ある面のクリップでは 3.8246、別の面のクリップでは 3.8251 と出て
いました。**同じ交線の同じ端です。** 半径を測ると 3.99954 で、真の円（4）の
内側 4.6e-4——折れ線に内接した位置です。**食い違いの大きさが、近似の
たわみと同じ桁なら、原因はほぼそれです。**

**稜を端点だけで見分けないでください。** 同じ2点を結ぶ別々の弧は普通に
あります。トーラスを傾けたスラブで切ると、管の底の継ぎ目で四半パッチが2枚
出会い、同じ2点の間に**中点が 3.839 離れた2本の弧**が出ます。端点だけで
突き合わせると、閉じた殻なのに「非多様体」「面が重複」「同じ向きに2度使われた
稜」と順に報告されます。**壊れていたのは立体ではなく数え方でした。**

**同じ間違いは、検査の数だけあります。** 上の欠陥は3か所にありました
——縫合の `StitchEdgeUse`、面の署名 `face_boundary_signature`、殻の
`same_directed_edge`。直すたびに次の検査が引っかかり、3段構えで同じことを
していたと分かりました。しかも `EdgeUse` は**中点を前から持っていて、
使っていなかっただけ**です。**構造体が持っている情報を、比較が使っているか
確かめてください。**

**「これが原因だろう」で書いたコードは、測ってから残してください。**
非多様体の原因を「面積ゼロの薄片」と考えて落とす直しを書きましたが、
**面積を測ったら 2.28・35.19・36.19** でした。薄片ではありません。
書いたものは取り下げました。**症状に合う話を思いついたことは、それが
起きている証拠ではありません。**

**判定に使う量は、判定の帯より細かく測れていなければなりません。** 領域の
面積の和が元に戻るかを 1e-6 の帯で見ていたのに、面積は uv の多角形（1区間
96点）で測っていました。切り込みが傾くと誤差が 1.1e-5 になり、**正しい
分割が帯に落ちます**。面そのものの積分に替えれば済む話でした。

**断り文は、必ず「どれが」を言ってください。** 「a cut end does not lie on
any wire of the face」は、どの端かを言いません。名指しするようにした瞬間、
半径を測れば片が付きました（9.9137——外周 10 でも穴 4 でもない、鎖の継ぎ目）。
**追いかける人に、面の全部の稜を測らせないでください。**

**切り込みの端に境界を要求するのは、相手のいない端だけです。** 切り込みが
鎖になっていれば、中の継ぎ目は面の内側にあります。そこに境界を求めると、
正しい鎖が丸ごと断られます。

**入れ子は、自分自身に対しても成り立ってしまいます。** 連結成分の外形を
「それを含むいちばん小さい面」の穴として付けるとき、**自分の成分の面が
いちばん小さい**ので、素直に探すとそこに付きます。外周の外形が外周の面の
穴になり、314.16 の面が**面積 0** になりました。巡回ごとに通った節点を
覚えて、同じ成分を除いてください。

**穴の輪は、外周と逆向きです。** 巡回が出す外形は既に逆向き（面積が負）
なので、そのまま使えば済みます。裏返して持つと、縫合が同方向の稜を出します
——**衝突した片が1枚だけで、それが相手全部と衝突していたら、その1枚が
裏返っています。**

**「代表点が面の中か」を訊く前に、代表点の取り方を確かめてください。**
外周に囲まれた領域の重心は、円環では穴の中に落ちます。そこで「穴の中だから
面ではない」と判定され、正しい領域が丸ごと捨てられていました。

## 6. リポジトリの見取り図

| 場所 | 中身 |
| :--- | :--- |
| `crates/zenith_math` | 点・ベクトル・変換・ロバスト述語 |
| `crates/zenith_geom` | NURBS 曲線・曲面、Coons/Gordon、曲率、ノット挿入と分割、点列補間、交線のマーチング（`ssi_march`） |
| `crates/zenith_topo` | Vertex/Edge/Wire/Face/Shell/Solid、シェル検証 |
| `crates/zenith_algo` | ブーリアン・押出/回転/ロフト/掃引・穴/フィレット・断面・質量特性・全周の正規化（`regularize`）・一般の面分割（`face_split`）・STEP 書き出し口（`StepInterop`） |
| `crates/zenith_tess` | テッセレーション（積分領域はノット区間に整合） |
| `crates/zenith_io` | STEP 読み書き、STL/OBJ/glTF/DXF/IGES |
| `crates/zenith_py` | PyO3 バインディング（`#[pyfunction]` 58個 ＋ `#[pymethods]` 2ブロック） |
| `crates/zenith_server` | Seamless プロトコルの TCP 骨組み。`create_stack` / `delete_stack` 以外の**中身は未実装**で、操作名を含む明示エラーを返す。カーネルは呼んでいない |
| `crates/zenith_algo/examples/` | **測定・診断ツール**（73個） |
| `crates/zenith_algo/tests/fixtures/` | ルート直下にOpenCASCADE が書いた STEP 20本。`include_str!` で読むので `target/` を消しても検査は走る |
| `tools/*.py` | FreeCAD ヘッドレス検証（`verify_*` はゲート、`occ_*` は診断・検体生成用） |
| `target/showcase/` | 代表54形状の STEP。作り方は 7 章 |
| `target/iges/` | IGES 5.3 の検体5形状。突き合わせは `tools/verify_iges.py` |

Blender アドオン本体（`__init__.py`・オペレータ・パネル）は未着手です。
`blender_addon/` にはビルド済みの `zenith_cad.pyd` のみが入っています。

---

## 7. 出力物の作り方と置き場所

```bash
cargo run --release -p zenith_algo --example export_showcase          # target/showcase   54形状
cargo run --release -p zenith_algo --example export_validation_suite  # target/validation 22形状
cargo run --release -p zenith_algo --example foreign_reexport         # target/reexport    7形状
cargo run --release -p zenith_algo --example export_iges_suite        # target/iges        5形状
cargo run --release -p zenith_algo --example export_mesh_suite        # target/mesh_exports 8形状
```

| 置き場所 | 中身 | 突き合わせ |
| :--- | :--- | :--- |
| `target/showcase/` | 代表54形状。解析解を持つものは表に相対誤差が出る | `tools/verify_showcase.py`（54/54、2026/08/27 実測） |
| `target/validation/` | 相互検証用27形状＋OpenCASCADE が書いた参照ファイル | `tools/freecad_cross_validate.py`（27/27、不一致で非ゼロ終了） |
| `target/reexport/` | 他カーネルのファイルを読んで書き戻したもの7形状 | `tools/verify_reexport.py`（**ゲート**。解析解と 1e-8 以内、不一致で非ゼロ終了） |
| `target/iges/` | IGES 5.3 の検体5形状＋`manifest.json` | `tools/verify_iges.py`（**ゲート**。曲面の枚数と境界箱、不一致で非ゼロ終了） |
| `target/mesh_exports/` | STL / OBJ / glTF / DXF の検体8形状＋`manifest.json` | `tools/verify_mesh_exports.py`（**ゲート**。閉じているか・体積・3形式の一致・DXF の層。**FreeCAD 不要なので CI に入っている**） |

読み物のほうは別系統で、[`docs/treatise/`](docs/treatise/) に Markdown として
置いてあります。**束ねる生成（`tools/generate_unified_pdf.py`、HTML、PDF）は
2026年8月27日に削除しました**——本文が Markdown で読めるので束ねる意味がなく、
描画段は繰り返し実行で返ってきませんでした（4-114、4-115）。

ショーケースの 17〜24 は今回のセッションで通るようになった範囲です
（円錐を平面で切る3種、トーラスをスラブで切る2種、球を切る2種、
穴を5回重ねた板）。

---

## 8. リポジトリの状態

開発は `codex/tess-edge-contract` 上で進めています（2026年8月25日時点）。
基点の `main` は `origin/main` と同期しており、このブランチの各段は検証単位で
小さくコミットしています。作業再開時は、下の全ゲートに加えて必ず
`git status --short --branch` で未確定差分を確認してください。

追跡していないもの:

- リポジトリ直下の生成物 `*.step`、`target*/` — `.gitignore` 済み
- `reference/`（移植元の Seamless CAD と OCCT、合計 420MB）— `.gitignore` 済み
- `agent-work-log.json` — `.gitignore` 済み

**`.gitignore` の落とし穴**: `/tools/test_*.py` が無視されます。ここに置いた
検証スクリプトは追跡されないので、常設のゲートは `tools/verify_*.py` の名前で
作ってください（`verify_solid_api.py` は最初 `test_solid_api.py` という名前で
書いて、コミットに入らないことに後から気づきました）。

**ブランチをマージする前に、この一式が緑であることを確かめてください。**
どれか1つでも赤なら、`main` に持っていくものではありません。

```bash
cargo test --release --workspace --exclude zenith_py     # 119 バイナリ / 632 テスト（約17分。2026/08/27 実測）

# 同じもの＋プローブ73本を並列で回す。**速くはなりません**（実測 13分36秒、
# `cargo test` の約13分とほぼ同じ。4-73）。被覆が広いぶん時間もそこに行きます。
bash tools/fast_test.sh
cargo build --release --workspace --exclude zenith_py    # 警告 0

cargo run --release -p zenith_algo --example export_validation_suite
& "C:\Program Files\FreeCAD 1.1\bin\python.exe" tools/freecad_cross_validate.py   # 27/27

cargo run --release -p zenith_algo --example export_showcase
& "C:\Program Files\FreeCAD 1.1\bin\python.exe" tools/verify_showcase.py          # 54/54（2026/08/27 実測）

cargo run --release -p zenith_algo --example foreign_reexport
& "C:\Program Files\FreeCAD 1.1\bin\python.exe" tools/verify_reexport.py          # 7/7

cargo run --release -p zenith_algo --example export_iges_suite
& "C:\Program Files\FreeCAD 1.1\bin\python.exe" tools/verify_iges.py              # 5/5

# STL / OBJ / glTF / DXF を、書いたファイルだけから解き直す。**FreeCAD は要りません**
cargo run --release -p zenith_algo --example export_mesh_suite
py tools/verify_mesh_exports.py                                                   # 8/8

cargo run --release -p zenith_algo --example foreign_boolean_probe
# ok 26 / NOCUT 4 / 拒否 0 / WRONG 0 / PANIC 0、検証つきの口で 90/90、恒等式の残差 4.04e-9。返ってこないものが1件でも出たら赤

cargo run --release -p zenith_algo --example shape_variety_probe
# read 9 of 9 / volume ok 9 / WRONG 0

cargo run --release -p zenith_algo --example gate_membership_probe
# 0 of 6 refused。ゲートが正しい答えを断っていないか、数秒で見る（4-68）

cargo run --release -p zenith_algo --example foreign_distance_probe
# 36 checks, 0 miss(es)。読んだ立体への距離と内外が閉じた式に乗るか（4-69）

cargo run --release -p zenith_algo --example foreign_slice_probe
# 24 checks, 0 miss(es)。読んだ立体の断面が閉じた式に乗り、刻みで縮むか（4-70）

cargo run --release -p zenith_algo --example grid_fallback_probe
# 15 fixture(s), 0 over the allowance。格子から落ちた面が増えていないか（4-71）

cargo run --release -p zenith_algo --example foreign_edit_probe
# 20 applied / 22 closed-form / 0 WRONG。凹稜の負の除去体積を含め、読んだ立体の編集と面の計測が閉じた式に乗るか（4-72、4-92〜4-101）

cargo run --release -p zenith_algo --example foreign_inertia_probe
# 32 check(s), 0 miss(es)。読んだ立体の重心と慣性が閉じた式に乗るか（4-73）

cargo run --release -p zenith_algo --example contact_placement_probe
# **非多様体を返したもの 0** が赤の線。断られること自体は赤にしない（3-1）

# 傾けた切り手も測るなら（30分ほどかかるので既定では走りません）
$env:ZENITH_TILTED=1; cargo run --release -p zenith_algo --example foreign_boolean_probe
# ok 53 / NOCUT 7 / 拒否 0 / WRONG 0 / PANIC 0、検証つきの口で 180/180、恒等式の残差 7.88e-9。
# **拒否が1件でも出たら赤**です（4-67 で 0 になりました）。検証つきの口は
# 4-68 で 176/180 から 180/180 になりました。ここも1件でも欠けたら赤です

cargo build --release -p zenith_py
py tools/verify_solid_api.py
```

---

## 9. 実用に耐えるまでに何が要るか（判断。実測ではありません）

**この章だけは実測ではなく、実測から読んだ判断です。** 他の章と混ぜないよう
に分けてあります。**測定が出たら、この章の記述は上書きしてください。**
2026年8月24日時点の見立てです。

### 9-G. **一旦のゴール**（2026年8月28日に置いた数値目標）

**「どこまで行ったら GUI の話を始めるか」を、感覚ではなく数字で置きます。**

置く理由は1つです。**数字が無いと、私（AI）は「まだ心配です」と言い続けます。**
それは判断であって実測ではないので、決める人の邪魔になります。**達成したら、
心配の有無に関わらず次へ進んでよい**、という約束の形にしてあります。

**この節は判断です。** 数字そのものに根拠のあるものと、置いただけのものが
混じっています。区別できるよう、根拠を併記しました。**やってみて重すぎたら
下げてかまいません。**

| # | ゴール | いま（2026/08/28 実測） | なぜこの数字か |
| :--- | :--- | :--- | :--- |
| **G1** | **新しい掃き軸を3本足して、誤答が連続 0** | **達成**（2026/08/28 夜）。1本目 4-146＝7点、2本目 4-148＝4点、3本目 4-149＝4点、**合計15点すべて誤答 0**。その前は掃くたびに出ていた（4-133〜4-137 で4件、4-143 で1件）。**ただし15点はすべて `ZENITH_PAIR_QUICK=1` の3組**で、広い設定では測っていません。4本目以降の候補は 4-149 に | **枯れたかどうかは、1回では分かりません。** 3本連続で出なければ、出る確率が下がったと言ってよいと考えます |
| **G2** | **恒等式の掃き出しが、常設で15組以上** | **達成**（2026/08/28 夕）。`foreign_cross_pair_probe` の既定を**6検体・15組**にした（**722秒**、14組が3演算そろい、恒等式の破れ 0、残差の最悪 9.234e-10）。CI の上限 1200 秒に対して余裕 478 秒。短い側が要るときは `ZENITH_PAIR_QUICK=1` で3組・143秒 | 今日の誤答3件は**すべて恒等式でしか見えませんでした**（面は閉じ、非多様体でもなく、内外判定も通る）。**GUI では利用者が形を見ても気づけません**。CI の1本あたりの上限 1200 秒に収まる範囲が 15組です |
| **G3** | **トリム面1枚の三角形が 1万枚以下**（**画面に出る縫合メッシュで数えます**。4-151 で決め直しました） | **常設ゲートの分割では達成**（2026/08/28）。`ZENITH_PATCH_WHY=1` で、常設30配置・24分割・6,045面の最大が **4,604 枚**。傾けたトーラス × 箱の差では 24分割 1,152 枚 / 48分割 4,020 枚 / **64分割 10,354 枚（わずかに超過）** / 96分割 20,298 枚。**64分割以上は未達**です。`ZENITH_TRIM_WHY=1` の 163,014 枚は**積分側の別経路**で、そこは粗くしていません（4-150、4-151） | **これは画面を回すたびに効きます。** ブーリアン1回が遅いのは待てても、表示は待てません。1万枚なら、面が数十枚ある部品でも数十万枚に収まります |
| **G4** | **メッシュの非多様体が、常設の全配置で 0**（いまは B-Rep だけがゲート） | **達成**（2026/08/29）。`contact_placement_probe` の 81演算すべてで B-Rep もメッシュも非多様体 0。経過は 13本（4-129）→ 34本（4-150）→ 10本（4-152）→ **0本**（4-153。射影で1本に潰すのをやめた）。**常設30配置・90演算の実測範囲**です | **0 でないメッシュは STL に書けません。** GUI は表示も書き出しもメッシュ経由なので、ここは 0 でないと出口が塞がります |
| **G5** | **`main` への統合を1回** | **達成**（2026/08/29）。**173コミットを早送りで統合し、`origin/main` へ push しました**（衝突 0、`17b3d33..72e7506`）。作業ブランチ `codex/tess-edge-contract` も push 済みです | 技術ではなく運用です。**伸びるほど統合が重くなります。** GUI を別ブランチで始める前に一度合流させておくのが安全です |

**G1〜G5 が揃ったら、GUI（ブラウザを含む）の話を始めてよい**、とします。

> **2026年8月29日に、G1〜G5 が全部揃いました。**
>
> | | ゴール | 達成した日 | 実測 |
> | :--- | :--- | :--- | :--- |
> | G1 | 掃き軸3本で誤答連続 0 | 8/28 | 3軸・15点で誤答 0（4-146、4-148、4-149） |
> | G2 | 恒等式の掃き出しが常設で15組以上 | 8/28 | 6検体・15組・722秒 |
> | G3 | トリム面1枚が1万枚以下 | 8/28 | 常設30配置・24分割で最大 4,604 枚（4-150、4-151） |
> | G4 | メッシュ非多様体が常設の全配置で 0 | 8/29 | 81演算すべてで B-Rep もメッシュも 0（4-153） |
> | G5 | `main` への統合を1回 | 8/29 | 162コミットを早送り、衝突 0 |
>
> **数字は約束どおりです。ここから先へ進んでよい、という状態になりました。**
>
> **ただし、揃ったのは「置いた数字」であって「枯れた」ではありません。**
> どれも常設検体の実測範囲です。G3 は 64分割以上で未達（10,354 枚）、
> G4 は 30配置・90演算の範囲、G1 は 15点とも `ZENITH_PAIR_QUICK=1` の3組
> です。**この節を書いた理由（9-G の冒頭）どおり、心配の有無ではなく
> 数字で決める、という約束を守ります。**
>
> **`origin/main` へ push しました**（2026/08/29。`17b3d33..72e7506`、早送り、衝突 0）。作業ブランチも push 済みです。

#### ゴールに**入れなかった**もの（意図的です）

| 項目 | なぜ入れないか |
| :--- | :--- |
| **2Dスケッチ**（円弧・3Dとの接続。3-0-0 で未着手） | **GUI と一緒に設計すべきだからです。** スケッチは「何を編集させるか」で形が決まります。カーネル側だけで先に作ると、画面の話が始まってから作り直しになりがちです |
| **wasm で建つこと** | 依存を見た限り障壁は低い（`pyo3` は `zenith_py` だけ、`rayon` は1ファイル、CPU 固有命令なし、`import_solids_from_str` が既にある）。**ただし測っていません**——`cargo build --target wasm32-unknown-unknown` を1回通せば、この見立ての大半は実測に変わります。半日もかからないはずなので、**GUI の話を始めるときの最初の一歩**に置きます |
| **対話的な 0.1 秒級の応答** | 9-1 のとおり**桁が違います**。ここをゴールに入れると、他が全部止まります。**G3 を先に片づけてから測り直す**ほうが順序として正しいと考えます |
| **残る個別の未解決**（`torus × torus`、`torus × hollow_box` の軸平行配置） | **断っているので誤答ではありません。** 断りは形に固有ではなく配置に固有です（`torus × hollow_box` は 61 度回すと通ります。4-146）。GUI の前提にはなりません |

#### G5 は、思っていたより軽いです（2026/08/28 実測）

**`main` はこのブランチを切ってから一度も進んでいません。**

```bash
git merge-base --is-ancestor main HEAD   # 通ります＝main は HEAD の祖先
git merge-tree $(git merge-base main HEAD) main HEAD | grep -cE "^CONFLICT"   # 0
```

**`grep -c CONFLICT` では 1 が出ます。この文書自身が引っかかるからです。**
上の行に `CONFLICT` と書いてあるので、`main..HEAD` の差分に含まれてしまい
ます（2026/08/29 に実際に踏みました）。**行頭で見てください**——本物の
衝突は `CONFLICT (...)` として行頭に出ます。

**自分を数えてしまう検査は、緑にも赤にもなり得ます。** ここは「0 のはず」
だったので赤に見えましたが、逆向きの取り違えもあり得ます。

| | 値 |
| :--- | ---: |
| 未統合のコミット | **151** |
| 変更されたファイル | 113 |
| 追加 / 削除 | 26,646 / 721 行 |
| **衝突** | **0** |

**早送り（fast-forward）で統合できます。** 「151コミット」という数字だけ見ると
重そうですが、**衝突の解消は要りません**。止まっているのは判断であって、
作業量ではありません。

**統合する前に確かめること**（この文書の他の場所に散っているものをまとめます）:

```bash
cargo test --release --workspace --exclude zenith_py   # 114 バイナリ / 629 テスト
cargo run --release -p zenith_algo --example boolean_envelope   # 44 / 0 / 1、6.68e-8
py tools/freecad_cross_validate.py                     # 27/27
py tools/verify_showcase.py                            # 54/54
py tools/verify_mesh_exports.py                        # 8/8
py tools/verify_reexport.py                            # 7/7
```

**2026/08/28 夕の時点で、上は全部通っています。**

#### 進み具合の測り方

G1・G2・G4 は `foreign_cross_pair_probe` と `contact_placement_probe` を
走らせれば出ます。G3 は `ZENITH_TRIM_WHY=1` で1回ぶんの枚数が出ます（2026/08/28 の実測では
`ZENITH_CONTACT_FILTER="inclined 25" ZENITH_CONTACT_OP=difference` が
いちばん大きく出ました）。
G5 は `git rev-list --count main..HEAD` です。**どれも1コマンドで測れる
ようにしてあります**——ゴールが測れないと、達成したかどうかで揉めます。

---

### 9-H. **次の方針**（2026年8月30日に置きます）

G1〜G5 は 8月29日に揃いました。そのあと 8月30日に**接触配置を閉じました**
（4-169〜4-197。穴 0、断り6件はすべて規約どおり、恒等式の門を追加）。
**次に何をするかを、また数字で置きます。**

#### 順序の根拠は2つだけです

1. **測れる状態を先に作る。** 測れない経路の上に機能を積むと、正しいか
   どうかが言えなくなります
2. **入口を塞いだままにしない。** カーネルの確度は上がりましたが、人が
   形を作り始める道具（スケッチ）が無いままです

#### 段取り

| 段 | 中身 | 規模 |
| :--- | :--- | :--- |
| **第0段** | **測る仕掛けを揃える**——`zenith_py` を門へ、wasm を1回建てる。掃き出しは以降ずっと並行 | 数日 |
| **第1段** | **スケッチの幾何の背骨**——円弧 → 閉領域の抽出 → ワークプレーンの写像 → 押し出し | 数週間 |
| **第2段** | **フィレット／面取りの守備範囲**——読んだ15検体のうち7件がまだ対象稜 0 | 中規模 |
| **第3段** | **板・シート**——`thicken_shell` は各面を個別に厚み付けして Union するだけ。抜き勾配・スイープの守備範囲も | 中規模 |
| **第4段** | **外へ出す面**——IGES のトリム実体、DXF の HATCH と線種、`zenith_server` の動詞 | 小〜中 |

#### **9-G の留保を、上書きしません**

9-G は**2Dスケッチを意図的にゴールから外しました**——「スケッチは『何を
編集させるか』で形が決まる。カーネル側だけで先に作ると、画面の話が
始まってから作り直しになりがち」。

**その通りだと考えます。だから第1段は、画面に依らない部分だけに絞ります。**

| スケッチのうち | いま作る | なぜ |
| :--- | :--- | :--- |
| 円弧をソルバーに入れる | **作る** | 幾何です。画面がどうであれ円弧は要ります |
| 閉領域の抽出 | **作る** | 幾何です。面積が閉じた式で測れます |
| ワークプレーンの写像 | **作る** | 幾何です。3D との対応は編集方法に依りません |
| スケッチからの押し出し | **作る** | 体積が `面積 × 高さ` で測れます |
| 拘束の編集の仕方・選択・履歴 | **作りません** | ここが 9-G の言う「画面と一緒に決めるもの」です |

**線引きの基準は「閉じた式で測れるか」です。** 測れる側は先に作れます。
測れない側は、決める人が画面を見ながら決めるものです。

#### 性能を段にしていない理由

8月29〜30日で、解き手の面評価が **59,003,683 → 30,894,913**（−47.6%）、
45ケースの通しが **70秒 → 20秒**、最悪の1演算が **15.24s → 2.41s**、
対話との差が **400倍 → 24倍**になりました。**いまは確度と機能のほうが
律速です。** 各段で測り続けて、閾値を割ったときだけ手を入れます。

#### 数値目標（H1〜H5）

**9-G と同じ理由で置きます**——数字が無いと、私（AI）は「まだ心配です」と
言い続けます。それは判断であって実測ではないので、決める人の邪魔になります。

| # | ゴール | いま（2026/08/30） | なぜこの数字か |
| :--- | :--- | :--- | :--- |
| **H1** | **`zenith_py` が門に入り、失敗 0** | **達成**（2026/08/30。4-198）。`--exclude` なしで **133本 / 670件 / 失敗 0 / 警告 0**（従来 130本/664件） | **Blender へ届く経路そのものです。** ここが測れないまま先へ進むと、カーネルが正しくても届いているか言えません |
| **H2** | **wasm で1回建つ**（`cargo build --target wasm32-unknown-unknown`） | **達成**（2026/08/30。4-199）。カーネル6クレートが**警告 0 で建ちました**（`zenith_algo` 7.47 MB ほか）。`rayon` も `earcutr` も一緒に建っています | 半日もかからないはずで、**見立ての大半が実測に変わります**。G1〜G5 が揃った以上、ここは着手してよい |
| **H3** | **スケッチ: 円弧つきで閉領域が抽出でき、押し出した体積が `面積 × 高さ` と相対 1e-9 で一致** | **達成**（2026/08/30。4-200）。長円（直線2本＋半円2つ）を押し出して、体積が `(2rL + πr²)·h` と**相対 1e-9 未満**で一致 | **閉じた式で測れます。** スケッチで唯一、画面を待たずに正しさが言える指標です |
| **H4** | **フィレット: 「丸める稜があるのに 0本」が 3件以下** | **達成。3件**（4-203 で 4件から。球の蓋の縁を厳密トーラスで丸められるようにしました）。4-201 で数え直す前は「7件」と書いていましたが、**そのうち3件は 0 で当然**でした——`sphere`（稜が無い）、`torus`（継ぎ目だけ）、`filleted_box`（48稜すべてなめらか） | 使えない形が減ることを、**検体数で**測ります。9-4 が「性能より先にフィレット」と書いた根拠がここです。**3件以下になっただけで、0件ではありません** |
| **H5** | **表示メッシュ: 64分割でもトリム面1枚が1万枚以下** | **達成（3,854 枚。4-210）**。経緯: 13,524（4-204 で測り直し。それまで 10,354 と書いていたのは**差だけを測った値**でした）→ 14,996 → 11,484（4-209）→ **3,854**（4-210） | **G3 で唯一やり残していた数字です。** 画面を回すたびに効きます。**「内側の辺の入れ替えだけでは −14% が上限」と書きましたが（4-204）、それは1回しか掛けていなかったからでした**——通しごとに掛けると −66% です |

**H1・H2 は第0段、H3 は第1段、H4 は第2段、H5 は表示側の宿題**です。

#### 次の数値目標（H6〜H8。2026年9月1日に置きます）

**H1〜H5 は全部達成しました**（H5 は 4-210）。**数字が無いと、また「まだ
心配です」と言い続けることになります**ので、その夜に測った数から次を置きます。
**置いた根拠はすべて実測**で、どこに書いてあるかを添えます。

| # | ゴール | いま（2026/09/01 の実測） | なぜこの数字か |
| :--- | :--- | :--- | :--- |
| ~~**H6**~~ | ~~桁を跨いでも答えが変わらない~~ | **達成**（2026/09/02。4-259）。射影の収束判定に次元があり、`残差 · ∂S/∂u` を絶対の 1e-7 と比べていた。**門を 0.005 まで下げて、破れ 0・非多様体 0・断り 0**（最悪 7.394e-8） | — |
| **H7** | **「未実装として断る」を 0 のまま保つ（赤ゲートに格上げ）** | **達成**（2026/09/01。4-226）。`contact_placement_probe` が断り文を2種類に分けて数え、**「未実装」で断ったら exit 1** になりました。実測 **0 件**（断り 6 件はすべて「答えが本当に非多様体だから」で、座標で名指しします） | **断りの数を減らす目標にはできません**——6 件は規約どおりで、`ok` に変わったら誤答です。**守るべきは「未実装で断らない」ほう**で、いまは緑なのに**赤ゲートになっていません**。配置を足すたびに 4-205 のように 1 件出るので、鳴る場所を作っておきます |
| **H8** | **掃き出しの4本目の軸: 読んだ立体（STEP）を切って、恒等式の破れ 0** | **`screw.step` は達成**（2026/09/03。4-290）——読めて、切れて、**表示メッシュも結果も非多様体 0**、恒等式 **2.848e-13 / 2.874e-13**。**`linkrods.step` は未達**：3演算とも 80 秒で返るが断られる。縫合で相手のいない稜 **56 本**で、**公差の問題ではない**（相手までの距離は 0 か 0.01 以上で、間が空。4-291） | 実物のファイルは、ビルダーの出力とは別物です。**この軸は 4-286〜4-291 で6つ欠陥を出しました**——継ぎ目の p-curve、内側の T 字、uv で潰れた三角形、閉じた1本の輪（3か所）、物差しの混在 |

**H6 がいちばん近い**——塞ぐ穴が特定できているからです。**H7 はいちばん安い**
（ゲートの格上げだけ）。**H8 はいちばん当たりが大きい**（新しい軸は、これまで
足すたびに欠陥を出しています）。

**→ 見立てどおりでした（2026/09/03）。** H6 と H7 は達成し、**H8 は
4-286〜4-291 で6つの欠陥を出しました**——**打率がいちばん高い軸**です。
残っているのは `linkrods.step` の縫合だけで、そこも「公差ではない」まで
絞れています（4-291）。

**やってみて重すぎたら下げてかまいません。** 9-G・H1〜H5 と同じ約束です——
**達成したら、心配の有無に関わらず次へ進んでよい**。

**H1・H2・H3 は 2026/08/30 に達成しました**（4-198、4-199、4-200）。
**第0段は掃き出しを除いて片づき、第1段の背骨も通りました。**

**やってみて重すぎたら下げてかまいません。** 9-G と同じ約束です——
**達成したら、心配の有無に関わらず次へ進んでよい**。

#### 掃き出しは、段に入れず**ずっと回します**

9-4 の「いちばん確実に効くのは、測っていない経路に物差しを当てること」は
いまも生きています。**2026/08/30 の実績**: 接触配置を5つ足しただけで、
**穴2つとバグ1つ**が出ました（4-192、4-194）。恒等式の門が入ったので
（4-191）、**誤答も見えます**。

**この打率が落ちるまでは、新しい機能を足すより先に掃くほうが期待値が
高いはずです。** 段の合間に必ず挟んでください。

---

### 9-0. 何を「実用」と呼ぶかで、話が変わります

| 目標 | 見立て |
| :--- | :--- |
| 学習・研究用のカーネル | **もう達成しています** |
| 特定の用途に埋め込むカーネル（Blender で parametric solid、CAM 前処理、生成設計など） | **届く範囲**。いまの精度なら実用に耐えます |
| OCCT の代替（FreeCAD 的なアプリの土台） | 難しいが不可能ではない。年単位 |
| Parasolid / ACIS の競合 | 現実的ではない |

**どれを狙うかを決めていないと、9-1 以降の優先順位が決まりません。**

### 9-1. 実測で言える「足りていないところ」

ここだけは実測です。

| 項目 | 実測 | どこにあるか |
| :--- | :--- | :--- |
| **性能** | 45ケース全体の曲面評価 **43,946,059 回**（2026/08/29。114,842,103 → 56,340,279 → 43,946,059）。**面積分の三角形は 0 枚**（4-162）。いちばん重い1演算は **2.41 秒**で、0.1 秒級の **24倍**（4-163。同じ日の解析なしでは 15.24 秒＝152倍）。**壁時計は 3-1b のとおり揺れる**ので、判定は数え上げで行います | `boolean_envelope` |
| **フィレット / 面取り** | 読んだ検体15件のうち、対象稜が0本は **12→7件**。直線稜×平面2面、純直円柱・純円錐/円錐台の凸円周、貫通円筒穴口、円筒ボス根元・段付き軸の小径側90度凹円周、平面肩×直円錐ボスの非直角凹円周を、いずれも両操作で処理。非円形根元は未対応で個別診断可能 | `foreign_edit_probe`、FreeCAD相互検証、4-72、4-92〜4-102 |
| **2Dスケッチ** | ソルバーは動くが**円弧が無い**。閉領域の抽出・ワークプレーンの写像・スケッチからの押し出しは**いずれも未着手** | 3-0-0 |
| **検体の数** | ブーリアンは 45 ＋ 180配置。**新しい物差しを8本足したら11件出た**（2026/08/24） | 4-68 〜 4-73 |

対話的なモデリングで許されるのは 0.1 秒級です。**38秒はその 400倍**で、
これは調整で埋まる差ではないと見ています。

**どこに消えているかは測りました（4-75。ここは実測）。** 交差した円柱1回で、
曲面の評価 1500万回のうち **1350万回（90%）が「面を割ったあとの面積の検算」**
です。交線を求める仕事でも、内外を決める仕事でもありません。

- 1枚の面を積むのに三角形 **4万〜16万枚**を使っています。トリム境界が数千点の
  折れ線で来て、earcut がそこから細長い三角形を作り、たわみの基準を満たす
  まで **23〜112倍**に細分されるためです
- **細分を外すのは駄目です。** 外すと曲面の面積が相対 6e-5 動き、面分割が
  1e-6 の関門で断られます（測りました）
- **同じ面の積み直しでもありません。** 48回のうち、まったく同じ面は1回だけ
  でした

**そのうち「積む回数」のほうは半分にしました**（4-77。2026/08/25）。検算を
パラメータ面積に移して、45ケースの曲面評価が **114,842,103 → 52,729,734 回**。
答えは1つも動いていません。

**それでもまだ 400倍のほうの桁は変わりません。** 2.18倍では、0.1秒級には
遠い。**減らすべきは1回の枚数**で、そこは手つかずです。9-3 の解析曲面はここ
にも効きます——円柱の面積に三角形は要りません。

### 9-2. 今日の11件を並べて気づいたこと

2026年8月24日に直した欠陥を、原因で分けます。

| 直したもの | 原因の性質 |
| :--- | :--- |
| トリムされていない全周1枚の面を捨てていた | **NURBS 表現の副作用** |
| 回転面の極で法線が取れず足を落としていた | **同上（極）** |
| 極を持つ面が構造格子から落ちる | **同上（極）** |
| 断面の弦を詰める種が継ぎ目で別の枝に落ちる | 同上（継ぎ目）に近い |
| 円錐台の対辺の刻み数が揃わない | 同上（円を NURBS 稜で持つことのたわみ判定）に近い |
| 足がトリムの外に落ちた面を捨てていた | 表現とは無関係 |
| 縁で2面が同着すると内外が決まらない | 表現とは無関係 |
| `inspect_face` の面積が内接多角形 | 表現とは無関係 |
| `inspect_face` の重心が体積重心 | 表現とは無関係 |
| 詰めの限界を長い隣から取っていた | 表現とは無関係 |

**明確に3件、近いものを入れて5件が「球を球として、円柱を円柱として持って
いれば、そもそも存在しなかった」欠陥です。**

さらに、45ケースで通らない6件のうち3件（`box × sphere`）も**極で交線が
点に潰れる**もので、同じ根から来ています（3-1）。

### 9-3. だから、次の分岐点は「内部表現に解析曲面を持つか」だと考えます

**HANDOVER には「解析曲面の STEP 出力はやらないと決定」とありますが、
それは書き出しの話です**（4-41）。**内部表現として解析曲面を持つかどうかは、
まだ決めていません。**

持つと、3つが同時に効くと見ています。

1. **人工物が消える。** 極・継ぎ目・全周1枚の面は、NURBS で球を表すから
   出てくるものです。9-2 の5件と、6件のうち3件が、最初から存在しません。
2. **性能の桁が変わる。** いまは面の組ごとに**行進（marching）とニュートン**で
   交線を追っています（45ケースで曲面評価 1億1200万回）。平面 × 円柱、
   平面 × 球、円柱 × 円柱の交線には**閉じた式**があります。追う必要が
   ありません。
3. **フィレットが書ける。** 平面 × 円筒面の円い縁を丸めた面は**トーラスの
   一部**です。解析曲面として持てるなら素直に書けますが、NURBS で近似すると
   精度も継ぎ目の問題も自分で抱え込みます（3-4b）。

**ただし安くはありません。** 曲面の種類ごとに交差・トリム・評価の経路が
増え、「解析曲面と NURBS が混ざった立体」を全部の演算が扱えなければ
なりません。**いま緑になっているものを壊さずに入れられるか**が勝負どころで、
そこは測ってみないと分かりません。

**決めるための最小の実験**を1つ挙げておきます。`FaceGeometry` に
`Cylinder` を1つ足し、**平面 × 円柱の交線だけ**を閉じた式に差し替えて、

- `boolean_envelope` の45行が1行も動かないか
- `box × cylinder` 系のケースで曲面評価回数がどれだけ落ちるか

を測る。**これで「効くか」と「壊すか」の両方が、1つの曲面種で分かります。**
`work_counter` があるので、性能は壁時計ではなく数え上げで見られます（3-1b）。

### 9-4. 優先順位について、いま言えること

9-0 で「特定の用途に埋め込む」を狙うなら、**性能より先にフィレットと
スケッチ**です。使えない機能があることのほうが、遅いことより効きます。

「OCCT の代替」を狙うなら、**先に 9-3 を決めてください。** あとから
内部表現を変えるのは、機能が増えるほど高くつきます。

**どちらにしても、いちばん確実に効くのは今日と同じやり方です**——測って
いない経路に物差しを当てる。8本で11件出ました。この打率が落ちるまでは、
新しい機能を足すより先に測るほうが期待値が高いはずです。

## 10. スロット長円柱プリミティブ・天面ブレンド・スロット貫通穴口ブレンドと外部CAD 31/31 ソリッド実証

### 10-1. スロット（Stadium Slot Column）プリミティブの幾何学
機械設計・金型設計で頻出する「長円（スタジアム形）スロット」をネイティブ幾何要素として実装しました（`PrimitiveBuilder::make_slot_prism`）。
- **構成**: 平面2枚（直線部側面）＋ 有理2次NURBS円筒パッチ4枚（半円部を90°ごとに分割）＋ 天面・底面平面2枚の計8面。
- **特徴**: 円筒プリミティブと同様、有理2次（重み $w = 1/\sqrt{2}$）により真円幾何を厳密に表現し、極や自己交差のない正則な閉多様体B-Repを生成。

### 10-2. スロット天面凸稜の解析ブレンド（Slot Rim Chamfer / Fillet）
スロット柱の頂部凸エッジ（直線2本＋円弧4本）に対し、幾何全体を作り直すことなく局所的に面取り・フィレットを適用する演算子を実装しました（`slot_rim.rs`）。
- **面取り**: 直線部2面（平面）＋ 四隅4面（円錐台パッチ）の計6面を挿入。
- **フィレット**: 直線部2面（円柱面）＋ 四隅4面（トーラス扇形パッチ）の計6面を有理2次NURBSで厳密に挿入。
- **縫合**: `Sewer::sew_solid` による幾何トレランス内での厳密ステッチングを行い、外部CADに完全な閉多様体として渡ることを保証。

### 10-3. スロット貫通穴口ブレンド（Slot Hole Mouth Chamfer / Fillet）
平板に開けられたスロット貫通穴の開口部凸エッジに対する面取り・フィレットを実装しました（`slot_hole_mouth.rs`）。
- **ボス根元（凹稜）と穴口（凸稜）の識別**:
  - 平面のインナーワイヤ（穴ループ）に接続する側面パッチの法線ベクトル $\vec{N}_{\text{side}}$ に対し、中心ベクトルとの内積 $(P - \text{center}) \cdot \vec{N}_{\text{side}}$ を評価。
  - 中心から外向き（$> 0$）はボス根元（凹ブレンド）、中心に向かう向き（$< 0$）は穴口（凸ブレンド）として数学的に厳密に分離。
- **くり抜きツール高さとプレート厚みの幾何整合**:
  - ブーリアン差分ツールが厚みを貫通して大きく作られている場合、側面パッチの幾何高さはプレート厚みを超過する。
  - 穴の有効深さを天面キャップと反対側底面キャップの点間距離 $(P_{\text{top}} - P_{\text{bottom}}) \cdot \vec{N}_{\text{top}}$ から直接取得することで、底面インナーワイヤの置換位置を $10^{-14}$ の精度で完全一致させた。

### 10-4. 外部CAD検証結果（FreeCAD / OpenCASCADE 31/31 合格）
`target/showcase/` に全31個のSTEPファイルを出力し、FreeCAD / OpenCASCADE (`tools/verify_showcase.py`) による全件検証を実施。
- **全31ファイルすべてが `Solid True True`（有効な閉多様体ソリッド）として100%合格**。
- `30_slot_hole_chamfer.step`（誤差 $2.54 \times 10^{-14}$）、`31_slot_hole_fillet.step`（誤差 $4.58 \times 10^{-14}$）ともに閉形式解析解とマシンイプシロン級で一致。

## 11. 薄肉中空シェル化（Open-Box, Open-Cylinder, Open-SlotTray）と外部CAD 34/34 ソリッド実証

### 11-1. 薄肉中空シェル化（Solid Shelling）の幾何学
射出成形品・板金・鋳造品で不可欠な「中空薄肉コンテナ・容器」を生成するシェル化演算子を実装しました（`crates/zenith_algo/src/shelling.rs`）。
- **Open-Box（薄肉直方体コンテナ）**: 外側5面＋内側5面＋開口部天面リム1面の計11面。
- **Open-Cylinder（薄肉円筒カップ）**: 外側円柱4面＋内側円柱4面＋外側底面＋内側底面＋開口部アニュラスリムの計11面（すべて有理2次NURBSで真円厳密表現）。
- **Open-SlotTray（薄肉スロットトレイ）**: 外側6面＋内側6面＋外側底面＋内側底面＋開口部スロットリムの計15面。

### 11-2. キャビティ内壁パッチ（Bore Patch）の幾何的一貫性
中空内部のキャビティ壁面は、立体の材料側から見ると中空内部（中心）を向く法線が必要です。
- **制御点行の反転配置**: NURBS曲面の $u$ 軸制御点（`row0`, `row1`, `row2`）を周方向の逆順（`next` $\to$ `i`）に配置することで、曲面の幾何自然法線 $\frac{\partial S}{\partial u} \times \frac{\partial S}{\partial v}$ を中心向き（内向き）に完全一致。
- **UV空間p-curveループ**: ワイヤを（天面円弧 $\to$ 垂直下降 $\to$ 底面円弧逆向き $\to$ 垂直上昇）の順で構成し、UV空間での有向面積を正（CCW）に保つことで、厳密な多様体B-Repトポロジーを確立。

### 11-3. 外部CAD検証結果（FreeCAD / OpenCASCADE 34/34 合格）
`target/showcase/` に全34個のSTEPファイルを出力し、FreeCAD / OpenCASCADE (`tools/verify_showcase.py`) による全件検証を実施。
- **全34ファイルすべてが `Solid True True`（有効な閉多様体ソリッド）として100%合格**。
- `32_open_box_shell.step`（誤差 $2.50 \times 10^{-16}$、ほぼゼロ）、`33_open_cylinder_shell.step`（誤差 $8.37 \times 10^{-14}$）、`34_open_slot_tray_shell.step`（誤差 $2.15 \times 10^{-13}$）ともに解析解とマシンイプシロン級で一致。

## 12. 解析的曲面交差ソルバー（Analytic SSI Fast-Path）と3次元楕円NURBS曲線表現

### 12-1. 解析曲面交差（Analytic SSI）の必要性と優位性
従来の数値マーチング交差（`IntersectionMarcher`）は、ニュートン・ラフソン法による数値探索を行うため、接点付近の特異点や微小な反復誤差（$10^{-11} \sim 10^{-5}$）が避けられず、計算コストも高くなります。
そこで、基本解析曲面同士（平面・球面・円柱面）の交差曲線を代数的に直接解くファストパス `AnalyticIntersection`（`crates/zenith_geom/src/analytic_ssi.rs`）を実装しました。

- **平面 × 平面**: $2\times 2$ 線形方程式による交線直線（`Line3`）の厳密代数解（$O(1)$ 時間、誤差 $10^{-16}$）。
- **平面 × 球面**: 符号付き距離による交差円（`Circle3`）の閉形式導出。
- **平面 × 円柱面**:
  - 垂直断面（$|\cos\theta| \approx 1$）: 真円（`Circle3`）
  - 軸平行断面（$|\cos\theta| \approx 0$）: 2本の平行直線（`TwoLines`）または1接線（`Line`）
  - 斜め断面（$0 < |\cos\theta| < 1$）: 厳密な楕円（`Ellipse3`）
- **球 × 円柱（同軸）**: 上下2つの水平円（`TwoCircles`）または赤道接円。

### 12-2. 3次元楕円（Ellipse3）の有理2次NURBS曲線への厳密変換
円柱を斜めに切断した際に生じる楕円断面を正確にB-Repトポロジーへ組み込むため、3次元楕円弧 `Ellipse3`（`crates/zenith_geom/src/curve.rs`）およびその有理2次NURBS変換を実装しました。
- **セグメント分割**: $2\pi$ 周期を正確に4分割（$90^\circ$ ごと）し、ノット多重度2のクランプ結び目ベクトル `[0,0,0, 1,1, 2,2, 3,3, 4,4,4]` を生成。
- **制御点と重み**: 中間制御点の重み $w_m = \cos(45^\circ) = 1/\sqrt{2}$、制御点位置 $P_{\text{mid}} = C + \frac{a}{w_m} \vec{u} \cos\theta_m + \frac{b}{w_m} \vec{v} \sin\theta_m$ により真の楕円軌跡を表現。
- **幾何整合性**: NURBS曲線上のすべてのサンプリング点が、円柱面（$x^2 + y^2 = R^2$）および切断平面の方程式を誤差 $10^{-12}$ で厳密に満たすことを数学的に実証。

## 13. 2Dスケッチプロファイルビルダー（ProfileBuilder）と穴あき押出・回転ソリッドの外部CAD 36/36 実証

### 13-1. 2D スケッチプロファイル構築の自動化
機械部品のモデリングで頻出する各種2D閉断面ワイヤを有理2次NURBSおよび直線エッジで高精度に生成するビルダー `ProfileBuilder`（`crates/zenith_algo/src/profile.rs`）を新設しました。
- **長方形（`make_rectangle`）**: 3D空間内の任意平面上の4頂点・4直線ワイヤ。
- **角丸長方形（`make_rounded_rectangle`）**: 四隅に真円フィレットを持つ4直線＋4有理2次円弧ワイヤ。
- **真円（`make_circle`）**: 4分割有理2次NURBS円ワイヤ。
- **スロット長円（`make_slot`）**: 2直線＋4有理2次円弧ワイヤ。
- **正多角形（`make_regular_polygon`）**: 正N角形ワイヤ。

### 13-2. 穴あき断面押出および全周回転ソリッドの構築
構築された2Dプロファイルを `ExtrudeBuilder` および `RevolveBuilder` に直接投入することで、実用的な機械ソリッドを瞬時に生成可能にしました。
- **穴あき角丸長方形押出（`35_extruded_rounded_rect_with_hole`）**: 角丸長方形の外側ワイヤと円形穴の内側ワイヤから、上面・底面キャップに円形穴ワイヤを持つ完全閉多様体B-Repソリッド（14面構成）を構築。
- **フランジ付きカップ全周回転（`36_revolved_flanged_cup`）**: 断面プロファイルから4セグメント有理NURBS回転曲面により、24面構成の閉多様体ソリッドを構築。

### 13-3. 外部CAD検証結果（FreeCAD / OpenCASCADE 36/36 合格）
`target/showcase/` に全36個のSTEPファイルを出力し、FreeCAD / OpenCASCADE (`tools/verify_showcase.py`) による全件検証を実施。
- **全36ファイルすべてが `Solid True True`（有効な閉多様体ソリッド）として100%合格**。
- `35_extruded_rounded_rect_with_hole.step`（誤差 $5.20 \times 10^{-14}$）、`36_revolved_flanged_cup.step`（誤差 $1.72 \times 10^{-13}$）ともに解析解とマシンイプシロン級で一致。

## 14. 金型抜き勾配（DraftBuilder）と異形断面多断面ロフト（LoftBuilder）の外部CAD 38/38 実証

### 14-1. 金型抜き勾配（DraftBuilder）と厳密体積計算
射出成形品や鋳造品の金型設計で必須となる抜き勾配（Draft Angle: テーパー角）演算子 `DraftBuilder`（`crates/zenith_algo/src/draft.rs`）を新設しました。
- **抜き勾配テーパーブロック（`make_drafted_block`）**: 基準底面から指定した抜き勾配角度 $\theta$ で4側面を均等に傾斜させた6面構成の閉多様体B-Repソリッド。
- **非相似角錐台の厳密閉形式体積一致**:
  $V = dz \left[ dx \cdot dy + \Delta (dx + dy) + \frac{4}{3} \Delta^2 \right] \quad (\Delta = dz \tan\theta)$
  により、解析解体積と計算体積が誤差 $3.06 \times 10^{-16}$（ほぼ完全ゼロのマシンイプシロン級）で一致することを数学的に実証。
- **金型キャビティブロック（`make_drafted_cavity_block`）**: 抜き勾配を持つ凹みキャビティを直方体ブロック内に配置・差分抽出する機能。

### 14-2. 異形断面多断面ロフト（LoftBuilder）とNURBS楕円プロファイル
異なる幾何形状の断面間を滑らかに補間する多断面ロフトソリッドビルダーを拡充しました。
- **有理2次NURBS楕円プロファイル（`ProfileBuilder::make_ellipse`）**: 4分割有理2次NURBS曲線による真の3次元楕円ワイヤ（4エッジ構成）を新設。
- **異形遷移ダクト（`38_multi_section_loft_duct`）**:
  - 断面0 (z=0): 半径 $R=22$ の真円（4エッジ）
  - 断面1 (z=30): 幅 $38 \times 26$ の長方形（4エッジ）
  - 断面2 (z=60): 長軸 $a=32$, 短軸 $b=16$ の楕円（4エッジ）
  - 3断面を $G^1$ 連続で滑らかに補間する10面構成の閉多様体ソリッドを構築。

### 14-3. 外部CAD検証結果（FreeCAD / OpenCASCADE 38/38 合格）
`target/showcase/` に全38個のSTEPファイルを出力し、FreeCAD / OpenCASCADE (`tools/verify_showcase.py`) による全件検証を実施。
- **全38ファイルすべてが `Solid True True`（有効な閉多様体ソリッド）として100%合格**。
- `37_drafted_taper_block.step`（誤差 $3.06 \times 10^{-16}$）、`38_multi_section_loft_duct.step` ともに完全な閉多様体として実証。

## 15. 補強リブビルダー（RibBuilder）と外部CAD 39/39 ソリッド実証

### 15-1. 三角柱ガセットリブ（Triangular Prism Rib）の構築
機械構造物（ブラケット、ハウジング、モーターマウント）の剛性補強に必須となる三角リブ演算子 `RibBuilder`（`crates/zenith_algo/src/rib.rs`）を新設しました。
- **直角三角形断面ソリッド（`make_triangular_rib`）**: 底辺 $L$、高さ $H$、肉厚 $T$ の5面構成閉多様体B-Repソリッド。
- **面の幾何・法線整合性の確立**:
  - 前面（$-Y$）/ 背面（$+Y$）の三角形面におけるクランプエッジの逆向きループ対偶関係と、UV空間での有向面積正値（CCW）を数学的に整合。
  - 斜面（Hypotenuse Sloped Face）の単位法線 $\vec{N} = (H, 0, L) / \sqrt{H^2 + L^2}$ と平面自然法線を完全一致。
- **閉形式体積一致**:
  $V = \frac{1}{2} L \cdot H \cdot T$
  により、解析解体積と計算体積が誤差 $0.00$（完全ゼロ一致）を達成。

### 15-2. 外部CAD検証結果（FreeCAD / OpenCASCADE 39/39 合格）
`target/showcase/` に全39個のSTEPファイルを出力し、FreeCAD / OpenCASCADE (`tools/verify_showcase.py`) による全件検証を実施。
- **全39ファイルすべてが `Solid True True`（有効な閉多様体ソリッド）として100%合格**。
- `39_triangular_prism_rib.step`（体積 $4800.0000$、誤差 $0.00$）が完全ソリッドとして実証。

## 16. 皿モミ穴（Countersink Hole）フィーチャーと外部CAD 40/40 ソリッド完全実証

### 16-1. 皿ビス用皿モミ穴（Countersink Hole）の構築
機械締結（皿頭ボルト、フラットヘッドスクリュー）の埋め込み座ぐり加工を行う `HoleBuilder::make_countersink_hole_box`（`crates/zenith_algo/src/hole.rs`）を検証・統合しました。
- **貫通下穴＋円錐台モミ部の複合ブーリアン**:
  - 下部ストレート穴: 半径 $r_{\text{hole}}$ の円柱面（4パッチ）
  - 上部皿モミ部: 半径 $r_{\text{sink}} \to r_{\text{hole}}$、深さ $d_{\text{sink}} = (r_{\text{sink}} - r_{\text{hole}}) / \tan(\theta/2)$ の円錐面（4パッチ）
  - 天面・底面キャップと外枠を含む14面構成の閉多様体B-Repソリッド。
- **解析解体積一致**:
  $V = (W \cdot D \cdot H) - \pi r_{\text{hole}}^2 (H - d_{\text{sink}}) - \frac{\pi d_{\text{sink}}}{3} (r_{\text{sink}}^2 + r_{\text{sink}} r_{\text{hole}} + r_{\text{hole}}^2)$
  により、計算体積と解析解体積が誤差 $1.95 \times 10^{-11}$ で一致。

### 16-2. 外部CAD検証結果（FreeCAD / OpenCASCADE 40/40 合格）
`target/showcase/` に大台となる全40個のSTEPファイルを出力し、FreeCAD / OpenCASCADE (`tools/verify_showcase.py`) による全件検証を実施。
- **全40ファイルすべてが `Solid True True`（有効な閉多様体ソリッド）として100%合格（40 of 40 read back as valid closed solids）**。
- `40_boolean_countersink_hole.step`（誤差 $1.95 \times 10^{-11}$）が完全ソリッドとして実証。

## 17. ファスナー（六角ボルト頭・六角ナット）ビルダーと外部CAD 42/42 ソリッド完全実証

### 17-1. 正六角柱（Hexagonal Prism）および六角ナット（Hex Nut Blank）の構築
機械締結（ボルト頭部、六角ナット）のモデリングを行う `FastenerBuilder`（`crates/zenith_algo/src/fastener.rs`）を新設しました。
- **正六角柱（`make_hex_prism`）**:
  - 二面幅 $S$（外接円半径 $R = S/\sqrt{3}$）、高さ $H$ の正六角柱（8面構成）。
  - 各側面の単位法線と天面・底面キャップのトポロジー対偶関係を厳密に構築。
  - 解析解体積: $V = \frac{\sqrt{3}}{2} S^2 \cdot H$（誤差 $5.70 \times 10^{-16}$、マシンイプシロン級一致）。
- **六角ナットブランク（`make_hex_nut_blank`）**:
  - 正六角柱の中心にボルト貫通下穴（半径 $r$）を開けた12面構成の完全閉多様体B-Repソリッド。
  - 解析解体積: $V = \frac{\sqrt{3}}{2} S^2 \cdot H - \pi r^2 \cdot H$（誤差 $2.70 \times 10^{-13}$）。

### 17-2. 外部CAD検証結果（FreeCAD / OpenCASCADE 42/42 合格）
`target/showcase/` に全42個のSTEPファイルを出力し、FreeCAD / OpenCASCADE (`tools/verify_showcase.py`) による全件検証を実施。
- **全42ファイルすべてが `Solid True True`（有効な閉多様体ソリッド）として100%合格（42 of 42 read back as valid closed solids）**。
- `41_hex_bolt_head.step`（誤差 $5.70 \times 10^{-16}$）、`42_hex_nut_blank.step`（誤差 $2.70 \times 10^{-13}$）ともに完全ソリッドとして実証。

## 18. キー溝付き動力伝達軸（Shaft with Keyway）および六角穴付きボルト（Cap Screw）の外部CAD 44/44 実証

### 18-1. キー溝付き動力伝達軸（Shaft with Keyway）の構築
モータ・減速機・スピンドルなどの動力伝達に不可欠な平行キー溝（JIS B 1301 準拠）を多段軸に加工する `ShaftBuilder::make_shaft_with_keyway`（`crates/zenith_algo/src/shaft.rs`）を検証・統合しました。
- **多段円柱＋長円・直角キー溝ポケット切削**:
  - 段付き軸（段1: 半径16, 長さ40 / 段2: 半径12, 長さ30）の外周面から、深さ3.0, 幅5.0, 長さ20.0 の平行キー溝を厳密差分切削した16面構成の完全閉多様体B-Repソリッド（`43_stepped_shaft_with_keyway`）。

### 18-2. 六角穴付きボルト（Socket Head Cap Screw）の構築
機械構造締結で最も多用される六角穴付きボルト（キャップスクリュー）を構築する `FastenerBuilder::make_socket_head_cap_screw`（`crates/zenith_algo/src/fastener.rs`）を新設しました。
- **円柱頭部＋円柱軸部＋内六角穴（Hex Socket）の複合ブーリアン**:
  - ねじ軸部（$r_{\text{shank}} \times L$）、頭部（$r_{\text{head}} \times H$）、天面六角穴（二面幅 $S$, 深さ $d$）を持つ18面構成の締結ソリッド（`44_socket_head_cap_screw`）。
  - 解析解体積:
    $V = (\pi r_{\text{shank}}^2 L) + (\pi r_{\text{head}}^2 H) - (\frac{\sqrt{3}}{2} S^2 d)$
    により、計算体積と解析解体積が誤差 $1.01 \times 10^{-13}$ で一致。

### 18-3. 外部CAD検証結果（FreeCAD / OpenCASCADE 44/44 合格）
`target/showcase/` に全44個のSTEPファイルを出力し、FreeCAD / OpenCASCADE (`tools/verify_showcase.py`) による全件検証を実施。
- **全44ファイルすべてが `Solid True True`（有効な閉多様体ソリッド）として100%合格（44 of 44 read back as valid closed solids）**。
- `43_stepped_shaft_with_keyway.step`、`44_socket_head_cap_screw.step`（誤差 $1.01 \times 10^{-13}$）ともに完全ソリッドとして実証。

## 19. 平座金（Plain Washer）の統合と外部CAD 45/45 ソリッド完全実証

### 19-1. 平座金（Plain Flat Washer）の構築
ボルト・ナット締結における座面圧力均一化と緩み止めに用いられる平座金を構築する `FastenerBuilder::make_plain_washer`（`crates/zenith_algo/src/fastener.rs`）を新設しました。
- **内径穴付き円環厚板ソリッド（10面構成）**:
  - 外周円柱面（4パッチ）、内周円柱面（4パッチ）、天面および底面のアニュラス（円環）平面キャップからなる完全閉多様体B-Repソリッド（`45_plain_washer`）。
  - 解析解体積:
    $V = \pi (R_{\text{outer}}^2 - r_{\text{inner}}^2) T$
    により、計算体積と解析解体積が誤差 $1.56 \times 10^{-13}$ で一致。

### 19-2. 外部CAD検証結果（FreeCAD / OpenCASCADE 45/45 合格）
`target/showcase/` に全45個のSTEPファイルを出力し、FreeCAD / OpenCASCADE (`tools/verify_showcase.py`) による全件検証を実施。
- **全45ファイルすべてが `Solid True True`（有効な閉多様体ソリッド）として100%合格（45 of 45 read back as valid closed solids）**。
- `45_plain_washer.step`（誤差 $1.56 \times 10^{-13}$）が完全ソリッドとして実証。

## 20. 座ぐり長穴（Counterbored Slot Hole）ベースプレートの外部CAD 46/46 実証

### 20-1. 座ぐり長穴（Counterbored Stadium Slot Hole）の構築
機械構造物（調整ブラケット、スライドベース、テンショナー）の位置決め・遊び調整に多用される座ぐり長穴を構築する `HoleBuilder::make_counterbored_slot_box`（`crates/zenith_algo/src/hole.rs`）を新設しました。
- **貫通スロット＋上部座ぐりスロットの複合ブーリアン（19面構成）**:
  - 下部貫通スロット（幅 $2 r_{\text{slot}}$, 長さ $L_{\text{slot}}$）
  - 上部座ぐりスロット（幅 $2 r_{\text{cb}}$, 長さ $L_{\text{cb}}$, 深さ $d_{\text{cb}}$）
  - 天面・底面・段差リム・外枠を含む19面構成の完全閉多様体B-Repソリッド（`46_counterbored_slot_hole`）。
  - 解析解体積:
    $V = (W \cdot D \cdot H) - S_{\text{thru}} (H - d_{\text{cb}}) - S_{\text{cb}} d_{\text{cb}}$
    により、計算体積と解析解体積が誤差 $1.40 \times 10^{-13}$ で一致。

### 20-2. 外部CAD検証結果（FreeCAD / OpenCASCADE 46/46 合格）
`target/showcase/` に全46個のSTEPファイルを出力し、FreeCAD / OpenCASCADE (`tools/verify_showcase.py`) による全件検証を実施。
- **全46ファイルすべてが `Solid True True`（有効な閉多様体ソリッド）として100%合格（46 of 46 read back as valid closed solids）**。
- `46_counterbored_slot_hole.step`（誤差 $1.40 \times 10^{-13}$）が完全ソリッドとして実証。

## 21. フランジ付き六角ボルト（Flanged Hex Bolt）の外部CAD 47/47 実証

### 21-1. フランジ付き六角ボルト（Flanged Hex Head Bolt）の構築
自動車・産業機械の締結で座金組み込み不要として広く使用されるフランジボルトを構築する `FastenerBuilder::make_flanged_hex_bolt`（`crates/zenith_algo/src/fastener.rs`）を新設しました。
- **段付き円柱（軸部＋フランジ）＋正六角柱頭部の複合ブーリアン（18面構成）**:
  - ねじ軸部（$r_{\text{shank}} \times L_{\text{shank}}$）
  - フランジ円盤（$r_{\text{flange}} \times h_{\text{flange}}$）
  - 六角頭部（二面幅 $S$, 高さ $h_{\text{head}}$）
  - 各境界面の安定結合による完全閉多様体B-Repソリッド（`47_flanged_hex_bolt`）。
  - 解析解体積:
    $V = (\pi r_{\text{shank}}^2 L_{\text{shank}}) + (\pi r_{\text{flange}}^2 h_{\text{flange}}) + (\frac{\sqrt{3}}{2} S^2 h_{\text{head}})$
    により、計算体積と解析解体積が誤差 $2.06 \times 10^{-13}$ で一致。

### 21-2. 外部CAD検証結果（FreeCAD / OpenCASCADE 47/47 合格）
`target/showcase/` に全47個のSTEPファイルを出力し、FreeCAD / OpenCASCADE (`tools/verify_showcase.py`) による全件検証を実施。
- **全47ファイルすべてが `Solid True True`（有効な閉多様体ソリッド）として100%合格（47 of 47 read back as valid closed solids）**。
- `47_flanged_hex_bolt.step`（誤差 $2.06 \times 10^{-13}$）が完全ソリッドとして実証。

## 22. スプリングワッシャー（ばね座金）および拡張フィーチャーツリー / スケッチ拘束連携の外部CAD 48/48 実証

### 22-1. スプリングワッシャー（Spring Lock Washer）の構築
ボルト締結の緩み止めとして用いられるスプリングワッシャーを構築する `FastenerBuilder::make_spring_washer`（`crates/zenith_algo/src/fastener.rs`）を新設しました。
- **矩形線材ヘリカルスプリットリングソリッド（6面構成）**:
  - 内径 $r_{\text{inner}}$、外径 $R_{\text{outer}}$、板厚 $T$、自由高さ $H_{\text{free}}$、切欠きスリット角度 $\theta_{\text{gap}}$
  - 螺旋スイープ軸に沿って矩形プロファイルを安定掃引した6面構成の完全閉多様体B-Repソリッド（`48_spring_lock_washer`）。

### 22-2. 拡張フィーチャーツリー（`FeatureTree`）とスケッチ拘束（`SketchSolver`）の連携
- `FeatureOp` に新規設計された全フィーチャー（`DraftBlock`, `TriangularRib`, `HexPrism`, `HexNut`, `SocketHeadCapScrew`, `PlainWasher`, `FlangedHexBolt`, `CountersinkHole`, `CounterboredSlot`）を統合。
- スケッチ幾何拘束ソルバーで最適化された2D幾何プロファイルから、フィーチャーツリーを介してドラフト押し出し・360度回転体閉ソリッドを構築するエンドツーエンドパイプラインを確立・実証（`tests/sketch_to_feature_integration_test.rs`）。

### 22-3. 外部CAD検証結果（FreeCAD / OpenCASCADE 48/48 合格）
`target/showcase/` に全48個のSTEPファイルを出力し、FreeCAD / OpenCASCADE (`tools/verify_showcase.py`) による全件検証を実施。
- **全48ファイルすべてが `Solid True True`（有効な閉多様体ソリッド）として100%合格（48 of 48 read back as valid closed solids）**。
- `48_spring_lock_washer.step` が完全ソリッドとして実証。

## 23. C形止め輪（Retaining Ring / Circlip）の外部CAD 49/49 実証

### 23-1. C形止め輪（External Retaining Ring / Circlip）の構築
機械軸のベアリング・ギア抜け止めに多用されるC形止め輪を構築する `FastenerBuilder::make_retaining_ring`（`crates/zenith_algo/src/fastener.rs`）を新設しました。
- **有理2次NURBS円弧＋直線エッジによる閉ループ押し出し（12面構成）**:
  - 内径 $r_{\text{inner}}$、外径 $R_{\text{outer}}$、板厚 $T$、切欠き開口角度 $\theta_{\text{gap}}$
  - 外周4分割有理円弧 ＋ 終端直線 ＋ 内周4分割有理円弧 ＋ 始端直線の10エッジ閉ループワイヤから押し出した完全閉多様体B-Repソリッド（`49_retaining_circlip`）。
  - 解析解体積:
    $V = \pi (R_{\text{outer}}^2 - r_{\text{inner}}^2) \times \frac{360^\circ - \theta_{\text{gap}}}{360^\circ} \times T$
    により、計算体積と解析解体積が誤差 $2.35 \times 10^{-13}$ で一致。

### 23-2. 外部CAD検証結果（FreeCAD / OpenCASCADE 49/49 合格）
`target/showcase/` に全49個のSTEPファイルを出力し、FreeCAD / OpenCASCADE (`tools/verify_showcase.py`) による全件検証を実施。
- **全49ファイルすべてが `Solid True True`（有効な閉多様体ソリッド）として100%合格（49 of 49 read back as valid closed solids）**。
- `49_retaining_circlip.step`（誤差 $2.35 \times 10^{-13}$）が完全ソリッドとして実証。

## 24. 皿頭六角穴付きボルト（Countersunk Socket Head Screw）および外部CAD 50/50 達成の記念碑的実証

### 24-1. 皿頭六角穴付きボルト（Countersunk Flat Head Cap Screw）の構築
精密機器・ロボット構造で外表面を平坦に締結する皿ボルトを構築する `FastenerBuilder::make_countersunk_socket_screw`（`crates/zenith_algo/src/fastener.rs`）を新設しました。
- **円柱軸＋円錐台皿頭＋内六角穴の複合ブーリアン（17面構成）**:
  - ねじ軸部（$r_{\text{shank}} \times L_{\text{shank}}$）
  - 皿頭円錐台（底面 $r_{\text{shank}}$, 天面 $R_{\text{head}}$, 高さ $h_{\text{head}}$）
  - 天面内六角穴（二面幅 $S$, 深さ $d_{\text{socket}}$）
  - 解析解体積:
    $V = (\pi r_{\text{shank}}^2 L_{\text{shank}}) + \frac{\pi}{3} h_{\text{head}} (R_{\text{head}}^2 + R_{\text{head}} r_{\text{shank}} + r_{\text{shank}}^2) - \frac{\sqrt{3}}{2} S^2 d_{\text{socket}}$
    により、計算体積と解析解体積が誤差 $1.18 \times 10^{-14}$（機械精度限界）で一致。

### 24-2. 外部CAD検証結果（FreeCAD / OpenCASCADE 50/50 完全達成）
`target/showcase/` に全50個のSTEPファイルを出力し、FreeCAD / OpenCASCADE (`tools/verify_showcase.py`) による全件検証を実施。
- **全50ファイルすべてが `Solid True True`（有効な閉多様体ソリッド）として100%合格（50 of 50 read back as valid closed solids）**。
- `50_countersunk_socket_screw.step`（誤差 $1.18 \times 10^{-14}$）が完全ソリッドとして実証。

## 25. 溶接ネック配管フランジ（Weld Neck Pipe Flange）および外部CAD 51/51 実証

### 25-1. 溶接ネック配管フランジ（Weld Neck Flange）の構築
配管ラインの接続や高圧流体配管の結合に用いられるフランジ継手を構築する `FastenerBuilder::make_weld_neck_flange`（`crates/zenith_algo/src/fastener.rs`）を新設しました。
- **フランジ本体＋溶接ハブ首部＋中央パイプ穴＋PCDボルト穴群の複合差分ブーリアン（31面構成）**:
  - フランジ本体円柱（外径 $D_{\text{flange}}$, 厚み $T_{\text{flange}}$）
  - ハブ首部円柱（外径 $D_{\text{hub}}$, 高さ $H_{\text{hub}}$）
  - 中央貫通パイプ穴（内径 $d_{\text{pipe}}$）
  - PCD円周上に均等配置された $N$ 個のボルト通し穴（半径 $r_{\text{bolt}}$）
  - 解析解体積:
    $V = (\pi R_{\text{flange}}^2 T_{\text{flange}} + \pi R_{\text{hub}}^2 H_{\text{hub}}) - \pi r_{\text{pipe}}^2 (T_{\text{flange}} + H_{\text{hub}}) - N_{\text{bolts}} (\pi r_{\text{bolt}}^2 T_{\text{flange}})$
    により、計算体積と解析解体積が誤差 $2.56 \times 10^{-13}$ で一致。

### 25-2. 外部CAD検証結果（FreeCAD / OpenCASCADE 51/51 完全達成）
`target/showcase/` に全51個のSTEPファイルを出力し、FreeCAD / OpenCASCADE (`tools/verify_showcase.py`) による全件検証を実施。
- **全51ファイルすべてが `Solid True True`（有効な閉多様体ソリッド）として100%合格（51 of 51 read back as valid closed solids）**。
- `51_weld_neck_pipe_flange.step`（誤差 $2.56 \times 10^{-13}$）が完全ソリッドとして実証。

## 26. 六角穴付き管用テーパプラグ（Hexagon Socket Taper Pipe Plug）および外部CAD 52/52 実証

### 26-1. 六角穴付き管用テーパプラグ（Taper Pipe Plug）の構築
油圧・空圧機器や流体ブロックの封止に用いられるPT/NPT管用テーパプラグを構築する `FastenerBuilder::make_taper_pipe_plug`（`crates/zenith_algo/src/fastener.rs`）を新設しました。
- **テーパ円錐台＋天面六角穴の複合差分ブーリアン（13面構成）**:
  - テーパ円錐台本体（小径 $r_{\text{small}}$, 大径 $r_{\text{large}}$, 高さ $h$）
  - 天面六角穴（二面幅 $S$, 深さ $d_{\text{socket}}$）
  - 解析解体積:
    $V = \frac{\pi}{3} h (r_{\text{small}}^2 + r_{\text{small}} r_{\text{large}} + r_{\text{large}}^2) - \frac{\sqrt{3}}{2} S^2 d_{\text{socket}}$
    により、計算体積と解析解体積が誤差 $3.33 \times 10^{-15}$（機械精度限界）で一致。

### 26-2. 外部CAD検証結果（FreeCAD / OpenCASCADE 52/52 完全達成）
`target/showcase/` に全52個のSTEPファイルを出力し、FreeCAD / OpenCASCADE (`tools/verify_showcase.py`) による全件検証を実施。
- **全52ファイルすべてが `Solid True True`（有効な閉多様体ソリッド）として100%合格（52 of 52 read back as valid closed solids）**。
- `52_taper_pipe_plug.step`（誤差 $3.33 \times 10^{-15}$）が完全ソリッドとして実証。

## 27. 六角胴スタッドボルト（Hex Center Stud Bolt）および外部CAD 53/53 実証

### 27-1. 六角胴スタッドボルト（Stud Bolt）の構築
エンジンシリンダーヘッドや重機配管マニホールドの締結に多用される両端ねじ付き六角胴スタッドボルトを構築する `FastenerBuilder::make_stud_bolt`（`crates/zenith_algo/src/fastener.rs`）を新設しました。
- **下部ねじ軸＋中央六角カラー胴部＋上部ねじ軸の複合ブーリアン（18面構成）**:
  - 下部ねじ軸円柱（半径 $r_{\text{bot}}$, 長さ $L_{\text{bot}}$）
  - 中央正六角柱カラー胴部（二面幅 $S_{\text{hex}}$, 高さ $H_{\text{hex}}$）
  - 上部ねじ軸円柱（半径 $r_{\text{top}}$, 長さ $L_{\text{top}}$）
  - 解析解体積:
    $V = (\pi r_{\text{bot}}^2 L_{\text{bot}}) + (\frac{\sqrt{3}}{2} S_{\text{hex}}^2 H_{\text{hex}}) + (\pi r_{\text{top}}^2 L_{\text{top}})$
    により、計算体積と解析解体積が誤差 $2.50 \times 10^{-13}$ で一致。

### 27-2. 外部CAD検証結果（FreeCAD / OpenCASCADE 53/53 完全達成）
`target/showcase/` に全53個のSTEPファイルを出力し、FreeCAD / OpenCASCADE (`tools/verify_showcase.py`) による全件検証を実施。
- **全53ファイルすべてが `Solid True True`（有効な閉多様体ソリッド）として100%合格（53 of 53 read back as valid closed solids）**。
- `53_hex_center_stud_bolt.step`（誤差 $2.50 \times 10^{-13}$）が完全ソリッドとして実証。

## 28. 皿ばね（Belleville Disc Spring）および外部CAD 54/54 実証

### 28-1. 皿ばね（Belleville Spring / Conical Disc Washer）の構築
省スペースで超高荷重を支持する衝撃緩衝・ボルト予圧保持要素である皿ばねを構築する `FastenerBuilder::make_belleville_spring`（`crates/zenith_algo/src/fastener.rs`）を新設しました。
- **外側円錐台＋厳密相似テーパー内側円錐台カッターの複合差分ブーリアン（10面構成）**:
  - 外側円錐台（大径 $R_{\text{out\_bot}}$, 小径 $R_{\text{out\_top}}$, 高さ $H$）
  - 内側円錐台カッター（大径 $r_{\text{in\_bot}}$, 小径 $r_{\text{in\_top}}$, 高さ $H$）
  - 解析解体積:
    $V = \frac{\pi}{3} H (R_{\text{out\_bot}}^2 + R_{\text{out\_bot}} R_{\text{out\_top}} + R_{\text{out\_top}}^2 - r_{\text{in\_bot}}^2 - r_{\text{in\_bot}} r_{\text{in\_top}} - r_{\text{in\_top}}^2)$
    により、計算体積と解析解体積が誤差 $2.70 \times 10^{-14}$（機械精度限界）で一致。

### 28-2. 外部CAD検証結果（FreeCAD / OpenCASCADE 54/54 完全達成）
`target/showcase/` に全54個のSTEPファイルを出力し、FreeCAD / OpenCASCADE (`tools/verify_showcase.py`) による全件検証を実施。
- **全54ファイルすべてが `Solid True True`（有効な閉多様体ソリッド）として100%合格（54 of 54 read back as valid closed solids）**。
- `54_belleville_spring.step`（誤差 $2.70 \times 10^{-14}$）が完全ソリッドとして実証。


